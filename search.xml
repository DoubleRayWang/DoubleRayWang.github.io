<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title></title>
    <url>/2021/06/24/article-blog-coding/</url>
    <content><![CDATA[<hr>
<p>layout: post<br>title: “与时俱进（折腾）”<br>date: 2017-04-11 15:25:15 +0800<br>categories: 实践之路<br>tags: Web</p>
<hr>
<p>　　2017年3月21日收到一条来自多说的消息，多说将要关闭服务继而成为历史，首先感谢多说团队这些年来提供的免费服务！希望你们转型后能更加美好～</p>
<p>　　那么，我们的评论该迁移至何处呢？这个问题，对有服务器的同学来说其实也并不是问题，在服务器上部署一个评论系统不是一个难事，也可以直接安装诸如 wordpress 之类的产品，自带了完善了评论功能，并且还支持导入多说的数据。其实，受伤的是那些使用诸如 hexo/jekyll 构建的静态博客，没有数据库能力的，首先想到的有两个方案：</p>
<ul>
<li><a href="https://disqus.com/" target="_blank" rel="noopener">Disqus</a>：这款国外最流行的社会化评论组件，体验优良，而且基于评论做了一个交流社区，形成了良好的生态；</li>
<li><a href="https://commentit.io/" target="_blank" rel="noopener">commentit.io</a>：可以将评论直接作为 commit，用 JS 脚本提交到 Github，保存进代码库。</li>
</ul>
<p>　　但是呢，这两个解决方案都有被墙的可能，左思右想而且还都亲自尝试了一下，在国内体验确实比较糟糕，那么只能暂时另辟蹊径了！<a id="more"></a></p>
<h2 id="多说到跟帖"><a href="#多说到跟帖" class="headerlink" title="多说到跟帖"></a>多说到跟帖</h2><p>　　通过多方“打听”和尝试，最后选择了<strong>网易云跟帖</strong>。<strong>先声明我不是打广告</strong>，我也是个多说的忠实用户，现在多说要关闭了，很难过的就是它的之前的评论，看了友言不支持导入多说、畅言必须是备案的域名，使用‘网易云跟帖’，不必备案域名，也支持导入多说的评论。真是好极了。。。</p>
<blockquote>
<p>有兴趣的可以参看这里<a href="https://blog.vadxq.com/dstogentie/" target="_blank" rel="noopener">从多说到跟帖：推荐网易云跟帖</a>，文章中详细说明了 <strong>注册网易云跟帖站点</strong> 和 <strong>如何导入多说的评论</strong>。</p>
</blockquote>
<h2 id="拥抱Coding"><a href="#拥抱Coding" class="headerlink" title="拥抱Coding"></a>拥抱Coding</h2><p>　　<a href="https://coding.net/" target="_blank" rel="noopener">CODING</a> 是国内首个一站式云端软件服务平台，于 2014 年 2 月在深圳成立，并于北京、上海、成都、西雅图设立分部。在云计算时代，CODING 致力于通过技术创新推动软件开发与交付模式的转变，让开发更简单。</p>
<p>　　CODING 旗下自主研发运营 Coding.net 云端软件开发协作平台，目前已累积 30 万开发者，50 万个项目。秉承着“让开发更简单”的使命，将代码托管、项目管理、WebIDE 等开发工具集成到浏览器中，免除繁杂的开发环境部署，降低开发和部署软件的成本，提升团队协作沟通效率，提高软件开发生产力，最终实现 “Coding Anytime Anywhere” 的愿景。</p>
<h3 id="Coding-Pages"><a href="#Coding-Pages" class="headerlink" title="Coding Pages"></a>Coding Pages</h3><p>　　Coding Pages 是一个免费的静态网页托管和演示服务。您可以使用 Coding Pages 托管博客、项目官网等静态网页，还可以绑定自定义域名。而且Coding Pages 可自动构建 Jekyll 网站，目前支持 Jekyll 3.0。</p>
<p>　　<strong>等等。。。</strong></p>
<p>　　这不是和github pages同效嘛，既然想让站点更快，那就干脆回到祖国的怀抱吧，OK，正式拥抱 Coding！</p>
<blockquote>
<p>没有用迁移这个说法，这是因为就是重新部署了一下。至于要切过去的原因，我乃天朝子民，岂久用番邦外夷器具乎（i.e. vpn严重影响打开速度）。</p>
</blockquote>
<p>具体步骤和github差不多，而且还是母语，相信不用教程也完全可以，我就不详细说了，有问题请自行看<a href="https://coding.net/help/" target="_blank" rel="noopener">帮助</a>或者去<a href="https://www.google.com.hk/?gws_rd=ssl" target="_blank" rel="noopener">Google</a>。</p>
<p>　　转移仓库时Coding提供了一个导入仓库功能，可直接从github上“pull”过来，如图：</p>
<p>![coding git](/styles/images/build blog/coding_git.png)</p>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>　　互联网的世界真是日新月异，生命不息折腾不止！</p>
<hr>]]></content>
  </entry>
  <entry>
    <title>安装 node-sass 报错的解决记录</title>
    <url>/2020/06/18/article-node-sass-error/</url>
    <content><![CDATA[<h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><p>yarn 或 npm install 命令的时候，总是在安装node-sass这一步卡住。终其原因还是「墙」的问题。<a id="more"></a></p>
<h2 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h2><p>npm和yarn的解决方法一样，这里以yarn为示例。</p>
<p>查看 yarn 配置 yarn config list</p>
<p>将 yarn 源切换至淘宝源</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">yarn config set registry https://registry.npm.taobao.org</span><br></pre></td></tr></table></figure>

<p>执行 <code>yarn install</code> 或者 <code>yarn</code></p>
<ol>
<li>如果遇到错误 <em>error An unexpected error occurred: “EINVAL: invalid argument, symlink</em></li>
</ol>
<p>请在你的执行命令之后添加 <code>--no-bin-links</code></p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">yarn install –no-bin-links</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>如果遇到错误 _error: xxxx node-sass: Command failed_，将 sass-binary-site 添加至 config 中，指定 node-sass 从 npm 的淘宝源中下载。</li>
</ol>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">yarn config set sass-binary-site https://npm.taobao.org/mirrors/node-sass</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>Node</tag>
        <tag>yarn</tag>
        <tag>npm</tag>
      </tags>
  </entry>
  <entry>
    <title>node多版本管理（Windows）</title>
    <url>/2020/04/13/other-node-nvm-windows/</url>
    <content><![CDATA[<p>旧的项目依赖低版本的node，但是现在的node版本变化太快，所以使用 nvm-windows 来管理不同版本的node。<a id="more"></a></p>
<p>软件地址：<a href="https://github.com/coreybutler/nvm-windows/releases" target="_blank" rel="noopener">https://github.com/coreybutler/nvm-windows/releases</a></p>
<h3 id="卸载现有的node和npm"><a href="#卸载现有的node和npm" class="headerlink" title="卸载现有的node和npm"></a>卸载现有的node和npm</h3><p>请注意，在安装NVM for Windows之前，您需要卸载任何现有版本的node.js. 还删除可能保留的任何现有nodejs安装目录（例如，C\Program Files\nodejs）。NVM生成的符号链接不会覆盖现有（甚至是空的）安装目录。</p>
<p>还应该删除现有的npm安装位置（例如C\Users&lt;user&gt;\AppData\Roaming\npm），以便正确使用nvm安装位置。</p>
<h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><p>在releases中下载最新版本nvm-setup.zip，解压后，是一个安装文件，直接安装即可。</p>
<p>由于国内在一些情况下有些特殊。Node.js 官方镜像源又在国外，经常通过 nvm 安装 Node.js 时，速度比较慢，或者没有响应。</p>
<p>根据这种情况，nvm 允许更改安装的镜像源，我们可以将镜像源切换到国内的淘宝提供的镜像源。</p>
<p>默认安装目录（C:\Users[username]\AppData\Roaming\nvm）下的settings.txt文件中添加：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">node_mirror: https://npm.taobao.org/mirrors/node/</span><br><span class="line">npm_mirror: https://npm.taobao.org/mirrors/npm/</span><br></pre></td></tr></table></figure>

<h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 查看已安装的node</span><br><span class="line">nvm -ls </span><br><span class="line"># 安装指定版本的node</span><br><span class="line">nvm install &lt;version&gt;</span><br><span class="line"># 指定自己要使用的版本号</span><br><span class="line">nvm use &lt;version&gt;</span><br><span class="line"># 卸载指定版本的node</span><br><span class="line">nvm uninstall &lt;version&gt;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>具体参考：<a href="https://github.com/coreybutler/nvm-windows#usage" target="_blank" rel="noopener">https://github.com/coreybutler/nvm-windows#usage</a></p>
</blockquote>
]]></content>
      <categories>
        <category>杂记</category>
      </categories>
      <tags>
        <tag>NODE</tag>
      </tags>
  </entry>
  <entry>
    <title>CentOS配置Docker API TLS认证</title>
    <url>/2020/04/03/other-docker-tls/</url>
    <content><![CDATA[<p>我们利用Portainer来管理docker环境和用Jenkins来自动构建和部署docker时的远程管理都会使用到Docker API，通常我们只是开启了没有安全保护的2375（通常）端口，这个比较危险，会导致远程劫持攻击。那么我们就需要配置TLS认证的2376（通常）端口。<a id="more"></a></p>
<p>下面我们针对CentOS系统进行配置：</p>
<p>一、利用系统自带的openssl生成相应的服务端和客户端证书<br>具体过程可参考：<a href="https://docs.docker.com/engine/security/https/" target="_blank" rel="noopener">https://docs.docker.com/engine/security/https/</a></p>
<p>我们利用脚本自动生成，这样非常便捷，脚本（auto-tls-certs.sh）如下：</p>
<figure class="highlight shell"><figcaption><span>script</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash">!/bin/bash</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> </span></span><br><span class="line"><span class="meta">#</span><span class="bash"> -------------------------------------------------------------</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 自动创建 Docker TLS 证书</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> -------------------------------------------------------------</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 以下是配置信息</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> Config start</span></span><br><span class="line">IP="39.39.139.39"</span><br><span class="line">PASSWORD="123456"</span><br><span class="line">COUNTRY="CN"</span><br><span class="line">STATE="Beijing"</span><br><span class="line">CITY="Beijing"</span><br><span class="line">ORGANIZATION="COM"</span><br><span class="line">ORGANIZATIONAL_UNIT="Dev"</span><br><span class="line">COMMON_NAME="$IP"</span><br><span class="line">EMAIL="COM@china.cn"</span><br><span class="line"><span class="meta">#</span><span class="bash"> Config end</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 生成 CA 密钥</span></span><br><span class="line">if [[ ! -f ca-key.pem ]]; then</span><br><span class="line">    echo " - 生成 CA 密钥"</span><br><span class="line">    openssl genrsa -aes256 -passout "pass:$PASSWORD" -out "ca-key.pem" 4096</span><br><span class="line">fi</span><br><span class="line"><span class="meta">#</span><span class="bash"> 生成 CA</span></span><br><span class="line">if [[ ! -f ca.pem ]]; then</span><br><span class="line">    echo " - 生成 CA"</span><br><span class="line">    openssl req -new -x509 -days 365 -key "ca-key.pem" -sha256 -out "ca.pem" -passin "pass:$PASSWORD" -subj "/C=$COUNTRY/ST=$STATE/L=$CITY/O=$ORGANIZATION/OU=$ORGANIZATIONAL_UNIT/CN=$COMMON_NAME/emailAddress=$EMAIL"</span><br><span class="line">fi</span><br><span class="line"><span class="meta">#</span><span class="bash"> 生成服务器密钥 &amp; 服务器证书</span></span><br><span class="line">if [[ ! -f server-key.pem ]]; then</span><br><span class="line">    echo " - 生成服务器密钥"</span><br><span class="line">    openssl genrsa -out "server-key.pem" 4096</span><br><span class="line">fi</span><br><span class="line">if [[ ! -f server.csr ]]; then</span><br><span class="line">     openssl req -subj "/CN=$COMMON_NAME" -sha256 -new -key "server-key.pem" -out server.csr</span><br><span class="line">fi</span><br><span class="line">if [[ ! -f server-cert.pem ]]; then</span><br><span class="line">    echo " - 生成服务器证书"</span><br><span class="line">    echo "subjectAltName = IP:$IP,IP:127.0.0.1" &gt;&gt; extfile.cnf</span><br><span class="line">    echo "extendedKeyUsage = serverAuth" &gt;&gt; extfile.cnf</span><br><span class="line">    openssl x509 -req -days 365 -sha256 -in server.csr -passin "pass:$PASSWORD" -CA "ca.pem" -CAkey "ca-key.pem" -CAcreateserial -out "server-cert.pem" -extfile extfile.cnf</span><br><span class="line">fi</span><br><span class="line">rm -f extfile.cnf</span><br><span class="line"><span class="meta">#</span><span class="bash"> 生成客户端证书</span></span><br><span class="line">if [[ ! -f key.pem ]]; then</span><br><span class="line">    openssl genrsa -out "key.pem" 4096</span><br><span class="line">fi</span><br><span class="line">if [[ ! -f cert.pem ]]; then</span><br><span class="line">    openssl req -subj '/CN=client' -new -key "key.pem" -out client.csr</span><br><span class="line">    echo extendedKeyUsage = clientAuth &gt;&gt; extfile.cnf</span><br><span class="line">    openssl x509 -req -days 365 -sha256 -in client.csr -passin "pass:$PASSWORD" -CA "ca.pem" -CAkey "ca-key.pem" -CAcreateserial -out "cert.pem" -extfile extfile.cnf</span><br><span class="line">fi</span><br><span class="line">chmod -v 0400 "ca-key.pem" "key.pem" "server-key.pem"</span><br><span class="line">chmod -v 0444 "ca.pem" "server-cert.pem" "cert.pem"</span><br><span class="line"><span class="meta">#</span><span class="bash"> 打包客户端证书</span></span><br><span class="line">echo " - 打包客户端证书为 tls-client-certs.tar.gz"</span><br><span class="line">mkdir -p "tls-client-certs"</span><br><span class="line">cp -f "ca.pem" "cert.pem" "key.pem" "tls-client-certs/"</span><br><span class="line">cd "tls-client-certs"</span><br><span class="line">tar zcf "tls-client-certs.tar.gz" *</span><br><span class="line">mv "tls-client-certs.tar.gz" ../</span><br><span class="line">cd ..</span><br><span class="line">rm -rf "tls-client-certs"</span><br><span class="line"><span class="meta">#</span><span class="bash"> 拷贝服务端证书</span></span><br><span class="line">mkdir -p /etc/docker/certs.d</span><br><span class="line">cp -f "ca.pem" "server-cert.pem" "server-key.pem" /etc/docker/certs.d/</span><br><span class="line"><span class="meta">#</span><span class="bash"> 清理</span></span><br><span class="line">rm -vf client.csr server.csr extfile.cnf ca.srl server-cert.pem server-key.pem cert.pem</span><br><span class="line">echo "Connect to server via docker-cli:"</span><br><span class="line">echo "docker -H $IP:2376 --tlsverify --tlscacert ~/.docker/ca.pem --tlscert ~/.docker/cert.pem --tlskey ~/.docker/key.pem ps -a"</span><br><span class="line"><span class="meta">#</span><span class="bash"> 客户端使用 cURL 连接</span></span><br><span class="line">echo "Connect to server via curl:"</span><br><span class="line">echo "curl --cacert ~/.docker/ca.pem --cert ~/.docker/cert.pem --key ~/.docker/key.pem https://$IP:2376/containers/json"</span><br><span class="line">echo -e "\e[1;32mAll be done.\e[0m"</span><br></pre></td></tr></table></figure>

<p>执行脚本前可能由于脚本是在windows端写地，会报一个错 $’\r’: command not found；所以先对脚本执行以下这个命令 ：</p>
<p><code>dos2unix auto-tls-certs.sh</code></p>
<p>然后再执行命令</p>
<p><code>sh ./auto-tls.certs.sh</code></p>
<p>对脚本中的变量进行修改并执行后，自动会创建好tls证书，服务器的证书会自动move到/etc/docker/certs.d/目录下；</p>
<p>客户端的证书在运行脚本的目录下，同时还自动打好了一个.tar.gz的包，很方便，可以下载到本地，配置时直接对应上传即可。</p>
<p>二、配置Docker服务<br>在 /usr/lib/systemd/system 目录下找到 文件 docker.service</p>
<p>原来的ExecStart项为：</p>
<blockquote>
<p>ExecStart=/usr/bin/dockerd -H fd:// –containerd=/run/containerd/containerd.sock</p>
</blockquote>
<p>修改为：</p>
<blockquote>
<p>ExecStart=/usr/bin/dockerd -H fd:// –containerd=/run/containerd/containerd.sock -H unix:///var/run/docker.sock -D -H tcp://0.0.0.0:2376   <br>         –tlsverify –tlscacert=/etc/docker/certs.d/ca.pem   <br>            –tlscert=/etc/docker/certs.d/server-cert.pem     <br>        –tlskey=/etc/docker/certs.d/server-key.pem</p>
</blockquote>
<p>然后执行命令重启docker服务：</p>
<p><code>systemctl daemon-reload</code><br><code>systemctl restart docker</code></p>
<p>成功重启后，服务端的配置就结束了(成功与否的检测方法在自动化脚本中有说明，也可以直接进行下一步)。</p>
<p>三、配置Portainer远程TLS连接</p>
<img src="/styles/images/Portainer-tls.png">

<p>证书对应选择：</p>
<p>TLS CA certificate：ca.pem</p>
<p>TLS certificate：cert.pem</p>
<p>TLS key：key.pem</p>
<p>这样就完成了。</p>
]]></content>
      <categories>
        <category>杂记</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Harbor安装及使用</title>
    <url>/2020/03/05/other-docker-harbor/</url>
    <content><![CDATA[<p>参考： <a href="https://istone.dev/2019/07/19/harbor-install/" target="_blank" rel="noopener">https://istone.dev/2019/07/19/harbor-install/</a></p>
<p><code>tar xvf harbor-offline-installer-v1.10.1.tgz</code></p>
<p><code>cd harbor</code></p>
<p>修改配置文件 harbor.yml</p>
<p><code>./install.sh --with-notary --with-clair --with-chartmuseum</code> 参数可选，分别对应三个扩展功能</p>
<p>完成后就可以访问了</p>
<p>将harbor加入开机自启，通过服务的形式</p>
<p>参考：<a href="https://www.cnblogs.com/kirito-c/p/11145881.html" target="_blank" rel="noopener">https://www.cnblogs.com/kirito-c/p/11145881.html</a></p>
<p><code>sudo systemctl enable harbor</code></p>
<p><code>sudo systemctl start harbor</code></p>
<p><code>systemctl status harbor.service -l</code>  检查是否开启</p>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker入门及实践</title>
    <url>/2020/02/25/other-docker-know/</url>
    <content><![CDATA[<p>Docker官方文档：<a href="https://docs.docker.com/get-started/" target="_blank" rel="noopener">https://docs.docker.com/get-started/</a></p>
<p>腾讯云的中文文档：<a href="https://cloud.tencent.com/developer/information/docker%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3" target="_blank" rel="noopener">https://cloud.tencent.com/developer/information/docker%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3</a></p>
<p>DockerInfo中文文档：<a href="http://www.dockerinfo.net/document" target="_blank" rel="noopener">http://www.dockerinfo.net/document</a></p>
<p>Docker — 从入门到实践： <a href="https://vuepress.mirror.docker-practice.com/" target="_blank" rel="noopener">https://vuepress.mirror.docker-practice.com/</a></p>
<a id="more"></a>

<hr>
<p>所有命令可参考：<a href="https://www.runoob.com/docker/docker-command-manual.html" target="_blank" rel="noopener">https://www.runoob.com/docker/docker-command-manual.html</a></p>
<ul>
<li>列出本地已有镜像：  <code>docker images</code></li>
<li>移除镜像： <code>docker rmi 或者 docker image rm [id|name]</code></li>
</ul>
<blockquote>
<p>注意：在删除镜像之前要先用 docker rm 删掉依赖于这个镜像的所有容器。可加参数 -f 强制删除。</p>
</blockquote>
<ul>
<li>生成镜像： <code>docker build -t [repositoryName]:[tag] [Directory]</code></li>
</ul>
<p>命令行参数参考：<a href="https://www.runoob.com/docker/docker-build-command.html" target="_blank" rel="noopener">https://www.runoob.com/docker/docker-build-command.html</a></p>
<blockquote>
<p>“Directory” 是 Dockerfile 所在的路径<br>一个镜像不能超过 127 层,编写Dockerfile时应注意</p>
</blockquote>
<ul>
<li>从镜像run容器：<code>docker run -d -p 1202:3000 --name=&quot;quoted&quot; tmd.ictr.cn:89/web-server/quoted:1.0</code></li>
</ul>
<p>命令行参数参考：<a href="https://www.runoob.com/docker/docker-run-command.html" target="_blank" rel="noopener">https://www.runoob.com/docker/docker-run-command.html</a></p>
<ul>
<li>docker重启退出的容器 docker attach [container]</li>
</ul>
<p><code>docker ps</code>: 查看当前运行的容器<br><code>docker ps -a</code>:查看所有容器，包括停止的。<br><code>docker ps -l</code>:查看最新创建的容器，只列出最后创建的。<br><code>docker ps -n=2</code>:-n=x选项，会列出最后创建的x个容器。</p>
<ul>
<li>移除容器：<code>docker rm [id]</code></li>
</ul>
<p>查看日志： <code>docker logs name</code></p>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>CentOS 8安装Node</title>
    <url>/2020/02/23/other-Centos-node/</url>
    <content><![CDATA[<h3 id="wget下载源码"><a href="#wget下载源码" class="headerlink" title="wget下载源码"></a>wget下载源码</h3><p>在需要安装nodejs的目录中执行</p>
<p><code>wget https://nodejs.org/dist/v12.16.1/node-v12.16.1-linux-x64.tar.xz</code></p>
<p><a href="https://nodejs.org/dist" target="_blank" rel="noopener">https://nodejs.org/dist</a> 这个目录下可以找到各个版本的源码</p>
<h3 id="解压"><a href="#解压" class="headerlink" title="解压"></a>解压</h3><p><code>xz -d node-v12.16.1-linux-x64.tar.xz</code></p>
<p><code>tar -xvf node-v12.16.1-linux-x64.tar</code></p>
<h3 id="配置软连接"><a href="#配置软连接" class="headerlink" title="配置软连接"></a>配置软连接</h3><p>要想node能够在全局能够使用，需要添加软连接，配置完即可使用</p>
<p><code>ln -s /usr/local/node-v12.16.1-linux-x64/bin/node /usr/local/bin/node</code></p>
<p><code>ln -s /usr/local/node-v12.16.1-linux-x64/bin/npm /usr/local/bin/npm</code></p>
<h3 id="全局安装的包执行shell时找不到命令的解决方法"><a href="#全局安装的包执行shell时找不到命令的解决方法" class="headerlink" title="全局安装的包执行shell时找不到命令的解决方法"></a>全局安装的包执行shell时找不到命令的解决方法</h3><p><a href="https://blog.csdn.net/m0_37263637/article/details/81942435" target="_blank" rel="noopener">https://blog.csdn.net/m0_37263637/article/details/81942435</a></p>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>Node</tag>
      </tags>
  </entry>
  <entry>
    <title>CentOS 8安装Docker</title>
    <url>/2020/02/17/other-CentOS8-Docker/</url>
    <content><![CDATA[<h2 id="CentOS-8安装Docker"><a href="#CentOS-8安装Docker" class="headerlink" title="CentOS 8安装Docker"></a>CentOS 8安装Docker</h2><p>本文参考<a href="https://docs.docker.com/install/linux/docker-ce/centos/" target="_blank" rel="noopener">Docker 官方 CentOS 安装文档</a><a id="more"></a></p>
<h3 id="安装依赖"><a href="#安装依赖" class="headerlink" title="安装依赖"></a>安装依赖</h3><pre><code>yum install -y yum-utils  device-mapper-persistent-data  lvm2</code></pre><blockquote>
<p>yum options -h（帮助），-y（当安装过程提示选择全部为”yes”），-q（不显示安装的过程）    </p>
</blockquote>
<h3 id="更改yum源为国内源"><a href="#更改yum源为国内源" class="headerlink" title="更改yum源为国内源"></a>更改yum源为国内源</h3><pre><code>yum-config-manager --add-repo https://mirrors.ustc.edu.cn/docker-ce/linux/centos/docker-ce.repo</code></pre><h3 id="更新-yum-软件源缓存"><a href="#更新-yum-软件源缓存" class="headerlink" title="更新 yum 软件源缓存"></a>更新 yum 软件源缓存</h3><pre><code>yum makecache</code></pre><h3 id="安装-docker-ce"><a href="#安装-docker-ce" class="headerlink" title="安装 docker-ce"></a>安装 docker-ce</h3><pre><code>yum install docker-ce -y</code></pre><p>如果报错： Problem: package docker-ce-3:19.03.7-3.el7.x86_64 requires containerd.io &gt;= 1.2.2-3, but none of theproviders can be installed</p>
<p>问题的根源在于containerd.io的版本过低，这里通过安装较高版本的containerd.io即可解决问题。<br>访问官网：<a href="https://containerd.io/downloads/，根据提示，直接安装二进制包：" target="_blank" rel="noopener">https://containerd.io/downloads/，根据提示，直接安装二进制包：</a></p>
<pre><code>wget https://github.com/containerd/containerd/releases/download/v1.3.2/containerd-1.3.2.linux-amd64.tar.gz tar xvf containerd-1.3.2.linux-amd64.tar.gz</code></pre><p>这时候又有一个问题就是官网下载的网速奇慢，所以直接使用DNF工具从docker-ce的仓库（<a href="https://download.docker.com/linux/centos/7/x86_64/stable/Packages/）中下载：" target="_blank" rel="noopener">https://download.docker.com/linux/centos/7/x86_64/stable/Packages/）中下载：</a></p>
<pre><code>dnf install https://download.docker.com/linux/centos/7/x86_64/stable/Packages/containerd.io-1.2.6-3.3.el7.x86_64.rpm

yum install docker-ce -y

docker --version （查看安装是否成功）</code></pre><p>###开启Docker服务</p>
<pre><code>systemctl enable docker （开机自启）

systemctl start docker  （启动docker服务）

systemctl restart docker 重启</code></pre><h3 id="Docker加速"><a href="#Docker加速" class="headerlink" title="Docker加速"></a>Docker加速</h3><p><a href="https://blog.csdn.net/varyall/article/details/104266706" target="_blank" rel="noopener">https://blog.csdn.net/varyall/article/details/104266706</a></p>
<p>该脚本可以将 –registry-mirror 加入到你的 Docker 配置文件 /etc/docker/daemon.json 中。适用于 Ubuntu14.04、Debian、CentOS6 、CentOS7、Fedora、Arch Linux、openSUSE Leap 42.1，其他版本可能有细微不同。更多详情请访问文档。</p>
<p>curl -sSL <a href="https://get.daocloud.io/daotools/set_mirror.sh" target="_blank" rel="noopener">https://get.daocloud.io/daotools/set_mirror.sh</a> | sh -s <a href="http://f1361db2.m.daocloud.io" target="_blank" rel="noopener">http://f1361db2.m.daocloud.io</a></p>
<p><code>docker pull node:12.16.1-alpine</code></p>
<p><code>docker run -it --name node-test node:12.16.1-alpine</code></p>
<p>我们在doker容器中直接运行node服务，并且打印字符串。这里我们通过run指令，用node镜像创建的一个叫node-test的容器， -i(interactive,表示开启交互), -t(terminal, 表示终端)，-it是两个参数的简写，表示通过命令行的方式运行。还可以添加-d(deamon)表示在后台运行。退出控制台后，我们通过docker ps -a查看当前的所有容器.</p>
<p>接下来我们再次启动这个容器，并进入其中运行bash命令行:</p>
<p><code>docker exec -it node-test sh</code></p>
<p>我们一般可能会在容器启动后进入容器，常用的是docker attach 镜像id，但是启动镜像的时候如果没有带 参数 -it的话，attach进去后可能是日志界面，并不能执行命令。所以我们会用 docker exec -it [镜像id] /bin/bash/</p>
<p>平常的容器一般都可以执行/bin/bash，但是alpine没有，改成 docker exec -it [镜像id] sh 就好了。</p>
<h2 id="安装docker-compose"><a href="#安装docker-compose" class="headerlink" title="安装docker-compose"></a>安装docker-compose</h2><p>下载docker-compose二进制文件安装，因为github网速慢，所以改到了daocloud</p>
<pre><code>curl -L https://get.daocloud.io/docker/compose/releases/download/1.25.4/docker-compose-`uname -s`-`uname -m` &gt; /usr/local/bin/docker-compose</code></pre><p>添加可执行权限 </p>
<p> <code>chmod +x /usr/local/bin/docker-compose</code></p>
<p>测试安装结果 </p>
<p><code>docker-compose --version</code></p>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>Auto Polyfill</title>
    <url>/2019/11/14/article-js-auto-polyfill/</url>
    <content><![CDATA[<h2 id="什么是补丁？"><a href="#什么是补丁？" class="headerlink" title="什么是补丁？"></a>什么是补丁？</h2><blockquote>
<p>A polyfill, or polyfiller, is a piece of code (or plugin) that provides the technology that you, the developer, expect the browser to provide natively. Flattening the API landscape if you will.</p>
</blockquote>
<p>我们希望浏览器提供一些特性，但是没有，然后我们自己写一段代码来实现他，那这段代码就是补丁。<a id="more"></a></p>
<hr>
<p>如果你是一个 3 年陈 + 的前端，应该会有听说过 shim、sham、es5-shim 和 es6-shim 等等现在看起来很古老的补丁方式。</p>
<p>那么，shim 和 sham 是啥？又有什么区别？</p>
<ul>
<li>shim 是能用的补丁</li>
<li>sham 顾名思义，是假的意思，所以 sham 是一些假的方法，只能使用保证不出错，但不能用。至于为啥会有 sham，因为有些方法的低端浏览器里根本实现不了</li>
</ul>
<hr>
<p>在 shim 和 sham 之后，还有一种补丁方式是引入包含所有语言层补丁的 babel-polyfill.js。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https://cdnjs.cloudflare.com/ajax/libs/babel-polyfill/7.2.5/polyfill.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<hr>
<p>@babel/preset-env + useBuiltins: entry + targets</p>
<p>babel-polyfill 包含所有补丁，那我只需要支持某些浏览器的某些版本，是否有办法只包含这些浏览器的补丁？这就是 @babel/preset-env + useBuiltins: entry + targets 配置的方案。</p>
<p>我们先在入口文件里引入 @babel/polyfill，</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'@babel/polyfill'</span>;</span><br></pre></td></tr></table></figure>

<p>然后配置 .babelrc，添加 preset @babel/preset-env，并设置 useBuiltIns 和 targets，</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"presets"</span>: [</span><br><span class="line">    [<span class="string">"@babel/env"</span>, &#123;</span><br><span class="line">      useBuiltIns: 'entry',</span><br><span class="line">      targets: &#123; chrome: 62 &#125;</span><br><span class="line">    &#125;]</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>useBuiltIns: entry 的含义是找到入口文件里引入的 @babel/polyfill，并替换为 targets 浏览器/环境需要的补丁列表。</p>
<p>替换后的内容，比如：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">"core-js/modules/es7.string.pad-start"</span>;</span><br><span class="line"><span class="keyword">import</span> <span class="string">"core-js/modules/es7.string.pad-end"</span>;</span><br><span class="line"><span class="comment">//...</span></span><br></pre></td></tr></table></figure>

<p>这样就只会引入 chrome@62 及以上所需要的补丁，什么 Promise 之类的都不会再打包引入。</p>
<p>这种方案存在的问题：</p>
<ul>
<li>特性列表是按浏览器整理的，那怎么知道哪些特性我用了，哪些没有用到，没有用到的部分也引入了是不是也是冗余？@babel/preset-env 有提供 exclude 的配置，如果我配置了 exclude，后面是否得小心翼翼地确保不要用到 exclude 掉的特性</li>
<li>补丁是打包到静态文件的，如果我配置 targets 为 chrome: 62, ie: 9，那意味着 chrome 62 也得载入 ie 9 相关的补丁，这也是一份冗余</li>
<li>我们是基于 core-js 打的补丁，所以只会包含 ecmascript 规范里的内容，其他比如说 dom 里的补丁，就不在此列，应该如何处理？</li>
</ul>
<h2 id="在线补丁，比如：polyfill-io"><a href="#在线补丁，比如：polyfill-io" class="headerlink" title="在线补丁，比如：polyfill.io"></a>在线补丁，比如：polyfill.io</h2><p>目前最流行的应该就是 polyfill.io，提供的是 cdn 服务，有些站点在用，例如 <a href="https://spectrum.chat/。另外，polyfill.io" target="_blank" rel="noopener">https://spectrum.chat/。另外，polyfill.io</a> 还开源了 polyfill-service 供我们自己搭建使用。</p>
<p>使用上，比如：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https://polyfill.io/v3/polyfill.min.js?features=default%2CPromise"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https://polyfill.alicdn.com/polyfill.min.js?features=default,es5,es6,es7,RegeneratorRuntime"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后在 Chrome@71 下的输出是：</p>
<p>/* Disable minification (remove <code>.min</code> from URL path) for more info */<br>啥都没有，因为 Promsie 特性 Chrome@71 已经支持了。</p>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>Polyfill</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue3.0 Function API</title>
    <url>/2019/07/07/article-vue3-function-api/</url>
    <content><![CDATA[<h1 id="1-引言"><a href="#1-引言" class="headerlink" title="1. 引言"></a>1. 引言</h1><p>Vue 3.0 的发布引起了轩然大波，让我们解读下它的 <a href="https://github.com/vuejs/rfcs/blob/function-apis/active-rfcs/0000-function-api.md#comparison-with-react-hooks" target="_blank" rel="noopener">function api RFC</a> 详细了解一下 Vue 团队是怎么想的吧！</p>
<a id="more"></a>
<p>首先官方回答了几个最受关注的问题：</p>
<p><strong>Vue 3.0 是否有 break change，就像 Python 3 / Angular 2 一样？</strong></p>
<p>不，100% 兼容 Vue 2.0，且暂未打算废弃任何 API（未来也不）。之前有草案试图这么做，但由于用户反馈太猛，被撤回了。</p>
<p><strong>Vue 3.0 的设计盖棺定论了吗？</strong></p>
<p>没有呀，这次精读的稿子就是 RFC（Request For Comments），翻译成中文就是 “意见征求稿”，还在征求大家意见中哦。</p>
<p><strong>这 RFC 咋这么复杂？</strong></p>
<p>RFC 是写给贡献者/维护者的，要考虑许多边界情况与细节，所以当然会复杂很多喽！当然 Vue 本身使用起来还是很简单的。</p>
<blockquote>
<p>Vue 本身 Mutable + Template 就注定了是个用起来简单（约定 + 自然），实现起来复杂（解析 + 双绑）的框架。</p>
</blockquote>
<p><strong>这次改动很像在模仿 React，为啥不直接用 React？</strong></p>
<p>首先 Template 机制还是没变，其次模仿的是 Hooks 而不是 React 全部，如果你不喜欢这个改动，那你更不会喜欢用 React。</p>
<p>PS: 问这个问题的人，一定没有同时理解 React 与 Vue，其实这两个框架到现在差别蛮大的，后面精读会详细说明。</p>
<p>下面正式进入 Vue 3.0 Function API 的介绍。</p>
<h1 id="2-概述"><a href="#2-概述" class="headerlink" title="2. 概述"></a>2. 概述</h1><p>Vue 函数式基本 Demo：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;span&gt;count is &#123;&#123; count &#125;&#125;&lt;/span&gt;</span><br><span class="line">    &lt;span&gt;plusOne is &#123;&#123; plusOne &#125;&#125;&lt;/span&gt;</span><br><span class="line">    &lt;button @click=&quot;increment&quot;&gt;count++&lt;/button&gt;</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script&gt;</span><br><span class="line">import &#123; value, computed, watch, onMounted &#125; from &apos;vue&apos;</span><br><span class="line"></span><br><span class="line">export default &#123;</span><br><span class="line">  setup() &#123;</span><br><span class="line">    // reactive state</span><br><span class="line">    const count = value(0)</span><br><span class="line">    // computed state</span><br><span class="line">    const plusOne = computed(() =&gt; count.value + 1)</span><br><span class="line">    // method</span><br><span class="line">    const increment = () =&gt; &#123; count.value++ &#125;</span><br><span class="line">    // watch</span><br><span class="line">    watch(() =&gt; count.value * 2, val =&gt; &#123;</span><br><span class="line">      console.log(`count * 2 is $&#123;val&#125;`)</span><br><span class="line">    &#125;)</span><br><span class="line">    // lifecycle</span><br><span class="line">    onMounted(() =&gt; &#123;</span><br><span class="line">      console.log(`mounted`)</span><br><span class="line">    &#125;)</span><br><span class="line">    // expose bindings on render context</span><br><span class="line">    return &#123;</span><br><span class="line">      count,</span><br><span class="line">      plusOne,</span><br><span class="line">      increment</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>

<p>函数式风格的入口是 <code>setup</code> 函数，采用了函数式风格后可以享受如下好处：类型自动推导、减少打包体积。</p>
<p><code>setup</code> 函数返回值就是注入到页面模版的变量。我们也可以返回一个函数，通过使用 <code>value</code> 这个 API 产生属性并修改：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; value &#125; <span class="keyword">from</span> <span class="string">'vue'</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> MyComponent = &#123;</span><br><span class="line">  setup(props) &#123;</span><br><span class="line">    <span class="keyword">const</span> msg = value(<span class="string">'hello'</span>)</span><br><span class="line">    <span class="keyword">const</span> appendName = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      msg.value = <span class="string">`hello <span class="subst">$&#123;props.name&#125;</span>`</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      msg,</span><br><span class="line">      appendName</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  template: <span class="string">`&lt;div @click="appendName"&gt;&#123;&#123; msg &#125;&#125;&lt;/div&gt;`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>要注意的是，<code>value()</code> 返回的是一个对象，通过 <code>.value</code> 才能访问到其真实值。</p>
<p>为何 <code>value()</code> 返回的是 Wrappers 而非具体值呢？原因是 Vue 采用双向绑定，只有对象形式访问值才能保证访问到的是最终值，这一点类似 React 的 <code>useRef()</code> API 的 <code>.current</code> 规则。</p>
<p>那既然所有 <code>value()</code> 返回的值都是 Wrapper，那直接给模版使用时要不要调用 <code>.value</code> 呢？<strong>答案是否定的，直接使用即可，模版会自动 <code>Unwrapping</code>:</strong></p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> MyComponent = &#123;</span><br><span class="line">  setup() &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      count: value(<span class="number">0</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  template: <span class="string">`&lt;button @click="count++"&gt;&#123;&#123; count &#125;&#125;&lt;/button&gt;`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接下来是 <strong>Hooks</strong>，下面是一个使用 Hooks 实现获得鼠标实时位置的例子：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">useMouse</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> x = value(<span class="number">0</span>)</span><br><span class="line">  <span class="keyword">const</span> y = value(<span class="number">0</span>)</span><br><span class="line">  <span class="keyword">const</span> update = <span class="function"><span class="params">e</span> =&gt;</span> &#123;</span><br><span class="line">    x.value = e.pageX</span><br><span class="line">    y.value = e.pageY</span><br><span class="line">  &#125;</span><br><span class="line">  onMounted(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">window</span>.addEventListener(<span class="string">'mousemove'</span>, update)</span><br><span class="line">  &#125;)</span><br><span class="line">  onUnmounted(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">window</span>.removeEventListener(<span class="string">'mousemove'</span>, update)</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">return</span> &#123; x, y &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in consuming component</span></span><br><span class="line"><span class="keyword">const</span> Component = &#123;</span><br><span class="line">  setup() &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; x, y &#125; = useMouse()</span><br><span class="line">    <span class="keyword">const</span> &#123; z &#125; = useOtherLogic()</span><br><span class="line">    <span class="keyword">return</span> &#123; x, y, z &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  template: <span class="string">`&lt;div&gt;&#123;&#123; x &#125;&#125; &#123;&#123; y &#125;&#125; &#123;&#123; z &#125;&#125;&lt;/div&gt;`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到，<code>useMouse</code> 将所有与 “处理鼠标位置” 相关的逻辑都封装了进去，乍一看与 React Hooks 很像，但是有两个区别：</p>
<ol>
<li><code>useMouse</code> 函数内改变 <code>x</code>、<code>y</code> 后，不会重新触发 <code>setup</code> 执行。</li>
<li><code>x</code> <code>y</code> 拿到的都是 Wrapper 而不是原始值，且这个值会动态变化。</li>
</ol>
<p>另一个重要 API 就是 <strong><code>watch</code></strong>，它的作用类似 React Hooks 的 <strong>useEffect</strong>，但实现原理和调用时机其实完全不一样。</p>
<p><code>watch</code> 的目的是监听某些变量变化后执行逻辑，比如当 <code>id</code> 变化后重新取数：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> MyComponent = &#123;</span><br><span class="line">  props: &#123;</span><br><span class="line">    id: <span class="built_in">Number</span></span><br><span class="line">  &#125;,</span><br><span class="line">  setup(props) &#123;</span><br><span class="line">    <span class="keyword">const</span> data = value(<span class="literal">null</span>)</span><br><span class="line">    watch(<span class="function"><span class="params">()</span> =&gt;</span> props.id, <span class="keyword">async</span> (id) =&gt; &#123;</span><br><span class="line">      data.value = <span class="keyword">await</span> fetchData(id)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>之所以要 <code>watch</code>，因为在 Vue 中，<code>setup</code> 函数仅执行一次，所以不像 React Function Component，每次组件 <code>props</code> 变化都会重新执行，因此无论是在变量、<code>props</code> 变化时如果想做一些事情，都需要包裹在 <code>watch</code> 中。</p>
<p>后面还有 <code>unwatching</code>、生命周期函数、依赖注入，都是一些语法定义，感兴趣可以继续<a href="https://github.com/vuejs/rfcs/blob/function-apis/active-rfcs/0000-function-api.md#dependency-injection" target="_blank" rel="noopener">阅读原文</a>，笔者就不赘述了。</p>
<h2 id="Vue-与-React-逻辑结构"><a href="#Vue-与-React-逻辑结构" class="headerlink" title="Vue 与 React 逻辑结构"></a>Vue 与 React 逻辑结构</h2><p>React Function Component 与 Hooks，虽然在实现原理上，与 Vue3.0 存在 Immutable 与 Mutable、JSX 与 Template 的区别，但逻辑理解上有着相通之处。</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> MyComponent = &#123;</span><br><span class="line">  setup(props) &#123;</span><br><span class="line">    <span class="keyword">const</span> x = value(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> setXRandom = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      x.value = <span class="built_in">Math</span>.random()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123; x, setXRandom &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  template: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;button @onClick="setXRandom"/&gt;&#123;&#123;x&#125;&#125;&lt;/button&gt;</span></span><br><span class="line"><span class="string">  `</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>虽然在 Vue 中，<code>setup</code> 函数仅执行一次，看上去与 React 函数完全不一样（React 函数每次都执行），但其实 Vue 将渲染层（Template）与数据层（setup）分开了，而 React 合在了一起。</p>
<p>我们可以利用 React Hooks 将数据层与渲染层完全隔离：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 类似 vue 的 setup 函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">useMyComponentSetup</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> [x, setX] = useState(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> setXRandom = useCallback(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    setX(<span class="built_in">Math</span>.random())</span><br><span class="line">  &#125;, [setX])</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> &#123; x, setXRandom &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 类似 vue 的 template 函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyComponent</span>(<span class="params">props: &#123; name: String &#125;</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> &#123; x, setXRandom &#125; = useMyComponentSetup(props)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    &lt;button onClick=&#123;setXRandom&#125;&gt;&#123;x&#125;&lt;<span class="regexp">/button&gt;</span></span><br><span class="line"><span class="regexp">  )</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br></pre></td></tr></table></figure>

<p>这源于 JSX 与 Template 的根本区别。JSX 使模版与 JS 可以写在一起，因此数据层与渲染层可以耦合在一起写（也可以拆分），但 Vue 采取的 Template 思路使数据层强制分离了，这也使代码分层更清晰了。</p>
<p>而实际上 Vue3.0 的 <code>setup</code> 函数也是可选的，再配合其支持的 TSX 功能，与 React 真的只有 Mutable 的区别了：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这是个 Vue 组件</span></span><br><span class="line"><span class="keyword">const</span> MyComponent = createComponent(<span class="function">(<span class="params">props: &#123; msg: string &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="params">()</span> =&gt;</span> h(<span class="string">'div'</span>, props.msg)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>我们很难评价 Template 与 JSX 的好坏，但为了更透彻的理解 Vue 与 React，需要抛开 JSX&amp;Template，Mutable&amp;Immutable 去看，其实去掉这两个框架无关的技术选型，React@16 与 Vue@3 已经非常像了。</p>
<blockquote>
<p>Vue3.0 的精髓是学习了 React Hooks 概念，因此正好可以用 Hooks 在 React 中模拟 Vue 的 setup 函数。</p>
</blockquote>
<p>关于这两套技术选型，已经是相对完美的组合，不建议在 JSX 中再实现类似 Mutable + JSX 的花样来（因为喜欢 Mutable 可以用 Vue 呀）：</p>
<ul>
<li>Vue：Mutable + Template</li>
<li>React：Immutable + JSX</li>
</ul>
<p>真正影响编码习惯的就是 Mutable 与 Immutable，使用 Vue 就坚定使用 Mutable，使用 React 就坚定使用 Immutable，这样能最大程度发挥两套框架的价值。</p>
<h2 id="Vue-Hooks-与-React-Hooks-的差异"><a href="#Vue-Hooks-与-React-Hooks-的差异" class="headerlink" title="Vue Hooks 与 React Hooks 的差异"></a>Vue Hooks 与 React Hooks 的差异</h2><p>先看 React Hooks 的简单语法：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> [ count, setCount ] = useState(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> setToOne = <span class="function"><span class="params">()</span> =&gt;</span> setCount(<span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<p>Vue Hooks 的简单语法：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> count = value(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> setToOne = <span class="function"><span class="params">()</span> =&gt;</span> count.value = <span class="number">1</span></span><br></pre></td></tr></table></figure>

<p>之所以 React 返回的 <code>count</code> 是一个数字，是因为 Immutable 规则，而 Vue 返回的 <code>count</code> 是个对象，拥有 <code>count.value</code> 属性，也是因为 Vue Mutable 规则导致，这使得 Vue 定义的所有变量都类似 React 中 <code>useRef</code> 定义变量，因此不存 React <code>capture value</code> 的特性。</p>
<blockquote>
<p>关于 capture value 更多信息，可以阅读 <a href="https://github.com/dt-fe/weekly/blob/v2/095.%E7%B2%BE%E8%AF%BB%E3%80%8AFunction%20VS%20Class%20%E7%BB%84%E4%BB%B6%E3%80%8B.md#capture-props" target="_blank" rel="noopener">精读《Function VS Class 组件》 Capute Value 介绍</a></p>
</blockquote>
<p>另外，对于 Hooks 的值变更机制也不同，我们看 Vue 的代码：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Component = &#123;</span><br><span class="line">  setup() &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; x, y &#125; = useMouse()</span><br><span class="line">    <span class="keyword">const</span> &#123; z &#125; = useOtherLogic()</span><br><span class="line">    <span class="keyword">return</span> &#123; x, y, z &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  template: <span class="string">`&lt;div&gt;&#123;&#123; x &#125;&#125; &#123;&#123; y &#125;&#125; &#123;&#123; z &#125;&#125;&lt;/div&gt;`</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于 <code>setup</code> 函数仅执行一次，怎么做到当 <code>useMouse</code> 导致 <code>x</code>、<code>y</code> 值变化时，可以在 <code>setup</code> 中拿到最新的值？</p>
<p>在 React 中，<code>useMouse</code> 如果修改了 <code>x</code> 的值，那么使用 <code>useMouse</code> 的函数就会被重新执行，以此拿到最新的 <code>x</code>，而在 Vue 中，将 Hooks 与 Mutable 深度结合，通过包装 <code>x.value</code>，使得当 <code>x</code> 变更时，引用保持不变，仅值发生了变化。所以 Vue 利用 Proxy 监听机制，可以做到 <code>setup</code> 函数不重新执行，但 Template 重新渲染的效果。</p>
<p>这就是 Mutable 的好处，Vue Hooks 中，不需要 <code>useMemo</code> <code>useCallback</code> <code>useRef</code> 等机制，仅需一个 <code>value</code> 函数，直观的 Mutable 修改，就可以实现 React 中一套 Immutable 性能优化后的效果，这个是 Mutable 的魅力所在。</p>
<h2 id="Vue-Hooks-的优势"><a href="#Vue-Hooks-的优势" class="headerlink" title="Vue Hooks 的优势"></a>Vue Hooks 的优势</h2><p>笔者对 RFC 中对 Vue、React Hooks 的对比做一个延展解释：</p>
<p>首先最大的不同：<code>setup</code> 仅执行一遍，而 React Function Component 每次渲染都会执行。</p>
<p><strong>Vue 的代码使用更符合 JS 直觉。</strong></p>
<p>这句话直截了当戳中了 JS 软肋，JS 并非是针对 Immutable 设计的语言，所以 Mutable 写法非常自然，而 Immutable 的写法就比较别扭。</p>
<p>当 Hooks 要更新值时，Vue 只要用等于号赋值即可，而 React Hooks 需要调用赋值函数，<strong>当对象类型复杂时，还需借助第三方库才能保证进行了正确的 Immutable 更新。</strong></p>
<p><strong>对 Hooks 使用顺序无要求，而且可以放在条件语句里。</strong></p>
<p>对 React Hooks 而言，调用必须放在最前面，而且不能被包含在条件语句里，这是因为 React Hooks 采用下标方式寻找状态，一旦位置不对或者 Hooks 放在了条件中，就无法正确找到对应位置的值。</p>
<p>而 Vue Function API 中的 Hooks 可以放在任意位置、任意命名、被条件语句任意包裹的，因为其并不会触发 <code>setup</code> 的更新，只在需要的时候更新自己的引用值即可，而 Template 的重渲染则完全继承 Vue 2.0 的依赖收集机制，它不管值来自哪里，只要用到的值变了，就可以重新渲染了。</p>
<p><strong>不会再每次渲染重复调用，减少 GC 压力。</strong></p>
<p>这确实是 React Hooks 的一个问题，所有 Hooks 都在渲染闭包中执行，每次重渲染都有一定性能压力，而且频繁的渲染会带来许多闭包，虽然可以依赖 GC 机制回收，但会给 GC 带来不小的压力。</p>
<p>而 Vue Hooks 只有一个引用，所以存储的内容就非常精简，也就是占用内存小，而且当值变化时，也不会重新触发 <code>setup</code> 的执行，所以确实不会造成 GC 压力。</p>
<p><strong>必须要总包裹 <code>useCallback</code> 函数确保不让子元素频繁重渲染。</strong></p>
<p>React Hooks 有一个问题，就是完全依赖 Immutable 属性。<strong>而在 Function Component 内部创建函数时，每次都会创建一个全新的对象，这个对象如果传给子组件，必然导致子组件无法做性能优化。</strong> 因此 React 采取了 <code>useCallback</code> 作为优化方案：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> fn = useCallback(<span class="function"><span class="params">()</span> =&gt;</span> <span class="comment">/* .. */</span>, [])</span><br></pre></td></tr></table></figure>

<p>只有当第二个依赖参数变化时才返回新引用。但第二个依赖参数需要 lint 工具确保依赖总是正确的（关于为何要对依赖诚实，感兴趣可以移步 <a href="https://github.com/dt-fe/weekly/blob/v2/104.%E7%B2%BE%E8%AF%BB%E3%80%8AFunction%20Component%20%E5%85%A5%E9%97%A8%E3%80%8B.md#%E6%B0%B8%E8%BF%9C%E5%AF%B9%E4%BE%9D%E8%B5%96%E9%A1%B9%E8%AF%9A%E5%AE%9E" target="_blank" rel="noopener">精读《Function Component 入门》 - 永远对依赖诚实</a>）。</p>
<p>回到 Vue 3.0，由于 <code>setup</code> 仅执行一次，因此函数本身只会创建一次，不存在多实例问题，不需要 <code>useCallback</code> 的概念，更不需要使用 <a href="https://www.npmjs.com/package/eslint-plugin-react-hooks" target="_blank" rel="noopener">lint 插件</a> 保证依赖书写正确，这对开发者是实实在在的友好。</p>
<p><strong>不需要使用 <code>useEffect</code> <code>useMemo</code> 等进行性能优化，所有性能优化都是自动的。</strong></p>
<p>这也是实在话，毕竟 Mutable + 依赖自动收集就可以做到最小粒度的精确更新，根本不会触发不必要的 Rerender，因此 <code>useMemo</code> 这个概念也不需要了。</p>
<p>而 <code>useEffect</code> 也需要传递第二个参数 “依赖项”，在 Vue 中根本不需要传递 “依赖项”，所以也不会存在用户不小心传错的问题，更不需要像 React 写一个 lint 插件保证依赖的正确性。（这也是笔者想对 React Hooks 吐槽的点，React 团队如何保障每个人都安装了 lint？就算装了 lint，如果 IDE 有 BUG，导致没有生效，随时可能写出依赖不正确的 “危险代码”，造成比如死循环等严重后果）</p>
<h1 id="3-总结"><a href="#3-总结" class="headerlink" title="3. 总结"></a>3. 总结</h1><p>通过对比 Vue Hooks 与 React Hooks 可以发现，Vue 3.0 将 Mutable 特性完美与 Hooks 结合，规避了一些 React Hooks 的硬伤。所以我们可以说 Vue 借鉴了 React Hooks 的思想，但创造出来的确实一个更精美的艺术品。</p>
<p>但 React Hooks 遵循的 Immutable 也有好的一面，就是每次渲染中状态被稳定的固化下来了，不用担心状态突然变更带来的影响（其实反而要注意状态用不变更带来的影响），对于数据记录、程序运行的稳定性都有较高的可预期性。</p>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>Vue</tag>
      </tags>
  </entry>
  <entry>
    <title>重新思考 Redux</title>
    <url>/2019/07/06/article-react-redux-think/</url>
    <content><![CDATA[<h2 id="1-引言"><a href="#1-引言" class="headerlink" title="1 引言"></a>1 引言</h2><p>《重新思考 Redux》是 <a href="https://github.com/rematch/rematch" target="_blank" rel="noopener">rematch</a> 作者 <a href="https://github.com/ShMcK" target="_blank" rel="noopener">Shawn McKay</a> 写的一篇干货软文。</p>
<p><a href="https://github.com/dvajs/dva" target="_blank" rel="noopener">dva</a> 之后，有许多基于 redux 的状态管理框架，但大部分都很局限，甚至是倒退。但直到看到了 rematch，总算觉得 redux 社区又进了一步。</p>
<p>这篇文章的宝贵之处在于，抛开 Mobx、RXjs 概念，仅针对 redux 做深入的重新思考，对大部分还在使用 redux 的工程场景非常有帮助。</p>
<a id="more"></a>
<h2 id="2-概述"><a href="#2-概述" class="headerlink" title="2 概述"></a>2 概述</h2><p>比较新颖的是，作者给出一个公式，评价一个框架或工具的质量：</p>
<p><code>工具质量 = 工具节省的时间/使用工具消耗的时间</code></p>
<p>如果这样评估原生的 redux，我们会发现，使用 redux 需要额外花费的时间可能超过了其节省下来的时间，从这个角度看，redux 是会降低工作效率的。</p>
<p>但 redux 的数据管理思想是正确的，复杂的前端项目也确实需要这种理念，为了更有效率的使用 redux，我们需要使用基于 redux 的框架。作者从 6 个角度阐述了基于 redux 的框架需要解决什么问题。</p>
<h3 id="简化初始化"><a href="#简化初始化" class="headerlink" title="简化初始化"></a>简化初始化</h3><p>redux 初始化代码涉及的概念比较多，比如 <code>compose</code> <code>thunk</code> 等等，同时将 <code>reducer</code>、<code>initialState</code>、<code>middlewares</code> 这三个重要概念拆分成了函数方式调用，而不是更容易接受的配置方式：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> store = <span class="function"><span class="params">preloadedState</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> createStore(</span><br><span class="line">    rootReducer,</span><br><span class="line">    preloadedState,</span><br><span class="line">    compose(applyMiddleware(thunk, api), DevTools.instrument())</span><br><span class="line">  );</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如果换成配置方式，理解成本会降低不少：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> store = <span class="keyword">new</span> Redux.Store(&#123;</span><br><span class="line">  instialState: &#123;&#125;,</span><br><span class="line">  reducers: &#123; count &#125;,</span><br><span class="line">  middlewares: [api, devTools]</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>笔者注：redux 的初始化方式非常函数式，而下面的配置方式就更面向对象一些。相比之下，还是面向对象的方式更好理解，毕竟 store 是一个对象。<code>instialState</code> 也存在同样问题，相比显示申明，将 <code>preloadedState</code> 作为函数入参就比较抽象了，同时 redux 对初始 state 的赋值也比较隐蔽，<code>createStore</code> 时统一赋值比较别扭，因为 reducers 是分散的，如果在 reducers 中赋值，要利用 es 的默认参数特性，看起来更像业务思考，而不是 redux 提供的能力。</p>
</blockquote>
<h3 id="简化-Reducers"><a href="#简化-Reducers" class="headerlink" title="简化 Reducers"></a>简化 Reducers</h3><p>redux 的 reducer 粒度太大，不但导致函数内手动匹配 <code>type</code>，还带来了 <code>type</code>、<code>payload</code> 等理解成本：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> countReducer = <span class="function">(<span class="params">state, action</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">switch</span> (action.type) &#123;</span><br><span class="line">    <span class="keyword">case</span> INCREMENT:</span><br><span class="line">      <span class="keyword">return</span> state + action.payload;</span><br><span class="line">    <span class="keyword">case</span> DECREMENT:</span><br><span class="line">      <span class="keyword">return</span> state - action.payload;</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">      <span class="keyword">return</span> state;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如果用配置的方式设置 reducers，就像定义一个对象一样，会更清晰：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> countReducer = &#123;</span><br><span class="line">  INCREMENT: <span class="function">(<span class="params">state, action</span>) =&gt;</span> state + action.payload,</span><br><span class="line">  DECREMENT: <span class="function">(<span class="params">state, action</span>) =&gt;</span> state - action.payload</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="支持-async-await"><a href="#支持-async-await" class="headerlink" title="支持 async/await"></a>支持 async/await</h3><p>redux 支持动态数据还是挺费劲的，需要理解高阶函数，理解中间件的使用方式，否则你不会知道为什么这样写是对的：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> incrementAsync = <span class="function"><span class="params">count</span> =&gt;</span> <span class="keyword">async</span> dispatch =&gt; &#123;</span><br><span class="line">  <span class="keyword">await</span> delay();</span><br><span class="line">  dispatch(increment(count));</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>为什么不抹掉理解成本，直接允许 <code>async</code> 类型的 action 呢？</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> incrementAsync = <span class="keyword">async</span> count =&gt; &#123;</span><br><span class="line">  <span class="keyword">await</span> delay();</span><br><span class="line">  dispatch(increment(count));</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>笔者注：我们发现 rematch 的方式，dispatch 是 import 进来的（全局变量），而 redux 的 dispatch 是注入进来的，乍一看似乎 redux 更合理，但其实我更推崇 rematch 的方案。经过长期实践，组件最好不要使用数据流，项目的数据流只用一个实例完全够用了，全局 dispatch 的设计其实更合理，而注入 dispatch 的设计看似追求技术极致，但忽略了业务使用场景，导致画蛇添足，增加了不必要的麻烦。</p>
</blockquote>
<h3 id="将-action-reducer-改为两种-action"><a href="#将-action-reducer-改为两种-action" class="headerlink" title="将 action + reducer 改为两种 action"></a>将 action + reducer 改为两种 action</h3><p>redux 抽象的 action 与 reducer 的指责很清晰，action 负责改 store 以外所有事，而 reducer 负责改 store，偶尔用来做数据处理。这种概念其实比较模糊，因为往往不清楚数据处理放在 action 还是 reducer 里，同时过于简单的 reducer 又要写 action 与之匹配，感觉过于形式化，而且繁琐。</p>
<p>重新考虑这个问题，我们只有两类 action：<code>reducer action</code> 与 <code>effect action</code>。</p>
<ul>
<li>reducer action：改变 store。</li>
<li>effect action：处理异步场景，能调用其他 action，不能修改 store。</li>
</ul>
<p>同步的场景，一个 reducer 函数就能处理，只有异步场景需要 <code>effect action</code> 处理掉异步部分，同步部分依然交给 reducer 函数，这两种 action 职责更清晰。</p>
<h3 id="不再显示申明-action-type"><a href="#不再显示申明-action-type" class="headerlink" title="不再显示申明 action type"></a>不再显示申明 action type</h3><p>不要在用一个文件存储 Action 类型了，<code>const ACTION_ONE = &#39;ACTION_ONE&#39;</code> 其实重复写了一遍字符串，直接用对象的 key 表示 action 的值，再加上 store 的 name 为前缀保证唯一性即可。</p>
<p>同时 redux 建议使用 <code>payload</code> key 来传值，那为什么不强制使用 <code>payload</code> 作为入参，而要通过 <code>action.payload</code> 取值呢？直接使用 <code>payload</code> 不但视觉上减少代码数量，容易理解，同时也强制约束了代码风格，让建议真正落地。</p>
<h3 id="Reducer-直接作为-ActionCreator"><a href="#Reducer-直接作为-ActionCreator" class="headerlink" title="Reducer 直接作为 ActionCreator"></a>Reducer 直接作为 ActionCreator</h3><p>redux 调用 action 比较繁琐，使用 <code>dispatch</code> 或者将 reducer 经过 <code>ActionCreator</code> 函数包装。为什么不直接给 reducer 自动包装 <code>ActionCreator</code> 呢？减少样板代码，让每一行代码都有业务含义。</p>
<p>最后作者给出了一个 <code>rematch</code> 完整的例子：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; init, dispatch &#125; <span class="keyword">from</span> <span class="string">"@rematch/core"</span>;</span><br><span class="line"><span class="keyword">import</span> delay <span class="keyword">from</span> <span class="string">"./makeMeWait"</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> count = &#123;</span><br><span class="line">  state: <span class="number">0</span>,</span><br><span class="line">  reducers: &#123;</span><br><span class="line">    increment: <span class="function">(<span class="params">state, payload</span>) =&gt;</span> state + payload,</span><br><span class="line">    decrement: <span class="function">(<span class="params">state, payload</span>) =&gt;</span> state - payload</span><br><span class="line">  &#125;,</span><br><span class="line">  effects: &#123;</span><br><span class="line">    <span class="keyword">async</span> incrementAsync(payload) &#123;</span><br><span class="line">      <span class="keyword">await</span> delay();</span><br><span class="line">      <span class="keyword">this</span>.increment(payload);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> store = init(&#123;</span><br><span class="line">  models: &#123; count &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">dispatch.count.incrementAsync(<span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<h2 id="3-精读"><a href="#3-精读" class="headerlink" title="3 精读"></a>3 精读</h2><p>我觉得本文基本上把 redux 存在的工程问题分析透彻了，同时还给出了一套非常好的实现。</p>
<h3 id="细节的极致优化"><a href="#细节的极致优化" class="headerlink" title="细节的极致优化"></a>细节的极致优化</h3><p>首先是直接使用 <code>payload</code> 而不是整个 <code>action</code> 作为入参，加强了约束同时简化代码复杂度:</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line">increment: <span class="function">(<span class="params">state, payload</span>) =&gt;</span> state + payload;</span><br></pre></td></tr></table></figure>

<p>其次使用 <code>async</code> 在 effects 函数中，使用 <code>this.increment</code> 函数调用方式，取代 <code>put({type: &quot;increment&quot;})</code>（dva），在 typescript 中拥有了类型支持，不但可以用自动跳转代替字符串搜索，还能校验参数类型，在 redux 框架中非常难得。</p>
<p>最后在 <code>dispatch</code> 函数，也提供了两种调用方式：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line">dispatch(&#123; <span class="keyword">type</span>: <span class="string">"count/increment"</span>, payload: <span class="number">1</span> &#125;);</span><br><span class="line">dispatch.count.increment(<span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>如果为了更好的类型支持，或者屏蔽 <code>payload</code> 概念，可以使用第二种方案，再一次简化 redux 概念。</p>
<h3 id="内置了比较多的插件"><a href="#内置了比较多的插件" class="headerlink" title="内置了比较多的插件"></a>内置了比较多的插件</h3><p>rematch 将常用的 reselect、persist、immer 等都集成为了插件，相对比较强化插件生态的概念。数据流对数据缓存，性能优化，开发体验优化都有进一步施展的空间，拥抱插件生态是一个良好的发展方向。</p>
<p>比如 <a href="https://github.com/rematch/rematch/blob/master/plugins/immer/README.md" target="_blank" rel="noopener">rematch-immer</a> 插件，可以用 mutable 的方式修改 store：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> count = &#123;</span><br><span class="line">  state: <span class="number">0</span>,</span><br><span class="line">  reducers: &#123;</span><br><span class="line">    add(state) &#123;</span><br><span class="line">      state += <span class="number">1</span>;</span><br><span class="line">      <span class="keyword">return</span> state;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>但是当 state 为非对象时，immer 将不起作用，所以最好能养成 <code>return state</code> 的习惯。</p>
<p>最后说一点瑕疵的地方，<code>reducers</code> 申明与调用参数不一致。</p>
<h3 id="Reducers-申明与调用参数不一致"><a href="#Reducers-申明与调用参数不一致" class="headerlink" title="Reducers 申明与调用参数不一致"></a>Reducers 申明与调用参数不一致</h3><p>比如下面的 reducers：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> count = &#123;</span><br><span class="line">  state: <span class="number">0</span>,</span><br><span class="line">  reducers: &#123;</span><br><span class="line">    increment: <span class="function">(<span class="params">state, payload</span>) =&gt;</span> state + payload,</span><br><span class="line">    decrement: <span class="function">(<span class="params">state, payload</span>) =&gt;</span> state - payload</span><br><span class="line">  &#125;,</span><br><span class="line">  effects: &#123;</span><br><span class="line">    <span class="keyword">async</span> incrementAsync(payload) &#123;</span><br><span class="line">      <span class="keyword">await</span> delay();</span><br><span class="line">      <span class="keyword">this</span>.increment(payload);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>定义时 <code>increment</code> 是两个参数，而 <code>incrementAsync</code> 调用它时，只有一个参数，这样可能造成一些误导，笔者建议保持参数对应关系，将 <code>state</code> 放在 <code>this</code> 中：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> count = &#123;</span><br><span class="line">  state: <span class="number">0</span>,</span><br><span class="line">  reducers: &#123;</span><br><span class="line">    increment: <span class="function"><span class="params">payload</span> =&gt;</span> <span class="keyword">this</span>.state + payload,</span><br><span class="line">    decrement: <span class="function"><span class="params">payload</span> =&gt;</span> <span class="keyword">this</span>.state - payload</span><br><span class="line">  &#125;,</span><br><span class="line">  effects: &#123;</span><br><span class="line">    <span class="keyword">async</span> incrementAsync(payload) &#123;</span><br><span class="line">      <span class="keyword">await</span> delay();</span><br><span class="line">      <span class="keyword">this</span>.increment(payload);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>当然 rematch 的方式保持了函数的无副作性质，可以看出是做了一些取舍。</p>
<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4 总结"></a>4 总结</h2><p>重复一下作者提出工具质量的公式：</p>
<p><code>工具质量 = 工具节省的时间/使用工具消耗的时间</code></p>
<p>如果一个工具能节省开发时间，但本身带来了很大使用成本，在想清楚如何减少使用成本之前，不要急着用在项目中，这是我得到的最大启发。</p>
<p>最后感谢 rematch 作者精益求精的精神，给 redux 带来进一步的极致优化。</p>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
        <tag>Redux</tag>
      </tags>
  </entry>
  <entry>
    <title>如何安全地使用 React context</title>
    <url>/2019/06/22/article-react-c-context/</url>
    <content><![CDATA[<h2 id="1-引言"><a href="#1-引言" class="headerlink" title="1 引言"></a>1 引言</h2><p>在 React 源码中，context 始终存在，却在 React 0.14 的官方文档中才有所体现。在目前最新的官方文档中，仍不建议使用 context，也表明 context 是一个实验性的 API，在未来 React 版本中可能被更改。那么哪些场景下需要用到 context，而哪些情况下应该避免使用，context 又有什么坑呢？让我们一起来讨论一下。</p>
<a id="more"></a>
<h2 id="2-内容概要"><a href="#2-内容概要" class="headerlink" title="2 内容概要"></a>2 内容概要</h2><p>React context 可以把数据直接传递给组件树的底层组件，而无需中间组件的参与。Redux 作者 Dan Abramov 为 contenxt 的使用总结了一些注意事项：</p>
<ul>
<li>如果你是一个库的作者，需要将信息传递给深层次组件时，context 在一些情况下可能无法更新成功。</li>
<li>如果是界面主题、本地化信息，context 被应用于不易改变的全局变量，可以提供一个高阶组件，以便在 API 更新时只需修改一处。</li>
<li>如果库需要你使用 context，请它提供高阶组件给你。</li>
</ul>
<p>正如 Dan 第一条所述，在 React issue 中，经常能找到 React.PureComponent、shouldComponentUpdate 与包含 Context 的库结合后引发的一些问题。原因在于 shouldComponentUpdate 会切断子树的 rerender，当 state 或 props 没有发生变化时，可能意外中断上层 context 传播。也就是当 shouldComponentUpdate 返回 false 时，context 的变化是无法被底层所感知的。</p>
<p>因此，我们认为 context 应该是不变的，在构造时只接受 context 一次，使用 context，应类似于依赖注入系统来进行。结合精读文章的示例总结一下思路，不变的 context 中包含可变的元素，元素的变化触发自身的监听器实现底层组件的更新，从而绕过 shouldComponentUpdate。</p>
<p>最后作者提出了 Mobx 下的两种解决方案。context 中的可变元素可用 observable 来实现，从而避免上述事件监听器编写，因为 observable 会帮你完成元素改变后的响应。当然 Provider + inject 也可以完成，具体可参考精读文章中的代码。</p>
<h2 id="3-精读"><a href="#3-精读" class="headerlink" title="3 精读"></a>3 精读</h2><p>本次提出独到观点的同学有：<br><a href="https://www.zhihu.com/people/turbe-xue" target="_blank" rel="noopener">@monkingxue</a> <a href="https://github.com/alcat2008" target="_blank" rel="noopener">@alcat2008</a> <a href="https://www.zhihu.com/people/huang-zi-yi-83" target="_blank" rel="noopener">@ascoders</a>，精读由此归纳。</p>
<h3 id="context-的使用场景"><a href="#context-的使用场景" class="headerlink" title="context 的使用场景"></a>context 的使用场景</h3><blockquote>
<p>In some cases, you want to pass data through the component tree without having to pass the props down manually at every level.</p>
</blockquote>
<p>context 的本质在于为组件树提供一种跨层级通信的能力，原本在 React 只能通过 props 逐层传递数据，而 context 打破了这一层束缚。</p>
<p>context 虽然不被建议使用，但在一些流行库中却非常常见，例如：<a href="https://github.com/reactjs/react-redux" target="_blank" rel="noopener">react-redux</a>、<a href="https://github.com/ReactTraining/react-router" target="_blank" rel="noopener">react-router</a>。究其原因，我认为是单一顶层与多样底层间不是单纯父子关系的结果。例如：react-redux 中的 Provider，react-router 中的 Router，均在顶层控制 store 信息与路由信息。而对于 Connect 与 Route 而言，它们在 view 中的层级是多样化的，通过 context 获取顶层 Provider 与 Router 中的相关信息再合适不过。</p>
<h3 id="context-的坑"><a href="#context-的坑" class="headerlink" title="context 的坑"></a>context 的坑</h3><ul>
<li>context 相当于一个全局变量，难以追溯数据源，很难找到是在哪个地方中对 context 进行了更新。</li>
<li>组件中依赖 context，会使组件耦合度提高，既不利于组件复用，也不利于组件测试。</li>
<li>当 props 改变或是 setState 被调用，getChildContext 也会被调用，生成新的 context，但 shouldComponentUpdate 返回的 false 会 block 住 context，导致没有更新，这也是精读文章的重点内容。</li>
</ul>
<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4 总结"></a>4 总结</h2><p>正如精读文章开头所说，context 是一个非常强大的，具有很多免责声明的特性，就像伊甸园中的禁果。的确，引入全局变量似乎是应用混乱的开始，而 context 与 props/state 相比也实属异类。在业务代码中，我们应抵制使用 context，而在框架和库中可结合场景适当使用，相信 context 也并非洪水猛兽。</p>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>React Hooks</title>
    <url>/2019/05/19/article-react-hooks-intro/</url>
    <content><![CDATA[<h2 id="1-引言"><a href="#1-引言" class="headerlink" title="1 引言"></a>1 引言</h2><p>React Hooks 是 React <code>16.7.0-alpha</code> 版本推出的新特性，想尝试的同学安装此版本即可。</p>
<p><strong>React Hooks 要解决的问题是状态共享</strong>，是继 <a href="https://reactjs.org/docs/render-props.html" target="_blank" rel="noopener">render-props</a> 和 <a href="https://reactjs.org/docs/higher-order-components.html" target="_blank" rel="noopener">higher-order components</a> 之后的第三种状态共享方案，不会产生 JSX 嵌套地狱问题。</p>
<p>状态共享可能描述的不恰当，称为<strong>状态逻辑复用</strong>会更恰当，因为只共享数据处理逻辑，不会共享数据本身。</p>
<p>为了更快理解 React Hooks 是什么，先看下面一段 renderProps 代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function App() &#123;</span><br><span class="line">  return (</span><br><span class="line">    &lt;Toggle initial=&#123;false&#125;&gt;</span><br><span class="line">      &#123;(&#123; on, toggle &#125;) =&gt; (</span><br><span class="line">        &lt;Button type=&quot;primary&quot; onClick=&#123;toggle&#125;&gt; Open Modal &lt;/Button&gt;</span><br><span class="line">        &lt;Modal visible=&#123;on&#125; onOk=&#123;toggle&#125; onCancel=&#123;toggle&#125; /&gt;</span><br><span class="line">      )&#125;</span><br><span class="line">    &lt;/Toggle&gt;</span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>恰巧，React Hooks 解决的也是这个问题：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function App() &#123;</span><br><span class="line">  const [open, setOpen] = useState(false);</span><br><span class="line">  return (</span><br><span class="line">    &lt;&gt;</span><br><span class="line">      &lt;Button type=&quot;primary&quot; onClick=&#123;() =&gt; setOpen(true)&#125;&gt;</span><br><span class="line">        Open Modal</span><br><span class="line">      &lt;/Button&gt;</span><br><span class="line">      &lt;Modal</span><br><span class="line">        visible=&#123;open&#125;</span><br><span class="line">        onOk=&#123;() =&gt; setOpen(false)&#125;</span><br><span class="line">        onCancel=&#123;() =&gt; setOpen(false)&#125;</span><br><span class="line">      /&gt;</span><br><span class="line">    &lt;/&gt;</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到，React Hooks 就像一个内置的打平 renderProps 库，我们可以随时创建一个值，与修改这个值的方法。看上去像 function 形式的 setState，其实这等价于依赖注入，与使用 setState 相比，<strong>这个组件是没有状态的</strong>。</p>
<a id="more"></a>
<h2 id="2-概述"><a href="#2-概述" class="headerlink" title="2 概述"></a>2 概述</h2><p>React Hooks 带来的好处不仅是 “更 FP，更新粒度更细，代码更清晰”，还有如下三个特性：</p>
<ol>
<li>多个状态不会产生嵌套，写法还是平铺的（renderProps 可以通过 compose 解决，可不但使用略为繁琐，而且因为强制封装一个新对象而增加了实体数量）。</li>
<li>Hooks 可以引用其他 Hooks。</li>
<li>更容易将组件的 UI 与状态分离。</li>
</ol>
<p>第二点展开说一下：Hooks 可以引用其他 Hooks，我们可以这么做：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import &#123; useState, useEffect &#125; from &quot;react&quot;;</span><br><span class="line"></span><br><span class="line">// 底层 Hooks, 返回布尔值：是否在线</span><br><span class="line">function useFriendStatusBoolean(friendID) &#123;</span><br><span class="line">  const [isOnline, setIsOnline] = useState(null);</span><br><span class="line"></span><br><span class="line">  function handleStatusChange(status) &#123;</span><br><span class="line">    setIsOnline(status.isOnline);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  useEffect(() =&gt; &#123;</span><br><span class="line">    ChatAPI.subscribeToFriendStatus(friendID, handleStatusChange);</span><br><span class="line">    return () =&gt; &#123;</span><br><span class="line">      ChatAPI.unsubscribeFromFriendStatus(friendID, handleStatusChange);</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line">  return isOnline;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 上层 Hooks，根据在线状态返回字符串：Loading... or Online or Offline</span><br><span class="line">function useFriendStatusString(props) &#123;</span><br><span class="line">  const isOnline = useFriendStatusBoolean(props.friend.id);</span><br><span class="line"></span><br><span class="line">  if (isOnline === null) &#123;</span><br><span class="line">    return &quot;Loading...&quot;;</span><br><span class="line">  &#125;</span><br><span class="line">  return isOnline ? &quot;Online&quot; : &quot;Offline&quot;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 使用了底层 Hooks 的 UI</span><br><span class="line">function FriendListItem(props) &#123;</span><br><span class="line">  const isOnline = useFriendStatusBoolean(props.friend.id);</span><br><span class="line"></span><br><span class="line">  return (</span><br><span class="line">    &lt;li style=&#123;&#123; color: isOnline ? &quot;green&quot; : &quot;black&quot; &#125;&#125;&gt;&#123;props.friend.name&#125;&lt;/li&gt;</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 使用了上层 Hooks 的 UI</span><br><span class="line">function FriendListStatus(props) &#123;</span><br><span class="line">  const status = useFriendStatusString(props);</span><br><span class="line"></span><br><span class="line">  return &lt;li&gt;&#123;status&#125;&lt;/li&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个例子中，有两个 Hooks：<code>useFriendStatusBoolean</code> 与 <code>useFriendStatusString</code>, <code>useFriendStatusString</code> 是利用 <code>useFriendStatusBoolean</code> 生成的新 Hook，这两个 Hook 可以给不同的 UI：<code>FriendListItem</code>、<code>FriendListStatus</code> 使用，而因为两个 Hooks 数据是联动的，因此两个 UI 的状态也是联动的。</p>
<p>顺带一提，这个例子也可以用来理解 <a href="https://zhuanlan.zhihu.com/p/48264713" target="_blank" rel="noopener">对 React Hooks 的一些思考</a> 一文的那句话：<strong>“有状态的组件没有渲染，有渲染的组件没有状态”</strong>：</p>
<ul>
<li><code>useFriendStatusBoolean</code> 与 <code>useFriendStatusString</code> 是有状态的组件（使用 <code>useState</code>），没有渲染（返回非 UI 的值），这样就可以作为 <strong>Custom Hooks</strong> 被任何 UI 组件调用。</li>
<li><code>FriendListItem</code> 与 <code>FriendListStatus</code> 是有渲染的组件（返回了 JSX），没有状态（没有使用 <code>useState</code>），这就是一个纯函数 UI 组件，</li>
</ul>
<h3 id="利用-useState-创建-Redux"><a href="#利用-useState-创建-Redux" class="headerlink" title="利用 useState 创建 Redux"></a>利用 useState 创建 Redux</h3><p>Redux 的精髓就是 Reducer，而利用 React Hooks 可以轻松创建一个 Redux 机制：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 这就是 Redux</span><br><span class="line">function useReducer(reducer, initialState) &#123;</span><br><span class="line">  const [state, setState] = useState(initialState);</span><br><span class="line"></span><br><span class="line">  function dispatch(action) &#123;</span><br><span class="line">    const nextState = reducer(state, action);</span><br><span class="line">    setState(nextState);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  return [state, dispatch];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个自定义 Hook 的 value 部分当作 redux 的 state，setValue 部分当作 redux 的 dispatch，合起来就是一个 redux。而 react-redux 的 connect 部分做的事情与 Hook 调用一样：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 一个 Action</span><br><span class="line">function useTodos() &#123;</span><br><span class="line">  const [todos, dispatch] = useReducer(todosReducer, []);</span><br><span class="line"></span><br><span class="line">  function handleAddClick(text) &#123;</span><br><span class="line">    dispatch(&#123; type: &quot;add&quot;, text &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  return [todos, &#123; handleAddClick &#125;];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 绑定 Todos 的 UI</span><br><span class="line">function TodosUI() &#123;</span><br><span class="line">  const [todos, actions] = useTodos();</span><br><span class="line">  return (</span><br><span class="line">    &lt;&gt;</span><br><span class="line">      &#123;todos.map((todo, index) =&gt; (</span><br><span class="line">        &lt;div&gt;&#123;todo.text&#125;&lt;/div&gt;</span><br><span class="line">      ))&#125;</span><br><span class="line">      &lt;button onClick=&#123;actions.handleAddClick&#125;&gt;Add Todo&lt;/button&gt;</span><br><span class="line">    &lt;/&gt;</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>useReducer</code> 已经作为一个内置 Hooks 了，在这里可以查阅所有 <a href="https://reactjs.org/docs/hooks-reference.html" target="_blank" rel="noopener">内置 Hooks</a>。</p>
<p>不过这里需要注意的是，每次 <code>useReducer</code> 或者自己的 Custom Hooks 都不会持久化数据，所以比如我们创建两个 App，App1 与 App2:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function App1() &#123;</span><br><span class="line">  const [todos, actions] = useTodos();</span><br><span class="line"></span><br><span class="line">  return &lt;span&gt;todo count: &#123;todos.length&#125;&lt;/span&gt;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">function App2() &#123;</span><br><span class="line">  const [todos, actions] = useTodos();</span><br><span class="line"></span><br><span class="line">  return &lt;span&gt;todo count: &#123;todos.length&#125;&lt;/span&gt;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">function All() &#123;</span><br><span class="line">  return (</span><br><span class="line">    &lt;&gt;</span><br><span class="line">      &lt;App1 /&gt;</span><br><span class="line">      &lt;App2 /&gt;</span><br><span class="line">    &lt;/&gt;</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这两个实例同时渲染时，并不是共享一个 todos 列表，而是分别存在两个独立 todos 列表。也就是 React Hooks 只提供状态处理方法，不会持久化状态。</p>
<p>如果要真正实现一个 Redux 功能，也就是全局维持一个状态，任何组件 <code>useReducer</code> 都会访问到同一份数据，可以和 <a href="https://reactjs.org/docs/hooks-reference.html#usecontext" target="_blank" rel="noopener">useContext</a> 一起使用。</p>
<p>大体思路是利用 <code>useContext</code> 共享一份数据，作为 Custom Hooks 的数据源。具体实现可以参考 <a href="https://github.com/facebookincubator/redux-react-hook/blob/master/src/index.ts" target="_blank" rel="noopener">redux-react-hook</a>。</p>
<h3 id="利用-useEffect-代替一些生命周期"><a href="#利用-useEffect-代替一些生命周期" class="headerlink" title="利用 useEffect 代替一些生命周期"></a>利用 useEffect 代替一些生命周期</h3><p>在 useState 位置附近，可以使用 useEffect 处理副作用：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">useEffect(() =&gt; &#123;</span><br><span class="line">  const subscription = props.source.subscribe();</span><br><span class="line">  return () =&gt; &#123;</span><br><span class="line">    // Clean up the subscription</span><br><span class="line">    subscription.unsubscribe();</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p><code>useEffect</code> 的代码既会在初始化时候执行，也会在后续每次 rerender 时执行，而返回值在析构时执行。这个更多带来的是便利，对比一下 React 版 G2 调用流程：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class Component extends React.PureComponent&lt;Props, State&gt; &#123;</span><br><span class="line">  private chart: G2.Chart = null;</span><br><span class="line">  private rootDomRef: React.ReactInstance = null;</span><br><span class="line"></span><br><span class="line">  componentDidMount() &#123;</span><br><span class="line">    this.rootDom = ReactDOM.findDOMNode(this.rootDomRef) as HTMLDivElement;</span><br><span class="line"></span><br><span class="line">    this.chart = new G2.Chart(&#123;</span><br><span class="line">      container: document.getElementById(&quot;chart&quot;),</span><br><span class="line">      forceFit: true,</span><br><span class="line">      height: 300</span><br><span class="line">    &#125;);</span><br><span class="line">    this.freshChart(this.props);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  componentWillReceiveProps(nextProps: Props) &#123;</span><br><span class="line">    this.freshChart(nextProps);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  componentWillUnmount() &#123;</span><br><span class="line">    this.chart.destroy();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  freshChart(props: Props) &#123;</span><br><span class="line">    // do something</span><br><span class="line">    this.chart.render();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    return &lt;div ref=&#123;ref =&gt; (this.rootDomRef = ref)&#125; /&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>用 React Hooks 可以这么做：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function App() &#123;</span><br><span class="line">  const ref = React.useRef(null);</span><br><span class="line">  let chart: G2.Chart = null;</span><br><span class="line"></span><br><span class="line">  React.useEffect(() =&gt; &#123;</span><br><span class="line">    chart = new G2.Chart(&#123;</span><br><span class="line">      container: ReactDOM.findDOMNode(ref.current) as HTMLDivElement,</span><br><span class="line">      width: 500,</span><br><span class="line">      height: 500</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    // do something</span><br><span class="line">    chart.render();</span><br><span class="line"></span><br><span class="line">    return () =&gt; chart.destroy();</span><br><span class="line">  &#125;, []);</span><br><span class="line"></span><br><span class="line">  return &lt;div ref=&#123;ref&#125; /&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到将细碎的代码片段结合成了一个完整的代码块，更易维护。</p>
<p>现在介绍了 <code>useState</code> <code>useContext</code> <code>useEffect</code> <code>useRef</code> 等常用 hooks，更多可以查阅：<a href="https://reactjs.org/docs/hooks-reference.html" target="_blank" rel="noopener">内置 Hooks</a>，相信不久的未来，这些 API 又会成为一套新的前端规范。</p>
<h3 id="Hooks-带来的约定"><a href="#Hooks-带来的约定" class="headerlink" title="Hooks 带来的约定"></a>Hooks 带来的约定</h3><p>Hook 函数必须以 “use” 命名开头，因为这样才方便 eslint 做检查，防止用 condition 判断包裹 useHook 语句。</p>
<p>为什么不能用 condition 包裹 useHook 语句，详情可以见 <a href="https://reactjs.org/docs/hooks-rules.html#explanation" target="_blank" rel="noopener">官方文档</a>，这里简单介绍一下。</p>
<p>React Hooks 并不是通过 Proxy 或者 getters 实现的（具体可以看这篇文章 <a href="https://medium.com/@ryardley/react-hooks-not-magic-just-arrays-cd4f1857236e" target="_blank" rel="noopener">React hooks: not magic, just arrays</a>），而是通过数组实现的，每次 <code>useState</code> 都会改变下标，如果 <code>useState</code> 被包裹在 condition 中，那每次执行的下标就可能对不上，导致 <code>useState</code> 导出的 <code>setter</code> 更新错数据。</p>
<p>虽然有 <a href="https://www.npmjs.com/package/eslint-plugin-react-hooks" target="_blank" rel="noopener">eslint-plugin-react-hooks</a> 插件保驾护航，但这第一次将 “约定优先” 理念引入了 React 框架中，带来了前所未有的<strong>代码命名和顺序限制</strong>（函数命名遭到官方限制，JS 自由主义者也许会暴跳如雷），但带来的便利也是前所未有的（没有比 React Hooks 更好的状态共享方案了，约定带来提效，自由的代价就是回到 renderProps or HOC，各团队可以自行评估）。</p>
<p>笔者认为，React Hooks 的诞生，也许来自于这个灵感：“不如通过增加一些约定，彻底解决状态共享问题吧！”</p>
<blockquote>
<p>React 约定大于配置脚手架 <a href="https://github.com/zeit/next.js" target="_blank" rel="noopener">nextjs</a> <a href="https://github.com/umijs/umi" target="_blank" rel="noopener">umi</a> 以及笔者的 <a href="https://github.com/prijs/pri" target="_blank" rel="noopener">pri</a> 都通过有 “约定路由” 的功能，大大降低了路由配置复杂度，<strong>那么 React Hooks 就像代码级别的约定</strong>，大大降低了代码复杂度。</p>
</blockquote>
<h3 id="状态与-UI-的界限会越来越清晰"><a href="#状态与-UI-的界限会越来越清晰" class="headerlink" title="状态与 UI 的界限会越来越清晰"></a>状态与 UI 的界限会越来越清晰</h3><p>因为 React Hooks 的特性，如果一个 Hook 不产生 UI，那么它可以永远被其他 Hook 封装，虽然允许有副作用，但是被包裹在 <code>useEffect</code> 里，总体来说还是挺函数式的。而 Hooks 要集中在 UI 函数顶部写，也很容易养成书写无状态 UI 组件的好习惯，践行 “状态与 UI 分开” 这个理念会更容易。</p>
<p>不过这个理念稍微有点蹩脚的地方，那就是 “状态” 到底是什么。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function App() &#123;</span><br><span class="line">  const [count, setCount] = useCount();</span><br><span class="line">  return &lt;span&gt;&#123;count&#125;&lt;/span&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们知道 <code>useCount</code> 算是无状态的，因为 React Hooks 本质就是 renderProps 或者 HOC 的另一种写法，换成 renderProps 就好理解了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;Count&gt;&#123;(count, setCount) =&gt; &lt;App count=&#123;count&#125; setCount=&#123;setCount&#125; /&gt;&#125;&lt;/Count&gt;;</span><br><span class="line"></span><br><span class="line">function App(props) &#123;</span><br><span class="line">  return &lt;span&gt;&#123;props.count&#125;&lt;/span&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到 App 组件是无状态的，输出完全由输入（Props）决定。</p>
<p>那么有状态无 UI 的组件就是 <code>useCount</code> 了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function useCount() &#123;</span><br><span class="line">  const [count, setCount] = useState(0);</span><br><span class="line">  return [count, setCount];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>有状态的地方应该指 <code>useState(0)</code> 这句，不过这句和无状态 UI 组件 App 的 <code>useCount()</code> 很像，既然 React 把 <code>useCount</code> 成为自定义 Hook，那么 <code>useState</code> 就是官方 Hook，具有一样的定义，因此可以认为 <code>useCount</code> 是无状态的，<code>useState</code> 也是一层 renderProps，最终的状态其实是 <code>useState</code> 这个 React 内置的组件。</p>
<p>我们看 renderProps 嵌套的表达：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;UseState&gt;</span><br><span class="line">  &#123;(count, setCount) =&gt; (</span><br><span class="line">    &lt;UseCount&gt;</span><br><span class="line">      &#123;&quot; &quot;&#125;</span><br><span class="line">      &#123;/**虽然是透传，但给 count 做了去重，不可谓没有作用 */&#125;</span><br><span class="line">      &#123;(count, setCount) =&gt; &lt;App count=&#123;count&#125; setCount=&#123;setCount&#125; /&gt;&#125;</span><br><span class="line">    &lt;/UseCount&gt;</span><br><span class="line">  )&#125;</span><br><span class="line">&lt;/UseState&gt;</span><br></pre></td></tr></table></figure>

<p>能确定的是，App 一定有 UI，而上面两层父级组件一定没有 UI。为了最佳实践，我们尽量避免 App 自己维护状态，而其父级的 RenderProps 组件可以维护状态（也可以不维护状态，做个二传手）。因此可以考虑在 “有状态的组件没有渲染，有渲染的组件没有状态” 这句话后面加一句：没渲染的组件也可以没状态。</p>
<h2 id="3-总结"><a href="#3-总结" class="headerlink" title="3 总结"></a>3 总结</h2><p>把 React Hooks 当作更便捷的 RenderProps 去用吧，虽然写法看上去是内部维护了一个状态，但其实等价于注入、Connect、HOC、或者 renderProps，那么如此一来，使用 renderProps 的门槛会大大降低，因为 Hooks 用起来实在是太方便了，我们可以抽象大量 Custom Hooks，让代码更加 FP，同时也不会增加嵌套层级。</p>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>React 高阶组件</title>
    <url>/2019/05/18/article-react-hoc/</url>
    <content><![CDATA[<h1 id="1-引言"><a href="#1-引言" class="headerlink" title="1 引言"></a>1 引言</h1><p>高阶组件（ higher-order component ，HOC ）是 React 中复用组件逻辑的一种进阶技巧。它本身并不是 React 的 API，而是一种 React 组件的设计理念，众多的 React 库已经证明了它的价值，例如耳熟能详的 react-redux。</p>
<p>高阶组件的概念其实并不难，我们能通过类比高阶函数迅速掌握。高阶函数是把函数作为参数传入到函数中并返回一个新的函数。这里我们把函数替换为组件，就是高阶组件了。</p>
<p><code>const EnhancedComponent = higherOrderComponent(WrappedComponent);</code></p>
<p>当然了解高阶组件的概念只是万里长征第一步，精读文章在阐述其概念与实现外，也强调了其重要性与局限性，以及与其他方案的比较，让我们一起来领略吧。</p>
<a id="more"></a>
<h1 id="2-内容概要"><a href="#2-内容概要" class="headerlink" title="2 内容概要"></a>2 内容概要</h1><p>高阶组件常见有两种实现方式，一种是 Props Proxy，它能够对 WrappedComponent 的 props 进行操作，提取 WrappedComponent state 以及使用其他元素来包裹 WrappedComponent。Props Proxy 作为一层代理，具有隔离的作用，因此传入 WrappedComponent 的 ref 将无法访问到其本身，需要在 Props Proxy 内完成中转，具体可参考以下代码，react-redux 也是这样实现的。</p>
<p>此外各个 Props Proxy 的默认名称是相同的，需要根据 WrappedComponent 来进行不同命名。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ppHOC</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">PP</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 实现 HOC 不同的命名</span></span><br><span class="line">    <span class="keyword">static</span> displayName = <span class="string">`HOC(<span class="subst">$&#123;WrappedComponent.displayName&#125;</span>)`</span>;</span><br><span class="line"></span><br><span class="line">    getWrappedInstance() &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">this</span>.wrappedInstance;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 实现 ref 的访问</span></span><br><span class="line">    setWrappedInstance(ref) &#123;</span><br><span class="line">      <span class="keyword">this</span>.wrappedInstance = ref;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> &#123;</span></span></span><br><span class="line"><span class="xml">        ...this.props,</span></span><br><span class="line"><span class="xml">        ref: this.setWrappedInstance.bind(this),</span></span><br><span class="line"><span class="xml">      &#125; /&gt;</span></span><br><span class="line"><span class="xml">    &#125;</span></span><br><span class="line"><span class="xml">  &#125;</span></span><br><span class="line"><span class="xml">&#125;</span></span><br><span class="line"><span class="xml"></span></span><br><span class="line"><span class="xml">@ppHOC</span></span><br><span class="line"><span class="xml">class Example extends React.Component &#123;</span></span><br><span class="line"><span class="xml">  static displayName = 'Example';</span></span><br><span class="line"><span class="xml">  handleClick() &#123; ... &#125;</span></span><br><span class="line"><span class="xml">  ...</span></span><br><span class="line"><span class="xml">&#125;</span></span><br><span class="line"><span class="xml"></span></span><br><span class="line"><span class="xml">class App extends React.Component &#123;</span></span><br><span class="line"><span class="xml">  handleClick() &#123;</span></span><br><span class="line"><span class="xml">    this.refs.example.getWrappedInstance().handleClick();</span></span><br><span class="line"><span class="xml">  &#125;</span></span><br><span class="line"><span class="xml">  render() &#123;</span></span><br><span class="line"><span class="xml">    return (</span></span><br><span class="line">      &lt;div&gt;</span><br><span class="line">        &lt;button onClick=&#123;this.handleClick.bind(this)&#125;&gt;按钮&lt;/button&gt;</span><br><span class="line">        &lt;Example ref="example" /&gt;</span><br><span class="line">      &lt;/div&gt;  </span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>另一种是 Inheritance Inversion，HOC 类继承了 WrappedComponent，意味着可以访问到 WrappedComponent 的 state、props、生命周期和 render 等方法。如果在 HOC 中定义了与 WrappedComponent 同名方法，将会发生覆盖，就必须手动通过 super 进行调用了。通过完全操作 WrappedComponent 的 render 方法返回的元素树，可以真正实现渲染劫持。这种方案依然是继承的思想，对于 WrappedComponent 也有较强的侵入性，因此并不常见。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ppHOC</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">ExampleEnhance</span> <span class="keyword">extends</span> <span class="title">WrappedComponent</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    componentDidMount() &#123;</span><br><span class="line">      <span class="keyword">super</span>.componentDidMount();</span><br><span class="line">    &#125;</span><br><span class="line">    componentWillUnmount() &#123;</span><br><span class="line">      <span class="keyword">super</span>.componentWillUnmount();</span><br><span class="line">    &#125;</span><br><span class="line">    render() &#123;</span><br><span class="line">      ...</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">super</span>.render();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="3-精读"><a href="#3-精读" class="headerlink" title="3 精读"></a>3 精读</h1><p>本次提出独到观点的同学有：<br><a href="https://www.zhihu.com/people/turbe-xue" target="_blank" rel="noopener">@monkingxue</a> <a href="https://github.com/alcat2008" target="_blank" rel="noopener">@alcat2008</a> <a href="https://www.zhihu.com/people/BlackGanglion" target="_blank" rel="noopener">@淡苍</a> <a href="https://www.zhihu.com/people/camsong" target="_blank" rel="noopener">@camsong</a>，精读由此归纳。</p>
<h2 id="HOC-的适用范围"><a href="#HOC-的适用范围" class="headerlink" title="HOC 的适用范围"></a>HOC 的适用范围</h2><p>对比 HOC 范式 <code>compose(render)(state)</code> 与父组件（Parent Component）的范式 <code>render(render(state))</code>，如果完全利用 HOC 来实现 React 的 implement，将操作与 view 分离，也未尝不可，但却不优雅。HOC 本质上是统一功能抽象，强调逻辑与 UI 分离。但在实际开发中，前端无法逃离 DOM ，而逻辑与 DOM 的相关性主要呈现 3 种关联形式：</p>
<ul>
<li>与 DOM 相关，建议使用父组件，类似于原生 HTML 编写</li>
<li>与 DOM 不相关，如校验、权限、请求发送、数据转换这类，通过数据变化间接控制 DOM，可以使用 HOC 抽象</li>
<li>交叉的部分，DOM 相关，但可以做到完全内聚，即这些 DOM 不会和外部有关联，均可</li>
</ul>
<p>DOM 的渲染适合使用父组件，这是 React JSX 原生支持的方式，清晰易懂。最好是能封装成木偶组件（Dumb Component）。HOC 适合做 DOM 不相关又是多个组件共性的操作。如 Form 中，validator 校验操作就是纯数据操作的，放到了 HOC 中。但 validator 信息没有放到 HOC 中。但如果能把 Error 信息展示这些逻辑能够完全隔离，也可以放到 HOC 中（可结合下一小节 Form 具体实践详细了解）。<br>数据请求是另一类 DOM 不相关的场景，<a href="https://github.com/heroku/react-refetch" target="_blank" rel="noopener">react-refetch</a> 的实现就是使用了 HOC，做到了高效和优雅：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">connect(<span class="function"><span class="params">props</span> =&gt;</span> (&#123;</span><br><span class="line">  usersFetch: <span class="string">`/users?status=<span class="subst">$&#123;props.status&#125;</span>&amp;page=<span class="subst">$&#123;props.page&#125;</span>`</span>,</span><br><span class="line">  userStatsFetch: &#123; <span class="attr">url</span>: <span class="string">`/users/stats`</span>, <span class="attr">force</span>: <span class="literal">true</span> &#125;</span><br><span class="line">&#125;))(UsersList)</span><br></pre></td></tr></table></figure>

<h2 id="HOC-的具体实践"><a href="#HOC-的具体实践" class="headerlink" title="HOC 的具体实践"></a>HOC 的具体实践</h2><p>HOC 在真实场景下的运行非常多，之前笔者在 <a href="https://zhuanlan.zhihu.com/p/22054582" target="_blank" rel="noopener">基于Decorator的组件扩展实践</a> 一文中也提过使用高阶组件将更细粒度的组件组合成 Selector 与 Search。结合精读文章，这次让我们通过 Form 组件的抽象来表现 HOC 具有的良好扩展机制。</p>
<p>Form 中会包含各种不同的组件，常见的有 Input、Selector、Checkbox 等等，也会有根据业务需求加入的自定义组件。Form 灵活多变，从功能上看，表单校验可能为单组件值校验，也可能为全表单值校验，可能为常规检验，比如：非空、输入限制，也可能需要与服务端配合，甚至需要根据业务特点进行定制。从 UI 上看，检验结果显示的位置，可能在组件下方，也可能是在组件右侧。</p>
<p>直接裸写 Form，无疑是机械而又重复的。将 Form 中组件的 value 经过 validator，把 value，validator 产生的 error 信息储存到 state 或 redux store 中，然后在 view 层完成显示。这条路大家都是相同的，可以进行复用，只是我们面对的是不同的组件，不同的 validator，不同的 view 而已。对于 Form 而言，既要满足通用，又要满足部分个性化的需求，以往单纯的配置化只会让使用愈加繁琐，我们所需要抽象的是 Form 功能而非 UI，因此通过 HOC 针对 Form 的功能进行提取就成为了必然。</p>
<p><img src="https://user-images.githubusercontent.com/9314735/27116337-3f1f16a8-5103-11e7-8dc6-c7197e1b1eab.png" alt="image"></p>
<p>至于 HOC 在 Form 上的具体实现，首先将表单中的组件（Input、Selector…）与相应 validator 与组件值回调函数名（trigger）传入 Decorator，将 validator 与 trigger 相绑定。Decorator 完成了各种不同组件与 Form 内置 Store 间 value 的传递、校验功能的抽象，即精读文章中提到 Props Proxy 方式的其中两种作用：<strong>提取state</strong> 与 <strong>操作props</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">formFactoryFactory</span>(<span class="params">&#123;</span></span></span><br><span class="line"><span class="function"><span class="params">  validator,</span></span></span><br><span class="line"><span class="function"><span class="params">  trigger = <span class="string">'onChange'</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">  ...</span></span></span><br><span class="line"><span class="function"><span class="params">&#125;</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> FormFactory(WrappedComponent) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">Decorator</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">      getBind(trigger, validator) &#123;</span><br><span class="line">        ...</span><br><span class="line">      &#125;</span><br><span class="line">      render() &#123;</span><br><span class="line">        <span class="keyword">const</span> newProps = &#123;</span><br><span class="line">          ...this.props,</span><br><span class="line">          [trigger]: <span class="keyword">this</span>.getBind(trigger, validator),</span><br><span class="line">          ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> &#123;<span class="attr">...newProps</span>&#125; /&gt;</span></span></span><br><span class="line"><span class="xml">      &#125;</span></span><br><span class="line"><span class="xml">    &#125;</span></span><br><span class="line"><span class="xml">  &#125;</span></span><br><span class="line"><span class="xml">&#125;</span></span><br><span class="line"><span class="xml"></span></span><br><span class="line"><span class="xml">// 调用</span></span><br><span class="line"><span class="xml">formFactoryFactory(&#123;</span></span><br><span class="line"><span class="xml">  validator: (value) =&gt; &#123;</span></span><br><span class="line"><span class="xml">    return value !== '';</span></span><br><span class="line"><span class="xml">  &#125;</span></span><br><span class="line">&#125;)(&lt;Input placeholder="请输入..." /&gt;)</span><br></pre></td></tr></table></figure>

<p>当然为了考虑个性化需求，Form Store 也向外暴露很多 API，可以直接获取和修改 value、error 的值。现在我们需要对一个表单的所有值提交到后端进行校验，根据后端返回，分别列出各项的校验错误信息，就需要借助相应项的 setError 去完成了。</p>
<p>这里主要参考了 <a href="https://github.com/react-component/form" target="_blank" rel="noopener">rc-form</a> 的实现方式，有兴趣的读者可以阅读其源码。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createForm &#125; <span class="keyword">from</span> <span class="string">'rc-form'</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Form</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  submit = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">this</span>.props.form.validateFields(<span class="function">(<span class="params">error, value</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(error, value);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; getFieldError, getFieldDecorator &#125; = <span class="keyword">this</span>.props.form;</span><br><span class="line">    <span class="keyword">const</span> errors = getFieldError(<span class="string">'required'</span>);</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      &lt;div&gt;</span><br><span class="line">        &#123;getFieldDecorator(<span class="string">'required'</span>, &#123;</span><br><span class="line">          rules: [&#123; <span class="attr">required</span>: <span class="literal">true</span> &#125;],</span><br><span class="line">        &#125;)(<span class="xml"><span class="tag">&lt;<span class="name">Input</span> /&gt;</span>)&#125;</span></span><br><span class="line"><span class="xml">        &#123;errors ? errors.join(',') : null&#125;</span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;this.submit&#125;</span>&gt;</span>submit<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> createForm()(Form);</span><br></pre></td></tr></table></figure>

<h1 id="4-总结"><a href="#4-总结" class="headerlink" title="4 总结"></a>4 总结</h1><p>React 始终强调组合优于继承的理念，期望通过复用小组件来构建大组件使得开发变得简单而又高效，与传统面向对象思想是截然不同的。高阶函数（HOC）的出现替代了原有 Mixin 侵入式的方案，对比隐式的 Mixin 或是继承，HOC 能够在 Devtools 中显示出来，满足抽象之余，也方便了开发与测试。当然，不可过度抽象是我们始终要秉持的原则。</p>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>setState 做了什么</title>
    <url>/2019/04/13/article-react-setstate/</url>
    <content><![CDATA[<h1 id="1-引言"><a href="#1-引言" class="headerlink" title="1 引言"></a>1 引言</h1><p><code>setState</code> 是 React 框架最常用的命令，它是用来更新状态的，这也是 React 框架划时代的功能。</p>
<p>但是 <code>setState</code> 函数是 <code>react</code> 包导出的，他们又是如何与 <code>react-dom</code> <code>react-native</code> <code>react-art</code> 这些包结合的呢？</p>
<a id="more"></a>
<h1 id="2-概述"><a href="#2-概述" class="headerlink" title="2 概述"></a>2 概述</h1><p><code>setState</code> 函数是在 <code>React.Component</code> 组件中调用的，所以最自然的联想是，更新 DOM 的逻辑在 <code>react</code> 包中实现。</p>
<p>但是 <code>react</code> 却可以和 <code>react-dom</code> <code>react-native</code> <code>react-art</code> 这些包打配合，甚至与 <code>react-dom/server</code> 配合在服务端运行，那可以肯定 <code>react</code> 包中不含有 DOM 更新逻辑。</p>
<p>所以可以推断，<strong>平台相关的 UI 更新逻辑分布在平台相关的包里，<code>react</code> 包只做了代理。</strong></p>
<h2 id="React-引擎不在-react-包里"><a href="#React-引擎不在-react-包里" class="headerlink" title="React 引擎不在 react 包里"></a>React 引擎不在 react 包里</h2><p>从 react 0.14 版本之后，引擎代码就从 react 包中抽离了，react 包仅仅做通用接口抽象。</p>
<p>也就是说，react 包定义了标准的状态驱动模型的 API，而 <code>react-dom</code> <code>react-native</code> <code>react-art</code> 这些包是在各自平台的具体实现。</p>
<p>各平台具体的渲染引擎实现被称为 <a href="https://github.com/facebook/react/tree/master/packages/react-reconciler" target="_blank" rel="noopener">reconciler</a>，通过这个链接可以看到 <code>react-dom</code> <code>react-native</code> <code>react-art</code> 这三个包的 reconciler 实现。</p>
<p>这说明了 react 包仅告诉你 React 拥有哪些语法，而并不关心如何实现他们，所以我们需要结合 react 包与 react-xxx 一起使用。</p>
<p>对于 <code>context</code>，react 包仅仅会做如下定义：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// A bit simplified</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">createContext</span>(<span class="params">defaultValue</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> context = &#123;</span><br><span class="line">    _currentValue: defaultValue,</span><br><span class="line">    Provider: <span class="literal">null</span>,</span><br><span class="line">    Consumer: <span class="literal">null</span></span><br><span class="line">  &#125;;</span><br><span class="line">  context.Provider = &#123;</span><br><span class="line">    $$<span class="keyword">typeof</span>: <span class="built_in">Symbol</span>.for(<span class="string">"react.provider"</span>),</span><br><span class="line">    _context: context</span><br><span class="line">  &#125;;</span><br><span class="line">  context.Consumer = &#123;</span><br><span class="line">    $$<span class="keyword">typeof</span>: <span class="built_in">Symbol</span>.for(<span class="string">"react.context"</span>),</span><br><span class="line">    _context: context</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> context;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>具体用到时，由 <code>react-dom</code> 和 <code>react-native</code> 决定用何种方式实现 <code>MyContext.Provider</code> 这个 API。</p>
<p>这也说明了，如果你不同步升级 <code>react</code> 与 <code>react-dom</code> 版本的话，就可能碰到这样的报错：<a href="https://stackoverflow.com/questions/49662743/context-components-in-react-16-3-are-invalid/49677020#49677020" target="_blank" rel="noopener">fail saying these types are invalid</a>，原因是 API 定义与实现不匹配。</p>
<h2 id="setState-怎么调用平台实现"><a href="#setState-怎么调用平台实现" class="headerlink" title="setState 怎么调用平台实现"></a>setState 怎么调用平台实现</h2><p>每个平台对 UI 更新逻辑的实现，会封装在 <code>updater</code> 函数里，所以不同平台代码会为组件添加各自的 <code>updater</code> 实现：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Inside React DOM</span></span><br><span class="line"><span class="keyword">const</span> inst = <span class="keyword">new</span> YourComponent();</span><br><span class="line">inst.props = props;</span><br><span class="line">inst.updater = ReactDOMUpdater;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Inside React DOM Server</span></span><br><span class="line"><span class="keyword">const</span> inst = <span class="keyword">new</span> YourComponent();</span><br><span class="line">inst.props = props;</span><br><span class="line">inst.updater = ReactDOMServerUpdater;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Inside React Native</span></span><br><span class="line"><span class="keyword">const</span> inst = <span class="keyword">new</span> YourComponent();</span><br><span class="line">inst.props = props;</span><br><span class="line">inst.updater = ReactNativeUpdater;</span><br></pre></td></tr></table></figure>

<p>不同于 <code>props</code>, <code>updater</code> 无法被直接调用，因为这个 API 是由 react 引擎在 <code>setState</code> 时调用的：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// A bit simplified</span></span><br><span class="line">setState(partialState, callback) &#123;</span><br><span class="line">  <span class="comment">// Use the `updater` field to talk back to the renderer!</span></span><br><span class="line">  <span class="keyword">this</span>.updater.enqueueSetState(<span class="keyword">this</span>, partialState, callback);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关系可以这么描述：<code>react</code> -&gt; setState -&gt; updater &lt;- <code>react-dom</code> 等。</p>
<h2 id="Hooks"><a href="#Hooks" class="headerlink" title="Hooks"></a>Hooks</h2><p>Hooks 的原理与 <code>setState</code> 类似，当调用 <code>useState</code> 或 <code>useEffect</code> 时，其内部调用如下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// In React (simplified a bit)</span></span><br><span class="line"><span class="keyword">const</span> React = &#123;</span><br><span class="line">  <span class="comment">// Real property is hidden a bit deeper, see if you can find it!</span></span><br><span class="line">  __currentDispatcher: <span class="literal">null</span>,</span><br><span class="line"></span><br><span class="line">  useState(initialState) &#123;</span><br><span class="line">    <span class="keyword">return</span> React.__currentDispatcher.useState(initialState);</span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  useEffect(initialState) &#123;</span><br><span class="line">    <span class="keyword">return</span> React.__currentDispatcher.useEffect(initialState);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>ReactDOM 提供了 <code>__currentDispatcher</code>（简化的说法）:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// In React DOM</span></span><br><span class="line"><span class="keyword">const</span> prevDispatcher = React.__currentDispatcher;</span><br><span class="line">React.__currentDispatcher = ReactDOMDispatcher;</span><br><span class="line"><span class="keyword">let</span> result;</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  result = YourComponent(props);</span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">  <span class="comment">// Restore it back</span></span><br><span class="line">  React.__currentDispatcher = prevDispatcher;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到，Hooks 的原理与 <code>setState</code> 基本一致，但需要注意 <code>react</code> 与 <code>react-dom</code> 之间传递了 <code>dispatch</code>，虽然你看不到。但这个 <code>dispatch</code> 必须对应到唯一的 React 实例，这就是为什么 Hooks 不允许同时加载多个 React 实例的原因。</p>
<p>和 <code>updater</code> 一样，<code>dispatch</code> 也可以被各平台实现重写，比如 <code>react-debug-hooks</code> 就重写了 <a href="https://github.com/facebook/react/blob/ce43a8cd07c355647922480977b46713bd51883e/packages/react-debug-tools/src/ReactDebugHooks.js#L203-L214" target="_blank" rel="noopener">dispatcher</a>。</p>
<p>由于需要同时实现 <code>readContext</code>, <code>useCallback</code>, <code>useContext</code>, <code>useEffect</code>, <code>useImperativeMethods</code>, <code>useLayoutEffect</code>, <code>useMemo</code>, <code>useReducer</code>, <code>useRef</code>, <code>useState</code>，工程量比较浩大，建议了解基本架构就足够了，除非你要深入参与 React 生态建设。</p>
<h1 id="3-精读"><a href="#3-精读" class="headerlink" title="3 精读"></a>3 精读</h1><p>与其他 React 分析文章不同，本文并没有过于刨根问题的上来就剖析 <a href="https://github.com/facebook/react/tree/master/packages/react-reconciler" target="_blank" rel="noopener">reconciler</a> 实现，而是问了一个最基本的疑问：为什么 <code>setState</code> 来自 <code>react</code> 包，但实现却在 <code>react-dom</code> 里？React 是如何实现这个 magic 的？</p>
<p>通过这个疑问，我们了解了 React 更上层的抽象能力，如何用一个包制定规范，用 N 包去实现它。</p>
<h2 id="接口的力量"><a href="#接口的力量" class="headerlink" title="接口的力量"></a>接口的力量</h2><p>在日常编程中，接口也拥有的强大力量，下面举几个例子。</p>
<h3 id="UI-组件跨三端的接口"><a href="#UI-组件跨三端的接口" class="headerlink" title="UI 组件跨三端的接口"></a>UI 组件跨三端的接口</h3><p>由于 RN、Weex、Flutter 的某些不足，越来越多的人选择 “一个思想三端实现” 的方式做跨三端的 UI 组件，这样既兼顾了性能，又可以照顾到平台差异性，对不同平台组件细节做定制优化。</p>
<p>要实施这个方案，最大问题就是接口约定。一定要保证三套实现遵循同一套 API 接口，业务代码才可以实现 “针对任意一个平台编写，自动移植到其他平台”。</p>
<p>比较常用的做法是，通过一套统一的 API 文件约束，固定组件的输入输出，不同平台的组件做平台具体实现。这个思想和 React 如出一辙。</p>
<blockquote>
<p>当然 RN 这些框架本身也是同一接口在不同平台实现的典型，只是做的不够彻底，JS 与 Native 的通信导致了性能不如原生。</p>
</blockquote>
<h3 id="通用数据查询服务"><a href="#通用数据查询服务" class="headerlink" title="通用数据查询服务"></a>通用数据查询服务</h3><p>通用数据查询服务也比较流行，通过磨平各数据库语法，让用户通过一套 SQL 查询各种类型数据库的数据。</p>
<p>这个方案中，一套通用的查询语法就类似 React 定义的 API，执行阶段会转化为各数据库平台的 SQL 方言。</p>
<h3 id="小程序融合方案"><a href="#小程序融合方案" class="headerlink" title="小程序融合方案"></a>小程序融合方案</h3><p>现在这种方案很火。通过基于 template 或者 jsx 的语法，一键发布到各平台小程序应用。</p>
<p>这种方案一定会抽象一套通用语法，甚至几乎等价与 <code>react</code> 与 <code>react-dom</code> 的关系：所有符合规范的语法，转化为各小程序平台的实现。</p>
<h1 id="4-总结"><a href="#4-总结" class="headerlink" title="4 总结"></a>4 总结</h1><p>这种分平台实现方案与跨平台方案还是有很大区别的，像 JAVA 虚拟机本质还是一套实现方案。而分平台的实现可以带来最原生的性能与体验，同样收到的约束也最大，应该其 API 应该是所有平台支持的一个子集。</p>
<p>另外，这种方案不仅可以用于 一套规范，不同平台的实现，甚至可以用在 “同一平台的实现”。</p>
<p>无论是公司还是开源节界，都有许多重复的轮子或者平台，如果通过技术委员会约定一套平台的实现规范，大家都遵循这个规范开发平台，那未来就比较好做收敛，或者说收敛的第一步都是先统一 API 规范。</p>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>React 的多态性</title>
    <url>/2019/03/09/article-react-surprising-polymorphism/</url>
    <content><![CDATA[<h2 id="1-引言"><a href="#1-引言" class="headerlink" title="1 引言"></a>1 引言</h2><p>参考的文章是：<a href="https://medium.com/@bmeurer/surprising-polymorphism-in-react-applications-63015b50abc" target="_blank" rel="noopener">surprising-polymorphism-in-react-applications</a>，看看作者是如何解释这个多态性含义的。</p>
<p>读完文章才发现，文章标题改为 Redux 的多态性更妥当，因为整篇文章都在说 Redux，而 Redux 使用场景不局限于 React。</p>
<a id="more"></a>
<h2 id="2-概述"><a href="#2-概述" class="headerlink" title="2 概述"></a>2 概述</h2><p>Redux immutable 特性可能产生浏览器无法优化的性能问题，也就是浏览器无法做 <a href="https://github.com/dt-fe/weekly/blob/master/62.%E7%B2%BE%E8%AF%BB%E3%80%8AJS%20%E5%BC%95%E6%93%8E%E5%9F%BA%E7%A1%80%E4%B9%8B%20Shapes%20and%20Inline%20Caches%E3%80%8B.md#shapes" target="_blank" rel="noopener">shapes 优化</a>，也就是上一篇精读《JS 引擎基础之 Shapes and Inline Caches》 里提到的。</p>
<p>先看看普通的 redux 的 reducer：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> todo = <span class="function">(<span class="params">state = &#123;&#125;, action</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">switch</span> (action.type) &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">"ADD_TODO"</span>:</span><br><span class="line">      <span class="keyword">return</span> &#123;</span><br><span class="line">        id: action.id,</span><br><span class="line">        text: action.text,</span><br><span class="line">        completed: <span class="literal">false</span></span><br><span class="line">      &#125;;</span><br><span class="line">    <span class="keyword">case</span> <span class="string">"TOGGLE_TODO"</span>:</span><br><span class="line">      <span class="keyword">if</span> (state.id !== action.id) &#123;</span><br><span class="line">        <span class="keyword">return</span> state;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">return</span> <span class="built_in">Object</span>.assign(&#123;&#125;, state, &#123;</span><br><span class="line">        completed: !state.completed</span><br><span class="line">      &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">      <span class="keyword">return</span> state;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>我们简化一下使用场景，假设基于这个 reducer <code>todo</code>，生成了两个新 store <code>s1</code> <code>s2</code>：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> s1 = todo(</span><br><span class="line">  &#123;&#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">type</span>: <span class="string">"ADD_TODO"</span>,</span><br><span class="line">    id: <span class="number">1</span>,</span><br><span class="line">    text: <span class="string">"Finish blog post"</span></span><br><span class="line">  &#125;</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> s2 = todo(s1, &#123;</span><br><span class="line">  <span class="keyword">type</span>: <span class="string">"TOGGLE_TODO"</span>,</span><br><span class="line">  id: <span class="number">1</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>看上去很常见，也的确如此，我们每次 dispatch 都会根据 reducer 生成新的 store 树，而且是一个新的对象。然而对 js 引擎而言，这样的代码可能做不了 Shapes 优化（关于 Shapes 优化建议阅读上一期精读 <a href="https://github.com/dt-fe/weekly/blob/master/62.%E7%B2%BE%E8%AF%BB%E3%80%8AJS%20%E5%BC%95%E6%93%8E%E5%9F%BA%E7%A1%80%E4%B9%8B%20Shapes%20and%20Inline%20Caches%E3%80%8B.md#shapes" target="_blank" rel="noopener">Shapes 优化</a>），也就是最需要做优化的全局 store，在生成新 store 时无法被浏览器优化，这个问题很容易被忽视，但的确影响不小。</p>
<p>至于为什么会阻止 js 引擎的 shapes 优化，看下面的代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// transition-trees.js</span></span><br><span class="line"><span class="keyword">let</span> a = &#123;<span class="attr">x</span>:<span class="number">1</span>, <span class="attr">y</span>:<span class="number">2</span>, <span class="attr">z</span>:<span class="number">3</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = &#123;&#125;;</span><br><span class="line">b.x = <span class="number">1</span>;</span><br><span class="line">b.y = <span class="number">2</span>;</span><br><span class="line">b.z = <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"a is"</span>, a);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"b is"</span>, b);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"a and b have same map:"</span>, %HaveSameMap(a, b));</span><br></pre></td></tr></table></figure>

<p>通过 <code>node --allow-natives-syntax test.js</code> 执行，通过调用 node 原生函数 <code>%HaveSameMap</code> 判断这种情况下 <code>a</code> 与 <code>b</code> 是否共享一个 shape（v8 引擎的 Shape 实现称为 Map）。</p>
<p><img src="https://user-images.githubusercontent.com/7970947/42121947-089a1796-7c6c-11e8-89de-3eaaf81eb02e.png" alt="image"></p>
<p>结果是 <code>false</code>，也就是 js 引擎无法对 <code>a</code> <code>b</code> 做 Shapes 优化，这是因为 <code>a</code> 与 <code>b</code> 对象初始化的方式不同。</p>
<p>同样，在 Redux 代码中常用的 <code>Object.assign</code> 也有这个问题：</p>
<p><img src="https://user-images.githubusercontent.com/7970947/42121964-55d0f3ae-7c6c-11e8-9d6e-995cac2f83d3.png" alt="image"></p>
<p>因为新的对象以 <code>{}</code> 空对象作为最初状态，js 引擎会为新对象创建 Empty Shape，这与原对象的 Shape 一定不同。</p>
<p>顺带一提 es6 的解构语法也存在同样的问题，因为 <code>babel</code> 将解构最终解析为 <code>Object.assign</code>：</p>
<p><img src="https://user-images.githubusercontent.com/7970947/42121971-9c0bb8d6-7c6c-11e8-85ed-e15f27654d5b.png" alt="image"></p>
<p>对这种尴尬的情况，作者的建议是对所有对象赋值时都是用 <code>Object.assign</code> 以保证 js 引擎可以做 Shapes 优化：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = <span class="built_in">Object</span>.assign(&#123;&#125;, &#123;<span class="attr">x</span>:<span class="number">1</span>, <span class="attr">y</span>:<span class="number">2</span>, <span class="attr">z</span>:<span class="number">3</span>&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="built_in">Object</span>.assign(&#123;&#125;, a);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"a is"</span>, a);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"b is"</span>, b);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"a and b have same map:"</span>, %HaveSameMap(a, b)); <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h2 id="3-精读"><a href="#3-精读" class="headerlink" title="3 精读"></a>3 精读</h2><p>这篇文章需要与上一篇 <a href="https://github.com/dt-fe/weekly/blob/master/62.%E7%B2%BE%E8%AF%BB%E3%80%8AJS%20%E5%BC%95%E6%93%8E%E5%9F%BA%E7%A1%80%E4%B9%8B%20Shapes%20and%20Inline%20Caches%E3%80%8B.md" target="_blank" rel="noopener">精读《JS 引擎基础之 Shapes and Inline Caches》</a> 连起来看容易理解。</p>
<p>作者描述的性能问题是引擎级别的 Shapes 优化问题，读过上篇精读就很容易知道，只有相同初始化方式的对象才被 js 引擎做优化，而 Redux 频繁生成的 immutable 全局 store 是否能被优化呢？答案是“往往不能”，因为 immutable 赋值问题，我们往往采用 <code>Object.assign</code> 或者解构方式赋值，这种方式产生的新对象与原对象的 Shape 不同，导致 Shape 无法复用。</p>
<p>这里解释一下疑惑，为什么说 immutable 对象之间也要优化呢？这不是两个不同的引用吗？这是因为 js 引擎级别的 Shapes 优化就是针对不同引用的对象，将对象的结构：Shape 与数据分离开，这样可以大幅优化存储效率，对数组也一样，上一篇精读有详细介绍。</p>
<p>所以笔者更推荐使用比如 <a href="https://github.com/facebook/immutable-js" target="_blank" rel="noopener">immutable-js</a> 这种库操作 immutable 对象，而不是 Object.assign，因为封装库内部是可能通过统一对象初始化方式利用 js 引擎进行优化的。</p>
<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4 总结"></a>4 总结</h2><p>原文提到的多态是指多个相同结构对象，被拆分成了多个 Shape；而单态是指这些对象可以被一个 Shape 复用。</p>
<p>笔者以前也经历过从 <code>Object.assign</code> 到 Immutablejs 库，最后又回到解构新语法的经历，觉得在层级不深情况下解构语法可以代替 Immutablejs 库。</p>
<p>通过最近两篇精读的分析，我们需要重新思考这样做带来的优缺点，因为在 js 环境中，<code>Object.assign</code> 的优化效率比 Immutablejs 库更低。</p>
<p>最后，也完全没必要现在就开始重构，因为这只是 js 运行环境中很小一部分影响因素，比如为了引入 Immutablejs 让你的网络延时增加了 100%？所以仅在有必要的时候优化它。</p>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>React 八种条件渲染</title>
    <url>/2019/02/23/article-react-condition-render/</url>
    <content><![CDATA[<h2 id="1-引言"><a href="#1-引言" class="headerlink" title="1 引言"></a>1 引言</h2><p><a href="https://blog.logrocket.com/conditional-rendering-in-react-c6b0e5af381e" target="_blank" rel="noopener">8 React conditional rendering methods</a></p>
<p>介绍了八种 React 条件渲染方式。</p>
<p>模板条件渲染非常常见，遇到的时候往往会随机选择一种方式使用，那么怎么写会有较好的维护性呢？先一起了解下有哪八种条件渲染方式吧！</p>
<a id="more"></a>
<h2 id="2-概述"><a href="#2-概述" class="headerlink" title="2 概述"></a>2 概述</h2><h3 id="IF-ELSE"><a href="#IF-ELSE" class="headerlink" title="IF/ELSE"></a>IF/ELSE</h3><p>既然 JSX 支持 js 与 html 混写，那么交替使用就能解决条件渲染的问题：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  if (renderComponent1) &#123;</span><br><span class="line">    return &lt;Component1 /&gt;;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    return &lt;div /&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="return-null"><a href="#return-null" class="headerlink" title="return null"></a>return <code>null</code></h3><p>如果不想渲染空元素，最好使用 <code>null</code> 代替空的 <code>div</code>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  if (renderComponent1) &#123;</span><br><span class="line">    return &lt;Component1 /&gt;;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    return null;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样对 React 渲染效率有提升。</p>
<h3 id="组件变量"><a href="#组件变量" class="headerlink" title="组件变量"></a>组件变量</h3><p>将组件赋值到变量，就可以在 return 前任意修改它了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  let component = null;</span><br><span class="line"></span><br><span class="line">  if (renderComponent1) &#123;</span><br><span class="line">    component = &lt;Component1 /&gt;;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  return component;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="三元运算符"><a href="#三元运算符" class="headerlink" title="三元运算符"></a>三元运算符</h3><p><a href="https://en.wikipedia.org/wiki/%3F:" target="_blank" rel="noopener">三元运算符</a>的语法如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">condition ? expr_if_true : expr_if_false</span><br></pre></td></tr></table></figure>

<p>用在 JSX 上也很方便：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  return renderComponent1 ? &lt;Component1 /&gt; : null;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但三元运算符产生嵌套时，理解成本会变得很高。</p>
<h3 id="amp-amp"><a href="#amp-amp" class="headerlink" title="&amp;&amp;"></a>&amp;&amp;</h3><p>这个是最常用了，因为代码量最少。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  return renderComponent1 &amp;&amp; &lt;Component1 /&gt;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="IIFE"><a href="#IIFE" class="headerlink" title="IIFE"></a>IIFE</h3><p>IIFE 含义是立即执行函数，也就是如下代码：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span> <span class="title">myFunction</span>(<span class="params"><span class="comment">/* arguments */</span></span>) </span>&#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;)(<span class="comment">/* arguments */</span>);</span><br></pre></td></tr></table></figure>

<p>当深陷 JSX 代码中，又想写一大块逻辑时，除了回到上方，还可以使用 IIFE：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  return (</span><br><span class="line">    &lt;div&gt;</span><br><span class="line">      &#123;(() =&gt; &#123;</span><br><span class="line">        if (renderComponent1) &#123;</span><br><span class="line">          return &lt;Component1 /&gt;;</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">          return &lt;div /&gt;;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;)()&#125;</span><br><span class="line">    &lt;/div&gt;</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="子组件"><a href="#子组件" class="headerlink" title="子组件"></a>子组件</h3><p>这是 IIFE 的变种，也就是把这段立即执行函数替换成一个普通函数：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  return (</span><br><span class="line">    &lt;div&gt;</span><br><span class="line">      &lt;SubRender /&gt;</span><br><span class="line">    &lt;/div&gt;</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">function SubRender() &#123;</span><br><span class="line">  if (renderComponent1) &#123;</span><br><span class="line">    return &lt;Component1 /&gt;;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    return &lt;div /&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="IF-组件"><a href="#IF-组件" class="headerlink" title="IF 组件"></a>IF 组件</h3><p>做一个条件渲染组件 <code>IF</code> 代替 js 函数的 <code>if</code>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;If condition=&#123;true&#125;&gt;</span><br><span class="line">  &lt;span&gt;Hi!&lt;/span&gt;</span><br><span class="line">&lt;/If&gt;</span><br></pre></td></tr></table></figure>

<p>这个组件实现也很简单</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const If = props =&gt; &#123;</span><br><span class="line">  const condition = props.condition || false;</span><br><span class="line">  const positive = props.then || null;</span><br><span class="line">  const negative = props.else || null;</span><br><span class="line"></span><br><span class="line">  return condition ? positive : negative;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="高阶组件"><a href="#高阶组件" class="headerlink" title="高阶组件"></a>高阶组件</h3><p>高阶组件，就是返回一个新组件的函数，并且接收一个组件作为参数。</p>
<p>那么我们就能在高阶组件里写条件语句，返回不同的组件即可：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function higherOrderComponent(Component) &#123;</span><br><span class="line">  return function EnhancedComponent(props) &#123;</span><br><span class="line">    if (condition) &#123;</span><br><span class="line">      return &lt;AnotherComponent &#123;...props&#125; /&gt;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    return &lt;Component &#123;...props&#125; /&gt;;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-精读"><a href="#3-精读" class="headerlink" title="3 精读"></a>3 精读</h2><p>这么多方法都能实现条件渲染，那么重点在于可读性与可维护性。</p>
<p>比如通过调用函数实现组件渲染：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div&gt;&#123;renderButton()&#125;&lt;/div&gt;</span><br></pre></td></tr></table></figure>

<p>看上去还是比较冗余，如果使用 <code>renderButton</code> getter 定义，我们就可以这么写它：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div&gt;&#123;button&#125;&lt;/div&gt;</span><br></pre></td></tr></table></figure>

<p>其实我们想要的就是 button，而不是 <code>renderButton</code>。那么还可以进一步，干脆封装成 JSX 组件：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div&gt;</span><br><span class="line">  &lt;Button /&gt;</span><br><span class="line">&lt;/div&gt;</span><br></pre></td></tr></table></figure>

<p>是否要付出这些努力，取决于应用的复杂度。如果应用复杂度非常高，那你应当尽量使用最后一种封装，让每个文件的逻辑尽量独立、简单。</p>
<p>如果应用复杂度比较低，那么注意不要过度封装，以免把自己绕进去。</p>
<p>所以看来这又是一个没有固定答案的问题，选择何种方式封装，取决于应用复杂度。</p>
<h3 id="应用复杂度"><a href="#应用复杂度" class="headerlink" title="应用复杂度"></a>应用复杂度</h3><p>对任何代码封装，都会增加这段 <strong>连接逻辑</strong> 的复杂度。</p>
<p>假定无论如何代码的复杂度都是恒定不变的，下面这段代码，连接复杂度为 0，而对于 <code>render</code> 函数而言，逻辑复杂度是 100：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  if (renderComponent) &#123;</span><br><span class="line">    return isOk ? &lt;Component1 /&gt; : &lt;Component2 /&gt;;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    return &lt;div /&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面这段代码拆成了两个函数，逻辑复杂度对 <code>render</code> <code>SubComponent</code> 来说都是 50，但连接复杂度是 50：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">render</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (renderComponent) &#123;</span><br><span class="line">    <span class="keyword">return</span> &lt;SubComponent&gt;;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> &lt;div /&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">SubComponent</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> isOk ? &lt;Component1 /&gt; : &lt;Component2 /&gt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到，我们通过函数拆分，降低了每个函数的逻辑复杂度，但却提高了连接复杂度。</p>
<p>下面来做一个比较，我们假设一个正常的程序员，可以一次性轻松记忆 10 个函数。如果再多，函数之间的调用关系就会让人摸不着头脑。</p>
<h4 id="应用较小时"><a href="#应用较小时" class="headerlink" title="应用较小时"></a>应用较小时</h4><p>在应用代码量比较小时，假设一共有 10 个函数，如果做了逻辑抽象，拆分出了 10 个子函数，那么总逻辑复杂度不变，函数变成了 20 个。</p>
<p>此时小王要修改此项目，他需要找到关键代码的位置。</p>
<p>如果没有做逻辑抽象，小王一下子就记住了 10 个函数，并且很快完成了需求。</p>
<p>如果应用做了逻辑抽象，他需要理解的逻辑复杂度是不变的，但是要读的函数变成了 20 个。小王需要像侦探一样在线索中不断跳转，他还是只找了 10 个关键函数，但一共也就 20 个函数，逻辑并不复杂，这值得吗？</p>
<p>小王心里可能会嘀咕：简单的逻辑瞎抽象，害我文件找了半天！</p>
<h4 id="应用较大时"><a href="#应用较大时" class="headerlink" title="应用较大时"></a>应用较大时</h4><p>此时应用代码量比较大，假设一共有 500 个函数，我们不考虑抽象后带来的复用好处，假设都无法复用，那么做了逻辑抽象后，那么总逻辑复杂度不变，函数变成了 1000 个。</p>
<p>此时小王接到了需求，终于维护了一个大项目。</p>
<p>小王知道这个项目很复杂，从一开始就没觉得能理解项目全貌，所以把自己当作一名侦探，准备一步步探索。</p>
<p>现在有两种选择，一种是在未做逻辑抽象时探索，一种是在做过逻辑抽象后探索。</p>
<p>如果没做逻辑抽象，小王需要面对 <code>500</code> 个这种函数：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  if (renderComponent) &#123;</span><br><span class="line">    return isOk ? &lt;Component1 /&gt; : &lt;Component2 /&gt;;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    return isReady ? &lt;Component3 /&gt; : &lt;Component4 /&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果做了逻辑抽象，小王需要面对 <code>1000</code> 个这种函数：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function render() &#123;</span><br><span class="line">  if (renderComponent) &#123;</span><br><span class="line">    return &lt;Component1And2 /&gt;;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    return &lt;Component3And4 /&gt;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在项目庞大后，总函数数量并不会影响对线索的查找，而总线索深度也几乎总是固定的，一般在 5 层左右。</p>
<p>小王理解 5 个或 10 个函数成本都差不多，但没有做逻辑抽象时，这 5 个函数各自参杂了其他逻辑，反而影响对函数的理解。</p>
<p>这时做逻辑抽象是合适的。</p>
<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4 总结"></a>4 总结</h2><p>所以总的来说，笔者更倾向使用子函数、子组件、IF 组件、高阶组件做条件渲染，因为这四种方式都能提高程序的抽象能力。</p>
<p>往往抽象后的代码会更具有复用性，单个函数逻辑更清晰，在切面编程时更利于理解。</p>
<p>当项目很简单时，整个项目的理解成本都很低，抽象带来的复杂度反而让项目变成了需要切面编程的时候，就得不偿失了。</p>
<p>总结一下：</p>
<ul>
<li>当项目很简单，或者条件渲染的逻辑确认无法复用时，推荐在代码中用 <code>&amp;&amp;</code> 或者三元运算符、IIFE 等直接实现条件渲染。</li>
<li>当项目很复杂时，尽量都使用 子函数、子组件、IF 组件、高阶组件 等方式做更有抽象度的条件渲染。</li>
<li>在做逻辑抽象时，考虑下项目的复杂度，避免因为抽象带来的成本增加，让本可以整体理解的项目变得支离破碎。</li>
</ul>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>React 代码整洁之道</title>
    <url>/2019/01/26/article-react-clean/</url>
    <content><![CDATA[<h2 id="1-引言"><a href="#1-引言" class="headerlink" title="1 引言"></a>1 引言</h2><p>编程也是艺术行为，当我们思考代码复用、变量命名时，就是在进行艺术思考。</p>
<p>可能这篇文章没法提高面试能力、开发效率，因为涉及的内容都是 “软能力”。但如果与我一样，时常害怕自己代码不够优雅，那就在茶余饭后看看这篇文章，也许，可以解决一部分你心中的困惑。</p>
<a id="more"></a>
<h2 id="2-内容概要"><a href="#2-内容概要" class="headerlink" title="2 内容概要"></a>2 内容概要</h2><p>作者整理了几个好的思维习惯，尝试认同它，再看看如何实践。</p>
<h3 id="不冗余"><a href="#不冗余" class="headerlink" title="不冗余"></a>不冗余</h3><p>避免重复代码段，对 JSX 同理：</p>
<figure class="highlight typescript"><figcaption><span>jsx</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// Dirty</span></span><br><span class="line"><span class="keyword">const</span> MyComponent = <span class="function"><span class="params">()</span> =&gt;</span> (</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;OtherComponent <span class="keyword">type</span>=<span class="string">"a"</span> className=<span class="string">"colorful"</span> foo=&#123;<span class="number">123</span>&#125; bar=&#123;<span class="number">456</span>&#125; /&gt;</span><br><span class="line">    &lt;OtherComponent <span class="keyword">type</span>=<span class="string">"b"</span> className=<span class="string">"colorful"</span> foo=&#123;<span class="number">123</span>&#125; bar=&#123;<span class="number">456</span>&#125; /&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">);</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">/</span><span class="regexp">/ Clean</span></span><br><span class="line"><span class="regexp">const MyOtherComponent = (&#123; type &#125;) =&gt; (</span></span><br><span class="line"><span class="regexp">  &lt;OtherComponent type=&#123;type&#125; className="colorful" foo=&#123;123&#125; bar=&#123;456&#125; /</span>&gt;</span><br><span class="line">);</span><br><span class="line"><span class="keyword">const</span> MyComponent = <span class="function"><span class="params">()</span> =&gt;</span> (</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;MyOtherComponent <span class="keyword">type</span>=<span class="string">"a"</span> /&gt;</span><br><span class="line">    &lt;MyOtherComponent <span class="keyword">type</span>=<span class="string">"b"</span> /&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">);</span></span><br></pre></td></tr></table></figure>

<p>但也不要过度优化，过度优化和搞破坏没什么区别。</p>
<h3 id="可预测、可测试"><a href="#可预测、可测试" class="headerlink" title="可预测、可测试"></a>可预测、可测试</h3><p>如果使用 <a href="https://facebook.github.io/jest/" target="_blank" rel="noopener">Jest</a> 测试，可以考虑截图测试插件：<a href="https://github.com/americanexpress/jest-image-snapshot" target="_blank" rel="noopener">Jest Image Snapshot</a></p>
<h3 id="自我解释"><a href="#自我解释" class="headerlink" title="自我解释"></a>自我解释</h3><p>尽可能减少代码中的注释。可以通过让变量名更语义化、只注释复杂、潜在逻辑，来减少注释量，同时也提高了可维护性，毕竟不用总在代码与注释之间同步了。</p>
<figure class="highlight typescript"><figcaption><span>jsx</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// Dirty</span></span><br><span class="line"><span class="keyword">const</span> fetchUser = <span class="function">(<span class="params">id</span>) =&gt;</span> (</span><br><span class="line">  fetch(buildUri<span class="string">`/users/<span class="subst">$&#123;id&#125;</span>`</span>) <span class="comment">// Get User DTO record from REST API</span></span><br><span class="line">    .then(convertFormat) <span class="comment">// Convert to snakeCase</span></span><br><span class="line">    .then(validateUser) <span class="comment">// Make sure the the user is valid</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Clean</span></span><br><span class="line"><span class="keyword">const</span> fetchUser = <span class="function">(<span class="params">id</span>) =&gt;</span> (</span><br><span class="line">  fetch(buildUri<span class="string">`/users/<span class="subst">$&#123;id&#125;</span>`</span>)</span><br><span class="line">    .then(snakeToCamelCase)</span><br><span class="line">    .then(validateUser)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>上面的例子，方法 <code>convertFormat</code> 含义是 “转换格式”，太过于笼统，以至于不得不添加注释。如果换成 <code>snakeToCamelCase</code> （转换为驼峰风格），这个名字就解释了自己的功能。</p>
<h3 id="斟酌变量名"><a href="#斟酌变量名" class="headerlink" title="斟酌变量名"></a>斟酌变量名</h3><blockquote>
<p>布尔值或者返回值是布尔类型的函数，命名以 <code>is</code> <code>has</code> <code>should</code> 开头：</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Dirty</span></span><br><span class="line"><span class="keyword">const</span> done = current &gt;= goal;</span><br><span class="line"><span class="comment">// Clean</span></span><br><span class="line"><span class="keyword">const</span> isComplete = current &gt;= goal;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>函数以其效果命名，而不是怎么做的来命名</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Dirty</span></span><br><span class="line"><span class="keyword">const</span> loadConfigFromServer = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// Clean</span></span><br><span class="line"><span class="keyword">const</span> loadConfig = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>很多时候我也经常犯这种错误，毕竟写代码的时候总要考虑实现，一不小心就将实现的方式带入了函数名中。</p>
<h3 id="遵循设计模式"><a href="#遵循设计模式" class="headerlink" title="遵循设计模式"></a>遵循设计模式</h3><p>这里的设计模式，并不是指工程上的，而是更广泛的开发中的设计模式，比如 “你应该使用 tslint 校验代码格式” “typescript 开启 stricts 模式” “编写一个 React 函数，应当将 React 作为 <code>peerDependency</code>” 等等（当然，不要随意设置 <code>peerDependency</code> 也是一种江湖约定）。</p>
<p>对于 React，遵循以下几个最佳实践：</p>
<ul>
<li>单一责任原则, 确保每个功能都完整完成一项功能，比如更细粒度的组件拆分，同时也更利于测试。</li>
<li>不要把组件的内部依赖强加给使用方。</li>
<li>lint 规则尽量严格。</li>
</ul>
<p>根据我的体验，尤为痛恨违背第二条的组件，<strong>比如当 React 组件使用了数据流，但必须依赖项目初始化该数据流才能执行，如果不是被生活所迫，我才不会使用这种组件。</strong></p>
<p>第三条也一样，<strong>如果你是一个知名轮子的作者，请毫不留情的使用最严格的 lint 规则。如果使用者的 lint 规则比你还严格，你的组件将无法使用。</strong></p>
<h3 id="考虑到以上几点并不会降低编码速度"><a href="#考虑到以上几点并不会降低编码速度" class="headerlink" title="考虑到以上几点并不会降低编码速度"></a>考虑到以上几点并不会降低编码速度</h3><p>编写整洁的代码在开始一定会放慢开发速度，因为你需要转变自己的思维模式，但随着不断迭代，它的带来的效率提升会逐渐弥补前面的损失，并不断带来开发效率的提升。</p>
<p>写组件库也是同理，用脚写固然能快速完成，但后续往往要重构掉。我很羡慕函数式工作环境的开发者，他们几乎只要为每个功能写一遍，剩下的就是记住并调用它。</p>
<h3 id="在-React-中的实践"><a href="#在-React-中的实践" class="headerlink" title="在 React 中的实践"></a>在 React 中的实践</h3><p>略过几个没意思的例子。。</p>
<blockquote>
<p>在 React 使用 defaultProps 代替在代码中动态判断</p>
</blockquote>
<p>显然，利用 React 组件的规则，将入参的默认值预先定义好是最高效的。但顺带一句，如果在 ts 最严格的 <code>stricts</code> 模式里，依然会报错：变量可能未定义。这是因为 <code>defaultProps</code> 依然是个约定，而不能通过强类型推导出，目前还没有更优雅的解决思路。</p>
<blockquote>
<p>渲染与判断逻辑分开</p>
</blockquote>
<figure class="highlight typescript"><figcaption><span>jsx</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// Dirty</span></span><br><span class="line"><span class="keyword">class</span> User <span class="keyword">extends</span> Component &#123;</span><br><span class="line">  state = &#123; loading: <span class="literal">true</span> &#125;;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; loading, user &#125; = <span class="keyword">this</span>.state;</span><br><span class="line">    <span class="keyword">return</span> loading</span><br><span class="line">      ? &lt;div&gt;Loading...&lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">      : &lt;div&gt;</span></span><br><span class="line"><span class="regexp">          &lt;div&gt;</span></span><br><span class="line"><span class="regexp">            First name: &#123;user.firstName&#125;</span></span><br><span class="line"><span class="regexp">          &lt;/</span>div&gt;</span><br><span class="line">          &lt;div&gt;</span><br><span class="line">            First name: &#123;user.lastName&#125;</span><br><span class="line">          &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">          ...</span></span><br><span class="line"><span class="regexp">        &lt;/</span>div&gt;;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  componentDidMount() &#123;</span><br><span class="line">    fetchUser(<span class="keyword">this</span>.props.id)</span><br><span class="line">      .then(<span class="function">(<span class="params">user</span>) =&gt;</span> &#123; <span class="keyword">this</span>.setState(&#123; loading: <span class="literal">false</span>, user &#125;)&#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Clean</span></span><br><span class="line"><span class="keyword">import</span> RenderUser <span class="keyword">from</span> <span class="string">'./RenderUser'</span>;</span><br><span class="line"><span class="keyword">class</span> User <span class="keyword">extends</span> Component &#123;</span><br><span class="line">  state = &#123; loading: <span class="literal">true</span> &#125;;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; loading, user &#125; = <span class="keyword">this</span>.state;</span><br><span class="line">    <span class="keyword">return</span> loading ? &lt;Loading /&gt; : &lt;RenderUser user=&#123;user&#125; /&gt;;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  componentDidMount() &#123;</span><br><span class="line">    fetchUser(<span class="keyword">this</span>.props.id)</span><br><span class="line">      .then(<span class="function"><span class="params">user</span> =&gt;</span> &#123; <span class="keyword">this</span>.setState(&#123; loading: <span class="literal">false</span>, user &#125;)&#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>逻辑与渲染分离，便于维护，其次便于测试。</p>
<p>当然有人可能会问 “就算逻辑与渲染分离了，但组成的大组件不还是逻辑耦合的吗”，对，这就像函数单一指责一样，<code>render</code> 是过程代码，但过程中涉及到的逻辑，分配给单一指责的渲染组件渲染，如果把逻辑与渲染写在一起，就类似一个函数把功能全做完，这样做显然诸事不利。</p>
<h3 id="提倡无状态组件"><a href="#提倡无状态组件" class="headerlink" title="提倡无状态组件"></a>提倡无状态组件</h3><figure class="highlight typescript"><figcaption><span>jsx</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// Dirty</span></span><br><span class="line"><span class="keyword">class</span> TableRowWrapper <span class="keyword">extends</span> Component &#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      &lt;tr&gt;</span><br><span class="line">        &#123;<span class="keyword">this</span>.props.children&#125;</span><br><span class="line">      &lt;<span class="regexp">/tr&gt;</span></span><br><span class="line"><span class="regexp">    );</span></span><br><span class="line"><span class="regexp">  &#125;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">/</span><span class="regexp">/ Clean</span></span><br><span class="line"><span class="regexp">const TableRowWrapper = (&#123; children &#125;) =&gt; (</span></span><br><span class="line"><span class="regexp">  &lt;tr&gt;</span></span><br><span class="line"><span class="regexp">    &#123;children&#125;</span></span><br><span class="line"><span class="regexp">  &lt;/</span>tr&gt;</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>性能是一个原因，原文比较强调性能与代码量。我认为 <code>stateless</code> 重点在于阻碍了内部状态的使用，移除了生命周期，所以提高了组件的可控性，也就拓宽了组件的使用场景。</p>
<p>受控与非受控组件都有其适用场景，像非常基础的底层组件库，往往倾向提供两套机制，通过 <code>value</code> 与 <code>defaultValue</code> 决定是否受控。拥有这样能力的组件源码就没法通过 <code>stateless</code> 写，所以无状态组件的面向对象并不是基础底层组件，而且这些基础组件也没必要完全无状态，两者都提供是最好的选择。</p>
<p>说到这，也就是考虑到成本问题，那么无状态组件也就更适合上层具有业务含义的组件。页面级别组件状态太多，不适合，所以我认为无状态组件比较适合 <code>Wrapper</code> 层，也就是对基础组件包裹并增强业务能力这一层。</p>
<h3 id="解构"><a href="#解构" class="headerlink" title="解构"></a>解构</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Dirty</span></span><br><span class="line"><span class="keyword">const</span> splitLocale = locale.split(<span class="string">'-'</span>);</span><br><span class="line"><span class="keyword">const</span> language = splitLocale[<span class="number">0</span>];</span><br><span class="line"><span class="keyword">const</span> country = splitLocale[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// Clean</span></span><br><span class="line"><span class="keyword">const</span> [language, country] = locale.split(<span class="string">'-'</span>);</span><br></pre></td></tr></table></figure>

<p>ES6 新增的语法可以提升不少代码可读性，需要刻意训练去培养这个习惯。</p>
<h2 id="3-精读"><a href="#3-精读" class="headerlink" title="3 精读"></a>3 精读</h2><p>本周精读已经融于内容概要中 ^_^。最后推荐在 typescript 中开启 <code>strict</code> 模式，强制使用良好的开发习惯。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Bad</span></span><br><span class="line">onChange(<span class="function"><span class="params">value</span> =&gt;</span> <span class="built_in">console</span>.log(value.name))</span><br><span class="line"><span class="comment">// Dirty</span></span><br><span class="line">onChange(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (!value) &#123;</span><br><span class="line">    value = &#123;&#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">console</span>.log(value.name)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">// Clean</span></span><br><span class="line">onChange(<span class="function">(<span class="params">value = &#123;&#125;</span>) =&gt;</span> <span class="built_in">console</span>.log(value.name))</span><br><span class="line"><span class="comment">// Clean</span></span><br><span class="line">onChange(<span class="function"><span class="params">value</span> =&gt;</span> <span class="built_in">console</span>.log(value?.name))</span><br></pre></td></tr></table></figure>

<p>不要信任任何回调函数给你的变量，它们随时可能是 <code>undefined</code>，使用初始值是个不错的选择，但有的时候初始值没什么意义，使用 <code>?.</code> 语法可以安全的访问属性，是时候抛弃 <code>_.get</code> 了。</p>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>那些入坑 React 前没有人会提醒你的事</title>
    <url>/2018/12/15/article-react-cao-dian/</url>
    <content><![CDATA[<h1 id="1-引言"><a href="#1-引言" class="headerlink" title="1 引言"></a>1 引言</h1><p>站在 React 已经繁荣昌盛、无需四处布道宣传的今天，我们不妨冷静下来问问自己，React 真的是一个完美的框架吗？社区里一直不缺少吐槽的声音，这周我们就来看看，React 到底有哪些槽点。</p>
<p>针对以下两篇文章：</p>
<p>一篇是 Gianluca Guarini 写的 《<a href="https://medium.com/@gianluca.guarini/things-nobody-will-tell-you-about-react-js-3a373c1b03b4" target="_blank" rel="noopener">Things nobody will tell you about React.js</a>》，我将它译作 《那些入坑 React 前没有人会提醒你的事》，因为作者行文中明显带着对 React 的批判和失望。</p>
<p>另一篇则是 Facebook 员工，也是 Redux 作者的 Dan Abramov 针对上文的回复 《<a href="https://medium.com/@dan_abramov/hey-thanks-for-feedback-bf9502689ca4" target="_blank" rel="noopener">Hey, thanks for feedback!</a>》。</p>
<a id="more"></a>
<h1 id="2-内容概要"><a href="#2-内容概要" class="headerlink" title="2 内容概要"></a>2 内容概要</h1><p>Gianluca Guarini 着重吐槽的点在于：</p>
<ul>
<li>React 项目文件组织规范不统一，社区中 Starter Kit 太多（100+），新手不知道该怎么组织文件</li>
<li>由于 React 只关心 View 层，开发者就要面临选择 mobx 还是 redux 的纠结，无论选择哪种都会带来一系列的问题（重新配置构建脚本，更新 eslint 规则等）</li>
<li>如果选了 mobx，会发现 mobx 无法保证自己的 store 不被外部更新（官方建议是加上特殊的前缀）</li>
<li>如果选了 redux，会发现要实现同样的功能需要写很多的重复代码（这也是为什么社区中有海量的 redux helper 存在）</li>
<li>路由用起来也很蛋疼，因为 React Router 几乎是社区中唯一的选择，但是这货版本更新太快，一不小心就用了废弃的 API</li>
<li>用 JSX 的时候总是要嵌很多没必要的 <code>div</code> 或 <code>span</code></li>
<li>要上手一个 React 应用，要配置很多的构建工具和规则才能看到效果</li>
<li>…</li>
</ul>
<p>Dan Abramov 的回复：</p>
<ul>
<li>「React 16.0 引入的 Fiber 架构会导致现有代码全部需要重构」的说法是不对的，因为新的架构做到了向后兼容，而且 Facebook 内部超过 3 万个组件都能无痛迁移到新架构上</li>
<li>缺少统一脚手架的问题，可以通过 create-react-app 解决</li>
<li>觉得 redux 和 mobx 繁琐的话，对于刚刚上手的小应用不建议使用</li>
<li>React Router 升级太频繁？2015 年发布的 1.0，2016 年 2 月发布的 2.0，2016 年 10 月发布的 3.0。虽然 4.0 紧接着 3.0 马上就发布了，但是 React Router 很早就已经公布了这样的升级计划。</li>
<li>…</li>
</ul>
<h1 id="3-精读"><a href="#3-精读" class="headerlink" title="3 精读"></a>3 精读</h1><p>本次提出独到观点的同学有：<a href="https://www.zhihu.com/people/rccoder/answers" target="_blank" rel="noopener">@rccoder</a>  <a href="https://www.zhihu.com/people/turbe-xue" target="_blank" rel="noopener">@Turbe Xue</a> <a href="https://github.com/Pines-Cheng" target="_blank" rel="noopener">@Pines-Cheng</a> <a href="https://github.com/jin5354" target="_blank" rel="noopener">@An Yan</a> <a href="https://www.zhihu.com/people/5ac53c9c0484e83672e1c1716bdf0ff9" target="_blank" rel="noopener">@淡苍</a>  <a href="https://www.zhihu.com/people/3ec85a04bc9eaa35b1830874cc463a52" target="_blank" rel="noopener">@黄子毅</a> <a href="https://www.zhihu.com/people/twobin/pins/posts" target="_blank" rel="noopener">@宾彬</a> <a href="https://github.com/cisen" target="_blank" rel="noopener">@cisen</a> <a href="https://github.com/ybning" target="_blank" rel="noopener">@Bobo</a> 精读由此归纳。</p>
<p>很高兴能看到不少新同学积极参与到精读的讨论中来，每一个人的声音都是社区发展的一份力量。</p>
<h3 id="React-上手困难"><a href="#React-上手困难" class="headerlink" title="React 上手困难"></a>React 上手困难</h3><p>很早之前我们去四处布道 React 的时候，都会强调 React 很简单，因为它的 public API 非常之少，React 完整的文档 1 个小时就能看完。</p>
<p>那么说「React 上手困难」又是从何谈起呢？参与精读的同学中有不少都有 Vue 的使用经验（包括本周吐槽文的作者），所以不免会把两个框架上手的难易程度放在心里做个对比。</p>
<p>都说没有对比就没有伤害，大家普遍的观点是 Vue 上手简单、文档清晰、构建工具完善、脚手架统一……再反观 React，虽然 Dan 在文章里做了不少解释，但引用 @An Yan 的原话，『他也只是在说「事情没有那么糟糕」』。</p>
<p>所以说，大家认为的 React 上手困难，很大程度上不是 React 本身，而是 React 附带的生态圈野蛮发展太快，导致新人再进入的时候普遍感觉无所适从。虽然官方的 create-react-app 缓解了这一问题，但还没有从根本程度上找到解法。</p>
<h3 id="状态管理的迷思"><a href="#状态管理的迷思" class="headerlink" title="状态管理的迷思"></a>状态管理的迷思</h3><p>在今时今日的前端圈子里，说 React 不说 Redux 就像说 Ruby 却不说 Rails 一样，总感觉缺点儿什么。</p>
<p>因为 React 将自己定位成 View 层的解决方案，所以对于中大型业务来说一个合适的状态管理方案是不可或缺的。从最早的 Backbone Model，到 Flux，再到 reflux、Redux，再到 mobx 和 redux-observable，你不得不感叹 React 社区的活力是多么强大。</p>
<p>然而当你真正开始做新项目架构的时候，你到底是选 Redux 还是 Mobx，疑惑是封装解决方案如 dva 呢？ @淡苍 认为，Redux 与 MobX，React 两大状态管理方案，各有千秋，Redux 崇尚自由，扩展性好，却也带来了繁琐，一个简单的异步请求都必须引入中间件才能解决，MobX 上手容易，Reactive 避免不必要的渲染，带来性能提升，但相对封闭，不利于业务抽象，缺少最佳实践。至于如何选择？根据具体场景与需求判断。</p>
<p>不难看出，想要做好基于 React 的前端架构，你不仅需要对自己的业务了如指掌，还需要对各种解决方案的特性以及适合怎样的业务形态了如指掌。在 React 社区，永远没有标准解决方案。</p>
<h3 id="Redux-亦非万能解"><a href="#Redux-亦非万能解" class="headerlink" title="Redux 亦非万能解"></a>Redux 亦非万能解</h3><p>Redux 在刚刚推出的时候凭借酷炫的 devtool 和时间旅行功能，瞬间俘获了不少工程师的心。</p>
<p>但当你真正开始使用 Redux 的时候，你会发现你不仅需要学习很多新的概念，如 reducer、store、dispatch、action 等，还有很多基础的问题都没有标准解法，最典型的例子就是异步 action。虽然 Redux 的 middleware 机制提供了实现异步 action 的可能性，但是对于小白来说去 dispatch 一个非 Object 类型的 action 之前需要先了解 thunk 的概念，还要给 Redux 添加一个 redux-thunk 中间件实属难题。</p>
<p>不仅如此，在前端工程中常见的表单处理，Redux 社区也一直没有给出完美的解法。前有简单的 util 工具 redux-form-utils，后有庞大复杂的 redux-form，还有 rc-component 实现的一套基于 HOC 的解决方案。若没有充分的了解和调研，你将如何选择？</p>
<p>这还没有提到最近非常火热的 redux-saga 和 redux-observable，虽然 Dan 说如果你不需要的话完全可以不用了解，但是如果你不了解他们的话怎么知道自己需不需要呢？</p>
<h3 id="React-与-Vue-之争"><a href="#React-与-Vue-之争" class="headerlink" title="React 与 Vue 之争"></a>React 与 Vue 之争</h3><p>Vue 之所以觉得入门简单，因为一开始就提供了 umd 的引入方式，这与传统 js 开发的习惯一致，以及 Avalon 多年布道的铺垫，大家可以很快接受一个不依赖于构建的 Vue。</p>
<p>React 因为引入了 JSX 概念，本可以以 umd 方式推广，但为了更好的 DX 所以上来就推荐大家使用 JSX，导致新手觉得门槛高。</p>
<p>React + Mobx 约等于一个复杂的 Vue，但这不是抛弃 React 的理由。为什么大家觉得 Vuex 比 Redux 更适合 Vue 呢？因为 Vuex 简单，而 Redux 麻烦，这已经将两个用户群划分开了。</p>
<p>一个简单的小公司，就是需要这种数据流简单，不需要编译，没有太多技术选型要考虑的框架，他们看中的是开发效率，可维护性并不是第一位，这点根本性的导致了这两类人永远也撮合不到一块。</p>
<p>而 Vue 就是解决了这个问题，帮助了那么多开发者，仅凭这点就非常值得称赞，而我们不应该从 React 维护性的角度去抨击谁好谁坏，因为站在我们的角度，大部分中小公司的开发者是不 care 的。</p>
<p>React 用户圈汇集了一批高端用户，他们不断探索技术选型，为开源社区迸发活力，如果大家都转向 Vue，这块摊子就死了，函数式、响应式编程的演进也会从框架的大统一而暂时终止，起码这是不利于技术进步的，也是不可能发生的。Vue 在自己的领域做好，将 React 敏捷思想借鉴过来，帮助更多适合场景的开发者，应该才是作者的目的。</p>
<h3 id="小贴士：如何在开源社区优雅的撕逼"><a href="#小贴士：如何在开源社区优雅的撕逼" class="headerlink" title="小贴士：如何在开源社区优雅的撕逼"></a>小贴士：如何在开源社区优雅的撕逼</h3><p>开源社区撕逼常有，各种嘴炮也吃充斥在社区里，甚至有人在 Github 上维护了一份开源社区撕逼历史。虽然说做技术的人有争论很正常，但是撕的有理有据令人信服的案例却不多。这次 Facebook 的员工 Dan Abramov 就做出了很好的表率。面对咄咄逼人的文章，逐条回复，不回避、不扯淡且态度保持克制，实属难能可贵。</p>
<h1 id="3-总结"><a href="#3-总结" class="headerlink" title="3 总结"></a>3 总结</h1><p>React 开发者们也不要因为产生了 Mobx 这种亲 Vue 派而产生焦虑，这也是对特定业务场景的权衡，未来更多更好的数据流方案还会继续诞生，技术社区对技术的优化永无止尽。</p>
<p>比如 <a href="https://github.com/mobxjs/mobx-state-tree" target="_blank" rel="noopener">mobx-state-tree</a> 就是一种 redux 与 mobx 结合的大胆尝试，作者在很早之前也申明了，Mobx 一样可以做时间旅行，只要遵守一定的开发规范。</p>
<p>最后打个比方：安卓手机在不断进步，体验越来越逼近苹果，作为一个逼格高的用户，果断换苹果吧。但作为 java 开发人员的你，是否要为此换到 oc 流派呢？换，或者不换，其实都一样，安卓和苹果已经越来越像了。</p>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>React学习总结</title>
    <url>/2018/11/17/article-react-learn-sum/</url>
    <content><![CDATA[<h2 id="React开发步骤总结"><a href="#React开发步骤总结" class="headerlink" title="React开发步骤总结"></a>React开发步骤总结</h2><ol>
<li>UI拆分（单一职责原则）</li>
<li>构建静态版本（无state，无交互，可测试页面构成）</li>
<li>确定state（为了好维护：最小但完整）</li>
<li>某些可以根据props计算而来 或者 根据已有state和props计算而来的 不是必须state。</li>
<li>确定状态的位置</li>
<li>考虑涉及到同一state的组件有哪些，将该state放到所有关联组件的共同祖先组件</li>
<li>反向数据流</li>
<li>在确定好state之后，通过props向下传递，形成正向数据流；反向数据流就是事件响应等修改祖先组件中的state。</li>
</ol>
<h2 id="React基础知识总结"><a href="#React基础知识总结" class="headerlink" title="React基础知识总结"></a>React基础知识总结</h2><ol>
<li>React只更新必需要更新的部分，即使render了整个react元素，也只更新需要更新的部分，不必纠结render执行的资源消耗，参见<a href="https://zh-hans.reactjs.org/docs/reconciliation.html#the-diffing-algorithm" target="_blank" rel="noopener">react diff</a></li>
<li>JSX 可防止注入攻击，由于其在渲染前，所有数据会被转义成字符串处理；JSX本身可看作对象，即React元素，React会根据这些React元素来构建真正的DOM，并保持更新。</li>
<li>state状态更新可能是异步的，React 为了优化性能，有可能会将多个 setState() 调用合并为一次更新，所以不能依赖他们的值计算下一个state(状态)；</li>
<li>组件的状态只能自身访问，组件外不能获取；一个组件可以选择将 state(状态) 向下传递，作为其子组件的 props(属性)；</li>
<li><strong>事件处理</strong> (通过preventDefault来阻止默认行为)，注意事件处理函数的this绑定；</li>
<li><strong>条件渲染：</strong><ul>
<li>if + 变量，根据不同state或者props来返回不同组件；</li>
<li>在返回的组件JSX中使用 &amp;&amp; ，JSX中可使用任何表达式（内联）;</li>
<li>三元表达式（内联）；</li>
</ul>
</li>
<li>组件属性 props.children 表示组件包裹的元素，可包裹JSX组件元素、字符串、{}包裹的js表达式、回调函数。false，null，undefined，和 true 都是有效的的 children(子元素)，但是并不会被渲染。</li>
<li>return null 可防止组件被渲染（可在函数组件中return null），从组件的 render 方法返回 null 不会影响组件生命周期方法的触发。 例如， componentWillUpdate 和 componentDidUpdate 仍将被调用；</li>
<li><strong>列表渲染：</strong><ul>
<li>列表组件</li>
<li>键(Keys) 帮助 React 标识哪个项被修改、添加或者移除了。数组中的每一个元素都应该有一个唯一不变的键(Keys)来标识；keys 在同辈元素中必须是唯一的，不需要全局唯一；key不会作为props传入组件内部，若想使用，可利用props传入相同的值；</li>
</ul>
</li>
<li><strong>表单</strong>（受控组件）(React元素中input、textare、select标签的value，onChange与selected属性都与HTML元素不同，都接受一个 value 属性来实现一个受控组件)</li>
<li><strong>状态提升：</strong><ul>
<li>React遵循单一数据流原则，即从上到下的数据流，通过将state转props进行父子通信，若需要组件间通信，则需要将组件的状态提升到最近的共同祖先组件；</li>
<li>当你看到 UI 中的错误，你可以使用 React 开发者工具来检查 props ，并向上遍历树，直到找到负责更新状态的组件。这使你可以跟踪到 bug 的源头;</li>
<li>组件可以接受任意的 props(属性) ，包括原始值、React 元素，或者函数;</li>
</ul>
</li>
</ol>
<h2 id="redux-学习记录"><a href="#redux-学习记录" class="headerlink" title="redux 学习记录"></a>redux 学习记录</h2><p><a href="https://segmentfault.com/a/1190000017064759" target="_blank" rel="noopener">中文文档 segmentfault</a></p>
<p><a href="https://cn.redux.js.org/" target="_blank" rel="noopener">中文文档 GitBook</a></p>
<h3 id="redux-的来源与思想"><a href="#redux-的来源与思想" class="headerlink" title="redux 的来源与思想"></a>redux 的来源与思想</h3><ol>
<li>来源：Redux 由 Flux 演变而来，但受 Elm （函数式编程语言）的启发，避开了 Flux 的复杂性。</li>
<li>思想：Web 应用是一个状态机，视图与状态是一一对应的。所有的状态，保存在一个对象里面。让 state 的变化变得可预测 。</li>
<li>三大原则：<ul>
<li>单一数据源（整个应用的 state 被储存在一棵 object tree 中，并且这个 object tree 只存在于唯一一个 store 中）</li>
<li>State 是只读的（唯一改变 state 的方法就是触发 action）</li>
<li>使用纯函数来执行修改（通过 reducers 函数来修改 state tree）</li>
</ul>
</li>
</ol>
<h3 id="redux-的适用场景"><a href="#redux-的适用场景" class="headerlink" title="redux 的适用场景"></a>redux 的适用场景</h3><p>从应用的角度：</p>
<ul>
<li>用户的使用方式复杂</li>
<li>不同身份的用户有不同的使用方式（比如普通用户和管理员）</li>
<li>多个用户之间可以协作</li>
<li>与服务器大量交互，或者使用了WebSocket</li>
<li>View要从多个来源获取数据</li>
</ul>
<p>从组件的角度：</p>
<ul>
<li>某个组件的状态，需要共享</li>
<li>某个状态需要在任何地方都可以拿到</li>
<li>一个组件需要改变全局状态</li>
<li>一个组件需要改变另一个组件的状态</li>
</ul>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>单页面 SEO 优化</title>
    <url>/2018/10/10/other-SEO/</url>
    <content><![CDATA[<h2 id="通用方法"><a href="#通用方法" class="headerlink" title="通用方法"></a>通用方法</h2><ol>
<li>logo上加首页的地址，并且只将logo是放在<code>h1</code>标签中</li>
<li>外链优化，提高关键词排名，<code>meta</code>信息</li>
<li>对外联css,以及js使用了延迟加载以及<code>dns-prefetch</code>，<code>preload</code></li>
<li>使用<code>Robots.txt</code></li>
</ol>
<p>… 等等</p>
<h2 id="服务端渲染-后端渲染SSR"><a href="#服务端渲染-后端渲染SSR" class="headerlink" title="服务端渲染(后端渲染SSR)"></a>服务端渲染(后端渲染SSR)</h2><p>可以使用<a href="https://zh.nuxtjs.org/guide" target="_blank" rel="noopener">Nuxt.js</a>作为框架来处理项目的所有UI呈现</p>
<ul>
<li>好处：前端耗时少（前端只负责将<code>html</code>进行展示），利于SEO</li>
<li>坏处：网络传输数据量大，占用（部分、少部分）服务器运算资源，response 出的数据量会（稍）大点，模板改了前端的交互和样式什么的一样得跟着联动修改</li>
</ul>
<h2 id="预渲染的方式-PreRender"><a href="#预渲染的方式-PreRender" class="headerlink" title="预渲染的方式(PreRender)"></a>预渲染的方式(PreRender)</h2><p><a href="https://github.com/muwoo/vue-meta-info" target="_blank" rel="noopener">vue-meta-info</a> 动态设置<code>meta</code>信息，配合 <a href="https://github.com/chrisvfritz/prerender-spa-plugin" target="_blank" rel="noopener">prerender-spa-plugin</a> 实现关键页面的预渲染。</p>
<blockquote>
<p>需要路由使用<code>history</code>模式，使用<code>hash</code>模式会导致失效</p>
</blockquote>
<ul>
<li>缺点：不能很好地处理用户独特性路由，预渲染路由过多的话会增加编译时间。</li>
</ul>
]]></content>
      <categories>
        <category>杂记</category>
      </categories>
      <tags>
        <tag>SEO</tag>
      </tags>
  </entry>
  <entry>
    <title>学习 React</title>
    <url>/2018/09/01/article-js-react/</url>
    <content><![CDATA[<p>react众所周知的前端3大主流框架之一，由于出色的性能，完善的周边设施风头一时无两。本文就带大家一起掌握react。</p>
<p><strong>以下内容参考学习 <a href="https://segmentfault.com/a/1190000016281174" target="_blank" rel="noopener">掌握react，这一篇就够了</a></strong></p>
<a id="more"></a>

<h2 id="jsx语法"><a href="#jsx语法" class="headerlink" title="jsx语法"></a>jsx语法</h2><p>前端MVVM主流框架都有一套自己的模板处理方法，react则使用它独特的jsx语法。在组件中插入html类似的语法，简化创建view的流程。</p>
<p>下面让我们来认识一下构建的两种元素</p>
<h3 id="原生元素"><a href="#原生元素" class="headerlink" title="原生元素"></a>原生元素</h3><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;h1&gt;标题&lt;<span class="regexp">/h1&gt;</span></span><br><span class="line"><span class="regexp">  &lt;/</span>div&gt;</span><br><span class="line">), <span class="built_in">document</span>.getElementById(<span class="string">'root'</span>))</span><br></pre></td></tr></table></figure>

<p>通过简单的语法页面就会被插入一个div+一个h1标签。原生的html元素可以被直接使用。以上的语法并不是js支持的语法，需要被转换之后才能运行。</p>
<h3 id="自定义元素"><a href="#自定义元素" class="headerlink" title="自定义元素"></a>自定义元素</h3><p>react强大之处就在于可以组件的自定义，实现组件的复用。如果我们创建了一个组件。我们也可以通过jsx语法调用。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Page</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      home111 &amp;copy; © \ua9</span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;Page/&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<p>我们定义了一个Page组件，可以在jsx里面像调用html一样直接调用。</p>
<h3 id="插入动态数据"><a href="#插入动态数据" class="headerlink" title="插入动态数据"></a>插入动态数据</h3><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> name = <span class="string">'hi'</span></span><br><span class="line"></span><br><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &#123;name&#125;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<p>使用{}就可以插入数据，但是{}中间的必须是js表达式，不能是语句。如果表达式的执行结果是一个数组，则会自动join。</p>
<h3 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h3><p>jsx语法和html语法一样，也是可以插入注释，只不过写的时候有一些区别</p>
<h4 id="子组件注释"><a href="#子组件注释" class="headerlink" title="子组件注释"></a>子组件注释</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> name = <span class="string">'hi'</span></span><br><span class="line"></span><br><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &#123;<span class="comment">/* 注释 */</span>&#125;</span><br><span class="line">    &#123;name&#125;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<p>在子组件中插入注释，需要使用{}包裹起来，在/ /之间插入注释文字。</p>
<h4 id="属性注释"><a href="#属性注释" class="headerlink" title="属性注释"></a>属性注释</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> name = <span class="string">'hi'</span></span><br><span class="line"></span><br><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &#123;name&#125;</span><br><span class="line">    &lt;img <span class="comment">/*</span></span><br><span class="line"><span class="comment">        多行注释</span></span><br><span class="line"><span class="comment">    */</span> src=<span class="string">"1.jpg"</span>/&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<p>在标签中间，可以插入一个多行注释，类似上面的代码。</p>
<h3 id="属性props"><a href="#属性props" class="headerlink" title="属性props"></a>属性props</h3><ul>
<li>可以向使用html的attr一样使用属性，就像下面img的src一样</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> name = <span class="string">'hi'</span></span><br><span class="line"></span><br><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;img src=<span class="string">"1.png"</span>/&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<ul>
<li>如果需要传递动态属性，使用{}，多个属性，使用展开运算符</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> props = &#123;</span><br><span class="line">    src: <span class="string">'1.png'</span>,</span><br><span class="line">    alt: <span class="string">'1图片'</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;img src=&#123;<span class="string">"1.png"</span>&#125;/&gt;</span><br><span class="line">    &lt;img &#123;...props&#125;/&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<ul>
<li>两个转换,class–&gt;className for–&gt;htmlFor</li>
</ul>
<p>因为class和for是javascript关键字，所以这里需要用转换之后名称</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div className=<span class="string">"tab"</span>&gt;</span><br><span class="line">    &lt;label htmlFor=<span class="string">"name"</span>&gt;姓名:<span class="xml"><span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><span class="xml"><span class="tag">&lt;<span class="name">input</span> <span class="attr">id</span>=<span class="string">"name"</span>/&gt;</span></span></span><br><span class="line"><span class="xml">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">), <span class="built_in">document</span>.getElementById(<span class="string">'root'</span>))</span><br></pre></td></tr></table></figure>

<ul>
<li>布尔属性</li>
</ul>
<p>如果一个属性的值是布尔值，当这个值是true的时候则可以省略=后面的值，只保留key。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div className=<span class="string">"tab"</span>&gt;</span><br><span class="line">    &lt;input type=<span class="string">"text"</span> required/&gt;</span><br><span class="line">    &lt;input type=<span class="string">"text"</span> required=&#123;<span class="literal">true</span>&#125;/&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<ul>
<li>原生元素的自定义属性</li>
</ul>
<p>react对元素属性做了校验，如果在原生属性上使用此元素不支持的属性，则不能编译成功。必须使用data-前缀</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div className=<span class="string">"tab"</span>&gt;</span><br><span class="line">    &lt;input type=<span class="string">"text"</span> data-init=<span class="string">"22"</span>/&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<h3 id="插入html"><a href="#插入html" class="headerlink" title="插入html"></a>插入html</h3><p>如果动态的插入html元素,react出于安全性考虑会自动帮我们转义。所以一定要动态的插入元素的话，使用dangerouslySetInnerHTML</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div className=<span class="string">"tab"</span>&gt;</span><br><span class="line">    &lt;div dangerouslySetInnerHTML=&#123;&#123;<span class="attr">__html</span>: <span class="string">'&lt;span&gt;test&lt;/span&gt;'</span>&#125;&#125;&gt;<span class="xml"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<h2 id="React组件创建"><a href="#React组件创建" class="headerlink" title="React组件创建"></a>React组件创建</h2><h3 id="React-createClass"><a href="#React-createClass" class="headerlink" title="React.createClass"></a>React.createClass</h3><p>这是旧版本的api，使用React.createClass创建组件，配套的一些api，有getDefaultProps, getinitialstate。官方已经不建议使用了，使用下面新的api替代。</p>
<h3 id="ES6-classes"><a href="#ES6-classes" class="headerlink" title="ES6 classes"></a>ES6 classes</h3><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Page</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      home</span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是一个实现了render方法的class。也是一个基本的react组件。</p>
<h3 id="无状态函数"><a href="#无状态函数" class="headerlink" title="无状态函数"></a>无状态函数</h3><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Button</span>(<span class="params">props, context</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        &lt;button&gt;</span><br><span class="line">            &lt;em&gt;&#123;props.text&#125;&lt;<span class="regexp">/em&gt;</span></span><br><span class="line"><span class="regexp">            &lt;span&gt;&#123;context.name&#125;&lt;/</span>span&gt;</span><br><span class="line">        &lt;<span class="regexp">/button&gt;</span></span><br><span class="line"><span class="regexp">    );</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br></pre></td></tr></table></figure>

<p>纯函数,不存在state，只接受props和state。纯函数有优点，优点就是易于测试，无副作用。</p>
<h2 id="React数据流"><a href="#React数据流" class="headerlink" title="React数据流"></a>React数据流</h2><h3 id="state"><a href="#state" class="headerlink" title="state"></a>state</h3><p>state是组件的内部状态，需要在视图里面用到的状态，才需要放到state里面去。如下，我们在类上创建一个state属性，在视图里面通过使用this.state.name去引用。而这里的state定义则代替的是getinitialstate方法。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Page</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  state = &#123;</span><br><span class="line">      name: <span class="string">'小明'</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      &#123;this&#125;</span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如何更新state呢，直接更改state其实可以可以的，不过这样子无法触发组件视图的更新机制。所以使用setState()api。值得注意的是setState是异步的，原因是react内部需要对setState做优化，不是state变了立刻去更新视图，而是拦截一部分state的改变，等到合适的时机再去更新视图。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Page</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  state = &#123;</span><br><span class="line">      name: <span class="string">'小明'</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">this</span>.setState(&#123;<span class="attr">name</span>: <span class="string">'小明儿子'</span>&#125;)， <span class="number">5000</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      &#123;this.state.name&#125;</span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>真实开发中绝不要在render函数里面去更改state，以上只是为了演示</strong></p>
</blockquote>
<h3 id="props"><a href="#props" class="headerlink" title="props"></a>props</h3><p>props是组件之间传递数据的最主要api, react推崇的是自顶向下的数据流向，也就是组件的数据要从父组件传给子组件。如果子组件需要向父组件传递数据，则需要使用回调函数的方式。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      &#123;this.props.parentName&#125;</span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  state = &#123;</span><br><span class="line">      name: <span class="string">'小明'</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">this</span>.setState(&#123;<span class="attr">name</span>: <span class="string">'小明儿子'</span>&#125;)， <span class="number">5000</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line">      &lt;Child parentName=&#123;this.state.name&#125;/&gt;</span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span>)</span></span><br><span class="line"><span class="xml">  &#125;</span></span><br><span class="line"><span class="xml">&#125;</span></span><br></pre></td></tr></table></figure>

<p>可以看到Child组件显示了父组件的name。当父组件状态更新了，子组件同步更新。那如何在子组件中更改父组件状态呢？答案是回调函数。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  update() &#123;</span><br><span class="line">      <span class="keyword">this</span>.props.onChange(<span class="string">'小明名字改了'</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      &#123;this.props.parentName&#125;</span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;this.update.bind(this)&#125;</span>&gt;</span>更新<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  state = &#123;</span><br><span class="line">      name: <span class="string">'小明'</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  changeName(name) &#123;</span><br><span class="line">      <span class="keyword">this</span>.setState(&#123;</span><br><span class="line">          name</span><br><span class="line">      &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">this</span>.setState(&#123;<span class="attr">name</span>: <span class="string">'小明儿子'</span>&#125;)， <span class="number">5000</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line">      &lt;Child onChange=&#123;this.changeName.bind(this)&#125; parentName=&#123;this.state.name&#125;/&gt;</span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span>)</span></span><br><span class="line"><span class="xml">  &#125;</span></span><br><span class="line"><span class="xml">&#125;</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>注意哈：props是不可以更改的，这既不符合react单向数据流思想，也为维护带来灾难</strong>。</p>
</blockquote>
<h3 id="事件"><a href="#事件" class="headerlink" title="事件"></a>事件</h3><p>react里面的用户事件都是合成事件，被React封装过。内部使用的还是事件的委托机制。<br>常用的事件有点击事件onClick，input的onChange事件等，官网都可以查到。</p>
<h4 id="合成事件的this指向问题"><a href="#合成事件的this指向问题" class="headerlink" title="合成事件的this指向问题"></a>合成事件的this指向问题</h4><p>就像上文一样，我们绑定事件的方式很奇怪，使用了bind来显示绑定this的指向。因为传递到组件内部的只是一个函数，而脱离了当前对象的函数的this指向是不能指到当前组件的，需要显示指定。</p>
<ul>
<li>通过bind</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;button onClick=&#123;<span class="keyword">this</span>.update.bind(<span class="keyword">this</span>)&#125;&gt;更新&lt;<span class="regexp">/button&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>构造器内部指定</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">     <span class="keyword">super</span>(props)</span><br><span class="line">     <span class="keyword">this</span>.update = <span class="keyword">this</span>.update.bind(<span class="keyword">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  update() &#123;</span><br><span class="line">      <span class="keyword">this</span>.props.onChange(<span class="string">'小明名字改了'</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      &#123;this.props.parentName&#125;</span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;this.update&#125;</span>&gt;</span>更新<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>箭头函数</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  update =&gt; e = &#123;</span><br><span class="line">      <span class="keyword">this</span>.props.onChange(<span class="string">'小明名字改了'</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      &#123;this.props.parentName&#125;</span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;this.update&#125;</span>&gt;</span>更新<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>装饰器</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">     <span class="keyword">super</span>(props)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  @autoBind</span><br><span class="line">  update() &#123;</span><br><span class="line">      <span class="keyword">this</span>.props.onChange(<span class="string">'小明名字改了'</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      &#123;this.props.parentName&#125;</span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;this.update&#125;</span>&gt;</span>更新<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>装饰器是es7语法，如果需要使用需要安装对应的babel：present版本。而typescript则原生支持。</p>
<blockquote>
<p><strong>autoBind原理大概就是劫持get方法，get时改变this指向</strong></p>
</blockquote>
<h4 id="如何获得evnt原生事件"><a href="#如何获得evnt原生事件" class="headerlink" title="如何获得evnt原生事件"></a>如何获得evnt原生事件</h4><p>通过e.nativeEvent获取原生事件对象</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">     <span class="keyword">super</span>(props)</span><br><span class="line">     <span class="keyword">this</span>.update = <span class="keyword">this</span>.update.bind(<span class="keyword">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  update(e) &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(e.nativeEvent)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="xml">      <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;this.update&#125;</span>&gt;</span>更新<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="解决冒泡和取消默认事件"><a href="#解决冒泡和取消默认事件" class="headerlink" title="解决冒泡和取消默认事件"></a>解决冒泡和取消默认事件</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">e.preventDefault() <span class="comment">//取消默认行为</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">e.stopPropagation() <span class="comment">//取消冒泡</span></span><br></pre></td></tr></table></figure>

<p>这个和浏览器原生事件处理方案是一致的。问题是我们只可以调合成事件的e的方法，不可以通过e.nativeEvent方法做这些操作，原因是上文讲过的委托。</p>
<h2 id="ReactDom"><a href="#ReactDom" class="headerlink" title="ReactDom"></a>ReactDom</h2><h3 id="ref"><a href="#ref" class="headerlink" title="ref"></a>ref</h3><p>特殊的props，ref组件对象的引用，现在官方也不建议直接给ref赋值，需要通过函数来赋值。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">ReactDOM.render((</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;Calendar ref=&#123;ref =&gt; <span class="keyword">this</span>.c = ref&#125; any-ss=<span class="string">"text"</span>/&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">), document.getElementById('root'))</span></span><br></pre></td></tr></table></figure>

<h3 id="render"><a href="#render" class="headerlink" title="render"></a>render</h3><p>顶层api,只有在根组件时候才需要使用。第一个参数是Component,第二个参数是dom节点</p>
<h3 id="findDOMNode"><a href="#findDOMNode" class="headerlink" title="findDOMNode"></a>findDOMNode</h3><p>通过传入component实例获取此component根dom节点，在这里可以去dom节点进行操作了，虽然极其不建议这么做，但是你确实可以做。</p>
<h3 id="unmountComponentAtNode"><a href="#unmountComponentAtNode" class="headerlink" title="unmountComponentAtNode"></a>unmountComponentAtNode</h3><p>卸载此组件，并销毁组件state和事件。<br>接收组件的引用，也就是ref。仅仅是取消挂载，组件还在，如果需要彻底清除的话，需要手动删掉此dom。</p>
<h2 id="表单"><a href="#表单" class="headerlink" title="表单"></a>表单</h2><h3 id="onchange配合value"><a href="#onchange配合value" class="headerlink" title="onchange配合value"></a>onchange配合value</h3><p>与vue框架不同的是，react如果要实现表单元素变化，状态同步更新，必须要自己去监听表单事件。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  state = &#123;</span><br><span class="line">      name: <span class="string">'小明'</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">     <span class="keyword">super</span>(props)</span><br><span class="line">     <span class="keyword">this</span>.update = <span class="keyword">this</span>.update.bind(<span class="keyword">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  update(e) &#123;</span><br><span class="line">      <span class="keyword">this</span>.setState(&#123;</span><br><span class="line">          name: e.target.value</span><br><span class="line">      &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line">      &lt;input onChange=&#123;this.update&#125; value=&#123;this.state.name&#125;/&gt;</span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span>)</span></span><br><span class="line"><span class="xml">  &#125;</span></span><br><span class="line"><span class="xml">&#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="受控组件和非受控组件"><a href="#受控组件和非受控组件" class="headerlink" title="受控组件和非受控组件"></a>受控组件和非受控组件</h3><p>受控组件和非受控组件这些都是指的表单组件，当一个表单的值是通过value改变的而不是通过defaultValue是受控组件，否则就是非受控组件。</p>
<ul>
<li>下面组件中的input就是受控组件</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  state = &#123;</span><br><span class="line">      name: <span class="string">'小明'</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">     <span class="keyword">super</span>(props)</span><br><span class="line">     <span class="keyword">this</span>.update = <span class="keyword">this</span>.update.bind(<span class="keyword">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  update(e) &#123;</span><br><span class="line">      <span class="keyword">this</span>.setState(&#123;</span><br><span class="line">          name: e.target.value</span><br><span class="line">      &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line">      &lt;input onChange=&#123;this.update&#125; value=&#123;this.state.name&#125;/&gt;</span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span>)</span></span><br><span class="line"><span class="xml">  &#125;</span></span><br><span class="line"><span class="xml">&#125;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>下面组件中的input是非受控组件</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  state = &#123;</span><br><span class="line">      name: <span class="string">'小明'</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">     <span class="keyword">super</span>(props)</span><br><span class="line">     <span class="keyword">this</span>.update = <span class="keyword">this</span>.update.bind(<span class="keyword">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  update(e) &#123;</span><br><span class="line">      <span class="keyword">this</span>.setState(&#123;</span><br><span class="line">          name: e.target.value</span><br><span class="line">      &#125;)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span></span><br><span class="line">      &lt;input onChange=&#123;this.update&#125; defaultValue=&#123;this.state.name&#125;/&gt;</span><br><span class="line"><span class="xml">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span>)</span></span><br><span class="line"><span class="xml">  &#125;</span></span><br><span class="line"><span class="xml">&#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="组件之间通讯"><a href="#组件之间通讯" class="headerlink" title="组件之间通讯"></a>组件之间通讯</h2><h3 id="父子之间通讯"><a href="#父子之间通讯" class="headerlink" title="父子之间通讯"></a>父子之间通讯</h3><p>父子之间通讯又分为父-&gt;子，子-&gt;父。</p>
<p>因为react单向数据流向的缘故，父-&gt;子通信的话直接通过props。父组件数据变动，直接传递给子组件。</p>
<p>子-&gt;父组件之间就要通过回调函数来通信了，父组件传递一个回调函数给子组件，子组件通过调用此函数的方式通知父组件通信。</p>
<h3 id="跨级组件通信"><a href="#跨级组件通信" class="headerlink" title="跨级组件通信"></a>跨级组件通信</h3><p>react为了实现祖先组件和后辈组件之间的通信问题，引入了contextApi。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Button</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      &lt;button style=&#123;&#123;<span class="attr">background</span>: <span class="keyword">this</span>.context.color&#125;&#125;&gt;</span><br><span class="line">        &#123;<span class="keyword">this</span>.props.children&#125;</span><br><span class="line">      &lt;<span class="regexp">/button&gt;</span></span><br><span class="line"><span class="regexp">    );</span></span><br><span class="line"><span class="regexp">  &#125;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">Button.contextTypes = &#123;</span></span><br><span class="line"><span class="regexp">  color: React.PropTypes.string</span></span><br><span class="line"><span class="regexp">&#125;;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">class Message extends React.Component &#123;</span></span><br><span class="line"><span class="regexp">  render() &#123;</span></span><br><span class="line"><span class="regexp">    return (</span></span><br><span class="line"><span class="regexp">      &lt;div&gt;</span></span><br><span class="line"><span class="regexp">        &#123;this.props.text&#125; &lt;Button&gt;Delete&lt;/</span>Button&gt;</span><br><span class="line">      &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">    );</span></span><br><span class="line"><span class="regexp">  &#125;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">class MessageList extends React.Component &#123;</span></span><br><span class="line"><span class="regexp">  getChildContext() &#123;</span></span><br><span class="line"><span class="regexp">    return &#123;color: "purple"&#125;;</span></span><br><span class="line"><span class="regexp">  &#125;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">  render() &#123;</span></span><br><span class="line"><span class="regexp">    const children = this.props.messages.map((message) =&gt;</span></span><br><span class="line"><span class="regexp">      &lt;Message text=&#123;message.text&#125; /</span>&gt;</span><br><span class="line">    );</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span>&#123;children&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">MessageList.childContextTypes = &#123;</span><br><span class="line">  color: React.PropTypes.string</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>MessageList中的color会自动更新到儿孙组件里面去，实现跨级啊通信。如果需要反过来通信，则需要借助其他工具，比如事件系统(Pub/Sub)。</p>
<h3 id="没有嵌套关系组件之间通信"><a href="#没有嵌套关系组件之间通信" class="headerlink" title="没有嵌套关系组件之间通信"></a>没有嵌套关系组件之间通信</h3><p>组件之间通信最主流的两种方式脱胎于观察这模式和中介者模式这两种。</p>
<p>跨级之间通信现在最主流的方式就是观察这模式的实现Pub/Sub，react社区中的redux也是使用这种方式实现的。</p>
<p><em>vue2.X版本也去掉了跨组件通信的功能。那如何在2.x中做跨组件通信呢？如果不借助外力的话，是不是可以使用$parent和$childen的递归调用实现全局组件通信呢？比如我想广播一个事件，我就查找到所有的子组件，挨个触发$emit(xx)，上报一个事件也是同理，只不过需要查找所有的$parent。结合起来就可以实现组件之间的通信，只不过这种查找效率比较低，需要慎用和优化</em></p>
]]></content>
      <categories>
        <category>前沿技术</category>
      </categories>
      <tags>
        <tag>ES6</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>ES6 Class 的继承</title>
    <url>/2018/07/10/article-ES6-Class-extends/</url>
    <content><![CDATA[<p><strong>以下内容参考学习 <a href="http://es6.ruanyifeng.com/#docs/class-extends" target="_blank" rel="noopener">ECMAScript 6 入门</a></strong></p>
<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>Class 可以通过<code>extends</code>关键字实现继承，这比 ES5 的通过修改原型链实现继承，要清晰和方便很多。<a id="more"></a></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ColorPoint</span> <span class="keyword">extends</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码定义了一个<code>ColorPoint</code>类，该类通过<code>extends</code>关键字，继承了<code>Point</code>类的所有属性和方法。但是由于没有部署任何代码，所以这两个类完全一样，等于复制了一个<code>Point</code>类。下面，我们在<code>ColorPoint</code>内部加上代码。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ColorPoint</span> <span class="keyword">extends</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(x, y, color) &#123;</span><br><span class="line">    <span class="keyword">super</span>(x, y); <span class="comment">// 调用父类的constructor(x, y)</span></span><br><span class="line">    <span class="keyword">this</span>.color = color;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  toString() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.color + <span class="string">' '</span> + <span class="keyword">super</span>.toString(); <span class="comment">// 调用父类的toString()</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>constructor</code>方法和<code>toString</code>方法之中，都出现了<code>super</code>关键字，它在这里表示父类的构造函数，用来新建父类的<code>this</code>对象。</p>
<p>子类必须在<code>constructor</code>方法中调用<code>super</code>方法，否则新建实例时会报错。这是因为子类自己的<code>this</code>对象，必须先通过父类的构造函数完成塑造，得到与父类同样的实例属性和方法，然后再对其进行加工，加上子类自己的实例属性和方法。如果不调用<code>super</code>方法，子类就得不到<code>this</code>对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ColorPoint</span> <span class="keyword">extends</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> cp = <span class="keyword">new</span> ColorPoint(); <span class="comment">// ReferenceError</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>ColorPoint</code>继承了父类<code>Point</code>，但是它的构造函数没有调用<code>super</code>方法，导致新建实例时报错。</p>
<p>ES5 的继承，实质是先创造子类的实例对象<code>this</code>，然后再将父类的方法添加到<code>this</code>上面（<code>Parent.apply(this)</code>）。ES6 的继承机制完全不同，实质是先将父类实例对象的属性和方法，加到<code>this</code>上面（所以必须先调用<code>super</code>方法），然后再用子类的构造函数修改<code>this</code>。</p>
<p>如果子类没有定义<code>constructor</code>方法，这个方法会被默认添加，代码如下。也就是说，不管有没有显式定义，任何一个子类都有<code>constructor</code>方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ColorPoint</span> <span class="keyword">extends</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ColorPoint</span> <span class="keyword">extends</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(...args) &#123;</span><br><span class="line">    <span class="keyword">super</span>(...args);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>另一个需要注意的地方是，在子类的构造函数中，只有调用<code>super</code>之后，才可以使用<code>this</code>关键字，否则会报错。这是因为子类实例的构建，基于父类实例，只有<code>super</code>方法才能调用父类实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(x, y) &#123;</span><br><span class="line">    <span class="keyword">this</span>.x = x;</span><br><span class="line">    <span class="keyword">this</span>.y = y;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ColorPoint</span> <span class="keyword">extends</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(x, y, color) &#123;</span><br><span class="line">    <span class="keyword">this</span>.color = color; <span class="comment">// ReferenceError</span></span><br><span class="line">    <span class="keyword">super</span>(x, y);</span><br><span class="line">    <span class="keyword">this</span>.color = color; <span class="comment">// 正确</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，子类的<code>constructor</code>方法没有调用<code>super</code>之前，就使用<code>this</code>关键字，结果报错，而放在<code>super</code>方法之后就是正确的。</p>
<p>下面是生成子类实例的代码。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> cp = <span class="keyword">new</span> ColorPoint(<span class="number">25</span>, <span class="number">8</span>, <span class="string">'green'</span>);</span><br><span class="line"></span><br><span class="line">cp <span class="keyword">instanceof</span> ColorPoint <span class="comment">// true</span></span><br><span class="line">cp <span class="keyword">instanceof</span> Point <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，实例对象<code>cp</code>同时是<code>ColorPoint</code>和<code>Point</code>两个类的实例，这与 ES5 的行为完全一致。</p>
<p>最后，父类的静态方法，也会被子类继承。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> hello() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'hello world'</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">B.hello()  <span class="comment">// hello world</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>hello()</code>是<code>A</code>类的静态方法，<code>B</code>继承<code>A</code>，也继承了<code>A</code>的静态方法。</p>
<h2 id="Object-getPrototypeOf"><a href="#Object-getPrototypeOf" class="headerlink" title="Object.getPrototypeOf()"></a>Object.getPrototypeOf()</h2><p><code>Object.getPrototypeOf</code>方法可以用来从子类上获取父类。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.getPrototypeOf(ColorPoint) === Point</span><br><span class="line"><span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>因此，可以使用这个方法判断，一个类是否继承了另一个类。</p>
<h2 id="super-关键字"><a href="#super-关键字" class="headerlink" title="super 关键字"></a>super 关键字</h2><p><code>super</code>这个关键字，既可以当作函数使用，也可以当作对象使用。在这两种情况下，它的用法完全不同。</p>
<p>第一种情况，<code>super</code>作为函数调用时，代表父类的构造函数。ES6 要求，子类的构造函数必须执行一次<code>super</code>函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，子类<code>B</code>的构造函数之中的<code>super()</code>，代表调用父类的构造函数。这是必须的，否则 JavaScript 引擎会报错。</p>
<p>注意，<code>super</code>虽然代表了父类<code>A</code>的构造函数，但是返回的是子类<code>B</code>的实例，即<code>super</code>内部的<code>this</code>指的是<code>B</code>，因此<code>super()</code>在这里相当于<code>A.prototype.constructor.call(this)</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">new</span>.target.name);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">new</span> A() <span class="comment">// A</span></span><br><span class="line"><span class="keyword">new</span> B() <span class="comment">// B</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>new.target</code>指向当前正在执行的函数。可以看到，在<code>super()</code>执行时，它指向的是子类<code>B</code>的构造函数，而不是父类<code>A</code>的构造函数。也就是说，<code>super()</code>内部的<code>this</code>指向的是<code>B</code>。</p>
<p>作为函数时，<code>super()</code>只能用在子类的构造函数之中，用在其他地方就会报错。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  m() &#123;</span><br><span class="line">    <span class="keyword">super</span>(); <span class="comment">// 报错</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>super()</code>用在<code>B</code>类的<code>m</code>方法之中，就会造成句法错误。</p>
<p>第二种情况，<code>super</code>作为对象时，在普通方法中，指向父类的原型对象；在静态方法中，指向父类。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  p() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">super</span>.p()); <span class="comment">// 2</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="keyword">new</span> B();</span><br></pre></td></tr></table></figure>

<p>上面代码中，子类<code>B</code>当中的<code>super.p()</code>，就是将<code>super</code>当作一个对象使用。这时，<code>super</code>在普通方法之中，指向<code>A.prototype</code>，所以<code>super.p()</code>就相当于<code>A.prototype.p()</code>。</p>
<p>这里需要注意，由于<code>super</code>指向父类的原型对象，所以定义在父类实例上的方法或属性，是无法通过<code>super</code>调用的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.p = <span class="number">2</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">get</span> m() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">super</span>.p;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="keyword">new</span> B();</span><br><span class="line">b.m <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>p</code>是父类<code>A</code>实例的属性，<code>super.p</code>就引用不到它。</p>
<p>如果属性定义在父类的原型对象上，<code>super</code>就可以取到。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;&#125;</span><br><span class="line">A.prototype.x = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">super</span>.x) <span class="comment">// 2</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="keyword">new</span> B();</span><br></pre></td></tr></table></figure>

<p>上面代码中，属性<code>x</code>是定义在<code>A.prototype</code>上面的，所以<code>super.x</code>可以取到它的值。</p>
<p>ES6 规定，在子类普通方法中通过<code>super</code>调用父类的方法时，方法内部的<code>this</code>指向当前的子类实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.x = <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  print() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.x);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="keyword">this</span>.x = <span class="number">2</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  m() &#123;</span><br><span class="line">    <span class="keyword">super</span>.print();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="keyword">new</span> B();</span><br><span class="line">b.m() <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>super.print()</code>虽然调用的是<code>A.prototype.print()</code>，但是<code>A.prototype.print()</code>内部的<code>this</code>指向子类<code>B</code>的实例，导致输出的是<code>2</code>，而不是<code>1</code>。也就是说，实际上执行的是<code>super.print.call(this)</code>。</p>
<p>由于<code>this</code>指向子类实例，所以如果通过<code>super</code>对某个属性赋值，这时<code>super</code>就是<code>this</code>，赋值的属性会变成子类实例的属性。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.x = <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="keyword">this</span>.x = <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">super</span>.x = <span class="number">3</span>;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">super</span>.x); <span class="comment">// undefined</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.x); <span class="comment">// 3</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="keyword">new</span> B();</span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>super.x</code>赋值为<code>3</code>，这时等同于对<code>this.x</code>赋值为<code>3</code>。而当读取<code>super.x</code>的时候，读的是<code>A.prototype.x</code>，所以返回<code>undefined</code>。</p>
<p>如果<code>super</code>作为对象，用在静态方法之中，这时<code>super</code>将指向父类，而不是父类的原型对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> myMethod(msg) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'static'</span>, msg);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  myMethod(msg) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'instance'</span>, msg);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> myMethod(msg) &#123;</span><br><span class="line">    <span class="keyword">super</span>.myMethod(msg);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  myMethod(msg) &#123;</span><br><span class="line">    <span class="keyword">super</span>.myMethod(msg);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Child.myMethod(<span class="number">1</span>); <span class="comment">// static 1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> child = <span class="keyword">new</span> Child();</span><br><span class="line">child.myMethod(<span class="number">2</span>); <span class="comment">// instance 2</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>super</code>在静态方法之中指向父类，在普通方法之中指向父类的原型对象。</p>
<p>另外，在子类的静态方法中通过<code>super</code>调用父类的方法时，方法内部的<code>this</code>指向当前的子类，而不是子类的实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.x = <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">static</span> print() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.x);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="keyword">this</span>.x = <span class="number">2</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">static</span> m() &#123;</span><br><span class="line">    <span class="keyword">super</span>.print();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">B.x = <span class="number">3</span>;</span><br><span class="line">B.m() <span class="comment">// 3</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，静态方法<code>B.m</code>里面，<code>super.print</code>指向父类的静态方法。这个方法里面的<code>this</code>指向的是<code>B</code>，而不是<code>B</code>的实例。</p>
<p>注意，使用<code>super</code>的时候，必须显式指定是作为函数、还是作为对象使用，否则会报错。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">super</span>); <span class="comment">// 报错</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>console.log(super)</code>当中的<code>super</code>，无法看出是作为函数使用，还是作为对象使用，所以 JavaScript 引擎解析代码的时候就会报错。这时，如果能清晰地表明<code>super</code>的数据类型，就不会报错。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">super</span>.valueOf() <span class="keyword">instanceof</span> B); <span class="comment">// true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="keyword">new</span> B();</span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>super.valueOf()</code>表明<code>super</code>是一个对象，因此就不会报错。同时，由于<code>super</code>使得<code>this</code>指向<code>B</code>的实例，所以<code>super.valueOf()</code>返回的是一个<code>B</code>的实例。</p>
<p>最后，由于对象总是继承其他对象的，所以可以在任意一个对象中，使用<code>super</code>关键字。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  toString() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"MyObject: "</span> + <span class="keyword">super</span>.toString();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">obj.toString(); <span class="comment">// MyObject: [object Object]</span></span><br></pre></td></tr></table></figure>

<h2 id="类的-prototype-属性和-proto-属性"><a href="#类的-prototype-属性和-proto-属性" class="headerlink" title="类的 prototype 属性和__proto__属性"></a>类的 prototype 属性和__proto__属性</h2><p>大多数浏览器的 ES5 实现之中，每一个对象都有<code>__proto__</code>属性，指向对应的构造函数的<code>prototype</code>属性。Class 作为构造函数的语法糖，同时有<code>prototype</code>属性和<code>__proto__</code>属性，因此同时存在两条继承链。</p>
<p>（1）子类的<code>__proto__</code>属性，表示构造函数的继承，总是指向父类。</p>
<p>（2）子类<code>prototype</code>属性的<code>__proto__</code>属性，表示方法的继承，总是指向父类的<code>prototype</code>属性。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">B.__proto__ === A <span class="comment">// true</span></span><br><span class="line">B.prototype.__proto__ === A.prototype <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，子类<code>B</code>的<code>__proto__</code>属性指向父类<code>A</code>，子类<code>B</code>的<code>prototype</code>属性的<code>__proto__</code>属性指向父类<code>A</code>的<code>prototype</code>属性。</p>
<p>这样的结果是因为，类的继承是按照下面的模式实现的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// B 的实例继承 A 的实例</span></span><br><span class="line"><span class="built_in">Object</span>.setPrototypeOf(B.prototype, A.prototype);</span><br><span class="line"></span><br><span class="line"><span class="comment">// B 继承 A 的静态属性</span></span><br><span class="line"><span class="built_in">Object</span>.setPrototypeOf(B, A);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> b = <span class="keyword">new</span> B();</span><br></pre></td></tr></table></figure>

<p>《对象的扩展》一章给出过<code>Object.setPrototypeOf</code>方法的实现。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.setPrototypeOf = <span class="function"><span class="keyword">function</span> (<span class="params">obj, proto</span>) </span>&#123;</span><br><span class="line">  obj.__proto__ = proto;</span><br><span class="line">  <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因此，就得到了上面的结果。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.setPrototypeOf(B.prototype, A.prototype);</span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line">B.prototype.__proto__ = A.prototype;</span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.setPrototypeOf(B, A);</span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line">B.__proto__ = A;</span><br></pre></td></tr></table></figure>

<p>这两条继承链，可以这样理解：作为一个对象，子类（<code>B</code>）的原型（<code>__proto__</code>属性）是父类（<code>A</code>）；作为一个构造函数，子类（<code>B</code>）的原型对象（<code>prototype</code>属性）是父类的原型对象（<code>prototype</code>属性）的实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.create(A.prototype);</span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line">B.prototype.__proto__ = A.prototype;</span><br></pre></td></tr></table></figure>

<p><code>extends</code>关键字后面可以跟多种类型的值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码的<code>A</code>，只要是一个有<code>prototype</code>属性的函数，就能被<code>B</code>继承。由于函数都有<code>prototype</code>属性（除了<code>Function.prototype</code>函数），因此<code>A</code>可以是任意函数。</p>
<p>下面，讨论两种情况。第一种，子类继承<code>Object</code>类。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> <span class="keyword">extends</span> <span class="title">Object</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">A.__proto__ === <span class="built_in">Object</span> <span class="comment">// true</span></span><br><span class="line">A.prototype.__proto__ === <span class="built_in">Object</span>.prototype <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>这种情况下，<code>A</code>其实就是构造函数<code>Object</code>的复制，<code>A</code>的实例就是<code>Object</code>的实例。</p>
<p>第二种情况，不存在任何继承。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">A.__proto__ === <span class="built_in">Function</span>.prototype <span class="comment">// true</span></span><br><span class="line">A.prototype.__proto__ === <span class="built_in">Object</span>.prototype <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>这种情况下，<code>A</code>作为一个基类（即不存在任何继承），就是一个普通函数，所以直接继承<code>Function.prototype</code>。但是，<code>A</code>调用后返回一个空对象（即<code>Object</code>实例），所以<code>A.prototype.__proto__</code>指向构造函数（<code>Object</code>）的<code>prototype</code>属性。</p>
<h3 id="实例的-proto-属性"><a href="#实例的-proto-属性" class="headerlink" title="实例的 __proto__ 属性"></a>实例的 __proto__ 属性</h3><p>子类实例的<code>__proto__</code>属性的<code>__proto__</code>属性，指向父类实例的<code>__proto__</code>属性。也就是说，子类的原型的原型，是父类的原型。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p1 = <span class="keyword">new</span> Point(<span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line"><span class="keyword">var</span> p2 = <span class="keyword">new</span> ColorPoint(<span class="number">2</span>, <span class="number">3</span>, <span class="string">'red'</span>);</span><br><span class="line"></span><br><span class="line">p2.__proto__ === p1.__proto__ <span class="comment">// false</span></span><br><span class="line">p2.__proto__.__proto__ === p1.__proto__ <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>ColorPoint</code>继承了<code>Point</code>，导致前者原型的原型是后者的原型。</p>
<p>因此，通过子类实例的<code>__proto__.__proto__</code>属性，可以修改父类实例的行为。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">p2.__proto__.__proto__.printName = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Ha'</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">p1.printName() <span class="comment">// "Ha"</span></span><br></pre></td></tr></table></figure>

<p>上面代码在<code>ColorPoint</code>的实例<code>p2</code>上向<code>Point</code>类添加方法，结果影响到了<code>Point</code>的实例<code>p1</code>。</p>
<h2 id="原生构造函数的继承"><a href="#原生构造函数的继承" class="headerlink" title="原生构造函数的继承"></a>原生构造函数的继承</h2><p>原生构造函数是指语言内置的构造函数，通常用来生成数据结构。ECMAScript 的原生构造函数大致有下面这些。</p>
<ul>
<li>Boolean()</li>
<li>Number()</li>
<li>String()</li>
<li>Array()</li>
<li>Date()</li>
<li>Function()</li>
<li>RegExp()</li>
<li>Error()</li>
<li>Object()</li>
</ul>
<p>以前，这些原生构造函数是无法继承的，比如，不能自己定义一个<code>Array</code>的子类。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyArray</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">Array</span>.apply(<span class="keyword">this</span>, <span class="built_in">arguments</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">MyArray.prototype = <span class="built_in">Object</span>.create(<span class="built_in">Array</span>.prototype, &#123;</span><br><span class="line">  <span class="keyword">constructor</span>: &#123;</span><br><span class="line">    value: MyArray,</span><br><span class="line">    writable: <span class="literal">true</span>,</span><br><span class="line">    configurable: <span class="literal">true</span>,</span><br><span class="line">    enumerable: <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>上面代码定义了一个继承 Array 的<code>MyArray</code>类。但是，这个类的行为与<code>Array</code>完全不一致。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> colors = <span class="keyword">new</span> MyArray();</span><br><span class="line">colors[<span class="number">0</span>] = <span class="string">"red"</span>;</span><br><span class="line">colors.length  <span class="comment">// 0</span></span><br><span class="line"></span><br><span class="line">colors.length = <span class="number">0</span>;</span><br><span class="line">colors[<span class="number">0</span>]  <span class="comment">// "red"</span></span><br></pre></td></tr></table></figure>

<p>之所以会发生这种情况，是因为子类无法获得原生构造函数的内部属性，通过<code>Array.apply()</code>或者分配给原型对象都不行。原生构造函数会忽略<code>apply</code>方法传入的<code>this</code>，也就是说，原生构造函数的<code>this</code>无法绑定，导致拿不到内部属性。</p>
<p>ES5 是先新建子类的实例对象<code>this</code>，再将父类的属性添加到子类上，由于父类的内部属性无法获取，导致无法继承原生的构造函数。比如，<code>Array</code>构造函数有一个内部属性<code>[[DefineOwnProperty]]</code>，用来定义新属性时，更新<code>length</code>属性，这个内部属性无法在子类获取，导致子类的<code>length</code>属性行为不正常。</p>
<p>下面的例子中，我们想让一个普通对象继承<code>Error</code>对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> e = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyNames(<span class="built_in">Error</span>.call(e))</span><br><span class="line"><span class="comment">// [ 'stack' ]</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyNames(e)</span><br><span class="line"><span class="comment">// []</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，我们想通过<code>Error.call(e)</code>这种写法，让普通对象<code>e</code>具有<code>Error</code>对象的实例属性。但是，<code>Error.call()</code>完全忽略传入的第一个参数，而是返回一个新对象，<code>e</code>本身没有任何变化。这证明了<code>Error.call(e)</code>这种写法，无法继承原生构造函数。</p>
<p>ES6 允许继承原生构造函数定义子类，因为 ES6 是先新建父类的实例对象<code>this</code>，然后再用子类的构造函数修饰<code>this</code>，使得父类的所有行为都可以继承。下面是一个继承<code>Array</code>的例子。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyArray</span> <span class="keyword">extends</span> <span class="title">Array</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(...args) &#123;</span><br><span class="line">    <span class="keyword">super</span>(...args);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> MyArray();</span><br><span class="line">arr[<span class="number">0</span>] = <span class="number">12</span>;</span><br><span class="line">arr.length <span class="comment">// 1</span></span><br><span class="line"></span><br><span class="line">arr.length = <span class="number">0</span>;</span><br><span class="line">arr[<span class="number">0</span>] <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>

<p>上面代码定义了一个<code>MyArray</code>类，继承了<code>Array</code>构造函数，因此就可以从<code>MyArray</code>生成数组的实例。这意味着，ES6 可以自定义原生数据结构（比如<code>Array</code>、<code>String</code>等）的子类，这是 ES5 无法做到的。</p>
<p>上面这个例子也说明，<code>extends</code>关键字不仅可以用来继承类，还可以用来继承原生的构造函数。因此可以在原生数据结构的基础上，定义自己的数据结构。下面就是定义了一个带版本功能的数组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">VersionedArray</span> <span class="keyword">extends</span> <span class="title">Array</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="keyword">this</span>.history = [[]];</span><br><span class="line">  &#125;</span><br><span class="line">  commit() &#123;</span><br><span class="line">    <span class="keyword">this</span>.history.push(<span class="keyword">this</span>.slice());</span><br><span class="line">  &#125;</span><br><span class="line">  revert() &#123;</span><br><span class="line">    <span class="keyword">this</span>.splice(<span class="number">0</span>, <span class="keyword">this</span>.length, ...this.history[<span class="keyword">this</span>.history.length - <span class="number">1</span>]);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> x = <span class="keyword">new</span> VersionedArray();</span><br><span class="line"></span><br><span class="line">x.push(<span class="number">1</span>);</span><br><span class="line">x.push(<span class="number">2</span>);</span><br><span class="line">x <span class="comment">// [1, 2]</span></span><br><span class="line">x.history <span class="comment">// [[]]</span></span><br><span class="line"></span><br><span class="line">x.commit();</span><br><span class="line">x.history <span class="comment">// [[], [1, 2]]</span></span><br><span class="line"></span><br><span class="line">x.push(<span class="number">3</span>);</span><br><span class="line">x <span class="comment">// [1, 2, 3]</span></span><br><span class="line">x.history <span class="comment">// [[], [1, 2]]</span></span><br><span class="line"></span><br><span class="line">x.revert();</span><br><span class="line">x <span class="comment">// [1, 2]</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>VersionedArray</code>会通过<code>commit</code>方法，将自己的当前状态生成一个版本快照，存入<code>history</code>属性。<code>revert</code>方法用来将数组重置为最新一次保存的版本。除此之外，<code>VersionedArray</code>依然是一个普通数组，所有原生的数组方法都可以在它上面调用。</p>
<p>下面是一个自定义<code>Error</code>子类的例子，可以用来定制报错时的行为。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ExtendableError</span> <span class="keyword">extends</span> <span class="title">Error</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(message) &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="keyword">this</span>.message = message;</span><br><span class="line">    <span class="keyword">this</span>.stack = (<span class="keyword">new</span> <span class="built_in">Error</span>()).stack;</span><br><span class="line">    <span class="keyword">this</span>.name = <span class="keyword">this</span>.constructor.name;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyError</span> <span class="keyword">extends</span> <span class="title">ExtendableError</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(m) &#123;</span><br><span class="line">    <span class="keyword">super</span>(m);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> myerror = <span class="keyword">new</span> MyError(<span class="string">'ll'</span>);</span><br><span class="line">myerror.message <span class="comment">// "ll"</span></span><br><span class="line">myerror <span class="keyword">instanceof</span> <span class="built_in">Error</span> <span class="comment">// true</span></span><br><span class="line">myerror.name <span class="comment">// "MyError"</span></span><br><span class="line">myerror.stack</span><br><span class="line"><span class="comment">// Error</span></span><br><span class="line"><span class="comment">//     at MyError.ExtendableError</span></span><br><span class="line"><span class="comment">//     ...</span></span><br></pre></td></tr></table></figure>

<p>注意，继承<code>Object</code>的子类，有一个<a href="http://stackoverflow.com/questions/36203614/super-does-not-pass-arguments-when-instantiating-a-class-extended-from-object" target="_blank" rel="noopener">行为差异</a>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NewObj</span> <span class="keyword">extends</span> <span class="title">Object</span></span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>()&#123;</span><br><span class="line">    <span class="keyword">super</span>(...arguments);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> o = <span class="keyword">new</span> NewObj(&#123;<span class="attr">attr</span>: <span class="literal">true</span>&#125;);</span><br><span class="line">o.attr === <span class="literal">true</span>  <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>NewObj</code>继承了<code>Object</code>，但是无法通过<code>super</code>方法向父类<code>Object</code>传参。这是因为 ES6 改变了<code>Object</code>构造函数的行为，一旦发现<code>Object</code>方法不是通过<code>new Object()</code>这种形式调用，ES6 规定<code>Object</code>构造函数会忽略参数。</p>
<h2 id="Mixin-模式的实现"><a href="#Mixin-模式的实现" class="headerlink" title="Mixin 模式的实现"></a>Mixin 模式的实现</h2><p>Mixin 指的是多个对象合成一个新的对象，新对象具有各个组成成员的接口。它的最简单实现如下。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> a = &#123;</span><br><span class="line">  a: <span class="string">'a'</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">const</span> b = &#123;</span><br><span class="line">  b: <span class="string">'b'</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">const</span> c = &#123;...a, ...b&#125;; <span class="comment">// &#123;a: 'a', b: 'b'&#125;</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，<code>c</code>对象是<code>a</code>对象和<code>b</code>对象的合成，具有两者的接口。</p>
<p>下面是一个更完备的实现，将多个类的接口“混入”（mix in）另一个类。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">mix</span>(<span class="params">...mixins</span>) </span>&#123;</span><br><span class="line">  <span class="class"><span class="keyword">class</span> <span class="title">Mix</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> mixin <span class="keyword">of</span> mixins) &#123;</span><br><span class="line">    copyProperties(Mix.prototype, mixin); <span class="comment">// 拷贝实例属性</span></span><br><span class="line">    copyProperties(Mix.prototype, <span class="built_in">Reflect</span>.getPrototypeOf(mixin)); <span class="comment">// 拷贝原型属性</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> Mix;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">copyProperties</span>(<span class="params">target, source</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> key <span class="keyword">of</span> <span class="built_in">Reflect</span>.ownKeys(source)) &#123;</span><br><span class="line">    <span class="keyword">if</span> ( key !== <span class="string">"constructor"</span></span><br><span class="line">      &amp;&amp; key !== <span class="string">"prototype"</span></span><br><span class="line">      &amp;&amp; key !== <span class="string">"name"</span></span><br><span class="line">    ) &#123;</span><br><span class="line">      <span class="keyword">let</span> desc = <span class="built_in">Object</span>.getOwnPropertyDescriptor(source, key);</span><br><span class="line">      <span class="built_in">Object</span>.defineProperty(target, key, desc);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码的<code>mix</code>函数，可以将多个对象合成为一个类。使用的时候，只要继承这个类即可。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DistributedEdit</span> <span class="keyword">extends</span> <span class="title">mix</span>(<span class="title">Loggable</span>, <span class="title">Serializable</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>自用正则手册</title>
    <url>/2018/06/06/article-js-regex/</url>
    <content><![CDATA[<p>正则表达式regex是用于匹配字符串中字符组合的模式，由参数pattern + 标志flags构成。</p>
<blockquote>
<p>参考学习 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Regular_Expressions" target="_blank" rel="noopener">MDN：正则表达式</a></p>
</blockquote>
<blockquote>
<p>参考学习 <a href="http://es6.ruanyifeng.com/#docs/regex" target="_blank" rel="noopener">阮一峰：ES6正则的扩展</a></p>
</blockquote>
<blockquote>
<p>参考学习 <a href="http://www.runoob.com/regexp/regexp-syntax.html" target="_blank" rel="noopener">菜鸟教程：正则表达式</a></p>
</blockquote>
<a id="more"></a>

<h2 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h2><h3 id="普通字符"><a href="#普通字符" class="headerlink" title="普通字符"></a>普通字符</h3><p>指所有字母、数字、符号等</p>
<h3 id="非打印字符"><a href="#非打印字符" class="headerlink" title="非打印字符"></a>非打印字符</h3><p>指换行、回车、空白等不会实际显示出来的字符</p>
<table>
<thead>
<tr>
<th align="left">字符</th>
<th align="left">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="left">\cx</td>
<td align="left">匹配由x指明的控制字符。例如， \cM 匹配一个 Control-M 或回车符。x 的值必须为 A-Z 或 a-z 之一。否则，将 c 视为一个原义的 ‘c’ 字符。</td>
</tr>
<tr>
<td align="left">\f</td>
<td align="left">匹配一个换页符。等价于 \x0c 和 \cL。</td>
</tr>
<tr>
<td align="left">\n</td>
<td align="left">匹配一个换行符。等价于 \x0a 和 \cJ。</td>
</tr>
<tr>
<td align="left">\r</td>
<td align="left">匹配一个回车符。等价于 \x0d 和 \cM。</td>
</tr>
<tr>
<td align="left">\s</td>
<td align="left">匹配任何空白字符，包括空格、制表符、换页符等等。等价于 [ \f\n\r\t\v]。注意 Unicode 正则表达式会匹配全角空格符。</td>
</tr>
<tr>
<td align="left">\S</td>
<td align="left">匹配任何非空白字符。等价于 [^ \f\n\r\t\v]。</td>
</tr>
<tr>
<td align="left">\t</td>
<td align="left">匹配一个制表符。等价于 \x09 和 \cI。</td>
</tr>
<tr>
<td align="left">\v</td>
<td align="left">匹配一个垂直制表符。等价于 \x0b 和 \cK。</td>
</tr>
</tbody></table>
<h3 id="限定符"><a href="#限定符" class="headerlink" title="限定符"></a>限定符</h3><p>指定匹配前面的子表达式必须要出现多少次才能满足</p>
<table>
<thead>
<tr>
<th align="left">字符</th>
<th align="left">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="left">*</td>
<td align="left">零次或多次。例如，zo* 能匹配 “z” 以及 “zoo”。* 等价于{0,}。</td>
</tr>
<tr>
<td align="left">+</td>
<td align="left">一次或多次。例如，’zo+’ 能匹配 “zo” 以及 “zoo”，但不能匹配 “z”。+ 等价于 {1,}。</td>
</tr>
<tr>
<td align="left">?</td>
<td align="left">零次或一次。例如，”do(es)?” 可以匹配 “do” 或 “does” 。? 等价于 {0,1}。</td>
</tr>
<tr>
<td align="left">{n}</td>
<td align="left">n &gt;= 0。匹配确定的 n 次。例如，’o{2}’ 不能匹配 “Bob” 中的 ‘o’，但是能匹配 “food” 中的两个 o。</td>
</tr>
<tr>
<td align="left">{n,}</td>
<td align="left">n &gt;= 0。至少匹配 n 次。例如，’o{2,}’ 不能匹配 “Bob” 中的 ‘o’，但能匹配 “foooood” 中的所有 o。’o{1,}’ 等价于 ‘o+’。’o{0,}’ 则等价于 ‘o*’。</td>
</tr>
<tr>
<td align="left">{n,m}</td>
<td align="left">m &gt;= n&gt;= 0。例如，”o{1,3}” 将匹配 “fooooood” 中的前三个 o。’o{0,1}’ 等价于 ‘o?’。逗号和两个数之间不能有空格。</td>
</tr>
</tbody></table>
<h3 id="定位符"><a href="#定位符" class="headerlink" title="定位符"></a>定位符</h3><p>描述字符串定边界</p>
<table>
<thead>
<tr>
<th align="left">字符</th>
<th align="left">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="left">^</td>
<td align="left">匹配输入字符串的开始位置。如果设置了 RegExp 对象的 Multiline 属性，^ 也匹配 ‘\n’ 或 ‘\r’ 之后的位置。</td>
</tr>
<tr>
<td align="left">$</td>
<td align="left">匹配输入字符串的结束位置。如果设置了RegExp 对象的 Multiline 属性，$ 也匹配 ‘\n’ 或 ‘\r’ 之前的位置。</td>
</tr>
<tr>
<td align="left">\b</td>
<td align="left">匹配一个单词边界，也就是指单词和空格间的位置。例如， ‘er\b’ 可以匹配”never” 中的 ‘er’，但不能匹配 “verb” 中的 ‘er’。</td>
</tr>
<tr>
<td align="left">\B</td>
<td align="left">匹配非单词边界。’er\B’ 能匹配 “verb” 中的 ‘er’，但不能匹配 “never” 中的 ‘er’。</td>
</tr>
</tbody></table>
<h3 id="圆括号"><a href="#圆括号" class="headerlink" title="圆括号"></a>圆括号</h3><p>组，应用于限制多选结构的范围/分组/捕获文本/环视/特殊模式处理</p>
<table><thead><tr><th>字符</th><th align="left">说明</th></tr></thead><tbody><tr><td>(abc)</td><td align="left">匹配 'abc' 并且记住匹配项，括号被称为捕获括号。模式/(foo) (bar) \1 \2/中的 '(foo)' 和 '(bar)' 匹配并记住字符串 "foo bar foo bar" 中前两个单词。模式中的 \1 和 \2 匹配字符串的后两个单词。注意 \1、\2、\n 是用在正则表达式的匹配环节。在正则表达式的替换环节，则要使用像 $1、$2、$n 这样的语法，例如，'bar foo'.replace( /(...) (...)/, '$2 $1' )。</td></tr><tr><td>(?:abc)</td><td align="left">匹配 'abc' 这样一组，但不记录，不保存到$变量中，否则可以通过$x取第几个括号所匹配到的项，比如：(aaa)(bbb)(ccc)(?:ddd)(eee)，可以用$1获取(aaa)匹配到的内容，而$3则获取到了(ccc)匹配到的内容，而$4则获取的是由(eee)匹配到的内容，因为前一对括号没有保存变量</td></tr><tr><td>a(?=bbb)</td><td align="left">正向肯定查找，表示a后面必须紧跟3个连续的b。例如，"Windows(?=95|98|NT|2000)"能匹配"Windows2000"中的"Windows"，但不能匹配"Windows3.1"中的"Windows"。预查不消耗字符，也就是说，在一个匹配发生后，在最后一次匹配之后立即开始下一次匹配的搜索，而不是从包含预查的字符之后开始。</td></tr><tr><td>a(?!bbb)</td><td align="left">正向否定查找，表示a后面不能跟3个连续的b。例如"Windows(?!95|98|NT|2000)"能匹配"Windows3.1"中的"Windows"，但不能匹配"Windows2000"中的"Windows"。</td></tr><tr><td>(?&lt;=bbb)a</td><td align="left">反向肯定查找，表示a前面必须紧跟3个连续的b。例如，"(?&lt;=95|98|NT|2000)Windows"能匹配"2000Windows"中的"Windows"，但不能匹配"3.1Windows"中的"Windows"。</td></tr><tr><td>(?&lt;!bbb)a</td><td align="left">反向否定查找，表示a前面不能跟3个连续的b。例如"(?&lt;!95|98|NT|2000)Windows"能匹配"3.1Windows"中的"Windows"，但不能匹配"2000Windows"中的"Windows"。</td></tr></tbody></table>

<h3 id="中括号"><a href="#中括号" class="headerlink" title="中括号"></a>中括号</h3><p>单个匹配，字符集/排除字符集/命名字符集</p>
<table><thead><tr><th>字符</th><th align="left">说明</th></tr></thead><tbody><tr><td>[xyz]</td><td align="left">字符集合。匹配所包含的任意一个字符。例如， '[abc]' 可以匹配 "plain" 中的 'a'。</td></tr><tr><td>[^xyz]</td><td align="left">负值字符集合。匹配未包含的任意字符。例如， '[^abc]' 可以匹配 "plain" 中的'p'、'l'、'i'、'n'。</td></tr><tr><td>[a-z]</td><td align="left">字符范围。匹配指定范围内的任意字符。例如，'[a-z]' 可以匹配 'a' 到 'z' 范围内的任意小写字母字符。</td></tr><tr><td>[^a-z]</td><td align="left">负值字符范围。匹配任何不在指定范围内的任意字符。例如，'[^a-z]' 可以匹配任何不在 'a' 到 'z' 范围内的任意字符。</td></tr></tbody></table>

<h3 id="其他特殊字符"><a href="#其他特殊字符" class="headerlink" title="其他特殊字符"></a>其他特殊字符</h3><table><thead><tr><th>字符</th><th align="left">说明</th></tr></thead><tbody><tr><td>\</td><td align="left">将下一个字符标记为一个特殊字符、或一个原义字符、或一个 向后引用、或一个八进制转义符。例如，'n' 匹配字符 "n"。'\n' 匹配一个换行符，而 "\(" 则匹配 "("。</td></tr><tr><td>?</td><td align="left">当该字符紧跟在任何一个其他限制符 (*, +, ?, {n}, {n,}, {n,m}) 后面时，匹配模式是非贪婪的。非贪婪模式尽可能少的匹配所搜索的字符串，而默认的贪婪模式则尽可能多的匹配所搜索的字符串。例如，对于字符串 "oooo"，'o+?' 将匹配单个 "o"，而 'o+' 将匹配所有 'o'。</td></tr><tr><td>.</td><td align="left">匹配除换行符（\n、\r）之外的任何单个字符。要匹配包括 '\n' 在内的任何字符，请使用像"(.|\n)"的模式。</td></tr><tr><td>x|y</td><td align="left">匹配 x 或 y。例如，'z|food' 能匹配 "z" 或 "food"。'(z|f)ood' 则匹配 "zood" 或 "food"。</td></tr><tr><td>\d</td><td align="left">匹配一个数字字符。等价于 [0-9]。</td></tr><tr><td>\D</td><td align="left">匹配一个非数字字符。等价于 [^0-9]。</td></tr><tr><td>\w</td><td align="left">匹配字母、数字、下划线。等价于'[A-Za-z0-9_]'。</td></tr><tr><td>\W</td><td align="left">匹配非字母、数字、下划线。等价于 '[^A-Za-z0-9_]'。</td></tr><tr><td>\xn</td><td align="left">匹配 n，其中 n 为十六进制转义值。十六进制转义值必须为确定的两个数字长。例如，'\x41' 匹配 "A"。'\x041' 则等价于 '\x04' &amp; "1"。正则表达式中可以使用 ASCII 编码。</td></tr><tr><td>\num</td><td align="left">匹配 num，其中 num 是一个正整数。对所获取的匹配的引用。例如，'(.)\1' 匹配两个连续的相同字符</td></tr><tr><td>\n</td><td align="left">标识一个八进制转义值或一个向后引用。如果 \n 之前至少 n 个获取的子表达式，则 n 为向后引用。否则，如果 n 为八进制数字 (0-7)，则 n 为一个八进制转义值。</td></tr><tr><td>\nm</td><td align="left">标识一个八进制转义值或一个向后引用。如果 \nm 之前至少有 nm 个获得子表达式，则 nm 为向后引用。如果 \nm 之前至少有 n 个获取，则 n 为一个后跟文字 m 的向后引用。如果前面的条件都不满足，若 n 和 m 均为八进制数字 (0-7)，则 \nm 将匹配八进制转义值 nm。</td></tr><tr><td>\nml</td><td align="left">如果 n 为八进制数字 (0-3)，且 m 和 l 均为八进制数字 (0-7)，则匹配八进制转义值 nml。</td></tr><tr><td>\un</td><td align="left">匹配 n，其中 n 是一个用四个十六进制数字表示的 Unicode 字符。例如， \u00A9 匹配版权符号 (?)。</td></tr></tbody></table>

<h2 id="标志"><a href="#标志" class="headerlink" title="标志"></a>标志</h2><p>有6个标志，可单独或一起使用</p>
<table><thead><tr><th align="left">flags</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left">g</td><td align="left">全局搜索</td></tr><tr><td align="left">i</td><td align="left">不区分大小写搜索</td></tr><tr><td align="left">m</td><td align="left">多行搜索</td></tr><tr><td align="left">u</td><td align="left">正确处理四个字节的 UTF-16 编码</td></tr><tr><td align="left">y</td><td align="left">粘连搜索</td></tr><tr><td align="left">s</td><td align="left">dotAll模式，即点（dot）代表一切字符</td></tr></tbody></table>

<h2 id="创建"><a href="#创建" class="headerlink" title="创建"></a>创建</h2><p>有以下两种方式构建一个正则表达式：</p>
<h3 id="字面量：由包含在斜杠之间的模式组成"><a href="#字面量：由包含在斜杠之间的模式组成" class="headerlink" title="字面量：由包含在斜杠之间的模式组成"></a>字面量：由包含在斜杠之间的模式组成</h3><ul>
<li><p>格式： pattern/flags</p>
</li>
<li><p>优点：加载时编译，性能好</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> regex = <span class="regexp">/ab+c/</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> regex = <span class="regexp">/hello/gi</span>;</span><br></pre></td></tr></table></figure>

<h3 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h3><ul>
<li><p>格式：new RegExp(pattern [, flags])</p>
</li>
<li><p>优点：运行时编译，可动态修改</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> regex = <span class="keyword">new</span> <span class="built_in">RegExp</span>(<span class="string">"ab+c"</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> regex = <span class="keyword">new</span> <span class="built_in">RegExp</span>(<span class="regexp">/hello/</span>, <span class="string">"gi"</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> regex = <span class="keyword">new</span> <span class="built_in">RegExp</span>(<span class="regexp">/hello/gi</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> regex = <span class="keyword">new</span> <span class="built_in">RegExp</span>(<span class="string">"hello"</span>, <span class="string">"gi"</span>);</span><br></pre></td></tr></table></figure>

<h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> regex = <span class="regexp">/nn/</span>;</span><br><span class="line"><span class="keyword">let</span> str = <span class="string">'runnobnnnbnn'</span>;</span><br></pre></td></tr></table></figure>

<table><thead><tr><th align="left">方法</th><th align="left">说明</th><th align="left">示例</th><th align="left">返回值</th></tr></thead><tbody><tr><td align="left">test</td><td align="left">测试是否匹配，返回true或false</td><td align="left">regex.test(str)</td><td align="left">true</td></tr><tr><td align="left">exec</td><td align="left">查找匹配的内容，有则返回一个数组，未匹配则返回null</td><td align="left">regex.exec(str)</td><td align="left">["nn", index: 2, input: "runnobnnnbnn", groups: undefined]</td></tr><tr><td align="left">match</td><td align="left">查找匹配的内容，有则返回一个数组，未匹配则返回null</td><td align="left">str.match(regex)</td><td align="left">["nn", index: 2, input: "runnobnnnbnn", groups: undefined]</td></tr><tr><td align="left">search</td><td align="left">查找匹配的内容，有则返回位置索引，未匹配则返回-1</td><td align="left">str.match(regex)</td><td align="left">2</td></tr><tr><td align="left">replace</td><td align="left">查找匹配的内容，并且使用替换字符str1串替换掉匹配到的子字符串</td><td align="left">str.replace(regex, str1)</td><td align="left">ruccobnnnbnn</td></tr><tr><td align="left">split</td><td align="left">使用正则表达式或者一个固定字符串分隔一个字符串，并将分隔后的子字符串存储到数组中</td><td align="left">str.split(regex)</td><td align="left">["ru", "ob", "nb", ""]</td></tr></tbody></table>

<ul>
<li>match：非全局匹配时，跟exec很相似；全局匹配时，就大不同了，上面的例子，在全局匹配时的返回值如下：</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">regex.exec(str) <span class="comment">// ["nn", index: 6, input: "runnobnnnbnn", groups: undefined]</span></span><br><span class="line"></span><br><span class="line">str.match(regex) <span class="comment">// ["nn", "nn", "nn"]</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>jQuery API 替代的方法</title>
    <url>/2018/05/20/article-js-jq-native/</url>
    <content><![CDATA[<p>前端发展很快，现代浏览器原生 API 已经足够好用。我们并不需要为了操作 DOM、Event 等再学习一下 jQuery 的 API。同时由于 React、Angular、Vue 等框架的流行，直接操作 DOM 不再是好的模式，jQuery 使用场景大大减少。本项目总结了大部分 jQuery API 替代的方法，暂时只支持 IE10 以上浏览器。<a id="more"></a></p>
<h2 id="Query-Selector"><a href="#Query-Selector" class="headerlink" title="Query Selector"></a>Query Selector</h2><p>常用的 class、id、属性 选择器都可以使用 <code>document.querySelector</code> 或 <code>document.querySelectorAll</code> 替代。区别是</p>
<ul>
<li><code>document.querySelector</code> 返回第一个匹配的 Element</li>
<li><code>document.querySelectorAll</code> 返回所有匹配的 Element 组成的 NodeList。它可以通过 <code>[].slice.call()</code> 把它转成 Array</li>
<li>如果匹配不到任何 Element，jQuery 返回空数组 <code>[]</code>，但 <code>document.querySelector</code> 返回 <code>null</code>，注意空指针异常。当找不到时，也可以使用 <code>||</code> 设置默认的值，如 <code>document.querySelectorAll(selector) || []</code></li>
</ul>
<blockquote>
<p>注意：<code>document.querySelector</code> 和 <code>document.querySelectorAll</code> 性能很<strong>差</strong>。如果想提高性能，尽量使用 <code>document.getElementById</code>、<code>document.getElementsByClassName</code> 或 <code>document.getElementsByTagName</code>。</p>
</blockquote>
<ul>
<li><a href="#1.0">1.0</a> <a name="1.0"></a> 选择器查询</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">'selector'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">document</span>.querySelectorAll(<span class="string">'selector'</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#1.1">1.1</a> <a name="1.1"></a> class 查询</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">'.class'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">document</span>.querySelectorAll(<span class="string">'.class'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// or</span></span><br><span class="line"><span class="built_in">document</span>.getElementsByClassName(<span class="string">'class'</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#1.2">1.2</a> <a name="1.2"></a> id 查询</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">'#id'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#id'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// or</span></span><br><span class="line"><span class="built_in">document</span>.getElementById(<span class="string">'id'</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#1.3">1.3</a> <a name="1.3"></a> 属性查询</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">'a[target=_blank]'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">document</span>.querySelectorAll(<span class="string">'a[target=_blank]'</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#1.4">1.4</a> <a name="1.4"></a> 后代查询</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.find(<span class="string">'li'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.querySelectorAll(<span class="string">'li'</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#1.5">1.5</a> <a name="1.5"></a> 兄弟及上下元素</p>
<ul>
<li>兄弟元素</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.siblings();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native - latest, Edge13+</span></span><br><span class="line">[...el.parentNode.children].filter(<span class="function">(<span class="params">child</span>) =&gt;</span></span><br><span class="line">  child !== el</span><br><span class="line">);</span><br><span class="line"><span class="comment">// Native (alternative) - latest, Edge13+</span></span><br><span class="line"><span class="built_in">Array</span>.from(el.parentNode.children).filter(<span class="function">(<span class="params">child</span>) =&gt;</span></span><br><span class="line">  child !== el</span><br><span class="line">);</span><br><span class="line"><span class="comment">// Native - IE10+</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.filter.call(el.parentNode.children, (child) =&gt;</span><br><span class="line">  child !== el</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<ul>
<li>上一个元素</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.prev();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.previousElementSibling;</span><br></pre></td></tr></table></figure>

<ul>
<li>下一个元素</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// next</span></span><br><span class="line">$el.next();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.nextElementSibling;</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#1.6">1.6</a> <a name="1.6"></a> Closest</p>
<p>Closest 获得匹配选择器的第一个祖先元素，从当前元素开始沿 DOM 树向上。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.closest(queryString);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native - Only latest, NO IE</span></span><br><span class="line">el.closest(selector);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native - IE10+</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">closest</span>(<span class="params">el, selector</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> matchesSelector = el.matches || el.webkitMatchesSelector || el.mozMatchesSelector || el.msMatchesSelector;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (el) &#123;</span><br><span class="line">    <span class="keyword">if</span> (matchesSelector.call(el, selector)) &#123;</span><br><span class="line">      <span class="keyword">return</span> el;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      el = el.parentElement;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#1.7">1.7</a> <a name="1.7"></a> Parents Until</p>
<p>获取当前每一个匹配元素集的祖先，不包括匹配元素的本身。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.parentsUntil(selector, filter);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">parentsUntil</span>(<span class="params">el, selector, filter</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> result = [];</span><br><span class="line">  <span class="keyword">const</span> matchesSelector = el.matches || el.webkitMatchesSelector || el.mozMatchesSelector || el.msMatchesSelector;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// match start from parent</span></span><br><span class="line">  el = el.parentElement;</span><br><span class="line">  <span class="keyword">while</span> (el &amp;&amp; !matchesSelector.call(el, selector)) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!filter) &#123;</span><br><span class="line">      result.push(el);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (matchesSelector.call(el, filter)) &#123;</span><br><span class="line">        result.push(el);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    el = el.parentElement;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#1.8">1.8</a> <a name="1.8"></a> Form</p>
<ul>
<li>Input/Textarea</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">'#my-input'</span>).val();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#my-input'</span>).value;</span><br></pre></td></tr></table></figure>

<ul>
<li>获取 e.currentTarget 在 <code>.radio</code> 中的数组索引</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">'.radio'</span>).index(e.currentTarget);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.indexOf.call(<span class="built_in">document</span>.querySelectorAll(<span class="string">'.radio'</span>), e.currentTarget);</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#1.9">1.9</a> <a name="1.9"></a> Iframe Contents</p>
<p>jQuery 对象的 iframe <code>contents()</code> 返回的是 iframe 内的 <code>document</code></p>
<ul>
<li>Iframe contents</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$iframe.contents();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">iframe.contentDocument;</span><br></pre></td></tr></table></figure>

<ul>
<li>Iframe Query</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$iframe.contents().find(<span class="string">'.css'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">iframe.contentDocument.querySelectorAll(<span class="string">'.css'</span>);</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#1.10">1.10</a> <a name="1.10"></a> 获取 body</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">'body'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">document</span>.body;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#1.11">1.11</a> <a name="1.11"></a> 获取或设置属性</p>
<ul>
<li>获取属性</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.attr(<span class="string">'foo'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.getAttribute(<span class="string">'foo'</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li>设置属性</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery, note that this works in memory without change the DOM</span></span><br><span class="line">$el.attr(<span class="string">'foo'</span>, <span class="string">'bar'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.setAttribute(<span class="string">'foo'</span>, <span class="string">'bar'</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li>获取 <code>data-</code> 属性</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.data(<span class="string">'foo'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (use `getAttribute`)</span></span><br><span class="line">el.getAttribute(<span class="string">'data-foo'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (use `dataset` if only need to support IE 11+)</span></span><br><span class="line">el.dataset[<span class="string">'foo'</span>];</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="CSS-amp-Style"><a href="#CSS-amp-Style" class="headerlink" title="CSS &amp; Style"></a>CSS &amp; Style</h2><ul>
<li><p><a href="#2.1">2.1</a> <a name="2.1"></a> CSS</p>
<ul>
<li>Get style</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.css(<span class="string">"color"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="comment">// 注意：此处为了解决当 style 值为 auto 时，返回 auto 的问题</span></span><br><span class="line"><span class="keyword">const</span> win = el.ownerDocument.defaultView;</span><br><span class="line"></span><br><span class="line"><span class="comment">// null 的意思是不返回伪类元素</span></span><br><span class="line">win.getComputedStyle(el, <span class="literal">null</span>).color;</span><br></pre></td></tr></table></figure>

<ul>
<li>Set style</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.css(&#123; <span class="attr">color</span>: <span class="string">"#ff0011"</span> &#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.style.color = <span class="string">'#ff0011'</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>Get/Set Styles</p>
<p>注意，如果想一次设置多个 style，可以参考 oui-dom-utils 中 <a href="https://github.com/oneuijs/oui-dom-utils/blob/master/src/index.js#L194" target="_blank" rel="noopener">setStyles</a> 方法</p>
</li>
<li><p>Add class</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.addClass(className);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.classList.add(className);</span><br></pre></td></tr></table></figure>

<ul>
<li>Remove class</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.removeClass(className);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.classList.remove(className);</span><br></pre></td></tr></table></figure>

<ul>
<li>has class</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.hasClass(className);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.classList.contains(className);</span><br></pre></td></tr></table></figure>

<ul>
<li>Toggle class</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.toggleClass(className);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.classList.toggle(className);</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#2.2">2.2</a> <a name="2.2"></a> Width &amp; Height</p>
<p>Width 与 Height 获取方法相同，下面以 Height 为例：</p>
<ul>
<li>Window height</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// window height</span></span><br><span class="line">$(<span class="built_in">window</span>).height();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 含 scrollbar</span></span><br><span class="line"><span class="built_in">window</span>.document.documentElement.clientHeight;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 不含 scrollbar，与 jQuery 行为一致</span></span><br><span class="line"><span class="built_in">window</span>.innerHeight;</span><br></pre></td></tr></table></figure>

<ul>
<li>Document height</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="built_in">document</span>).height();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="keyword">const</span> body = <span class="built_in">document</span>.body;</span><br><span class="line"><span class="keyword">const</span> html = <span class="built_in">document</span>.documentElement;</span><br><span class="line"><span class="keyword">const</span> height = <span class="built_in">Math</span>.max(</span><br><span class="line">  body.offsetHeight,</span><br><span class="line">  body.scrollHeight,</span><br><span class="line">  html.clientHeight,</span><br><span class="line">  html.offsetHeight,</span><br><span class="line">  html.scrollHeight</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<ul>
<li>Element height</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.height();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getHeight</span>(<span class="params">el</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> styles = <span class="keyword">this</span>.getComputedStyle(el);</span><br><span class="line">  <span class="keyword">const</span> height = el.offsetHeight;</span><br><span class="line">  <span class="keyword">const</span> borderTopWidth = <span class="built_in">parseFloat</span>(styles.borderTopWidth);</span><br><span class="line">  <span class="keyword">const</span> borderBottomWidth = <span class="built_in">parseFloat</span>(styles.borderBottomWidth);</span><br><span class="line">  <span class="keyword">const</span> paddingTop = <span class="built_in">parseFloat</span>(styles.paddingTop);</span><br><span class="line">  <span class="keyword">const</span> paddingBottom = <span class="built_in">parseFloat</span>(styles.paddingBottom);</span><br><span class="line">  <span class="keyword">return</span> height - borderBottomWidth - borderTopWidth - paddingTop - paddingBottom;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 精确到整数（border-box 时为 height - border 值，content-box 时为 height + padding 值）</span></span><br><span class="line">el.clientHeight;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 精确到小数（border-box 时为 height 值，content-box 时为 height + padding + border 值）</span></span><br><span class="line">el.getBoundingClientRect().height;</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#2.3">2.3</a> <a name="2.3"></a> Position &amp; Offset</p>
<ul>
<li><p>Position</p>
<p>获得匹配元素相对父元素的偏移</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.position();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">&#123; <span class="attr">left</span>: el.offsetLeft, <span class="attr">top</span>: el.offsetTop &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>Offset</p>
<p>获得匹配元素相对文档的偏移</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.offset();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getOffset</span> (<span class="params">el</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> box = el.getBoundingClientRect();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    top: box.top + <span class="built_in">window</span>.pageYOffset - <span class="built_in">document</span>.documentElement.clientTop,</span><br><span class="line">    left: box.left + <span class="built_in">window</span>.pageXOffset - <span class="built_in">document</span>.documentElement.clientLeft</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#2.4">2.4</a> <a name="2.4"></a> Scroll Top</p>
</li>
</ul>
<p>  获取元素滚动条垂直位置。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="built_in">window</span>).scrollTop();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">(<span class="built_in">document</span>.documentElement &amp;&amp; <span class="built_in">document</span>.documentElement.scrollTop) || <span class="built_in">document</span>.body.scrollTop;</span><br></pre></td></tr></table></figure>

<h2 id="DOM-Manipulation"><a href="#DOM-Manipulation" class="headerlink" title="DOM Manipulation"></a>DOM Manipulation</h2><ul>
<li><p><a href="#3.1">3.1</a> <a name="3.1"></a> Remove</p>
<p>从 DOM 中移除元素。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.remove();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.parentNode.removeChild(el);</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#3.2">3.2</a> <a name="3.2"></a> Text</p>
<ul>
<li><p>Get text</p>
<p>返回指定元素及其后代的文本内容。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.text();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.textContent;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>Set text</p>
<p>设置元素的文本内容。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.text(string);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.textContent = string;</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#3.3">3.3</a> <a name="3.3"></a> HTML</p>
<ul>
<li>Get HTML</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.html();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.innerHTML;</span><br></pre></td></tr></table></figure>

<ul>
<li>Set HTML</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.html(htmlString);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.innerHTML = htmlString;</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#3.4">3.4</a> <a name="3.4"></a> Append</p>
<p>Append 插入到子节点的末尾</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.append(<span class="string">"&lt;div id='container'&gt;hello&lt;/div&gt;"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (HTML string)</span></span><br><span class="line">el.insertAdjacentHTML(<span class="string">'beforeend'</span>, <span class="string">'&lt;div id="container"&gt;Hello World&lt;/div&gt;'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (Element)</span></span><br><span class="line">el.appendChild(newEl);</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#3.5">3.5</a> <a name="3.5"></a> Prepend</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.prepend(<span class="string">"&lt;div id='container'&gt;hello&lt;/div&gt;"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (HTML string)</span></span><br><span class="line">el.insertAdjacentHTML(<span class="string">'afterbegin'</span>, <span class="string">'&lt;div id="container"&gt;Hello World&lt;/div&gt;'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (Element)</span></span><br><span class="line">el.insertBefore(newEl, el.firstChild);</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#3.6">3.6</a> <a name="3.6"></a> insertBefore</p>
<p>在选中元素前插入新节点</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$newEl.insertBefore(queryString);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (HTML string)</span></span><br><span class="line">el.insertAdjacentHTML(<span class="string">'beforebegin '</span>, <span class="string">'&lt;div id="container"&gt;Hello World&lt;/div&gt;'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (Element)</span></span><br><span class="line"><span class="keyword">const</span> el = <span class="built_in">document</span>.querySelector(selector);</span><br><span class="line"><span class="keyword">if</span> (el.parentNode) &#123;</span><br><span class="line">  el.parentNode.insertBefore(newEl, el);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#3.7">3.7</a> <a name="3.7"></a> insertAfter</p>
<p>在选中元素后插入新节点</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$newEl.insertAfter(queryString);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (HTML string)</span></span><br><span class="line">el.insertAdjacentHTML(<span class="string">'afterend'</span>, <span class="string">'&lt;div id="container"&gt;Hello World&lt;/div&gt;'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native (Element)</span></span><br><span class="line"><span class="keyword">const</span> el = <span class="built_in">document</span>.querySelector(selector);</span><br><span class="line"><span class="keyword">if</span> (el.parentNode) &#123;</span><br><span class="line">  el.parentNode.insertBefore(newEl, el.nextSibling);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#3.8">3.8</a> <a name="3.8"></a> is</p>
<p>如果匹配给定的选择器，返回true</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.is(selector);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.matches(selector);</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#3.9">3.9</a> <a name="3.9"></a> clone</p>
<p>深拷贝被选元素。（生成被选元素的副本，包含子节点、文本和属性。）</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//jQuery</span></span><br><span class="line">$el.clone();</span><br><span class="line"></span><br><span class="line"><span class="comment">//Native</span></span><br><span class="line">el.cloneNode();</span><br><span class="line"></span><br><span class="line">  <span class="comment">//深拷贝添加参数‘true’</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#3.10">3.10</a> <a name="3.10"></a> empty</p>
<p>移除所有子节点</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//jQuery</span></span><br><span class="line">$el.empty();</span><br><span class="line"></span><br><span class="line"><span class="comment">//Native</span></span><br><span class="line">el.innerHTML = <span class="string">''</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#3.11">3.11</a> <a name="3.11"></a> wrap</p>
<p>把每个被选元素放置在指定的HTML结构中。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//jQuery</span></span><br><span class="line">$(<span class="string">".inner"</span>).wrap(<span class="string">'&lt;div class="wrapper"&gt;&lt;/div&gt;'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//Native</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.forEach.call(<span class="built_in">document</span>.querySelector(<span class="string">'.inner'</span>), (el) =&gt; &#123;</span><br><span class="line">   <span class="keyword">const</span> wrapper = <span class="built_in">document</span>.createElement(<span class="string">'div'</span>);</span><br><span class="line">   wrapper.className = <span class="string">'wrapper'</span>;</span><br><span class="line">   el.parentNode.insertBefore(wrapper, el);</span><br><span class="line">   el.parentNode.removeChild(el);</span><br><span class="line">   wrapper.appendChild(el);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
</li>
<li><p><a href="#3.12">3.12</a> <a name="3.12"></a> unwrap</p>
<p>移除被选元素的父元素的DOM结构</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">'.inner'</span>).unwrap();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.forEach.call(<span class="built_in">document</span>.querySelectorAll(<span class="string">'.inner'</span>), (el) =&gt; &#123;</span><br><span class="line">      <span class="keyword">let</span> elParentNode = el.parentNode</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span>(elParentNode !== <span class="built_in">document</span>.body) &#123;</span><br><span class="line">          elParentNode.parentNode.insertBefore(el, elParentNode)</span><br><span class="line">          elParentNode.parentNode.removeChild(elParentNode)</span><br><span class="line">      &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#3.13">3.13</a> <a name="3.13"></a> replaceWith</p>
<p>用指定的元素替换被选的元素</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//jQuery</span></span><br><span class="line">$(<span class="string">'.inner'</span>).replaceWith(<span class="string">'&lt;div class="outer"&gt;&lt;/div&gt;'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//Native</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.forEach.call(<span class="built_in">document</span>.querySelectorAll(<span class="string">'.inner'</span>),(el) =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> outer = <span class="built_in">document</span>.createElement(<span class="string">"div"</span>);</span><br><span class="line">  outer.className = <span class="string">"outer"</span>;</span><br><span class="line">  el.parentNode.insertBefore(outer, el);</span><br><span class="line">  el.parentNode.removeChild(el);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#3.14">3.14</a> <a name="3.14"></a> simple parse</p>
<p>解析 HTML/SVG/XML 字符串</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="string">`&lt;ol&gt;</span></span><br><span class="line"><span class="string">  &lt;li&gt;a&lt;/li&gt;</span></span><br><span class="line"><span class="string">  &lt;li&gt;b&lt;/li&gt;</span></span><br><span class="line"><span class="string">&lt;/ol&gt;</span></span><br><span class="line"><span class="string">&lt;ol&gt;</span></span><br><span class="line"><span class="string">  &lt;li&gt;c&lt;/li&gt;</span></span><br><span class="line"><span class="string">  &lt;li&gt;d&lt;/li&gt;</span></span><br><span class="line"><span class="string">&lt;/ol&gt;`</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">range = <span class="built_in">document</span>.createRange();</span><br><span class="line">parse = range.createContextualFragment.bind(range);</span><br><span class="line"></span><br><span class="line">parse(<span class="string">`&lt;ol&gt;</span></span><br><span class="line"><span class="string">  &lt;li&gt;a&lt;/li&gt;</span></span><br><span class="line"><span class="string">  &lt;li&gt;b&lt;/li&gt;</span></span><br><span class="line"><span class="string">&lt;/ol&gt;</span></span><br><span class="line"><span class="string">&lt;ol&gt;</span></span><br><span class="line"><span class="string">  &lt;li&gt;c&lt;/li&gt;</span></span><br><span class="line"><span class="string">  &lt;li&gt;d&lt;/li&gt;</span></span><br><span class="line"><span class="string">&lt;/ol&gt;`</span>);</span><br></pre></td></tr></table></figure>

<h2 id="Ajax"><a href="#Ajax" class="headerlink" title="Ajax"></a>Ajax</h2><p><a href="https://fetch.spec.whatwg.org/" target="_blank" rel="noopener">Fetch API</a> 是用于替换 XMLHttpRequest 处理 ajax 的新标准，Chrome 和 Firefox 均支持，旧浏览器可以使用 polyfills 提供支持。</p>
<p>IE9+ 请使用 <a href="http://github.com/github/fetch" target="_blank" rel="noopener">github/fetch</a>，IE8+ 请使用 <a href="https://github.com/camsong/fetch-ie8/" target="_blank" rel="noopener">fetch-ie8</a>，JSONP 请使用 <a href="https://github.com/camsong/fetch-jsonp" target="_blank" rel="noopener">fetch-jsonp</a>。</p>
<ul>
<li><a href="#4.1">4.1</a> <a name="4.1"></a> 从服务器读取数据并替换匹配元素的内容。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(selector).load(url, completeCallback)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">fetch(url).then(<span class="function"><span class="params">data</span> =&gt;</span> data.text()).then(<span class="function"><span class="params">data</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">document</span>.querySelector(selector).innerHTML = data</span><br><span class="line">&#125;).then(completeCallback)</span><br></pre></td></tr></table></figure>

<h2 id="Events"><a href="#Events" class="headerlink" title="Events"></a>Events</h2><p>完整地替代命名空间和事件代理，链接到 <a href="https://github.com/oneuijs/oui-dom-events" target="_blank" rel="noopener">https://github.com/oneuijs/oui-dom-events</a></p>
<ul>
<li><a href="#5.0">5.0</a> <a name="5.0"></a> Document ready by <code>DOMContentLoaded</code></li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(<span class="built_in">document</span>).ready(eventHandler);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="comment">// 检测 DOMContentLoaded 是否已完成</span></span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">document</span>.readyState !== <span class="string">'loading'</span>) &#123;</span><br><span class="line">  eventHandler();</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="built_in">document</span>.addEventListener(<span class="string">'DOMContentLoaded'</span>, eventHandler);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#5.1">5.1</a> <a name="5.1"></a> 使用 on 绑定事件</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.on(eventName, eventHandler);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.addEventListener(eventName, eventHandler);</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#5.2">5.2</a> <a name="5.2"></a> 使用 off 解绑事件</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.off(eventName, eventHandler);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.removeEventListener(eventName, eventHandler);</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#5.3">5.3</a> <a name="5.3"></a> Trigger</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$(el).trigger(<span class="string">'custom-event'</span>, &#123;<span class="attr">key1</span>: <span class="string">'data'</span>&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">window</span>.CustomEvent) &#123;</span><br><span class="line">  <span class="keyword">const</span> event = <span class="keyword">new</span> CustomEvent(<span class="string">'custom-event'</span>, &#123;<span class="attr">detail</span>: &#123;<span class="attr">key1</span>: <span class="string">'data'</span>&#125;&#125;);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> event = <span class="built_in">document</span>.createEvent(<span class="string">'CustomEvent'</span>);</span><br><span class="line">  event.initCustomEvent(<span class="string">'custom-event'</span>, <span class="literal">true</span>, <span class="literal">true</span>, &#123;<span class="attr">key1</span>: <span class="string">'data'</span>&#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">el.dispatchEvent(event);</span><br></pre></td></tr></table></figure>

<h2 id="Utilities"><a href="#Utilities" class="headerlink" title="Utilities"></a>Utilities</h2><p>大部分实用工具都能在 native API 中找到. 其他高级功能可以选用专注于该领域的稳定性和性能都更好的库来代替，推荐 <a href="https://lodash.com" target="_blank" rel="noopener">lodash</a>。</p>
<ul>
<li><p><a href="#6.1">6.1</a> <a name="6.1"></a> 基本工具</p>
<ul>
<li>isArray</li>
</ul>
<p>检测参数是不是数组。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.isArray(range);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">Array</span>.isArray(range);</span><br></pre></td></tr></table></figure>

<ul>
<li><p>isWindow</p>
<p>检测参数是不是 window。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.isWindow(obj);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isWindow</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> obj !== <span class="literal">null</span> &amp;&amp; obj !== <span class="literal">undefined</span> &amp;&amp; obj === obj.window;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>inArray</p>
<p>在数组中搜索指定值并返回索引 (找不到则返回 -1)。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.inArray(item, array);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">array.indexOf(item) &gt; <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ES6-way</span></span><br><span class="line">array.includes(item);</span><br></pre></td></tr></table></figure>

<ul>
<li><p>isNumeric</p>
<p>检测传入的参数是不是数字。<br>Use <code>typeof</code> to decide the type or the <code>type</code> example for better accuracy.</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.isNumeric(item);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isNumeric</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> !<span class="built_in">isNaN</span>(<span class="built_in">parseFloat</span>(n)) &amp;&amp; <span class="built_in">isFinite</span>(n);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>isFunction</p>
<p>检测传入的参数是不是 JavaScript 函数对象。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.isFunction(item);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isFunction</span>(<span class="params">item</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> item === <span class="string">'function'</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">var</span> type = <span class="built_in">Object</span>.prototype.toString(item);</span><br><span class="line">  <span class="keyword">return</span> type === <span class="string">'[object Function]'</span> || type === <span class="string">'[object GeneratorFunction]'</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>isEmptyObject</p>
<p>检测对象是否为空 (包括不可枚举属性).</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.isEmptyObject(obj);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isEmptyObject</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Object</span>.keys(obj).length === <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>isPlainObject</p>
<p>检测是不是扁平对象 (使用 “{}” 或 “new Object” 创建).</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.isPlainObject(obj);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isPlainObject</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> (obj) !== <span class="string">'object'</span> || obj.nodeType || obj !== <span class="literal">null</span> &amp;&amp; obj !== <span class="literal">undefined</span> &amp;&amp; obj === obj.window) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (obj.constructor &amp;&amp;</span><br><span class="line">      !<span class="built_in">Object</span>.prototype.hasOwnProperty.call(obj.constructor.prototype, <span class="string">'isPrototypeOf'</span>)) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>extend</p>
<p>合并多个对象的内容到第一个对象。<br>object.assign 是 ES6 API，也可以使用 <a href="https://github.com/ljharb/object.assign" target="_blank" rel="noopener">polyfill</a>。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.extend(&#123;&#125;, defaultOpts, opts);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">Object</span>.assign(&#123;&#125;, defaultOpts, opts);</span><br></pre></td></tr></table></figure>

<ul>
<li><p>trim</p>
<p>移除字符串头尾空白。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.trim(string);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">string.trim();</span><br></pre></td></tr></table></figure>

<ul>
<li><p>map</p>
<p>将数组或对象转化为包含新内容的数组。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.map(array, (value, index) =&gt; &#123;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">array.map(<span class="function">(<span class="params">value, index</span>) =&gt;</span> &#123;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ul>
<li><p>each</p>
<p>轮询函数，可用于平滑的轮询对象和数组。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.each(array, (index, value) =&gt; &#123;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">array.forEach(<span class="function">(<span class="params">value, index</span>) =&gt;</span> &#123;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ul>
<li><p>grep</p>
<p>找到数组中符合过滤函数的元素。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.grep(array, (value, index) =&gt; &#123;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">array.filter(<span class="function">(<span class="params">value, index</span>) =&gt;</span> &#123;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ul>
<li><p>type</p>
<p>检测对象的 JavaScript [Class] 内部类型。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.type(obj);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">type</span>(<span class="params">item</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> reTypeOf = <span class="regexp">/(?:^\[object\s(.*?)\]$)/</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Object</span>.prototype.toString.call(item)</span><br><span class="line">    .replace(reTypeOf, <span class="string">'$1'</span>)</span><br><span class="line">    .toLowerCase();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>merge</p>
<p>合并第二个数组内容到第一个数组。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.merge(array1, array2);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="comment">// 使用 concat，不能去除重复值</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">merge</span>(<span class="params">...args</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> [].concat(...args)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ES6，同样不能去除重复值</span></span><br><span class="line">array1 = [...array1, ...array2]</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 Set，可以去除重复值</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">merge</span>(<span class="params">...args</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Array</span>.from(<span class="keyword">new</span> <span class="built_in">Set</span>([].concat(...args)))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>now</p>
<p>返回当前时间的数字呈现。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.now();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">Date</span>.now();</span><br></pre></td></tr></table></figure>

<ul>
<li><p>proxy</p>
<p>传入函数并返回一个新函数，该函数绑定指定上下文。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.proxy(fn, context);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">fn.bind(context);</span><br></pre></td></tr></table></figure>

<ul>
<li><p>makeArray</p>
<p>类数组对象转化为真正的 JavaScript 数组。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.makeArray(arrayLike);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.slice.call(arrayLike);</span><br><span class="line"></span><br><span class="line"><span class="comment">// ES6-way</span></span><br><span class="line"><span class="built_in">Array</span>.from(arrayLike);</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#6.2">6.2</a> <a name="6.2"></a> 包含</p>
<p>检测 DOM 元素是不是其他 DOM 元素的后代.</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.contains(el, child);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el !== child &amp;&amp; el.contains(child);</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#6.3">6.3</a> <a name="6.3"></a> Globaleval</p>
<p>全局执行 JavaScript 代码。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.globaleval(code);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Globaleval</span>(<span class="params">code</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> script = <span class="built_in">document</span>.createElement(<span class="string">'script'</span>);</span><br><span class="line">  script.text = code;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">document</span>.head.appendChild(script).parentNode.removeChild(script);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Use eval, but context of eval is current, context of $.Globaleval is global.</span></span><br><span class="line"><span class="built_in">eval</span>(code);</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#6.4">6.4</a> <a name="6.4"></a> 解析</p>
<ul>
<li>parseHTML</li>
</ul>
<p>解析字符串为 DOM 节点数组.</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.parseHTML(htmlString);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">parseHTML</span>(<span class="params">string</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> context = <span class="built_in">document</span>.implementation.createHTMLDocument();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Set the base href for the created document so any parsed elements with URLs</span></span><br><span class="line">  <span class="comment">// are based on the document's URL</span></span><br><span class="line">  <span class="keyword">const</span> base = context.createElement(<span class="string">'base'</span>);</span><br><span class="line">  base.href = <span class="built_in">document</span>.location.href;</span><br><span class="line">  context.head.appendChild(base);</span><br><span class="line"></span><br><span class="line">  context.body.innerHTML = string;</span><br><span class="line">  <span class="keyword">return</span> context.body.children;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>parseJSON</p>
<p>传入格式正确的 JSON 字符串并返回 JavaScript 值.</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.parseJSON(str);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">JSON</span>.parse(str);</span><br></pre></td></tr></table></figure>

<h2 id="Promises"><a href="#Promises" class="headerlink" title="Promises"></a>Promises</h2><p>Promise 代表异步操作的最终结果。jQuery 用它自己的方式处理 promises，原生 JavaScript 遵循 <a href="http://promises-aplus.github.io/promises-spec/" target="_blank" rel="noopener">Promises/A+</a> 标准实现了最小 API 来处理 promises。</p>
<ul>
<li><p><a href="#7.1">7.1</a> <a name="7.1"></a> done, fail, always</p>
<p><code>done</code> 会在 promise 解决时调用，<code>fail</code> 会在 promise 拒绝时调用，<code>always</code> 总会调用。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$promise.done(doneCallback).fail(failCallback).always(alwaysCallback)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">promise.then(doneCallback, failCallback).then(alwaysCallback, alwaysCallback)</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#7.2">7.2</a> <a name="7.2"></a> when</p>
<p><code>when</code> 用于处理多个 promises。当全部 promises 被解决时返回，当任一 promise 被拒绝时拒绝。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$.when($promise1, $promise2).done(<span class="function">(<span class="params">promise1Result, promise2Result</span>) =&gt;</span> &#123;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="built_in">Promise</span>.all([$promise1, $promise2]).then([promise1Result, promise2Result] =&gt; &#123;&#125;);</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#7.3">7.3</a> <a name="7.3"></a> Deferred</p>
<p>Deferred 是创建 promises 的一种方式。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">asyncFunc</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> defer = <span class="keyword">new</span> $.Deferred();</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="literal">true</span>) &#123;</span><br><span class="line">      defer.resolve(<span class="string">'some_value_computed_asynchronously'</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      defer.reject(<span class="string">'failed'</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, <span class="number">1000</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> defer.promise();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">asyncFunc</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">        resolve(<span class="string">'some_value_computed_asynchronously'</span>);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        reject(<span class="string">'failed'</span>);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;, <span class="number">1000</span>);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Deferred way</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">defer</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> deferred = &#123;&#125;;</span><br><span class="line">  <span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    deferred.resolve = resolve;</span><br><span class="line">    deferred.reject = reject;</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line">  deferred.promise = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> promise;</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> deferred;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">asyncFunc</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> defer = defer();</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="literal">true</span>) &#123;</span><br><span class="line">      defer.resolve(<span class="string">'some_value_computed_asynchronously'</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      defer.reject(<span class="string">'failed'</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, <span class="number">1000</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> defer.promise();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Animation"><a href="#Animation" class="headerlink" title="Animation"></a>Animation</h2><ul>
<li><a href="#8.1">8.1</a> <a name="8.1"></a> Show &amp; Hide</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.show();</span><br><span class="line">$el.hide();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="comment">// 更多 show 方法的细节详见 https://github.com/oneuijs/oui-dom-utils/blob/master/src/index.js#L363</span></span><br><span class="line">el.style.display = <span class="string">''</span>|<span class="string">'inline'</span>|<span class="string">'inline-block'</span>|<span class="string">'inline-table'</span>|<span class="string">'block'</span>;</span><br><span class="line">el.style.display = <span class="string">'none'</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#8.2">8.2</a> <a name="8.2"></a> Toggle</p>
<p>显示或隐藏元素。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.toggle();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="keyword">if</span> (el.ownerDocument.defaultView.getComputedStyle(el, <span class="literal">null</span>).display === <span class="string">'none'</span>) &#123;</span><br><span class="line">  el.style.display = <span class="string">''</span>|<span class="string">'inline'</span>|<span class="string">'inline-block'</span>|<span class="string">'inline-table'</span>|<span class="string">'block'</span>;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  el.style.display = <span class="string">'none'</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#8.3">8.3</a> <a name="8.3"></a> FadeIn &amp; FadeOut</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.fadeIn(<span class="number">3000</span>);</span><br><span class="line">$el.fadeOut(<span class="number">3000</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.style.transition = <span class="string">'opacity 3s'</span>;</span><br><span class="line"><span class="comment">// fadeIn</span></span><br><span class="line">el.style.opacity = <span class="string">'1'</span>;</span><br><span class="line"><span class="comment">// fadeOut</span></span><br><span class="line">el.style.opacity = <span class="string">'0'</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#8.4">8.4</a> <a name="8.4"></a> FadeTo</p>
<p>调整元素透明度。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.fadeTo(<span class="string">'slow'</span>,<span class="number">0.15</span>);</span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.style.transition = <span class="string">'opacity 3s'</span>; <span class="comment">// 假设 'slow' 等于 3 秒</span></span><br><span class="line">el.style.opacity = <span class="string">'0.15'</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#8.5">8.5</a> <a name="8.5"></a> FadeToggle</p>
<p>动画调整透明度用来显示或隐藏。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.fadeToggle();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.style.transition = <span class="string">'opacity 3s'</span>;</span><br><span class="line"><span class="keyword">const</span> &#123; opacity &#125; = el.ownerDocument.defaultView.getComputedStyle(el, <span class="literal">null</span>);</span><br><span class="line"><span class="keyword">if</span> (opacity === <span class="string">'1'</span>) &#123;</span><br><span class="line">  el.style.opacity = <span class="string">'0'</span>;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  el.style.opacity = <span class="string">'1'</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><a href="#8.6">8.6</a> <a name="8.6"></a> SlideUp &amp; SlideDown</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.slideUp();</span><br><span class="line">$el.slideDown();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="keyword">const</span> originHeight = <span class="string">'100px'</span>;</span><br><span class="line">el.style.transition = <span class="string">'height 3s'</span>;</span><br><span class="line"><span class="comment">// slideUp</span></span><br><span class="line">el.style.height = <span class="string">'0px'</span>;</span><br><span class="line"><span class="comment">// slideDown</span></span><br><span class="line">el.style.height = originHeight;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#8.7">8.7</a> <a name="8.7"></a> SlideToggle</p>
<p>滑动切换显示或隐藏。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.slideToggle();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line"><span class="keyword">const</span> originHeight = <span class="string">'100px'</span>;</span><br><span class="line">el.style.transition = <span class="string">'height 3s'</span>;</span><br><span class="line"><span class="keyword">const</span> &#123; height &#125; = el.ownerDocument.defaultView.getComputedStyle(el, <span class="literal">null</span>);</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">parseInt</span>(height, <span class="number">10</span>) === <span class="number">0</span>) &#123;</span><br><span class="line">  el.style.height = originHeight;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line"> el.style.height = <span class="string">'0px'</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p><a href="#8.8">8.8</a> <a name="8.8"></a> Animate</p>
<p>执行一系列 CSS 属性动画。</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// jQuery</span></span><br><span class="line">$el.animate(&#123; params &#125;, speed);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Native</span></span><br><span class="line">el.style.transition = <span class="string">'all '</span> + speed;</span><br><span class="line"><span class="built_in">Object</span>.keys(params).forEach(<span class="function">(<span class="params">key</span>) =&gt;</span></span><br><span class="line">  el.style[key] = params[key]</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<h2 id="Alternatives"><a href="#Alternatives" class="headerlink" title="Alternatives"></a>Alternatives</h2><ul>
<li><a href="http://youmightnotneedjquery.com/" target="_blank" rel="noopener">你可能不需要 jQuery (You Might Not Need jQuery)</a> - 如何使用原生 JavaScript 实现通用事件，元素，ajax 等用法。</li>
<li><a href="http://github.com/npm-dom" target="_blank" rel="noopener">npm-dom</a> 以及 <a href="http://github.com/webmodules" target="_blank" rel="noopener">webmodules</a> - 在 NPM 上提供独立 DOM 模块的组织</li>
</ul>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>ES6 的小技巧</title>
    <url>/2018/05/06/daily-js-es6-skill/</url>
    <content><![CDATA[<h2 id="调试"><a href="#调试" class="headerlink" title="调试"></a>调试</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> a=<span class="number">6</span>,b=<span class="number">9</span>,c=<span class="number">0</span>;</span><br><span class="line"><span class="built_in">console</span>.log(&#123;a,b,c&#125;);</span><br><span class="line"><span class="comment">//输出 &#123;a:5,b:9,c:0&#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="结构Async-Await"><a href="#结构Async-Await" class="headerlink" title="结构Async/Await"></a>结构Async/Await</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> [grade,fee] = <span class="keyword">await</span> <span class="built_in">Promise</span>.all([</span><br><span class="line">    fetch(<span class="string">'/grade'</span>),</span><br><span class="line">    fetch(<span class="string">'/fee'</span>)</span><br><span class="line">])</span><br></pre></td></tr></table></figure>

<h2 id="合并数据"><a href="#合并数据" class="headerlink" title="合并数据"></a>合并数据</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> arr_1 = [<span class="string">'a'</span>,<span class="string">'b'</span>,<span class="string">'c'</span>]</span><br><span class="line"><span class="keyword">const</span> arr_2 = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="keyword">const</span> result = [...arr_1,...arr_2]</span><br></pre></td></tr></table></figure>

<h2 id="深拷贝对象和数组"><a href="#深拷贝对象和数组" class="headerlink" title="深拷贝对象和数组"></a>深拷贝对象和数组</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> object_1 = &#123;</span><br><span class="line">    a:<span class="number">5</span>,</span><br><span class="line">    b:<span class="number">6</span>,</span><br><span class="line">    c:<span class="number">7</span></span><br><span class="line">&#125;,</span><br><span class="line">arr_1 = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> obj = &#123;...object_1&#125;</span><br><span class="line"><span class="keyword">const</span> arr = [...arr_1]</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(obj===object_1)  <span class="comment">//false</span></span><br><span class="line"><span class="built_in">console</span>.log(arr===arr_1)  <span class="comment">//false</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>ES6</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 跳转页面</title>
    <url>/2018/04/16/daily-js-location/</url>
    <content><![CDATA[<p>JavaScript 如何跳转页面？也许你不知道有这么多解法</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 跳转</span></span><br><span class="line"><span class="built_in">window</span>.location.replace(<span class="string">'https://www.awesomes.cn'</span>) <span class="comment">// 不将被跳转页面加入浏览器记录</span></span><br><span class="line"><span class="built_in">window</span>.location.assign(<span class="string">'https://www.awesomes.cn'</span>)</span><br><span class="line"><span class="built_in">window</span>.location.href = <span class="string">'https://www.awesomes.cn'</span></span><br><span class="line"><span class="built_in">window</span>.location = <span class="string">'https://www.awesomes.cn'</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回上一页</span></span><br><span class="line"><span class="built_in">window</span>.history.back()</span><br><span class="line"><span class="built_in">window</span>.history.go(<span class="number">-1</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 刷新当前页</span></span><br><span class="line"><span class="built_in">window</span>.location.reload()</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>chrome 调试小技巧</title>
    <url>/2018/04/01/article-js-mvvm-vue/</url>
    <content><![CDATA[<blockquote>
<p>Chrome F12 中有一个很多同学都不知道的功能，审查元素并切换到 Styles 面板中，按住 Ctrl 键，移到某个样式属性的值（出现下划线），然后点击即可导航到定义该样式属性的 CSS 文件中。</p>
</blockquote>
<blockquote>
<p>Chrome 开发者工具已支持在选中的元素上按下 h 键快速隐藏该元素（如果你不想按 Del 键直接删除该元素的话）</p>
</blockquote>
<blockquote>
<p>Chrome 开发者工具的 Network 信息项头部右键即可选择要显示哪些请求信息</p>
</blockquote>
]]></content>
      <categories>
        <category>前沿技术</category>
      </categories>
      <tags>
        <tag>Debug</tag>
      </tags>
  </entry>
  <entry>
    <title>ES6新特性 —— Class</title>
    <url>/2018/03/14/article-ES6-class-know/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>类语法(<code>class</code>)是<code>ES6</code>中新增的一个亮点特性，下文简单对<code>class</code>的理解做一个简要的记录说明。</p>
<a id="more"></a>

<h2 id="js传统模式实例化对象方法——prototype"><a href="#js传统模式实例化对象方法——prototype" class="headerlink" title="js传统模式实例化对象方法——prototype"></a>js传统模式实例化对象方法——<code>prototype</code></h2><p>在JavaScript中，实例化一个对象的传统使用方法是通过构造函数。<br>如下示例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Count</span>(<span class="params">num1,num2</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.num1 = num1;</span><br><span class="line">    <span class="keyword">this</span>.num2 = num2;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//构造函数</span></span><br><span class="line">Count.prototype.number = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.num1,<span class="keyword">this</span>.num2);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//实例化对象</span></span><br><span class="line"><span class="keyword">var</span> count = <span class="keyword">new</span> Count(<span class="number">1</span>,<span class="number">2</span>);</span><br><span class="line">count.number(<span class="number">1</span>,<span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<h2 id="ES6新特性——使用Class（类）实例化对象"><a href="#ES6新特性——使用Class（类）实例化对象" class="headerlink" title="ES6新特性——使用Class（类）实例化对象"></a>ES6新特性——使用Class（类）实例化对象</h2><p>ES6引入了<code>Class</code>(类)这一概念，可以通过class关键字，定义类。</p>
<p>如下示例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//定义类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Count</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//构造方法</span></span><br><span class="line">    <span class="keyword">constructor</span>(x,y)&#123;</span><br><span class="line">        <span class="comment">//this表示实例对象</span></span><br><span class="line">        <span class="keyword">this</span>.x = x;</span><br><span class="line">        <span class="keyword">this</span>.y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//非静态方法，可以直接用实例.方法名来访问</span></span><br><span class="line">    sum(x,y)&#123;</span><br><span class="line">        <span class="keyword">let</span> sum = x+y;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'sum is :'</span>,sum)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//静态方法，需要通过构造器或者类才能访问的到</span></span><br><span class="line">    <span class="keyword">static</span> sub(x,y)&#123;</span><br><span class="line">        <span class="keyword">let</span> sub = x - y;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'sub is :'</span>,sub);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> count = <span class="keyword">new</span> Count();</span><br><span class="line"></span><br><span class="line"><span class="comment">//非静态方法，可以直接访问</span></span><br><span class="line">count.sum(<span class="number">1</span>,<span class="number">2</span>);                         <span class="comment">// sum is :3</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//静态方法：</span></span><br><span class="line"><span class="comment">//（1）实例对象.constructor.方法名</span></span><br><span class="line">count.constructor.sub(<span class="number">5</span>,<span class="number">1</span>);             <span class="comment">// sub is :4</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//（2）类名.方法名</span></span><br><span class="line">Count.sub(<span class="number">5</span>,<span class="number">1</span>);                         <span class="comment">// sub is :4</span></span><br><span class="line"></span><br><span class="line">count.hasOwnProperty(<span class="string">'x'</span>);              <span class="comment">// 定义在this上，是实例对象自身的属性true</span></span><br><span class="line">count.hasOwnProperty(<span class="string">'y'</span>);              <span class="comment">// true</span></span><br><span class="line">count.hasOwnProperty(<span class="string">'sum'</span>);            <span class="comment">// 定义在原型对象上，false</span></span><br><span class="line">count._proto_.hasOwnProperty(<span class="string">'sum'</span>);    <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>通过以上的小实例，总结下面几点:</p>
<ul>
<li>定义“类”的方法时，不用加function关键字。</li>
<li>方法之间不要加逗号，加了会报错。</li>
<li>类中必须要有constructor方法，若没有显示定义，则一个空的会被添加。</li>
<li>类必须使用new调用。否则报错，这是类和构造函数的一个主要区别。</li>
<li>类中使用static关键字声明的方法，需要通过构造函数constructor或者使用类名才可以访问的到。</li>
<li>静态方法，实例对象不能直接访问。但是父类的静态方法，子类可以继承。</li>
<li>非静态方法，可以直接通过“实例对象.方法名”访问。</li>
<li>实例自身的属性，除非显示定义在this对象上，否则都是定义在原型上（即class上）。</li>
</ul>
<blockquote>
<p>其实，ES6的类，可以看作是构造函数的另外一种写法。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span></span>&#123;&#125;</span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">new</span> B();</span><br><span class="line"><span class="keyword">typeof</span> B;               <span class="comment">//`function`</span></span><br><span class="line"><span class="keyword">typeof</span>  B === B.prototype.constructor                   <span class="comment">//true</span></span><br><span class="line"><span class="keyword">typeof</span>  b.constructor === B.prototype.constructor      <span class="comment">//true</span></span><br></pre></td></tr></table></figure>

<h3 id="对比ES6和ES5，有以下相同点"><a href="#对比ES6和ES5，有以下相同点" class="headerlink" title="对比ES6和ES5，有以下相同点"></a>对比ES6和ES5，有以下相同点</h3><ul>
<li>类的数据类型就是函数，类本身就指向构造函数。</li>
<li>在类的实例上调用方法，就是调用原型上的方法。</li>
</ul>
<h3 id="再来看ES6与ES5的不同点"><a href="#再来看ES6与ES5的不同点" class="headerlink" title="再来看ES6与ES5的不同点"></a>再来看ES6与ES5的不同点</h3><blockquote>
<p>由于类的方法都定义在prototype上面，那么我们可以使用Object.assign方法给类一次添加多个方法。prototype的constructor方法，直接指向“类”本身，这和ES5的行为是有区别的。另外，类内部所有定义的方法，都是不可枚举的，而ES5中，prototype上的方法都是可枚举的。</p>
</blockquote>
<p>示例如下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//ES5</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Test</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line">Test.prototype.log = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line"><span class="keyword">var</span> test = <span class="keyword">new</span> Test();</span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.keys(Test.prototype);    <span class="comment">//列举出给定对象自身可枚举属性(不包括原型上的属性)   </span></span><br><span class="line"><span class="comment">//['log']</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyNames(Test.prototype);      <span class="comment">//列举出给定对象所有自身属性的名称（包括不可枚举属性但不包括Symbol值作为名称的属性）  </span></span><br><span class="line"><span class="comment">//['prototype','log']</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//ES6</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test2</span></span>&#123;</span><br><span class="line">    toValue()&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> test2 = <span class="keyword">new</span> Test2();</span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.keys(Test2.prototype);</span><br><span class="line"><span class="comment">//[]</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyNames(Test2.prototype);</span><br><span class="line"><span class="comment">//['constructor','toValue'];</span></span><br></pre></td></tr></table></figure>

<h2 id="Class的静态方法"><a href="#Class的静态方法" class="headerlink" title="Class的静态方法"></a>Class的静态方法</h2><p> 类中所定义的方法，都会被实例继承，如果在一个方法前，加上static关键字，那该静态方法不会被实例继承，而是直接通过类来调用的。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> addMethod()&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>);</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'hello class'</span>);</span><br><span class="line">        <span class="comment">//this.</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//直接通过类来调用</span></span><br><span class="line">Foo.addMethod();        <span class="comment">//Foo ,  hello class</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//实例不会继承静态方法，所以调用会报错</span></span><br><span class="line"><span class="keyword">var</span> foo = <span class="keyword">new</span> Foo();</span><br><span class="line">foo.addMethod();    <span class="comment">//TypeError:foo.addMethod is not a function</span></span><br></pre></td></tr></table></figure>

<p><strong>注意：</strong></p>
<ul>
<li>如果static静态方法包含this关键字，这个this指的是类，而不是实例。</li>
<li>静态方法可以与非静态方法同名。</li>
<li>父类的静态方法，可以被子类继承。</li>
<li>静态方法可以从super对象上调用。</li>
</ul>
]]></content>
      <categories>
        <category>前沿技术</category>
      </categories>
      <tags>
        <tag>ES6</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>移动端滚动穿透问题</title>
    <url>/2018/03/06/daily-mobile-scroll/</url>
    <content><![CDATA[<p>滚动穿透是指在移动端当有 <code>fixed</code> 遮罩背景和弹出层时，在屏幕上滑动能够滑动背景下面的内容。网上整理了几种解决方案，但有些还是存在一定的问题。<a id="more"></a></p>
<h3 id="设置overflow为hidden"><a href="#设置overflow为hidden" class="headerlink" title="设置overflow为hidden"></a>设置overflow为hidden</h3><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.modal-open</span> &#123;</span><br><span class="line">    <span class="selector-tag">&amp;</span>, <span class="selector-tag">body</span> &#123;</span><br><span class="line">        <span class="attribute">overflow</span>: hidden;</span><br><span class="line">        <span class="attribute">height</span>: <span class="number">100%</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>即当弹出层弹出时在html上添加.modal-open,禁用 html 和 body 的滚动条,但实际用上就会发现：</p>
<ol>
<li><p>由于 html 和 body的滚动条都被禁用，弹出层后页面的滚动位置会丢失，需要用 js 来计算原来滚动的位置，在弹出时保持滚动位置；</p>
</li>
<li><p>但是页面的背景还是能够有滚的动的效果。</p>
</li>
</ol>
<h3 id="js-之-touchmove-preventDefault"><a href="#js-之-touchmove-preventDefault" class="headerlink" title="js 之 touchmove + preventDefault"></a>js 之 touchmove + preventDefault</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">modal.addEventListener(<span class="string">'touchmove'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">  e.preventDefault();</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<p>即通过阻止移动端touchmove事件，但实际用上会发现弹出层需要滚动时也会被阻止。</p>
<h3 id="最后解决方案：position-fixed"><a href="#最后解决方案：position-fixed" class="headerlink" title="最后解决方案：position: fixed"></a>最后解决方案：position: fixed</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span><span class="selector-class">.modal-open</span> &#123;</span><br><span class="line">    <span class="attribute">position</span>: fixed;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">100%</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种方式同样当弹出层弹出时滚动条会丢失，所以还需要使用js来保存滚动条的位置，在关闭弹出层时将滚动位置还原；</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> ModalHelper = (<span class="function"><span class="keyword">function</span>(<span class="params">bodyCls</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> scrollTop; <span class="comment">// 在闭包中定义一个用来保存滚动位置的变量</span></span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    afterOpen: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">//弹出之后记录保存滚动位置，并且给body添加.modal-open</span></span><br><span class="line">      scrollTop = <span class="built_in">document</span>.scrollingElement.scrollTop;</span><br><span class="line">      <span class="built_in">document</span>.body.classList.add(bodyCls);</span><br><span class="line">      <span class="built_in">document</span>.body.style.top = -scrollTop + <span class="string">'px'</span>;</span><br><span class="line">    &#125;,</span><br><span class="line">    beforeClose: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">//关闭时将.modal-open移除并还原之前保存滚动位置</span></span><br><span class="line">      <span class="built_in">document</span>.body.classList.remove(bodyCls);</span><br><span class="line">      <span class="built_in">document</span>.scrollingElement.scrollTop = scrollTop;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;)(<span class="string">'modal-open'</span>);</span><br></pre></td></tr></table></figure>

<p>这个方案比较完美的解决了移动端滚动穿透问题！</p>
]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>chorme浏览器记住密码后input黄色背景处理方法总结（三种）</title>
    <url>/2018/02/28/daily-css-input/</url>
    <content><![CDATA[<h3 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h3><p>正常情况：</p>
<p><img src="/styles/images/css/input/e9a0e22d4a1ea5ffe4b84ba92f7f6d9a.png" alt="正常情况"></p>
<p>记住密码后访问：</p>
<p><img src="/styles/images/css/input/7090b6f0388da0cb693a7cf32a0e59dd.png" alt="记住密码后访问"></p>
<h3 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h3><h4 id="方法1：阴影覆盖"><a href="#方法1：阴影覆盖" class="headerlink" title="方法1：阴影覆盖"></a>方法1：阴影覆盖</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">input</span><span class="selector-pseudo">:-webkit-autofill</span> &#123;</span><br><span class="line">    <span class="attribute">-webkit-box-shadow</span>: <span class="number">0</span> <span class="number">0</span> <span class="number">0</span> <span class="number">1000px</span> white inset <span class="meta">!important</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：由于是设置颜色覆盖，所以只对非透明的纯色背景有效；</p>
</blockquote>
<h4 id="方法2：修改chrome浏览器渲染黄色背景的时间（推荐）"><a href="#方法2：修改chrome浏览器渲染黄色背景的时间（推荐）" class="headerlink" title="方法2：修改chrome浏览器渲染黄色背景的时间（推荐）"></a>方法2：修改chrome浏览器渲染黄色背景的时间（推荐）</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-pseudo">:-webkit-autofill</span> &#123;</span><br><span class="line">    <span class="attribute">-webkit-text-fill-color</span>: <span class="number">#fff</span> <span class="meta">!important</span>;</span><br><span class="line">    <span class="attribute">transition</span>: background-color <span class="number">5000s</span> ease-in-out <span class="number">0s</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：此方法适用于上图那种背景为透明色的输入框</p>
</blockquote>
<h4 id="方法3：jq方式去除"><a href="#方法3：jq方式去除" class="headerlink" title="方法3：jq方式去除"></a>方法3：jq方式去除</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (navigator.userAgent.toLowerCase().indexOf(<span class="string">"chrome"</span>) &gt;= <span class="number">0</span>)&#123;</span><br><span class="line">  <span class="keyword">let</span> _interval = <span class="built_in">window</span>.setInterval(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> autofills = $(<span class="string">'input:-webkit-autofill'</span>);</span><br><span class="line">    <span class="keyword">if</span> (autofills.length &gt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="built_in">window</span>.clearInterval(_interval); <span class="comment">// 停止轮询</span></span><br><span class="line">      autofills.each(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">let</span> clone = $(<span class="keyword">this</span>).clone(<span class="literal">true</span>, <span class="literal">true</span>);</span><br><span class="line">        $(<span class="keyword">this</span>).after(clone).remove();</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, <span class="number">20</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 箭头函数语法小结</title>
    <url>/2018/02/23/daily-js-arrow-func/</url>
    <content><![CDATA[<p>箭头函数的确与传统函数有不同之处，但仍存在共同的特点。</p>
<p>例如：</p>
<ol>
<li>对箭头函数进行typeof操作会返回“function”。</li>
<li>箭头函数仍是Function的实例，故而instanceof的执行方式与传统函数一致。</li>
<li>call/apply/bind方法仍适用于箭头函数，但就算调用这些方法扩充当前作用域，this也依旧不会变化。</li>
<li>箭头函数与传统函数最大的不同之处在，禁用new操作<a id="more"></a>

</li>
</ol>
<h3 id="没有参数时"><a href="#没有参数时" class="headerlink" title="没有参数时"></a>没有参数时</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> demo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>相当于：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> demo = <span class="function"><span class="params">()</span> =&gt;</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="只有一个参数时"><a href="#只有一个参数时" class="headerlink" title="只有一个参数时"></a>只有一个参数时</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> demo = <span class="function"><span class="keyword">function</span>(<span class="params">a</span>)</span>&#123;   </span><br><span class="line">    <span class="keyword">return</span> a;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>相当于：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> demo = <span class="function"><span class="params">a</span> =&gt;</span> a</span><br></pre></td></tr></table></figure>

<h3 id="多个参数需要用到小括号，参数间逗号间隔"><a href="#多个参数需要用到小括号，参数间逗号间隔" class="headerlink" title="多个参数需要用到小括号，参数间逗号间隔"></a>多个参数需要用到小括号，参数间逗号间隔</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> demo = <span class="function"><span class="keyword">function</span>(<span class="params">a,b</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a+b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>相当于：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> demo = <span class="function">(<span class="params">a,b</span>) =&gt;</span> a+b</span><br></pre></td></tr></table></figure>

<h3 id="函数体多条语句需要用到大括号"><a href="#函数体多条语句需要用到大括号" class="headerlink" title="函数体多条语句需要用到大括号"></a>函数体多条语句需要用到大括号</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> demo = <span class="function"><span class="keyword">function</span>(<span class="params">a,b</span>)</span>&#123;</span><br><span class="line"><span class="keyword">if</span>(a&gt;b)&#123;</span><br><span class="line">    <span class="keyword">return</span> a-b;</span><br><span class="line">&#125; <span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> b-a;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>相当于：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> demo = <span class="function">(<span class="params">a,b</span>) =&gt;</span>&#123;</span><br><span class="line"><span class="keyword">if</span>(a&gt;b)&#123;</span><br><span class="line">    <span class="keyword">return</span> a-b;</span><br><span class="line">&#125; <span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> b-a;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="返回对象时需要用小括号包起来，因为大括号被占用解释为代码块了"><a href="#返回对象时需要用小括号包起来，因为大括号被占用解释为代码块了" class="headerlink" title="返回对象时需要用小括号包起来，因为大括号被占用解释为代码块了"></a>返回对象时需要用小括号包起来，因为大括号被占用解释为代码块了</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> demo = <span class="function">(<span class="params">name,age</span>) =&gt;</span>&#123;</span><br><span class="line"><span class="keyword">return</span> (&#123;</span><br><span class="line">    name: name,</span><br><span class="line">    age: age</span><br><span class="line">   &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="作为数组排序回调"><a href="#作为数组排序回调" class="headerlink" title="作为数组排序回调"></a>作为数组排序回调</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">9</span> , <span class="number">2</span>, <span class="number">4</span>, <span class="number">3</span>, <span class="number">8</span>].sort(<span class="function">(<span class="params">a, b</span>) =&gt;</span> &#123;</span><br><span class="line"> <span class="keyword">if</span> (a - b &gt; <span class="number">0</span> ) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line"> &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line"> &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>ES6</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 修改远程仓库地址</title>
    <url>/2018/02/22/daily-git-remote-change/</url>
    <content><![CDATA[<blockquote>
<p>查看仓库地址 ： <code>git remote -v</code></p>
</blockquote>
<p>方法有三种：</p>
<p>1、 修改命令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git remote set-url origin [url]</span><br></pre></td></tr></table></figure>

<p>2、 先删后加</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git remote rm origin</span><br><span class="line">git remote add origin [url]</span><br></pre></td></tr></table></figure>

<p>3、 直接修改config文件</p>
]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 的 in 和 for..in</title>
    <url>/2018/02/06/daily-js-for-in/</url>
    <content><![CDATA[<p><code>for...in...</code>  遍历一个对象的属性，把属性名和属性值都提出来<br><code>in</code>  判断一个属性是否属于一个对象<a id="more"></a></p>
<h2 id="for…in"><a href="#for…in" class="headerlink" title="for…in"></a>for…in</h2><p><code>for…in</code> 语句用于遍历<strong>数组</strong>或者<strong>对象的属性</strong></p>
<blockquote>
<p>对数组或者对象的属性进行循环操作</p>
</blockquote>
<p><code>for … in</code> 循环中的代码每执行一次，就会对<strong>数组的元素</strong>或者<strong>对象的属性</strong>进行一次操作</p>
<p>语法:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (变量 <span class="keyword">in</span> 对象)&#123;</span><br><span class="line">    <span class="comment">//todo</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>变量:用来指定变量，指定的变量可以是数组的属性，也可以是对象的属性<br>  如果是数组属性默认为下标</p>
</blockquote>
<p>实例:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//数组</span></span><br><span class="line"><span class="keyword">var</span> mycars = [<span class="string">"Saab"</span>,<span class="string">"Volvo"</span>,<span class="string">"BMW"</span>];</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> x <span class="keyword">in</span> mycars)&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(x);</span><br><span class="line">	<span class="built_in">console</span>.log(mycars[x]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//0</span></span><br><span class="line"><span class="comment">//Saab</span></span><br><span class="line"><span class="comment">//1</span></span><br><span class="line"><span class="comment">//Volvo</span></span><br><span class="line"><span class="comment">//2</span></span><br><span class="line"><span class="comment">//BMW</span></span><br><span class="line"><span class="comment">//对象</span></span><br><span class="line"><span class="keyword">var</span> mycars = &#123;</span><br><span class="line">	s:<span class="string">"Saab"</span>,</span><br><span class="line">	v:<span class="string">"Volvo"</span>,</span><br><span class="line">	b:<span class="string">"BMW"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> x <span class="keyword">in</span> mycars)&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(x);</span><br><span class="line">	<span class="built_in">console</span>.log(mycars[x]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//s</span></span><br><span class="line"><span class="comment">//Saab</span></span><br><span class="line"><span class="comment">//v</span></span><br><span class="line"><span class="comment">//Volvo</span></span><br><span class="line"><span class="comment">//b</span></span><br><span class="line"><span class="comment">//BMW</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>对于遍历的key是<code>string</code>类型,所以 <code>typeof x == &quot;string&quot;</code><br>  数组是特殊的对象<br>  使用<code>for-in</code>进行循环也被称为“枚举”<br>  如果数组/对象<code>prototype</code>的原型上添加一个函数如:<code>Array.prototype.test=function(){}</code>/<code>Object.prototype.test=function(){}</code>,也会遍历出来,可以使用<code>hasOwnProperty()</code>获得本身的属性，排除原型链上的属性<br>  由于以上的原因，尽量不要使用该方法遍历数组<br>  在<code>for-in</code>中，属性列表的顺序（序列）是不能保证的,所以最好数组使用正常的<code>for</code>循环，对象使用<code>for-in</code>循环</p>
</blockquote>
<h2 id="in"><a href="#in" class="headerlink" title="in"></a>in</h2><p><code>in</code>操作符用来判断某个属性属于某个对象，可以是对象的直接属性，也可以是通过<code>prototype</code>继承的属性</p>
<p>对于一般的对象属性需要用字符串指定<strong>属性的名称</strong>:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> mycar = &#123;</span><br><span class="line">	make: <span class="string">"Honda"</span>,</span><br><span class="line">	model: <span class="string">"Accord"</span>, </span><br><span class="line">	year: <span class="number">1998</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"make"</span> <span class="keyword">in</span> mycar); <span class="comment">//true</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"makse"</span> <span class="keyword">in</span> mycar); <span class="comment">//false</span></span><br></pre></td></tr></table></figure>

<p>对于数组属性需要指定数字形式的<strong>索引值</strong>来表示数组的属性名称（固有属性除外，如<code>length</code>）</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> trees = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="string">"redwood"</span>, <span class="string">"bay"</span>, <span class="string">"cedar"</span>, <span class="string">"oak"</span>, <span class="string">"maple"</span>);</span><br><span class="line"><span class="number">0</span> <span class="keyword">in</span> trees        <span class="comment">// returns true</span></span><br><span class="line"><span class="number">3</span> <span class="keyword">in</span> trees        <span class="comment">// returns true</span></span><br><span class="line"><span class="number">6</span> <span class="keyword">in</span> trees        <span class="comment">// returns false</span></span><br><span class="line"><span class="string">"bay"</span> <span class="keyword">in</span> trees    <span class="comment">// returns false (you must specify the index number,</span></span><br><span class="line">                  <span class="comment">// not the value at that index)</span></span><br><span class="line"><span class="string">"length"</span> <span class="keyword">in</span> trees <span class="comment">// returns true (length is an Array property)</span></span><br></pre></td></tr></table></figure>

<p>in的右边<strong>必须是一个对象</strong>,如：你可以指定一个用<code>String</code>构造器生成的，但是<strong>不能指定字符串直接量</strong>的形式：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> color1 = <span class="keyword">new</span> <span class="built_in">String</span>(<span class="string">"green"</span>);</span><br><span class="line"><span class="string">"length"</span> <span class="keyword">in</span> color1 <span class="comment">// returns true</span></span><br><span class="line"><span class="keyword">var</span> color2 = <span class="string">"coral"</span>;</span><br><span class="line"><span class="string">"length"</span> <span class="keyword">in</span> color2 <span class="comment">// Uncaught TypeError</span></span><br></pre></td></tr></table></figure>

<p>如果你使用<code>delete</code>操作符删除了一个属性，再次用<code>in</code>检查时，会返回<code>false</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> mycar = &#123;<span class="attr">make</span>: <span class="string">"Honda"</span>, <span class="attr">model</span>: <span class="string">"Accord"</span>, <span class="attr">year</span>: <span class="number">1998</span>&#125;;</span><br><span class="line"><span class="keyword">delete</span> mycar.make;</span><br><span class="line"><span class="string">"make"</span> <span class="keyword">in</span> mycar;  <span class="comment">// 返回false</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> trees = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="string">"redwood"</span>, <span class="string">"bay"</span>, <span class="string">"cedar"</span>, <span class="string">"oak"</span>, <span class="string">"maple"</span>);</span><br><span class="line"><span class="keyword">delete</span> trees[<span class="number">3</span>];</span><br><span class="line"><span class="number">3</span> <span class="keyword">in</span> trees; <span class="comment">// 返回false</span></span><br></pre></td></tr></table></figure>

<p>如果你把一个属性值设为<code>undefined</code>，但是没有使用<code>delete</code>操作符，使用<code>in</code>检查，会返回<code>true</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> mycar = &#123;<span class="attr">make</span>: <span class="string">"Honda"</span>, <span class="attr">model</span>: <span class="string">"Accord"</span>, <span class="attr">year</span>: <span class="number">1998</span>&#125;;</span><br><span class="line">mycar.make = <span class="literal">undefined</span>;</span><br><span class="line"><span class="string">"make"</span> <span class="keyword">in</span> mycar;  <span class="comment">// 返回true</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> trees = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="string">"redwood"</span>, <span class="string">"bay"</span>, <span class="string">"cedar"</span>, <span class="string">"oak"</span>, <span class="string">"maple"</span>);</span><br><span class="line">trees[<span class="number">3</span>] = <span class="literal">undefined</span>;</span><br><span class="line"><span class="number">3</span> <span class="keyword">in</span> trees; <span class="comment">// 返回true</span></span><br></pre></td></tr></table></figure>

<p>如果一个属性是从原型链上继承来的，in 运算符也会返回 true。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="string">"toString"</span> <span class="keyword">in</span> &#123;&#125;; <span class="comment">// 返回true</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>微信H5 Video 开发小结</title>
    <url>/2018/02/01/daily-h5-video/</url>
    <content><![CDATA[<span itemprop="image" itemscope itemtype="http://schema.org/ImageObject"><img itemprop="url image" src="/styles/images/wechat/video/video_bg.jpg" class="full-image" alt="微信H5 Video" title="微信H5 Video"><meta itemprop="width" content="auto"><meta itemprop="height" content="auto"></span>

<p>最近开发公众号内H5网页过程中，为了达到“炫”的效果，有些特效采用了video展示，但是微信内的video展示着实太费神了，这里总结一些遇到的坑和解决的办法。<a id="more"></a></p>
<h2 id="video的属性"><a href="#video的属性" class="headerlink" title="video的属性"></a>video的属性</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">video</span></span></span><br><span class="line"><span class="tag">  <span class="attr">id</span>=<span class="string">"video"</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">src</span>=<span class="string">"video.mp4"</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">controls</span> = <span class="string">"true"</span></span></span><br><span class="line">  poster="images.jpg"   &lt;!--视频封面--&gt;</span><br><span class="line">  preload="auto" </span><br><span class="line">  webkit-playsinline="true" <span class="comment">&lt;!--这个属性是ios 10中设置可以让视频在小窗内播放，也就是不是全屏播放 --&gt;</span> </span><br><span class="line">  playsinline="true"  <span class="comment">&lt;!--IOS微信浏览器支持小窗内播放--&gt;</span></span><br><span class="line">  x-webkit-airplay="allow" </span><br><span class="line">  x5-video-player-type="h5"  <span class="comment">&lt;!--启用H5播放器,是wechat安卓版特性--&gt;</span></span><br><span class="line">  x5-video-player-fullscreen="true" <span class="comment">&lt;!--全屏设置，设置为 true 是防止横屏--&gt;</span></span><br><span class="line">  x5-video-orientation="portraint" <span class="comment">&lt;!--播放器支付的方向， landscape横屏，portraint竖屏，默认值为竖屏--&gt;</span></span><br><span class="line">  style="object-fit:fill"&gt;</span><br><span class="line"><span class="tag">&lt;/<span class="name">video</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p><code>src</code>: 视频的地址</p>
</li>
<li><p><code>controls</code>: 加上这个属性，Gecko 会提供用户控制，允许用户控制视频的播放，包括音量，跨帧，暂停/恢复播放。</p>
</li>
<li><p><code>poster</code>: 属性规定视频下载时显示的图像，或者在用户点击播放按钮前显示的图像。如果未设置该属性，则使用视频的第一帧来代替。</p>
</li>
<li><p><code>preload</code>: 属性规定在页面加载后载入视频。</p>
</li>
<li><p><code>webkit-playsinline</code>和<code>playsinline</code>: <strong>视频播放时局域播放，不脱离文档流</strong>。但是这个属性比较特别，需要嵌入网页的APP比如WeChat中UIwebview 的allowsInlineMediaPlayback = YES webview.allowsInlineMediaPlayback = YES，才能生效。换句话说，如果APP不设置，你页面中加了这标签也无效，这也就是为什么安卓手机WeChat 播放视频总是全屏，因为APP不支持playsinline，而IOS的WeChat却支持。<br>这里就要补充下，如果是想做全屏直播或者全屏H5体验的用户，IOS需要设置删除 webkit-playsinline 标签，因为你设置 false 是不支持的 ，安卓则不需要，因为默认全屏。但这时候全屏是有播放控件的，无论你有没有设置control。 做直播的可能用得着播放控件，但是全屏H5是不需要的，那么去除全屏播放时候的控件，需要以下设置：同层播放</p>
</li>
<li><p><code>x-webkit-airplay=&quot;allow&quot;</code> : <strong>这个属性应该是使此视频支持ios的AirPlay功能</strong>。使用AirPlay可以直接从使用iOS的设备上的不同位置播放视频、音乐还有照片文件，也就是说通过AirPlay功能可以实现影音文件的无线播放，当然前提是播放的终端设备也要支持相应的功能</p>
</li>
<li><p><code>x5-video-player-type</code>: <strong>启用同层H5播放器，就是在视频全屏的时候，div可以呈现在视频层上，也是WeChat安卓版特有的属性</strong>。同层播放别名也叫做沉浸式播放，播放的时候看似全屏，但是已经除去了control和微信的导航栏，只留下”X”和”&lt;”两键。目前的同层播放器只在Android（包括微信）上生效，暂时不支持iOS。至于为什么同层播放只对安卓开放，是因为安卓不能像ISO一样局域播放，默认的全屏会使得一些界面操作被阻拦，如果是全屏H5还好，但是做直播的话，诸如弹幕那样的功能就无法实现了，所以这时候同层播放的概念就解决了这个问题。不过在测试的过程中发现，不同版本的IOS和安卓效果略有不同</p>
</li>
<li><p><code>x5-video-orientation</code>: <strong>声明播放器支持的方向，可选值landscape 横屏, portraint竖屏</strong>。默认值<code>portraint</code>。无论是直播还是全屏H5一般都是竖屏播放，但是这个属性需要x5-video-player-type开启H5模式</p>
</li>
<li><p><code>x5­-video­-player­-fullscreen</code>:<strong>全屏设置</strong>。它又两个属性值，ture和false，true支持全屏播放，false不支持全屏播放。其实，IOS 微信浏览器是Chrome的内核，相关的属性都支持，也是为什么X5同层播放不支持的原因。安卓微信浏览器是X5内核，一些属性标签比如playsinline就不支持，所以始终全屏。</p>
</li>
</ul>
<blockquote>
<p>playsinline 属性在 iOS 10 之前需要写成 webkit-playsinline，它的浏览器厂商前缀在 iOS 10 中被移除。但是目前 iOS 微信还不支持去掉前缀的写法，两个属性最好都加上。</p>
</blockquote>
<h2 id="Media方法和属性"><a href="#Media方法和属性" class="headerlink" title="Media方法和属性"></a>Media方法和属性</h2><p>HTMLVideoElement 和HTMLAudioElement 均继承自HTMLMediaElement</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//错误状态 </span></span><br><span class="line">Media.error; <span class="comment">//null:正常 </span></span><br><span class="line">Media.error.code; <span class="comment">//1.用户终止 2.网络错误 3.解码错误 4.URL无效 </span></span><br><span class="line"></span><br><span class="line"><span class="comment">//网络状态 </span></span><br><span class="line">Media.currentSrc; <span class="comment">//返回当前资源的URL </span></span><br><span class="line">Media.src = value; <span class="comment">//返回或设置当前资源的URL </span></span><br><span class="line">Media.canPlayType(type); <span class="comment">//是否能播放某种格式的资源 </span></span><br><span class="line">Media.networkState; <span class="comment">//0.此元素未初始化 1.正常但没有使用网络 2.正在下载数据 3.没有找到资源 </span></span><br><span class="line">Media.load(); <span class="comment">//重新加载src指定的资源 </span></span><br><span class="line">Media.buffered; <span class="comment">//返回已缓冲区域，TimeRanges </span></span><br><span class="line">Media.preload; <span class="comment">//none:不预载 metadata:预载资源信息 auto: </span></span><br><span class="line"></span><br><span class="line"><span class="comment">//准备状态 </span></span><br><span class="line">Media.readyState;  <span class="comment">//1:HAVE_NOTHING 2:HAVE_METADATA 3.HAVE_CURRENT_DATA 4.HAVE_FUTURE_DATA 5.HAVE_ENOUGH_DATA </span></span><br><span class="line">Media.seeking; <span class="comment">//是否正在seeking </span></span><br><span class="line"></span><br><span class="line"><span class="comment">//回放状态 </span></span><br><span class="line">Media.currentTime = value; <span class="comment">//当前播放的位置，赋值可改变位置 </span></span><br><span class="line">Media.startTime; <span class="comment">//一般为0，如果为流媒体或者不从0开始的资源，则不为0 </span></span><br><span class="line">Media.duration; <span class="comment">//当前资源长度 流返回无限 </span></span><br><span class="line">Media.paused; <span class="comment">//是否暂停 </span></span><br><span class="line">Media.defaultPlaybackRate = value;<span class="comment">//默认的回放速度，可以设置 </span></span><br><span class="line">Media.playbackRate = value;<span class="comment">//当前播放速度，设置后马上改变 </span></span><br><span class="line">Media.played; <span class="comment">//返回已经播放的区域，TimeRanges，关于此对象见下文 </span></span><br><span class="line">Media.seekable; <span class="comment">//返回可以seek的区域 TimeRanges </span></span><br><span class="line">Media.ended; <span class="comment">//是否结束 </span></span><br><span class="line">Media.autoPlay; <span class="comment">//是否自动播放 </span></span><br><span class="line">Media.loop; <span class="comment">//是否循环播放 </span></span><br><span class="line">Media.play();  <span class="comment">//播放 </span></span><br><span class="line">Media.pause();  <span class="comment">//暂停 </span></span><br><span class="line"></span><br><span class="line"><span class="comment">//控制 </span></span><br><span class="line">Media.controls;<span class="comment">//是否有默认控制条 </span></span><br><span class="line">Media.volume = value; <span class="comment">//音量 </span></span><br><span class="line">Media.muted = value; <span class="comment">//静音 </span></span><br><span class="line"></span><br><span class="line"><span class="comment">//TimeRanges(区域)对象 </span></span><br><span class="line">TimeRanges.length; <span class="comment">//区域段数 </span></span><br><span class="line">TimeRanges.start(index) <span class="comment">//第index段区域的开始位置 </span></span><br><span class="line">TimeRanges.end(index) <span class="comment">//第index段区域的结束位置</span></span><br></pre></td></tr></table></figure>

<h2 id="Media相关的事件"><a href="#Media相关的事件" class="headerlink" title="Media相关的事件"></a>Media相关的事件</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;loadstart&quot;：客户端开始请求数据</span><br><span class="line">&quot;progress&quot;：客户端正在请求数据</span><br><span class="line">&quot;suspend&quot;：延迟下载</span><br><span class="line">&quot;abort&quot;：客户端主动终止下载（不是因为错误引起），</span><br><span class="line">&quot;error&quot;：请求数据时遇到错误</span><br><span class="line">&quot;stalled&quot;：网速失速</span><br><span class="line">&quot;play&quot;：play()和autoplay开始播放时触发</span><br><span class="line">&quot;pause&quot;：pause()触发</span><br><span class="line">&quot;loadedmetadata&quot;：成功获取资源长度</span><br><span class="line">&quot;loadeddata&quot;：</span><br><span class="line">&quot;waiting&quot;：等待数据，并非错误</span><br><span class="line">&quot;playing&quot;：开始回放</span><br><span class="line">&quot;canplay&quot;：可以播放，但中途可能因为加载而暂停</span><br><span class="line">&quot;canplaythrough&quot;：可以播放，歌曲全部加载完毕</span><br><span class="line">&quot;seeking&quot;：寻找中</span><br><span class="line">&quot;seeked&quot;：寻找完毕</span><br><span class="line">&quot;timeupdate&quot;：播放时间改变</span><br><span class="line">&quot;ended&quot;：播放结束</span><br><span class="line">&quot;ratechange&quot;：播放速率改变</span><br><span class="line">&quot;durationchange&quot;：资源长度改变</span><br><span class="line">&quot;volumechange&quot;：音量改变</span><br></pre></td></tr></table></figure>

<h2 id="事件、属性及展示效果使用常见问题"><a href="#事件、属性及展示效果使用常见问题" class="headerlink" title="事件、属性及展示效果使用常见问题"></a>事件、属性及展示效果使用常见问题</h2><h3 id="预加载"><a href="#预加载" class="headerlink" title="预加载"></a>预加载</h3><p>移动端原因， video 并不会主动去预加载用户未需求的资源，因此我们需要手动去触发 video 的预加载资源（安卓端下，滚动行为不归属在用户操作行为中）</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.getElementById(<span class="string">'my-video'</span>).play();</span><br></pre></td></tr></table></figure>

<p>这样就可以了吗？不，万恶的微信限制了必须用户行为才能播放媒体资源，因此我们只能再祭出万能 hack：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"//res.wx.qq.com/open/js/jweixin-1.2.0.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.addEventListener(<span class="string">'DOMContentLoaded'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> video = <span class="built_in">document</span>.getElementById(<span class="string">'my-video'</span>);</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">preload</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        video.play();</span><br><span class="line">        setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">            video.pause();</span><br><span class="line">        &#125;, <span class="number">200</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">document</span>.addEventListener(<span class="string">"WeixinJSBridgeReady"</span>,  preload, <span class="literal">false</span>);</span><br><span class="line">    <span class="comment">/*这一段调用会有内存问题，暂未找到原因*/</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> WeixinJSBridge == <span class="string">"object"</span> &amp;&amp; <span class="keyword">typeof</span> WeixinJSBridge.invoke == <span class="string">"function"</span>) &#123;</span><br><span class="line">        WeixinJSBridge.invoke(<span class="string">"getNetworkType"</span>, &#123;&#125;, preload);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//preload(); </span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="全屏处理"><a href="#全屏处理" class="headerlink" title="全屏处理"></a>全屏处理</h3><h4 id="IOS"><a href="#IOS" class="headerlink" title="IOS"></a>IOS</h4><p>ios加<code>playsinline</code>属性，之前只带webkit前缀的在ios10以后，会吊起系统自带播放器，两个属性都加上基本ios端都可以保证内敛到浏览器webview里面了。如果仍有个别版本的ios会吊起播放器，还可以引用一个库<code>iphone-inline-video</code>（具体用法很简单看它 <a href="https://github.com/bfred-it/iphone-inline-video" target="_blank" rel="noopener">github</a>，这里不介绍了，只需加js一句话，css加点），github地址加上<code>playsinline webkit-playsinline</code>这两个属性和这个库基本可以保证ios端没有问题了（不过亲测，只加这两个属性不引入库好像也是ok的，至今没有在ios端微信没有出现问题，如果你要兼容uc或者qq的浏览器建议带上这个库）</p>
<h4 id="android"><a href="#android" class="headerlink" title="android"></a>android</h4><p>x5-video-player-type=”h5”属性，腾讯x5内核系的android微信和手Q内置浏览器用的浏览器webview的内核，使用这个属性在微信中视频会有不同的表现，会呈现全屏状态，貌似播放控件剥去了，至少加了这个属性后视频上层可以有其他dom元素出现了（非腾讯白名单机制的一种处理措施）。</p>
<h3 id="自动播放"><a href="#自动播放" class="headerlink" title="自动播放"></a>自动播放</h3><p>android始终不能自动播放，不多说。值得一提的是经测现在ios10后版本的safari和微信都不让视频自动播放了（顺带音频也不能自动播放了），但微信提供了一个事件<code>WeixinJSBridgeReady</code>，在微信嵌入webview全局的这个事件触发后，视频仍可以自动播放，这个应该是现在在ios端微信的视频自动播放的比较靠谱的方式，其他如手q或者其他浏览器，建议就引导用户出发触屏的行为操作出发比较好。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.addEventListener(<span class="string">"WeixinJSBridgeReady"</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123; </span><br><span class="line">    video.play();</span><br><span class="line">    video.pause();</span><br><span class="line">&#125;, <span class="literal">false</span>)</span><br></pre></td></tr></table></figure>

<h3 id="播放控制"><a href="#播放控制" class="headerlink" title="播放控制"></a>播放控制</h3><p>对于video或者audio等媒体元素，有一些方法，常用的有<code>play(),pause()</code>;也有一些事件，如<code>&#39;loadstart&#39;,&#39;canplay&#39;,&#39;canplaythrough&#39;,&#39;ended&#39;,&#39;timeupdate&#39;.....</code>等等。<br>在移动端有一些坑需要注意，不要轻易使用媒体元素的除<code>&#39;ended&#39;,&#39;timeupdate&#39;</code>以外event事件，在不同的机子上可能有不同的情况产生，例如：ios下监听<code>&#39;canplay&#39;</code>和<code>&#39;canplaythrough&#39;</code>（是否已缓冲了足够的数据可以流畅播放）,当加载时是不会触发的，即使<code>preload=&quot;auto&quot;</code>也没用，但在pc的chrome调试器下和android下，是会在加载阶段就触发。ios需要播放后才会触发。总之就是现在的视频标准还不尽完善，有很多坑要注意，要使用前最好自己亲测一遍<br>就是当第一次播放视频的时候ios端，如果网络慢，视频从开始播到能展现画面会有短暂的黑屏（处理视频源数据的时间），为了避免这个黑屏，可以在视频上加个div浮层（可以一个假的视频第一帧），然后用<code>timeupdate</code>方法监听，视屏播放及有画面的时候再移除浮层</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">video.addEventListener(<span class="string">'timeupdate'</span>,<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">//当视频的currentTime大于0.1时表示黑屏时间已过，已有视频画面，可以移除浮层（.pagestart的div元素）</span></span><br><span class="line">    <span class="keyword">if</span> ( !video.isPlayed &amp;&amp; <span class="keyword">this</span>.currentTime&gt;<span class="number">0.1</span> )&#123;</span><br><span class="line">        $(<span class="string">'.pagestart'</span>).fadeOut(<span class="number">500</span>);</span><br><span class="line">        video.isPlayed = !<span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="隐藏播放控件"><a href="#隐藏播放控件" class="headerlink" title="隐藏播放控件"></a>隐藏播放控件</h3><p>据说腾讯的android团队的x5内核团队放开了视频播放的限制，视频不一定调用它们那个备受诟病的视频播放器了，<code>x5-video-player-type=&quot;h5&quot;</code>属性这个属性就有点那个意思，虽然体验还是有点…（导航栏也会清理）但至少播放器控件没有了，上层可以浮div或者其他元素了，这个还是值得一提。还有一点值得说的是，带播放器控件的隐藏.</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"videobox"</span> <span class="attr">ontouchmove</span>=<span class="string">"return false;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">video</span> <span class="attr">id</span>=<span class="string">"mainvideo"</span> <span class="attr">src</span>=<span class="string">"test.mp4"</span> <span class="attr">x5-video-player-type</span>=<span class="string">"h5"</span> <span class="attr">playsinline</span> <span class="attr">webkit-playsinline</span>&gt;</span><span class="tag">&lt;/<span class="name">video</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>比如这个videobox在android下隐藏，只用<code>display：none</code>貌似还是不行的，但加个<code>z-index:-1</code>，设置成-1就可以达到隐藏播放器控件的目的了。</p>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>CSS</tag>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 深拷贝性能分析</title>
    <url>/2018/01/21/article-js-deepcopy/</url>
    <content><![CDATA[<blockquote>
<p>原文：<a href="https://dassur.ma/things/deep-copy/" target="_blank" rel="noopener">Deep-copying in JavaScript - DasSur.ma</a></p>
</blockquote>
<p>如何在 JavaScript 中拷贝一个对象？对于这个很简单的问题，但是答案却不简单。<a id="more"></a></p>
<h2 id="引用传值"><a href="#引用传值" class="headerlink" title="引用传值"></a>引用传值</h2><p>先看下面的例子：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">mutate</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  obj.a = <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> obj = &#123;<span class="attr">a</span>: <span class="literal">false</span>&#125;;</span><br><span class="line">mutate(obj)</span><br><span class="line"><span class="built_in">console</span>.log(obj.a); <span class="comment">// 输出 true</span></span><br></pre></td></tr></table></figure>

<p>函数 mutate 改变了它的参数。在值传递的场景中，函数的形参只是实参的一个副本——a copy——当函数调用完成后，并不改变实参。但是在 JavaScript 这种引用传递的场景中，函数的形参和实参指向同一个对象，当参数内部改变形参的时候，函数外面的实参也被改变了。</p>
<p>因此在某些情况下，你需要保留原始对象，这时你需要把原始对象的一个拷贝传入到函数中，以防止函数改变原始对象。</p>
<h2 id="浅拷贝：Object-assign"><a href="#浅拷贝：Object-assign" class="headerlink" title="浅拷贝：Object.assign()"></a>浅拷贝：Object.assign()</h2><p>一个简单的获取对象拷贝的方式是使用 Object.assign(target, sources…)。它接受任意数量的源对象，枚举它们的所有属性并分配给target。如果我们使用一个新的空对象target，那么我们就可以实现对象的复制。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> obj =<span class="string">''</span> <span class="comment">/* ... */</span>;</span><br><span class="line"><span class="keyword">const</span> copy = <span class="built_in">Object</span>.assign(&#123;&#125;, obj);</span><br></pre></td></tr></table></figure>

<p>然而这只是一个浅副本。如果我们的对象包含其它对象作为自己的属性，它们将保持共享引用，这不是我们想要的：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">mutateDeepObject</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  obj.a.thing = <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> obj = &#123;<span class="attr">a</span>: &#123;<span class="attr">thing</span>: <span class="literal">false</span>&#125;&#125;;</span><br><span class="line"><span class="keyword">const</span> copy = <span class="built_in">Object</span>.assign(&#123;&#125;, obj);</span><br><span class="line">mutateDeepObject(copy)</span><br><span class="line"><span class="built_in">console</span>.log(obj.a.thing); <span class="comment">// prints true</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>Object.assign 方法只会拷贝源对象自身的并且可枚举的属性到目标对象。该方法使用源对象的[[Get]]和目标对象的[[Set]]，所以它会调用相关 getter 和 setter。因此，它分配属性，而不仅仅是复制或定义新的属性。如果合并源包含getter，这可能使其不适合将新属性合并到原型中。为了将属性定义（包括其可枚举性）复制到原型，应使用<a href="https://link.funteas.com?target=http%3A%2F%2Flink.zhihu.com%2F%3Ftarget%3Dhttps%253A%2F%2Fdeveloper.mozilla.org%2Fzh-CN%2Fdocs%2FWeb%2FJavaScript%2FReference%2FGlobal_Objects%2FObject%2FgetOwnPropertyDescriptor" target="_blank" rel="noopener">Object.getOwnPropertyDescriptor()</a>和<a href="https://link.funteas.com?target=http%3A%2F%2Flink.zhihu.com%2F%3Ftarget%3Dhttps%253A%2F%2Fdeveloper.mozilla.org%2Fzh-CN%2Fdocs%2FWeb%2FJavaScript%2FReference%2FGlobal_Objects%2FObject%2FdefineProperty" target="_blank" rel="noopener">Object.defineProperty()</a> 。</p>
</blockquote>
<p>所以现在怎么办？有几种方法可以创建一个对象的深拷贝。</p>
<p>注意：也许有人提到了<a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Spread_operator" target="_blank" rel="noopener">对象解构运算</a>，这也是浅拷贝。</p>
<h2 id="JSON-parse"><a href="#JSON-parse" class="headerlink" title="JSON.parse"></a>JSON.parse</h2><p>创建对象副本的最古老方法之一是：</p>
<p>将该对象转换为其 JSON 字符串表示形式，然后将其解析回对象。这感觉有点压抑，但它确实有效：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> obj = <span class="string">''</span><span class="comment">/* ... */</span>;</span><br><span class="line"><span class="keyword">const</span> copy = <span class="built_in">JSON</span>.parse(<span class="built_in">JSON</span>.stringify(obj));</span><br></pre></td></tr></table></figure>

<p>这里的缺点是你创建一个临时的，可能很大的字符串，只是为了把它重新放回解析器。另一个缺点是这种方法不能处理循环对象。而且循环对象经常发生。例如，当您构建树状数据结构，其中一个节点引用其父级，而父级又引用其子级。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> x = &#123;&#125;;</span><br><span class="line"><span class="keyword">const</span> y = &#123;x&#125;;</span><br><span class="line">x.y = y; <span class="comment">// Cycle: x.y.x.y.x.y.x.y.x...</span></span><br><span class="line"><span class="keyword">const</span> copy = <span class="built_in">JSON</span>.parse(<span class="built_in">JSON</span>.stringify(x)); <span class="comment">// throws!</span></span><br></pre></td></tr></table></figure>

<p>另外，诸如 Maps, Sets, RegExps, Dates, ArrayBuffers 和其他内置类型在进行序列化时会丢失。</p>
<h2 id="Structured-Clone-结构化克隆算法"><a href="#Structured-Clone-结构化克隆算法" class="headerlink" title="Structured Clone 结构化克隆算法"></a>Structured Clone 结构化克隆算法</h2><p><a href="https://html.spec.whatwg.org/multipage/structured-data.html#structuredserializeinternal" target="_blank" rel="noopener">Structured cloning</a> 是一种现有的算法，用于将值从一个地方转移到另一地方。例如，每当您调用postMessage将消息发送到另一个窗口或 <a href="https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Using_web_workers" target="_blank" rel="noopener">WebWorker</a> 时，都会使用它。关于结构化克隆的好处在于它处理循环对象并支持<a href="https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Structured_clone_algorithm#Supported_types" target="_blank" rel="noopener">大量的内置类型</a>。问题是，在编写本文时，该算法并不能直接使用，只能作为其他 API 的一部分。</p>
<h3 id="MessageChannel"><a href="#MessageChannel" class="headerlink" title="MessageChannel"></a>MessageChannel</h3><p>正如我所说的，只要你调用postMessage结构化克隆算法就可以使用。我们可以创建一个 <a href="https://developer.mozilla.org/en-US/docs/Web/API/MessageChannel/MessageChannel" target="_blank" rel="noopener">MessageChannel</a> 并发送消息。在接收端，消息包含我们原始数据对象的结构化克隆。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">structuralClone</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123;port1, port2&#125; = <span class="keyword">new</span> MessageChannel();</span><br><span class="line">    port2.onmessage = <span class="function"><span class="params">ev</span> =&gt;</span> resolve(ev.data);</span><br><span class="line">    port1.postMessage(obj);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> obj = <span class="string">''</span><span class="comment">/* ... */</span>;</span><br><span class="line"><span class="keyword">const</span> clone = <span class="keyword">await</span> structuralClone(obj);</span><br></pre></td></tr></table></figure>

<p>这种方法的缺点是它是<strong>异步的</strong>。虽然这并无大碍，但是有时候你需要使用同步的方式来深度拷贝一个对象。</p>
<h3 id="History-API"><a href="#History-API" class="headerlink" title="History API"></a>History API</h3><p>如果你曾经使用<code>history.pushState()</code>写过 SPA，你就知道你可以提供一个状态对象来保存 URL。事实证明，这个状态对象使用结构化克隆 - 而且是同步的。我们必须小心使用，不要把程序逻辑使用的状态对象搞乱了，所以我们需要在完成克隆之后恢复原始状态。为了防止发生任何意外，请使用<code>history.replaceState()</code>而不是<code>history.pushState()</code>。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">structuralClone</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> oldState = history.state;</span><br><span class="line">  history.replaceState(obj, <span class="built_in">document</span>.title);</span><br><span class="line">  <span class="keyword">const</span> copy = history.state;</span><br><span class="line">  history.replaceState(oldState, <span class="built_in">document</span>.title);</span><br><span class="line">  <span class="keyword">return</span> copy;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> obj = <span class="string">''</span><span class="comment">/* ... */</span>;</span><br><span class="line"><span class="keyword">const</span> clone = structuralClone(obj);</span><br></pre></td></tr></table></figure>

<p>然而，仅仅为了复制一个对象，而使用浏览器的引擎，感觉有点过分。另外，Safari 浏览器对replaceState调用的限制数量为 30 秒内 100 次。</p>
<h3 id="Notification-API"><a href="#Notification-API" class="headerlink" title="Notification API"></a>Notification API</h3><p>第三种方法来利用结构化克隆：Notification API。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">structuralClone</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> Notification(<span class="string">''</span>, &#123;<span class="attr">data</span>: obj, <span class="attr">silent</span>: <span class="literal">true</span>&#125;).data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> obj = <span class="string">''</span><span class="comment">/* ... */</span>;</span><br><span class="line"><span class="keyword">const</span> clone = structuralClone(obj);</span><br></pre></td></tr></table></figure>

<p>短小，简洁。</p>
<p>但是，它需要浏览器内部的权限机制，所以我怀疑它是很慢的。由于某种原因，Safari 总是返回<code>undefined</code>。</p>
<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><ul>
<li>如果您没有循环对象，并且不需要保留内置类型，则可以使用跨浏览器的<code>JSON.parse(JSON.stringify())</code>获得最快的克隆性能，这让我感到非常惊讶。</li>
<li>如果你想要一个适当的结构化克隆，<code>MessageChannel</code>是你唯一可靠的跨浏览器的选择。</li>
</ul>
<p>如果浏览器平台直接提供一个 structuredClone()函数，会不会更好？我当然这样认为，最新的 HTML 规范正在讨论这个 <a href="https://github.com/whatwg/html/issues/793" target="_blank" rel="noopener">Synchronous clone = global.structuredClone(value, transfer = []) API · Issue #793 · whatwg/html</a>。</p>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>HTML5 Canvas属性和方法汇总</title>
    <url>/2018/01/16/article-h5-canvas/</url>
    <content><![CDATA[<p>Canvas画布是html5新增标签元素，主要用于通过脚本(通常是javascript)来绘制图像，而canvas元素本身并没有绘制能力(它仅仅是图形的容器)，必须使用脚本来完成实际的绘图任务<br>getContext()方法可返回一个对象，该对象提供了用于在画布上绘图的方法和属性<br>详细Canvas API请参考: <a href="https://demo.luckyw.cn/src/canvas-api/" target="_blank" rel="noopener">//demo.luckyw.cn/src/canvas-api/</a></p>
<a id="more"></a>

<h2 id="浏览器支持"><a href="#浏览器支持" class="headerlink" title="浏览器支持"></a>浏览器支持</h2><p>IE9、Firefox、Opera、Chrome以及Safari均支持canvas及其属性和方法。</p>
<blockquote>
<p>IE8以及更早的版本不支持canvas元素</p>
</blockquote>
<h2 id="颜色、样式和阴影"><a href="#颜色、样式和阴影" class="headerlink" title="颜色、样式和阴影"></a>颜色、样式和阴影</h2><table>
<thead>
<tr>
<th align="left">属性</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">fillStyle</td>
<td align="left">设置或返回用于填充绘画的颜色、渐变或模式</td>
</tr>
<tr>
<td align="left">strokeStyle</td>
<td align="left">设置或返回用于笔触的颜色、渐变或模式</td>
</tr>
<tr>
<td align="left">shadowColor</td>
<td align="left">设置或返回用于阴影的颜色</td>
</tr>
<tr>
<td align="left">shadowBlur</td>
<td align="left">设置或返回用于阴影的模糊级别</td>
</tr>
<tr>
<td align="left">shadowOffsetX</td>
<td align="left">设置或返回阴影距形状的水平距离</td>
</tr>
<tr>
<td align="left">shadowOffsetY</td>
<td align="left">设置或返回阴影距形状的垂直距离</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="left">方法</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">createLinearGradient()</td>
<td align="left">创建线性渐变（用在画布内容上）</td>
</tr>
<tr>
<td align="left">createPattern()</td>
<td align="left">在指定的方向上重复指定的元素</td>
</tr>
<tr>
<td align="left">createRadialGradient()</td>
<td align="left">创建放射状/环形的渐变（用在画布内容上）</td>
</tr>
<tr>
<td align="left">addColorStop()</td>
<td align="left">规定渐变对象中的颜色和停止位置</td>
</tr>
</tbody></table>
<h2 id="线条样式"><a href="#线条样式" class="headerlink" title="线条样式"></a>线条样式</h2><table>
<thead>
<tr>
<th align="left">属性</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">lineCap</td>
<td align="left">设置或返回线条的结束端点样式</td>
</tr>
<tr>
<td align="left">lineJoin</td>
<td align="left">设置或返回两条线相交时，所创建的拐角类型</td>
</tr>
<tr>
<td align="left">lineWidth</td>
<td align="left">设置或返回当前的线条宽度</td>
</tr>
<tr>
<td align="left">miterLimit</td>
<td align="left">设置或返回最大斜接长度</td>
</tr>
</tbody></table>
<h2 id="矩形"><a href="#矩形" class="headerlink" title="矩形"></a>矩形</h2><table>
<thead>
<tr>
<th align="left">方法</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">rect()</td>
<td align="left">创建矩形</td>
</tr>
<tr>
<td align="left">fillRect()</td>
<td align="left">绘制“被填充”的矩形</td>
</tr>
<tr>
<td align="left">strokeRect()</td>
<td align="left">绘制矩形（无填充）</td>
</tr>
<tr>
<td align="left">clearRect()</td>
<td align="left">在给定的矩形内清除指定的像素</td>
</tr>
</tbody></table>
<h2 id="路径"><a href="#路径" class="headerlink" title="路径"></a>路径</h2><table>
<thead>
<tr>
<th align="left">方法</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">fill()</td>
<td align="left">填充当前绘图（路径）</td>
</tr>
<tr>
<td align="left">stroke()</td>
<td align="left">绘制已定义的路径</td>
</tr>
<tr>
<td align="left">beginPath()</td>
<td align="left">起始一条路径，或重置当前路径</td>
</tr>
<tr>
<td align="left">moveTo()</td>
<td align="left">把路径移动到画布中的指定点，不创建线条</td>
</tr>
<tr>
<td align="left">closePath()</td>
<td align="left">创建从当前点回到起始点的路径</td>
</tr>
<tr>
<td align="left">lineTo()</td>
<td align="left">添加一个新点，然后在画布中创建从该点到最后指定点的线条</td>
</tr>
<tr>
<td align="left">clip()</td>
<td align="left">从原始画布剪切任意形状和尺寸的区域</td>
</tr>
<tr>
<td align="left">quadraticCurveTo()</td>
<td align="left">创建二次贝塞尔曲线</td>
</tr>
<tr>
<td align="left">bezierCurveTo()</td>
<td align="left">创建三次方贝塞尔曲线</td>
</tr>
<tr>
<td align="left">arc()</td>
<td align="left">创建弧/曲线（用于创建圆形或部分圆）</td>
</tr>
<tr>
<td align="left">arcTo()</td>
<td align="left">创建两切线之间的弧/曲线</td>
</tr>
<tr>
<td align="left">isPointInPath()</td>
<td align="left">如果指定的点位于当前路径中，则返回 true，否则返回 false</td>
</tr>
</tbody></table>
<h2 id="转换"><a href="#转换" class="headerlink" title="转换"></a>转换</h2><table>
<thead>
<tr>
<th align="left">方法</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">scale()</td>
<td align="left">缩放当前绘图至更大或更小</td>
</tr>
<tr>
<td align="left">rotate()</td>
<td align="left">旋转当前绘图</td>
</tr>
<tr>
<td align="left">translate()</td>
<td align="left">重新映射画布上的 (0,0) 位置</td>
</tr>
<tr>
<td align="left">transform()</td>
<td align="left">替换绘图的当前转换矩阵</td>
</tr>
<tr>
<td align="left">setTransform()</td>
<td align="left">将当前转换重置为单位矩阵。然后运行 transform()</td>
</tr>
</tbody></table>
<h2 id="文本"><a href="#文本" class="headerlink" title="文本"></a>文本</h2><table>
<thead>
<tr>
<th align="left">属性</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">font</td>
<td align="left">设置或返回文本内容的当前字体属性</td>
</tr>
<tr>
<td align="left">textAlign</td>
<td align="left">设置或返回文本内容的当前对齐方式</td>
</tr>
<tr>
<td align="left">textBaseline</td>
<td align="left">设置或返回在绘制文本时使用的当前文本基线</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="left">方法</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">fillText()</td>
<td align="left">在画布上绘制“被填充的”文本</td>
</tr>
<tr>
<td align="left">strokeText()</td>
<td align="left">在画布上绘制文本（无填充）</td>
</tr>
<tr>
<td align="left">measureText()</td>
<td align="left">返回包含指定文本宽度的对象</td>
</tr>
</tbody></table>
<h2 id="图像绘制"><a href="#图像绘制" class="headerlink" title="图像绘制"></a>图像绘制</h2><table>
<thead>
<tr>
<th align="left">方法</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">drawImage()</td>
<td align="left">向画布上绘制图像、画布或视频</td>
</tr>
</tbody></table>
<h2 id="像素操作"><a href="#像素操作" class="headerlink" title="像素操作"></a>像素操作</h2><table>
<thead>
<tr>
<th align="left">属性</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">width</td>
<td align="left">返回 ImageData 对象的宽度</td>
</tr>
<tr>
<td align="left">height</td>
<td align="left">返回 ImageData 对象的高度</td>
</tr>
<tr>
<td align="left">data</td>
<td align="left">返回一个对象，其包含指定的 ImageData 对象的图像数据</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th align="left">方法</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">createImageData()</td>
<td align="left">创建新的、空白的 ImageData 对象</td>
</tr>
<tr>
<td align="left">getImageData()</td>
<td align="left">返回 ImageData 对象，该对象为画布上指定的矩形复制像素数据</td>
</tr>
<tr>
<td align="left">putImageData()</td>
<td align="left">把图像数据（从指定的 ImageData 对象）放回画布上</td>
</tr>
</tbody></table>
<h2 id="合成"><a href="#合成" class="headerlink" title="合成"></a>合成</h2><table>
<thead>
<tr>
<th align="left">属性</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">globalAlpha</td>
<td align="left">设置或返回绘图的当前 alpha 或透明值</td>
</tr>
<tr>
<td align="left">globalCompositeOperation</td>
<td align="left">设置或返回新图像如何绘制到已有的图像上</td>
</tr>
</tbody></table>
<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><table>
<thead>
<tr>
<th align="left">方法</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">save()</td>
<td align="left">保存当前环境的状态</td>
</tr>
<tr>
<td align="left">restore()</td>
<td align="left">返回之前保存过的路径状态和属性</td>
</tr>
<tr>
<td align="left">createEvent()</td>
<td align="left"></td>
</tr>
<tr>
<td align="left">getContext()</td>
<td align="left"></td>
</tr>
<tr>
<td align="left">toDataURL()</td>
<td align="left"></td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>高清适配方案中iPhone X全屏兼容</title>
    <url>/2018/01/02/daily-css-rem-iphone-x/</url>
    <content><![CDATA[<ol>
<li><p>需要在viewport中添加 <code>viewport-fit=cover</code>，js 和 meta 标签中都要添加；</p>
</li>
<li><p>同时iPhone X屏幕底部圆弧区域占位高度使用 <code>calc(constant(safe-area-inset-bottom) * 3)</code> 作为<code>padding-bottom</code>， <code>margin-bottom</code>等高度属性的值。iPhone X上，高清适配方案中，此值等价于<code>.34rem</code>，即34逻辑像素。根据实际情况可以修改* 3这个系数来调整占位区域的高度，但一定要使用<code>constant(safe-area-inset-bottom)</code>。在ios11下，非iPhone X的设备此值为0，使用时需要注意样式覆盖的情况。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>animationend 事件</title>
    <url>/2017/12/21/article-css-animation-event/</url>
    <content><![CDATA[<h3 id="CSS动画事件"><a href="#CSS动画事件" class="headerlink" title="CSS动画事件"></a>CSS动画事件</h3><blockquote>
<p>CSS 动画播放时，会发生以下三个事件：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">animationstart - CSS 动画开始后触发</span><br><span class="line">animationiteration - CSS 动画重复播放时触发</span><br><span class="line">animationend - CSS 动画完成后触发</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注意标准语法都是小写，兼容大小写驼峰写法</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">webkitAnimationStart</span><br><span class="line">webkitAnimationIteration</span><br><span class="line">webkitAnimationEnd</span><br></pre></td></tr></table></figure>

<h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">object.addEventListener(<span class="string">"webkitAnimationEnd"</span>, myScript);  <span class="comment">// Chrome, Safari 和 Opera </span></span><br><span class="line">object.addEventListener(<span class="string">"animationend"</span>, myScript);        <span class="comment">// 标准语法</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>参考：<a href="http://www.runoob.com/jsref/event-animationend.html" target="_blank" rel="noopener">http://www.runoob.com/jsref/event-animationend.html</a></p>
</blockquote>
]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>webpack 3 入门指南</title>
    <url>/2017/12/19/article-webpack-3-10-0/</url>
    <content><![CDATA[<p>年初对webpack学习 <a href="/2017/02/12/article-webpack-know/">webpack 入门指南</a>，虽然基本概念变化不大，但是语法稍有变化。</p>
<p>现在重新学习记录一下令人困惑的地方，顺带搭建一套自用的 cli（正在测试中）。<a id="more"></a></p>
<h2 id="Webpack的核心原理"><a href="#Webpack的核心原理" class="headerlink" title="Webpack的核心原理"></a>Webpack的核心原理</h2><p>Webpack的两个最核心的原理分别是：</p>
<ol>
<li>一切皆模块</li>
</ol>
<p>正如js文件可以是一个“模块（module）”一样，其他的（如css、image或html）文件也可视作模 块。因此，你可以require(‘myJSfile.js’)亦可以require(‘myCSSfile.css’)。这意味着我们可以将事物（业务）分割成更小的易于管理的片段，从而达到重复利用等的目的。</p>
<ol start="2">
<li>按需加载</li>
</ol>
<p>传统的模块打包工具（module bundlers）最终将所有的模块编译生成一个庞大的bundle.js文件。但是在真实的app里边，“bundle.js”文件可能有10M到15M之大可能会导致应用一直处于加载中状态。因此Webpack使用许多特性来分割代码然后生成多个“bundle”文件，而且异步加载部分代码以实现按需加载。</p>
<p>好了，下面来看看那些令人困惑的部分吧。</p>
<h2 id="1-开发模式和生产模式"><a href="#1-开发模式和生产模式" class="headerlink" title="1. 开发模式和生产模式"></a>1. 开发模式和生产模式</h2><p>首先要知道的是Webpack有许许多多的特性，一些是”开发模式“下才有的，一些是”生产模式“下才有的，还有一些是两种模式下都有的。</p>
<p><img src="/styles/images/webpack/Webpack_files.png" alt="A sample dev v/s prod Webpack files"></p>
<blockquote>
<p>通常使用到Webpack如此多特性的项目都会有两个比较大的Webpack配置文件</p>
</blockquote>
<p>为了生成bundles文件你可能在<code>package.json</code>文件加入如下的scripts项：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="string">"scripts"</span>: &#123;</span><br><span class="line">  <span class="comment">// 运行npm run build 来编译生成生产模式下的bundles</span></span><br><span class="line">  <span class="string">"build"</span>: <span class="string">"webpack --config webpack.config.prod.js"</span>,</span><br><span class="line">  <span class="comment">// 运行npm run dev来生成开发模式下的bundles以及启动本地server</span></span><br><span class="line">  <span class="string">"dev"</span>: <span class="string">"webpack-dev-server"</span></span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-webpack-CLI-和webpack-dev-server"><a href="#2-webpack-CLI-和webpack-dev-server" class="headerlink" title="2. webpack CLI 和webpack-dev-server"></a>2. webpack CLI 和webpack-dev-server</h2><p>值得注意的是，Webpack作为模块打包工具，提供两种用户交互接口：</p>
<ol>
<li>Webpack CLI tool：默认的交互方式（已随Webpack本身安装到本地）</li>
<li>webpack-dev-server：一个Node.js服务器（需要开发者从npm自行安装）</li>
</ol>
<h3 id="Webpack-CLI（有利于生产模式下打包）"><a href="#Webpack-CLI（有利于生产模式下打包）" class="headerlink" title="Webpack CLI（有利于生产模式下打包）"></a>Webpack CLI（有利于生产模式下打包）</h3><p>这种方式可以从命令行获取参数也可以从配置文件（默认叫webpack.config.js）获取，将获取到的参数传入Webpack来打包。</p>
<blockquote>
<p>当然你也可以从命令行（CLI）开始学习Webpack，以后你可能主要在生产模式下使用到它。</p>
</blockquote>
<p>用法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">方式<span class="number">1</span>: </span><br><span class="line"><span class="comment">// 全局模式安装webpack</span></span><br><span class="line">npm install webpack --g</span><br><span class="line"><span class="comment">// 在终端输入</span></span><br><span class="line">$ webpack <span class="comment">// &lt;--使用webpack.config.js生成bundle</span></span><br><span class="line"></span><br><span class="line">方式 <span class="number">2</span> :</span><br><span class="line"><span class="comment">// 费全局模式安装webpack然后添加到package.json依赖里边</span></span><br><span class="line">npm install webpack --save</span><br><span class="line"><span class="comment">// 添加build命令到package.json的scripts配置项</span></span><br><span class="line"><span class="string">"scripts"</span>: &#123;</span><br><span class="line"> <span class="string">"build"</span>: <span class="string">"webpack --config webpack.config.prod.js -p"</span>,</span><br><span class="line"> ...</span><br><span class="line"> &#125;</span><br><span class="line"><span class="comment">// 用法：</span></span><br><span class="line"><span class="string">"npm run build"</span></span><br></pre></td></tr></table></figure>

<h3 id="webpack-dev-server（有利于在开发模式下编译）"><a href="#webpack-dev-server（有利于在开发模式下编译）" class="headerlink" title="webpack-dev-server（有利于在开发模式下编译）"></a>webpack-dev-server（有利于在开发模式下编译）</h3><p>这是一个基于Express.js框架开发的web server，默认监听8080端口。server内部调用Webpack，这样做的好处是提供了额外的功能如热更新“Live Reload”以及热替换“Hot Module Replacement”（即HMR）。</p>
<p>用法：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">方式 <span class="number">1</span>:</span><br><span class="line"><span class="comment">// 全局安装</span></span><br><span class="line">npm install webpack-dev-server --save</span><br><span class="line"><span class="comment">// 终端输入</span></span><br><span class="line">$ webpack-dev-server --inline --hot</span><br><span class="line"></span><br><span class="line">用法 <span class="number">2</span>:</span><br><span class="line"><span class="comment">// 添加到package.json scripts</span></span><br><span class="line"><span class="string">"scripts"</span>: &#123;</span><br><span class="line"> <span class="string">"start"</span>: <span class="string">"webpack-dev-server --inline --hot"</span>,</span><br><span class="line"> ...</span><br><span class="line"> &#125;</span><br><span class="line"><span class="comment">// 运行： </span></span><br><span class="line">$ npm start</span><br><span class="line"></span><br><span class="line"><span class="comment">// 浏览器预览：</span></span><br><span class="line">http:<span class="comment">//localhost:8080</span></span><br></pre></td></tr></table></figure>

<h3 id="Webpack-VS-Webpack-dev-server选项"><a href="#Webpack-VS-Webpack-dev-server选项" class="headerlink" title="Webpack VS Webpack-dev-server选项"></a>Webpack VS Webpack-dev-server选项</h3><p>注意像<code>inline</code>和<code>hot</code>这些选项是Webpack-dev-server特有的，而另外的如<code>hide-modules</code>则是CLI模式特有的选项。</p>
<h3 id="webpack-dev-server-CLI选项和配置项"><a href="#webpack-dev-server-CLI选项和配置项" class="headerlink" title="webpack-dev-server CLI选项和配置项"></a>webpack-dev-server CLI选项和配置项</h3><p>另外值得注意的是你可以通过以下两种方式向webpack-dev-server传入参数：</p>
<ol>
<li>通过webpack.config.js文件的”devServer”对象</li>
<li>通过CLI选项</li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 通过CLI传参</span></span><br><span class="line">webpack-dev-server --hot --inline</span><br><span class="line"><span class="comment">// 通过webpack.config.js传参</span></span><br><span class="line">devServer: &#123;</span><br><span class="line">  inline: <span class="literal">true</span>,</span><br><span class="line">  hot:<span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>我发现有时devServer配置项（hot: true 和inline: true）不生效，我更偏向使用如下的方式向CLI传递参数：</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// package.json</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="string">"scripts"</span>: <span class="string">"webpack-dev-server --hot --inline"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注意：确定你没有同时传入<code>hot:true</code>和<code>-hot</code></p>
</blockquote>
<h3 id="webpack-dev-server的“hot”-和-“inline”选项"><a href="#webpack-dev-server的“hot”-和-“inline”选项" class="headerlink" title="webpack-dev-server的“hot” 和 “inline”选项"></a>webpack-dev-server的“hot” 和 “inline”选项</h3><p>“inline”选项会为入口页面添加“热加载”功能，“hot”选项则开启“热替换（Hot Module Reloading）”，即尝试重新加载组件改变的部分（而不是重新加载整个页面）。如果两个参数都传入，当资源改变时，webpack-dev-server将会先尝试HRM（即热替换），如果失败则重新加载整个入口页面。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 当资源发生改变，以下三种方式都会生成新的bundle，但是又有区别：</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// 1. 不会刷新浏览器</span></span><br><span class="line">$ webpack-dev-server</span><br><span class="line"><span class="comment">//2. 刷新浏览器</span></span><br><span class="line">$ webpack-dev-server --inline</span><br><span class="line"><span class="comment">//3. 重新加载改变的部分，HRM失败则刷新页面</span></span><br><span class="line">$ webpack-dev-server  --inline --hot</span><br></pre></td></tr></table></figure>

<h2 id="3-“entry”：值分别是字符串、数组和对象的情况"><a href="#3-“entry”：值分别是字符串、数组和对象的情况" class="headerlink" title="3. “entry”：值分别是字符串、数组和对象的情况"></a>3. “entry”：值分别是字符串、数组和对象的情况</h2><p>Enter配置项告诉Webpack应用的根模块或起始点在哪里，它的值可以是字符串、数组或对象。这看起来可能令人困惑，因为不同类型的值有着不同的目的。</p>
<p>像绝大多数app一样，倘若你的应用只有一个单一的入口，enter项的值你可以使用任意类型，最终输出的结果都是一样的。</p>
<p><img src="/styles/images/webpack/Different.png" alt="Different entry types but same output"></p>
<h3 id="enter：数组类型"><a href="#enter：数组类型" class="headerlink" title="enter：数组类型"></a>enter：数组类型</h3><p>但是，如果你想添加多个彼此不互相依赖的文件，你可以使用数组格式的值。</p>
<p>例如，你可能在html文件里引用了“googleAnalytics.js”文件，可以告诉Webpack将其加到bundle.js的最后。</p>
<p><img src="/styles/images/webpack/enter.png" alt="enter array"></p>
<h3 id="enter：对象"><a href="#enter：对象" class="headerlink" title="enter：对象"></a>enter：对象</h3><p>现在，假设你的应用是多页面的（multi-page application）而不是SPA，有多个html文件（index.html和profile.html）。然后你通过一个对象告诉Webpack为每一个html生成一个bundle文件。</p>
<p>以下的配置将会生成两个js文件：indexEntry.js和profileEntry.js分别会在index.html和profile.html中被引用。</p>
<p><img src="/styles/images/webpack/enter_1.png" alt="entry object"></p>
<p>用法：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">//profile.html</span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">”dist/profileEntry.js”</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">//index.html</span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">”dist/indexEntry.js”</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>注意：文件名取自“entry”对象的键名。</p>
<h3 id="enter：混合类型"><a href="#enter：混合类型" class="headerlink" title="enter：混合类型"></a>enter：混合类型</h3><p>你也可以在enter对象里使用数组类型，例如下面的配置将会生成3个文件：vender.js（包含三个文件），index.js和profile.js文件。</p>
<p><img src="/styles/images/webpack/enter_2.png" alt="enter combination"></p>
<h2 id="4-output：“path”项和“publicPath”项"><a href="#4-output：“path”项和“publicPath”项" class="headerlink" title="4. output：“path”项和“publicPath”项"></a>4. output：“path”项和“publicPath”项</h2><p>output项告诉webpack怎样存储输出结果以及存储到哪里。output的两个配置项“path”和“publicPath”可能会造成困惑。</p>
<p>“path”仅仅告诉Webpack结果存储在哪里，然而“publicPath”项则被许多Webpack的插件用于在生产模式下更新内嵌到css、html文件里的url值。</p>
<p><img src="/styles/images/webpack/enter_3.png" alt="publicPath in Development vs Production"></p>
<p>例如，在localhost（译者注：即本地开发模式）里的css文件中边你可能用“./test.png”这样的url来加载图片，但是在生产模式下“test.png”文件可能会定位到CDN上并且你的Node.js服务器可能是运行在HeroKu上边的。这就意味着在生产环境你必须手动更新所有文件里的url为CDN的路径。</p>
<p>然而你也可以使用Webpack的“publicPath”选项和一些插件来在生产模式下编译输出文件时自动更新这些url。</p>
<p><img src="/styles/images/webpack/publicPath.png" alt="publicPath Production example"></p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">// 开发环境：Server和图片都是在localhost（域名）下</span><br><span class="line"><span class="selector-class">.image</span> &#123; </span><br><span class="line">  <span class="attribute">background-image</span>: <span class="built_in">url</span>(<span class="string">'./test.png'</span>);</span><br><span class="line"> &#125;</span><br><span class="line">// 生产环境：Server部署下HeroKu但是图片在CDN上</span><br><span class="line"><span class="selector-class">.image</span> &#123; </span><br><span class="line">  <span class="attribute">background-image</span>: <span class="built_in">url</span>(<span class="string">'https://someCDN/test.png'</span>);</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<h2 id="5-模块加载和链式模块加载"><a href="#5-模块加载和链式模块加载" class="headerlink" title="5. 模块加载和链式模块加载"></a>5. 模块加载和链式模块加载</h2><p>模块加载器是可自由添加的Node模块，用于将不同类型的文件“load”或“import”并转换成浏览器可以识别的类型，如js、Stylesheet等。更高级的模块加载器甚至可以支持使用ES6里边的“require”或“import”引入模块。</p>
<p>例如，你可以使用<code>babel-loader</code>来将使用ES6语法写成的文件转换成ES5：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line"> loaders: [&#123;</span><br><span class="line">  test: <span class="regexp">/\.js$/</span>, <span class="comment">// 匹配.js文件，如果通过则使用下面的loader</span></span><br><span class="line">  exclude: <span class="regexp">/node_modules/</span>, <span class="comment">// 排除node_modules文件夹</span></span><br><span class="line">  loader: <span class="string">'babel'</span> <span class="comment">// 使用babel（babel-loader的简写）作为loader</span></span><br><span class="line"> &#125;]</span><br></pre></td></tr></table></figure>

<h3 id="链式（管道式）的加载器（从右往左执行）"><a href="#链式（管道式）的加载器（从右往左执行）" class="headerlink" title="链式（管道式）的加载器（从右往左执行）"></a>链式（管道式）的加载器（从右往左执行）</h3><p>多个loader可以用在同一个文件上并且被链式调用。链式调用时从右到左执行且loader之间用“!”来分割。</p>
<p>例如，假设我们有一个名为“myCssFile.css”的css文件，然后我们想将它的内容使用style标签内联到最终输出的html里边。我们可以使用css-loader和style-loader两个loader来达到目的。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line"> loaders: [&#123;</span><br><span class="line">  test: <span class="regexp">/\.css$/</span>,</span><br><span class="line">  loader: <span class="string">'style!css'</span> <span class="comment">//(short for style-loader!css-loader)</span></span><br><span class="line"> &#125;]</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<p>这里展示它是如何工作的：</p>
<p><img src="/styles/images/webpack/loaders.png" alt="chaining loaders"></p>
<ol>
<li>Webpack在模块颞部搜索在css的依赖项，即Webpack检查js文件是否有“require(‘myCssFile.css’)”的引用，如果它发现有css的依赖，Webpack将css文件交给“css-loader”去处理</li>
<li>css-loader加载所有的css文件以及css自身的依赖（如，@import 其他css）到JSON对象里，Webpack然后将处理结果传给“style-loader”</li>
<li>style-loader接受JSON值然后添加一个style标签并将其内嵌到html文件里</li>
</ol>
<h2 id="6-loader自身可以配置"><a href="#6-loader自身可以配置" class="headerlink" title="6. loader自身可以配置"></a>6. loader自身可以配置</h2><p>模块加载器（loader）自身可以根据传入不同的参数进行配置。</p>
<p>在下面的例子中，我们可以配置url-loader来将小于1024字节的图片使用DataUrl替换而大于1024字节的图片使用url，我们可以用如下两种方式通过传入“limit“参数来实现这一目的：</p>
<p><img src="/styles/images/webpack/loaders_1.png" alt="configure loaders"></p>
<h2 id="7-babelrc-文件"><a href="#7-babelrc-文件" class="headerlink" title="7. .babelrc 文件"></a>7. .babelrc 文件</h2><p>babal-loader使用”presets“配置项来标识如何将ES6语法转成ES5以及如何转换React的JSX成js文件。我们可以用如下的方式使用”query“参数传入配置：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line">  loaders: [</span><br><span class="line">    &#123;</span><br><span class="line">      test: <span class="regexp">/\.jsx?$/</span>,</span><br><span class="line">      exclude: <span class="regexp">/(node_modules|bower_components)/</span>,</span><br><span class="line">      loader: <span class="string">'babel'</span>,</span><br><span class="line">      query: &#123;</span><br><span class="line">        presets: [<span class="string">'react'</span>, <span class="string">'es2015'</span>]</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然而在很多项目里babel的配置可能比较大，因此你可以把babel-loader的配置项单独保存在一个名为”.babelrc“的文件中，在执行时babel-loader将会自动加载.babelrc文件。</p>
<p>所以在很多例子里，你可能会看到：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//webpack.config.js </span></span><br><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line">  loaders: [</span><br><span class="line">    &#123;</span><br><span class="line">      test: <span class="regexp">/\.jsx?$/</span>,</span><br><span class="line">      exclude: <span class="regexp">/(node_modules|bower_components)/</span>,</span><br><span class="line">      loader: <span class="string">'babel'</span></span><br><span class="line">    &#125;</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//.bablerc</span></span><br><span class="line">&#123;</span><br><span class="line"> presets: [<span class="string">'react'</span>, <span class="string">'es2015'</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="8-插件"><a href="#8-插件" class="headerlink" title="8. 插件"></a>8. 插件</h2><p>插件一般都是用于输出bundle的node模块。</p>
<p>例如，<a href="https://webpack.github.io/docs/list-of-plugins.html#uglifyjsplugin" target="_blank" rel="noopener">uglifyJSPlugin</a>获取bundle.js然后压缩和混淆内容以减小文件体积。</p>
<p>类似的<a href="https://github.com/webpack/extract-text-webpack-plugin" target="_blank" rel="noopener">extract-text-webpack-plugin</a>内部使用css-loader和style-loader来收集所有的css到一个地方最终将结果提取结果到一个独立的”styles.css“文件，并且在html里边引用style.css文件。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//webpack.config.js</span></span><br><span class="line"><span class="comment">// 获取所有的.css文件，合并它们的内容然后提取css内容到一个独立的”styles.css“里</span></span><br><span class="line"><span class="keyword">var</span> ETP = <span class="built_in">require</span>(<span class="string">"extract-text-webpack-plugin"</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line"> loaders: [</span><br><span class="line">  &#123;<span class="attr">test</span>: <span class="regexp">/\.css$/</span>, <span class="attr">loader</span>:ETP.extract(<span class="string">"style-loader"</span>,<span class="string">"css-loader"</span>) &#125;</span><br><span class="line">  ],</span><br><span class="line">plugins: [</span><br><span class="line">    <span class="keyword">new</span> ExtractTextPlugin(<span class="string">"styles.css"</span>) <span class="comment">//Extract to styles.css file</span></span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意：如果你只是想把css使用style标签内联到html里，你不必使用extract-text-webpack-plugin，仅仅使用css loader和style loader即可：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line"> loaders: [&#123;</span><br><span class="line">  test: <span class="regexp">/\.css$/</span>,</span><br><span class="line">  loader: <span class="string">'style!css'</span> <span class="comment">// (short for style-loader!css-loader)</span></span><br><span class="line"> &#125;]</span><br></pre></td></tr></table></figure>

<h2 id="9-加载器（loader）和插件"><a href="#9-加载器（loader）和插件" class="headerlink" title="9. 加载器（loader）和插件"></a>9. 加载器（loader）和插件</h2><p>你可能已经意识到了，Loader处理单独的文件级别并且通常作用于包生成之前或生成的过程中。</p>
<p>而插件则是处理包（bundle）或者chunk级别，且通常是bundle生成的最后阶段。一些插件如<a href="https://webpack.github.io/docs/list-of-plugins.html#commonschunkplugin" target="_blank" rel="noopener">commonschunkplugin</a>甚至更直接修改bundle的生成方式。</p>
<h2 id="10-处理文件的扩展名"><a href="#10-处理文件的扩展名" class="headerlink" title="10. 处理文件的扩展名"></a>10. 处理文件的扩展名</h2><p>很多Webpack的配置文件都有一个<code>resolve</code>属性，然后就像下面代码所示有一个空字符串的值。空字符串在此是为了resolve一些在import文件时不带文件扩展名的表达式，如<code>require(&#39;./myJSFile&#39;)</code>或者<code>import myJSFile from &#39;./myJSFile&#39;</code>（译者注：实际就是自动添加后缀，默认是当成js文件来查找路径）</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"> resolve: &#123;</span><br><span class="line">   extensions: [<span class="string">''</span>, <span class="string">'.js'</span>, <span class="string">'.jsx'</span>]</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>前沿技术</category>
      </categories>
      <tags>
        <tag>ES6</tag>
        <tag>JavaScript</tag>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 Flex 实现 5 种常用布局</title>
    <url>/2017/12/14/article-css-flex-layout/</url>
    <content><![CDATA[<blockquote class="blockquote-center"><p>flex 实现 5 种常用布局，学习一下! </p>
</blockquote>

<blockquote>
<p>参考：<a href="https://github.com/meikidd/flex-layout" target="_blank" rel="noopener">https://github.com/meikidd/flex-layout</a></p>
</blockquote>
<a id="more"></a>


<h2 id="Sticky-Footer"><a href="#Sticky-Footer" class="headerlink" title="Sticky Footer"></a>Sticky Footer</h2><p>经典的上-中-下布局。</p>
<p>当页面内容高度小于可视区域高度时，footer 吸附在底部；当页面内容高度大于可视区域高度时，footer 被撑开排在 content 下方</p>
<p><img src="/styles/images/css/flex-layout/demo1.png" alt="demo 1 - Sticky Footer"></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">header</span>&gt;</span>HEADER<span class="tag">&lt;/<span class="name">header</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">article</span>&gt;</span>CONTENT<span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">footer</span>&gt;</span>FOOTER<span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">100vh</span>;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">  <span class="attribute">flex-direction</span>: column;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">article</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: auto;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Fixed-Width-Sidebar"><a href="#Fixed-Width-Sidebar" class="headerlink" title="Fixed-Width Sidebar"></a>Fixed-Width Sidebar</h2><p>在上-中-下布局的基础上，加了左侧定宽 sidebar。</p>
<p><img src="/styles/images/css/flex-layout/demo2.png" alt="demo 2 - Fixed-Width Sidebar"></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">header</span>&gt;</span>HEADER<span class="tag">&lt;/<span class="name">header</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">aside</span>&gt;</span>ASIDE<span class="tag">&lt;/<span class="name">aside</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">article</span>&gt;</span>CONTENT<span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">footer</span>&gt;</span>FOOTER<span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">100vh</span>;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">  <span class="attribute">flex-direction</span>: column;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.content</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: auto;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.content</span> <span class="selector-tag">article</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: auto;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Sidebar"><a href="#Sidebar" class="headerlink" title="Sidebar"></a>Sidebar</h2><p>左边是定宽 sidebar，右边是上-中-下布局。</p>
<p><img src="/styles/images/css/flex-layout/demo3.png" alt="demo 3"></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">aside</span>&gt;</span>ASIDE<span class="tag">&lt;/<span class="name">aside</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">header</span>&gt;</span>HEADER<span class="tag">&lt;/<span class="name">header</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">article</span>&gt;</span>CONTENT<span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">footer</span>&gt;</span>FOOTER<span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">100vh</span>;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">aside</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: none;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.content</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: auto;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">  <span class="attribute">flex-direction</span>: column;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.content</span> <span class="selector-tag">article</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: auto;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Sticky-Header"><a href="#Sticky-Header" class="headerlink" title="Sticky Header"></a>Sticky Header</h2><p>还是上-中-下布局，区别是 header 固定在顶部，不会随着页面滚动。</p>
<p><img src="/styles/images/css/flex-layout/demo4.png" alt="demo 4 - Sticky Header"></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">header</span>&gt;</span>HEADER<span class="tag">&lt;/<span class="name">header</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">article</span>&gt;</span>CONTENT<span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">footer</span>&gt;</span>FOOTER<span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">100vh</span>;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">  <span class="attribute">flex-direction</span>: column;</span><br><span class="line">  <span class="attribute">padding-top</span>: <span class="number">60px</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">header</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">60px</span>;</span><br><span class="line">  <span class="attribute">position</span>: fixed;</span><br><span class="line">  <span class="attribute">top</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">left</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">right</span>: <span class="number">0</span>;</span><br><span class="line">  <span class="attribute">padding</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">article</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: auto;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">1000px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Sticky-Sidebar"><a href="#Sticky-Sidebar" class="headerlink" title="Sticky Sidebar"></a>Sticky Sidebar</h2><p>左侧 sidebar 固定在左侧且与视窗同高，当内容超出视窗高度时，在 sidebar 内部出现滚动条。左右两侧滚动条互相独立。</p>
<p><img src="/styles/images/css/flex-layout/demo5.png" alt="demo 5 - Sticky Sidebar"></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">aside</span>&gt;</span></span><br><span class="line">    ASIDE</span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span>&gt;</span>item<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span>&gt;</span>item<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- many items --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span>&gt;</span>item<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">aside</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">header</span>&gt;</span>HEADER<span class="tag">&lt;/<span class="name">header</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">article</span>&gt;</span>CONTENT<span class="tag">&lt;/<span class="name">article</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">footer</span>&gt;</span>FOOTER<span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">100vh</span>;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">aside</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: none;</span><br><span class="line">  <span class="attribute">width</span>: <span class="number">200px</span>;</span><br><span class="line">  <span class="attribute">overflow-y</span>: auto;</span><br><span class="line">  <span class="attribute">display</span>: block;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.content</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: auto;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">  <span class="attribute">flex-direction</span>: column;</span><br><span class="line">  <span class="attribute">overflow-y</span>: auto;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.content</span> <span class="selector-tag">article</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: auto;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>CSS</tag>
        <tag>Flex</tag>
      </tags>
  </entry>
  <entry>
    <title>css-alt</title>
    <url>/2017/11/14/daily-css-alt/</url>
    <content><![CDATA[<p>为了增强可访问性，让自己不忘给图片标签加上 alt 属性，有人在项目中加入了如下的全局 CSS 样式，值得学习</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">img</span><span class="selector-attr">[alt=""]</span>,<span class="selector-tag">img</span><span class="selector-pseudo">:not(</span><span class="selector-attr">[alt]</span>)&#123;</span><br><span class="line">    <span class="attribute">border</span>: <span class="number">2px</span> solid <span class="number">#c00</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>知识点</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript Promise启示录</title>
    <url>/2017/11/06/article-js-promise/</url>
    <content><![CDATA[<p>本篇，主要普及promise的用法。</p>
<p>一直以来，JavaScript处理异步都是以callback的方式，在前端开发领域callback机制几乎深入人心。在设计API的时候，不管是浏览器厂商还是SDK开发商亦或是各种类库的作者，基本上都已经遵循着callback的套路。</p>
<p>近几年随着JavaScript开发模式的逐渐成熟，CommonJS规范顺势而生，其中就包括提出了Promise规范，Promise完全改变了js异步编程的写法，让异步编程变得十分的易于理解。</p>
<a id="more"></a>

<p>在callback的模型里边，我们假设需要执行一个异步队列，代码看起来可能像这样：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">loadImg(<span class="string">'a.jpg'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">	loadImg(<span class="string">'b.jpg'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    	loadImg(<span class="string">'c.jpg'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        	<span class="built_in">console</span>.log(<span class="string">'all done!'</span>);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>这也就是我们常说的回调金字塔，当异步的任务很多的时候，维护大量的callback将是一场灾难。当今Node.js大热，好像很多团队都要用它来做点东西以沾沾“洋气”，曾经跟一个运维的同学聊天，他们也是打算使用Node.js做一些事情，可是一想到js的层层回调就望而却步。</p>
<p>好，扯淡完毕，下面进入正题。</p>
<p>Promise可能大家都不陌生，因为Promise规范已经出来好一段时间了，同时Promise也已经纳入了ES6，而且高版本的chrome、firefox浏览器都已经原生实现了Promise，只不过和现如今流行的类Promise类库相比少些API。</p>
<p>所谓Promise，字面上可以理解为“承诺”，就是说A调用B，B返回一个“承诺”给A，然后A就可以在写计划的时候这么写：当B返回结果给我的时候，A执行方案S1，反之如果B因为什么原因没有给到A想要的结果，那么A执行应急方案S2，这样一来，所有的潜在风险都在A的可控范围之内了。</p>
<p>上面这句话，翻译成代码类似：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> resB = B();</span><br><span class="line"><span class="keyword">var</span> runA = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">	resB.then(execS1, execS2);</span><br><span class="line">&#125;;</span><br><span class="line">runA();</span><br></pre></td></tr></table></figure>

<p>只看上面这行代码，好像看不出什么特别之处。但现实情况可能比这个复杂许多，A要完成一件事，可能要依赖不止B一个人的响应，可能需要同时向多个人询问，当收到所有的应答之后再执行下一步的方案。最终翻译成代码可能像这样：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> resB = B();</span><br><span class="line"><span class="keyword">var</span> resC = C();</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> runA = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    reqB</span><br><span class="line">        .then(resC, execS2)</span><br><span class="line">        .then(resD, execS3)</span><br><span class="line">        .then(resE, execS4)</span><br><span class="line">        ...</span><br><span class="line">        .then(execS1);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">runA();</span><br></pre></td></tr></table></figure>

<p>在这里，当每一个被询问者做出不符合预期的应答时都用了不同的处理机制。事实上，Promise规范没有要求这样做，你甚至可以不做任何的处理（即不传入then的第二个参数）或者统一处理。</p>
<p>好了，下面我们来认识下<a href="http://promises-aplus.github.io/promises-spec/" target="_blank" rel="noopener">Promise/A+规范</a>：</p>
<ul>
<li><p>一个promise可能有三种状态：等待（pending）、已完成（fulfilled）、已拒绝（rejected）</p>
</li>
<li><p>一个promise的状态只可能从“等待”转到“完成”态或者“拒绝”态，不能逆向转换，同时“完成”态和“拒绝”态不能相互转换</p>
</li>
<li><p>promise必须实现<code>then</code>方法（可以说，then就是promise的核心），而且then必须返回一个promise，同一个promise的then可以调用多次，并且回调的执行顺序跟它们被定义时的顺序一致</p>
</li>
<li><p>then方法接受两个参数，第一个参数是成功时的回调，在promise由“等待”态转换到“完成”态时调用，另一个是失败时的回调，在promise由“等待”态转换到“拒绝”态时调用。同时，then可以接受另一个promise传入，也接受一个“类then”的对象或方法，即thenable对象。</p>
</li>
</ul>
<p>可以看到，Promise规范的内容并不算多，大家可以试着自己实现以下Promise。</p>
<p>以下是笔者自己在参考许多类Promise库之后简单实现的一个Promise，代码请移步<a href="https://github.com/chemdemo/promiseA/blob/master/lib/Promise.js" target="_blank" rel="noopener">promiseA</a>。</p>
<p>简单分析下思路：</p>
<p>构造函数Promise接受一个函数<code>resolver</code>，可以理解为传入一个异步任务，resolver接受两个参数，一个是成功时的回调，一个是失败时的回调，这两参数和通过then传入的参数是对等的。</p>
<p>其次是then的实现，由于Promise要求then必须返回一个promise，所以在then调用的时候会新生成一个promise，挂在当前promise的<code>_next</code>上，同一个promise多次调用都只会返回之前生成的<code>_next</code>。</p>
<p>由于then方法接受的两个参数都是可选的，而且类型也没限制，可以是函数，也可以是一个具体的值，还可以是另一个promise。下面是then的具体实现：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Promise</span>.prototype.then = <span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> next = <span class="keyword">this</span>._next || (<span class="keyword">this</span>._next = <span class="built_in">Promise</span>());</span><br><span class="line">    <span class="keyword">var</span> status = <span class="keyword">this</span>.status;</span><br><span class="line">    <span class="keyword">var</span> x;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(<span class="string">'pending'</span> === status) &#123;</span><br><span class="line">        isFn(resolve) &amp;&amp; <span class="keyword">this</span>._resolves.push(resolve);</span><br><span class="line">        isFn(reject) &amp;&amp; <span class="keyword">this</span>._rejects.push(reject);</span><br><span class="line">        <span class="keyword">return</span> next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(<span class="string">'resolved'</span> === status) &#123;</span><br><span class="line">        <span class="keyword">if</span>(!isFn(resolve)) &#123;</span><br><span class="line">            next.resolve(resolve);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                x = resolve(<span class="keyword">this</span>.value);</span><br><span class="line">                resolveX(next, x);</span><br><span class="line">            &#125; <span class="keyword">catch</span>(e) &#123;</span><br><span class="line">                <span class="keyword">this</span>.reject(e);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(<span class="string">'rejected'</span> === status) &#123;</span><br><span class="line">        <span class="keyword">if</span>(!isFn(reject)) &#123;</span><br><span class="line">            next.reject(reject);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                x = reject(<span class="keyword">this</span>.reason);</span><br><span class="line">                resolveX(next, x);</span><br><span class="line">            &#125; <span class="keyword">catch</span>(e) &#123;</span><br><span class="line">                <span class="keyword">this</span>.reject(e);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> next;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这里，then做了简化，其他promise类库的实现比这个要复杂得多，同时功能也更多，比如还有第三个参数——notify，表示promise当前的进度，这在设计文件上传等时很有用。对then的各种参数的处理是最复杂的部分，有兴趣的同学可以参看其他类Promise库的实现。</p>
<p>在then的基础上，应该还需要至少两个方法，分别是完成promise的状态从pending到resolved或rejected的转换，同时执行相应的回调队列，即<code>resolve()</code>和<code>reject()</code>方法。</p>
<p>到此，一个简单的promise就设计完成了，下面简单实现下两个promise化的函数：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">sleep</span>(<span class="params">ms</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">v</span>) </span>&#123;</span><br><span class="line">    	<span class="keyword">var</span> p = <span class="built_in">Promise</span>();</span><br><span class="line"></span><br><span class="line">        setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        	p.resolve(v);</span><br><span class="line">        &#125;, ms);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> p;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getImg</span>(<span class="params">url</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">var</span> p = <span class="built_in">Promise</span>();</span><br><span class="line">    <span class="keyword">var</span> img = <span class="keyword">new</span> Image();</span><br><span class="line"></span><br><span class="line">    img.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    	p.resolve(<span class="keyword">this</span>);</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    img.onerror = <span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>&#123;</span><br><span class="line">    	p.reject(err);</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    img.url = url;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>由于Promise构造函数接受一个异步任务作为参数，所以<code>getImg</code>还可以这样调用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getImg</span>(<span class="params">url</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">    	<span class="keyword">var</span> img = <span class="keyword">new</span> Image();</span><br><span class="line"></span><br><span class="line">        img.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">            resolve(<span class="keyword">this</span>);</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        img.onerror = <span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>&#123;</span><br><span class="line">            reject(err);</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        img.url = url;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>接下来（见证奇迹的时刻），假设有一个BT的需求要这么实现：异步获取一个<a href="http://chemdemo.github.io/demos/promise/map.json" target="_blank" rel="noopener">json配置</a>，解析json数据拿到里边的图片，然后按顺序队列加载图片，没张图片加载时给个loading效果</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">addImg</span>(<span class="params">img</span>) </span>&#123;</span><br><span class="line">    $(<span class="string">'#list'</span>).find(<span class="string">'&gt; li:last-child'</span>).html(<span class="string">''</span>).append(img);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">prepend</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    $(<span class="string">'&lt;li&gt;'</span>)</span><br><span class="line">        .html(<span class="string">'loading...'</span>)</span><br><span class="line">        .appendTo($(<span class="string">'#list'</span>));</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    $(<span class="string">'#done'</span>).hide();</span><br><span class="line">    getData(<span class="string">'map.json'</span>)</span><br><span class="line">        .then(<span class="function"><span class="keyword">function</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">            $(<span class="string">'h4'</span>).html(data.name);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> data.list.reduce(<span class="function"><span class="keyword">function</span>(<span class="params">promise, item</span>) </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> promise</span><br><span class="line">                    .then(prepend)</span><br><span class="line">                    .then(sleep(<span class="number">1000</span>))</span><br><span class="line">                    .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                        <span class="keyword">return</span> getImg(item.url);</span><br><span class="line">                    &#125;)</span><br><span class="line">                    .then(addImg);</span><br><span class="line">            &#125;, <span class="built_in">Promise</span>.resolve());</span><br><span class="line">        &#125;)</span><br><span class="line">        .then(sleep(<span class="number">300</span>))</span><br><span class="line">        .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">            $(<span class="string">'#done'</span>).show();</span><br><span class="line">        &#125;);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">$(<span class="string">'#run'</span>).on(<span class="string">'click'</span>, run);</span><br></pre></td></tr></table></figure>

<p>这里的sleep只是为了看效果加的，可猛击查看<a href="http://chemdemo.github.io/demos/promise/browser.html" target="_blank" rel="noopener">demo</a>！当然，Node.js的例子可查看<a href="https://github.com/chemdemo/promiseA/blob/master/test/nodejs.js" target="_blank" rel="noopener">这里</a>。</p>
<p>在这里，<code>Promise.resolve(v)</code>静态方法只是简单返回一个以v为肯定结果的promise，v可不传入，也可以是一个函数或者是一个包含<code>then</code>方法的对象或函数（即thenable）。</p>
<p>类似的静态方法还有<code>Promise.cast(promise)</code>，生成一个以promise为肯定结果的promise；</p>
<p><code>Promise.reject(reason)</code>，生成一个以reason为否定结果的promise。</p>
<p>我们实际的使用场景可能很复杂，往往需要多个异步的任务穿插执行，并行或者串行同在。这时候，可以对Promise进行各种扩展，比如实现<code>Promise.all()</code>，接受promises队列并等待他们完成再继续，再比如<code>Promise.any()</code>，promises队列中有任何一个处于完成态时即触发下一步操作。</p>
<h3 id="标准的Promise"><a href="#标准的Promise" class="headerlink" title="标准的Promise"></a>标准的Promise</h3><p>可参考html5rocks的这篇文章<a href="http://www.html5rocks.com/en/tutorials/es6/promises/" target="_blank" rel="noopener">JavaScript Promises</a>，目前高级浏览器如chrome、firefox都已经内置了Promise对象，提供更多的操作接口，比如<code>Promise.all()</code>，支持传入一个promises数组，当所有promises都完成时执行then，还有就是更加友好强大的异常捕获，应对日常的异步编程，应该足够了。</p>
<h3 id="第三方库的Promise"><a href="#第三方库的Promise" class="headerlink" title="第三方库的Promise"></a>第三方库的Promise</h3><p>现今流行的各大js库，几乎都不同程度的实现了Promise，如dojo，jQuery、Zepto、when.js、Q等，只是暴露出来的大都是<code>Deferred</code>对象，以jQuery（Zepto类似）为例，实现上面的<code>getImg()</code>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getImg</span>(<span class="params">url</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> def = $.Deferred();</span><br><span class="line">    <span class="keyword">var</span> img = <span class="keyword">new</span> Image();</span><br><span class="line"></span><br><span class="line">    img.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        def.resolve(<span class="keyword">this</span>);</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    img.onerror = <span class="function"><span class="keyword">function</span>(<span class="params">err</span>) </span>&#123;</span><br><span class="line">        def.reject(err);</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    img.src = url;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> def.promise();</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>当然，jQuery中，很多的操作都返回的是Deferred或promise，如<code>animate</code>、<code>ajax</code>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// animate</span></span><br><span class="line">$(<span class="string">'.box'</span>)</span><br><span class="line">    .animate(&#123;<span class="string">'opacity'</span>: <span class="number">0</span>&#125;, <span class="number">1000</span>)</span><br><span class="line">    .promise()</span><br><span class="line">    .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'done'</span>);</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// ajax</span></span><br><span class="line">$.ajax(options).then(success, fail);</span><br><span class="line">$.ajax(options).done(success).fail(fail);</span><br><span class="line"></span><br><span class="line"><span class="comment">// ajax queue</span></span><br><span class="line">$.when($.ajax(options1), $.ajax(options2))</span><br><span class="line">    .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'all done.'</span>);</span><br><span class="line">    &#125;, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.error(<span class="string">'There something wrong.'</span>);</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>

<p>jQuery还实现了<code>done()</code>和<code>fail()</code>方法，其实都是then方法的shortcut。</p>
<p>处理promises队列，jQuery实现的是<code>$.when()</code>方法，用法和<code>Promise.all()</code>类似。</p>
<p>其他类库，这里值得一提的是<a href="https://github.com/cujojs/when" target="_blank" rel="noopener">when.js</a>，本身代码不多，完整实现Promise，同时支持browser和Node.js，而且提供更加丰富的API，是个不错的选择。这里限于篇幅，不再展开。</p>
<h3 id="尾声"><a href="#尾声" class="headerlink" title="尾声"></a>尾声</h3><p>我们看到，不管Promise实现怎么复杂，但是它的用法却很简单，组织的代码很清晰，从此不用再受callback的折磨了。</p>
<p>最后，Promise是如此的优雅！但Promise也只是解决了回调的深层嵌套的问题，真正简化JavaScript异步编程的还是Generator，在Node.js端，建议考虑Generator。</p>
]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>ES6</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS 五种方式实现 Footer 置底</title>
    <url>/2017/10/04/article-css-footer/</url>
    <content><![CDATA[<p>页脚置底（Sticky footer）就是让网页的footer部分始终在浏览器窗口的底部。</p>
<p>当网页内容足够长以至超出浏览器可视高度时，页脚会随着内容被推到网页底部；但如果网页内容不够长，置底的页脚就会保持在浏览器窗口底部。</p>
<img src="/styles/images/css/css-footer_1.png">

<blockquote>
<p>原文链接<a href="http://t.cn/RJ3nmhV" target="_blank" rel="noopener"> http://t.cn/RJ3nmhV </a>  </p>
</blockquote>
<a id="more"></a>


<h2 id="1-将内容部分的底部外边距设为负数"><a href="#1-将内容部分的底部外边距设为负数" class="headerlink" title="1. 将内容部分的底部外边距设为负数"></a>1. 将内容部分的底部外边距设为负数</h2><p>这是个比较主流的用法，把内容部分最小高度设为100%，再利用内容部分的负底部外边距值来达到当高度不满时，页脚保持在窗口底部，当高度超出则随之推出的效果。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"wrapper"</span>&gt;</span></span><br><span class="line">  </span><br><span class="line">      content</span><br><span class="line">      </span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"push"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">footer</span> <span class="attr">class</span>=<span class="string">"footer"</span>&gt;</span><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">html</span>, <span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.wrapper</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">100%</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/* 等于footer的高度 */</span></span><br><span class="line">  <span class="attribute">margin-bottom</span>: -<span class="number">50px</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.footer</span>,</span><br><span class="line"><span class="selector-class">.push</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">50px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>需要注意的是.wrapper的margin-bottom值需要和.footer的负的height值保持一致，这一点不太友好。</p>
<h2 id="2-将页脚的顶部外边距设为负数"><a href="#2-将页脚的顶部外边距设为负数" class="headerlink" title="2. 将页脚的顶部外边距设为负数"></a>2. 将页脚的顶部外边距设为负数</h2><p> 既然能在容器上使用负的margin bottom，那能否使用负margin top吗？当然可以。</p>
<p> 给内容外增加父元素，并让内容部分的底部内边距与页脚高度的值相等。</p>
 <figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content-inside"</span>&gt;</span></span><br><span class="line">      content</span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">footer</span> <span class="attr">class</span>=<span class="string">"footer"</span>&gt;</span><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">html</span>, <span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.content</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">100%</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.content-inside</span> &#123;</span><br><span class="line">  <span class="attribute">padding</span>: <span class="number">20px</span>;</span><br><span class="line">  <span class="attribute">padding-bottom</span>: <span class="number">50px</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.footer</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">50px</span>;</span><br><span class="line">  <span class="attribute">margin-top</span>: -<span class="number">50px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>不过这种方法和上一种一样，都需要额外添加不必要的html元素。</p>
<h2 id="3-使用calc-设置内容高度"><a href="#3-使用calc-设置内容高度" class="headerlink" title="3. 使用calc()设置内容高度"></a>3. 使用calc()设置内容高度</h2><p>有一种方法不需要任何多余元素——使用CSS3新增的计算函数calc()</p>
<p>这样元素间就不会有重叠发生，也不需要控制内外边距了～</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span></span><br><span class="line">    content</span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">footer</span> <span class="attr">class</span>=<span class="string">"footer"</span>&gt;</span><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.content</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="built_in">calc</span>(100vh - 70px);</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.footer</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">50px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可能你会疑惑内容高度calc()中为什么减去70px，而不是footer的高度50px，因为假设俩元素有20px的间距，所以70px=50px+20px</p>
<p>不过，你不必在意这些~</p>
<h2 id="4-使用flexbox弹性盒布局"><a href="#4-使用flexbox弹性盒布局" class="headerlink" title="4. 使用flexbox弹性盒布局"></a>4. 使用flexbox弹性盒布局</h2><p>以上三种方法的footer高度都是固定的，通常来说这不利于网页布局：内容会改变，它们都是弹性的，一旦内容超出固定高度就会破坏布局。所以给footer使用flexbox吧，让它的高度可以变大变小变漂亮~</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span></span><br><span class="line">    content</span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">footer</span> <span class="attr">class</span>=<span class="string">"footer"</span>&gt;</span><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">html</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">100%</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">display</span>: flex;</span><br><span class="line">  <span class="attribute">flex-direction</span>: column;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.content</span> &#123;</span><br><span class="line">  <span class="attribute">flex</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>你还可以在上面添加header或在下面添加更多元素。可从以下技巧选择其一：</p>
<ul>
<li><p><code>flex:1</code>使内容（如：.content）高度可以自动伸缩</p>
</li>
<li><p><code>margin-top: auto</code></p>
</li>
</ul>
<p>可参考<a href="/2017/01/15/article-css-flex/">Flex布局新旧混合写法详解</a></p>
<h2 id="5-使用Grid网格布局"><a href="#5-使用Grid网格布局" class="headerlink" title="5. 使用Grid网格布局"></a>5. 使用Grid网格布局</h2><p>grid比flexbox还要新很多，并且更佳很简洁，可参考<a href="http://ghmagical.com/article/page/id/eAhyFiDCFGb6" target="_blank" rel="noopener">CSS：Grid布局</a></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span></span><br><span class="line">    content</span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">footer</span> <span class="attr">class</span>=<span class="string">"footer"</span>&gt;</span><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">html</span> &#123;</span><br><span class="line">  <span class="attribute">height</span>: <span class="number">100%</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">  <span class="attribute">min-height</span>: <span class="number">100%</span>;</span><br><span class="line">  <span class="attribute">display</span>: grid;</span><br><span class="line">  <span class="attribute">grid-template-rows</span>: <span class="number">1</span>fr auto;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.footer</span> &#123;</span><br><span class="line">  <span class="attribute">grid-row-start</span>: <span class="number">2</span>;</span><br><span class="line">  <span class="attribute">grid-row-end</span>: <span class="number">3</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>遗憾的是，网格布局（Grid layout）目前仅支持Chrome Canary和Firefox Developer Edition版本。</p>
]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>微信公众号文章编辑</title>
    <url>/2017/08/08/article-wechat-edit/</url>
    <content><![CDATA[<blockquote class="blockquote-center"><p>微信公众号发布内容过程小白教程，大神请无视！</p>
</blockquote>


<span itemprop="image" itemscope itemtype="http://schema.org/ImageObject"><img itemprop="url image" src="/styles/banner/wechat-edit.jpg" class="full-image" alt="微信公众号" title="公众号文章编辑"><meta itemprop="width" content="auto"><meta itemprop="height" content="auto"></span>

<a id="more"></a>

<h2 id="注册微信订阅号"><a href="#注册微信订阅号" class="headerlink" title="注册微信订阅号"></a>注册微信订阅号</h2><blockquote>
<p>注册地址：<br><a href="https://mp.weixin.qq.com/cgi-bin/registermidpage?action=index&amp;lang=zh_CN" target="_blank" rel="noopener">https://mp.weixin.qq.com/cgi-bin/registermidpage?action=index&amp;lang=zh_CN</a></p>
</blockquote>
<p><img src="/styles/images/wechat/step_1.png" alt="step—1"></p>
<blockquote>
<p>根据需求，个人用户最好的是选择第一个 <strong>订阅号</strong> ；然后根据提示一步步的填写注册需要的信息，注意选择类型这块选择 <strong>订阅号</strong> ，选择之后是不可更改的。</p>
</blockquote>
<p><img src="/styles/images/wechat/step_2.png" alt="step—1"></p>
<blockquote>
<p>信息登记的主题类型选择 <strong>个人</strong>；</p>
</blockquote>
<p><img src="/styles/images/wechat/step_3.png" alt="step—1"></p>
<p>选择个人后继续填写相关的登记信息，有一步是需要身份验证的，验证用的微信号将会是你以后登录管理该公众号的公众号管理员。</p>
<blockquote>
<p>最后一步，填写公众号信息，包括公众号的名称和介绍。</p>
</blockquote>
<p><img src="/styles/images/wechat/step_4.png" alt="step—1"></p>
<blockquote>
<p>信息提交成功后，前往 <a href="https://mp.weixin.qq.com/cgi-bin/home" target="_blank" rel="noopener">https://mp.weixin.qq.com/cgi-bin/home</a>  进行公众号编辑</p>
</blockquote>
<h2 id="公众号基础设置"><a href="#公众号基础设置" class="headerlink" title="公众号基础设置"></a>公众号基础设置</h2><p><img src="/styles/images/wechat/step_5.png" alt="step—1"></p>
<blockquote>
<p>一图胜千言</p>
</blockquote>
<h2 id="文章编辑"><a href="#文章编辑" class="headerlink" title="文章编辑"></a>文章编辑</h2><p><img src="/styles/images/wechat/step_6.png" alt="step—1"></p>
<p>在素材管理中心新建图文素材，所谓的图文素材就是你将来要发布在公众号中文章可以用到的已经写好的内容。</p>
<p><img src="/styles/images/wechat/step_7.png" alt="step—1"></p>
<blockquote>
<p>在首页中选择新建群发后</p>
</blockquote>
<p><img src="/styles/images/wechat/step_8.png" alt="step—1"></p>
<p>这里新建的文章只能进行简单的排版，写出效果平平的文章，若要写出排版比较优雅的文章，网上有好多工具。</p>
<p>这里我罗列一些常用的编辑器（功能都是大同小异）</p>
<div class="note info"><p>135<br><a href="http://www.135editor.com/" target="_blank" rel="noopener">http://www.135editor.com/</a></p></div>


<div class="note info"><p>i排版<br><a href="https://ipaiban.com/" target="_blank" rel="noopener">https://ipaiban.com/</a></p></div>


<div class="note info"><p>易点<br><a href="http://www.wxeditor.com/" target="_blank" rel="noopener">http://www.wxeditor.com/</a></p></div>


<div class="note info"><p>秀米<br><a href="https://xiumi.us/" target="_blank" rel="noopener">https://xiumi.us/</a></p></div>


<div class="note info"><p>小蚂蚁<br><a href="http://www.xmyeditor.com/" target="_blank" rel="noopener">http://www.xmyeditor.com/</a></p></div>


<div class="note info"><p>非找你<br><a href="http://editor.fzn.cc/" target="_blank" rel="noopener">http://editor.fzn.cc/</a></p></div>


<div class="note info"><p>96<br><a href="http://bj.96weixin.com/" target="_blank" rel="noopener">http://bj.96weixin.com/</a></p></div>

<p>可参考 <a href="https://www.zhihu.com/question/30770510" target="_blank" rel="noopener">这里</a></p>
<hr>]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>Web</tag>
        <tag>WeChat</tag>
      </tags>
  </entry>
  <entry>
    <title>掌握Chrome开发工具</title>
    <url>/2017/07/31/article-chrome-tools/</url>
    <content><![CDATA[<p>你可能已经熟悉了Chrome开发工具的基本功能。: DOM检查器、样式面板和JavaScript控制台。 但也有一些不太为人所知的特性可以显著提高你调试或开发应用的速度。<a id="more"></a></p>
<img src="/styles/images/chrome/tools.png">


<h3 id="黑色主题"><a href="#黑色主题" class="headerlink" title="黑色主题"></a>黑色主题</h3><img src="/styles/images/chrome/black.png">

<p>Chrome开发工具的内置了黑色主题。你可以通过点击开发工具窗口右上角的三点图标，之后点击进入设置页面，切换主题。</p>
<p>有时候我觉得黑色主题让我的眼睛更舒服,并且黑色主题显然看起来更酷一些！</p>
<h3 id="选择模式"><a href="#选择模式" class="headerlink" title="选择模式"></a>选择模式</h3><img src="/styles/images/chrome/modul.png">

<p>Chrome开发者工具提供了很多选择元素的方法，其中最快捷的方法就是使用选择模式。</p>
<p>该功能通过点击调试面板左上角的按钮开启(或者通过组合键 ctrl + shift + c), 该模式下你只需单击页面上的元素就可以选中它。</p>
<p>一旦开启该模式，你可以将鼠标移动到页面来预览选定内容，然后单击来选择要检查的元素。</p>
<p>通过ctrl + shitf + c键，你可以直接打开调试工具并开启调试模式，来在页面上快速选择一个元素。</p>
<h3 id="存储为全局变量"><a href="#存储为全局变量" class="headerlink" title="存储为全局变量"></a>存储为全局变量</h3><img src="/styles/images/chrome/global.png">

<p>有时在控制台中查看一个复杂对象是一件很麻烦的事，因为他们可能有很多的键值或者一些很难手动解析的值。幸运的是，Chrome可以让检查这类JavaScript对象变得很容易。 你只需在控制台中右键点击对象后选择“存储为全局变量”，chrome就会将这个变量存储为一个名叫“temp1”的变量，之后你可以通过Javascript对其进行操作。</p>
<h3 id="动画工具"><a href="#动画工具" class="headerlink" title="动画工具"></a>动画工具</h3><img src="/styles/images/chrome/animation.png">

<p>最近，Chrome团队为调试和创建动画添加了一些新特性。</p>
<p>单击控制台左上角的下拉框中的“动画”开启动画调试工具，你可以通过它限制站点上所有动画的速度。</p>
<p>你也可以暂停所有动画。这对于一个充斥着动画内容的站点尤其有用。</p>
<img src="/styles/images/chrome/animation_1.png">

<p>动画查看器允许你单独控制每个属性的时间曲线！</p>
<img src="/styles/images/chrome/saibeier.png">

<p>通过点击一个元素 <code>transition</code> 属性中的紫色曲线图表按钮，你可以看到动画的移动曲线，并且微调他的属性。此外，你还可以使用一些预置的时间曲线来应用到你的元素上。</p>
<h3 id="模拟元素伪态"><a href="#模拟元素伪态" class="headerlink" title="模拟元素伪态"></a>模拟元素伪态</h3><img src="/styles/images/chrome/hover.png">

<p>通过点击样式窗口右上角的<code>:hover</code> 图标，你可以打开元素状态模拟工具。</p>
<p>该工具可以让你模拟一个元素的hover,active,focused和visited伪态，并且看到不同伪态的相关样式与选择符。</p>
<img src="/styles/images/chrome/hover_1.png">

<p>如果要为这些伪态添加样式，可以添加一个新的选择器（使用“+”图标），并将:添加到选择器的结尾。</p>
<p>例如，如果我想要给一个logo类的li标签添加hover伪态样式，我需要构造一个新的伪类<code>li.logo:hover</code>，并且给他添加相关属性。</p>
<p>之后可以通过模拟<code>hover</code>状态来检查你的样式是否正确。</p>
<h3 id="美化CSS和JavaScript"><a href="#美化CSS和JavaScript" class="headerlink" title="美化CSS和JavaScript"></a>美化CSS和JavaScript</h3><img src="/styles/images/chrome/good.png">

<p>调试、浏览压缩后的JavaScript和CSS是一件非常困难的事情，好在调试工具让这件事情变得容易了一些。</p>
<p>在“Source”的标签栏打开了一个压缩的文档后，你可以点击左下角的花括号logo，之后调试工具就会将代码进行格式化处理。</p>
<p>尽管在压缩过程中丢失了一些信息（例如变量名）,该工具对调试CSS和JavaScript文件还是很有用的。</p>
<h3 id="Alt-Up-Alt-Down"><a href="#Alt-Up-Alt-Down" class="headerlink" title="Alt + Up / Alt + Down"></a>Alt + Up / Alt + Down</h3><img src="/styles/images/chrome/alt.png">

<p>在调试CSS时，你可以选择一个属性然后使用上下箭头来调整它的值。默认情况下，上下箭头会将值加减1。但如果你按住了<code>alt</code>键，再通过上下箭头调整值时候，每次增减的值就会变成0.1，这在处理一些浮点类型的数值属性时非常有用。</p>
<p>相反的，你可以按住<code>shift</code>键一次将数值加减10;</p>
<h3 id="保存日志"><a href="#保存日志" class="headerlink" title="保存日志"></a>保存日志</h3><img src="/styles/images/chrome/save.png">

<p>保存日志是一个复选框，它允许在页面刷新后仍然保存日志。这在调试需要刷新页面的网站问题时非常有用，因为默认情况下，所有控制台输出的信息在页面刷新后都会被清除。</p>
<p>启用此选项后，控制台中会出现一个新的“导航”日志，它指向了刷新或是导航到的页面。</p>
<h3 id="网络-日志过滤"><a href="#网络-日志过滤" class="headerlink" title="网络 + 日志过滤"></a>网络 + 日志过滤</h3><img src="/styles/images/chrome/filter.png">

<p>当调试具有大量网络请求或控制台日志的应用程序时，过滤特定类型的事件是很有用的。</p>
<p>Chrome有一种支持多种属性的过滤语言，以及类似于*的通配符。</p>
<p>如果你输入了“-”，Chrome会出现一个包含了可选过滤选项的提示框。你也可以打开“正则模式”来对每一行的数据进行正则匹配。</p>
<h3 id="代码覆盖率"><a href="#代码覆盖率" class="headerlink" title="代码覆盖率"></a>代码覆盖率</h3><img src="/styles/images/chrome/cover.png">

<p>代码覆盖率可以在运行Web应用程序后针对每个JavaScript和CSS文件，查看哪些代码行运行了，哪些代码没有运行。这是很有用的，因为在处理复杂或长期项目时，很容易在项目中累积无用的代码。</p>
<p>如果想要使用这个功能的话，首先升级Chrome到59或更高版本，之后切换到“Coverage”页面。点击“record”，之后开始使用你的Web应用。当你使用结束后，Chrome将向你显示操作期间运行的具体代码。</p>
<h3 id="调试用户遇到的问题"><a href="#调试用户遇到的问题" class="headerlink" title="调试用户遇到的问题"></a>调试用户遇到的问题</h3><p>通常来说调试工具只有在你自己的机器上才会生效。但如果你有兴趣了解用户在使用过程中遇到的的bug和性能问题，不妨尝试一下 <a href="https://logrocket.com/" target="_blank" rel="noopener">LogRocket</a>。</p>
<img src="/styles/images/chrome/LogRocket.png">

<p><code>LogRocket</code> 是一个前端日志记录工具，它可以让你重放问题，就像它们发生在你自己的浏览器中一样。 不是通过猜测错误发生的原因，或要求用户提供截图或者错误日志，<code>logrocket</code>可以让你通过重放快速了解到问题出在哪里。它的兼容性很好，无论在任何框架下都可以使用，并且<code>LogRocket</code>对于React、Angular、和Vue的提供了额外的日志插件。</p>
<p>LogRocket 会记录你应用程序中的日志信息、带有header和body的网络请求、浏览器的元数据、Redux的行为与状态、以及应用进行时间与性能。并且他还能记录页面的HTML和CSS样式，完美的还原页面。</p>
<blockquote>
<p><strong>LogRocket | JavaScript应用的日志记录和会话回放 _LogRocket帮助你理解那些影响用户的问题, 这样你就可以重新构建更好的的软件</strong>。</p>
</blockquote>
<blockquote>
<p>本文转载自：<a href="http://www.zcfy.cc" target="_blank" rel="noopener">众成翻译</a></p>
</blockquote>
<blockquote>
<p>译者：<a href="http://www.zcfy.cc/@mirlysky" target="_blank" rel="noopener">一只叫做兰胖的食人魔魔法师</a></p>
</blockquote>
<blockquote>
<p>链接：<a href="http://www.zcfy.cc/article/3722" target="_blank" rel="noopener">http://www.zcfy.cc/article/3722</a></p>
</blockquote>
<blockquote>
<p>原文：<a href="https://medium.freecodecamp.com/mastering-chrome-developer-tools-next-level-front-end-development-techniques-3ac0b6fe8a3" target="_blank" rel="noopener">https://medium.freecodecamp.com/mastering-chrome-developer-tools-next-level-front-end-development-techniques-3ac0b6fe8a3</a></p>
</blockquote>
<hr>





















]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>Debug</tag>
      </tags>
  </entry>
  <entry>
    <title>gulp 常用插件的功能和用法</title>
    <url>/2017/06/30/article-gulp-normal/</url>
    <content><![CDATA[<p>　　gulp的官方定义非常简洁： 基于文件流的构建系统 。通过代码优于配置的策略，Gulp 让简单的任务简单，复杂的任务可管理。利用 Node.js 流的威力，你可以快速构建项目并减少频繁的 IO 操作。Gulp 严格的插件指南确保插件如你期望的那样简洁高质得工作。<a id="more"></a></p>
<h2 id="匹配符-、-、！、"><a href="#匹配符-、-、！、" class="headerlink" title="匹配符 、*、！、{}"></a>匹配符 <em>、*</em>、！、{}</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">gulp.src(<span class="string">'./js/*.js'</span>)               <span class="comment">// * 匹配js文件夹下所有.js格式的文件</span></span><br><span class="line">gulp.src(<span class="string">'./js/**/*.js'</span>)            <span class="comment">// ** 匹配js文件夹的0个或多个子文件夹</span></span><br><span class="line">gulp.src([<span class="string">'./js/*.js'</span>,<span class="string">'!./js/index.js'</span>])    <span class="comment">// ! 匹配除了index.js之外的所有js文件</span></span><br><span class="line">gulp.src(<span class="string">'./js/**/&#123;omui,common&#125;.js'</span>)        <span class="comment">// &#123;&#125; 匹配&#123;&#125;里的文件名</span></span><br></pre></td></tr></table></figure>

<h2 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h2><h3 id="del-替代gulp-clean"><a href="#del-替代gulp-clean" class="headerlink" title="del (替代gulp-clean)"></a>del (替代gulp-clean)</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> del = <span class="built_in">require</span>(<span class="string">'del'</span>);</span><br><span class="line">del(<span class="string">'./dist'</span>);                      <span class="comment">// 删除整个dist文件夹</span></span><br></pre></td></tr></table></figure>

<h3 id="gulp-rename"><a href="#gulp-rename" class="headerlink" title="gulp-rename"></a>gulp-rename</h3><p>描述：重命名文件。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> rename = <span class="built_in">require</span>(<span class="string">"gulp-rename"</span>);</span><br><span class="line">gulp.src(<span class="string">'./hello.txt'</span>)</span><br><span class="line">  .pipe(rename(<span class="string">'gb/goodbye.md'</span>))    <span class="comment">// 直接修改文件名和路径</span></span><br><span class="line">  .pipe(gulp.dest(<span class="string">'./dist'</span>));</span><br><span class="line"></span><br><span class="line">gulp.src(<span class="string">'./hello.txt'</span>)</span><br><span class="line">  .pipe(rename(&#123;</span><br><span class="line">    dirname: <span class="string">"text"</span>,                <span class="comment">// 路径名</span></span><br><span class="line">    basename: <span class="string">"goodbye"</span>,            <span class="comment">// 主文件名</span></span><br><span class="line">    prefix: <span class="string">"pre-"</span>,                 <span class="comment">// 前缀</span></span><br><span class="line">    suffix: <span class="string">"-min"</span>,                 <span class="comment">// 后缀</span></span><br><span class="line">    extname: <span class="string">".html"</span>                <span class="comment">// 扩展名</span></span><br><span class="line">  &#125;))</span><br><span class="line">  .pipe(gulp.dest(<span class="string">'./dist'</span>));</span><br></pre></td></tr></table></figure>

<h3 id="gulp-concat"><a href="#gulp-concat" class="headerlink" title="gulp-concat"></a>gulp-concat</h3><p>描述：合并文件。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> concat = <span class="built_in">require</span>(<span class="string">'gulp-concat'</span>);</span><br><span class="line">gulp.src(<span class="string">'./js/*.js'</span>)</span><br><span class="line">    .pipe(concat(<span class="string">'all.js'</span>))         <span class="comment">// 合并all.js文件</span></span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>));</span><br><span class="line"></span><br><span class="line">gulp.src([<span class="string">'./js/demo1.js'</span>,<span class="string">'./js/demo2.js'</span>,<span class="string">'./js/demo2.js'</span>])</span><br><span class="line">    .pipe(concat(<span class="string">'all.js'</span>))         <span class="comment">// 按照[]里的顺序合并文件</span></span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>));</span><br></pre></td></tr></table></figure>

<h3 id="gulp-filter"><a href="#gulp-filter" class="headerlink" title="gulp-filter"></a>gulp-filter</h3><p>描述：在虚拟文件流中过滤文件。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> filter = <span class="built_in">require</span>(<span class="string">'gulp-filter'</span>);</span><br><span class="line"><span class="keyword">const</span> f = filter([<span class="string">'**'</span>, <span class="string">'!*/index.js'</span>]);</span><br><span class="line">gulp.src(<span class="string">'js/**/*.js'</span>)</span><br><span class="line">    .pipe(f)                        <span class="comment">// 过滤掉index.js这个文件</span></span><br><span class="line">    .pipe(gulp.dest(<span class="string">'dist'</span>));</span><br><span class="line"><span class="keyword">const</span> f1 = filter([<span class="string">'**'</span>, <span class="string">'!*/index.js'</span>], &#123;<span class="attr">restore</span>: <span class="literal">true</span>&#125;);</span><br><span class="line">gulp.src(<span class="string">'js/**/*.js'</span>)</span><br><span class="line">    .pipe(f1)                       <span class="comment">// 过滤掉index.js这个文件</span></span><br><span class="line">    .pipe(uglify())                 <span class="comment">// 对其他文件进行压缩</span></span><br><span class="line">    .pipe(f1.restore)               <span class="comment">// 返回到未过滤执行的所有文件</span></span><br><span class="line">    .pipe(gulp.dest(<span class="string">'dist'</span>));       <span class="comment">// 再对所有文件操作，包括index.js</span></span><br></pre></td></tr></table></figure>

<h2 id="压缩"><a href="#压缩" class="headerlink" title="压缩"></a>压缩</h2><h3 id="gulp-uglify"><a href="#gulp-uglify" class="headerlink" title="gulp-uglify"></a>gulp-uglify</h3><p>描述：压缩js文件大小。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> uglify = <span class="built_in">require</span>(<span class="string">"gulp-uglify"</span>);</span><br><span class="line">gulp.src(<span class="string">'./hello.js'</span>)</span><br><span class="line">    .pipe(uglify())                 <span class="comment">// 直接压缩hello.js</span></span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br><span class="line"></span><br><span class="line"> gulp.src(<span class="string">'./hello.js'</span>)</span><br><span class="line">    .pipe(uglify(&#123;</span><br><span class="line">        mangle: <span class="literal">true</span>,               <span class="comment">// 是否修改变量名，默认为 true</span></span><br><span class="line">        compress: <span class="literal">true</span>,             <span class="comment">// 是否完全压缩，默认为 true</span></span><br><span class="line">        preserveComments: <span class="string">'all'</span>     <span class="comment">// 保留所有注释</span></span><br><span class="line">    &#125;))</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br></pre></td></tr></table></figure>

<h3 id="gulp-csso"><a href="#gulp-csso" class="headerlink" title="gulp-csso"></a>gulp-csso</h3><p>描述：压缩优化css。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> csso = <span class="built_in">require</span>(<span class="string">'gulp-csso'</span>);</span><br><span class="line">gulp.src(<span class="string">'./css/*.css'</span>)</span><br><span class="line">    .pipe(csso())</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist/css'</span>))</span><br></pre></td></tr></table></figure>

<h3 id="gulp-html-minify"><a href="#gulp-html-minify" class="headerlink" title="gulp-html-minify"></a>gulp-html-minify</h3><p>描述：压缩HTML。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> htmlminify = <span class="built_in">require</span>(<span class="string">'gulp-html-minify'</span>);</span><br><span class="line">gulp.src(<span class="string">'index.html'</span>)</span><br><span class="line">    .pipe(htmlminify())</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br></pre></td></tr></table></figure>

<h3 id="gulp-imagemin"><a href="#gulp-imagemin" class="headerlink" title="gulp-imagemin"></a>gulp-imagemin</h3><p>描述：压缩图片。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> imagemin = <span class="built_in">require</span>(<span class="string">'gulp-imagemin'</span>);</span><br><span class="line">gulp.src(<span class="string">'./img/*.&#123;jpg,png,gif,ico&#125;'</span>)</span><br><span class="line">    .pipe(imagemin())</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist/img'</span>))</span><br></pre></td></tr></table></figure>

<h3 id="gulp-zip"><a href="#gulp-zip" class="headerlink" title="gulp-zip"></a>gulp-zip</h3><p>描述：ZIP压缩文件。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> zip = <span class="built_in">require</span>(<span class="string">'gulp-zip'</span>);</span><br><span class="line">gulp.src(<span class="string">'./src/*'</span>)</span><br><span class="line">    .pipe(zip(<span class="string">'all.zip'</span>))                   <span class="comment">// 压缩成all.zip文件</span></span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br></pre></td></tr></table></figure>

<h2 id="JS-CSS自动注入"><a href="#JS-CSS自动注入" class="headerlink" title="JS/CSS自动注入"></a>JS/CSS自动注入</h2><h3 id="gulp-autoprefixer"><a href="#gulp-autoprefixer" class="headerlink" title="gulp-autoprefixer"></a>gulp-autoprefixer</h3><p>描述：自动为css添加浏览器前缀。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> autoprefixer = <span class="built_in">require</span>(<span class="string">'gulp-autoprefixer'</span>);</span><br><span class="line">gulp.src(<span class="string">'./css/*.css'</span>)</span><br><span class="line">    .pipe(autoprefixer())           <span class="comment">// 直接添加前缀</span></span><br><span class="line">    .pipe(gulp.dest(<span class="string">'dist'</span>))</span><br><span class="line">gulp.src(<span class="string">'./css/*.css'</span>)</span><br><span class="line">    .pipe(autoprefixer(&#123;</span><br><span class="line">        browsers: [<span class="string">'last 2 versions'</span>],      <span class="comment">// 浏览器版本</span></span><br><span class="line">        cascade：<span class="literal">true</span>                       <span class="comment">// 美化属性，默认true</span></span><br><span class="line">        add: <span class="literal">true</span>                           <span class="comment">// 是否添加前缀，默认true</span></span><br><span class="line">        remove: <span class="literal">true</span>                        <span class="comment">// 删除过时前缀，默认true</span></span><br><span class="line">        flexbox: <span class="literal">true</span>                       <span class="comment">// 为flexbox属性添加前缀，默认true</span></span><br><span class="line">    &#125;))</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br></pre></td></tr></table></figure>

<p>查看更多配置：<a href="https://github.com/postcss/autoprefixer#options" target="_blank" rel="noopener">options</a></p>
<p>更多浏览器版本：<a href="https://github.com/ai/browserslist#queries" target="_blank" rel="noopener">browsers</a></p>
<h3 id="gulp-useref"><a href="#gulp-useref" class="headerlink" title="gulp-useref"></a>gulp-useref</h3><p>描述：解析构建块在HTML文件来代替引用未经优化的脚本和样式表。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// index.html</span></span><br><span class="line">&lt;!-- build:css /css/all.css --&gt;</span><br><span class="line">&lt;link rel=<span class="string">"stylesheet"</span> href=<span class="string">"css/normalize.css"</span>&gt;</span><br><span class="line">&lt;link rel=<span class="string">"stylesheet"</span> href=<span class="string">"css/main.css"</span>&gt;</span><br><span class="line">&lt;!-- endbuild --&gt;</span><br><span class="line"><span class="comment">// gulpfile.js</span></span><br><span class="line"><span class="keyword">var</span> useref = <span class="built_in">require</span>(<span class="string">'gulp-useref'</span>);</span><br><span class="line">gulp.src(<span class="string">'index.html'</span>)</span><br><span class="line">    .pipe(useref())</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br></pre></td></tr></table></figure>

<p>替换之后的index.html中就会变成：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">href</span>=<span class="string">"css/all.css"</span>&gt;</span>  // 之前的两个<span class="tag">&lt;<span class="name">link</span>&gt;</span>替换成一个了</span><br></pre></td></tr></table></figure>

<h3 id="gulp-rev"><a href="#gulp-rev" class="headerlink" title="gulp-rev"></a>gulp-rev</h3><p>描述：给静态资源文件名添加hash值:unicorn.css =&gt; unicorn-d41d8cd98f.css</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> rev = <span class="built_in">require</span>(<span class="string">'gulp-rev'</span>);</span><br><span class="line">gulp.src(<span class="string">'./css/*.css'</span>)</span><br><span class="line">    .pipe(rev())</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist/css'</span>))</span><br></pre></td></tr></table></figure>

<h3 id="gulp-rev-replace"><a href="#gulp-rev-replace" class="headerlink" title="gulp-rev-replace"></a>gulp-rev-replace</h3><p>描述：重写被gulp-rev重命名的文件名。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> rev = <span class="built_in">require</span>(<span class="string">'gulp-rev'</span>);</span><br><span class="line"><span class="keyword">var</span> revReplace = <span class="built_in">require</span>(<span class="string">'gulp-rev-replace'</span>);</span><br><span class="line"><span class="keyword">var</span> useref = <span class="built_in">require</span>(<span class="string">'gulp-useref'</span>);</span><br><span class="line">gulp.src(<span class="string">'index.html'</span>)</span><br><span class="line">    .pipe(useref())                         <span class="comment">// 替换HTML中引用的css和js</span></span><br><span class="line">    .pipe(rev())                            <span class="comment">// 给css,js,html加上hash版本号</span></span><br><span class="line">    .pipe(revReplace())                     <span class="comment">// 把引用的css和js替换成有版本号的名字</span></span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br></pre></td></tr></table></figure>

<h3 id="gulp-html-replace"><a href="#gulp-html-replace" class="headerlink" title="gulp-html-replace"></a>gulp-html-replace</h3><p>描述：替换html中的构建块。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// index.html</span></span><br><span class="line">&lt;!-- build:css --&gt;                          <span class="comment">// css是buildName,可以自己定义</span></span><br><span class="line">&lt;link rel=<span class="string">"stylesheet"</span> href=<span class="string">"css/normalize.css"</span>&gt;</span><br><span class="line">&lt;link rel=<span class="string">"stylesheet"</span> href=<span class="string">"css/main.css"</span>&gt;</span><br><span class="line">&lt;!-- endbuild --&gt;</span><br><span class="line"><span class="comment">// gulpfile.js</span></span><br><span class="line"><span class="keyword">var</span> htmlreplace = <span class="built_in">require</span>(<span class="string">'gulp-html-replace'</span>);</span><br><span class="line">gulp.src(<span class="string">'index.html'</span>)</span><br><span class="line">    .pipe(htmlreplace(&#123;</span><br><span class="line">        <span class="string">'css'</span>:<span class="string">'all.css'</span>                     <span class="comment">// css是index.html中定义的buildName</span></span><br><span class="line">    &#125;))</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br><span class="line">替换之后的index.html中就会变成：</span><br><span class="line">&lt;link rel=<span class="string">"stylesheet"</span> href=<span class="string">"all.css"</span>&gt;      <span class="comment">// 之前的两个&lt;link&gt;替换成一个了</span></span><br></pre></td></tr></table></figure>

<h2 id="工具"><a href="#工具" class="headerlink" title="工具"></a>工具</h2><h3 id="gulp-load-plugins"><a href="#gulp-load-plugins" class="headerlink" title="gulp-load-plugins"></a>gulp-load-plugins</h3><p>描述：从包的依赖和附件里加载gulp插件到一个对象里给你选择。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// package.json</span></span><br><span class="line"><span class="string">"devDependencies"</span>: &#123;</span><br><span class="line">    <span class="string">"gulp"</span>: <span class="string">"^3.9.1"</span>,</span><br><span class="line">    <span class="string">"gulp-concat"</span>: <span class="string">"^2.6.1"</span>,</span><br><span class="line">    <span class="string">"gulp-rename"</span>: <span class="string">"^1.2.2"</span>,</span><br><span class="line">    <span class="string">"gulp-uglify"</span>: <span class="string">"^2.0.1"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// gulpfile.js</span></span><br><span class="line"><span class="keyword">var</span> $ = <span class="built_in">require</span>(<span class="string">'gulp-load-plugins'</span>)();     <span class="comment">// $ 是一个对象,加载了依赖里的插件</span></span><br><span class="line">gulp.src(<span class="string">'./**/*.js'</span>)</span><br><span class="line">    .pipe($.concat(<span class="string">'all.js'</span>))               <span class="comment">// 使用插件就可以用$.PluginsName()</span></span><br><span class="line">    .pipe($.uglify())</span><br><span class="line">    .pipe($.rename(<span class="string">'all.min.js'</span>))</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br></pre></td></tr></table></figure>

<h3 id="gulp-sass"><a href="#gulp-sass" class="headerlink" title="gulp-sass"></a>gulp-sass</h3><p>描述：编译sass。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> sass = <span class="built_in">require</span>(<span class="string">'gulp-sass'</span>);</span><br><span class="line">gulp.src(<span class="string">'./sass/**/*.scss'</span>)</span><br><span class="line">    .pipe(sass(&#123;</span><br><span class="line">        outputStyle: <span class="string">'compressed'</span>           <span class="comment">// 配置输出方式,默认为nested</span></span><br><span class="line">    &#125;))</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist/css'</span>));</span><br><span class="line"></span><br><span class="line">gulp.watch(<span class="string">'./sass/**/*.scss'</span>, [<span class="string">'sass'</span>]);   <span class="comment">// 实时监听sass文件变动,执行sass任务</span></span><br></pre></td></tr></table></figure>

<h3 id="gulp-babel"><a href="#gulp-babel" class="headerlink" title="gulp-babel"></a>gulp-babel</h3><p>描述：将ES6代码编译成ES5。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> babel = <span class="built_in">require</span>(<span class="string">'gulp-babel'</span>);</span><br><span class="line">gulp.src(<span class="string">'./js/index.js'</span>)</span><br><span class="line">    .pipe(babel(&#123;</span><br><span class="line">        presets: [<span class="string">'es2015'</span>]</span><br><span class="line">    &#125;))</span><br><span class="line">    .pipe(gulp.dest(<span class="string">'./dist'</span>))</span><br></pre></td></tr></table></figure>

<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>gulp</tag>
      </tags>
  </entry>
  <entry>
    <title>5种你未必知道的JavaScript和CSS交互的方法</title>
    <url>/2017/06/24/article-ways-css-javascript/</url>
    <content><![CDATA[<p>随着浏览器不断的升级改进，CSS和JavaScript之间的界限越来越模糊。本来它们是负责着完全不同的功能，但最终，它们都属于网页前端技术，它们需要相互密切的合作。我们的网页中都有.js文件和.css文件，但这并不意味着CSS和js是独立不能交互的。下面要讲的这五种JavaScript和CSS共同合作的方法你也许未必知道！<a id="more"></a></p>
<h3 id="用JavaScript获取伪元素-pseudo-element-属性"><a href="#用JavaScript获取伪元素-pseudo-element-属性" class="headerlink" title="用JavaScript获取伪元素(pseudo-element)属性"></a>用JavaScript获取伪元素(pseudo-element)属性</h3><p>大家都知道如何通过一个元素的<code>style</code>属性获取它的CSS样式值，但能获取伪元素(pseudo-element)的属性值吗？可以的，使用<code>JavaScript</code>也可以访问页面中的伪元素。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Get the color value of .element:before</span></span><br><span class="line"><span class="keyword">var</span> color = <span class="built_in">window</span>.getComputedStyle(</span><br><span class="line">	<span class="built_in">document</span>.querySelector(<span class="string">'.element'</span>), <span class="string">':before'</span></span><br><span class="line">).getPropertyValue(<span class="string">'color'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Get the content value of .element:before</span></span><br><span class="line"><span class="keyword">var</span> content = <span class="built_in">window</span>.getComputedStyle(</span><br><span class="line">	<span class="built_in">document</span>.querySelector(<span class="string">'.element'</span>), <span class="string">':before'</span></span><br><span class="line">).getPropertyValue(<span class="string">'content'</span>);</span><br></pre></td></tr></table></figure>

<p>看见了吗，我能访问伪元素里的<code>content</code>属性值。如果你想创建一个动态的，风格别致的网站，这是一种非常有用的技术！</p>
<h3 id="classList-API"><a href="#classList-API" class="headerlink" title="classList API"></a>classList API</h3><p>很多的JavaScript工具库里都有<code>addClass</code>，<code>removeClass</code>和<code>toggleClass</code>等方法。为了对老式浏览器的兼容，这些类库采用的方法都是先搜索元素的<code>className</code>，追加和删除这个类，然后更新<code>className</code>。其实有一个新型的API提供了添加，删除和反转CSS类属性的方法，叫做<code>classList</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">myDiv.classList.add(<span class="string">'myCssClass'</span>); <span class="comment">// Adds a class</span></span><br><span class="line"></span><br><span class="line">myDiv.classList.remove(<span class="string">'myCssClass'</span>); <span class="comment">// Removes a class</span></span><br><span class="line"></span><br><span class="line">myDiv.classList.toggle(<span class="string">'myCssClass'</span>); <span class="comment">// Toggles a class</span></span><br></pre></td></tr></table></figure>

<p>大多数的浏览器里很早就实现了<code>classList</code>API，而且最终 IE10 里也实现了它。</p>
<h3 id="直接对样式表进行添加和删除样式规则"><a href="#直接对样式表进行添加和删除样式规则" class="headerlink" title="直接对样式表进行添加和删除样式规则"></a>直接对样式表进行添加和删除样式规则</h3><p>我们都非常熟悉使用<code>element.style.propertyName</code>来修改样式，使用JavaScript能帮助我们做到这些，但你知道如何新增或修一个现有的CSS样式规则吗？其实非常的简单。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">addCSSRule</span>(<span class="params">sheet, selector, rules, index</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(sheet.insertRule) &#123;</span><br><span class="line">		sheet.insertRule(selector + <span class="string">"&#123;"</span> + rules + <span class="string">"&#125;"</span>, index);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> &#123;</span><br><span class="line">		sheet.addRule(selector, rules, index);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Use it!</span></span><br><span class="line">addCSSRule(<span class="built_in">document</span>.styleSheets[<span class="number">0</span>], <span class="string">"header"</span>, <span class="string">"float: left"</span>);</span><br></pre></td></tr></table></figure>

<p>这种方法通常是用来创建一个新的样式规则，但如果你想修改一个现有的规则，也可以这样做。</p>
<h3 id="加载CSS文件"><a href="#加载CSS文件" class="headerlink" title="加载CSS文件"></a>加载CSS文件</h3><p>延迟加载图片、JSON、脚本等是用来加快页面显示速度的好方法。我们可以使用curl.js等这样JavaScript加载器来延迟加载这些外部资源，可你知道CSS样式表也可以延迟加载吗，而且在加载成功后回调函数会给予通知。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">curl(</span><br><span class="line">	[</span><br><span class="line">		<span class="string">"namespace/MyWidget"</span>,</span><br><span class="line">		<span class="string">"css!namespace/resources/MyWidget.css"</span></span><br><span class="line">	], </span><br><span class="line">	<span class="function"><span class="keyword">function</span>(<span class="params">MyWidget</span>) </span>&#123;</span><br><span class="line">		<span class="comment">// 你可以对MyWidget进行操作</span></span><br><span class="line">		<span class="comment">// 这里没有对这个CSS文件引用，因为不需要;</span></span><br><span class="line">		<span class="comment">// 我们只需要它已经加载到页面上了</span></span><br><span class="line">	&#125;);</span><br></pre></td></tr></table></figure>

<p><a href="http://www.webhek.com/" target="_blank" rel="noopener">原文站点</a> 使用的PrismJS语法高亮脚本就是延迟加载的。当所有的资源都加载后，回调函数就会触发，可在回调函数里加载它。据说非常有用（还未尝试）！</p>
<h3 id="CSS鼠标指针事件"><a href="#CSS鼠标指针事件" class="headerlink" title="CSS鼠标指针事件"></a>CSS鼠标指针事件</h3><p>CSS鼠标指针事件<code>pointer-events</code>属性非常的有趣，它的功效非常像JavaScript，当你把这个属性设置为<code>none</code>时，它能有效的阻止禁止这个元素，你也许会说“这又如何？”，但事实上，它是禁止了这个元素上的任何JavaScript事件或回调函数！</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.disabled</span> &#123; <span class="attribute">pointer-events</span>: none; &#125;</span><br></pre></td></tr></table></figure>

<p>点击这个元素，你会发现任何你放置在这个元素上的监听器都不会触发任何事件。一个神奇的功能，真的——你不在需要为了防止某个事件会被触发而去检查某个css类是否存在。</p>
<blockquote>
<p>原文<a href="https://davidwalsh.name/ways-css-javascript-interact" target="_blank" rel="noopener">5 Ways that CSS and JavaScript Interact That You May Not Know About.</a></p>
</blockquote>
<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>5个数组的高效方法</title>
    <url>/2017/05/21/article-s-array/</url>
    <content><![CDATA[<p>今天学习到了JavaScript里关于数组的比较高效的5个原生方法，这5个方法可以说是每个想要高效的进行JavaScript编程的程序员必须知道的。<a id="more"></a></p>
<h3 id="Array-forEach"><a href="#Array-forEach" class="headerlink" title="Array.forEach()"></a>Array.forEach()</h3><p><code>.forEach()</code> 方法能够方便的让你 遍历数组里的每个元素，你可以在回调函数里对每个元素进行操作。<code>.forEach()</code>方法没有返回值，你不需要在回调函数里写<code>return</code>，这是无意义的。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> animals = [<span class="string">'dog'</span>, <span class="string">'cat'</span>, <span class="string">'mouse'</span>];</span><br><span class="line"></span><br><span class="line">animals.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">item</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(item);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="Array-map"><a href="#Array-map" class="headerlink" title="Array.map()"></a>Array.map()</h3><p><code>.map()</code> 方法能够遍历整个数组，然后 返回一个新数组，这个新数组里的元素是经过了指定的回调函数处理过的。</p>
<blockquote>
<p>如果你想修改数组里的每个元素，然后将修改后的数组存入新的数组，那使用 .map() 方法最方便。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> numbers = [<span class="number">2</span>, <span class="number">4</span>, <span class="number">6</span>, <span class="number">8</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> doubleNums = numbers.map(<span class="function"><span class="keyword">function</span>(<span class="params">element</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> element * <span class="number">2</span>;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'doubleNums: '</span>, doubleNums)</span><br></pre></td></tr></table></figure>

<h3 id="Array-filter"><a href="#Array-filter" class="headerlink" title="Array.filter()"></a>Array.filter()</h3><p><code>.filter()</code> 方法能够 过滤掉数组中的某些元素，你可以在回调函数里设定条件，不符合条件的元素都会排除在外。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> scores = [<span class="number">3</span>, <span class="number">12</span>, <span class="number">5</span>, <span class="number">23</span>, <span class="number">19</span>, <span class="number">7</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> topScores = scores.filter(<span class="function"><span class="keyword">function</span>(<span class="params">item</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (item &gt; <span class="number">10</span>)&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'topScores: '</span>, topScores);</span><br></pre></td></tr></table></figure>

<h3 id="Array-indexOf"><a href="#Array-indexOf" class="headerlink" title="Array.indexOf()"></a>Array.indexOf()</h3><p><code>.indexOf()</code> 能够告诉你 某个元素在数组中的位置，它返回的是索引值，如果数组里有重复的元素，它会返回第一个元素的位置。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = [<span class="number">2</span>, <span class="number">9</span>, <span class="number">9</span>, <span class="number">18</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> i = a.indexOf(<span class="number">9</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'i: '</span>, i);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">if (a.indexOf(7) === -1) &#123;</span></span><br><span class="line"><span class="comment">  // 数组中没有这个元素</span></span><br><span class="line"><span class="comment">&#125;*/</span></span><br></pre></td></tr></table></figure>

<h3 id="Array-every"><a href="#Array-every" class="headerlink" title="Array.every()"></a>Array.every()</h3><p><code>.every()</code> 方法的作用是用指定的回调函数去检查数组中的每个元素，如果对于每个元素，这个回调函数都返回<code>true</code>，则<code>.every()``返回true</code>。否则，<code>.every()</code> <code>返回false</code>。</p>
<blockquote>
<p>如果你想知道数组中的所有元素都是否符合某种条件，使用 .every() 最方便。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> ages = [<span class="number">23</span>, <span class="number">19</span>, <span class="number">32</span>, <span class="number">44</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> olderThan18 = ages.every(<span class="function"><span class="keyword">function</span>(<span class="params">element</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> element &gt; <span class="number">18</span>;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'olderThan18: '</span>, olderThan18);</span><br></pre></td></tr></table></figure>

<p>上面的这5个方法只是很多JavaScript方法中关于数组的最重要的几个，还有很多关于数组的方法、工具包(<a href="https://lodash.com/" target="_blank" rel="noopener">lodash.js</a> and <a href="http://underscorejs.org/" target="_blank" rel="noopener">underscore.js</a>)等都非常的有用。</p>
<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>移动端 H5 踩坑记录</title>
    <url>/2017/04/28/article-mobile-summarize/</url>
    <content><![CDATA[<span itemprop="image" itemscope itemtype="http://schema.org/ImageObject"><img itemprop="url image" src="/styles/banner/mobile-h5.jpg" class="full-image" alt="移动端" title="H5踩坑"><meta itemprop="width" content="auto"><meta itemprop="height" content="auto"></span>

<p>这里收集了许多移动端上遇到的各种坑与相对解决方案！</p>
<a id="more"></a>



<h2 id="工具类网站"><a href="#工具类网站" class="headerlink" title="工具类网站"></a>工具类网站</h2><p> <a href="http://html5please.com/" title="html5与css3技术应用评估" target="_blank" rel="noopener">HTML5 与 CSS3 技术应用评估</a></p>
<p> <a href="http://browserhacks.com/" title="各种奇妙的hack" target="_blank" rel="noopener">各种奇妙的hack</a></p>
<p> <a href="http://en.wikipedia.org/wiki/List_of_displays_by_pixel_density" title="几乎所有设备的屏幕尺寸与像素密度表" target="_blank" rel="noopener">几乎所有设备的屏幕尺寸与像素密度表</a></p>
<p> <a href="http://screensiz.es/phone" title="移动设备参数表" target="_blank" rel="noopener">移动设备参数表</a></p>
<p> <a href="http://ivomynttinen.com/blog/the-ios-design-cheat-sheet-volume-2/" title="ios端移动设备参数速查" target="_blank" rel="noopener">ios端移动设备参数速查</a></p>
<p> <a href="http://www.quirksmode.org/compatibility.html" title="浏览器兼容表" target="_blank" rel="noopener">浏览器兼容表</a></p>
<p> <a href="https://deviceatlas.com/device-data/devices" title="移动设备查询器" target="_blank" rel="noopener">移动设备查询器</a></p>
<p> <a href="http://51degrees.codeplex.com/" title="移动设备适配库" target="_blank" rel="noopener">移动设备适配库</a></p>
<p> <a href="http://detectmobilebrowsers.com/" title="移动设备适配库2" target="_blank" rel="noopener">移动设备适配库2</a></p>
<p> <a href="https://deviceatlas.com/device-data/devices" title="viewport与设备尺寸在线检测器" target="_blank" rel="noopener">viewport与设备尺寸在线检测器</a></p>
<p> <a href="http://mobilehtml5.org/" title="html5移动端兼容性速查" target="_blank" rel="noopener">html5 移动端兼容性速查</a></p>
<p> <a href="http://www.fontsquirrel.com/tools/webfont-generator" title="在线转换字体" target="_blank" rel="noopener">在线转换字体</a></p>
<p> <a href="http://tools.css3.info/selectors-test/test.html" title="css3选择器测试" target="_blank" rel="noopener">css3 选择器测试</a></p>
<p> <a href="http://caniuse.com/" title="兼容性速查表" target="_blank" rel="noopener">兼容性速查表</a></p>
<p> <a href="http://www.browserscope.org/" title="浏览器的一些独特参数" target="_blank" rel="noopener">浏览器的一些独特参数</a></p>
<p> <a href="http://nmsdvid.com/snippets/" title="各种各样的媒体查询收集" target="_blank" rel="noopener">各种各样的媒体查询收集</a></p>
<p> <a href="http://css3lib.alloyteam.com/#animation/AnimatedButtons" title="css3动画在线制作器" target="_blank" rel="noopener">css3 动画在线制作器</a></p>
<p> <a href="http://www.colorzilla.com/gradient-editor/" title="css3渐变在线制作器" target="_blank" rel="noopener">css3 渐变在线制作器</a></p>
<p><a href="http://ww1.sinaimg.cn/bmiddle/c2c57f68jw1e4fh7dmw12j20fi2w6qe1.jpg" title="移动端手势表" target="_blank" rel="noopener">移动端手势表</a></p>
<p><a href="http://ued.ctrip.com/blog/wp-content/webkitcss/" title="webkit独有的样式分析" target="_blank" rel="noopener">webkit独有的样式分析</a></p>
<p><a href="https://github.com/Modernizr/Modernizr/wiki/HTML5-Cross-Browser-Polyfills" target="_blank" rel="noopener">HTML5 Cross Browser Polyfills</a></p>
<p><a href="http://html5polyfill.com/" title="HTML5 POLYFILLS" target="_blank" rel="noopener">HTML5 POLYFILLS</a></p>
<h2 id="iphone6的那些事"><a href="#iphone6的那些事" class="headerlink" title="iphone6的那些事"></a>iphone6的那些事</h2><p><a href="http://wileam.com/iphone-6-screen-cn/" target="_blank" rel="noopener">iPhone 6 屏幕揭秘</a></p>
<h2 id="响应式测试工具"><a href="#响应式测试工具" class="headerlink" title="响应式测试工具"></a>响应式测试工具</h2><p>Firefox 浏览器内置了 <code>自定义设计视图</code> 的功能，可以通过 <code>Firefox-&gt;Web 开发者-&gt;自定义设计视图</code>（或者摁下 <code>Shift + Ctrl + m</code> ）。相比网络工具，运行更加流畅，无需联网。</p>
<p><a href="http://yujiangshui.com/document/css-media-queries-for-ipads-and-iphones-chinese-version/" target="_blank" rel="noopener">判断 iPad 和 iPhone 的版本和状态的 CSS 媒体查询代码</a></p>
<p><a href="http://lab.maltewassermann.com/viewport-resizer/" title="resize" target="_blank" rel="noopener">Viewport Resizer</a></p>
<p><a href="http://beta.screenqueri.es/" title="resize" target="_blank" rel="noopener">http://beta.screenqueri.es/</a></p>
<p><a href="http://responsivepx.com" title="resize" target="_blank" rel="noopener">http://responsivepx.com</a></p>
<p><a href="http://www.responsinator.com/" title="resize" target="_blank" rel="noopener">http://www.responsinator.com/</a></p>
<p><a href="http://resizemybrowser.com/" title="resize" target="_blank" rel="noopener">http://resizemybrowser.com/</a></p>
<p><a href="https://quirktools.com/screenfly/" title="resize" target="_blank" rel="noopener">https://quirktools.com/screenfly/</a></p>
<p>  媒体查询常用样式表：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">media</span>=<span class="string">"all and (orientation:portrait)"</span> <span class="attr">href</span>=<span class="string">"portrait.css"</span>&gt;</span>    // 竖放加载</span><br><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">media</span>=<span class="string">"all and (orientation:landscape)"</span><span class="attr">href</span>=<span class="string">"landscape.css"</span>&gt;</span>   // 横放加载</span><br></pre></td></tr></table></figure>

<pre><code>竖屏时使用的样式</code></pre><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span> <span class="attr">media</span>=<span class="string">"all and (orientation:portrait)"</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line"><span class="css">	<span class="selector-id">#landscape</span> &#123; <span class="attribute">display</span>: none; &#125;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br></pre></td></tr></table></figure>

<pre><code>//横屏时使用的样式</code></pre><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span> <span class="attr">media</span>=<span class="string">"all and (orientation:landscape)"</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line"><span class="css">	<span class="selector-id">#portrait</span> &#123; <span class="attribute">display</span>: none; &#125;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="Web-app-开发的最佳实践与中文总结"><a href="#Web-app-开发的最佳实践与中文总结" class="headerlink" title="Web app 开发的最佳实践与中文总结"></a>Web app 开发的最佳实践与中文总结</h2><p><a href="http://blog.forecast.io/its-not-a-web-app-its-an-app-you-install-from-the-web/" target="_blank" rel="noopener">It’s not a web app. It’s an app you install from the web.</a></p>
<p><a href="http://lyric.im/best-practice-for-web-app-development/" target="_blank" rel="noopener">当前 WEB APP 开发的最佳实践</a></p>
<p><a href="https://www.icloud.com/keynote/AwBWCAESEJd5uucfBPGt6KPotb3tNfsaKm-Q7fqs2-4ojmPoPJuWZCvjYgKl5jEf1URdRgdgNHe38BTzeF3DK7q1ewMCUCAQEEIJ85mw21ii_AwybOqxoF-V02v51Vdg855ED4qVA_8bXr#Mobile_Webpage_如何自适应屏幕_2" target="_blank" rel="noopener">如何自适应网页屏幕</a><br>以及<a href="https://github.com/unbug/generator-webappstarter/blob/master/app/templates/app/src/util/MetaHandler.js" target="_blank" rel="noopener">配套的解决方案</a></p>
<p>来自<a href="https://github.com/maxzhang" title="ava" target="_blank" rel="noopener">maxzhang</a>的一些移动端经验总结干货</p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/8" title="ava" target="_blank" rel="noopener">移动Web单页应用开发实践——页面结构化</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/1" title="ava" target="_blank" rel="noopener">移动Web产品前端开发口诀——“快”</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/7" title="ava" target="_blank" rel="noopener">移动Web开发，4行代码检测浏览器是否支持position:fixed</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/4" title="ava" target="_blank" rel="noopener">使用border-image实现类似iOS7的1px底边</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/2" title="ava" target="_blank" rel="noopener">移动端web页面使用position:fixed问题总结</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/11" title="ava" target="_blank" rel="noopener">移动Web开发实践——解决position:fixed自适应BUG</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/19" title="ava" target="_blank" rel="noopener">移动手机浏览器m3u8格式视频流播放支持程度测试</a></p>
<p> <a href="http://www.cnblogs.com/pifoo/archive/2011/05/23/webkit-touch-event-1.html" title="article1" target="_blank" rel="noopener">指尖下的js ——多触式web前端开发之一：对于Touch的处理</a></p>
<p> <a href="http://www.cnblogs.com/pifoo/archive/2011/05/22/webkit-touch-event-2.html" title="article2" target="_blank" rel="noopener">指尖下的js ——多触式web前端开发之二：处理简单手势</a></p>
<p> <a href="http://www.cnblogs.com/pifoo/archive/2011/05/22/webkit-touch-event-3.html" title="article3" target="_blank" rel="noopener">指尖下的js —— 多触式web前端开发之三：处理复杂手势</a></p>
<h2 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h2><h3 id="meta标签"><a href="#meta标签" class="headerlink" title="meta标签"></a>meta标签</h3><p>meta标签大全 <a href="http://segmentfault.com/blog/ciaocc/1190000002407912" target="_blank" rel="noopener">http://segmentfault.com/blog/ciaocc/1190000002407912</a></p>
<p>meta标签，这些meta标签在开发webapp时起到非常重要的作用</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">content</span>=<span class="string">"width=device-width; initial-scale=1.0; maximum-scale=1.0; user-scalable=0"</span> <span class="attr">name</span>=<span class="string">"viewport"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">content</span>=<span class="string">"yes"</span> <span class="attr">name</span>=<span class="string">"apple-mobile-web-app-capable"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">content</span>=<span class="string">"black"</span> <span class="attr">name</span>=<span class="string">"apple-mobile-web-app-status-bar-style"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">content</span>=<span class="string">"telephone=no"</span> <span class="attr">name</span>=<span class="string">"format-detection"</span> /&gt;</span></span><br></pre></td></tr></table></figure>

<p>第一个meta标签表示：强制让文档的宽度与设备的宽度保持1:1，并且文档最大的宽度比例是1.0，且不允许用户点击屏幕放大浏览；<br>尤其要注意的是content里多个属性的设置一定要用分号+空格来隔开，如果不规范将不会起作用。</p>
<p>注意根据 <a href="http://www.weibo.com/avajayam" title="ava" target="_blank" rel="noopener">public_00</a> 提供的资料补充，content 使用分号作为分隔，在老的浏览器是支持的，但不是规范写法。</p>
<p>规范的写法应该是使用逗号分隔，参考 <a href="http://developer.apple.com/library/safari/#documentation/appleapplications/reference/SafariHTMLRef/Articles/MetaTags.html" target="_blank" rel="noopener">Safari HTML Reference - Supported Meta Tags</a> 和 <a href="http://developer.android.com/guide/webapps/targeting.html" target="_blank" rel="noopener">Android - Supporting Different Screens in Web Apps</a></p>
<p>其中：</p>
<ul>
<li>width - viewport的宽度</li>
<li>height - viewport的高度</li>
<li>initial-scale - 初始的缩放比例</li>
<li>minimum-scale - 允许用户缩放到的最小比例</li>
<li>maximum-scale - 允许用户缩放到的最大比例</li>
<li>user-scalable - 用户是否可以手动缩放</li>
</ul>
<p>第二个meta标签是iphone设备中的safari私有meta标签，它表示：允许全屏模式浏览；<br>第三个meta标签也是iphone的私有标签，它指定的iphone中safari顶端的状态条的样式；<br>第四个meta标签表示：告诉设备忽略将页面中的数字识别为电话号码</p>
<p>在设置了initial-scale=1 之后，我们终于可以以1:1 的比例进行页面设计了。<br>关于viewport，还有一个很重要的概念是：iphone 的safari 浏览器完全没有滚动条，而且不是简单的“隐藏滚动条”，<br>是根本没有这个功能。iphone 的safari 浏览器实际上从一开始就完整显示了这个网页，然后用viewport 查看其中的一部分。<br>当你用手指拖动时，其实拖的不是页面，而是viewport。浏览器行为的改变不止是滚动条，交互事件也跟普通桌面不一样。<br>(请参考：指尖的下JS 系列文章)</p>
<p>更详细的 viewport 相关的知识也可以参考</p>
<p><a href="http://www.w3cplus.com/css/A-pixel-is-not-a-pixel-is-not-a-pixel.html" title="pixel" target="_blank" rel="noopener">此像素非彼像素</a></p>
<h2 id="移动开发事件"><a href="#移动开发事件" class="headerlink" title="移动开发事件"></a>移动开发事件</h2><p><a href="http://wo.poco.cn/manson/post/id/268780" target="_blank" rel="noopener">手机浏览器常用手势动作监听封装</a></p>
<h3 id="手势事件"><a href="#手势事件" class="headerlink" title="手势事件"></a>手势事件</h3><ul>
<li>touchstart            //当手指接触屏幕时触发</li>
<li>touchmove           //当已经接触屏幕的手指开始移动后触发</li>
<li>touchend             //当手指离开屏幕时触发</li>
<li>touchcancel</li>
</ul>
<h3 id="触摸事件"><a href="#触摸事件" class="headerlink" title="触摸事件"></a>触摸事件</h3><ul>
<li>gesturestart          //当两个手指接触屏幕时触发</li>
<li>gesturechange      //当两个手指接触屏幕后开始移动时触发</li>
<li>gestureend</li>
</ul>
<h3 id="屏幕旋转事件"><a href="#屏幕旋转事件" class="headerlink" title="屏幕旋转事件"></a>屏幕旋转事件</h3><ul>
<li>onorientationchange    </li>
</ul>
<h3 id="检测触摸屏幕的手指何时改变方向"><a href="#检测触摸屏幕的手指何时改变方向" class="headerlink" title="检测触摸屏幕的手指何时改变方向"></a>检测触摸屏幕的手指何时改变方向</h3><ul>
<li>orientationchange      </li>
</ul>
<h3 id="touch事件支持的相关属性"><a href="#touch事件支持的相关属性" class="headerlink" title="touch事件支持的相关属性"></a>touch事件支持的相关属性</h3><ul>
<li>touches</li>
<li>targetTouches</li>
<li>changedTouches</li>
<li>clientX　　　　// X coordinate of touch relative to the viewport (excludes scroll offset)</li>
<li>clientY　　　　// Y coordinate of touch relative to the viewport (excludes scroll offset)</li>
<li>screenX　　　 // Relative to the screen</li>
<li>screenY 　　  // Relative to the screen</li>
<li>pageX　　 　　// Relative to the full page (includes scrolling)</li>
<li>pageY　　　　 // Relative to the full page (includes scrolling)</li>
<li>target　　　　 // Node the touch event originated from</li>
<li>identifier　　   // An identifying number, unique to each touch event</li>
<li>屏幕旋转事件：onorientationchange</li>
</ul>
<h3 id="判断屏幕是否旋转"><a href="#判断屏幕是否旋转" class="headerlink" title="判断屏幕是否旋转"></a>判断屏幕是否旋转</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">	<span class="function"><span class="keyword">function</span> <span class="title">orientationChange</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">		<span class="keyword">switch</span>(<span class="built_in">window</span>.orientation) &#123;</span><br><span class="line">		　　<span class="keyword">case</span> <span class="number">0</span>:</span><br><span class="line">				alert(<span class="string">"肖像模式 0,screen-width: "</span> + screen.width + <span class="string">"; screen-height:"</span> + screen.height);</span><br><span class="line">				<span class="keyword">break</span>;</span><br><span class="line">		　　<span class="keyword">case</span> <span class="number">-90</span>:</span><br><span class="line">				alert(<span class="string">"左旋 -90,screen-width: "</span> + screen.width + <span class="string">"; screen-height:"</span> + screen.height);</span><br><span class="line">				<span class="keyword">break</span>;</span><br><span class="line">		　　<span class="keyword">case</span> <span class="number">90</span>:</span><br><span class="line">				alert(<span class="string">"右旋 90,screen-width: "</span> + screen.width + <span class="string">"; screen-height:"</span> + screen.height);</span><br><span class="line">				<span class="keyword">break</span>;</span><br><span class="line">		　　<span class="keyword">case</span> <span class="number">180</span>:</span><br><span class="line">			　　alert(<span class="string">"风景模式 180,screen-width: "</span> + screen.width + <span class="string">"; screen-height:"</span> + screen.height);</span><br><span class="line">			　　<span class="keyword">break</span>;</span><br><span class="line">		&#125;;&#125;;</span><br><span class="line"></span><br><span class="line">	addEventListener(<span class="string">'load'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">		orientationChange();</span><br><span class="line">		<span class="built_in">window</span>.onorientationchange = orientationChange;</span><br><span class="line">	&#125;);</span><br><span class="line"><span class="string">``</span><span class="string">` </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">### JS 单击延迟</span></span><br><span class="line"><span class="string">click 事件因为要等待单击确认，会有 300ms 的延迟，体验并不是很好。</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">开发者大多数会使用封装的 tap 事件来代替click 事件，所谓的 tap 事件由 touchstart 事件 + touchmove 判断 + touchend 事件封装组成。</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"> [Creating Fast Buttons for Mobile Web Applications](https://developers.google.com/mobile/articles/fast_buttons?hl=de-DE "article5")</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"> [Eliminate 300ms delay on click events in mobile Safari](http://stackoverflow.com/questions/12238587/eliminate-300ms-delay-on-click-events-in-mobile-safari "article5")</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">## WebKit CSS:</span></span><br><span class="line"><span class="string">[携程 UED 整理的 Webkit CSS 文档](http://ued.ctrip.com/blog/wp-content/webkitcss/index.html) ，全面、方便查询，下面为常用属性。</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">①“盒模型”的具体描述性质的包围盒块内容，包括边界，填充等等。</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>css</span><br><span class="line">	-webkit-border-bottom-left-radius: radius;</span><br><span class="line">	-webkit-border-top-left-radius: horizontal_radius vertical_radius;</span><br><span class="line">	-webkit-border-radius: radius;      <span class="comment">//容器圆角</span></span><br><span class="line">	-webkit-box-sizing: sizing_model; 边框常量值：border-box/content-box</span><br><span class="line">	-webkit-box-shadow: hoff voff blur color; <span class="comment">/*容器阴影（参数分别为：水平X 方向偏移量；垂直Y方向偏移量；高斯模糊半径值；阴影颜色值）*/</span></span><br><span class="line">	-webkit-margin-bottom-collapse: collapse_behavior; <span class="comment">/*常量值：collapse/discard/separate*/</span></span><br><span class="line">	-webkit-margin-start: width;</span><br><span class="line">	-webkit-padding-start: width;</span><br><span class="line">	-webkit-border-image: url(borderimg.gif) <span class="number">25</span> <span class="number">25</span> <span class="number">25</span> <span class="number">25</span> round/stretch round/stretch;</span><br><span class="line">	-webkit-appearance: push-button;   <span class="comment">/*内置的CSS 表现，暂时只支持push-button*/</span></span><br></pre></td></tr></table></figure>

<p>②“视觉格式化模型”描述性质，确定了位置和大小的块元素。</p>
<pre><code>direction: rtl
unicode-bidi: bidi-override; 常量：bidi-override/embed/normal</code></pre><p>③“视觉效果”描述属性，调整的视觉效果块内容，包括溢出行为，调整行为，能见度，动画，变换，和过渡。</p>
<pre><code>clip: rect(10px, 5px, 10px, 5px)
resize: auto; 常量：auto/both/horizontal/none/vertical
visibility: visible; 常量: collapse/hidden/visible
-webkit-transition: opacity 1s linear; 动画效果 ease/linear/ease-in/ease-out/ease-in-out
-webkit-backface-visibility: visibler; 常量：visible(默认值)/hidden
-webkit-box-reflect: right 1px; 镜向反转
-webkit-box-reflect: below 4px -webkit-gradient(linear, left top, left bottom,
from(transparent), color-stop(0.5, transparent), to(white));
-webkit-mask-image: -webkit-gradient(linear, left top, left bottom, from(rgba(0,0,0,1)), to(rgba(0,0,0,0)));;   //CSS 遮罩/蒙板效果
-webkit-mask-attachment: fixed; 常量：fixed/scroll
-webkit-perspective: value; 常量：none(默认)
-webkit-perspective-origin: left top;
-webkit-transform: rotate(5deg);
-webkit-transform-style: preserve-3d; 常量：flat/preserve-3d; (2D 与3D)</code></pre><p>④“生成的内容，自动编号，并列出”描述属性，允许您更改内容的一个组成部分，创建自动编号的章节和标题，和操纵的风格清单的内容。</p>
<pre><code>content: “Item” counter(section) ” “;
This resets the counter.
First section
&gt;two section
three section
counter-increment: section 1;
counter-reset: section;</code></pre><p>⑤“分页媒体”描述性能与外观的属性，控制印刷版本的网页，如分页符的行为。</p>
<pre><code>page-break-after: auto; 常量：always/auto/avoid/left/right
page-break-before: auto; 常量：always/auto/avoid/left/right
page-break-inside: auto; 常量：auto/avoid</code></pre><p>⑥“颜色和背景”描述属性控制背景下的块级元素和颜色的文本内容的组成部分。</p>
<pre><code>-webkit-background-clip: content; 常量：border/content/padding/text
-webkit-background-origin: padding; 常量：border/content/padding/text
-webkit-background-size: 55px; 常量：length/length_x/length_y</code></pre><p>⑦ “字型”的具体描述性质的文字字体的选择范围内的一个因素。报告还描述属性用于下载字体定义。</p>
<pre><code>unicode-range: U+00-FF, U+980-9FF;</code></pre><p>⑧“文本”描述属性的特定文字样式，间距和自动滚屏。</p>
<pre><code>text-shadow: #00FFFC 10px 10px 5px;
text-transform: capitalize; 常量：capitalize/lowercase/none/uppercase
word-wrap: break-word; 常量：break-word/normal
-webkit-marquee: right large infinite normal 10s; 常量：direction(方向) increment(迭代次数) repetition(重复) style(样式) speed(速度);
-webkit-marquee-direction: ahead/auto/backwards/down/forwards/left/reverse/right/up
-webkit-marquee-incrementt: 1-n/infinite(无穷次)
-webkit-marquee-speed: fast/normal/slow
-webkit-marquee-style: alternate/none/scroll/slide
-webkit-text-fill-color: #ff6600; 常量：capitalize, lowercase, none, uppercase
-webkit-text-security: circle; 常量：circle/disc/none/square
-webkit-text-size-adjust: none; 常量:auto/none;
-webkit-text-stroke: 15px #fff;
-webkit-line-break: after-white-space; 常量：normal/after-white-space
-webkit-appearance: caps-lock-indicator;
-webkit-nbsp-mode: space; 常量： normal/space
-webkit-rtl-ordering: logical; 常量：visual/logical
-webkit-user-drag: element; 常量：element/auto/none
-webkit-user-modify: read- only; 常量：read-write-plaintext-only/read-write/read-only
-webkit-user-select: text; 常量：text/auto/none</code></pre><p>⑨“表格”描述的布局和设计性能表的具体内容。</p>
<pre><code>-webkit-border-horizontal-spacing: 2px;
-webkit-border-vertical-spacing: 2px;
-webkit-column-break-after: right; 常量：always/auto/avoid/left/right
-webkit-column-break-before: right; 常量：always/auto/avoid/left/right
–webkit-column-break-inside: logical; 常量：avoid/auto
-webkit-column-count: 3; //分栏
-webkit-column-rule: 1px solid #fff;
style:dashed,dotted,double,groove,hidden,inset,none,outset,ridge,solid</code></pre><p>⑩“用户界面”描述属性，涉及到用户界面元素在浏览器中，如滚动文字区，滚动条，等等。报告还描述属性，范围以外的网页内容，如光标的标注样式和显示当您按住触摸触摸<br>目标，如在iPhone上的链接。</p>
<pre><code>-webkit-box-align: baseline,center,end,start,stretch 常量：baseline/center/end/start/stretch
-webkit-box-direction: normal;常量：normal/reverse
-webkit-box-flex: flex_valuet
-webkit-box-flex-group: group_number
-webkit-box-lines: multiple; 常量：multiple/single
-webkit-box-ordinal-group: group_number
-webkit-box-orient: block-axis; 常量：block-axis/horizontal/inline-axis/vertical/orientation
–webkit-box-pack: alignment; 常量：center/end/justify/start</code></pre><p>动画过渡<br>这是 Webkit 中最具创新力的特性：使用过渡函数定义动画。</p>
<pre><code>-webkit-animation: title infinite ease-in-out 3s;
animation 有这几个属性：
-webkit-animation-name： //属性名，就是我们定义的keyframes
-webkit-animation-duration：3s //持续时间
-webkit-animation-timing-function： //过渡类型：ease/ linear(线性) /ease-in(慢到快)/ease-out(快到慢) /ease-in-out(慢到快再到慢) /cubic-bezier
-webkit-animation-delay：10ms //动画延迟(默认0)
-webkit-animation-iteration-count： //循环次数(默认1)，infinite 为无限
-webkit-animation-direction： //动画方式：normal(默认 正向播放)； alternate(交替方向，第偶数次正向播放，第奇数次反向播放)</code></pre><p>这些同样是可以简写的。但真正让我觉的很爽的是keyframes，它能定义一个动画的转变过程供调用，过程为0%到100%或from(0%)到to(100%)。简单点说，只要你有想法，你想让元素在这个过程中以什么样的方式改变都是很简单的。</p>
<pre><code>-webkit-transform: 类型（缩放scale/旋转rotate/倾斜skew/位移translate）
scale(num,num) 放大倍率。scaleX 和 scaleY(3)，可以简写为：scale(* , *)
rotate(*deg) 转动角度。rotateX 和 rotateY，可以简写为：rotate(* , *)
Skew(*deg) 倾斜角度。skewX 和skewY，可简写为：skew(* , *)
translate(*,*) 坐标移动。translateX 和translateY，可简写为：translate(* , *)。</code></pre><h3 id="页面描述"><a href="#页面描述" class="headerlink" title="页面描述"></a>页面描述</h3><pre><code>&lt;link rel=&quot;apple-touch-icon-precomposed&quot; href=&quot;http://www.xxx.com/App_icon_114.png&quot; /&gt;
&lt;link rel=&quot;apple-touch-icon-precomposed&quot; sizes=&quot;72x72&quot; href=&quot;http://www.xxx.com/App_icon_72.png&quot; /&gt;
&lt;link rel=&quot;apple-touch-icon-precomposed&quot; sizes=&quot;114x114&quot; href=&quot;http://www.xxx.com/App_icon_114.png&quot; /&gt;</code></pre><p>这个属性是当用户把连接保存到手机桌面时使用的图标，如果不设置，则会用网页的截图。有了这，就可以让你的网页像APP一样存在手机里了</p>
<pre><code>&lt;link rel=&quot;apple-touch-startup-image&quot; href=&quot;/img/startup.png&quot; /&gt;</code></pre><p>这个是APP启动画面图片，用途和上面的类似，如果不设置，启动画面就是白屏，图片像素就是手机全屏的像素</p>
<pre><code>&lt;meta name=&quot;apple-mobile-web-app-status-bar-style&quot; content=&quot;black-translucent&quot; /&gt;</code></pre><p>这个描述是表示打开的web app的最上面的时间、信号栏是黑色的，当然也可以设置其它参数，详细参数说明请参照：<a href="https://developer.apple.com/library/safari/documentation/appleapplications/reference/SafariHTMLRef/Articles/MetaTags.html" target="_blank" rel="noopener">Safari HTML Reference - Supported Meta Tags</a></p>
<pre><code>&lt;meta name=&quot;apple-touch-fullscreen&quot; content=&quot;yes&quot; /&gt;
&lt;meta name=&quot;apple-mobile-web-app-capable&quot; content=&quot;yes&quot; /&gt;</code></pre><h2 id="常见的-iPhone-和-Android-屏幕参数。"><a href="#常见的-iPhone-和-Android-屏幕参数。" class="headerlink" title="常见的 iPhone 和 Android 屏幕参数。"></a>常见的 iPhone 和 Android 屏幕参数。</h2><ul>
<li>设备         分辨率        设备像素比率</li>
<li>Android LDPI    320×240    0.75</li>
<li>Iphone 3 &amp; Android MDPI    320×480    1</li>
<li>Android HDPI    480×800    1.5</li>
<li>Iphone 4    960×640    2.0</li>
</ul>
<p>iPhone 4的一个 CSS 像素实际上表现为一块 2×2 的像素。所以图片像是被放大2倍一样，模糊不清晰。</p>
<p>解决办法：</p>
<p>1、页面引用</p>
<pre><code>&lt;link rel=&quot;stylesheet&quot; media=&quot;screen and (-webkit-device-pixel-ratio: 0.75)&quot; href=&quot;ldpi.css&quot; /&gt;
&lt;link rel=&quot;stylesheet&quot; media=&quot;screen and (-webkit-device-pixel-ratio: 1.0)&quot; href=&quot;mdpi.css&quot; /&gt;
&lt;link rel=&quot;stylesheet&quot; media=&quot;screen and (-webkit-device-pixel-ratio: 1.5)&quot; href=&quot;hdpi.css&quot; /&gt;
&lt;link rel=&quot;stylesheet&quot; media=&quot;screen and (-webkit-device-pixel-ratio: 2.0)&quot; href=&quot;retina.css&quot; /&gt;</code></pre><p>2、CSS文件里</p>
<pre><code>#header {
    background:url(mdpi/bg.png);
}

@media screen and (-webkit-device-pixel-ratio: 1.5) {
    /*CSS for high-density screens*/
    #header {
        background:url(hdpi/bg.png);
    }
}</code></pre><h2 id="移动-Web-开发经验技巧"><a href="#移动-Web-开发经验技巧" class="headerlink" title="移动 Web 开发经验技巧"></a>移动 Web 开发经验技巧</h2><h3 id="点击与click事件"><a href="#点击与click事件" class="headerlink" title="点击与click事件"></a>点击与click事件</h3><p>对于a标记的点击导航，默认是在onclick事件中处理的。而移动客户端对onclick的响应相比PC浏览器有着明显的几百毫秒延迟。</p>
<p>在移动浏览器中对触摸事件的响应顺序应当是： </p>
<pre><code>ontouchstart -&gt; ontouchmove -&gt; ontouchend -&gt; onclick</code></pre><p>因此，如果确实要加快对点击事件的响应，就应当绑定ontouchend事件。</p>
<p>使用click会出现绑定点击区域闪一下的情况，解决：给该元素一个样式如下</p>
<pre><code>-webkit-tap-highlight-color: rgba(0,0,0,0);</code></pre><p>如果不使用click，也不能简单的用touchstart或touchend替代，需要用touchstart的模拟一个click事件，并且不能发生touchmove事件，或者用zepto中的tap（轻击）事件。</p>
<pre><code>body
{
    -webkit-overflow-scrolling: touch;
}</code></pre><p>用iphone或ipad浏览很长的网页滚动时的滑动效果很不错吧？不过如果是一个div，然后设置 <code>height:200px;overflow:auto;</code>的话，可以滚动但是完全没有那滑动效果，很郁闷吧？</p>
<p>我看到很多网站为了实现这一效果，用了第三方类库，最常用的是iscroll（包括新浪手机页，百度等）<br>我一开始也使用，不过自从用了<code>-webkit-overflow-scrolling: touch;</code>样式后，就完全可以抛弃第三方类库了，把它加在body{}区域，所有的overflow需要滚动的都可以生效了。</p>
<p>另外有一篇比较全的移动端点击解决方案 <a href="http://www.zhihu.com/question/28979857" target="_blank" rel="noopener">http://www.zhihu.com/question/28979857</a></p>
<h3 id="锁定-viewport"><a href="#锁定-viewport" class="headerlink" title="锁定 viewport"></a>锁定 viewport</h3><pre><code>ontouchmove=&quot;event.preventDefault()&quot; //锁定viewport，任何屏幕操作不移动用户界面（弹出键盘除外）。</code></pre><h3 id="利用-Media-Query监听"><a href="#利用-Media-Query监听" class="headerlink" title="利用 Media Query监听"></a>利用 Media Query监听</h3><p>Media Query 相信大部分人已经使用过了。其实 JavaScript可以配合 Media Query这么用：</p>
<pre><code>var mql = window.matchMedia(&quot;(orientation: portrait)&quot;);
mql.addListener(handleOrientationChange);
handleOrientationChange(mql); 
function handleOrientationChange(mql) {
  if (mql.matches) {
    alert(&apos;The device is currently in portrait orientation &apos;)
  } else {
    alert(&apos;The device is currently in landscape orientation&apos;)
  }}</code></pre><p>借助了 Media Query 接口做的事件监听，所以很强大！</p>
<p>也可以通过获取 CSS 值来使用 Media Query 判断设备情况，详情请看：<a href="http://yujiangshui.com/use-javascript-css-media-queries-detect-device-state/" target="_blank" rel="noopener">JavaScript 依据 CSS Media Queries 判断设备的方法</a>。</p>
<h3 id="rem最佳实践"><a href="#rem最佳实践" class="headerlink" title="rem最佳实践"></a>rem最佳实践</h3><p>rem是非常好用的一个属性，可以根据html来设定基准值，而且兼容性也很不错。不过有的时候还是需要对一些莫名其妙的浏览器优雅降级。以下是两个实践</p>
<ol>
<li><a href="http://jsbin.com/vaqexuge/4/edit" target="_blank" rel="noopener">http://jsbin.com/vaqexuge/4/edit</a>  这有个demo，发现chrome当font-size小于12时，rem会按照12来计算。因此设置基准值要考虑这一点</li>
<li>可以用以下的代码片段保证在低端浏览器下也不会出问题</li>
</ol>
<pre><code>html { font-size: 62.5%; } 
body { font-size: 14px; font-size: 1.4rem; } /* =14px */
h1   { font-size: 24px; font-size: 2.4rem; } /* =24px */</code></pre><h3 id="被点击元素的外观变化，可以使用样式来设定："><a href="#被点击元素的外观变化，可以使用样式来设定：" class="headerlink" title="被点击元素的外观变化，可以使用样式来设定："></a>被点击元素的外观变化，可以使用样式来设定：</h3><pre><code>-webkit-tap-highlight-color: 颜色</code></pre><h3 id="检测判断-iPhone-iPod"><a href="#检测判断-iPhone-iPod" class="headerlink" title="检测判断 iPhone/iPod"></a>检测判断 iPhone/iPod</h3><p>开发特定设备的移动网站，首先要做的就是设备侦测了。下面是使用Javascript侦测iPhone/iPod的UA，然后转向到专属的URL。</p>
<pre><code>if((navigator.userAgent.match(/iPhone/i)) || (navigator.userAgent.match(/iPod/i))) {
　　if (document.cookie.indexOf(&quot;iphone_redirect=false&quot;) == -1) {
　　　　window.location = &quot;http://m.example.com&quot;;
　　}
}</code></pre><p>虽然Javascript是可以在水果设备上运行的，但是用户还是可以禁用。它也会造成客户端刷新和额外的数据传输，所以下面是服务器端侦测和转向：</p>
<pre><code>if(strstr($_SERVER[&apos;HTTP_USER_AGENT&apos;],&apos;iPhone&apos;) || strstr($_SERVER[&apos;HTTP_USER_AGENT&apos;],&apos;iPod&apos;)) {
　　header(&apos;Location: http://yoursite.com/iphone&apos;);
　　exit();
}</code></pre><h3 id="阻止旋转屏幕时自动调整字体大小"><a href="#阻止旋转屏幕时自动调整字体大小" class="headerlink" title="阻止旋转屏幕时自动调整字体大小"></a>阻止旋转屏幕时自动调整字体大小</h3><pre><code>html, body, form, fieldset, p, div, h1, h2, h3, h4, h5, h6 {-webkit-text-size-adjust:none;}</code></pre><h3 id="模拟-hover伪类"><a href="#模拟-hover伪类" class="headerlink" title="模拟:hover伪类"></a>模拟:hover伪类</h3><p>因为iPhone并没有鼠标指针，所以没有hover事件。那么CSS :hover伪类就没用了。但是iPhone有Touch事件，onTouchStart 类似 onMouseOver，onTouchEnd 类似 onMouseOut。所以我们可以用它来模拟hover。使用Javascript：</p>
<pre><code>var myLinks = document.getElementsByTagName(&apos;a&apos;);
for(var i = 0; i &lt; myLinks.length; i++){
　　myLinks[i].addEventListener(’touchstart’, function(){this.className = “hover”;}, false);
　　myLinks[i].addEventListener(’touchend’, function(){this.className = “”;}, false);
}</code></pre><p>然后用CSS增加hover效果：</p>
<pre><code>a:hover, a.hover { /* 你的hover效果 */ }</code></pre><p>这样设计一个链接，感觉可以更像按钮。并且，这个模拟可以用在任何元素上。</p>
<h3 id="Flexbox-布局"><a href="#Flexbox-布局" class="headerlink" title="Flexbox 布局"></a>Flexbox 布局</h3><p> <a href="http://jsbin.com/ibuwol/2/edit" title="article5" target="_blank" rel="noopener">Flex 模板和实例</a></p>
<p> <a href="http://www.w3cplus.com/blog/666.html" title="article6" target="_blank" rel="noopener">深入了解 Flexbox 伸缩盒模型</a></p>
<p> <a href="http://yehao.diandian.com/post/2013-09-15/40052216426" target="_blank" rel="noopener">CSS Flexbox Intro</a></p>
<p><a href="http://www.w3.org/TR/css3-flexbox/" target="_blank" rel="noopener">http://www.w3.org/TR/css3-flexbox/</a></p>
<h3 id="居中问题"><a href="#居中问题" class="headerlink" title="居中问题"></a>居中问题</h3><p>居中是移动端跟pc端共同的噩梦。这里有两种兼容性比较好的新方案。</p>
<ul>
<li><p>table布局法</p>
<p>  .box{</p>
<pre><code>text-align:center; 
display:table-cell;
vertical-align:middle;</code></pre><p>  }</p>
</li>
<li><p>老版本flex布局法</p>
<p>  .box{</p>
<pre><code>display:-webkit-box;
-webkit-box-pack: center;
-webkit-box-align: center; 
text-align:center;</code></pre><p>  }</p>
</li>
</ul>
<p>以上两种其实分别是retchat跟ionic的布局基石。</p>
<p>这里有更详细的更多的选择<a href="http://www.zhouwenbin.com/%E5%9E%82%E7%9B%B4%E5%B1%85%E4%B8%AD%E7%9A%84%E5%87%A0%E7%A7%8D%E6%96%B9%E6%B3%95/" target="_blank" rel="noopener">http://www.zhouwenbin.com/%E5%9E%82%E7%9B%B4%E5%B1%85%E4%B8%AD%E7%9A%84%E5%87%A0%E7%A7%8D%E6%96%B9%E6%B3%95/</a> 来自周文彬的博客</p>
<h3 id="移动端实现标题文字截断"><a href="#移动端实现标题文字截断" class="headerlink" title="移动端实现标题文字截断"></a>移动端实现标题文字截断</h3><p><a href="http://www.75team.com/archives/611" target="_blank" rel="noopener">http://www.75team.com/archives/611</a></p>
<h3 id="处理-Retina-双倍屏幕"><a href="#处理-Retina-双倍屏幕" class="headerlink" title="处理 Retina 双倍屏幕"></a>处理 Retina 双倍屏幕</h3><p> <a href="http://miekd.com/articles/using-css-sprites-to-optimize-your-website-for-retina-displays/" title="article5" target="_blank" rel="noopener">（经典）Using CSS Sprites to optimize your website for Retina Displays</a></p>
<p> <a href="http://www.w3cplus.com/css/css-background-size-graphics.html" title="article5" target="_blank" rel="noopener">使用CSS3的background-size优化苹果的Retina屏幕的图像显示</a> </p>
<p> <a href="http://www.w3cplus.com/css/using-css-sprites-to-optimize-your-website-for-retina-displays.html" title="article5" target="_blank" rel="noopener">使用 CSS sprites 来优化你的网站在 Retina 屏幕下显示</a> </p>
<p> <a href="http://alexthorpe.com/uncategorized/css-sprites-for-retina-display-devices/683/" title="article5" target="_blank" rel="noopener">（案例）CSS IMAGE SPRITES FOR RETINA (HIRES) DEVICES</a> </p>
<h3 id="input类型为date情况下不支持placeholder（来自于江水）"><a href="#input类型为date情况下不支持placeholder（来自于江水）" class="headerlink" title="input类型为date情况下不支持placeholder（来自于江水）"></a>input类型为date情况下不支持placeholder（来自于江水）</h3><p>这其实是浏览器自己的处理。因为浏览器会针对此类型 input 增加 datepicker 模块。</p>
<p>对 input type date 使用 placeholder 的目的是为了让用户更准确的输入日期格式，iOS 上会有 datepicker 不会显示 placeholder 文字，但是为了统一表单外观，往往需要显示。Android 部分机型没有 datepicker 也不会显示 placeholder 文字。</p>
<p>桌面端（Mac）</p>
<ul>
<li>Safari 不支持 datepicker，placeholder 正常显示。</li>
<li>Firefox 不支持 datepicker，placeholder 正常显示。</li>
<li>Chrome 支持 datepicker，显示 年、月、日 格式，忽略 placeholder。</li>
</ul>
<p>移动端</p>
<ul>
<li>iPhone5 iOS7 有 datepicker 功能，但是不显示 placeholder。</li>
<li>Andorid 4.0.4 无 datepicker 功能，不显示 placeholder</li>
</ul>
<p>解决方法：</p>
<pre><code>&lt;input placeholder=&quot;Date&quot; class=&quot;textbox-n&quot; type=&quot;text&quot; onfocus=&quot;(this.type=&apos;date&apos;)&quot;  id=&quot;date&quot;&gt; </code></pre><p>因为text是支持placeholder的。因此当用户focus的时候自动把type类型改变为date，这样既有placeholder也有datepicker了</p>
<h3 id="判断照片的横竖排列"><a href="#判断照片的横竖排列" class="headerlink" title="判断照片的横竖排列"></a>判断照片的横竖排列</h3><p>有这样一种需求，需要判断用户照片是横着拍出来的还是竖着拍出来的，这里需要使用照片得exif信息：</p>
<pre><code>$(&quot;input&quot;).change(function() {
    var file = this.files[0];
    fr   = new FileReader;

    fr.onloadend = function() {
        var exif = EXIF.readFromBinaryFile(new BinaryFile(this.result));
        alert(exif.Orientation);
    };

    fr.readAsBinaryString(file);
});</code></pre><p>可以使用这两个库 来取exif信息<a href="http://www.nihilogic.dk/labs/binaryajax/binaryajax.js" target="_blank" rel="noopener">http://www.nihilogic.dk/labs/binaryajax/binaryajax.js</a> <a href="http://www.nihilogic.dk/labs/exif/exif.js" target="_blank" rel="noopener">http://www.nihilogic.dk/labs/exif/exif.js</a></p>
<h3 id="Android上当viewport的width大于device-width时出现文字无故折行的解决办法"><a href="#Android上当viewport的width大于device-width时出现文字无故折行的解决办法" class="headerlink" title="Android上当viewport的width大于device-width时出现文字无故折行的解决办法"></a>Android上当viewport的width大于device-width时出现文字无故折行的解决办法</h3><p><a href="http://www.iunbug.com/archives/2013/04/23/798.html" target="_blank" rel="noopener">http://www.iunbug.com/archives/2013/04/23/798.html</a></p>
<p>###白屏解决与优化方案</p>
<p>当前很多无线页面都使用前端模板进行数据渲染，那么在糟糕的网速情况下，一进去页面，看到的不是白屏就是 loading，这成为白屏问题。</p>
<p>此问题发生的原因基本可以归结为网速跟静态资源 </p>
<p>1、css文件加载需要一些时间，在加载的过程中页面是空白的。 解决：可以考虑将css代码前置和内联。<br>2、首屏无实际的数据内容，等待异步加载数据再渲染页面导致白屏。 解决：在首屏直接同步渲染html，后续的滚屏等再采用异步请求数据和渲染html。<br>3、首屏内联js的执行会阻塞页面的渲染。 解决：尽量不在首屏html代码中放置内联脚本。（来自翔歌）</p>
<p>解决方案</p>
<p>根本原因是客户端渲染的无力，因此最简单的方法是在服务器端，使用模板引擎渲染所有页面。同时</p>
<p> 1减少文件加载体积，如html压缩，js压缩<br> 2加快js执行速度 比如常见的无限滚动的页面，可以使用js先渲染一个屏幕范围内的东西<br> 3提供一些友好的交互，比如提供一些假的滚动条<br> 4使用本地存储处理静态文件。</p>
<h3 id="如何实现打开已安装的app，若未安装则引导用户安装"><a href="#如何实现打开已安装的app，若未安装则引导用户安装" class="headerlink" title="如何实现打开已安装的app，若未安装则引导用户安装?"></a>如何实现打开已安装的app，若未安装则引导用户安装?</h3><p>来自 <a href="http://gallery.kissyui.com/redirectToNative/1.2/guide/index.html" target="_blank" rel="noopener">http://gallery.kissyui.com/redirectToNative/1.2/guide/index.html</a> kissy mobile<br>通过iframe src发送请求打开app自定义url scheme，如taobao://home（淘宝首页） 、etao://scan（一淘扫描）);<br>如果安装了客户端则会直接唤起，直接唤起后，之前浏览器窗口（或者扫码工具的webview）推入后台；<br>如果在指定的时间内客户端没有被唤起，则js重定向到app下载地址。<br>大概实现代码如下    </p>
<pre><code>goToNative:function(){

    if(!body) {
            setTimeout(function(){
                doc.body.appendChild(iframe);
            }, 0);
        } else {
            body.appendChild(iframe);
        }

setTimeout(function() {
            doc.body.removeChild(iframe);
            gotoDownload(startTime);//去下载，下载链接一般是itunes app store或者apk文件链接
            /**
             * 测试时间设置小于800ms时，在android下的UC浏览器会打开native app时并下载apk，
             * 测试android+UC下打开native的时间最好大于800ms;
             */
        }, 800);
}</code></pre><p>需要注意的是 如果是android chrome 25版本以后，在iframe src不会发送请求，<br>原因如下<a href="https://developers.google.com/chrome/mobile/docs/intents" target="_blank" rel="noopener">https://developers.google.com/chrome/mobile/docs/intents</a> ，通过location href使用intent机制拉起客户端可行并且当前页面不跳转。</p>
<pre><code>window.location = &apos;intent://&apos; + schemeUrl + &apos;#Intent;scheme=&apos; + scheme + &apos;;package=&apos; + self.package + &apos;;end&apos;;</code></pre><p>补充一个来自三水清的详细讲解 <a href="http://js8.in/2013/12/16/ios%E4%BD%BF%E7%94%A8schema%E5%8D%8F%E8%AE%AE%E8%B0%83%E8%B5%B7app/" target="_blank" rel="noopener">http://js8.in/2013/12/16/ios%E4%BD%BF%E7%94%A8schema%E5%8D%8F%E8%AE%AE%E8%B0%83%E8%B5%B7app/</a></p>
<h3 id="active的兼容-来自薛端阳"><a href="#active的兼容-来自薛端阳" class="headerlink" title="active的兼容(来自薛端阳)"></a>active的兼容(来自薛端阳)</h3><p>今天发现，要让a链接的CSS active伪类生效，只需要给这个a链接的touch系列的任意事件touchstart/touchend绑定一个空的匿名方法即可hack成功</p>
<pre><code>&lt;style&gt;
a {
color: #000;
}
a:active {
color: #fff;
}
&lt;/style&gt;
&lt;a herf=”asdasd”&gt;asdasd&lt;/a&gt;
&lt;script&gt;
var a=document.getElementsByTagName(‘a’);
for(var i=0;i&lt;a.length;i++){
a[i].addEventListener(‘touchstart’,function(){},false);
}
&lt;/script&gt;</code></pre><h3 id="消除transition闪屏"><a href="#消除transition闪屏" class="headerlink" title="消除transition闪屏"></a>消除transition闪屏</h3><p>两个方法：使用css3动画的时尽量利用3D加速，从而使得动画变得流畅。动画过程中的动画闪白可以通过 backface-visibility 隐藏。</p>
<pre><code>-webkit-transform-style: preserve-3d;
/*设置内嵌的元素在 3D 空间如何呈现：保留 3D*/
-webkit-backface-visibility: hidden;
/*（设置进行转换的元素的背面在面对用户时是否可见：隐藏）*/</code></pre><h3 id="测试是否支持svg图片"><a href="#测试是否支持svg图片" class="headerlink" title="测试是否支持svg图片"></a>测试是否支持svg图片</h3><pre><code>document.implementation.hasFeature(&quot;http:// www.w3.org/TR/SVG11/feature#Image&quot;, &quot;1.1&quot;)</code></pre><h2 id="考虑兼容“隐私模式”"><a href="#考虑兼容“隐私模式”" class="headerlink" title="考虑兼容“隐私模式”"></a>考虑兼容“隐私模式”</h2><p>(from <a href="http://blog.youyo.name/archives/smarty-phones-webapp-deverlop-advance.html" target="_blank" rel="noopener">http://blog.youyo.name/archives/smarty-phones-webapp-deverlop-advance.html</a>)<br>ios的safari提供一种“隐私模式”，如果你的webapp考虑兼容这个模式，那么在使用html5的本地存储的一种————localStorage时，可能因为“隐私模式”下没有权限读写localstorge而使代码抛出错误，导致后续的js代码都无法运行了。</p>
<p>既然在safari的“隐私模式”下，没有调用localStorage的权限，首先想到的是先判断是否支持localStorage，代码如下：</p>
<pre><code>if(&apos;localStorage&apos; in window){
    //需要使用localStorage的代码写在这
}else{
    //不支持的提示和向下兼容代码
}</code></pre><p>测试发现，即使在safari的“隐私模式”下，’localStorage’ in window的返回值依然为true，也就是说，if代码块内部的代码依然会运行，问题没有得到解决。<br>接下来只能相当使用try catch了，虽然这是一个不太推荐被使用的方法，使用try catch捕获错误，使后续的js代码可以继续运行，代码如下：</p>
<pre><code>try{
    if(&apos;localStorage&apos; in window){
         //需要使用localStorage的代码写在这
    }else{
         //不支持的提示和向下兼容代码
    }
}catch(e){
    // 隐私模式相关提示代码和不支持的提示和向下兼容代码
}</code></pre><p>所以，提醒大家注意，在需要兼容ios的safari的“隐私模式”的情况下，本地存储相关的代码需要使用try catch包裹并降级兼容。</p>
<h3 id="安卓手机点击锁定页面效果问题"><a href="#安卓手机点击锁定页面效果问题" class="headerlink" title="安卓手机点击锁定页面效果问题"></a>安卓手机点击锁定页面效果问题</h3><p>有些安卓手机，页面点击时会停止页面的javascript，css3动画等的执行，这个比较蛋疼。不过可以用阻止默认事件解决。详细见<br><a href="http://stackoverflow.com/questions/10246305/android-browser-touch-events-stop-display-being-updated-inc-canvas-elements-h" target="_blank" rel="noopener">http://stackoverflow.com/questions/10246305/android-browser-touch-events-stop-display-being-updated-inc-canvas-elements-h</a></p>
<pre><code>function touchHandlerDummy(e)
{
    e.preventDefault();
    return false;
}
document.addEventListener(&quot;touchstart&quot;, touchHandlerDummy, false);
document.addEventListener(&quot;touchmove&quot;, touchHandlerDummy, false);
document.addEventListener(&quot;touchend&quot;, touchHandlerDummy, false);</code></pre><h3 id="消除ie10里面的那个叉号"><a href="#消除ie10里面的那个叉号" class="headerlink" title="消除ie10里面的那个叉号"></a>消除ie10里面的那个叉号</h3><p><a href="http://msdn.microsoft.com/en-us/library/windows/apps/hh767361.aspx" title="article4" target="_blank" rel="noopener">IE Pseudo-elements</a></p>
<pre><code>input:-ms-clear{display:none;}</code></pre><h3 id="关于ios与os端字体的优化-横竖屏会出现字体加粗不一致等"><a href="#关于ios与os端字体的优化-横竖屏会出现字体加粗不一致等" class="headerlink" title="关于ios与os端字体的优化(横竖屏会出现字体加粗不一致等)"></a>关于ios与os端字体的优化(横竖屏会出现字体加粗不一致等)</h3><p><a href="http://blog.sina.com.cn/s/blog_6da647a601011u4v.html" title="article5" target="_blank" rel="noopener">mac下网页中文字体优化</a></p>
<p><a href="http://stackoverflow.com/questions/3220662/uiwebview-font-is-thinner-in-portrait-than-landscape" title="article5" target="_blank" rel="noopener">UIWebView font is thinner in portrait than landscape</a></p>
<h2 id="判断用户是否是“将网页添加到主屏后，再从主屏幕打开这个网页”的"><a href="#判断用户是否是“将网页添加到主屏后，再从主屏幕打开这个网页”的" class="headerlink" title="判断用户是否是“将网页添加到主屏后，再从主屏幕打开这个网页”的"></a>判断用户是否是“将网页添加到主屏后，再从主屏幕打开这个网页”的</h2><pre><code>navigator.standalone</code></pre><h3 id="隐藏地址栏-amp-处理事件的时候，防止滚动条出现："><a href="#隐藏地址栏-amp-处理事件的时候，防止滚动条出现：" class="headerlink" title="隐藏地址栏 &amp; 处理事件的时候，防止滚动条出现："></a>隐藏地址栏 &amp; 处理事件的时候，防止滚动条出现：</h3><pre><code>// 隐藏地址栏  &amp; 处理事件的时候 ，防止滚动条出现
addEventListener(&apos;load&apos;, function(){
        setTimeout(function(){ window.scrollTo(0, 1); }, 100);
});</code></pre><h3 id="ios7-可以通过meta标签的minimal来隐藏地址栏了"><a href="#ios7-可以通过meta标签的minimal来隐藏地址栏了" class="headerlink" title="ios7 可以通过meta标签的minimal来隐藏地址栏了"></a>ios7 可以通过meta标签的minimal来隐藏地址栏了</h3><p><a href="http://darkblue.sdf.org/weblog/ios-7-dot-1-mobile-safari-minimal-ui.html" target="_blank" rel="noopener">http://darkblue.sdf.org/weblog/ios-7-dot-1-mobile-safari-minimal-ui.html</a></p>
<h3 id="判断是否为iPhone："><a href="#判断是否为iPhone：" class="headerlink" title="判断是否为iPhone："></a>判断是否为iPhone：</h3><pre><code>// 判断是否为 iPhone ：
function isAppleMobile() {
    return (navigator.platform.indexOf(&apos;iPhone&apos;) != -1);
};</code></pre><h3 id="localStorage"><a href="#localStorage" class="headerlink" title="localStorage:"></a>localStorage:</h3><pre><code>var v = localStorage.getItem(&apos;n&apos;) ? localStorage.getItem(&apos;n&apos;) : &quot;&quot;;   // 如果名称是  n 的数据存在 ，则将其读出 ，赋予变量  v  。
localStorage.setItem(&apos;n&apos;, v);                                           // 写入名称为 n、值为  v  的数据
localStorage.removeItem(&apos;n&apos;);        // 删除名称为  n  的数据</code></pre><h3 id="使用特殊链接："><a href="#使用特殊链接：" class="headerlink" title="使用特殊链接："></a>使用特殊链接：</h3><p>如果你关闭自动识别后 ，又希望某些电话号码能够链接到 iPhone 的拨号功能 ，那么可以通过这样来声明电话链接 ,</p>
<pre><code>&lt;a href=&quot;tel:12345654321&quot;&gt;打电话给我&lt;/a&gt;
&lt;a href=&quot;sms:12345654321&quot;&gt;发短信&lt;/a&gt;</code></pre><p>或用于单元格：</p>
<pre><code>&lt;td onclick=&quot;location.href=&apos;tel:122&apos;&quot;&gt;</code></pre><h3 id="自动大写与自动修正"><a href="#自动大写与自动修正" class="headerlink" title="自动大写与自动修正"></a>自动大写与自动修正</h3><p>要关闭这两项功能，可以通过autocapitalize 与autocorrect 这两个选项：</p>
<pre><code>&lt;input type=&quot;text&quot; autocapitalize=&quot;off&quot; autocorrect=&quot;off&quot; /&gt;</code></pre><h3 id="不让-Android-识别邮箱"><a href="#不让-Android-识别邮箱" class="headerlink" title="不让 Android 识别邮箱"></a>不让 Android 识别邮箱</h3><pre><code>&lt;meta content=&quot;email=no&quot; name=&quot;format-detection&quot; /&gt;</code></pre><h3 id="禁止-iOS-弹出各种操作窗口"><a href="#禁止-iOS-弹出各种操作窗口" class="headerlink" title="禁止 iOS 弹出各种操作窗口"></a>禁止 iOS 弹出各种操作窗口</h3><pre><code>-webkit-touch-callout:none</code></pre><h3 id="禁止用户选中文字"><a href="#禁止用户选中文字" class="headerlink" title="禁止用户选中文字"></a>禁止用户选中文字</h3><pre><code>-webkit-user-select:none</code></pre><h3 id="动画效果中，使用-translate-比使用定位性能高"><a href="#动画效果中，使用-translate-比使用定位性能高" class="headerlink" title="动画效果中，使用 translate 比使用定位性能高"></a>动画效果中，使用 translate 比使用定位性能高</h3><p><a href="http://paulirish.com/2012/why-moving-elements-with-translate-is-better-than-posabs-topleft/" target="_blank" rel="noopener">Why Moving Elements With Translate() Is Better Than Pos:abs Top/left</a></p>
<h3 id="拿到滚动条"><a href="#拿到滚动条" class="headerlink" title="拿到滚动条"></a>拿到滚动条</h3><pre><code>window.scrollY
window.scrollX</code></pre><p> 比如要绑定一个touchmove的事件，正常的情况下类似这样(来自呼吸二氧化碳)</p>
<pre><code>$(&apos;div&apos;).on(&apos;touchmove&apos;, function(){
//.….code
{});</code></pre><p>而如果中间的code需要处理的东西多的话，fps就会下降影响程序顺滑度，而如果改成这样</p>
<pre><code>$(&apos;div&apos;).on(&apos;touchmove&apos;, function(){
setTimeout(function(){
//.….code
},0);
{});</code></pre><p>把代码放在setTimeout中，会发现程序变快.</p>
<h3 id="关于-iOS-系统中，Web-APP-启动图片在不同设备上的适应性设置"><a href="#关于-iOS-系统中，Web-APP-启动图片在不同设备上的适应性设置" class="headerlink" title="关于 iOS 系统中，Web APP 启动图片在不同设备上的适应性设置"></a>关于 iOS 系统中，Web APP 启动图片在不同设备上的适应性设置</h3><p><a href="http://stackoverflow.com/questions/4687698/mulitple-apple-touch-startup-image-resolutions-for-ios-web-app-esp-for-ipad/10011893#10011893" target="_blank" rel="noopener">http://stackoverflow.com/questions/4687698/mulitple-apple-touch-startup-image-resolutions-for-ios-web-app-esp-for-ipad/10011893#10011893</a></p>
<h3 id="position-sticky与position-fixed布局"><a href="#position-sticky与position-fixed布局" class="headerlink" title="position:sticky与position:fixed布局"></a>position:sticky与position:fixed布局</h3><p><a href="http://www.zhouwenbin.com/positionsticky-%E7%B2%98%E6%80%A7%E5%B8%83%E5%B1%80/" target="_blank" rel="noopener">http://www.zhouwenbin.com/positionsticky-%E7%B2%98%E6%80%A7%E5%B8%83%E5%B1%80/</a><br><a href="http://www.zhouwenbin.com/sticky%E6%A8%A1%E6%8B%9F%E9%97%AE%E9%A2%98/" target="_blank" rel="noopener">http://www.zhouwenbin.com/sticky%E6%A8%A1%E6%8B%9F%E9%97%AE%E9%A2%98/</a></p>
<h3 id="关于-iOS-系统中，中文输入法输入英文时，字母之间可能会出现一个六分之一空格"><a href="#关于-iOS-系统中，中文输入法输入英文时，字母之间可能会出现一个六分之一空格" class="headerlink" title="关于 iOS 系统中，中文输入法输入英文时，字母之间可能会出现一个六分之一空格"></a>关于 iOS 系统中，中文输入法输入英文时，字母之间可能会出现一个六分之一空格</h3><p>可以通过正则去掉 </p>
<pre><code>this.value = this.value.replace(/\u2006/g, &apos;&apos;);</code></pre><h3 id="关于android-webview中，input元素输入时出现的怪异情况"><a href="#关于android-webview中，input元素输入时出现的怪异情况" class="headerlink" title="关于android webview中，input元素输入时出现的怪异情况"></a>关于android webview中，input元素输入时出现的怪异情况</h3><p>见下图</p>
<p><img src="http://cdn.bielousov.com/wp-content/uploads/2012/08/android-input-label-text-issue.png" alt="怪异图"></p>
<p>Android Web 视图,至少在 HTC EVO 和三星的 Galaxy Nexus 中，文本输入框在输入时表现的就像占位符。情况为一个类似水印的东西在用户输入区域，一旦用户开始输入便会消失(见图片)。</p>
<p>在 Android 的默认样式下当输入框获得焦点后，若存在一个绝对定位或者 fixed 的元素，布局会被破坏，其他元素与系统输入字段会发生重叠(如搜索图标将消失为搜索字段)，可以观察到布局与原始输入字段有偏差(见截图)。</p>
<p>这是一个相当复杂的问题，以下简单布局可以重现这个问题:</p>
<pre><code>&lt;label for=&quot;phone&quot;&gt;Phone: *&lt;/label&gt;
&lt;input type=&quot;tel&quot; name=&quot;phone&quot; id=&quot;phone&quot; minlength=&quot;10&quot; maxlength=&quot;10&quot; inputmode=&quot;latin digits&quot; required=&quot;required&quot; /&gt;</code></pre><p>解决方法</p>
<pre><code>-webkit-user-modify: read-write-plaintext-only</code></pre><p>详细参考<a href="http://www.bielousov.com/2012/android-label-text-appears-in-input-field-as-a-placeholder/" target="_blank" rel="noopener">http://www.bielousov.com/2012/android-label-text-appears-in-input-field-as-a-placeholder/</a><br>注意，该属性会导致中文不能输入词组，只能单个字。感谢鬼哥与飞（游勇飞）贡献此问题与解决方案</p>
<p>另外，在position:fixed后的元素里，尽量不要使用输入框。更多的bug可参考<br><a href="http://www.cosdiv.com/page/M0/S882/882353.html" target="_blank" rel="noopener">http://www.cosdiv.com/page/M0/S882/882353.html</a></p>
<p>依旧无法解决（摩托罗拉ME863手机），则使用input:text类型而非password类型，并设置其设置  -webkit-text-security: disc; 隐藏输入密码从而解决。</p>
<h3 id="JS动态生成的select下拉菜单在Android2-x版本的默认浏览器里不起作用"><a href="#JS动态生成的select下拉菜单在Android2-x版本的默认浏览器里不起作用" class="headerlink" title="JS动态生成的select下拉菜单在Android2.x版本的默认浏览器里不起作用"></a>JS动态生成的select下拉菜单在Android2.x版本的默认浏览器里不起作用</h3><p>解决方法删除了overflow-x:hidden; 然后在JS生成下来菜单之后focus聚焦，这两步操作之后解决了问题。(来自岛都-小Qi)</p>
<p>参考<a href="http://stackoverflow.com/questions/4697908/html-select-control-disabled-in-android-webview-in-emulator" target="_blank" rel="noopener">http://stackoverflow.com/questions/4697908/html-select-control-disabled-in-android-webview-in-emulator</a></p>
<h3 id="Andriod-上去掉语音输入按钮"><a href="#Andriod-上去掉语音输入按钮" class="headerlink" title="Andriod 上去掉语音输入按钮"></a>Andriod 上去掉语音输入按钮</h3><pre><code>input::-webkit-input-speech-button {display: none}</code></pre><h2 id="IE10-的特殊鼠标事件"><a href="#IE10-的特殊鼠标事件" class="headerlink" title="IE10 的特殊鼠标事件"></a>IE10 的特殊鼠标事件</h2><p><a href="http://www.mansonchor.com/blog/blog_detail_73.html" title="article5" target="_blank" rel="noopener">IE10 事件监听</a></p>
<h2 id="iOS-输入框最佳实践"><a href="#iOS-输入框最佳实践" class="headerlink" title="iOS 输入框最佳实践"></a>iOS 输入框最佳实践</h2><p><a href="http://stackoverflow.com/questions/11219242/mobile-friendly-input-of-a-digits-spaces-string-a-credit-card-number" target="_blank" rel="noopener">Mobile-friendly input of a digits + spaces string (a credit card number)</a></p>
<p><a href="http://stackoverflow.com/questions/8216278/html5-input-type-number-vs-tel" target="_blank" rel="noopener">HTML5 input type number vs tel</a></p>
<p><a href="http://stackoverflow.com/questions/6178556/iphone-numeric-keyboard-for-text-input" target="_blank" rel="noopener">iPhone: numeric keyboard for text input</a></p>
<p><a href="https://developer.apple.com/library/ios/documentation/StringsTextFonts/Conceptual/TextAndWebiPhoneOS/KeyboardManagement/KeyboardManagement.html" target="_blank" rel="noopener">Text Programming Guide for iOS - Managing the Keyboard</a></p>
<p><a href="http://www.miketaylr.com/code/input-type-attr.html" target="_blank" rel="noopener">HTML5 inputs and attribute support</a></p>
<h2 id="往返缓存问题"><a href="#往返缓存问题" class="headerlink" title="往返缓存问题"></a>往返缓存问题</h2><p>点击浏览器的回退，有时候不会自动执行js，特别是在mobilesafari中。这与<strong>往返缓存(bfcache)</strong>有关系。有很多hack的处理方法，可以参考</p>
<p><a href="http://stackoverflow.com/questions/24046/the-safari-back-button-problem" target="_blank" rel="noopener">http://stackoverflow.com/questions/24046/the-safari-back-button-problem</a></p>
<p><a href="http://stackoverflow.com/questions/11979156/mobile-safari-back-button" target="_blank" rel="noopener">http://stackoverflow.com/questions/11979156/mobile-safari-back-button</a></p>
<h2 id="不暂停的计时器（safari的进程冻结）"><a href="#不暂停的计时器（safari的进程冻结）" class="headerlink" title="不暂停的计时器（safari的进程冻结）"></a>不暂停的计时器（safari的进程冻结）</h2><p><a href="https://www.imququ.com/post/ios-none-freeze-timer.html" target="_blank" rel="noopener">https://www.imququ.com/post/ios-none-freeze-timer.html</a><br>或者可以用postmessage方式:<br>主页面:</p>
<pre><code>// 解决ios safari tab在后台会遭遇进程冻结问题
// http://www.apple.com/safari/#gallery-icloud-tabs
// Safari takes advantage of power-saving technologies such as App Nap, which puts background Safari tabs into a low-power state until you start using them again. In addition, Safari Power Saver conserves battery life by intelligently pausing web videos and other plug‑in content when they’re not front and center on the web pages you visit. All told, Safari on OS X Mavericks lets you browse up to an hour longer than with Chrome or Firefox.1
var work;
function startWorker() {
    if (typeof(Worker) !== &quot;undefined&quot;) {
        if (typeof(work) == &quot;undefined&quot;) {
            work = new Worker(&quot;/workers.js&quot;);
        }
        work.onmessage = function(event) {
            // document.getElementById(&quot;result-count&quot;).innerHTML = event.data.count;
            // document.getElementById(&quot;result-url&quot;).innerHTML = event.data.targetURL;
            if (target &amp;&amp; event.data.targetURL != &quot;&quot;) target.location.href = event.data.targetURL;
        };
    } else {
        console.log(&apos;does not support Web Workers...&apos;);
    }
}

function stopWorker() {
    work.terminate();
}

startWorker();</code></pre><p>worker:</p>
<pre><code>// 解决ios safari tab在后台会遭遇进程冻结问题
// http://www.apple.com/safari/#gallery-icloud-tabs
// Safari takes advantage of power-saving technologies such as App Nap, which puts background Safari tabs into a low-power state until you start using them again. In addition, Safari Power Saver conserves battery life by intelligently pausing web videos and other plug‑in content when they’re not front and center on the web pages you visit. All told, Safari on OS X Mavericks lets you browse up to an hour longer than with Chrome or Firefox.1

importScripts(&apos;/socket.io/socket.io.js&apos;);

var count = 0,
    targetURL = &apos;&apos;
    ; 

var socket = io.connect(&apos;/&apos;);
socket.on(&apos;navigate&apos;, function (data) {
  count = count++;
  postMessage({targetURL:data.url,count:count});
});</code></pre><h2 id="Web移动端Fixed布局的解决方案"><a href="#Web移动端Fixed布局的解决方案" class="headerlink" title="Web移动端Fixed布局的解决方案"></a>Web移动端Fixed布局的解决方案</h2><p><a href="http://efe.baidu.com/blog/mobile-fixed-layout/" target="_blank" rel="noopener">http://efe.baidu.com/blog/mobile-fixed-layout/</a></p>
<h2 id="ios上background-attachment-fixed不能正常工作"><a href="#ios上background-attachment-fixed不能正常工作" class="headerlink" title="ios上background-attachment:fixed不能正常工作"></a>ios上background-attachment:fixed不能正常工作</h2><p>参考 <a href="http://stackoverflow.com/questions/20443574/fixed-background-image-with-ios7" target="_blank" rel="noopener">http://stackoverflow.com/questions/20443574/fixed-background-image-with-ios7</a></p>
<h2 id="如何让音频跟视频在ios跟android上自动播放"><a href="#如何让音频跟视频在ios跟android上自动播放" class="headerlink" title="如何让音频跟视频在ios跟android上自动播放"></a>如何让音频跟视频在ios跟android上自动播放</h2><pre><code>&lt;audio autoplay &gt;&lt;source  src=&quot;audio/alarm1.mp3&quot; type=&quot;audio/mpeg&quot;&gt;&lt;/audio&gt;</code></pre><p>系统默认情况下 audio的autoplay属性是无法生效的，这也是手机为节省用户流量做的考虑。<br>如果必须要自动播放，有两种方式可以解决。</p>
<p>1.捕捉一次用户输入后，让音频加载，下次即可播放。</p>
<pre><code>//play and pause it once
document.addEventListener(&apos;touchstart&apos;, function () {
    document.getElementsByTagName(&apos;audio&apos;)[0].play();
    document.getElementsByTagName(&apos;audio&apos;)[0].pause();
});</code></pre><p>这种方法需要捕获一次用户的点击事件来促使音频跟视频加载。当加载后，你就可以用javascript控制音频的播放了，如调用audio.play()</p>
<p>2.利用iframe加载资源</p>
<pre><code>var ifr=document.createElement(&quot;iframe&quot;);
ifr.setAttribute(&apos;src&apos;, &quot;http://mysite.com/myvideo.mp4&quot;);
ifr.setAttribute(&apos;width&apos;, &apos;1px&apos;);
ifr.setAttribute(&apos;height&apos;, &apos;1px&apos;);
ifr.setAttribute(&apos;scrolling&apos;, &apos;no&apos;);
ifr.style.border=&quot;0px&quot;;
document.body.appendChild(ifr);</code></pre><p>这种方式其实跟第一种原理是一样的。当资源加载了你就可以控制播放了，但是这里使用iframe来加载，相当于直接触发资源加载。<br>注意，使用创建audio标签并让其加载的方式是不可行的。<br>慎用这种方法，会对用户造成很糟糕的影响。。</p>
<h2 id="iOS-6-跟-iPhone-5-的那些事"><a href="#iOS-6-跟-iPhone-5-的那些事" class="headerlink" title="iOS 6 跟 iPhone 5 的那些事"></a>iOS 6 跟 iPhone 5 的那些事</h2><h3 id="IP5-的媒体查询"><a href="#IP5-的媒体查询" class="headerlink" title="IP5 的媒体查询"></a>IP5 的媒体查询</h3><pre><code>@media (device-height: 568px) and (-webkit-min-device-pixel-ratio: 2) {

/* iPhone 5 or iPod Touch 5th generation */

}</code></pre><h3 id="使用媒体查询，提供不同的启动图片："><a href="#使用媒体查询，提供不同的启动图片：" class="headerlink" title="使用媒体查询，提供不同的启动图片："></a>使用媒体查询，提供不同的启动图片：</h3><pre><code>&lt;link href=&quot;startup-568h.png&quot; rel=&quot;apple-touch-startup-image&quot; media=&quot;(device-height: 568px)&quot;&gt;
&lt;link href=&quot;startup.png&quot; rel=&quot;apple-touch-startup-image&quot; sizes=&quot;640x920&quot; media=&quot;(device-height: 480px)&quot;&gt;</code></pre><h3 id="拍照上传"><a href="#拍照上传" class="headerlink" title="拍照上传"></a>拍照上传</h3><pre><code>&lt;input type=file accept=&quot;video/*&quot;&gt;
&lt;input type=file accept=&quot;image/*&quot;&gt;</code></pre><p>不支持其他类型的文件 ，如音频，Pages文档或PDF文件。 也没有getUserMedia摄像头的实时流媒体支持。</p>
<h3 id="可以使用的-HTML5-高级-api"><a href="#可以使用的-HTML5-高级-api" class="headerlink" title="可以使用的 HTML5 高级 api"></a>可以使用的 HTML5 高级 api</h3><ul>
<li>multipart POST 表单提交上传</li>
<li>XMLHttpRequest 2 AJAX 上传（甚至进度支持）</li>
<li>文件 API ，在 iOS 6 允许 JavaScript 直接读取的字节数和客户端操作文件。</li>
</ul>
<h3 id="智能应用程序横幅"><a href="#智能应用程序横幅" class="headerlink" title="智能应用程序横幅"></a>智能应用程序横幅</h3><p>有了智能应用程序横幅，当网站上有一个相关联的本机应用程序时，Safari浏览器可以显示一个横幅。 如果用户没有安装这个应用程序将显示“安装”按钮，或已经安装的显示“查看”按钮可打开它。</p>
<p>在 iTunes Link Maker 搜索我们的应用程序和应用程序ID。</p>
<pre><code>&lt;meta name=&quot;apple-itunes-app&quot; content=&quot;app-id=9999999&quot;&gt;</code></pre><p>可以使用 app-argument 提供字符串值，如果参加iTunes联盟计划，可以添加元标记数据 </p>
<pre><code>&lt;meta name=&quot;apple-itunes-app&quot; content=&quot;app-id=9999999, app-argument=xxxxxx&quot;&gt;

&lt;meta name=&quot;apple-itunes-app&quot; content=&quot;app-id=9999999, app-argument=xxxxxx, affiliate-data=partnerId=99&amp;siteID=XXXX&quot;&gt;</code></pre><p>横幅需要156像素（设备是312 hi-dpi）在顶部，直到用户在下方点击内容或关闭按钮，你的网站才会展现全部的高度。 它就像HTML的DOM对象，但它不是一个真正的DOM。 </p>
<p>CSS3 滤镜</p>
<pre><code>-webkit-filter: blur(5px) grayscale (.5) opacity(0.66) hue-rotate(100deg);</code></pre><p>交叉淡变</p>
<pre><code>background-image: -webkit-cross-fade(url(&quot;logo1.png&quot;), url(&quot;logo2.png&quot;), 50%);</code></pre><p>Safari中的全屏幕</p>
<p>除了chrome-less 主屏幕meta标签，现在的iPhone和iPod Touch（而不是在iPad）支持全屏幕模式的窗口。 没有办法强制全屏模式，它需要由用户启动（工具栏上的最后一个图标）。需要引导用户按下屏幕上的全屏图标来激活全屏效果。 可以使用onresize事件检测是否用户切换到全屏幕。</p>
<p>支持requestAnimationFrameAPI</p>
<p>支持image-set,retina屏幕的利器</p>
<pre><code>-webkit-image-set(url(low.png) 1x, url(hi.jpg) 2x)</code></pre><p>应用程序缓存限制增加至25MB。</p>
<p>Web View（pseudobrowsers，PhoneGap/Cordova应用程序，嵌入式浏览器） 上Javascript运行比Safari慢3.3倍（或者说，Nitro引擎在Safari浏览器是Web应用程序是3.3倍速度）。</p>
<p>autocomplete属性的输入遵循DOM规范</p>
<p>来自DOM4的Mutation Observers已经实现。 您可以使用WebKitMutationObserver构造器捕获DOM的变化</p>
<p>Safari不再总是对用 -webkit-transform:preserve-3d 的元素创建硬件加速</p>
<p>支持window.selection 的Selection API</p>
<p>Canvas更新 ：createImageData有一个参数，现在有两个新的功能做好准备，用webkitGetImageDataHD和webkitPutImageDataHD提供高分辨率图像 。</p>
<p>更新SVG处理器和事件构造函数</p>
<h2 id="IOS7的大更新"><a href="#IOS7的大更新" class="headerlink" title="IOS7的大更新"></a>IOS7的大更新</h2><p><a href="http://jinlong.github.io/blog/2013/09/23/safari-ios7-html5-problems-apis-review/#jtss-tsina" target="_blank" rel="noopener">iOS 7 的 Safari 和 HTML5：问题，变化和新 API</a>(张金龙翻译)</p>
<p><a href="http://www.sencha.com/blog/the-html5-scorecard-the-good-the-bad-and-the-ugly-in-ios7" title="ios7的一些bug" target="_blank" rel="noopener">iOS 7 的一些坑(英文)</a></p>
<p><a href="http://www.mobilexweb.com/blog/safari-ios7-html5-problems-apis-review" title="ios7的一些bug" target="_blank" rel="noopener">ios7的一些坑2(英文)</a></p>
<h2 id="webview相关"><a href="#webview相关" class="headerlink" title="webview相关"></a>webview相关</h2><h1 id="Cache开启和设置"><a href="#Cache开启和设置" class="headerlink" title="Cache开启和设置"></a>Cache开启和设置</h1><pre><code>browser.getSettings().setAppCacheEnabled(true);
browser.getSettings().setAppCachePath(&quot;/data/data/[com.packagename]/cache&quot;);
browser.getSettings().setAppCacheMaxSize(5*1024*1024); // 5MB</code></pre><h1 id="LocalStorage相关设置"><a href="#LocalStorage相关设置" class="headerlink" title="LocalStorage相关设置"></a>LocalStorage相关设置</h1><pre><code>browser.getSettings().setDatabaseEnabled(true);
browser.getSettings().setDomStorageEnabled(true);
String databasePath = browser.getContext().getDir(&quot;databases&quot;, Context.MODE_PRIVATE).getPath();
browser.getSettings().setDatabasePath(databasePath);//Android　webview的LocalStorage有个问题，关闭APP或者重启后，就清楚了，所以需要browser.getSettings().setDatabase相关的操作，把LocalStoarge存到DB中

myWebView.setWebChromeClient(new WebChromeClient(){
　　　 @Override
　　　 public void onExceededDatabaseQuota(String url, String databaseIdentifier, long currentQuota, long estimatedSize, long totalUsedQuota, WebStorage.QuotaUpdater quotaUpdater)
　　　 {
　　　　　　　 quotaUpdater.updateQuota(estimatedSize * 2);
　　　 }
}</code></pre><h1 id="浏览器自带缩放按钮取消显示"><a href="#浏览器自带缩放按钮取消显示" class="headerlink" title="浏览器自带缩放按钮取消显示"></a>浏览器自带缩放按钮取消显示</h1><pre><code>browser.getSettings().setBuiltInZoomControls(false);</code></pre><h1 id="几个比较好的实践"><a href="#几个比较好的实践" class="headerlink" title="几个比较好的实践"></a>几个比较好的实践</h1><p>使用localstorage缓存html</p>
<p>使用lazyload，还要记得lazyload占位图虽然小，但是最好能提前加载到缓存</p>
<p>延时加载执行js</p>
<p>主要原因就在于Android Webview的onPageFinished事件，Android端一般是用这个事件来标识页面加载完成并显示的，也就是说在此之前，会一直loading，但是Android的OnPageFinished事件会在Javascript脚本执行完成之后才会触发。如果在页面中使用JQuery，会在处理完DOM对象，执行完$(document).ready(function() {});事件自会后才会渲染并显示页面。</p>
<h3 id="manifest与缓存相关"><a href="#manifest与缓存相关" class="headerlink" title="manifest与缓存相关:"></a>manifest与缓存相关:</h3><p><a href="http://www.alloyteam.com/2013/12/web-cache-6-hybrid-app-tailored-cache/" target="_blank" rel="noopener">http://www.alloyteam.com/2013/12/web-cache-6-hybrid-app-tailored-cache/</a><br>相关解决方案<br><a href="http://mt.tencent.com/" target="_blank" rel="noopener">http://mt.tencent.com/</a></p>
<h2 id="移动端调适篇"><a href="#移动端调适篇" class="headerlink" title="移动端调适篇"></a>移动端调适篇</h2><h3 id="手机抓包与配host"><a href="#手机抓包与配host" class="headerlink" title="手机抓包与配host"></a>手机抓包与配host</h3><p>在PC上，我们可以很方便地配host，但是手机上如何配host，这是一个问题。</p>
<p>这里主要使用fiddler和远程代理，实现手机配host的操作，具体操作如下：</p>
<p>首先，保证PC和移动设备在同一个局域网下；</p>
<p>PC上开启fiddler，并在设置中勾选“allow remote computers to connect”</p>
<ol>
<li><p>首先，保证PC和移动设备在同一个局域网下；</p>
</li>
<li><p>PC上开启fiddler，并在设置中勾选“allow remote computers to connect”<br><img src="https://github.com/hoosin/mobile-web-favorites/raw/master/img/01.png" alt="fiddler"></p>
</li>
<li><p>手机上设置代理，代理IP为PC的IP地址，端口为8888（这是fiddler的默认端口）。通常手机上可以直接设置代理，如果没有，可以去下载一个叫ProxyDroid的APP来实现代理的设置。</p>
</li>
<li><p>此时你会发现，用手机上网，走的其实是PC上的fiddler，所有的请求包都会在fiddler中列出来，配合willow使用，即可实现配host，甚至是反向代理的操作。</p>
</li>
</ol>
<p>也可以用CCProxy之类软件，还有一种方法就是买一个随身wifi，然后手机连接就可以了！</p>
<h3 id="高级抓包"><a href="#高级抓包" class="headerlink" title="高级抓包"></a>高级抓包</h3><p><a href="http://danqingdani.blog.163.com/blog/static/1860941952012112353515306/?suggestedreading&wumii" title="iPhone上使用Burp Suite捕捉HTTPS通信包方法" target="_blank" rel="noopener">iPhone上使用Burp Suite捕捉HTTPS通信包方法</a></p>
<p><a href="http://danqingdani.blog.163.com/blog/static/1860941952012101331848980/" title="mobile app 通信分析方法小议（iOS/Android)" target="_blank" rel="noopener">mobile app 通信分析方法小议（iOS/Android)</a></p>
<p><a href="http://danqingdani.blog.163.com/blog/static/1860941952012111954741585/" title="实时抓取移动设备上的通信包(ADVsock2pipe+Wireshark+nc+tcpdump)" target="_blank" rel="noopener">实时抓取移动设备上的通信包(ADVsock2pipe+Wireshark+nc+tcpdump)</a></p>
<h3 id="静态资源缓存问题"><a href="#静态资源缓存问题" class="headerlink" title="静态资源缓存问题"></a>静态资源缓存问题</h3><p>一般用代理软件代理过来的静态资源可以设置nocache避免缓存，但是有的手机比较诡异，会一直缓存住css等资源文件。由于静态资源一般都是用版本号管理的，我们以charles为例子来处理这个问题</p>
<p>charles 选择静态的html页面文件-saveResponse。之后把这个文件保存一下，修改一下版本号。之后继续发请求，<br>刚才的html页面文件 右键选择 –map local 选择我们修改过版本号的html文件即ok。这其实也是fiddler远程映射并修改文件的一个应用场景。</p>
<h3 id="安卓模拟器和真机区别"><a href="#安卓模拟器和真机区别" class="headerlink" title="安卓模拟器和真机区别"></a>安卓模拟器和真机区别</h3><p><a href="http://www.farsight.com.cn/news/emb105.htm" target="_blank" rel="noopener">http://www.farsight.com.cn/news/emb105.htm</a></p>
<p><a href="http://testerhome.com/topics/388" target="_blank" rel="noopener">http://testerhome.com/topics/388</a></p>
<p><a href="http://www.cnblogs.com/zdz8207/archive/2012/01/30/2332436.html" target="_blank" rel="noopener">http://www.cnblogs.com/zdz8207/archive/2012/01/30/2332436.html</a></p>
<h2 id="移动浏览器篇"><a href="#移动浏览器篇" class="headerlink" title="移动浏览器篇"></a>移动浏览器篇</h2><h3 id="微信浏览器"><a href="#微信浏览器" class="headerlink" title="微信浏览器"></a>微信浏览器</h3><p>微信浏览器的各种bug汇总 （x5内核） <a href="http://www.qianduan.net/qqliu-lan-qi-x5nei-he-wen-ti-hui-zong/" target="_blank" rel="noopener">http://www.qianduan.net/qqliu-lan-qi-x5nei-he-wen-ti-hui-zong/</a></p>
<p>因为微信浏览器屏蔽了一部分链接图片，所以需要引导用户去打开新页面，可以用以下方式判断微信浏览器的ua</p>
<pre><code>function is_weixn(){
    var ua = navigator.userAgent.toLowerCase();
    if(ua.match(/MicroMessenger/i)==&quot;micromessenger&quot;) {
        return true;
    } else {
        return false;
    }
}</code></pre><p>后端判断也很简单，比如php</p>
<pre><code>function is_weixin(){
    if ( strpos($_SERVER[&apos;HTTP_USER_AGENT&apos;], &apos;MicroMessenger&apos;) !== false ) {
            return true;
    }  
    return false;
}</code></pre><p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/31" target="_blank" rel="noopener">https://github.com/maxzhang/maxzhang.github.com/issues/31</a> 微信浏览器踩坑，来自maxZhang <a href="https://github.com/maxzhang" target="_blank" rel="noopener">https://github.com/maxzhang</a></p>
<h3 id="【UC浏览器】video标签脱离文档流"><a href="#【UC浏览器】video标签脱离文档流" class="headerlink" title="【UC浏览器】video标签脱离文档流"></a>【UC浏览器】video标签脱离文档流</h3><p>场景：<video>标签的父元素(祖辈元素)设置transform样式后，<video>标签会脱离文档流。</video></video></p>
<p>测试环境：UC浏览器 8.7/8.6 + Android 2.3/4.0 。</p>
<p>Demo：<a href="http://t.cn/zj3xiyu" target="_blank" rel="noopener">http://t.cn/zj3xiyu</a></p>
<p>解决方案：不使用transform属性。translate用top、margin等属性替代。</p>
<h3 id="【UC浏览器】video标签总在最前"><a href="#【UC浏览器】video标签总在最前" class="headerlink" title="【UC浏览器】video标签总在最前"></a>【UC浏览器】video标签总在最前</h3><p>场景：<video>标签总是在最前（可以理解为video标签的z-index属性是Max）。</video></p>
<p>测试环境：UC浏览器 8.7/8.6 + Android 2.3/4.0 。</p>
<h3 id="【UC浏览器】position-fixed-属性在UC浏览器的奇葩现象"><a href="#【UC浏览器】position-fixed-属性在UC浏览器的奇葩现象" class="headerlink" title="【UC浏览器】position:fixed 属性在UC浏览器的奇葩现象"></a>【UC浏览器】position:fixed 属性在UC浏览器的奇葩现象</h3><p>场景：设置了position: fixed 的元素会遮挡z-index值更高的同辈元素。</p>
<p>　　　在8.6的版本,这个情况直接出现。</p>
<p>　　　在8.7之后的版本,当同辈元素的height大于713这个「神奇」的数值时,才会被遮挡。</p>
<p>测试环境：UC浏览器 8.8_beta/8.7/8.6 + Android 2.3/4.0 。</p>
<p>Demo：<a href="http://t.cn/zYLTSg6" target="_blank" rel="noopener">http://t.cn/zYLTSg6</a></p>
<h3 id="【QQ手机浏览器】不支持HttpOnly"><a href="#【QQ手机浏览器】不支持HttpOnly" class="headerlink" title="【QQ手机浏览器】不支持HttpOnly"></a>【QQ手机浏览器】不支持HttpOnly</h3><p>场景：带有HttpOnly属性的Cookie，在QQ手机浏览器版本从4.0开始失效。JavaScript可以直接读取设置了HttpOnly的Cookie值。</p>
<p>测试环境：QQ手机浏览器 4.0/4.1/4.2 + Android 4.0 。</p>
<h3 id="【MIUI原生浏览器】浏览器地址栏hash不改变"><a href="#【MIUI原生浏览器】浏览器地址栏hash不改变" class="headerlink" title="【MIUI原生浏览器】浏览器地址栏hash不改变"></a>【MIUI原生浏览器】浏览器地址栏hash不改变</h3><p>场景：location.hash 被赋值后，地址栏的地址不会改变。</p>
<p>　　　但实际上 location.href 已经更新了，通过JavaScript可以顺利获取到更新后的地址。</p>
<p>　　　虽然不影响正常访问，但用户无法将访问过程中改变hash后的地址存为书签。</p>
<p>测试环境：MIUI 4.0</p>
<h3 id="【Chrome-Mobile】fixed元素无法点击"><a href="#【Chrome-Mobile】fixed元素无法点击" class="headerlink" title="【Chrome Mobile】fixed元素无法点击"></a>【Chrome Mobile】fixed元素无法点击</h3><p> 场景：父元素设置position: fixed;</p>
<p>　　　子元素设置position: absolute;</p>
<p>　　　此时，如果父元素/子元素还设置了overflow: hidden 则出现“父元素遮挡该子元素“的bug。</p>
<p>　　　视觉(view)层并没有出现遮挡，只是无法触发绑定在该子元素上的事件。可理解为：「看到点不到」。</p>
<p>补充： 页面往下滚动，触发position: fixed;的特性时，才会出现这个bug，在最顶不会出现。</p>
<p>测试平台： 小米1S，Android4.0的Chrome18</p>
<p>demo： <a href="http://maplejan.sinaapp.com/demo/fixed_chromemobile.html" target="_blank" rel="noopener">http://maplejan.sinaapp.com/demo/fixed_chromemobile.html</a></p>
<p>解决办法： 把父元素和子元素的overflow: hidden去掉。</p>
<p>以上来源于  <a href="http://www.cnblogs.com/maplejan/archive/2013/04/26/3045928.html" target="_blank" rel="noopener">http://www.cnblogs.com/maplejan/archive/2013/04/26/3045928.html</a></p>
<h2 id="库的使用实践"><a href="#库的使用实践" class="headerlink" title="库的使用实践"></a>库的使用实践</h2><h3 id="zepto-js"><a href="#zepto-js" class="headerlink" title="zepto.js"></a>zepto.js</h3><p> <a href="http://chaoskeh.com/blog/some-experience-of-using-zepto.html" title="zepto" target="_blank" rel="noopener">zepto的一篇使用注意点讲解</a></p>
<p> <a href="http://blog.youyo.name/archives/zepto-tap-click-through-research.html" title="zepto" target="_blank" rel="noopener">zepto的著名的tap“点透”bug</a></p>
<p> <a href="http://www.cnblogs.com/sky000/archive/2013/03/29/2988952.html" title="zepto" target="_blank" rel="noopener">zepto源码注释</a></p>
<h3 id="使用zeptojs内嵌到android-webview影响正常滚动时"><a href="#使用zeptojs内嵌到android-webview影响正常滚动时" class="headerlink" title="使用zeptojs内嵌到android webview影响正常滚动时"></a>使用zeptojs内嵌到android webview影响正常滚动时</h3><p><a href="https://github.com/madrobby/zepto/blob/master/src/touch.js" target="_blank" rel="noopener">https://github.com/madrobby/zepto/blob/master/src/touch.js</a> 去掉61行,其实就是使用原生的滚动</p>
<h3 id="iscroll4"><a href="#iscroll4" class="headerlink" title="iscroll4"></a>iscroll4</h3><p> iscroll4 的几个bug(来自 <a href="http://www.mansonchor.com/blog/blog_detail_64.html" target="_blank" rel="noopener">http://www.mansonchor.com/blog/blog_detail_64.html</a> 内有详细讲解)</p>
<p>1.滚动容器点击input框、select等表单元素时没有响应】</p>
<pre><code>onBeforeScrollStart: function (e) { e.preventDefault(); }</code></pre><p>改为</p>
<pre><code>onBeforeScrollStart: function (e) { var nodeType = e.explicitOriginalTarget © e.explicitOriginalTarget.nodeName.toLowerCase():(e.target © e.target.nodeName.toLowerCase():&apos;&apos;);if(nodeType !=&apos;select&apos;&amp;&amp; nodeType !=&apos;option&apos;&amp;&amp; nodeType !=&apos;input&apos;&amp;&amp; nodeType!=&apos;textarea&apos;) e.preventDefault(); }</code></pre><p>2.往iscroll容器内添加内容时，容器闪动的bug</p>
<p>源代码的</p>
<pre><code>has3d = &apos;WebKitCSSMatrix&apos; in window &amp;&amp; &apos;m11&apos; in new WebKitCSSMatrix()</code></pre><p>改成</p>
<pre><code>has3d = false</code></pre><p>在配置iscroll时，useTransition设置成false</p>
<p>3.过长的滚动内容，导致卡顿和app直接闪退</p>
<ol>
<li>不要使用checkDOMChanges。虽然checkDOMChanges很方便，定时检测容器长度是否变化来refresh，但这也意味着你要消耗一个Interval的内存空间</li>
<li>隐藏iscroll滚动条，配置时设置hScrollbar和vScrollbar为false。</li>
<li>不得已的情况下，去掉各种效果，momentum、useTransform、useTransition都设置为false</li>
</ol>
<p>4.左右滚动时，不能正确响应正文上下拉动</p>
<p>iscroll的闪动问题也与渲染有关系，可以参考<br> <a href="http://www.iunbug.com/archives/2012/09/19/411.html" title="iscroll4" target="_blank" rel="noopener">运用webkit绘制渲染页面原理解决iscroll4闪动的问题</a><br><a href="http://blog.csdn.net/gcz564539969/article/details/9156141" title="iscroll5" target="_blank" rel="noopener">iscroll4升级到5要注意的问题</a></p>
<h3 id="iscroll或者滚动类框架滚动时不点击的方法"><a href="#iscroll或者滚动类框架滚动时不点击的方法" class="headerlink" title="iscroll或者滚动类框架滚动时不点击的方法"></a>iscroll或者滚动类框架滚动时不点击的方法</h3><p>可以使用以下的解决方案(利用data-setapi)</p>
<pre><code>&lt;a ontouchmove=&quot;this.s=1&quot; ontouchend=&quot;this.s || window.open(this.dataset.href),this.s=0&quot; target=&quot;_blank&quot; data-href=&quot;http://www.hao123.com/topic/pig&quot;&gt;黄浦江死猪之谜&lt;/a&gt;</code></pre><p>也可以用这种方法</p>
<pre><code>$(document).delegate(&apos;[data-target]&apos;, &apos;touchmove&apos;, function () {
    $(this).attr(&apos;moving&apos;,&apos;moving&apos;);

})


$(document).delegate(&apos;[data-target]&apos;, &apos;touchend&apos;, function () {
    if ($(this).attr(&apos;moving&apos;) !== &apos;moving&apos;) {
     //做你想做的。。
        $(this).attr(&apos;moving&apos;, &apos;notMoving&apos;);
    } else {
        $(this).attr(&apos;moving&apos;, &apos;notMoving&apos;);
    }

})</code></pre><h2 id="移动端字体问题"><a href="#移动端字体问题" class="headerlink" title="移动端字体问题"></a>移动端字体问题</h2><p><a href="http://zhuanlan.zhihu.com/zhezhexiong/19565895" target="_blank" rel="noopener">知乎专栏 - [无线手册-4] dp、sp、px傻傻分不清楚[完整]</a></p>
<p><a href="http://www.sencha.com/blog/resolution-independent-mobile-ui" target="_blank" rel="noopener">Resolution Independent Mobile UI</a></p>
<p><a href="http://stackoverflow.com/questions/12058574/pixel-density-retina-display-and-font-size-in-css" target="_blank" rel="noopener">Pixel density, retina display and font-size in CSS</a></p>
<p><a href="http://bjango.com/articles/min-device-pixel-ratio/" target="_blank" rel="noopener">Device pixel density tests</a></p>
<h2 id="跨域问题"><a href="#跨域问题" class="headerlink" title="跨域问题"></a>跨域问题</h2><p>手机浏览器也是浏览器，在ajax调用外部api的时候也存在跨域问题。当然利用 PhoneGap 打包后，由于协议不一样就不存在跨域问题了。<br>但页面通常是需要跟后端进行调试的。一般会报类似</p>
<pre><code>XMLHttpRequest cannot load XXX
Origin null is not allowed by Access-Control-Allow-Origin.</code></pre><p>以及</p>
<pre><code>XMLHttpRequest cannot load http://. Request header field Content-Type is not allowed by Access-Control-Allow-Headers.&quot;</code></pre><p>这时候可以让后端加上两个http头</p>
<pre><code>Access-Control-Allow-Origin &quot;*&quot;
Access-Control-Allow-Headers &quot;Origin, X-Requested-With, Content-Type, Accept&quot;</code></pre><p>第一个头可以避免跨域问题，第二个头可以方便ajax请求设置content-type等配置项</p>
<p>这个会存在一些安全问题，可以参考这个问题的讨论 <a href="http://www.zhihu.com/question/22992229" target="_blank" rel="noopener">http://www.zhihu.com/question/22992229</a></p>
<h2 id="PhoneGap-部分"><a href="#PhoneGap-部分" class="headerlink" title="PhoneGap 部分"></a>PhoneGap 部分</h2><p><a href="http://snoopyxdy.blog.163.com/blog/static/60117440201432491123551" target="_blank" rel="noopener">http://snoopyxdy.blog.163.com/blog/static/60117440201432491123551</a> 这里有一大堆snoopy总结的phonggap开发坑</p>
<h3 id="Should-not-happen-no-rect-based-test-nodes-found"><a href="#Should-not-happen-no-rect-based-test-nodes-found" class="headerlink" title="Should not happen: no rect-based-test nodes found"></a>Should not happen: no rect-based-test nodes found</h3><p>在 Android 项目中的 assets 中的 HTML 页面中加入以下代码，便可解决问题</p>
<pre><code>window,html,body{
    overflow-x:hidden !important;
    -webkit-overflow-scrolling: touch !important;
    overflow: scroll !important;
}</code></pre><p>参考：</p>
<p><a href="http://stackoverflow.com/questions/12090899/android-webview-jellybean-should-not-happen-no-rect-based-test-nodes-found" target="_blank" rel="noopener">http://stackoverflow.com/questions/12090899/android-webview-jellybean-should-not-happen-no-rect-based-test-nodes-found</a></p>
<h3 id="拿联系人的时候报-ContactFindOptions-is-not-defined"><a href="#拿联系人的时候报-ContactFindOptions-is-not-defined" class="headerlink" title="拿联系人的时候报 ContactFindOptions is not defined"></a>拿联系人的时候报 ContactFindOptions is not defined</h3><p>出现这个问题可能是因为 Navigator 取 contacts 时绑定的 window.onload</p>
<p>注意使用 PhoneGap 的 API 时，一定要在 devicereay 事件的处理函数中使用 API</p>
<pre><code>document.addEventListener(&quot;deviceready&quot;, onDeviceReady, false);

    function onDeviceReady() {    
        callFetchContacts();
    }

function callFetchContacts(){
    var options = new ContactFindOptions();
    options.multiple = true;
    var fields       = [&quot;displayName&quot;, &quot;name&quot;,&quot;phoneNumbers&quot;];
    navigator.contacts.find(fields, onSuccess, onError,options);  
    }</code></pre><hr>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>Web</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 断点调试</title>
    <url>/2017/04/20/article-js-pausebug/</url>
    <content><![CDATA[<p>虽然网上已经有多的数不清的调试教程了，但仍然没有发现哪篇文章写的通俗易懂，索性自己尝试写写自己的一些使用习惯或者说是心得，希望对那些还不是很懂得使用断点调试的孩子有一些帮助（大神请无视~）。</p>
<p><strong>本文参考自 <a href="http://www.cnblogs.com/mqfblog/" target="_blank" rel="noopener">沐清风blog</a> 的 <a href="http://www.cnblogs.com/mqfblog/p/5397282.html" target="_blank" rel="noopener">js断点调试心得</a></strong><a id="more"></a></p>
<h2 id="1-断点调试是啥？难不难？"><a href="#1-断点调试是啥？难不难？" class="headerlink" title="1. 断点调试是啥？难不难？"></a>1. 断点调试是啥？难不难？</h2><p>断点调试其实并不是多么复杂的一件事，简单的理解无外呼就是打开浏览器，打开sources找到js文件，在行号上点一下罢了。操作起来似乎很简单，其实很多人纠结的是，是在哪里打断点？（我们先看一个断点截图，以chrome浏览器的断点为例）</p>
<p><img src="/styles/images/js/debug1.png" alt="debugdemo"></p>
<p>步骤记住没？</p>
<p>用chrome浏览器打开页面 → 按f12打开开发者工具 → 打开Sources → 打开你要调试的js代码文件 → 在行号上单击一下，OK!恭喜你的处女断点打上了,哈哈~~</p>
<h2 id="2-断点怎么打才合适？"><a href="#2-断点怎么打才合适？" class="headerlink" title="2. 断点怎么打才合适？"></a>2. 断点怎么打才合适？</h2><p>打断点操作很简单，核心的问题在于，断点怎么打才能够排查出代码的问题所在呢？下面我继续举个例子方便大家理解，废话不多说，上图：</p>
<p><img src="/styles/images/js/debug2.png" alt="debugdemo"></p>
<p>假设我们现在正在实现一个加载更多的功能，如上图，但是现在加载更多功能出现了问题，点击以后数据没有加载出来，这时候我们第一时间想到的应该是啥？（换一行写答案，大家可以看看自己的第一反应是啥）</p>
<p>我最先想到的是，我点击到底有没有成功？点击事件里的方法有没有运行？好，要想知道这个问题的答案，我们立马去打个断点试试看，断点打在哪？自己先琢磨一下。</p>
<p>接着上图：</p>
<p><img src="/styles/images/js/debug3.png" alt="debugdemo"></p>
<p>各位想到没？没错，既然想知道点击是否成功，我们当然是在代码中的点击事件处添加一个断点，切记不要添加在226行哦，因为被执行的是click方法内的函数，而不是226行的选择器。断点现在已经打上了，然后做什么呢？自己再琢磨琢磨~</p>
<p>继续上图：</p>
<p><img src="/styles/images/js/debug4.png" alt="debugdemo"></p>
<p>然后我们当然是回去点击加载更多按钮啦，为什么？额。。。如果你这么问，不点击加载更多按钮，怎么去触发点击事件？不触发点击事件，怎么去执行点击事件里的函数？咆哮状。。不过我相信大家肯定不会问这么low的问题<del>不瞎扯了</del></p>
<p>继续正题，上面的图就是点击加载更多按钮后的情况，我们可以看到左侧的页面被一个半透明的层给盖住了，页面上方还有一串英文和两个按钮，右侧代码227行被添加上了背景色，出现这个情况，先不管那些按钮英文是啥意思有啥作用，你从这个图得到了什么信息？继续琢磨琢磨~</p>
<p>如果出现了上图这个情况，说明一点，click事件中的函数被调用了，进一步说明了点击事件生效。那么我们对于这个问题产生的第一个“犯罪嫌疑人”就被排除了。</p>
<p>补充一下：</p>
<p>如果没有出现上面的情况咋办？那是不是说明点击事件没有生效呢？那是什么导致点击事件没有生效？大家自己思考思考~</p>
<p>可能导致点击事件没生效的原因很多，比多选择器错误，语法错误，被选择的元素是后生成的等。怎么解决呢？</p>
<p>选择器错误，大家可以继续往后看到console部分的内容，我想大家就知道怎么处理了</p>
<p>语法错误，细心排查一下，不熟悉的语法可以百度对比一下</p>
<p>被选择的元素是后生成的，最简单的处理就是使用.on()方法去处理，这个东东带有事件委托处理，详情可以自行百度。</p>
<p>那么接下来”犯罪嫌疑人“的身份锁定在哪里呢？</p>
<p>我们将目光投向事件内部，click事件触发了，那么接下来的问题就是它内部的函数问题了。如果你要问为什么？请给我一块豆腐。。。</p>
<p>打个比方，给你一支笔，让你写字，然后你在纸上写了一个字，发现字没出来，为啥？你说我写了呀，纸上都还有划痕。那是不是可能笔没有墨水或者笔尖坏了了？这个例子和点击加载更多一个道理，写字这个动作就是点击操作，而内部函数就是墨水或者笔尖。明白了不~</p>
<p>接着我们分析下点击事件里面的内容，里面包含三句话，第一句话是变量i自增长，第二句话是给按钮添加一个i标签，第三句话是调用请求数据的方法。</p>
<p>就通过这三句话的本身作用，我们可以将较大一部分嫌疑放在第三句话，一小部分放在第一句和第二句话上，有人可能会疑惑，第二句话怎么会有嫌疑呢？他的作用只不过是添加一个标签，对于数据完全没有影响啊，确实，这句话对于数据没有影响，但是出于严谨考虑，它仍然有可能出错，例如它要是少了一个分号呢？或者句子内部某个符号错误呢？往往就是这种小问题浪费我们很多时间。</p>
<p>好，为了进一步锁定”犯罪嫌疑人“，给大家介绍一个工具，也是上图出现两个图标之一，见下图：</p>
<p><img src="/styles/images/js/debug5.png" alt="debugdemo"></p>
<p>这个小图标的功能叫”逐语句执行“或者叫”逐步执行“，这是我个人理解的一个叫法，意思就是，每点击它一次，js语句就会往后执行一句，它还有一个快捷键，F10。下图示范一下它被点击以后的效果：</p>
<p><img src="/styles/images/js/debug6.png" alt="debugdemo"></p>
<p>我单击了两次这个按钮（或者使用F10快捷键），js代码从227行执行到了229行，所以我管它叫”逐语句执行“或者”逐步执行“。这个功能非常的实用，大部分的调试都会使用到它。</p>
<p>上面介绍到我单击了两次“逐语句执行”按钮，代码从227行运行到229行，大家觉得这意味着啥？是不是说明从语法上来说，前两句是没有问题的，那么是不是也同时意味着前两句就排除嫌疑了呢？我看不然。</p>
<p>大家都知道，加载更多就是一个下一页的功能，而其中最核心的一个就是传给后台的页码数值，每当我点击加载更多按钮一次，页码的数值就要加1，所以如果下一页的数据没出来，是不是有可能是因为页码数值也就是[i变量]（下面统一称呼i）有问题？那么如何排查页码是否存在问题呢？大家自己先思考思考。</p>
<p>下面教大家两种查看页码数值i]实际输出值的方法，上图：</p>
<p>第一种：</p>
<p><img src="/styles/images/js/debug7.png" alt="debugdemo"></p>
<p>操作步骤如下：</p>
<p>1.仍然是在227行打上断点 → 2. 点击加载更多按钮 → 3. 单击一次“逐语句执行“按钮，js代码执行到228行 → 4.用鼠标选中i++（什么叫选中大家里不理解？就是你要复制一个东西，是不是要选中它？对，就是这个选中） → 5. 选中以后，鼠标悬浮在目标上方，你就看到上图的结果。</p>
<p>第二种：</p>
<p><img src="/styles/images/js/debug8.png" alt="debugdemo"></p>
<p>这个方法其实和第一种差不多，只不过是在控制台输出i的值，大家只需要按照第一种方法执行到第三步 → 4. 打开和sources同一级栏目的console → 5. 在console下方的输入栏里输入i → 6. 按enter回车键即可。</p>
<p>上面的第二种方法里，提到了console这个东西，我们可以称呼它为控制台或者其他什么都可以，这不重要~console的功能很强大，在调试的过程中，我们往往需要知道某些变量的值到底输出了什么，或者我们使用选择器[$”.div”)这种]是否选中了我们想要的元素等，都可以在控制台打印出来。当然直接用第一种方法也可以。</p>
<p>给大家示范一下在console里打印我们想要选中的元素。上图~</p>
<p><img src="/styles/images/js/debug9.png" alt="debugdemo"></p>
<p>在控制台中输入$(this)，即可得到选择的元素，没错，正是我们所点击的对象——加载更多按钮元素。</p>
<p>在这里给大家说说我对console这个控制台的理解：这个东东就是一个js解析器，是浏览器本身用来解析运行js的家伙，只不过浏览器通过console让我们开发者在调试过程中，可以控制js的运行以及输出。通过上面的两种方法，大家可能觉得使用起来很简单，但是我要给大家提醒一下，或者说是一些新手比较容易遇到的困惑。</p>
<blockquote>
<p><strong>困惑一：在没有打断点的情况下，在console输入i，结果console报错了。</strong></p>
</blockquote>
<p>这应该是新手很常见的问题，为什么不打断点我就没有办法在控制台直接输出变量的值呢？个人理解这时候i只是一个局部变量，如果不打上断点，浏览器会把所有的js全部解析完成，console并不能访问到局部变量，只能访问到全局变量，所以这时候console会报错i未定义，但是当js打上断点时，console解析到了局部变量i所在的函数内，这时候i是能够被访问的。</p>
<blockquote>
<p><strong>困惑二：为什么我直接在console里输入$(“.xxx”)能打印出东西来呢？</strong></p>
</blockquote>
<p>很简单，console本身就是一个js解析器，$(“.xxx”)就是一个js语句，所以自然console能够解析这个语句然后输出结果。</p>
<p>介绍完“逐语句执行”按钮和console控制台的用法，最后再介绍一个按钮，上图：</p>
<p><img src="/styles/images/js/debug10.png" alt="debugdemo"></p>
<p>这个按钮我称呼它为“逐过程执行”按钮，和“逐语句执行”按钮不同，“逐过程执行”按钮常用在一个方法调用多个js文件时，涉及到的js代码比较长，则会使用到这个按钮。</p>
<p>上图：</p>
<p><img src="/styles/images/js/debug11.png" alt="debugdemo"></p>
<p>假设上图我只在227行打了个断点，然后一直点击逐语句执行”按钮到229行，这时候如果再点击一次“逐语句执行”按钮呢？则会进入下图的js里：</p>
<p><img src="/styles/images/js/debug12.png" alt="debugdemo"></p>
<p>这些都是zepto库文件的内容，没啥好看的，里面运行很复杂，我们不可能一直使用“逐语句执行”按钮，这样你会发现你按了大半天还在库文件里面绕。。。这时候咋办？那就该“逐过程执行”按钮上场了。</p>
<p>上图：</p>
<p><img src="/styles/images/js/debug13.png" alt="debugdemo"></p>
<p>我除了在227行打了一个断点，同时还在237行打了一个断点，当我们运行到229行时，直接单击“逐过程执行”按钮，你会发现，js直接跳过了库文件，运行到了237行，大家可以自己使用体验一下。</p>
<h2 id="最后总结"><a href="#最后总结" class="headerlink" title="最后总结"></a>最后总结</h2><p>本文主要介绍了“逐语句执行”按钮、“逐过程执行”按钮、console控制台这三个工具，以及调试bug时的一些思路。工具的用法我就不再赘述了，大家知道用法就行，具体怎么去更合理的使用，还需要大家通过大量的实践去总结提升~</p>
<p>我其实在本文主要想讲的是调试bug的一个思路，但是由于选的例子涉及东西太多。。。怕全部写下来内容太长，大家也没兴趣看，所以我就简单的选了一部分给大家讲解，不知道大家有没有收获。别看我调试三句话写了一堆的东西，如果真的在实际项目中你也像我这样去做，估计你调试一个Bug的时间会比写一个脚本的时间还长很多。。。在实际情况下，我们应该养成拿到问题的第一时间，自行在脑海中排查问题，找到最有可能出现问题的点，如果没办法迅速的排查出最重要的点，那么你可以使用最麻烦但是很靠谱的方法，利用“逐语句执行”按钮将整个和问题相关的js依次去执行一遍，在执行的过程中，自己也跟着理清思路，同时注意下每个变量的值以及选择器选中的元素是否正确，一般来说，这样做一遍下来，bug都解决的差不多了。</p>
<p>所以个人认为，我们调试bug的思路应该是这样的：首先，js是否成功的执行进来；其次，js是否存在逻辑问题，变量问题，参数问题等等；最后，如果上述都没有问题，请仔细查看各种符号。。。</p>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>深入理解 JavaScript 中的函数</title>
    <url>/2017/04/15/article-js-fn/</url>
    <content><![CDATA[<blockquote>
<p>本文转自 <a href="http://www.codeceo.com/" target="_blank" rel="noopener">码农网</a> 的 <a href="http://www.codeceo.com/article/javascript-function-coding.html" target="_blank" rel="noopener">深入理解 JavaScript 中的函数</a>。</p>
</blockquote>
<p>本文旨在提供web开发人员必须了解的所有JavaScript函数的基本知识。</p>
<p>函数于软件开发者而言并不是什么奇幻世界。如果你的日常活动涉及到编码，哪怕是一点点，那么在一天结束的时候，你一定创建/修改了一个或多个函数。</p>
<p>简而言之函数只不过是一组执行某个操作的语句。函数可能会有一些输入参数（在函数体中使用），并在执行后返回值。<a id="more"></a></p>
<p>JavaScript函数也具有这些特性，但它们不仅仅是常规函数。JavaScript函数是对象。你可以查看我曾经写的关于JavaScript对象的文章，里面我提到几乎JavaScript中的所有一切都是对象。</p>
<p>作为对象，JavaScript函数可能会有属性和其他函数（方法）。让我们来看看JavaScript中的一个典型的函数定义。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">myNotSoGreatFunc</span>(<span class="params">visitor</span>) </span>&#123;</span><br><span class="line">   <span class="built_in">console</span>.log(<span class="string">"Welcome to Code Morning Mr. "</span> + visitor);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>没错。上面的函数不涉及什么宏伟大业，因为它仅是对博客访问者表示了欢迎。但它展示了JavaScript函数的样子。函数定义从关键字function开始，然后是函数名，空的或有参数的括号。实际的函数代码（JavaScript语句）被封装在一对花括号内{ }。对于函数而言，return语句是可选的。JavaScript函数总是会返回一个值。当function主体中没有return语句时，那么function返回undefined。</p>
<p><img src="/styles/images/js/function.png" alt="函数"></p>
<p>下面的代码调用传递visitor name作为参数的函数。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">myNotSoGreatFunc(<span class="string">"Bob Martin"</span>);</span><br><span class="line"><span class="comment">// Output:&amp;nbsp;</span></span><br><span class="line"><span class="comment">// Welcome to Code Morning Mr. Bob Martin.</span></span><br></pre></td></tr></table></figure>

<p>到现在为止，我们了解了函数非常基本的特征。现在，我们将对JavaScript函数的一些高级概念一探究竟。</p>
<h2 id="1-匿名函数"><a href="#1-匿名函数" class="headerlink" title="1. 匿名函数"></a>1. 匿名函数</h2><p>JavaScript函数可以是匿名的。这意味着你可以从函数声明中省略函数名。但是，函数必须存储在变量中。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> addNumbers = <span class="function"><span class="keyword">function</span> (<span class="params">x, y</span>) </span>&#123; <span class="keyword">return</span> x + y; &#125;</span><br></pre></td></tr></table></figure>

<p>上述语法被也被称为函数表达式。你可以把变量addNumbers 当作函数名，以及像下面这样调用该函数。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> sum = addNumbers(<span class="number">2</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<p>当你想传递一个函数作为参数给另一个函数时，函数表达式就非常方便了。让我们用一个简单的例子来试着了解这一点。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> add = <span class="function"><span class="keyword">function</span> (<span class="params">first, second</span>) </span>&#123; <span class="keyword">return</span> first + second &#125;;</span><br><span class="line"><span class="keyword">var</span> multiply = <span class="function"><span class="keyword">function</span> (<span class="params">first, second</span>) </span>&#123; <span class="keyword">return</span> first * second &#125;;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">calculate</span>(<span class="params">fun, a, b</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> fun(a, b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>首先我已经创建了两个匿名函数。第一个返回两个数的加法运算，第二个返回两个数的乘法运算。相当简单，没有什么可值得炫耀的地方。然后，我定义函数calculate，这个函数接受函数作为第一个参数后跟两个参数接受两个数字。</p>
<p>我可以通过传递任意函数作为第一个参数来调用函数calculate。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> sum = calculate(add, <span class="number">2</span>, <span class="number">3</span>); <span class="comment">// sum = 5</span></span><br><span class="line"><span class="keyword">var</span> multiplication = calculate(multiply, <span class="number">2</span>, <span class="number">3</span>); <span class="comment">// multiplication = 6</span></span><br></pre></td></tr></table></figure>

<p>你可以看到将函数作为参数传递是多么容易。这种模式在AJAX中大量使用，当你在AJAX调用完成后，传递回调函数处理成功或失败的场景时。</p>
<h2 id="2-关于参数的更多内容"><a href="#2-关于参数的更多内容" class="headerlink" title="2. 关于参数的更多内容"></a>2. 关于参数的更多内容</h2><p>JavaScript是非常灵活的，当涉及到传递或访问函数参数的时候。让我们看一下函数参数可以被操纵的方式。</p>
<h3 id="2-1-缺少参数"><a href="#2-1-缺少参数" class="headerlink" title="2.1 缺少参数"></a>2.1 缺少参数</h3><p>调用函数时，函数的参数数量可以比要求的更少或更多。如果你调用的函数的参数比声明的少，那么缺少的参数被设置为undefined。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callMe</span>(<span class="params">a, b, c</span>) </span>&#123;</span><br><span class="line">   <span class="built_in">console</span>.log(<span class="string">"c is "</span> + <span class="keyword">typeof</span> c);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">callMe(<span class="string">"Code"</span>, <span class="string">"Morning"</span>); </span><br><span class="line"><span class="comment">// Output: "c is undefined"</span></span><br><span class="line">callMe(<span class="string">"Learn"</span>, <span class="string">"JavaScript"</span>, <span class="string">"Functions"</span>); </span><br><span class="line"><span class="comment">// Output: "c is string"</span></span><br></pre></td></tr></table></figure>

<h3 id="2-2-Arguments对象"><a href="#2-2-Arguments对象" class="headerlink" title="2.2 Arguments对象"></a>2.2 Arguments对象</h3><p>所有的JavaScript函数有一个特殊的对象，叫做arguments，它是在函数调用过程中传递的参数数组。该对象可以被用来访问单个参数或获得传递到函数的参数总数。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callMe</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">   <span class="keyword">var</span> i;</span><br><span class="line">   <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="built_in">arguments</span>.length; i++) &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[i]);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="built_in">console</span>.log(<span class="string">"Total arguments passed: "</span> + <span class="built_in">arguments</span>.length);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此函数假设没有传递任何参数，但就像我说的，你可以传递任何数量的参数到JavaScript函数。我可以像这样调用这个函数：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">callMe(<span class="string">"Code"</span>, <span class="string">"Morning"</span>, <span class="string">"Mr. Programmer"</span>);</span><br><span class="line"><span class="comment">// Output":</span></span><br><span class="line"><span class="comment">// Code</span></span><br><span class="line"><span class="comment">// Morning</span></span><br><span class="line"><span class="comment">// Mr. Programmer</span></span><br><span class="line"><span class="comment">// Total arguments passed: 3</span></span><br></pre></td></tr></table></figure>

<p>每个参数可以从arguments对象作为一个数组项被访问。被传递给函数的arguments的总数可从arguments.length属性获得。</p>
<h3 id="2-3-默认参数"><a href="#2-3-默认参数" class="headerlink" title="2.3 默认参数"></a>2.3 默认参数</h3><p>你是C ++或C#程序员吗？你见过使用默认参数的函数吗？也许你会回答yes！ ECMAScript 6带来了JavaScript的这一特性，就是你可以定义带有默认参数的函数。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">greetMyVisitors</span>(<span class="params">name, profession = <span class="string">"The cool programmer"</span></span>) </span>&#123;</span><br><span class="line">    alert(<span class="string">"Welcome Mr. "</span> + name + <span class="string">", "</span> + profession);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该函数有礼貌地地迎接了博客访问者。它有两个参数name 和profession，并在消息框中显示一个欢迎消息。如果在调用过程中没有参数（或“undefined”）传递，那么第二个参数取用默认值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">greetMyVisitors(<span class="string">"Justin Bieber"</span>, <span class="string">"The singer"</span>); </span><br><span class="line"><span class="comment">// Shows the message "Welcome Mr. Justin Bieber, The singer"</span></span><br><span class="line"> </span><br><span class="line">greetMyVisitors(<span class="string">"Bob Martin"</span>); </span><br><span class="line"><span class="comment">// Shows the message "Welcome Mr. Bob Martin, The cool programmer"</span></span><br><span class="line"> </span><br><span class="line">greetMyVisitors(<span class="string">"John Papa"</span>, <span class="literal">undefined</span>); </span><br><span class="line"><span class="comment">// Shows the message "Welcome Mr. John Papa, The cool programmer"</span></span><br></pre></td></tr></table></figure>

<h2 id="3-嵌套函数"><a href="#3-嵌套函数" class="headerlink" title="3. 嵌套函数"></a>3. 嵌套函数</h2><p>函数可以在它的内部包含一个或多个函数。内部函数可能会在内部再次包含函数。让我们来看看以下操作。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">wakeUpAndCode</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">   <span class="function"><span class="keyword">function</span> <span class="title">wakeUp</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">"I just woke up"</span>);</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   <span class="function"><span class="keyword">function</span> <span class="title">code</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">"I am ready to code now"</span>);</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   wakeUp();</span><br><span class="line">   code();</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">wakeUpAndCode();</span><br><span class="line"> </span><br><span class="line"><span class="comment">// Output:</span></span><br><span class="line"><span class="comment">// I just woke up</span></span><br><span class="line"><span class="comment">// I am ready to code now</span></span><br></pre></td></tr></table></figure>

<p>函数wakeUpAndCode包含两个内部函数wakeUp和code。当调用wakeUpAndCode时，函数主体开始执行函数主体。在外部函数中只有两个可执行语句，调用wakeUp和code的方法。调用wakeUp将执行内部wakeUp函数，这将写入string“I just woke up”到控制台。调用code将会写入“I am ready to code now”string到控制台。</p>
<p>内部函数可以访问所有外部函数的变量和参数。内部函数是函数内部某种private实现，并且不能从外部函数以外被调用。内部函数的使用生成了JavaScript闭包，这个我将另起一篇文章讨论。</p>
<h2 id="4-立即执行函数表达式（IIFE）"><a href="#4-立即执行函数表达式（IIFE）" class="headerlink" title="4. 立即执行函数表达式（IIFE）"></a>4. 立即执行函数表达式（IIFE）</h2><p>IIFE是被立即调用执行的匿名函数表达式。IIFE看上去像这样：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">   <span class="comment">// Your awesome code here</span></span><br><span class="line">&#125;());</span><br></pre></td></tr></table></figure>

<p>所有你要做的就是创建一个匿名函数，在函数定义后马上放一对圆括号以调用函数，最后将所有代码封装在另一对圆括号中。最外层的括号将它里面的所有一切转变成一个表达式，因为括号不能包含JavaScript语句。函数定义后面的圆括号则立即调用函数。</p>
<p>IIFE块中定义的任何变量或函数对块而言是本地的，并且不能被这个范围以外的任何代码改变。</p>
<p>看看IIFE的这个例子。此函数没有调用也会自动执行。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">   <span class="built_in">console</span>.log(<span class="string">"I run on my own."</span>);</span><br><span class="line">&#125;());</span><br></pre></td></tr></table></figure>

<p>只需在plunker中复制并粘贴代码，看看在浏览器控制台中的输出。如果你不知道去哪里找浏览器控制台，那么只要在浏览器窗口中按下F12就会出现开发者工具。跳转console选项卡以查看<code>console.log</code>语句的所有输出。</p>
<p>IIFE是一个在代码中创建局部范围的很好方法。它们可以帮助你保护变量和函数，以避免被应用程序的其他部分更改或覆盖。JavaScript中IIFE的其他优势？它们是如何解决全局范围污染问题的？欢迎点击查看我关于<a href="/2017/01/14/js-IIFE/">立即执行函数表达式</a>的文章。</p>
<h2 id="5-构造函数"><a href="#5-构造函数" class="headerlink" title="5. 构造函数"></a>5. 构造函数</h2><p>函数可以充当构造器的角色，并且可以使用构造函数来创建新的对象。这是使JavaScript面向对象的特点之一。使用构造函数的好处是，你将能够通过预定义的属性和方法，创造尽可能多的对象。如果你由此关联到其他语言中的类和对象，那么你做的对。</p>
<p>让我们创建一个带有一些属性和方法的构造函数Programmer。你可以假设它在你最喜欢的语言中是一个类。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Programmer</span>(<span class="params">name, company, expertise</span>) </span>&#123;</span><br><span class="line">   <span class="keyword">this</span>.name = name;</span><br><span class="line">   <span class="keyword">this</span>.company = company;</span><br><span class="line">   <span class="keyword">this</span>.expertise = expertise;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">this</span>.writeCode = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">"Writing some public static thing.."</span>);</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">this</span>.makeSkypeCall = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">"Making skype call.."</span>);</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">this</span>.doSalsa = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">"I'm a programmer, I can only do Gangnam style.."</span>);</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">this</span>.canWriteJavaScript = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> expertise === <span class="string">"JavaScript"</span>;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>函数有三个参数，并创建了一个具有三个属性和四种方法的对象。我不认为上面的代码需要任何解释。此外，我可以创建任意数量程序员对象。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> javaProgrammer = <span class="keyword">new</span> Programmer(<span class="string">"Mohit Srivastava"</span>, <span class="string">"Infosys"</span>, <span class="string">"Java"</span>);</span><br><span class="line"><span class="keyword">var</span> dotnetProgrammer = <span class="keyword">new</span> Programmer(<span class="string">"Atul Mishra"</span>, <span class="string">"Prowareness"</span>, <span class="string">".NET"</span>);</span><br></pre></td></tr></table></figure>

<p>虽然也可以创建一个使用对象文本语法带有相同属性和方法的对象，但我们需要多次编写相同的代码，这可不是什么伟大的实践。如果你知道编程DRY原则，那么你就不会不赞同我。构造函数使得可以一次定义对象，并创建真正的实例，无论什么时候你想要。</p>
<blockquote>
<p><strong>注意</strong>：</p>
</blockquote>
<p>始终使用new关键字来从构造器创建新的对象。忘记了new而像这个创建一个实例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> jsProgrammer = Programmer(<span class="string">"Douglas Crockford"</span>, <span class="string">"Yahoo"</span>, <span class="string">"JavaScript"</span>)</span><br></pre></td></tr></table></figure>

<p>最终将添加所有属性和方法到全局的window对象，哇哦，这将是太可怕了。原因是，除非明确指定，否则“this”指向全局的window对象。使用new 设置“this”上下文到被创建的当前对象。</p>
<p>然而，有一种变通方法可以来克服这个问题。你可以改变构造函数的实现以使域安全，然后在创建新的对象时，你就可以愉快地忽略new 关键字了。请参见以下修改了的构造函数代码。为了便于查看，我已删除了一些方法。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Programmer</span>(<span class="params">name, company, expertise</span>) </span>&#123;</span><br><span class="line">   <span class="keyword">if</span>(!(<span class="keyword">this</span> <span class="keyword">instanceof</span> Programmer)) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> Programmer(name, company, expertise);</span><br><span class="line">   &#125;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">this</span>.name = name;</span><br><span class="line">   <span class="keyword">this</span>.company = company;</span><br><span class="line">   <span class="keyword">this</span>.expertise = expertise;</span><br><span class="line"> </span><br><span class="line">   <span class="keyword">this</span>.writeCode = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">"Writing some public static thing.."</span>);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>if 条件检查了this 对象是否是Programmer的一个实例。如果不是，它会创建一个新的Programmer对象，并通过再次调用构造器返回相同的内容。</p>
<p>注意：你无法在不使用’new’关键字的情况下，在Strict模式下从构造器创建一个新的对象。Strict模式强制一些编码准则，并且在你写的东西不安全的情况下会抛出错误。要启用Strict模式，你只需要添加在你的代码开头添加字符串 ‘use strict’。在Strict模式下运行代码是一个良好的实践，可以查看我的另一篇文章<a href="/2017/01/24/js-strict/">JavaScript 严格模式（strict）</a>了解更多。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">'use strict'</span></span><br><span class="line"> <span class="function"><span class="keyword">function</span> <span class="title">doSomething</span>(<span class="params"></span>) </span>&#123; </span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">&#125;</span><br><span class="line"> <span class="comment">//....</span></span><br><span class="line"> <span class="comment">//....</span></span><br></pre></td></tr></table></figure>

<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>在这篇文章中，我几乎已经涵盖了有关函数的所有内容。函数被认为是JavaScript中的一等公民。如果你想掌握JavaScript的话，理解函数可能是最重要的事情。</p>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 实现继承的方式</title>
    <url>/2017/04/09/article-js-prototype-method/</url>
    <content><![CDATA[<p>　　这篇文章主要介绍 JavaScript 实现继承的方式：</p>
<ul>
<li>类式继承</li>
<li>构造函数继承</li>
<li>组合继承</li>
<li>寄生组合式继承</li>
<li>extends继承</li>
</ul>
<p>　　<strong>本文参考<a href="http://ghmagical.com/" target="_blank" rel="noopener">TG-路虽远，无所畏</a> 的<a href="http://ghmagical.com/article/page/id/omIoPb1AIBPu" target="_blank" rel="noopener">JavaScript实现继承的方式</a>。</strong><a id="more"></a></p>
<h2 id="1-类式继承"><a href="#1-类式继承" class="headerlink" title="1. 类式继承"></a>1. 类式继承</h2><p>简单的类式继承：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 声明父类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = <span class="string">'animal'</span>;</span><br><span class="line">  <span class="keyword">this</span>.type = [<span class="string">'pig'</span>, <span class="string">'cat'</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 为父类添加共有方法</span></span><br><span class="line">Animal.prototype.greet = <span class="function"><span class="keyword">function</span>(<span class="params">sound</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(sound);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 声明子类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Dog</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.name = <span class="string">'dog'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 继承父类</span></span><br><span class="line">Dog.prototype = <span class="keyword">new</span> Animal();</span><br><span class="line"><span class="keyword">var</span> dog = <span class="keyword">new</span> Dog();</span><br><span class="line">dog.greet(<span class="string">'汪汪'</span>);  <span class="comment">//  "汪汪"</span></span><br><span class="line"><span class="built_in">console</span>.log(dog.type); <span class="comment">// ["pig", "cat"]</span></span><br></pre></td></tr></table></figure>

<p>　　在上面的代码中，我们创建了两个类Animal和Dog，而且给<code>Animal.prototype</code>原型上添加了一个greet共有方法，然后通过new命令实例化一个Animal，并且赋值给<code>Dog.prototype</code>原型。</p>
<p>　　原理说明：在实例化一个类时，新创建的对象复制了父类的构造函数内的属性与方法并且将原型<code>__proto__</code>指向了父类的原型对象，这样就拥有了父类的原型对象上的属性与方法。</p>
<p>　　不过，通过类式继承方式，有两个缺点:</p>
<ul>
<li><strong>第一个是引用缺陷</strong>：</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">dog.type.push(<span class="string">'dog'</span>);</span><br><span class="line"><span class="keyword">var</span> dog2 = <span class="keyword">new</span> Dog();</span><br><span class="line"><span class="built_in">console</span>.log(dog2.type);  <span class="comment">// ["dog", "cat", "dog"]</span></span><br></pre></td></tr></table></figure>

<p>　　通过上面的执行结果，我们看到当通过dog实例对象修改继承自Animal中的数组type(引用类型)时，另外一个新创建的实例dog2也会受到影响。</p>
<ul>
<li><strong>第二个是我们无法为不同的实例初始化继承来的属性，我们可以修改一下上面的例子</strong>：</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span>(<span class="params">color</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.color = color;</span><br><span class="line">&#125;</span><br><span class="line">Dog.prototype = <span class="keyword">new</span> Animal(<span class="string">'白色'</span>);</span><br><span class="line"><span class="built_in">console</span>.log(dog.color); <span class="comment">// "白色"</span></span><br><span class="line"><span class="built_in">console</span>.log(do2.color); <span class="comment">// "白色"</span></span><br></pre></td></tr></table></figure>

<p>　　通过上面的代码可以看到，我们无法为不同dog赋值不同的颜色，所有dog只能同一种颜色。</p>
<h2 id="2-构造函数继承"><a href="#2-构造函数继承" class="headerlink" title="2. 构造函数继承"></a>2. 构造函数继承</h2><p>构造函数继承方式可以避免类式继承的缺陷：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 添加共有方法</span></span><br><span class="line">Animal.prototype.greet = <span class="function"><span class="keyword">function</span>(<span class="params">sound</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(sound);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 声明子类</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Dog</span>(<span class="params">color</span>) </span>&#123;</span><br><span class="line">  Animal.apply(<span class="keyword">this</span>, <span class="built_in">arguments</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> dog = <span class="keyword">new</span> Dog(<span class="string">'白色'</span>);</span><br><span class="line"><span class="keyword">var</span> dog2 = <span class="keyword">new</span> Dog(<span class="string">'黑色'</span>);</span><br><span class="line"></span><br><span class="line">dog.type.push(<span class="string">'dog'</span>);</span><br><span class="line"><span class="built_in">console</span>.log(dog.color);  <span class="comment">// "白色"</span></span><br><span class="line"><span class="built_in">console</span>.log(dog.type);  <span class="comment">// ["pig", "cat", "dog"]</span></span><br><span class="line"><span class="built_in">console</span>.log(dog2.type);  <span class="comment">// ["pig", "cat"]</span></span><br><span class="line"><span class="built_in">console</span>.log(dog2.color);  <span class="comment">// "黑色"</span></span><br></pre></td></tr></table></figure>

<p>　　首先要知道<code>apply</code>方法的运用，它是可以更改函数的作用域，所以在上面的例子中，我们在Dog子类中调用这个方法也就是将Dog子类的变量在父类中执行一遍，这样子类就拥有了父类中的共有属性和方法。</p>
<p>　　但是，构造函数继承也是有缺陷的，那就是我们无法获取到父类的共有方法，也就是通过原型prototype绑定的方法：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">dog.greet();  <span class="comment">// Uncaught TypeError: dog.greet is not a function</span></span><br></pre></td></tr></table></figure>

<h2 id="3-组合继承"><a href="#3-组合继承" class="headerlink" title="3. 组合继承"></a>3. 组合继承</h2><p>组合继承其实就是将类式继承和构造函数继承组合在一起：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 声明父类   </span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span>(<span class="params">color</span>) </span>&#123;    </span><br><span class="line">  <span class="keyword">this</span>.name = <span class="string">'animal'</span>;    </span><br><span class="line">  <span class="keyword">this</span>.type = [<span class="string">'pig'</span>,<span class="string">'cat'</span>];    </span><br><span class="line">  <span class="keyword">this</span>.color = color;   </span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 添加共有方法  </span></span><br><span class="line">Animal.prototype.greet = <span class="function"><span class="keyword">function</span>(<span class="params">sound</span>) </span>&#123;    </span><br><span class="line">  <span class="built_in">console</span>.log(sound);   </span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 声明子类   </span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Dog</span>(<span class="params">color</span>) </span>&#123; </span><br><span class="line">  <span class="comment">// 构造函数继承    </span></span><br><span class="line">  Animal.apply(<span class="keyword">this</span>, <span class="built_in">arguments</span>);   </span><br><span class="line">&#125;   </span><br><span class="line"><span class="comment">// 类式继承</span></span><br><span class="line">Dog.prototype = <span class="keyword">new</span> Animal();   </span><br><span class="line"><span class="keyword">var</span> dog = <span class="keyword">new</span> Dog(<span class="string">'白色'</span>);   </span><br><span class="line"><span class="keyword">var</span> dog2 = <span class="keyword">new</span> Dog(<span class="string">'黑色'</span>);     </span><br><span class="line">dog.type.push(<span class="string">'dog'</span>);   </span><br><span class="line"><span class="built_in">console</span>.log(dog.color); <span class="comment">// "白色"</span></span><br><span class="line"><span class="built_in">console</span>.log(dog.type);  <span class="comment">// ["pig", "cat", "dog"]</span></span><br><span class="line"><span class="built_in">console</span>.log(dog2.type); <span class="comment">// ["pig", "cat"]</span></span><br><span class="line"><span class="built_in">console</span>.log(dog2.color);  <span class="comment">// "黑色"</span></span><br><span class="line">dog.greet(<span class="string">'汪汪'</span>);  <span class="comment">// "汪汪"</span></span><br></pre></td></tr></table></figure>

<p>　　在上面的例子中，我们在子类构造函数中执行父类构造函数，在子类原型上实例化父类，这就是组合继承了，可以看到它综合了类式继承和构造函数继承的优点，同时去除了缺陷。</p>
<p>　　可能你会奇怪为什么组合式继承可以去除类式继承中的引用缺陷？其实这是由于原型链来决定的，由于JavaScript引擎在访问对象的属性时，会先在对象本身中查找，如果没有找到，才会去原型链中查找，如果找到，则返回值，如果整个原型链中都没有找到这个属性，则返回undefined。</p>
<p>　　也就是说，我们访问到的引用类型(比如上面的type)其实是通过apply复制到子类中的，所以不会发生共享。</p>
<p>　　这种组合继承也是有点小缺陷的，那就是它调用了两次父类的构造函数。</p>
<h2 id="4-寄生组合式继承"><a href="#4-寄生组合式继承" class="headerlink" title="4. 寄生组合式继承"></a>4. 寄生组合式继承</h2><p>寄生组合式继承强化的部分就是在组合继承的基础上减少一次多余的调用父类的构造函数：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Animal</span>(<span class="params">color</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.color = color;</span><br><span class="line">  <span class="keyword">this</span>.name = <span class="string">'animal'</span>;</span><br><span class="line">  <span class="keyword">this</span>.type = [<span class="string">'pig'</span>, <span class="string">'cat'</span>];</span><br><span class="line">&#125;</span><br><span class="line">Animal.prototype.greet = <span class="function"><span class="keyword">function</span>(<span class="params">sound</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(sound);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Dog</span>(<span class="params">color</span>) </span>&#123;</span><br><span class="line">  Animal.apply(<span class="keyword">this</span>, <span class="built_in">arguments</span>);</span><br><span class="line">  <span class="keyword">this</span>.name = <span class="string">'dog'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 注意下面两行 */</span></span><br><span class="line">Dog.prototype = <span class="built_in">Object</span>.create(Animal.prototype);</span><br><span class="line">Dog.prototype.constructor = Dog;</span><br><span class="line">Dog.prototype.getName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.name);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> dog = <span class="keyword">new</span> Dog(<span class="string">'白色'</span>);   </span><br><span class="line"><span class="keyword">var</span> dog2 = <span class="keyword">new</span> Dog(<span class="string">'黑色'</span>);     </span><br><span class="line">dog.type.push(<span class="string">'dog'</span>);   </span><br><span class="line"><span class="built_in">console</span>.log(dog.color);   <span class="comment">// "白色"</span></span><br><span class="line"><span class="built_in">console</span>.log(dog.type);   <span class="comment">// ["pig", "cat", "dog"]</span></span><br><span class="line"><span class="built_in">console</span>.log(dog2.type);  <span class="comment">// ["pig", "cat"]</span></span><br><span class="line"><span class="built_in">console</span>.log(dog2.color);  <span class="comment">// "黑色"</span></span><br><span class="line">dog.greet(<span class="string">'汪汪'</span>);  <span class="comment">//  "汪汪"</span></span><br></pre></td></tr></table></figure>

<p>　　在上面的例子中，我们并不像构造函数继承一样直接将父类Animal的一个实例赋值给Dog.prototype，而是使用Object.create()进行一次浅拷贝，将父类原型上的方法拷贝后赋给Dog.prototype，这样子类上就能拥有了父类的共有方法，而且少了一次调用父类的构造函数。</p>
<p>　　<code>Object.create()</code>的浅拷贝的作用类式下面的函数：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">create</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">F</span>(<span class="params"></span>) </span>&#123;&#125;;</span><br><span class="line">  F.prototype = obj;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> F();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　这里还需注意一点，由于对Animal的原型进行了拷贝后赋给Dog.prototype，因此Dog.prototype上的constructor属性也被重写了，所以我们要修复这一个问题：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">Dog.prototype.constructor = Dog;</span><br></pre></td></tr></table></figure>

<h2 id="5-extends继承"><a href="#5-extends继承" class="headerlink" title="5. extends继承"></a>5. extends继承</h2><p>　　<code>Class</code>和<code>extends</code>是在ES6中新增的，<code>Class</code>用来创建一个类，<code>extends</code>用来实现继承：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Animal</span> </span>&#123;   </span><br><span class="line">  <span class="keyword">constructor</span>(color) &#123;   </span><br><span class="line">    <span class="keyword">this</span>.color = color;   </span><br><span class="line">  &#125;   </span><br><span class="line">  greet(sound) &#123;   </span><br><span class="line">    <span class="built_in">console</span>.log(sound);   </span><br><span class="line">  &#125;  </span><br><span class="line">&#125;   </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Dog</span> <span class="keyword">extends</span> <span class="title">Animal</span> </span>&#123;   </span><br><span class="line">  <span class="keyword">constructor</span>(color) &#123;   </span><br><span class="line">    <span class="keyword">super</span>(color);   </span><br><span class="line">    <span class="keyword">this</span>.color = color;   </span><br><span class="line">  &#125;  </span><br><span class="line">&#125;   </span><br><span class="line"><span class="keyword">let</span> dog = <span class="keyword">new</span> Dog(<span class="string">'黑色'</span>);  </span><br><span class="line">dog.greet(<span class="string">'汪汪'</span>);  <span class="comment">// "汪汪"</span></span><br><span class="line"><span class="built_in">console</span>.log(dog.color); <span class="comment">// "黑色"</span></span><br></pre></td></tr></table></figure>

<p>　　在上面的代码中，创建了父类Animal，然后Dog子类继承父类，两个类中都有一个<code>constructor</code>构造方法，实质就是构造函数Animal和Dog。</p>
<p>　　不知道你有没有注意到一点，我在子类的构造方法中调用了<code>super</code>方法，它表示父类的构造函数，用来新建父类的<code>this</code>对象。</p>
<blockquote>
<p><strong>注意</strong>：子类必须在<code>constructor</code>方法中调用<code>super</code>方法，否则新建实例时会报错。这是因为子类没有自己的<code>this</code>对象，而是继承父类的<code>this</code>对象，然后对其进行加工。如果不调用<code>super</code>方法，子类就得不到<code>this</code>对象。</p>
</blockquote>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JS 构造函数内的方法与构造函数prototype属性上方法的对比</title>
    <url>/2017/04/08/article-js-constructor-vs-prototype/</url>
    <content><![CDATA[<p>　　最近用面向对象的思路写一个小小的H5小游戏，对之前零散的知识点进行了一下总结，本文的目的是让大家理解什么情况下把函数的方法写在JavaScript的构造函数上,什么时候把方法写在函数的<code>prototype</code>属性上;以及这样做的好处。</p>
<p>　　为了阅读方便,我们约定一下:把方法写在构造函数内的情况我们简称为<strong>函数内方法</strong>,把方法写在<code>prototype</code>属性上的情况我们简称为<strong>prototype上的方法</strong>。<a id="more"></a></p>
<p><strong>首先我们先了解一下这篇文章的重点:</strong></p>
<ul>
<li><strong>函数内的方法:</strong> 使用函数内的方法我们可以<strong>访问到函数内部的私有变量</strong>,如果我们通过构造函数<code>new</code>出来的对象需要我们操作构造函数内部的私有变量的话,<br>我们这个时候就要考虑使用函数内的方法.</li>
<li><strong>prototype上的方法:</strong> 当我们需要<strong>通过一个函数创建大量的对象</strong>,并且这些对象还都有许多的方法的时候;这时我们就要考虑在函数的<code>prototype</code>上添加这些方法.<br>这种情况下我们代码的<strong>内存占用</strong>就比较小.</li>
<li><strong>在实际的应用中,这两种方法往往是结合使用的;所以我们要首先了解我们需要的是什么,然后再去选择如何使用.</strong></li>
</ul>
<p>我们还是根据下面的代码来说明一下这些要点吧,下面是代码部分:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 构造函数A</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name || <span class="string">'a'</span>;</span><br><span class="line">    <span class="keyword">this</span>.sayHello = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'Hello, my name is: '</span> + <span class="keyword">this</span>.name);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 构造函数B</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">B</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name || <span class="string">'b'</span>;</span><br><span class="line">&#125;</span><br><span class="line">B.prototype.sayHello = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'Hello, my name is: '</span> + <span class="keyword">this</span>.name);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> a1 = <span class="keyword">new</span> A(<span class="string">'a1'</span>);</span><br><span class="line"><span class="keyword">var</span> a2 = <span class="keyword">new</span> A(<span class="string">'a2'</span>);</span><br><span class="line">a1.sayHello();</span><br><span class="line">a2.sayHello();</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> b1 = <span class="keyword">new</span> B(<span class="string">'b1'</span>);</span><br><span class="line"><span class="keyword">var</span> b2 = <span class="keyword">new</span> B(<span class="string">'b2'</span>);</span><br><span class="line">b1.sayHello();</span><br><span class="line">b2.sayHello();</span><br></pre></td></tr></table></figure>

<p>我们首先写了两个构造函数,第一个是<code>A</code>,这个构造函数里面包含了一个方法<code>sayHello</code>;第二个是构造函数<code>B</code>,<br>我们把那个方法<code>sayHello</code>写在了构造函数<code>B</code>的<code>prototype</code>属性上面.</p>
<p>需要指出的是,通过这两个构造函数<code>new</code>出来的对象具有一样的属性和方法,但是它们的区别我们可以通过下面的一个图来说明:</p>
<p><img src="/styles/images/js/constructor-vs-prototype.jpg" alt="1"></p>
<p>我们通过使用构造函数<code>A</code>创建了两个对象,分别是<code>a1</code>,<code>a2</code>;通过构造函数<code>B</code>创建了两个对象<code>b1</code>,<code>b2</code>;我们可以发现<code>b1</code>,<code>b2</code>这两个对象的那个<code>sayHello</code>方法都是指向了它们的构造函数的<code>prototype</code>属性的<code>sayHello</code>方法.而<code>a1</code>,<code>a2</code>都是在自己内部定义了这个方法。</p>
<p><strong>定义在构造函数内部的方法,会在它的每一个实例上都克隆这个方法;定义在构造函数的<code>prototype</code>属性上的方法会让它的所有示例都共享这个方法,但是不会在每个实例的内部重新定义这个方法.</strong></p>
<p><strong>如果我们的应用需要创建很多新的对象,并且这些对象还有许多的方法,为了节省内存,我们建议把这些方法都定义在构造函数的<code>prototype</code>属性上</strong></p>
<p>当然,在某些情况下,我们需要将某些方法定义在构造函数中,这种情况一般是因为我们需要<strong>访问构造函数内部的私有变量</strong>.</p>
<p>下面我们举一个两者结合的例子,代码如下:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name, family</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name;</span><br><span class="line">    <span class="keyword">this</span>.family = family;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">var</span> records = [&#123;<span class="attr">type</span>: <span class="string">"in"</span>, <span class="attr">amount</span>: <span class="number">0</span>&#125;];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">this</span>.addTransaction = <span class="function"><span class="keyword">function</span>(<span class="params">trans</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(trans.hasOwnProperty(<span class="string">"type"</span>) &amp;&amp; trans.hasOwnProperty(<span class="string">"amount"</span>)) &#123;</span><br><span class="line">           records.push(trans);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">this</span>.balance = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">       <span class="keyword">var</span> total = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">       records.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">record</span>) </span>&#123;</span><br><span class="line">           <span class="keyword">if</span>(record.type === <span class="string">"in"</span>) &#123;</span><br><span class="line">             total += record.amount;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="keyword">else</span> &#123;</span><br><span class="line">             total -= record.amount;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;);</span><br><span class="line">    </span><br><span class="line">        <span class="keyword">return</span> total;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Person.prototype.getFull = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.name + <span class="string">" "</span> + <span class="keyword">this</span>.family;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Person.prototype.getProfile = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">     <span class="keyword">return</span> <span class="keyword">this</span>.getFull() + <span class="string">", total balance: "</span> + <span class="keyword">this</span>.balance();</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>在上面的代码中,我们定义了一个<code>Person</code>构造函数;这个函数有一个内部的私有变量<code>records</code>,这个变量我们是不希望通过函数内部以外的方法去操作这个变量,所以我们把操作这个变量的方法都写在了函数的内部.而把一些可以公开的方法写在了<code>Person</code>的<code>prototype</code>属性上,比如方法<code>getFull</code>和<code>getProfile</code>.</p>
<p><strong>把方法写在构造函数的内部,增加了通过构造函数初始化一个对象的成本,把方法写在<code>prototype</code>属性上就有效的减少了这种成本.</strong></p>
<p><strong>你也许会觉得,调用对象上的方法要比调用它的原型链上的方法快得多,其实并不是这样的,如果你的那个对象上面不是有很多的原型的话,它们的速度其实是差不多的</strong></p>
<p>另外,需要注意的一些地方:</p>
<ul>
<li><p>首先如果是在函数的<code>prototype</code>属性上定义方法的话,要牢记一点,如果你改变某个方法,那么由这个构造函数产生的所有对象的那个方法都会被改变.</p>
</li>
<li><p>还有一点就是变量提升的问题,我们可以稍微的看一下下面的代码:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">func1(); <span class="comment">// 这里会报错,因为在函数执行的时候,func1还没有被赋值. error: func1 is not a function</span></span><br><span class="line"><span class="keyword">var</span> func1 = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'func1'</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">func2(); <span class="comment">// 这个会被正确执行,因为函数的声明会被提升.</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">func2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'func2'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>关于对象序列化的问题.定义在函数的<code>prototype</code>上的属性不会被序列化,可以看下面的代码:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params">name</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name;</span><br><span class="line">&#125;</span><br><span class="line">A.prototype.sayWhat = <span class="string">'say what...'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> a = <span class="keyword">new</span> A(<span class="string">'dreamapple'</span>);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">JSON</span>.stringify(a));</span><br></pre></td></tr></table></figure>

<p>我们可以看到输出结果是<code>{&quot;name&quot;:&quot;dreamapple&quot;}</code></p>
</li>
</ul>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS 奇巧淫技</title>
    <url>/2017/03/26/article-css3-skills-effects/</url>
    <content><![CDATA[<p>　　能使用html/css解决的问题就不要使用JS，为什么呢？ 两个字，因为简单。简单就意味着更快的开发速度，更小的维护成本，同时往往具有更好的体验(但是也不要太局限于这个思路，好的方法是适用才好，而不是为了追求性能而性能)，JS 几乎可以做任何事情，但是有时候会显得十分笨拙，在js/html/css三者间灵活地切换，往往会极大地简化开发，没有谁是最好的语言，只有适不适合。只要用得好，不管黑猫白猫，都是好猫。下面总结几个实例。<a id="more"></a></p>
<h2 id="1-自定义radio-checkbox的样式"><a href="#1-自定义radio-checkbox的样式" class="headerlink" title="1. 自定义radio/checkbox的样式"></a>1. 自定义radio/checkbox的样式</h2><p>　　我们知道，使用原生的radio/checkbox是不可以改变它的样式的，得自己用div/span去画，然后再去监听点击事件。但是这样需要自己去写逻辑控制，例如radio只能选一个的功能，另一个是没有办法使用change事件。就是没有用原生的方便。</p>
<p>　　但是实际上可以用一点CSS3的技巧实现自定义的目的，如下，就是用原生实现的radio：</p>
<p><img src="/styles/images/css/radio_checkbox.gif" alt="原生实现的radio"></p>
<p>　　这个主要是借助了CSS3提供的一个伪类:checkd，只要radio/checkbox是选中状态，这个伪类就会生效，因此可以利用选中和非选中的这两种状态，去切换不同的样式。如下把一个checkbox和一个用来自定义样式的span写在一个label里面，checkbox始终隐藏：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="css"><span class="selector-tag">input</span><span class="selector-attr">[type=checkbox]</span>&#123;</span></span><br><span class="line">    display: none;</span><br><span class="line">&#125;</span><br><span class="line"><span class="css"><span class="comment">/*未选中的checkbox的样式*/</span></span></span><br><span class="line"><span class="css"><span class="selector-class">.checkbox</span>&#123;</span></span><br><span class="line"> </span><br><span class="line">&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">label</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"checkbox"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"checkbox"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　写在label里面是为了能够点击span的时候改变checkbox的状态，然后再改一下选中态的样式即可：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">input</span><span class="selector-attr">[type=checkbox]</span><span class="selector-pseudo">:checked</span> + <span class="selector-class">.checkbox</span>&#123;</span><br><span class="line"> ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　关键在于这一步，添加一个打勾的背景图也好，使用图标字体也好。<br>  :checked兼容性还是比较好的，只要你不用兼容IE8就可以使用，或者说只要你可以用nth-of-type，就可以用:checked</p>
<h2 id="2-多列等高"><a href="#2-多列等高" class="headerlink" title="2. 多列等高"></a>2. 多列等高</h2><p>　　多列等高的问题是这样的，排成一行的几列由于内容长短不一致，导致容器的高度不一致：</p>
<p><img src="/styles/images/css/column_height1.png" alt="2"></p>
<p>　　你可以用js算一下，以最高的一列的高度去设置所有列的高度，然而这个会造成页面闪动，刚开始打开页面的时候高度不一致，然后发现突然又对齐了。这个解决办法主要有两种：</p>
<ul>
<li>第一种是利用<strong>CSS 伸缩盒布局</strong>，如下：</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="css">	<span class="selector-class">.wrapper</span> &#123;</span></span><br><span class="line">		display: flex;</span><br><span class="line">		flex-direction: row</span><br><span class="line">	&#125;</span><br><span class="line">			</span><br><span class="line"><span class="css">	<span class="selector-class">.wrapper</span>&gt;<span class="selector-tag">div</span> &#123;</span></span><br><span class="line">		width: 150px;</span><br><span class="line">		margin: 0 10px;</span><br><span class="line">		text-align: center;</span><br><span class="line"><span class="css">		<span class="selector-tag">background-color</span>: <span class="selector-id">#ececec</span>;</span></span><br><span class="line"><span class="css">		<span class="selector-tag">border</span>: 1<span class="selector-tag">px</span> <span class="selector-tag">solid</span> <span class="selector-id">#ccc</span>;</span></span><br><span class="line">	&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"wrapper"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span>column 1<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span>column 2<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span>column 3<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span>column 4<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　效果如下：</p>
<p><img src="/styles/images/css/column_height2.png" alt="2"></p>
<p>　　效果不错，只是兼容性你懂的，其他的关于flexbox的内容可以移步<a href="/2017/01/15/css-flex/">Flex布局新旧混合写法详解</a>。</p>
<ul>
<li>第二种办法是<strong>借助table的自适应</strong>特性:</li>
</ul>
<p>　　每个div都是一个td，td肯定是等高的，html结构不变，CSS改一下： </p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.wrapper</span>&#123;</span><br><span class="line">    <span class="attribute">display</span>: table;</span><br><span class="line">    <span class="attribute">border-spacing</span>: <span class="number">20px</span>;  <span class="comment">/* td间的间距*/</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="selector-class">.wrapper</span> &gt; <span class="selector-tag">div</span> &#123;</span><br><span class="line">    <span class="attribute">display</span>: table-cell;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">1000px</span>;         <span class="comment">/*设置很大的宽度，table自动平分宽度 */</span></span><br><span class="line">    <span class="attribute">border-radius</span>: <span class="number">5px</span>;    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-需要根据个数显示不同样式"><a href="#3-需要根据个数显示不同样式" class="headerlink" title="3. 需要根据个数显示不同样式"></a>3. 需要根据个数显示不同样式</h2><p>　　例如说可能有1~3个item显示在同一行，而item的个数不一定，如果1个，那这个item占宽100%，2个时每一个50%，3个时每一个33%，这个你也可以用js计算一下，但是用CSS3就可以解决这个问题：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line">    li&#123; </span><br><span class="line">        width: 100%;</span><br><span class="line">    &#125;   </span><br><span class="line"><span class="css">    <span class="selector-tag">li</span><span class="selector-pseudo">:first-child</span><span class="selector-pseudo">:nth-last-child(2)</span>,</span></span><br><span class="line"><span class="css">    <span class="selector-tag">li</span><span class="selector-pseudo">:first-child</span><span class="selector-pseudo">:nth-last-child(2)</span> ~ <span class="selector-tag">li</span>&#123; </span></span><br><span class="line">        width: 50%;</span><br><span class="line">    &#125;   </span><br><span class="line">    </span><br><span class="line"><span class="css">    <span class="selector-tag">li</span><span class="selector-pseudo">:first-child</span><span class="selector-pseudo">:nth-last-child(3)</span>,</span></span><br><span class="line"><span class="css">    <span class="selector-tag">li</span><span class="selector-pseudo">:first-child</span><span class="selector-pseudo">:nth-last-child(3)</span> ~ <span class="selector-tag">li</span>&#123; </span></span><br><span class="line">        width: 33%; </span><br><span class="line">    &#125;   </span><br><span class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">ul</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>3<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　第5行的意思就是选择li的第一个元素，并且它是倒数第二个元素，第6行的意思是选择前面有是第一个且是倒数第二个li的所有li，第一行是选择了第一个，第二行选择除第一个外的其它所有元素。有三个元素的类似。</p>
<h2 id="4-巧用CSS3伪类"><a href="#4-巧用CSS3伪类" class="headerlink" title="4. 巧用CSS3伪类"></a>4. 巧用CSS3伪类</h2><p>　　CSS3的伪类提供了状态切换，除了第3点提到的checked之外，还有其它几个很好用的，例如:focus、:invalid等;</p>
<ul>
<li>例如下面的效果：focus的时候把左边的放大镜颜色加深：</li>
</ul>
<p><img src="/styles/images/css/css3_focus.gif" alt="2"></p>
<p>　　借用:focus实现：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="css">    <span class="comment">/*正常状态为浅灰色*/</span></span></span><br><span class="line"><span class="css">    <span class="selector-class">.icon-search</span>&#123;</span></span><br><span class="line"><span class="css">        <span class="selector-tag">color</span>: <span class="selector-id">#ccc</span>;</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="css">    <span class="comment">/*input focus时为深灰色 */</span></span></span><br><span class="line"><span class="css">    <span class="selector-tag">input</span><span class="selector-attr">[type=search]</span><span class="selector-pseudo">:focus</span> + <span class="selector-class">.icon-search</span>&#123;</span></span><br><span class="line"><span class="css">        <span class="selector-tag">color</span>: <span class="selector-id">#111</span>;</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"> </span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"search"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"icon-search"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>再如，如果用户输入不合法，则下一步是半透明不可点的状态：</li>
</ul>
<p><img src="/styles/images/css/css3_invalid.gif" alt="2"></p>
<p>　　实现这一步可以用html5的input和css3的:invalid</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="css">    <span class="selector-tag">input</span><span class="selector-attr">[type=email]</span><span class="selector-pseudo">:invalid</span> + <span class="selector-class">.next-step</span>&#123;</span></span><br><span class="line"><span class="css">        <span class="selector-tag">opacity</span>: 0<span class="selector-class">.5</span>;</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"email"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"next-step"</span>&gt;</span>Next<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　通过input的type和pattern属性约束合法性，然后触发:invalid。</p>
<h2 id="5-自动监听回车事件（HTML）"><a href="#5-自动监听回车事件（HTML）" class="headerlink" title="5. 自动监听回车事件（HTML）"></a>5. 自动监听回车事件（HTML）</h2><p>　　这个的场景是希望按回车的时候能够触发请求，或者按回车实现跳转，或者是按下回车就送一条聊天消息。</p>
<p>　　通常的做法是监听下keypress事件，然后检查一下keycode是不是回车，如果是则发请求。</p>
<p>　　但是其实有个特别简单的办法，也是不需要一行JS，那就是把表单写在一个form里面，按回车会自动触发submit事件。读者可以自己试试。这个就启示我们要用语义的html组织，而不是全部都用div。如果用相应的html标签，浏览器会自动做一些优化，特别是表单提交的input。</p>
<hr>]]></content>
      <categories>
        <category>面试总结</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 闭包可被利用的常见场景</title>
    <url>/2017/03/18/article-js-closureuse/</url>
    <content><![CDATA[<p>　　闭包（Closures）在 JavaScript 这类支持现代函式编程（FP，Functional Programming）的语言中非常普遍。<a href="/2017/01/16/js-closure/">到底什么是闭包?</a>由于一个函式本身作为一级对象（first-class object），就像传统 OOP 中的对象一样，可以出现在任何一个值（value）允许出现的地方，因此只要你在一个（父）函式的内部声明、创建或者返回了一个新（子）函式，都有可能获得一个闭包。</p>
<p>　　下面介绍几种常见的场景。<a id="more"></a></p>
<h2 id="采用函数引用方式的setTimeout调用"><a href="#采用函数引用方式的setTimeout调用" class="headerlink" title="采用函数引用方式的setTimeout调用"></a>采用函数引用方式的setTimeout调用</h2><p>　　闭包的一个通常的用法是为一个在某一函数执行前先执行的函数提供参数。例如，在web环境中，一个函数作为setTimeout函数调用的第一个参数，是一种很常见的应用。</p>
<p>　　setTimeout将要执行的函数(或者一段javascript代码，但这不是我们要讨论的情况)作为它的第一个参数，下一个参数是需要延迟执行的时间。如果一段代码想通过setTimeout来调用，那么它需要传递一个函数对象的引用来作为第一个参数。延迟的毫秒数作为第二个参数，但这个函数对象的引用无法为将要被延迟执行的对象提供参数。</p>
<p>　　但是，可以调用另一个函数来返回一个内部函数的调用，将那个内部函数对象的引用传递给setTimeout函数。内部函数执行时需要的参数，在调用外部函数时传递给它。setTimeout在执行内部函数时无需传递参数，因为内部函数仍然能够访问外部函数调用时提供的参数：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">callLater</span>(<span class="params">paramA, paramB, paramC</span>) </span>&#123;</span><br><span class="line">    <span class="comment">/*使用函数表达式创建并放回一个匿名内部函数的引用*/</span></span><br><span class="line">    <span class="keyword">return</span> (<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        这个内部函数将被setTimeout函数执行；</span></span><br><span class="line"><span class="comment">        并且当它被执行时，</span></span><br><span class="line"><span class="comment">        它能够访问并操作外部函数传递过来的参数</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">        paramA[paramB] = paramC;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">调用这个函数将在它的执行上下文中创建，并最终返回内部函数对象的引用</span></span><br><span class="line"><span class="comment">传递过来的参数，内部函数在最终被执行时，将使用外部函数的参数</span></span><br><span class="line"><span class="comment">返回的引用被赋予了一个变量</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">var</span> funcRef = callLater(elStyle, <span class="string">"display"</span>, <span class="string">"none"</span>);</span><br><span class="line"><span class="comment">/*调用setTimeout函数，传递内部函数的引用作为第一个参数*/</span></span><br><span class="line">hideMenu = setTimeout(funcRef, <span class="number">500</span>);</span><br></pre></td></tr></table></figure>

<h2 id="将函数关联到对象的实例方法"><a href="#将函数关联到对象的实例方法" class="headerlink" title="将函数关联到对象的实例方法"></a>将函数关联到对象的实例方法</h2><p>　　有很多这样的场景：需要分配一个函数对象的引用，以便在未来的某个时间执行该函数。那么闭包对于为这个将要执行的函数提供引用会非常有帮助。因为该函数可能直到执行时才能够被访问。</p>
<p>　　有一个例子就是，一个javascript对象被封装用来参与一个特定DOM元素的交互。它有doOnClick、doMouseOver以及doMouseOut方法。并且想在DOM元素上对应的事件被触发时执行这些方法。但是，可能会有关联着DOM元素的任意数量的javascript对象被创建，并且单个的实例并不知道那些实例化它们的代码将如何处理它们。对象实例不知道怎样去“全局”地引用它们自己，因为它们不知道哪个全局变量(如果存在)的引用将被分配给它们。</p>
<p>　　所以，问题是执行一个与特定javascript对象实例关联的事件处理函数，并且知道调用那个对象的哪个方法。</p>
<p>　　接下来的一个例子，在有元素事件处理的对象实例的关联函数上使用一个简单的闭包。通过传递event对象以及要关联元素的一个引用，为事件处理器分配不同的对象实例方法以供调用。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">一个给对象实例关联一个事件处理器的普通方法，</span></span><br><span class="line"><span class="comment">返回的内部函数被作为事件的处理器，</span></span><br><span class="line"><span class="comment">对象实例被作为obj参数，对象上将要被调用的方法名称被作为第二个参数</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">associateObjWithEvent</span>(<span class="params">obj, methodName</span>) </span>&#123;</span><br><span class="line">    <span class="comment">/*返回的内部函数被用来作为一个DOM元素的事件处理器*/</span></span><br><span class="line">    <span class="keyword">return</span> (<span class="function"><span class="keyword">function</span> (<span class="params">e</span>) </span>&#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        事件对象在DOM标准的浏览器中将被转换为e参数，</span></span><br><span class="line"><span class="comment">        如果没有传递参数给事件处理内部函数，将统一处理成IE的事件对象</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">        e = e || <span class="built_in">window</span>.event;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        事件处理器调用obj对象上的以methodName字符串标识的方法</span></span><br><span class="line"><span class="comment">        并传递两个对象：通用的事件对象，事件处理器被订阅的元素的引用</span></span><br><span class="line"><span class="comment">        这里this参数能够使用，因为内部函数已经被执行作为事件处理器所在元素的一个方法</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">        <span class="keyword">return</span> obj[methodName](e, <span class="keyword">this</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">这个构造器函数，通过将元素的ID作为字符串参数传递进来，</span></span><br><span class="line"><span class="comment">来创建将自身关联到DOM元素上的对象，</span></span><br><span class="line"><span class="comment">对象实例想在对应的元素触发onclick、onmouseover、onmouseout事件时</span></span><br><span class="line"><span class="comment">对应的方法被调用。</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">DhtmlObject</span>(<span class="params">elementId</span>) </span>&#123;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    调用一个方法来获得一个DOM元素的引用</span></span><br><span class="line"><span class="comment">    如果没有找到，则为null</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">var</span> el = getElementWith(elementId);</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    因为if语句块，el变量的值在内部进行了类型转换，变成了boolean类型</span></span><br><span class="line"><span class="comment">    所以当它指向一个对象，结果就为true,如果为null则为false</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">if</span> (el) &#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        为了给元素指定一个事件处理函数，调用了associateObjWithEvent函数，</span></span><br><span class="line"><span class="comment">        利用它自己(this关键字)作为被调用方法的对象，并且提供方法名称</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">        el.onclick = associateObjWithEvent(<span class="keyword">this</span>, <span class="string">"doOnClick"</span>);</span><br><span class="line">        el.onmouseover = associateObjWithEvent(<span class="keyword">this</span>, <span class="string">"doOnMouseOver"</span>);</span><br><span class="line">        el.onmouseout = associateObjWithEvent(<span class="keyword">this</span>, <span class="string">"doOnMouseOut"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">DhtmlObject.prototype.doOnClick = <span class="function"><span class="keyword">function</span> (<span class="params">event, element</span>) </span>&#123;</span><br><span class="line">    <span class="comment">//doOnClick body</span></span><br><span class="line">&#125;</span><br><span class="line">DhtmlObject.prototype.doMouseOver = <span class="function"><span class="keyword">function</span> (<span class="params">event, element</span>) </span>&#123;</span><br><span class="line">    <span class="comment">//doMouseOver body</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">DhtmlObject.prototype.doMouseOut = <span class="function"><span class="keyword">function</span> (<span class="params">event, element</span>) </span>&#123;</span><br><span class="line">    <span class="comment">//doMouseOut body</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　任何DhtmlObject的实例都能够将它们自身关联到它们感兴趣的DOM元素上去，不需要去担心这些元素将被其他的代码怎么处理，以及被全局命名空间“污染”或者与其他的DhtmlObject的实例产生冲突。</p>
<h2 id="封装相关的功能集"><a href="#封装相关的功能集" class="headerlink" title="封装相关的功能集"></a>封装相关的功能集</h2><p>　　闭包可以创建额外的scope，这可以被用来组合相关的或有依赖性的代码。用这种方式可以最大限度地减少代码干扰的危害。假设，一个函数被用来创建一个字符串并且避免重复串联的操作(比如创建一系列的中间字符串)。一个想法是，用一个数组来顺序存储字符串的一部分，然后使用Array.prototype.join方法输出结果(使用一个空字符串作为它的参数)。数组将扮演着输出缓冲区的角色，但局部定义它又将会导致它在函数的每次执行时再次创建。如果这个数组只是作为唯一的变量被分配给每一个函数调用，这将会有点小题大做。</p>
<p>　　一个解决方案是将数组提升为全局变量，让它不需要被再次创建也能够再次使用。但结果并不是想的那么简单，另外，一个全局变量关联这使用缓冲数组的函数，那将会有第二个全局属性(函数本身也是window对象的属性)关联这个数组，这将让代码失去一定的可控性。因为如果将它使用在其他地方。这段代码的创建者不得不记住包含函数的定义以及数组的定义逻辑。它也使得代码不那么容易与其他代码整合，因为将从原来只需要确定函数名是否在全局命名空间中唯一，变成有必要确定和该函数关联的数组的名称是否在全局命名空间中保持唯一。</p>
<p>　　一个闭包可以让缓冲数组关联(干净地包含)它依赖的函数，并且同时保持缓冲数组的属性名称，像被分配在全局空间中一样，同时能够避免名称冲突以及代码交互干扰的危险。</p>
<p>　　这里有一招就是通过执行一个内联的函数表达式创建一个额外的执行上下文，让那个函数表达式返回一个内联的函数，该函数被外部代码使用。缓冲数组被定义在内联执行的函数表达式中，作为一个局部变量。它仅被调用一次，所以该数组只被创建一次。但是对于依赖它的函数来说该数组是一直可访问的，并且可被重用的。</p>
<p>　　接一下的代码创建了一个函数，将返回一个HTML字符串，其中的一部分是不变的，但那些不变的字符串需要被穿插进作为参数传递进来的变量中。</p>
<p>　　一个内联执行的函数表达式返回了内部函数对象的一个引用。并且分配了一个全局变量，让它可以被作为一个全局函数来调用。而缓冲数组作为一个局部变量被定义在外部函数表达式中。它没有被扩展到全局命名空间中，并且无论函数什么时候使用它都不需要被再次创建。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> 定义一个全局变量：getImgInPositionedDivHtml</span></span><br><span class="line"><span class="comment"> 被赋予对外部函数表达式一次调用返回的一个内部函数表达式</span></span><br><span class="line"><span class="comment"> </span></span><br><span class="line"><span class="comment"> 内部函数返回了一个HTML字符串，代表一个绝对定位的DIV</span></span><br><span class="line"><span class="comment"> 包裹这一个IMG元素，而所有的变量值都被作为函数调用的参数</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">var</span> getImgInPositionedDivHtml = (<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    buffAr 数组被定义在外部函数表达式中，作为一个局部变量</span></span><br><span class="line"><span class="comment">    它只被创建一次。数组的唯一实例对内部函数是可见的，</span></span><br><span class="line"><span class="comment">    所以它可以被用于每一次的内部函数执行</span></span><br><span class="line"><span class="comment"> </span></span><br><span class="line"><span class="comment">    空字符串仅仅被用来作为一个占位符，它将被内部函数的参数代替</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">var</span> buffAr = [</span><br><span class="line">         <span class="string">'&lt;div id="'</span>,</span><br><span class="line">        <span class="string">''</span>,   <span class="comment">//index 1, DIV ID attribute</span></span><br><span class="line">        <span class="string">'" style="position:absolute;top:'</span>,</span><br><span class="line">        <span class="string">''</span>,   <span class="comment">//index 3, DIV top position</span></span><br><span class="line">        <span class="string">'px;left:'</span>,</span><br><span class="line">        <span class="string">''</span>,   <span class="comment">//index 5, DIV left position</span></span><br><span class="line">        <span class="string">'px;width:'</span>,</span><br><span class="line">        <span class="string">''</span>,   <span class="comment">//index 7, DIV width</span></span><br><span class="line">        <span class="string">'px;height:'</span>,</span><br><span class="line">        <span class="string">''</span>,   <span class="comment">//index 9, DIV height</span></span><br><span class="line">        <span class="string">'px;overflow:hidden;\"&gt;&lt;img src=\"'</span>,</span><br><span class="line">        <span class="string">''</span>,   <span class="comment">//index 11, IMG URL</span></span><br><span class="line">        <span class="string">'\" width=\"'</span>,</span><br><span class="line">        <span class="string">''</span>,   <span class="comment">//index 13, IMG width</span></span><br><span class="line">        <span class="string">'\" height=\"'</span>,</span><br><span class="line">        <span class="string">''</span>,   <span class="comment">//index 15, IMG height</span></span><br><span class="line">        <span class="string">'\" alt=\"'</span>,</span><br><span class="line">        <span class="string">''</span>,   <span class="comment">//index 17, IMG alt text</span></span><br><span class="line">        <span class="string">'\"&gt;&lt;\/div&gt;'</span></span><br><span class="line">    ];</span><br><span class="line"> </span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    返回一个内部函数对象，他是函数表达式执行返回的结果</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">return</span> (<span class="function"><span class="keyword">function</span> (<span class="params">url, id, width, height, top, left, altText</span>) </span>&#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        分配各种参数给对应的数组元素</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">        buffAr[<span class="number">1</span>] = id;</span><br><span class="line">        buffAr[<span class="number">3</span>] = top;</span><br><span class="line">        buffAr[<span class="number">5</span>] = left;</span><br><span class="line">        buffAr[<span class="number">13</span>] = (buffAr[<span class="number">7</span>] = width);</span><br><span class="line">        buffAr[<span class="number">15</span>] = (buffAr[<span class="number">9</span>] = height);</span><br><span class="line">        buffAr[<span class="number">11</span>] = url;</span><br><span class="line">        buffAr[<span class="number">17</span>] = altText;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        返回连接每个元素后创建的字符串</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">        <span class="keyword">return</span> buffAr.join(<span class="string">''</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>　　如果一个函数依赖另一个或几个函数，但那些其他的函数并不期望与任何其他的代码产生交互。那么这个简单的技巧(使用一个对外公开的函数来扩展那些函数)就可以被用来组织那些函数。</p>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>rem 布局进阶篇</title>
    <url>/2017/03/06/article-html-rem/</url>
    <content><![CDATA[<p>　　相信很多刚开始写移动端页面的小伙伴都要面对页面自适应的问题，当然解决方案很多，比如：百分比布局，弹性布局flex（<a href="http://doubleray.win/2017/01/15/css-flex/" target="_blank" rel="noopener">什么是flex</a>），也都能获得不错的效果，这里主要介绍的是本人在实践中用的最顺手最简单的布局方案——rem 布局！</p>
<p>　　之前写 rem 布局时首先引入的<a href="http://doubleray.win/2017/02/02/js-common/#1-rem-布局处理根字体" target="_blank" rel="noopener"> JavaScript 代码</a> 特别简单。但随着时间的推移，该方案已然过时，故为大家介绍一个目前我极力推荐使用的，更加完美的方案——rem 布局（进阶版）。<a id="more"></a></p>
<p>　　新方案的使用也是相当简单，把这段 <code>原生JS</code> 放到 HTML 的 head 标签中即可（注:<strong>不要手动设置viewport，该方案自动帮你设置</strong>）</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">! <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">t</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (i[a]) <span class="keyword">return</span> i[a].exports;</span><br><span class="line">        <span class="keyword">var</span> n = i[a] = &#123;</span><br><span class="line">            exports: &#123;&#125;,</span><br><span class="line">            id: a,</span><br><span class="line">            loaded: !<span class="number">1</span></span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">return</span> e[a].call(n.exports, n, n.exports, t), n.loaded = !<span class="number">0</span>, n.exports</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> i = &#123;&#125;;</span><br><span class="line">    <span class="keyword">return</span> t.m = e, t.c = i, t.p = <span class="string">""</span>, t(<span class="number">0</span>)</span><br><span class="line">&#125;([<span class="function"><span class="keyword">function</span>(<span class="params">e, t</span>) </span>&#123;</span><br><span class="line"><span class="meta">    "use strict"</span>;</span><br><span class="line">    <span class="built_in">Object</span>.defineProperty(t, <span class="string">"__esModule"</span>, &#123;</span><br><span class="line">        value: !<span class="number">0</span></span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">var</span> i = <span class="built_in">window</span>;</span><br><span class="line">    t[<span class="string">"default"</span>] = i.flex = <span class="function"><span class="keyword">function</span>(<span class="params">e, t</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> a = e || <span class="number">100</span>,</span><br><span class="line">            n = t || <span class="number">1</span>,</span><br><span class="line">            r = i.document,</span><br><span class="line">            o = navigator.userAgent,</span><br><span class="line">            d = o.match(<span class="regexp">/Android[\S\s]+AppleWebkit\/(\d&#123;3&#125;)/i</span>),</span><br><span class="line">            l = o.match(<span class="regexp">/U3\/((\d+|\.)&#123;5,&#125;)/i</span>),</span><br><span class="line">            c = l &amp;&amp; <span class="built_in">parseInt</span>(l[<span class="number">1</span>].split(<span class="string">"."</span>).join(<span class="string">""</span>), <span class="number">10</span>) &gt;= <span class="number">80</span>,</span><br><span class="line">            p = navigator.appVersion.match(<span class="regexp">/(iphone|ipad|ipod)/gi</span>),</span><br><span class="line">            s = i.devicePixelRatio || <span class="number">1</span>;</span><br><span class="line">        p || d &amp;&amp; d[<span class="number">1</span>] &gt; <span class="number">534</span> || c || (s = <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">var</span> u = <span class="number">1</span> / s,</span><br><span class="line">            m = r.querySelector(<span class="string">'meta[name="viewport"]'</span>);</span><br><span class="line">        m || (m = r.createElement(<span class="string">"meta"</span>), m.setAttribute(<span class="string">"name"</span>, <span class="string">"viewport"</span>), r.head.appendChild(m)), m.setAttribute(<span class="string">"content"</span>, <span class="string">"width=device-width,user-scalable=no,initial-scale="</span> + u + <span class="string">",maximum-scale="</span> + u + <span class="string">",minimum-scale="</span> + u), r.documentElement.style.fontSize = a / <span class="number">2</span> * s * n + <span class="string">"px"</span></span><br><span class="line">    &#125;, e.exports = t[<span class="string">"default"</span>]</span><br><span class="line">&#125;]);</span><br><span class="line">flex(<span class="number">100</span>, <span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<h2 id="代码原理"><a href="#代码原理" class="headerlink" title="代码原理"></a>代码原理</h2><p>　　这是<em>阿里团队</em>的高清方案布局代码，所谓高清方案就是根据设备屏幕的DPR（设备像素比，又称DPPX，比如dpr=2时，表示1个CSS像素由4个物理像素点组成） <strong>动态设置 html 的 font-size, 同时根据设备 DPR 调整页面的缩放值，进而达到高清效果</strong>。</p>
<h2 id="方案优势"><a href="#方案优势" class="headerlink" title="方案优势"></a>方案优势</h2><ul>
<li>引用简单，布局简便</li>
<li>根据设备屏幕的DPR,自动设置最合适的高清缩放。</li>
<li>保证了不同设备下视觉体验的一致性。（老方案是，屏幕越大元素越大；此方案是，屏幕越大，看的越多）</li>
<li>有效解决移动端真实1px问题（这里的1px 是设备屏幕上的物理像素）</li>
</ul>
<h2 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h2><blockquote>
<p><strong>绝不是每个地方都要用rem，rem只适用于固定尺寸！</strong></p>
</blockquote>
<p>　　在相当数量的布局情境中（比如底部导航元素平分屏幕宽，大尺寸元素），你必须使用百分比或者flex才能完美布局！</p>
<p>　　此方案也是默认 <code>1rem = 100px</code>，所以你布局的时候，完全可以按照设计师给你的效果图写各种尺寸啦。比如你在效果图上量取的某个按钮元素长 55px, 宽37px ，那你直接可以这样写样式：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.btn</span> &#123;</span><br><span class="line">   <span class="attribute">width</span>: .<span class="number">55rem</span>;</span><br><span class="line">   <span class="attribute">height</span>: .<span class="number">37rem</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="常见问题说明"><a href="#常见问题说明" class="headerlink" title="常见问题说明"></a>常见问题说明</h2><blockquote>
<p><strong>1. 为什么效果图是要640或者750的，我非得弄个666的不行吗？</strong></p>
</blockquote>
<p>　　老实说当然可以，不过为了规范，640或者750是相对合适的。</p>
<p>　　拿Iphone 5s 举例，它的css像素宽度是320px，由于它的dpr=2，所以它的物理像素宽度为320 × 2 = 640px，这也就是为什么，你在5s上截了一张图，在电脑上打开，它的原始宽度是640px的原因。</p>
<ul>
<li>那 iphone 6 的截图宽度呢？ 375 × 2 = 750</li>
<li>那 iphone 6 sp 的截图宽度呢？ 414 × 3 = 1242</li>
</ul>
<p>　　以此类推，你现在能明白效果图为什么一般是 640 ，750 甚至是 1242 的原因了么？（真没有歧视安卓机的意思。。。）</p>
<blockquote>
<p><strong>2. 宽度用 rem 写的情况下， 在 iphone6 上没问题， 在 iphone5上会有横向滚动条，何解？</strong></p>
</blockquote>
<p>　　假设你的效果图宽度是750，在这个效果图上可能有一个宽度为7rem（高清方案默认 1rem = 100px）的元素。我们知道，高清方案的特点就是几乎完美还原效果图，也就是说，你写了一个宽度为 7rem 的元素，那么在目前主流移动设备上都是7rem。然而，iphone 5 的宽度为640，也就是6.4rem。于是横向滚动条不可避免的出现了。</p>
<p>　　怎么办呢？ 这是我目前推荐的比较安全的方式：<strong>如果元素的宽度超过效果图宽度的一半（效果图宽为640或750），果断使用百分比宽度，或者flex布局。就像把等屏宽的图片宽度设为100%一样。</strong></p>
<blockquote>
<p><strong>3. 不是 1rem = 100px吗，为什么我的代码写了一个宽度为3rem的元素，在电脑端的谷歌浏览器上宽度只有150px?</strong></p>
</blockquote>
<p>　　先说高清方案代码，再次强调咱们的高清方案代码是根据设备的dpr动态设置html 的 font-size，</p>
<ul>
<li>如果dpr=1(如电脑端），则html的font-size为50px，也就是 1rem = 50px</li>
<li>如果dpr=2(如iphone 5 和 6），则html的font-size为100px，也就是 1rem = 100px</li>
<li>如果dpr=3(如iphone 6 sp），则html的font-size为150px，也就是 1rem = 150px</li>
<li>如果dpr为其他值，即便不是整数，如3.4 , 也是一样直接将dpr 乘以 50 。</li>
</ul>
<p>　　再来说说效果图，一般来讲，我们的效果图宽度要么是640，要么是750，无论哪一个，它们对应设备的dpr=2，此时，1 rem = 50 × 2 = 100px。这也就是为什么高清方案默认1rem = 100px。而将1rem默认100px也是好处多多，可以帮你快速换算单位，比如在750宽度下的效果图，某元素宽度为53px，那么css宽度直接设为53/100=0.53rem了。</p>
<p>　　然而极少情况下，有设计师将效果图宽定为 1242px，因为他手里只有一个 iphone 6 sp (dpr = 3)，设计完效果图刚好可以在他的 iphone 6 sp 里查看调整。一切完毕之后，他将这个效果图交给你来切图。由于这个效果图对应设备的 dpr=3,也就是1rem = 50 × 3 = 150px。所以如果你量取了一个宽度为 90px 的元素，它的css宽度应该为 90/150=0.6rem。因为咱们的高清方案默认 1rem=100px。所以为了还原效果图，你需要这样换算。当然，一个技巧就是你可以直接修改咱们的高清方案的默认设置。在代码的最后 你会看到 flex(100, 1) ，将其修改成 flex(150, 1)就不用那么麻烦的换算了，此时那个 90px 的直接写成 0.9rem 就可以了。</p>
<blockquote>
<p><strong>4. 高清方案在微信上，有时候字体会不受控制变的很大，怎么办？</strong></p>
</blockquote>
<p>　　在 X5 新内核 Blink 中，在排版页面的时候，会主动对字体进行放大，会检测页面中的主字体，当某一块字体在我们的判定规则中，认为字号较小，并且是页面中的主要字体，就会采取主动放大的操作。然而这不是我们想要的，可以采取给最大高度解决。（经过项目实践，还是决定给 max-height 一个具体数值比较好，之前给的是 100% ，但有自身的局限性，比如 antd 的轮播组件在 max-height:100% 的情况下就不能正常显示。）</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">html</span>,<span class="selector-tag">body</span>,<span class="selector-tag">div</span>,<span class="selector-tag">span</span>,<span class="selector-tag">h1</span>,<span class="selector-tag">h2</span>,<span class="selector-tag">h3</span>,<span class="selector-tag">h4</span>,<span class="selector-tag">h5</span>,<span class="selector-tag">h6</span>,<span class="selector-tag">p</span>,<span class="selector-tag">pre</span>,<span class="selector-tag">a</span>,<span class="selector-tag">blockquote</span>,<span class="selector-tag">abbr</span>,<span class="selector-tag">address</span>,<span class="selector-tag">code</span>,<span class="selector-tag">del</span>,<span class="selector-tag">em</span>,<span class="selector-tag">img</span>,<span class="selector-tag">q</span>,<span class="selector-tag">small</span>,<span class="selector-tag">strong</span>,<span class="selector-tag">sub</span>,<span class="selector-tag">sup</span>,<span class="selector-tag">var</span>,<span class="selector-tag">b</span>,<span class="selector-tag">i</span>,<span class="selector-tag">dl</span>,<span class="selector-tag">dt</span>,<span class="selector-tag">dd</span>,<span class="selector-tag">ul</span>,<span class="selector-tag">ol</span>,<span class="selector-tag">li</span>,<span class="selector-tag">fieldset</span>,<span class="selector-tag">form</span>,<span class="selector-tag">label</span>,<span class="selector-tag">legend</span>,<span class="selector-tag">table</span>,<span class="selector-tag">caption</span>,<span class="selector-tag">tbody</span>,<span class="selector-tag">tfoot</span>,<span class="selector-tag">thead</span>,<span class="selector-tag">tr</span>,<span class="selector-tag">td</span>,<span class="selector-tag">th</span>&#123;</span><br><span class="line">    <span class="attribute">border</span>:<span class="number">0</span> none;</span><br><span class="line">    <span class="attribute">font-size</span>:inherit;</span><br><span class="line">    <span class="attribute">color</span>:inherit;</span><br><span class="line">    <span class="attribute">margin</span>:<span class="number">0</span>;</span><br><span class="line">    <span class="attribute">padding</span>:<span class="number">0</span>;</span><br><span class="line">    <span class="attribute">vertical-align</span>:baseline;</span><br><span class="line">    <span class="comment">/*给 max-height 一个具体数值解决在X5新内核Blink中在排版页面的时候，会主动对字体进行放大的问题*/</span></span><br><span class="line">    <span class="attribute">max-height</span>: <span class="number">100000px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　（本文参考 <a href="http://www.jianshu.com/p/985d26b40199" target="_blank" rel="noopener"><em>minooo</em> 的简书内容</a>）未完待续 ···</p>
<p>　　学习资料：<a href="http://div.io/topic/1092" target="_blank" rel="noopener">移动端高清多屏适配的解决方案</a></p>
<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>文本溢出 text-overflow</title>
    <url>/2017/03/02/article-css-ellipsis/</url>
    <content><![CDATA[<p>　　有时我们会遇到当长文本超出他的容器时做文本溢出的处理，之前有一个比较 low 的 <a href="http://doubleray.win/2017/02/03/js-effect/#9-超出部分以省略号显示" target="_blank" rel="noopener">方法</a>，用 jQuery 实现的（也是为了兼容性）。本文主要总结一下实现这个效果可用的方法及需要注意的小坑，方便日后查阅。<a id="more"></a></p>
<h2 id="单行文本"><a href="#单行文本" class="headerlink" title="单行文本"></a>单行文本</h2><p>　　如果实现单行文本的溢出显示省略号同学们应该都知道用text-overflow:ellipsis属性来，当然还需要加宽度width属来兼容部分浏览。</p>
<blockquote>
<p><strong>实现方法：</strong></p>
</blockquote>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span>&#123;</span><br><span class="line">    <span class="comment">/*display: block;需为块级元素*/</span></span><br><span class="line">    <span class="attribute">overflow</span>: hidden;</span><br><span class="line">    <span class="attribute">text-overflow</span>:ellipsis;</span><br><span class="line">    <span class="attribute">white-space</span>: nowrap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>效果如下：</strong></p>
</blockquote>
<p style="overflow: hidden;text-overflow:ellipsis;white-space: nowrap;width:300px;background-color:#ccc">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusamus, animi beatae dicta dolorem excepturi facilis, illum maiores quia, repellat sit vero voluptate voluptates? Aspernatur et iusto, omnis qui quo velit.</p>

<hr>

<h2 id="多行文本"><a href="#多行文本" class="headerlink" title="多行文本"></a>多行文本</h2><h3 id="方法一"><a href="#方法一" class="headerlink" title="方法一"></a>方法一</h3><blockquote>
<p><strong>实现方法：</strong></p>
</blockquote>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span>&#123;</span><br><span class="line">    <span class="attribute">display</span>: -webkit-box;</span><br><span class="line">    <span class="attribute">-webkit-box-orient</span>: vertical;</span><br><span class="line">    <span class="attribute">-webkit-line-clamp</span>: <span class="number">3</span>;</span><br><span class="line">    <span class="attribute">overflow</span>: hidden;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>效果如下：</strong></p>
</blockquote>
<p style="overflow: hidden;display: -webkit-box;-webkit-box-orient: vertical;-webkit-line-clamp: 3;width:300px;background-color:#ccc">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusamus, animi beatae dicta dolorem excepturi facilis, illum maiores quia, repellat sit vero voluptate voluptates? Aspernatur et iusto, omnis qui quo velit.</p>

<hr>

<blockquote>
<p><strong>适用范围：</strong></p>
</blockquote>
<p>因使用了 WebKit 的 CSS 扩展属性，该方法适用于 WebKit 浏览器及移动端；</p>
<p><strong>-webkit-line-clamp用来限制在一个块元素显示的文本的行数。 为了实现该效果，它需要组合其他的WebKit属性。</strong></p>
<p>常见结合属性：</p>
<ul>
<li>display: -webkit-box; 必须结合的属性 ，将对象作为弹性伸缩盒子模型显示；</li>
<li>-webkit-box-orient 必须结合的属性 ，设置或检索伸缩盒对象的子元素的排列方式 。</li>
</ul>
<h3 id="方法二"><a href="#方法二" class="headerlink" title="方法二"></a>方法二</h3><blockquote>
<p><strong>实现方法：</strong></p>
</blockquote>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span>&#123;</span><br><span class="line">    <span class="attribute">position</span>: relative; </span><br><span class="line">    <span class="attribute">line-height</span>: <span class="number">20px</span>; </span><br><span class="line">    <span class="attribute">max-height</span>: <span class="number">40px</span>;</span><br><span class="line">    <span class="attribute">overflow</span>: hidden;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">p</span><span class="selector-pseudo">::after</span>&#123;</span><br><span class="line">    <span class="attribute">content</span>: <span class="string">"..."</span>; </span><br><span class="line">    <span class="attribute">position</span>: absolute;</span><br><span class="line">    <span class="attribute">bottom</span>: <span class="number">0</span>; </span><br><span class="line">    <span class="attribute">right</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">padding-left</span>: <span class="number">40px</span>;</span><br><span class="line">    <span class="attribute">background</span>: <span class="built_in">-webkit-linear-gradient</span>(left, transparent, #fff 55%);</span><br><span class="line">    <span class="attribute">background</span>: <span class="built_in">-moz-linear-gradient</span>(right, transparent, #fff 55%);</span><br><span class="line">    <span class="attribute">background</span>: <span class="built_in">linear-gradient</span>(to right, transparent, #fff 55%);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>效果如下：</strong></p>
</blockquote>
<style>.demo-css-ellipsis{position: relative;line-height: 20px;max-height: 60px;overflow: hidden;width: 300px}.demo-css-ellipsis::after{content: "..."; position: absolute;bottom: 0; right: 0;padding-left: 20px;background: -webkit-linear-gradient(left, transparent, #fff 35%);background: -moz-linear-gradient(right, transparent, #fff 35%);background: linear-gradient(to right, transparent, #fff 35%)}</style>

<p class="demo-css-ellipsis">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusamus, animi beatae dicta dolorem excepturi facilis, illum maiores quia, repellat sit vero voluptate voluptates? Aspernatur et iusto, omnis qui quo velit.</p>

<hr>

<blockquote>
<p><strong>适用范围：</strong></p>
</blockquote>
<p>　　该方法适用范围广，但文字未超出行的情况下也会出现省略号,可结合 js 优化该方法。</p>
<ul>
<li>将height设置为line-height的整数倍，防止超出的文字露出。</li>
<li>给p::after添加渐变背景可避免文字只显示一半。</li>
<li>由于ie6-7不显示content内容，所以要添加标签兼容ie6-7（如：<code>&lt;span&gt;…&lt;span/&gt;</code>）；<strong>兼容ie8需要将::after替换成:after</strong>。</li>
</ul>
<hr>]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>前端切图大法</title>
    <url>/2017/03/01/article-ps-firework/</url>
    <content><![CDATA[<p>　　作为一名“称职”的前端开发者，今天要“不知羞耻”的写一篇关于切图大法的总结，事情是这样的，就在前不久面试过程中有实操这么一个环节，面试官给了我一个很 easy 的 PSD 来让我完成一个简单的页面，本来半个小时四十分钟能解决的事情，我让 PS 愣给憋了半个小时（得亏面试官很 nice，当时真的是很尴尬），看来真是好记性不如烂键盘，所以也要劝诫大家不能忘了基本功啊。</p>
<p>　　现代互联网对交互设计越来越重视，切图早已经不是简单的使用 Photoshop 的切片工具对 JPG、PNG 等文件进行切片操作了。UI 设计师给出的设计稿往往会是 PSD 分层文件，这种文件中包含了更加丰富的图层、通道、路径等信息，也为更优雅的前端交互奠定了基础。所以我觉得现在的‘<strong><code>切图</code></strong>’更多的是一种思路。<a id="more"></a></p>
<p>准备：</p>
<ol>
<li><p>Photoshop CC或CS6</p>
</li>
<li><p>一张 PSD 设计稿</p>
</li>
</ol>
<h2 id="1-打开设计稿"><a href="#1-打开设计稿" class="headerlink" title="1. 打开设计稿"></a>1. 打开设计稿</h2><p>　　先来简单熟悉一下 ps 的主界面：</p>
<img src="/styles/images/ps/ps-view.jpg" alt="ps 面板">

<p>　　① 为工具栏　　② 为画布　　③ 为图层操作面板</p>
<blockquote>
<p><strong>加载 PSD 文件时可能会遇到嵌入的配置文件不匹配、字体丢失等问题，不用理会，直接按确定键即可。</strong></p>
</blockquote>
<blockquote>
<p>Tips：<strong>如果③图层工作区默认没有显示，点击 菜单栏&gt;窗口&gt;图层 即可打开</strong></p>
</blockquote>
<h2 id="2-分析设计稿"><a href="#2-分析设计稿" class="headerlink" title="2. 分析设计稿"></a>2. 分析设计稿</h2><p>　　分析设计稿有哪些内容是需要进行切图保留的内容，主要内容考虑主要有以下几个部分：</p>
<img src="/styles/images/ps/ps-pic.jpg" alt="ps 分析设计稿">

<p>　　① 图标（是否可以使用伪元素 :before、:after 实现，例如各种箭头图标）</p>
<p>　　② 背景（是否可以使用 background 实现）</p>
<p>　　③ 按钮（是否可以使用 background、border-radius、:hover 等实现交互效果）</p>
<p>　　④ 字体（是否被高频使用，是否需要做成单独的字体文件或做成切片）</p>
<p>　　⑤ 装饰性元素（是否可以使用伪元素 :before、:after 实现）</p>
<h2 id="3-开始切图"><a href="#3-开始切图" class="headerlink" title="3. 开始切图"></a>3. 开始切图</h2><p>　　首先写几点操作过程中时不时需要注意的几点：</p>
<ul>
<li><p>参考线可以通过在主画板顶部及左侧的标尺中通过拖拽添加 / 删除，如果没有打开标尺，按快捷键 Ctrl+R 即可显示标尺。</p>
</li>
<li><p>保存为 web 格式的时候，选 PNG-24 记得勾选下方的‘交错’，选 JPEG 记得勾选‘连续’，这样在浏览器加载图片的时候会以模糊逐渐转为清晰的效果渐渐显示出来，浏览体验更好。转换为 sRGB 可以在绝大部分浏览器中显示出你期望的颜色。</p>
</li>
<li><p>需要保留透明度的选择PNG-24（PNG-8不支持半透明，这是为了照顾 IE6 的显示效果，现在谁还管 IE6，用 IE6 的人还想要什么优雅的交互体验，页面能显示出来主要内容就不错了，嫌弃脸.jpg &lt;_&lt;），不需要保留透明度的选择JPEG。另外.JPG 文件，相对于 PNG-24，对图片质量要求不高的情况下，JPEG 能获得更小的文件体积。</p>
</li>
</ul>
<p>　　具体到切图的方法，主流的方法主要有这么几种：</p>
<h3 id="3-1-传统切图法"><a href="#3-1-传统切图法" class="headerlink" title="3.1 传统切图法"></a>3.1 传统切图法</h3><p>　　普通切图方法就是使用<strong>切片工具&amp;&amp;切片选择工具</strong>，在设计稿上调整好切片然后输出。</p>
<p>　　首先开始划分出我们需要的切片（注意：<strong>设计稿中已经包含原作者留下的切片，为防干扰最好使用切片选择工具把这些切片删除</strong>）。早期的浏览器对含透明通道的PNG图支持不好，所以处理含透明通道的图时我们一般使用GIF，或者索性把图形连同背景合并到一起输出不透明的图。而随着前端技术的发展和古老浏览器的淘汰，现在更好的做法是使用透明通道的PNG图片，既保证了图像质量又保证了图片在不同页面中都能适应。</p>
<p>　　使用切片切图时建议使用 <strong>基于参考线的切片</strong>，也就是先用参考线选好，然后再选择切片工具，然后点击基于参考线的切片：</p>
<img src="/styles/images/ps/qiepian_01.jpg" alt="ps 切片切图法">
<img src="/styles/images/ps/qiepian_02.jpg" alt="ps 切片切图法">
<img src="/styles/images/ps/qiepian_03.jpg" alt="ps 切片切图法">

<h3 id="3-2-文艺切图法"><a href="#3-2-文艺切图法" class="headerlink" title="3.2 文艺切图法"></a>3.2 文艺切图法</h3><p>　　可以看到，普通切图法需要手动编辑切片，耗时耗力，当设计稿元素较多时容易出错。其实在处理大量图片时，我们常用到PS的脚本功能来对图像进行批处理，切图这一工作也可以通过脚本来实现半自动化。</p>
<p>　　文件&gt;脚本&gt;将图层导出到文件”这条命令是PS中预置的一个脚本，正如你想要的，他会把设计稿中的图层输出成图片。用这种方式切图，为了不破坏设计稿、脚本运行速度更快，我建议新建一个文档来进行。新建一个尺寸为800<em>800的文档，把需要切图的元素拖动到这个文档中来，位置随意摆放，因为这个脚本会将“图层导出到文件”，所以有些元素可能包含多个图层的要使用“*</em>图层&gt;合并图层**”将他们合并。</p>
<p>　　<strong>执行“文件&gt;脚本&gt;将图层导出到文件</strong>:</p>
<img src="/styles/images/ps/save-layer.jpg" alt="ps 脚本切图法">

<h3 id="3-3-2B-切图法"><a href="#3-3-2B-切图法" class="headerlink" title="3.3 2B 切图法"></a>3.3 2B 切图法</h3><p>　　2B 切图法就是使用裁剪工具，将设计稿中的每个元素分别提取出来并单独存储，仅需要输出少量图片或是要对图像局部调整时才使用此方法。</p>
<h3 id="3-4-直接导出"><a href="#3-4-直接导出" class="headerlink" title="3.4 直接导出"></a>3.4 直接导出</h3><p>　　新版 PS CC 可以右键直接导出 PNG 文件～～～</p>
<img src="/styles/images/ps/you-png.jpg">

<h3 id="3-5-简单实用"><a href="#3-5-简单实用" class="headerlink" title="3.5 简单实用"></a>3.5 简单实用</h3><p>（1） 如果你的图层很多，你可以用“ctrl+点击想要的图片“直接选中你想要图片的图层，简单到位，然后将图层转化为智能对象（当然可以几个图层拼到一起），然后双击弹出点击确定，就 ok 了；</p>
<p>（2） 对于大图，这个时候就有很多的东西存在，这里就使用，选区先选中你要切的图形，然后”编辑-合并拷贝-新建-拷贝“，就能建立一个完成整的图片切图的样子了，就 ok 了。</p>
<h2 id="4-结语"><a href="#4-结语" class="headerlink" title="4. 结语"></a>4. 结语</h2><p>　　当然，本文主要还是针对的传统 Web 切图工作，现在也有很多新兴的工具，可以更加有效率的完成前端的设计编码工作，不忘初衷，方得始终，与时俱进，一定要装作好像自己很熟练了的样子～～～</p>
<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>PhotoShop</tag>
      </tags>
  </entry>
  <entry>
    <title>H5 移动多终端适配</title>
    <url>/2017/02/26/article-html-adaptation/</url>
    <content><![CDATA[<p>　　移动端的浏览器在展示页面时会出现许多非预期的现象，比如有的可能浏览器自动缩放网页让整个内容能够在可视区域展示，但内容会缩小；有的可能会出现横向滚动条。这些都不是理想的浏览状态，理想的网页展示应该是用户进入页面不需要手动缩放就能够看清页面的内容也没有横向滚动条。</p>
<p>　　要如何去实现移动端网页展示达到理想效果涉及到很多因素，但主要包括三个方面：像素（pixels）、屏幕（screen）、视口（viewport）和缩放（scale）。<a id="more"></a></p>
<h2 id="1-像素（pixels）"><a href="#1-像素（pixels）" class="headerlink" title="1. 像素（pixels）"></a>1. 像素（pixels）</h2><p>　　像素这个词对绝大多素人都不陌生，正常的理解就是像素就好像是一个个“点”，屏幕由不同数量的点排列构成不同的分辨率，一张图片由不同的像素点构成不同的大小。同时我们经常将像素说成长度的单位，但实际上我们说1像素其实是一个1 x 1的小方块，它的面积是1。</p>
<h3 id="1-1-设备像素"><a href="#1-1-设备像素" class="headerlink" title="1.1 设备像素"></a>1.1 设备像素</h3><p>　　设备像素又称物理像素，是显示设备（PC、手机显示屏等）最小的物理部件，设备像素由操作系统控制来发光填色值，来让我们在视觉上看到相应的效果（如50 x 50的红色方块）。设备像素做为物理属性的存在是固定不变的，开发人员不可能通过程序控制设备像素数量上的增加减少。</p>
<h3 id="1-2-CSS-像素"><a href="#1-2-CSS-像素" class="headerlink" title="1.2 CSS 像素"></a>1.2 CSS 像素</h3><p>　　我们平时写网页的时候用的长度单位是 px，属于 CSS 像素。CSS 像素是抽象的，抽象意味着它没有物理属性的实体依据，它可以收缩放大。简单的理解为 CSS 像素点是有面积的，一个个 CSS 像素点铺起来盖在设备的显示屏上，其面积随着浏览器的缩放比例而同步缩放。例如在 PC 上的浏览器中，定义一 100 x 100 的红色方块，缩放比例为 100% 的情况下，一个 CSS 像素是与一个设备像素完全重叠的。当缩放比设置为 50% 即缩小一半，CSS像素会等比缩放一半，那一个 CSS 像素的面积就只占 100% 情况下的四分之一了，所以视觉上看到方块变成了 50x50 的大小（即占了 50x50 的设备像素）。</p>
<blockquote>
<p><strong>此时浏览器中检查元素大小会发现红色方块的大小仍旧为100x100，只是CSS像素的面积缩小了！</strong></p>
</blockquote>
<p>　　但是，到了移动终端，由于高密度显示技术出现，比如苹果的Retina显示屏中，当缩放比是 100% 的时候，一个 CSS 像素占的面积是 4 个设备像素的面积，即一个 CSS 像素横向宽度覆盖了 2 个设备像素的宽度。缩放比为 100% 时一个 CSS 像素占多少个设备像素是由 <strong>设备像素比（dpr）</strong> 决定的，dpr为 2，则占两个，dpr 为 3 则占三个。dpr 的取值可以通过如下代码得到：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.devicePixelRatio</span><br></pre></td></tr></table></figure>

<h2 id="2-屏幕（screen）"><a href="#2-屏幕（screen）" class="headerlink" title="2. 屏幕（screen）"></a>2. 屏幕（screen）</h2><p>　　屏幕的完整大小用设备像素来衡量，单位是物理属性的单位，做开发的时候也不需要太过关注，我们关注的是CSS像素。一般就可以理解为这个单位和CSS像素的大小在缩放比为100%时是一样大的。我们可以在chrome这类下载浏览器中能够通过<code>screen.width/height</code>来得到屏幕高宽的具体值。这个时候你会发现在PC上不管把浏览器窗口调整至多大,返回值总是一致的，移动端上的情况也很明朗，比如iphone6返回的值就是 375。</p>
<p>　　我们构建网页的时候关注的是 CSS 像素，用户看网页的时候关注的是浏览器宽度内的内容，浏览器宽度决定了用户在视觉上能够看到多少东西。所以我们开发的时候关注的也就是浏览器视觉宽度下，里面的 CSS 像素的数量多少。这个浏览器尺寸内的 CSS 像素多少可以通过<code>window.innerWidth/Height</code>来获取。由于 CSS 像素会缩放，所以同一个浏览器宽度下这个取值是可能不同的。</p>
<h2 id="3-视口（viewport）"><a href="#3-视口（viewport）" class="headerlink" title="3. 视口（viewport）"></a>3. 视口（viewport）</h2><p>　　我们进行网页布局的时候设置 html 元素宽度为100%，这个100%是依据什么设置的呢？浏览器窗口用 CSS 像素衡量的宽度（<code>window.innerWidth</code>)还是别的呢？我们可以发现在PC端的浏览器上设置100%，html 元素的 px 宽度就等于浏览器宽度；而在移动端不管是在什么设备上，html 设置100%，<strong>基本上宽度都等于980px</strong>。</p>
<h3 id="3-1-布局视口"><a href="#3-1-布局视口" class="headerlink" title="3.1 布局视口"></a>3.1 布局视口</h3><p>　　所以这就引出了布局视口的概念。宽度设置是根据布局视口来算的，只是PC端布局视口就等于浏览器宽度，而移动终端由于屏幕宽度较窄，为了显示在 PC 端网页显示的网页能在移动端基本能看，所以浏览器厂商一般将其设为 980px 然后进行缩放，让网页在窄屏幕上能够全部显示。布局视口尺寸可以通过如下代码获取：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.documentElement.clientWidth/Height;</span><br></pre></td></tr></table></figure>

<p>　　虽然<code>document.documentElement</code>就是 html 元素，但是取值却不是取的 html 元素的宽度。同时我们做的媒体查询中判断的<code>max-width</code>也是判断布局视口的宽度。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">@<span class="keyword">media</span> all and (max-width: <span class="number">400px</span>) &#123;</span><br><span class="line">	<span class="selector-class">.box</span>&#123;</span><br><span class="line">		<span class="attribute">width</span>: <span class="number">300px</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-2-视觉视口"><a href="#3-2-视觉视口" class="headerlink" title="3.2 视觉视口"></a>3.2 视觉视口</h3><p>　　视觉视口就是用户能够看到的窗口，单纯的用视觉去描述它就是浏览器屏幕的大小。用CSS像素去描述就是：<strong>视觉视口的大小就是屏幕大小范围内CSS像素堆积的总面积，不管CSS像素是否缩放</strong>。</p>
<ul>
<li>移动端布局需要达到的效果： <strong>设置布局视口的宽度正好等于屏幕的大小</strong>。</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--设置布局视口等于device-width--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width"</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 只设置缩放值也能将布局视口宽度设置为屏幕宽度，设置为0.5时由于CSS像素缩放了0.5，填到屏幕中的CSS像素数量正好是屏幕宽度 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no"</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 这是兼容写法 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width,initial-scale=1, maximum-scale=1,user-scalable=no"</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="4-移动适配方案总结"><a href="#4-移动适配方案总结" class="headerlink" title="4. 移动适配方案总结"></a>4. 移动适配方案总结</h2><h3 id="4-1-width-device-width"><a href="#4-1-width-device-width" class="headerlink" title="4.1 width=device-width"></a>4.1 width=device-width</h3><p>　　直接设置<code>width=device-width</code>，这样能够保证宽度一致，但考虑到苹果的Retina屏幕设计师出设计稿的时候都是放大了一倍设计稿，比如640px（基于iphone5）、750px（基于iphone6）。如果我们拿到的设计稿是750px宽，在设置缩放比为100%、布局视口等于屏幕宽度的情况下，我们拿到的设计稿宽度是大了一倍的，（网页中设置375px沾满屏幕，而设计稿切出的图是750px），所以在转换成网页的时候设置宽度需要将设计图的宽度除以2来，如果考虑到iphone6 plus的dpr为3，要保证在6 plus下图片还是高清，就需要将750px的设计稿再放大1.5倍，用@3倍图切出来。以上可以知道这种方案不需要动态的去根据屏幕dpr不同来设置meta标签内容，直接一句话搞定，然后需要调整的地方通过媒体查询来做。</p>
<h3 id="4-2-flexible"><a href="#4-2-flexible" class="headerlink" title="4.2 flexible"></a>4.2 flexible</h3><p>　　淘宝的 flexible 方案只解决苹果设备上的 dpr 为2和3的情况，不考虑安卓设备也不考虑 pad。做的事情也很简单，就是动态设置 meta 标签的内容，和第一种方案不同的是没有设置为 <code>width=device-width</code>,而是通过设置 <code>initial-scale</code> 来缩放布局视口，dpr 为1，缩放为1；dpr 为2，缩放0.5；dpr 为3，缩放0.3333；通过这种方法使得布局视口在视觉上还是屏幕大小但CSS像素数量却增加了4倍/9倍。而这样的直接好处就是比如 iphone6 的视觉视口横向有了750个 CSS 像素点，和设计图保持一致了，同时CSS像素缩放 0.5 解决了经典的 1px 像素问题。</p>
<p>　　该方案的核心原理是通过<code>window.navigator.appVersion</code>判断是否是iphone，其他的比如ipad、Android直接设置dpr为1，然后通过 1/ dpr 得到缩放值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> isAndroid = win.navigator.appVersion.match(<span class="regexp">/android/gi</span>);</span><br><span class="line"><span class="comment">/* 判断iphone ipad不考虑*/</span></span><br><span class="line"><span class="keyword">var</span> isIPhone = win.navigator.appVersion.match(<span class="regexp">/iphone/gi</span>);</span><br><span class="line"><span class="keyword">var</span> devicePixelRatio = win.devicePixelRatio;</span><br><span class="line"><span class="keyword">if</span> (isIPhone) &#123;</span><br><span class="line">    <span class="comment">// iOS下，对于2和3的屏，用2倍的方案，其余的用1倍方案</span></span><br><span class="line">    <span class="keyword">if</span> (devicePixelRatio &gt;= <span class="number">3</span> &amp;&amp; (!dpr || dpr &gt;= <span class="number">3</span>)) &#123;                </span><br><span class="line">        dpr = <span class="number">3</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (devicePixelRatio &gt;= <span class="number">2</span> &amp;&amp; (!dpr || dpr &gt;= <span class="number">2</span>))&#123;</span><br><span class="line">        dpr = <span class="number">2</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        dpr = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 其他设备下，仍旧使用1倍的方案</span></span><br><span class="line">    dpr = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*缩放值*/</span></span><br><span class="line">scale = <span class="number">1</span> / dpr;</span><br></pre></td></tr></table></figure>

<p>　　这个解决方案中用的布局单位是<strong>rem</strong>而不是<strong>px</strong>，这也是为了等比例缩放的问题。rem单位代表的是根节点也就是 html 的 fontSize 的值，html默认为16px，所以默认1rem = 16px。既然开发的设计标注图是750px的，将 html 设为 75px 就很方便了，10rem = 750px。然后放到所有屏幕情况下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">refreshRem</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">/*获取布局视口宽度,也可用document.documentElement.clientWidth*/</span></span><br><span class="line">    <span class="keyword">var</span> width = <span class="built_in">document</span>.documentElement.getBoundingClientRect().width;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 判断屏幕宽度，如果dpr为1且宽度&gt;540 则恒定为540px，dpr为2如果宽度&gt;1080则恒定为1080px*/</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (width / dpr &gt; <span class="number">540</span>) &#123;</span><br><span class="line">        width = <span class="number">540</span> * dpr;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*将布局视口宽度除以10得到html字体大小*/</span></span><br><span class="line">    <span class="keyword">var</span> rem = width / <span class="number">10</span>;</span><br><span class="line">    <span class="built_in">document</span>.documentElement.style.fontSize = rem + <span class="string">'px'</span></span><br></pre></td></tr></table></figure>

<p>　　比如iphone5的设备下布局视口是640，所以 html 的字体大小会设置为64px，而在开发的时候设置的单位为 rem,这个时候rem 自动根据 64px 转换，就实现了等比缩放。写 rem 的时候可以通过 CSSREM 插件来写，只是后期维护比较麻烦。</p>
<p>　　字体不用 rem 而采用 px 是因为我们希望一个文本字号不因为Retina显示屏的原因而缩放变小。然后在大屏上也不会放大而能够看到更多的文本。但是不同的dpr下又的确是要分别设置字号的。这个解决方案会在 htm l元素上设置 data-dpr 属性，然后通过该属性判断来设置字体大小。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">div</span><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">font-size</span>: <span class="number">12px</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-attr">[data-dpr="2"]</span> <span class="selector-tag">div</span>&#123;</span><br><span class="line">    <span class="attribute">font-size</span>: <span class="number">24px</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-attr">[data-dpr="3"]</span> <span class="selector-tag">div</span>&#123;</span><br><span class="line">    <span class="attribute">font-size</span>: <span class="number">36px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　设计稿设计方面要求为 750px，如果设计师有做标注就做在这个稿子上，没有标注的话我们开发的时候就依据这个设计稿来量间距之类的。同时将这个稿子放大 1.5 倍来得到 1125px 的设计稿，切图用这份设计稿保证 dpr 为 3 的屏幕图片显示足够清晰。在 iphone6 的屏幕下开发然后再向上向下适配。</p>
<h3 id="4-3-设置固宽"><a href="#4-3-设置固宽" class="headerlink" title="4.3 设置固宽"></a>4.3 设置固宽</h3><p>　　固定设置布局视口的宽度，比如设置为640，或750。</p>
<hr>

<p>　　移动端适配的原理很简单，各个项目在用的时候也能去选一个适合的方案。在 vw 和 vh 单位兼容性不够的时候，要做到设置的单位等比缩放只能用 rem。另外现在是不是有更好的适配方案不太清楚，本文主要是对现在我所知道的方案做一个总结。</p>
<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript 6 扫盲学习</title>
    <url>/2017/02/23/article-ES6-know/</url>
    <content><![CDATA[<p>　　ECMAScript 6 目前基本成为业界标准，它的普及速度比 ES5 要快很多，主要原因是现代浏览器对 ES6 的支持相当迅速，尤其是 Chrome 和 Firefox 浏览器，已经支持 ES6 中绝大多数的特性。</p>
<p>　　本文转自 <a href="http://barretlee.com/blog/2016/07/09/a-kickstarter-guide-to-writing-es6/" target="_blank" rel="noopener">ECMAScript 6 扫盲</a> ，参考 Github 上的 <a href="https://github.com/metagrover/ES6-for-humans" target="_blank" rel="noopener">一篇文章</a>，目的是对还不太熟悉 ES6 语法的小伙伴做一个简单的扫盲学习。<a id="more"></a></p>
<h2 id="1-let、const-和-block-作用域"><a href="#1-let、const-和-block-作用域" class="headerlink" title="1. let、const 和 block 作用域"></a>1. let、const 和 block 作用域</h2><p>　　let 允许创建块级作用域，ES6 推荐在函数中使用 let 定义变量，而非 var：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">2</span>;</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">let</span> a = <span class="number">3</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(a); <span class="comment">// 3</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(a); <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>

<p>　　同样在块级作用域有效的另一个变量声明方式是 const，它可以声明一个常量。ES6 中，const 声明的常量类似于指针，它指向某个引用，也就是说这个「常量」并非一成不变的，如：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="keyword">const</span> ARR = [<span class="number">5</span>,<span class="number">6</span>];</span><br><span class="line">  ARR.push(<span class="number">7</span>);</span><br><span class="line">  <span class="built_in">console</span>.log(ARR); <span class="comment">// [5,6,7]</span></span><br><span class="line">  <span class="comment">//ARR = 10; // TypeError</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>有几个点需要注意：</p>
<ul>
<li>let 关键词声明的变量不具备变量提升（hoisting）特性</li>
<li>let 和 const 声明只在最靠近的一个块中（花括号内）有效</li>
<li>当使用常量 const 声明时，请使用大写变量，如：CAPITAL_CASING</li>
<li>const 在声明时必须被赋值</li>
</ul>
<h2 id="2-箭头函数（Arrow-Functions）"><a href="#2-箭头函数（Arrow-Functions）" class="headerlink" title="2. 箭头函数（Arrow Functions）"></a>2. 箭头函数（Arrow Functions）</h2><p>　　ES6 中，箭头函数就是函数的一种简写形式，使用括号包裹参数，跟随一个 =&gt;，紧接着是函数体：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> getPrice = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">4.55</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// Implementation with Arrow Function</span></span><br><span class="line"><span class="keyword">var</span> getPrice = <span class="function"><span class="params">()</span> =&gt;</span> <span class="number">4.55</span>;</span><br></pre></td></tr></table></figure>

<p>　　需要注意的是，上面栗子中的 getPrice 箭头函数采用了简洁函数体，它不需要 reture 语句，下面这个栗子使用的是正常函数体：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="string">'apple'</span>, <span class="string">'banana'</span>, <span class="string">'orange'</span>];</span><br><span class="line"><span class="keyword">let</span> breakfast = arr.map(<span class="function"><span class="params">fruit</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> fruit + <span class="string">'s'</span>;</span><br><span class="line">&#125;);</span><br><span class="line"><span class="built_in">console</span>.log(breakfast); <span class="comment">// apples bananas oranges</span></span><br></pre></td></tr></table></figure>

<p>　　当然，箭头函数不仅仅是让代码变得简洁，函数中 this 总是绑定总是指向对象自身。具体可以看看下面几个栗子：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.age = <span class="number">0</span>;</span><br><span class="line">  setInterval(<span class="function"><span class="keyword">function</span> <span class="title">growUp</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 在非严格模式下，growUp() 函数的 this 指向 window 对象</span></span><br><span class="line">    <span class="keyword">this</span>.age++;</span><br><span class="line">  &#125;, <span class="number">1000</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> person = <span class="keyword">new</span> Person();</span><br></pre></td></tr></table></figure>

<p>　　我们经常需要使用一个变量来保存 this，然后在 growUp 函数中引用：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> self = <span class="keyword">this</span>;</span><br><span class="line">  self.age = <span class="number">0</span>;</span><br><span class="line">  setInterval(<span class="function"><span class="keyword">function</span> <span class="title">growUp</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    self.age++;</span><br><span class="line">  &#125;, <span class="number">1000</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　而使用箭头函数可以省却这个麻烦：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.age = <span class="number">0</span>;</span><br><span class="line">  setInterval(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// |this| 指向 person 对象</span></span><br><span class="line">    <span class="keyword">this</span>.age++;</span><br><span class="line">  &#125;, <span class="number">1000</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> person = <span class="keyword">new</span> Person();</span><br></pre></td></tr></table></figure>

<p>　　在 <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Arrow_functions#Lexical_this" target="_blank" rel="noopener">这里</a> 可以阅读更多 this 在箭头函数中的词法特性。</p>
<h2 id="3-函数参数默认值"><a href="#3-函数参数默认值" class="headerlink" title="3. 函数参数默认值"></a>3. 函数参数默认值</h2><p>　　ES6 中允许你对函数参数设置默认值：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> getFinalPrice = <span class="function">(<span class="params">price, tax=<span class="number">0.7</span></span>) =&gt;</span> price + price * tax;</span><br><span class="line">getFinalPrice(<span class="number">500</span>); <span class="comment">// 850</span></span><br></pre></td></tr></table></figure>

<h2 id="4-Spread-Rest-操作符"><a href="#4-Spread-Rest-操作符" class="headerlink" title="4. Spread / Rest 操作符"></a>4. Spread / Rest 操作符</h2><p>　　Spread / Rest 操作符指的是 …，具体是 Spread 还是 Rest 需要看上下文语境。当被用于迭代器中时，它是一个 Spread 操作符：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">x,y,z</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(x,y,z);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</span><br><span class="line">foo(...arr); <span class="comment">// 1 2 3</span></span><br></pre></td></tr></table></figure>

<p>　　当被用于函数传参时，是一个 Rest 操作符：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">...args</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(args);</span><br><span class="line">&#125;</span><br><span class="line">foo( <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>); <span class="comment">// [1, 2, 3, 4, 5]</span></span><br></pre></td></tr></table></figure>

<h2 id="5-对象词法扩展"><a href="#5-对象词法扩展" class="headerlink" title="5. 对象词法扩展"></a>5. 对象词法扩展</h2><p>　　ES6 允许声明在对象字面量时使用简写语法，来初始化属性变量和函数的定义方法，并且允许在对象属性中进行计算操作：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getCar</span>(<span class="params">make, model, value</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="comment">// 简写变量</span></span><br><span class="line">    make,  <span class="comment">// 等同于 make: make</span></span><br><span class="line">    model, <span class="comment">// 等同于 model: model</span></span><br><span class="line">    value, <span class="comment">// 等同于 value: value</span></span><br><span class="line">    <span class="comment">// 属性可以使用表达式计算值</span></span><br><span class="line">    [<span class="string">'make'</span> + make]: <span class="literal">true</span>,</span><br><span class="line">    <span class="comment">// 忽略 `function` 关键词简写对象函数</span></span><br><span class="line">    depreciate() &#123;</span><br><span class="line">      <span class="keyword">this</span>.value -= <span class="number">2500</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> car = getCar(<span class="string">'Barret'</span>, <span class="string">'Lee'</span>, <span class="number">40000</span>);</span><br><span class="line"><span class="comment">// output: &#123;</span></span><br><span class="line"><span class="comment">//     make: 'Barret',</span></span><br><span class="line"><span class="comment">//     model:'Lee',</span></span><br><span class="line"><span class="comment">//     value: 40000,</span></span><br><span class="line"><span class="comment">//     makeBarret: true,</span></span><br><span class="line"><span class="comment">//     depreciate: function()</span></span><br><span class="line"><span class="comment">// &#125;</span></span><br></pre></td></tr></table></figure>

<h2 id="6-二进制和八进制字面量"><a href="#6-二进制和八进制字面量" class="headerlink" title="6. 二进制和八进制字面量"></a>6. 二进制和八进制字面量</h2><p>　　ES6 支持二进制和八进制的字面量，通过在数字前面添加 0o 或者 0O 即可将其转换为二进制值：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> oValue = <span class="number">0o10</span>;</span><br><span class="line"><span class="built_in">console</span>.log(oValue); <span class="comment">// 8</span></span><br><span class="line"><span class="keyword">let</span> bValue = <span class="number">0b10</span>; <span class="comment">// 二进制使用 `0b` 或者 `0B`</span></span><br><span class="line"><span class="built_in">console</span>.log(bValue); <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>

<h2 id="7-对象和数组解构"><a href="#7-对象和数组解构" class="headerlink" title="7. 对象和数组解构"></a>7. 对象和数组解构</h2><p>　　解构可以避免在对象赋值时产生中间变量：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> arr = foo(); <span class="comment">// [1,2,3]</span></span><br><span class="line"><span class="keyword">let</span> [a, b, c] = foo();</span><br><span class="line"><span class="built_in">console</span>.log(a, b, c); <span class="comment">// 1 2 3</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    x: <span class="number">4</span>,</span><br><span class="line">    y: <span class="number">5</span>,</span><br><span class="line">    z: <span class="number">6</span></span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> &#123;<span class="attr">x</span>: x, <span class="attr">y</span>: y, <span class="attr">z</span>: z&#125; = bar();</span><br><span class="line"><span class="built_in">console</span>.log(x, y, z); <span class="comment">// 4 5 6</span></span><br></pre></td></tr></table></figure>

<h2 id="8-对象超类"><a href="#8-对象超类" class="headerlink" title="8. 对象超类"></a>8. 对象超类</h2><p>　　ES6 允许在对象中使用 super 方法：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> parent = &#123;</span><br><span class="line">  foo() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"Hello from the Parent"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> child = &#123;</span><br><span class="line">  foo() &#123;</span><br><span class="line">    <span class="keyword">super</span>.foo();</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"Hello from the Child"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">Object</span>.setPrototypeOf(child, parent);</span><br><span class="line">child.foo(); <span class="comment">// Hello from the Parent</span></span><br><span class="line">             <span class="comment">// Hello from the Child</span></span><br></pre></td></tr></table></figure>

<h2 id="9-模板语法和分隔符"><a href="#9-模板语法和分隔符" class="headerlink" title="9. 模板语法和分隔符"></a>9. 模板语法和分隔符</h2><p>　　ES6 中有一种十分简洁的方法组装一堆字符串和变量。</p>
<ul>
<li>${ … } 用来渲染一个变量</li>
<li>` 作为分隔符</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> user = <span class="string">'Barret'</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">`Hi <span class="subst">$&#123;user&#125;</span>!`</span>); <span class="comment">// Hi Barret!</span></span><br></pre></td></tr></table></figure>

<h2 id="10-for…of-VS-for…in"><a href="#10-for…of-VS-for…in" class="headerlink" title="10. for…of VS for…in"></a>10. for…of VS for…in</h2><p>　　for…of 用于遍历一个迭代器，如数组：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> nicknames = [<span class="string">'di'</span>, <span class="string">'boo'</span>, <span class="string">'punkeye'</span>];</span><br><span class="line">nicknames.size = <span class="number">3</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> nickname <span class="keyword">of</span> nicknames) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(nickname);</span><br><span class="line">&#125;</span><br><span class="line">Result: di, boo, punkeye</span><br></pre></td></tr></table></figure>

<p>　　for…in 用来遍历对象中的属性：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> nicknames = [<span class="string">'di'</span>, <span class="string">'boo'</span>, <span class="string">'punkeye'</span>];</span><br><span class="line">nicknames.size = <span class="number">3</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> nickname <span class="keyword">in</span> nicknames) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(nickname);</span><br><span class="line">&#125;</span><br><span class="line">Result: <span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, size</span><br></pre></td></tr></table></figure>

<h2 id="11-Map-和-WeakMap"><a href="#11-Map-和-WeakMap" class="headerlink" title="11. Map 和 WeakMap"></a>11. Map 和 WeakMap</h2><p>　　ES6 中两种新的数据结构集：Map 和 WeakMap。事实上每个对象都可以看作是一个 Map。</p>
<h3 id="Map"><a href="#Map" class="headerlink" title="Map"></a>Map</h3><p>　　一个对象由多个 key-val 对构成，在 Map 中，任何类型都可以作为对象的 key，如：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> myMap = <span class="keyword">new</span> <span class="built_in">Map</span>();</span><br><span class="line"><span class="keyword">var</span> keyString = <span class="string">"a string"</span>,</span><br><span class="line">    keyObj = &#123;&#125;,</span><br><span class="line">    keyFunc = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;&#125;;</span><br><span class="line"><span class="comment">// 设置值</span></span><br><span class="line">myMap.set(keyString, <span class="string">"value 与 'a string' 关联"</span>);</span><br><span class="line">myMap.set(keyObj, <span class="string">"value 与 keyObj 关联"</span>);</span><br><span class="line">myMap.set(keyFunc, <span class="string">"value 与 keyFunc 关联"</span>);</span><br><span class="line">myMap.size; <span class="comment">// 3</span></span><br><span class="line"><span class="comment">// 获取值</span></span><br><span class="line">myMap.get(keyString);    <span class="comment">// "value 与 'a string' 关联"</span></span><br><span class="line">myMap.get(keyObj);       <span class="comment">// "value 与 keyObj 关联"</span></span><br><span class="line">myMap.get(keyFunc);      <span class="comment">// "value 与 keyFunc 关联"</span></span><br></pre></td></tr></table></figure>

<h3 id="WeakMap"><a href="#WeakMap" class="headerlink" title="WeakMap"></a>WeakMap</h3><p>　　WeakMap 就是一个 Map，只不过它的所有 key 都是弱引用，意思就是 WeakMap 中的东西垃圾回收时不考虑，使用它不用担心内存泄漏问题。</p>
<p>　　另一个需要注意的点是，WeakMap 的所有 key 必须是对象。它只有四个方法 delete(key),has(key),get(key) 和 set(key, val)：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> w = <span class="keyword">new</span> <span class="built_in">WeakMap</span>();</span><br><span class="line">w.set(<span class="string">'a'</span>, <span class="string">'b'</span>); </span><br><span class="line"><span class="comment">// Uncaught TypeError: Invalid value used as weak map key</span></span><br><span class="line"><span class="keyword">var</span> o1 = &#123;&#125;,</span><br><span class="line">    o2 = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;,</span><br><span class="line">    o3 = <span class="built_in">window</span>;</span><br><span class="line">w.set(o1, <span class="number">37</span>);</span><br><span class="line">w.set(o2, <span class="string">"azerty"</span>);</span><br><span class="line">w.set(o3, <span class="literal">undefined</span>);</span><br><span class="line">w.get(o3); <span class="comment">// undefined, because that is the set value</span></span><br><span class="line">w.has(o1); <span class="comment">// true</span></span><br><span class="line">w.delete(o1);</span><br><span class="line">w.has(o1); <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<h2 id="12-Set-和-WeakSet"><a href="#12-Set-和-WeakSet" class="headerlink" title="12. Set 和 WeakSet"></a>12. Set 和 WeakSet</h2><h3 id="Set"><a href="#Set" class="headerlink" title="Set"></a>Set</h3><p>　　Set 对象是一组不重复的值，重复的值将被忽略，值类型可以是原始类型和引用类型：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> mySet = <span class="keyword">new</span> <span class="built_in">Set</span>([<span class="number">1</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">3</span>]);</span><br><span class="line">mySet.size; <span class="comment">// 3</span></span><br><span class="line">mySet.has(<span class="number">1</span>); <span class="comment">// true</span></span><br><span class="line">mySet.add(<span class="string">'strings'</span>);</span><br><span class="line">mySet.add(&#123; <span class="attr">a</span>: <span class="number">1</span>, <span class="attr">b</span>:<span class="number">2</span> &#125;);</span><br></pre></td></tr></table></figure>

<p>　　可以通过 forEach 和 for…of 来遍历 Set 对象：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">mySet.forEach(<span class="function">(<span class="params">item</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(item);</span><br><span class="line">    <span class="comment">// 1</span></span><br><span class="line">    <span class="comment">// 2</span></span><br><span class="line">    <span class="comment">// 3</span></span><br><span class="line">    <span class="comment">// 'strings'</span></span><br><span class="line">    <span class="comment">// Object &#123; a: 1, b: 2 &#125;</span></span><br><span class="line">&#125;);</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> value <span class="keyword">of</span> mySet) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(value);</span><br><span class="line">    <span class="comment">// 1</span></span><br><span class="line">    <span class="comment">// 2</span></span><br><span class="line">    <span class="comment">// 3</span></span><br><span class="line">    <span class="comment">// 'strings'</span></span><br><span class="line">    <span class="comment">// Object &#123; a: 1, b: 2 &#125;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　Set 同样有 delete() 和 clear() 方法。</p>
<h3 id="WeakSet"><a href="#WeakSet" class="headerlink" title="WeakSet"></a>WeakSet</h3><p>　　类似于 WeakMap，WeakSet 对象可以让你在一个集合中保存对象的弱引用，在 WeakSet 中的对象只允许出现一次：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> ws = <span class="keyword">new</span> <span class="built_in">WeakSet</span>();</span><br><span class="line"><span class="keyword">var</span> obj = &#123;&#125;;</span><br><span class="line"><span class="keyword">var</span> foo = &#123;&#125;;</span><br><span class="line">ws.add(<span class="built_in">window</span>);</span><br><span class="line">ws.add(obj);</span><br><span class="line">ws.has(<span class="built_in">window</span>); <span class="comment">// true</span></span><br><span class="line">ws.has(foo);    <span class="comment">// false, foo 没有添加成功</span></span><br><span class="line">ws.delete(<span class="built_in">window</span>); <span class="comment">// 从结合中删除 window 对象</span></span><br><span class="line">ws.has(<span class="built_in">window</span>);    <span class="comment">// false, window 对象已经被删除</span></span><br></pre></td></tr></table></figure>

<h2 id="13-类"><a href="#13-类" class="headerlink" title="13. 类"></a>13. 类</h2><p>　　ES6 中有 class 语法。值得注意是，这里的 class 不是新的对象继承模型，它只是原型链的语法糖表现形式。函数中使用 static 关键词定义构造函数的的方法和属性：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Task</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"task instantiated!"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  showId() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="number">23</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">static</span> loadAll() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"Loading all tasks.."</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> Task); <span class="comment">// function</span></span><br><span class="line"><span class="keyword">let</span> task = <span class="keyword">new</span> Task(); <span class="comment">// "task instantiated!"</span></span><br><span class="line">task.showId(); <span class="comment">// 23</span></span><br><span class="line">Task.loadAll(); <span class="comment">// "Loading all tasks.."</span></span><br></pre></td></tr></table></figure>

<p>类中的继承和超集：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Car</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"Creating a new car"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Porsche</span> <span class="keyword">extends</span> <span class="title">Car</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"Creating Porsche"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> c = <span class="keyword">new</span> Porsche();</span><br><span class="line"><span class="comment">// Creating a new car</span></span><br><span class="line"><span class="comment">// Creating Porsche</span></span><br></pre></td></tr></table></figure>

<p>　　extends 允许一个子类继承父类，需要注意的是，子类的 constructor 函数中需要执行 super() 函数。当然，你也可以在子类方法中调用父类的方法，如 super.parentMethodName()。</p>
<p>　　有两点值得注意的是：</p>
<ul>
<li>类的声明不会提升（hoisting)，如果你要使用某个 Class，那你必须在使用之前定义它，否则会抛出一个 ReferenceError 的错误</li>
<li>在类中定义函数不需要使用 function 关键词</li>
</ul>
<p>　　在 <a href="https://developer.mozilla.org/en/docs/Web/JavaScript/Reference/Classes" target="_blank" rel="noopener">这里</a> 阅读更多关于类的介绍。</p>
<h2 id="14-Symbol"><a href="#14-Symbol" class="headerlink" title="14. Symbol"></a>14. Symbol</h2><p>　　Symbol 是一种新的数据类型，它的值是唯一的，不可变的。ES6 中提出 symbol 的目的是为了生成一个唯一的标识符，不过你访问不到这个标识符：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> sym = <span class="built_in">Symbol</span>( <span class="string">"some optional description"</span> );</span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> sym); <span class="comment">// symbol</span></span><br></pre></td></tr></table></figure>

<p>　　注意，这里 Symbol 前面不能使用 new 操作符。如果它被用作一个对象的属性，那么这个属性会是不可枚举的：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> o = &#123;</span><br><span class="line">    val: <span class="number">10</span>,</span><br><span class="line">    [ <span class="built_in">Symbol</span>(<span class="string">"random"</span>) ]: <span class="string">"I'm a symbol"</span>,</span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getOwnPropertyNames(o)); <span class="comment">// val</span></span><br></pre></td></tr></table></figure>

<p>　　如果要获取对象 symbol 属性，需要使用 Object.getOwnPropertySymbols(o)。</p>
<h2 id="15-迭代器（Iterators）"><a href="#15-迭代器（Iterators）" class="headerlink" title="15. 迭代器（Iterators）"></a>15. 迭代器（Iterators）</h2><p>　　迭代器允许每次访问数据集合的一个元素，当指针指向数据集合最后一个元素是，迭代器便会退出。它提供了 next() 函数来遍历一个序列，这个方法返回一个包含 done 和 value 属性的对象。</p>
<p>　　ES6 中可以通过 Symbol.iterator 给对象设置默认的遍历器，无论什么时候对象需要被遍历，执行它的 @@iterator 方法便可以返回一个用于获取值的迭代器。</p>
<p>　　数组默认就是一个迭代器：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">11</span>,<span class="number">12</span>,<span class="number">13</span>];</span><br><span class="line"><span class="keyword">var</span> itr = arr[<span class="built_in">Symbol</span>.iterator]();</span><br><span class="line">itr.next(); <span class="comment">// &#123; value: 11, done: false &#125;</span></span><br><span class="line">itr.next(); <span class="comment">// &#123; value: 12, done: false &#125;</span></span><br><span class="line">itr.next(); <span class="comment">// &#123; value: 13, done: false &#125;</span></span><br><span class="line">itr.next(); <span class="comment">// &#123; value: undefined, done: true &#125;</span></span><br></pre></td></tr></table></figure>

<p>　　你可以通过 <a href>Symbol.iterator</a> 自定义一个对象的迭代器。</p>
<h2 id="16-Generators-和-Promises"><a href="#16-Generators-和-Promises" class="headerlink" title="16. Generators 和 Promises"></a>16. Generators 和 Promises</h2><h3 id="Generators"><a href="#Generators" class="headerlink" title="Generators"></a>Generators</h3><p>　　Generator 函数是 ES6 的新特性，它允许一个函数返回的可遍历对象生成多个值。在使用中你会看到 * 语法和一个新的关键词 yield:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> *<span class="title">infiniteNumbers</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> n = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">while</span> (<span class="literal">true</span>)&#123;</span><br><span class="line">    <span class="keyword">yield</span> n++;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> numbers = infiniteNumbers(); <span class="comment">// returns an iterable object</span></span><br><span class="line">numbers.next(); <span class="comment">// &#123; value: 1, done: false &#125;</span></span><br><span class="line">numbers.next(); <span class="comment">// &#123; value: 2, done: false &#125;</span></span><br><span class="line">numbers.next(); <span class="comment">// &#123; value: 3, done: false &#125;</span></span><br></pre></td></tr></table></figure>

<p>　　每次执行 yield 时，返回的值变为迭代器的下一个值。</p>
<h3 id="Promises"><a href="#Promises" class="headerlink" title="Promises"></a>Promises</h3><p>　　ES6 对 Promise 有了原生的支持，一个 Promise 是一个等待被异步执行的对象，当它执行完成后，其状态会变成 resolved 或者 rejected。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;  </span><br><span class="line">  <span class="keyword">if</span> (<span class="comment">/* condition */</span>) &#123;</span><br><span class="line">    <span class="comment">// fulfilled successfully</span></span><br><span class="line">    resolve(<span class="comment">/* value */</span>);  </span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// error, rejected</span></span><br><span class="line">    reject(<span class="comment">/* reason */</span>);  </span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>　　每一个 Promise 都有一个 .then 方法，这个方法接受两个参数，第一个是处理 resolved 状态的回调，一个是处理 rejected 状态的回调：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">p.then(<span class="function">(<span class="params">val</span>) =&gt;</span> <span class="built_in">console</span>.log(<span class="string">"Promise Resolved"</span>, val),</span><br><span class="line">       (err) =&gt; <span class="built_in">console</span>.log(<span class="string">"Promise Rejected"</span>, err));</span><br></pre></td></tr></table></figure>

<hr>
]]></content>
      <categories>
        <category>前沿技术</category>
      </categories>
      <tags>
        <tag>ES6</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Git 中 git push 到远程仓库不被授权的问题</title>
    <url>/2017/02/21/article-git-connect/</url>
    <content><![CDATA[<p>　　使用 Git 过程中，今天突然遇到这样的问题，在 git push 到 github 远程仓库时失败了，提示你说这次 push 不被授权，昨天还是好好的，难道是 github 又被墙了？后开始测试发现除了 SSH 所有的连接都是正常的。Webstorm 里直接 push 会提示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Push failed: Failed with error: fatal: Could not read from remote repository.</span><br></pre></td></tr></table></figure>

<h2 id="原因"><a href="#原因" class="headerlink" title="原因"></a>原因</h2><p>　　没有匹配正确 ssh 公钥密钥对。要么是你没有生成对应的公钥密钥对并将公钥部署在 github 上的对应项目，要么就是生成了密钥对并将公钥部署在了 github 上之后，你的本地目录没有将私钥配置上或者是<strong>之前有配置，但是过期了</strong>。</p>
<h2 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h2><ul>
<li><p>首先，生成一个公钥密钥对：<strong>ssh-keygen -t rsa -C “<a href="mailto:youemail@example.com" target="_blank" rel="noopener">youemail@example.com</a>“ -f ~/.ssh/id_rsa</strong>  此命令可创建 SSH 公钥密钥对，”<a href="mailto:youemail@example.com" target="_blank" rel="noopener">youemail@example.com</a>“为你自己的邮箱地址，”id_rsa”为公钥密钥名，可修改。 创建后的公钥密钥对存放在本地目录C:\Users[UserName].ssh\文件夹中，可以查看</p>
</li>
<li><p>将公钥部署上github，将（1）中生成的id_rsa.pub文件用文本编辑器打开，复制其中内容。在对应的github项目的“setting”中的“SSH and GPG keys”选择“new SSH key“，将公钥内容粘贴上去，新建一个SSH key。</p>
</li>
<li><p>在 git bash 命令行在添加私钥。查看私钥列表的命令为：<strong>ssh-add -l</strong>，如果输出以上命令出现Could not open a connection to your authentication agent.，则再输入<strong>ssh-agent bash</strong>，即可进入SSH bash。添加私钥的命令：<strong>ssh-add ~/.ssh/id_rsa</strong>，此时再查看私钥列表，若已经成功添加，则可以正常地使用 git push 进行对远程仓库的更新了。</p>
</li>
</ul>
<blockquote>
<p>其他 Git 常用命令行请移步 <a href="/2017/01/09/git-skills/">常用 git 命令行总结</a>。</p>
</blockquote>
<hr>]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>驯服 JavaScript 的“this”！</title>
    <url>/2017/02/18/article-js-this/</url>
    <content><![CDATA[<p>　　JavaScript 中的<code>this</code>关键字往往让菜鸟和大神都感到困惑。本文目的就是带你驯服它。我们将了解如何在每一个场景中正确地使用<code>this</code>，内容较多，多方整理。</p>
<p>　　在函数中 <code>this</code> 到底取何值，是在函数真正被调用执行的时候确定下来的，函数定义的时候确定不了。 因为 <code>this</code> 的取值是执行上下文环境的一部分，每次调用函数，都会产生一个新的执行上下文环境。当你在代码中使用了 <code>this</code>，这个 <code>this</code> 的值就直接从执行的上下文中获取了，而不会从作用域链中搜寻。<a id="more"></a></p>
<blockquote>
<p>JavaScript中很多时候会用到this，下面详细介绍每一种情况。在这里我想首先介绍一下宿主环境这个概念。一门语言在运行的时候，需要一个环境，叫做宿主环境。对于JavaScript，宿主环境最常见的是web浏览器，浏览器提供了一个JavaScript运行的环境，这个环境里面，需要提供一些接口，好让JavaScript引擎能够和宿主环境对接。JavaScript引擎才是真正执行JavaScript代码的地方，常见的引擎有V8(目前最快JavaScript引擎、Google生产)、JavaScript core。JavaScript引擎主要做了下面几件事情：</p>
</blockquote>
<ul>
<li>一套与宿主环境相联系的规则;</li>
<li>JavaScript引擎内核（基本语法规范、逻辑、命令和算法);</li>
<li>一组内置对象和API;</li>
<li>其他约定。</li>
</ul>
<blockquote>
<p>但是环境不是唯一的，也就是JavaScript不仅仅能够在浏览器里面跑，也能在其他提供了宿主环境的程序里面跑，最常见的就是nodejs。同样作为一个宿主环境，nodejs也有自己的JavaScript引擎–V8。根据官方的定义:<br>  Node.js is a platform built on Chrome’s JavaScript runtime for easily building fast, scalable network applications</p>
</blockquote>
<h2 id="global-this"><a href="#global-this" class="headerlink" title="global this"></a>global this</h2><ul>
<li>在浏览器里，在全局范围内，<code>this</code>等价于<code>window</code>对象。</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">this</span> === <span class="built_in">window</span>); <span class="comment">//true</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>在浏览器里，在全局范围内，用<code>var</code>声明一个变量和给<code>this</code>或者<code>window</code>添加属性是等价的。</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">  <span class="keyword">var</span> foo = <span class="string">"bar"</span>;</span></span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo); <span class="comment">//logs "bar"</span></span></span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="built_in">window</span>.foo); <span class="comment">//logs "bar"</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>如果你在声明一个变量的时候没有使用<code>var</code>或者<code>let</code>(ECMAScript 6),你就是在给全局的<code>this</code>添加或者改变属性值。</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">  foo = <span class="string">"bar"</span>;</span></span><br><span class="line"> </span><br><span class="line"><span class="actionscript">  <span class="function"><span class="keyword">function</span> <span class="title">testThis</span><span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">    foo = <span class="string">"foo"</span>;</span></span><br><span class="line">  &#125;</span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo); <span class="comment">//logs "bar"</span></span></span><br><span class="line">  testThis();</span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo); <span class="comment">//logs "foo"</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>在<code>node</code>环境里，如果使用REPL(Read-Eval-Print Loop，简称REPL:读取-求值-输出,是一个简单的，交互式的编程环境)来执行程序,<code>this</code>并不是最高级的命名空间，最高级的是<code>global</code>。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"> <span class="comment">//this</span></span><br><span class="line">&#123; <span class="attr">ArrayBuffer</span>: [<span class="built_in">Function</span>: <span class="built_in">ArrayBuffer</span>],</span><br><span class="line">  <span class="built_in">Int8Array</span>: &#123; [<span class="built_in">Function</span>: <span class="built_in">Int8Array</span>] BYTES_PER_ELEMENT: <span class="number">1</span> &#125;,</span><br><span class="line">  <span class="built_in">Uint8Array</span>: &#123; [<span class="built_in">Function</span>: <span class="built_in">Uint8Array</span>] BYTES_PER_ELEMENT: <span class="number">1</span> &#125;,</span><br><span class="line">  <span class="comment">//...</span></span><br><span class="line"> global === <span class="keyword">this</span></span><br><span class="line"> <span class="comment">//true</span></span><br></pre></td></tr></table></figure>

<ul>
<li>在<code>node</code>环境里，如果执行一个<code>js</code>脚本，在全局范围内，<code>this</code>以一个空对象开始作为最高级的命名空间，这个时候，它和<code>global</code>不是等价的。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//test.js脚本内容：</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">this</span>);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">this</span> === global);</span><br><span class="line"><span class="comment">//REPL运行脚本：</span></span><br><span class="line">$ node test.js</span><br><span class="line">&#123;&#125;</span><br><span class="line"><span class="literal">false</span></span><br></pre></td></tr></table></figure>

<ul>
<li>在<code>node</code>环境里，在全局范围内，如果你用REPL执行一个脚本文件，用<code>var</code>声明一个变量并不会和在浏览器里面一样将这个变量添加给<code>this</code>。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//test.js:</span></span><br><span class="line"><span class="keyword">var</span> foo = <span class="string">"bar"</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line"><span class="comment">//$ node test.js</span></span><br><span class="line"><span class="literal">undefined</span></span><br></pre></td></tr></table></figure>

<ul>
<li>但是如果你不是用REPL执行脚本文件，而是直接执行代码，结果和在浏览器里面是一样的(神坑)</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> foo = <span class="string">"bar"</span>;</span><br><span class="line"><span class="keyword">this</span>.foo</span><br><span class="line">bar</span><br><span class="line">global.foo</span><br><span class="line">bar</span><br></pre></td></tr></table></figure>

<ul>
<li>在<code>node</code>环境里，用REPL运行脚本文件的时候，如果在声明变量的时候没有使用<code>var</code>或者<code>let</code>，这个变量会自动添加到<code>global</code>对象，但是不会自动添加给<code>this</code>对象。如果是直接执行代码，则会同时添加给<code>global</code>和<code>this</code>。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//test.js</span></span><br><span class="line">foo = <span class="string">"bar"</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line"><span class="built_in">console</span>.log(global.foo);</span><br><span class="line"><span class="comment">//$ node test.js</span></span><br><span class="line"><span class="literal">undefined</span></span><br><span class="line">bar</span><br></pre></td></tr></table></figure>

<p>　　上面的八种情况可能大家已经绕晕了，总结起来就是：<strong>在浏览器里面this是老大，它等价于window对象，如果你声明一些全局变量(不管在任何地方)，这些变量都会作为this的属性</strong>。在<code>node</code>里面，有两种执行JavaScript代码的方式，<strong>一种是直接执行写好的JavaScript文件，另外一种是直接在里面执行一行行代码</strong>。对于直接运行一行行<code>JavaScript</code>代码的方式，<code>global</code>才是老大，<code>this</code>和它是等价的。在这种情况下，和浏览器比较相似，也就是声明一些全局变量会自动添加给老大<code>global</code>，顺带也会添加给<code>this</code>。但是在<code>node</code>里面直接脚本文件就不一样了，你声明的全局变量不会自动添加到<code>this</code>，但是会添加到global对象。所以相同点是，在全局范围内，<strong>全局变量终究是属于老大</strong>。 </p>
<h2 id="function-this"><a href="#function-this" class="headerlink" title="function this"></a>function this</h2><p>　　无论是在浏览器环境还是<code>node</code>环境， 除了在<code>DOM</code>事件处理程序里或者给出了<code>thisArg</code>(接下来会讲到)外，如果不是用<code>new</code>调用，在函数里面使用<code>this</code>都是指代全局范围的<code>this</code>。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">  foo = <span class="string">"bar"</span>; </span></span><br><span class="line"><span class="actionscript">  <span class="function"><span class="keyword">function</span> <span class="title">testThis</span><span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">this</span>.foo = <span class="string">"foo"</span>;</span></span><br><span class="line">  &#125;</span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo); <span class="comment">//logs "bar"</span></span></span><br><span class="line">  testThis();</span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo); <span class="comment">//logs "foo"</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//test.js</span></span><br><span class="line"> </span><br><span class="line">foo = <span class="string">"bar"</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">testThis</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.foo = <span class="string">"foo"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(global.foo);</span><br><span class="line">testThis();</span><br><span class="line"><span class="built_in">console</span>.log(global.foo);</span><br><span class="line"><span class="comment">//$ node test.js</span></span><br><span class="line">bar</span><br><span class="line">foo</span><br></pre></td></tr></table></figure>

<ul>
<li>除非你使用严格模式，这时候<code>this</code>就会变成<code>undefined</code>。</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">  foo = <span class="string">"bar"</span>;</span></span><br><span class="line"><span class="actionscript">  <span class="function"><span class="keyword">function</span> <span class="title">testThis</span><span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="javascript"><span class="meta">    "use strict"</span>;</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">this</span>.foo = <span class="string">"foo"</span>;</span></span><br><span class="line">  &#125;</span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo); <span class="comment">//logs "bar"</span></span></span><br><span class="line"><span class="actionscript">  testThis();  <span class="comment">//Uncaught TypeError: Cannot set property 'foo' of undefined </span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　如果你在调用函数的时候在前面使用了<code>new</code>，<code>this</code>就会变成一个新的值，和<code>global</code>的<code>this</code>脱离干系。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">  foo = <span class="string">"bar"</span>; </span></span><br><span class="line"><span class="actionscript">  <span class="function"><span class="keyword">function</span> <span class="title">testThis</span><span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">this</span>.foo = <span class="string">"foo"</span>;</span></span><br><span class="line">  &#125;</span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo); <span class="comment">//logs "bar"</span></span></span><br><span class="line"><span class="actionscript">  <span class="keyword">new</span> testThis();</span></span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo); <span class="comment">//logs "bar"</span></span></span><br><span class="line"><span class="javascript">  <span class="built_in">console</span>.log(<span class="keyword">new</span> testThis().foo); <span class="comment">//logs "foo"</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　我更喜欢把新的值称作一个实例。</p>
<blockquote>
<p>函数里面的this其实相对比较好理解，如果我们在一个函数里面使用this，需要注意的就是我们调用函数的方式，如果是正常的方式调用函数，this指代全局的this，如果我们加一个new，这个函数就变成了一个构造函数，我们就创建了一个实例，this指代这个实例，这个和其他面向对象的语言很像。另外，写JavaScript很常做的一件事就是绑定事件处理程序，也就是诸如button.addEventListener(‘click’, fn, false)之类的，如果在fn里面需要使用this，this指代事件处理程序对应的对象，也就是button。</p>
</blockquote>
<h2 id="prototype-this"><a href="#prototype-this" class="headerlink" title="prototype this"></a>prototype this</h2><ul>
<li>你创建的每一个函数都是函数对象。它们会自动获得一个特殊的属性<code>prototype</code>，你可以给这个属性赋值。当你用<code>new</code>的方式调用一个函数的时候，你就能通过<code>this</code>访问你给<code>prototype</code>赋的值了。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing(); <span class="comment">//logs "bar"</span></span><br><span class="line"><span class="built_in">console</span>.log(thing.foo);  <span class="comment">//logs "bar"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>当你使用<code>new</code>为你的函数创建多个实例的时候，这些实例会共享你给<code>prototype</code>设定的值。对于下面的例子，当你调用<code>this.foo</code>的时候，都会返回相同的值，除非你在某个实例里面重写了自己的<code>this.foo</code>。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.setFoo = <span class="function"><span class="keyword">function</span> (<span class="params">newFoo</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.foo = newFoo;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing1 = <span class="keyword">new</span> Thing();</span><br><span class="line"><span class="keyword">var</span> thing2 = <span class="keyword">new</span> Thing();</span><br><span class="line">thing1.logFoo(); <span class="comment">//logs "bar"</span></span><br><span class="line">thing2.logFoo(); <span class="comment">//logs "bar" </span></span><br><span class="line">thing1.setFoo(<span class="string">"foo"</span>);</span><br><span class="line">thing1.logFoo(); <span class="comment">//logs "foo";</span></span><br><span class="line">thing2.logFoo(); <span class="comment">//logs "bar";</span></span><br><span class="line">thing2.foo = <span class="string">"foobar"</span>;</span><br><span class="line">thing1.logFoo(); <span class="comment">//logs "foo";</span></span><br><span class="line">thing2.logFoo(); <span class="comment">//logs "foobar";</span></span><br></pre></td></tr></table></figure>

<ul>
<li>实例里面的<code>this</code>是一个特殊的对象。你可以把<code>this</code>想成一种获取<code>prototype</code>的值的一种方式。当你在一个实例里面直接给<code>this</code>添加属性的时候，会隐藏<code>prototype</code>中与之同名的属性。如果你想访问<code>prototype</code>中的这个属性值而不是你自己设定的属性值，你可以通过在实例里面删除你自己添加的属性的方式来实现。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.setFoo = <span class="function"><span class="keyword">function</span> (<span class="params">newFoo</span>) </span>&#123;</span><br><span class="line">   <span class="keyword">this</span>.foo = newFoo;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.deleteFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">delete</span> <span class="keyword">this</span>.foo;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">thing.setFoo(<span class="string">"foo"</span>);</span><br><span class="line">thing.logFoo(); <span class="comment">//logs "foo";</span></span><br><span class="line">thing.deleteFoo();</span><br><span class="line">thing.logFoo(); <span class="comment">//logs "bar";</span></span><br><span class="line">thing.foo = <span class="string">"foobar"</span>;</span><br><span class="line">thing.logFoo(); <span class="comment">//logs "foobar";</span></span><br><span class="line"><span class="keyword">delete</span> thing.foo;</span><br><span class="line">thing.logFoo(); <span class="comment">//logs "bar";</span></span><br></pre></td></tr></table></figure>

<ul>
<li>或者你也能直接通过引用函数对象的<code>prototype</code> 来获得你需要的值。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo, Thing.prototype.foo);</span><br><span class="line"> &#125;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">thing.foo = <span class="string">"foo"</span>;</span><br><span class="line">thing.logFoo(); <span class="comment">//logs "foo bar";</span></span><br></pre></td></tr></table></figure>

<ul>
<li>通过一个函数创建的实例会共享这个函数的<code>prototype</code>属性的值，如果你给这个函数的<code>prototype</code>赋值一个<code>Array</code>，那么所有的实例都会共享这个<code>Array</code>，除非你在实例里面重写了这个<code>Array</code>，这种情况下，函数的<code>prototype</code>的<code>Array</code>就会被隐藏掉。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.things = [];</span><br><span class="line"><span class="keyword">var</span> thing1 = <span class="keyword">new</span> Thing();</span><br><span class="line"><span class="keyword">var</span> thing2 = <span class="keyword">new</span> Thing();</span><br><span class="line">thing1.things.push(<span class="string">"foo"</span>);</span><br><span class="line"><span class="built_in">console</span>.log(thing2.things); <span class="comment">//logs ["foo"]</span></span><br></pre></td></tr></table></figure>

<ul>
<li>给一个函数的<code>prototype</code>赋值一个<code>Array</code>通常是一个错误的做法。如果你想每一个实例有他们专属的<code>Array</code>，你应该在函数里面创建而不是在<code>prototype</code>里面创建。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.things = [];</span><br><span class="line">&#125; </span><br><span class="line"> <span class="keyword">var</span> thing1 = <span class="keyword">new</span> Thing();</span><br><span class="line"> <span class="keyword">var</span> thing2 = <span class="keyword">new</span> Thing();</span><br><span class="line">thing1.things.push(<span class="string">"foo"</span>);</span><br><span class="line"><span class="built_in">console</span>.log(thing1.things); <span class="comment">//logs ["foo"]</span></span><br><span class="line"><span class="built_in">console</span>.log(thing2.things); <span class="comment">//logs []</span></span><br></pre></td></tr></table></figure>

<ul>
<li>实际上你可以通过把多个函数的<code>prototype</code>链接起来的从而形成一个原型链，因此<code>this</code>就会魔法般地沿着这条原型链往上查找直到找你你需要引用的值。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing1</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing1.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing2.prototype = <span class="keyword">new</span> Thing1();</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing2();</span><br><span class="line"><span class="built_in">console</span>.log(thing.foo); <span class="comment">//logs "bar"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>一些人利用原型链的特性来在<code>JavaScript</code>模仿经典的面向对象的继承方式。任何给用于构建原型链的函数的<code>this</code>的赋值的语句都会隐藏原型链上游的相同的属性。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing1</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing1.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.foo = <span class="string">"foo"</span>;</span><br><span class="line">&#125;</span><br><span class="line">Thing2.prototype = <span class="keyword">new</span> Thing1();</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing3</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing3.prototype = <span class="keyword">new</span> Thing2();</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing3();</span><br><span class="line"><span class="built_in">console</span>.log(thing.foo); <span class="comment">//logs "foo"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>我喜欢把被赋值给<code>prototype</code>的函数叫做方法。在上面的例子中，我已经使用过方法了，如<code>logFoo</code>。这些方法有着相同的<code>prototype</code>，即创建这些实力的原始函数。我通常把这些原始函数叫做构造函数。在<code>prototype</code>里面定义的方法里面使用<code>this</code>会影响到当前实例的原型链的上游的<code>this</code>。这意味着你直接给<code>this</code>赋值的时候，隐藏了原型链上游的相同的属性值。这个实例的任何方法都会使用这个最新的值而不是原型里面定义的这个相同的值。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing1</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing1.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing1.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line"> &#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.foo = <span class="string">"foo"</span>;</span><br><span class="line">&#125;</span><br><span class="line">Thing2.prototype = <span class="keyword">new</span> Thing1();</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing2();</span><br><span class="line">thing.logFoo(); <span class="comment">//logs "foo";</span></span><br></pre></td></tr></table></figure>

<ul>
<li>在<code>JavaScript</code>里面你可以嵌套函数，也就是你可以在函数里面定义函数。嵌套函数可以通过闭包捕获父函数的变量，但是这个函数没有继承<code>this</code>。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> info = <span class="string">"attempting to log this.foo:"</span>;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">doIt</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(info, <span class="keyword">this</span>.foo);</span><br><span class="line">     &#125;</span><br><span class="line">     doIt();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">thing.logFoo();  <span class="comment">//logs "attempting to log this.foo: undefined"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>在doIt里面的<code>this</code>是<code>global</code>对象或者在严格模式下面是<code>undefined</code>。这是造成很多不熟悉<code>JavaScript</code>的人深陷 <code>this</code>陷阱的根源。在这种情况下事情变得非常糟糕，就像你把一个实例的方法当作一个值，把这个值当作函数参数传递给另外一个函数但是却不把这个实例传递给这个函数一样。在这种情况下，一个方法里面的环境变成了全局范围，或者在严格模式下面的<code>undefined</code>。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;  </span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);   </span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">doIt</span>(<span class="params">method</span>) </span>&#123;</span><br><span class="line">   method();</span><br><span class="line">&#125;</span><br><span class="line"> <span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">thing.logFoo(); <span class="comment">//logs "bar"</span></span><br><span class="line">doIt(thing.logFoo); <span class="comment">//logs undefined</span></span><br></pre></td></tr></table></figure>

<ul>
<li>一些人喜欢先把<code>this</code>捕获到一个变量里面，通常这个变量叫做<code>self</code>，来避免上面这种情况的发生。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> self = <span class="keyword">this</span>;</span><br><span class="line">    <span class="keyword">var</span> info = <span class="string">"attempting to log this.foo:"</span>;</span><br><span class="line">   <span class="function"><span class="keyword">function</span> <span class="title">doIt</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">       <span class="built_in">console</span>.log(info, self.foo);</span><br><span class="line">   &#125;</span><br><span class="line">   doIt();</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">thing.logFoo();  <span class="comment">//logs "attempting to log this.foo: bar"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>但是当你需要把一个方法作为一个值传递给一个函数的时候并不管用。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; </span><br><span class="line">    <span class="keyword">var</span> self = <span class="keyword">this</span>;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">doIt</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">       <span class="built_in">console</span>.log(self.foo);</span><br><span class="line">    &#125;</span><br><span class="line">    doIt();</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">doItIndirectly</span>(<span class="params">method</span>) </span>&#123;</span><br><span class="line">    method();</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line"> thing.logFoo(); <span class="comment">//logs "bar"</span></span><br><span class="line"> doItIndirectly(thing.logFoo); <span class="comment">//logs undefined</span></span><br></pre></td></tr></table></figure>

<ul>
<li>你可以通过<code>bind</code>将实例和方法一切传递给函数来解决这个问题，<code>bind</code>是一个函数定义在所有函数和方法的函数对象上面。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; </span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">doIt</span>(<span class="params">method</span>) </span>&#123;</span><br><span class="line">    method();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">doIt(thing.logFoo.bind(thing)); <span class="comment">//logs bar</span></span><br></pre></td></tr></table></figure>

<ul>
<li>你同样可以使用<code>apply</code>和<code>call</code>来在新的上下文中调用方法或函数。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; </span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">doIt</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line">    &#125;</span><br><span class="line">   doIt.apply(<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">doItIndirectly</span>(<span class="params">method</span>) </span>&#123;</span><br><span class="line">    method();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">doItIndirectly(thing.logFoo.bind(thing)); <span class="comment">//logs bar</span></span><br></pre></td></tr></table></figure>

<ul>
<li>你可以用<code>bind</code>来代替任何一个函数或者方法的<code>this</code>，即便它没有赋值给实例的初始<code>prototype</code>。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">logFoo</span>(<span class="params">aStr</span>) </span>&#123;</span><br><span class="line">   <span class="built_in">console</span>.log(aStr, <span class="keyword">this</span>.foo);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">logFoo.bind(thing)(<span class="string">"using bind"</span>); <span class="comment">//logs "using bind bar"</span></span><br><span class="line">logFoo.apply(thing, [<span class="string">"using apply"</span>]); <span class="comment">//logs "using apply bar"</span></span><br><span class="line">logFoo.call(thing, <span class="string">"using call"</span>); <span class="comment">//logs "using call bar"</span></span><br><span class="line">logFoo(<span class="string">"using nothing"</span>); <span class="comment">//logs "using nothing undefined"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>你应该避免在构造函数里面返回任何东西，因为这可能代替本来应该返回的实例。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;&#125;;</span><br><span class="line">&#125;</span><br><span class="line"> Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line"> Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">     <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line"> thing.logFoo(); <span class="comment">//Uncaught TypeError: undefined is not a function</span></span><br></pre></td></tr></table></figure>

<p>　　奇怪的是，如果你在构造函数里面返回了一个原始值，上面所述的情况并不会发生并且返回语句被忽略了。最好不要在你将通过new调用的构造函数里面返回任何类型的数据，即便你知道自己正在做什么。如果你想创建一个工厂模式，通过一个函数来创建一个实例，这个时候不要使用<code>new</code>来调用函数。当然这个建议是可选的。</p>
<ul>
<li>你可以通过使用<code>Object.create</code>来避免使用<code>new</code>，这样同样能够创建一个实例。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"> &#125;</span><br><span class="line"> Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line"> Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">     <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">var</span> thing =  <span class="built_in">Object</span>.create(Thing.prototype);</span><br><span class="line"> thing.logFoo(); <span class="comment">//logs "bar"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>在这种情况下并不会调用构造函数：</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">     <span class="keyword">this</span>.foo = <span class="string">"foo"</span>;</span><br><span class="line"> &#125;</span><br><span class="line"> Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"> Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing =  <span class="built_in">Object</span>.create(Thing.prototype);</span><br><span class="line">thing.logFoo(); <span class="comment">//logs "bar"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>因为<code>Object.create</code>不会调用构造函数的特性在你继承模式下你想通过原型链重写构造函数的时候非常有用。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing1</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.foo = <span class="string">"foo"</span>;</span><br><span class="line"> &#125;</span><br><span class="line">Thing1.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.logFoo(); <span class="comment">//logs "bar"</span></span><br><span class="line">    Thing1.apply(<span class="keyword">this</span>);</span><br><span class="line">    <span class="keyword">this</span>.logFoo(); <span class="comment">//logs "foo"</span></span><br><span class="line">&#125;</span><br><span class="line">Thing2.prototype = <span class="built_in">Object</span>.create(Thing1.prototype);</span><br><span class="line">Thing2.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing2();</span><br></pre></td></tr></table></figure>

<h2 id="object-this"><a href="#object-this" class="headerlink" title="object this"></a>object this</h2><ul>
<li>在一个对象的一个函数里，你可以通过<code>this</code>来引用这个对象的其他属性。这个用<code>new</code>来新建一个实例是不一样的。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">    foo: <span class="string">"bar"</span>,</span><br><span class="line">   logFoo: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line">obj.logFoo(); <span class="comment">//logs "bar"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>注意，没有使用<code>new</code>，没有使用<code>Object.create</code>，也没有使用函数调用创建一个对象。你也可以将对象当作一个实例将函数绑定到上面。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">    foo: <span class="string">"bar"</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">logFoo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line"> &#125;</span><br><span class="line">logFoo.apply(obj); <span class="comment">//logs "bar"</span></span><br></pre></td></tr></table></figure>

<ul>
<li>当你用这种方式使用<code>this</code>的时候，并不会越出当前的对象。只有有相同直接父元素的属性才能通过<code>this</code>共享变量。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  foo: <span class="string">"bar"</span>,</span><br><span class="line">  deeper: &#123;</span><br><span class="line">      logFoo: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span>.foo);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line">obj.deeper.logFoo(); <span class="comment">//logs undefined</span></span><br></pre></td></tr></table></figure>

<ul>
<li>你可以直接通过对象引用你需要的属性</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">    foo: <span class="string">"bar"</span>,</span><br><span class="line">    deeper: &#123;</span><br><span class="line">        logFoo: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="built_in">console</span>.log(obj.foo);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line">obj.deeper.logFoo(); <span class="comment">//logs "bar"</span></span><br></pre></td></tr></table></figure>

<h2 id="DOM-event-this"><a href="#DOM-event-this" class="headerlink" title="DOM event this"></a>DOM event this</h2><ul>
<li>在一个<code>HTML DOM</code>事件处理程序里面，<code>this</code>始终指向这个处理程序被所绑定到的<code>HTML DOM</code>节点</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Listener</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">document</span>.getElementById(<span class="string">"foo"</span>).addEventListener(<span class="string">"click"</span>,</span><br><span class="line">    <span class="keyword">this</span>.handleClick);</span><br><span class="line">&#125;</span><br><span class="line">Listener.prototype.handleClick = <span class="function"><span class="keyword">function</span> (<span class="params">event</span>) </span>&#123;</span><br><span class="line">   <span class="built_in">console</span>.log(<span class="keyword">this</span>); <span class="comment">//logs "&lt;div id="foo"&gt;&lt;/div&gt;"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> listener = <span class="keyword">new</span> Listener();</span><br><span class="line"><span class="built_in">document</span>.getElementById(<span class="string">"foo"</span>).click();</span><br></pre></td></tr></table></figure>

<ul>
<li>除非你自己通过<code>bind</code>切换了上下文</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Listener</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">document</span>.getElementById(<span class="string">"foo"</span>).addEventListener(<span class="string">"click"</span>, </span><br><span class="line">      <span class="keyword">this</span>.handleClick.bind(<span class="keyword">this</span>));</span><br><span class="line">&#125;</span><br><span class="line">Listener.prototype.handleClick = <span class="function"><span class="keyword">function</span> (<span class="params">event</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>); <span class="comment">//logs Listener &#123;handleClick: function&#125;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> listener = <span class="keyword">new</span> Listener();</span><br><span class="line"><span class="built_in">document</span>.getElementById(<span class="string">"foo"</span>).click();</span><br></pre></td></tr></table></figure>

<h2 id="HTML-this"><a href="#HTML-this" class="headerlink" title="HTML this"></a>HTML this</h2><ul>
<li>在<code>HTML</code>节点的属性里面，你可以放置<code>JavaScript</code>代码，<code>this</code>指向了这个元素</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"foo"</span> <span class="attr">onclick</span>=<span class="string">"console.log(this);"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="javascript"><span class="built_in">document</span>.getElementById(<span class="string">"foo"</span>).click(); <span class="comment">//logs &lt;div id="foo"...</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="override-this"><a href="#override-this" class="headerlink" title="override this"></a>override this</h2><ul>
<li>你不能重写<code>this</code>，因为它是保留字。</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> <span class="keyword">this</span> = &#123;&#125;;  <span class="comment">// Uncaught SyntaxError: Unexpected token this </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="eval-this"><a href="#eval-this" class="headerlink" title="eval this"></a>eval this</h2><ul>
<li>你可以通过<code>eval</code>来访问<code>this</code></li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">eval</span>(<span class="string">"console.log(this.foo)"</span>); <span class="comment">//logs "bar"</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">thing.logFoo();</span><br></pre></td></tr></table></figure>

<p>　　这会造成一个安全问题，除非不用<code>eval</code>，没有其他方式来避免这个问题。</p>
<ul>
<li>在通过<code>Function</code>来创建一个函数的时候，同样能够访问<code>this</code></li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="keyword">new</span> <span class="built_in">Function</span>(<span class="string">"console.log(this.foo);"</span>);</span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">thing.logFoo(); <span class="comment">//logs "bar"</span></span><br></pre></td></tr></table></figure>

<h2 id="with-this"><a href="#with-this" class="headerlink" title="with this"></a>with this</h2><ul>
<li>你可以通过<code>with</code>来将<code>this</code>添加到当前的执行环境，并且读写<code>this</code>的属性的时候不需要通过<code>this</code></li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.foo = <span class="string">"bar"</span>;</span><br><span class="line">Thing.prototype.logFoo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">   <span class="keyword">with</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">       <span class="built_in">console</span>.log(foo);</span><br><span class="line">        foo = <span class="string">"foo"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing();</span><br><span class="line">thing.logFoo(); <span class="comment">// logs "bar"</span></span><br><span class="line"><span class="built_in">console</span>.log(thing.foo); <span class="comment">// logs "foo"</span></span><br></pre></td></tr></table></figure>

<p>许多人认为这样使用是不好的，因为<code>with</code>本身就饱受争议。</p>
<h2 id="jQuery-this"><a href="#jQuery-this" class="headerlink" title="jQuery this"></a>jQuery this</h2><p>　　和<code>HTML DOM</code>元素节点的事件处理程序一样，在许多情况下<code>JQuery</code>的<code>this</code>都指向<code>HTML</code>元素节点。这在事件处理程序和一些方便的方法中都是管用的，比如<code>$.each</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"foo bar1"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"foo bar2"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="javascript">$(<span class="string">".foo"</span>).each(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="javascript">    <span class="built_in">console</span>.log(<span class="keyword">this</span>); <span class="comment">//logs &lt;div class="foo...</span></span></span><br><span class="line">&#125;);</span><br><span class="line"><span class="javascript">$(<span class="string">".foo"</span>).on(<span class="string">"click"</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="javascript">    <span class="built_in">console</span>.log(<span class="keyword">this</span>); <span class="comment">//logs &lt;div class="foo...</span></span></span><br><span class="line">&#125;);</span><br><span class="line"><span class="javascript">$(<span class="string">".foo"</span>).each(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">this</span>.click();</span></span><br><span class="line">&#125;);</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="thisArg-this"><a href="#thisArg-this" class="headerlink" title="thisArg this"></a>thisArg this</h2><p>　　如果你用过<code>underscore.js</code> 或者 <code>lo-dash</code>你可能知道许多类库的方法可以通过一个叫做<code>thisArg</code> 的函数参数来传递实例，这个函数参数会作为<code>this</code>的上下文。举个例子，这适用于<code>_.each</code>。原生的JavaScript在ECMAScript 5的时候也允许函数传递一个<code>thisArg</code>参数了，比如<code>forEach</code>。事实上，之前阐述的<code>bind</code>，<code>apply</code>和<code>call</code>的使用已经给你创造了传递<code>thisArg</code>参数给函数的机会。这个参数将<code>this</code>绑定为你所传递的对象。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Thing</span>(<span class="params">type</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.type = type;</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.log = <span class="function"><span class="keyword">function</span> (<span class="params">thing</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.type, thing);</span><br><span class="line">&#125;</span><br><span class="line">Thing.prototype.logThings = <span class="function"><span class="keyword">function</span> (<span class="params">arr</span>) </span>&#123;</span><br><span class="line">   arr.forEach(<span class="keyword">this</span>.log, <span class="keyword">this</span>); <span class="comment">// logs "fruit apples..."</span></span><br><span class="line">   _.each(arr, <span class="keyword">this</span>.log, <span class="keyword">this</span>); <span class="comment">//logs "fruit apples..."</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> thing = <span class="keyword">new</span> Thing(<span class="string">"fruit"</span>);</span><br><span class="line">thing.logThings([<span class="string">"apples"</span>, <span class="string">"oranges"</span>, <span class="string">"strawberries"</span>, <span class="string">"bananas"</span>]);</span><br></pre></td></tr></table></figure>

<p>　　这使得代码变得更加简介，因为避免了一大堆<code>bind</code>语句、函数嵌套和<code>this</code>暂存的使用。</p>
<hr>]]></content>
      <categories>
        <category>面试总结</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Web 前端性能优化</title>
    <url>/2017/02/16/article-web-optimize/</url>
    <content><![CDATA[<p>　　前端性能优化是个巨大的课题，如果要面面俱到的说的话，估计三天三夜说不完。所以我们就从实际的工程应用角度出发，聊我们最常遇见的前端优化问题。</p>
<p>　　Yslow是雅虎开发的基于网页性能分析浏览器插件，可以检测出网页的具体性能值，并且有著名的Yslow 23条优化规则，那我们就先来看看这23条。<a id="more"></a></p>
<h2 id="Yslow-23条"><a href="#Yslow-23条" class="headerlink" title="Yslow 23条"></a>Yslow 23条</h2><hr>

<ul>
<li><strong>减少HTTP请求次数</strong></li>
</ul>
<p>　　尽量合并图片、CSS、JS。比如加载一个页面，如果有5个 css 文件的话，那么会发出5次 http 请求，这样会让用户第一次访问你的页面的时候会长时间等待。而如果把这个5个文件合成一个的话，就只需要发出一次 http 请求，节省网络请求时间，加快页面的加载。</p>
<hr>

<ul>
<li><strong>使用CDN</strong></li>
</ul>
<p>　　网站上静态资源即 css、js 全都可以使用 cdn 分发，图片亦然。</p>
<hr>

<ul>
<li><strong>避免空的 src 和 href</strong></li>
</ul>
<p>　　当 link 标签的 href 属性为空、script 标签的 src 属性为空的时候，浏览器渲染的时候会把<strong>当前页面的 URL</strong> 作为它们的属性值，从而把页面的内容加载进来作为它们的值。所以要避免这样的疏忽。</p>
<hr>

<ul>
<li><strong>为文件头指定 Expires</strong></li>
</ul>
<p>　　Exipres 是用来设置文件的过期时间的，一般对 css、js、图片资源有效。他可以使内容具有缓存性，这样下回再访问同样的资源时就通过浏览器缓存区读取，不需要再发出 http 请求。如下例子：</p>
<img src="/styles/images/web/optimize_1.png" alt="Expires 优化">

<p>　　新浪微博的这个css文件的Expires时间是2016-5-04 09:14:14。</p>
<hr>

<ul>
<li><strong>使用gzip压缩内容</strong></li>
</ul>
<p>　　gzip 能够压缩任何一个文本类型的响应，包括 html，xml，json 。大大缩小请求返回的数据量。</p>
<hr>

<ul>
<li><strong>把 CSS 放到顶部</strong></li>
</ul>
<p>　　网页上的资源加载时从上网下顺序加载的，所以 css 放在页面的顶部能够优先渲染页面，让用户感觉页面加载很快。</p>
<hr>

<ul>
<li><strong>把 JS 放到底部</strong></li>
</ul>
<p>　　加载 js 时会对后续的资源造成阻塞，必须得等 js 加载完才去加载后续的文件 ，所以就把 js 放在页面底部最后加载。</p>
<hr>

<ul>
<li><strong>避免使用 CSS 表达式</strong></li>
</ul>
<p>　　举个css表达式的例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">font-color: expression( (new Date()).getHours()%3 ? “#FFFFFF&quot; : “#AAAAAA&quot; );</span><br></pre></td></tr></table></figure>

<p>　　这个表达式会持续的在页面上计算样式，影响页面的性能。并且css表达式只被IE支持。</p>
<hr>

<ul>
<li><strong>将 CSS 和 JS 放到外部文件中</strong></li>
</ul>
<p>　　目的是缓存文件，可以参考原则 4。 但有时候为了减少请求，也会直接写到页面里，需根据 PV 和 IP 的比例权衡。</p>
<hr>

<ul>
<li><strong>权衡 DNS 查找次数</strong></li>
</ul>
<p>　　减少主机名可以节省响应时间。但同时，需要注意，减少主机会减少页面中并行下载的数量。</p>
<p>　　IE浏览器在同一时刻只能从同一域名下载两个文件。当在一个页面显示多张图片时，IE 用户的图片下载速度就会受到影响。所以新浪会搞N个二级域名来放图片。</p>
<p>　　下面是新浪微博的图片域名，我们可以看到他有多个域名，这样可以保证这些不同域名能够同时去下载图片，而不用排队。不过如果当使用的域名过多时，响应时间就会慢，因为不用响应域名时间不一致。</p>
<img src="/styles/images/web/optimize_2.png" alt="Expires 优化">

<hr>

<ul>
<li><strong>精简 CSS 和 JS</strong></li>
</ul>
<p>　　这里就涉及到 css 和 js 的压缩了。比如下面的新浪的一个 css 文件，把空格回车全部去掉，减少文件的大小。现在的压缩工具有很多，基本主流的前端构建工具都能进行 css 和 js 文件的压缩，如 grunt，glup 等。</p>
<hr>

<ul>
<li><strong>避免跳转</strong></li>
</ul>
<p>　　有种现象会比较坑爹，看起来没什么差别，其实多次了一次页面跳转。比如当URL本该有斜杠（/）却被忽略掉时。例如，当我们要访问 <em><a href="http://baidu.com" target="_blank" rel="noopener">http://baidu.com</a></em> 时，实际上返回的是一个包含301代码的跳转，它指向的是 _<a href="http://baidu.com/_（注意末尾的斜杠）。在" target="_blank" rel="noopener">http://baidu.com/_（注意末尾的斜杠）。在</a> nginx 服务器可以使用 rewrite；Apache 服务器中可以使用 Alias 或者 mod_rewrite 或者 the DirectorySlash 来避免。</p>
<p>　　另一种是不用域名之间的跳转， 比如访问  <em><a href="http://baidu.com/bbs" target="_blank" rel="noopener">http://baidu.com/bbs</a></em> 跳转到　_<a href="http://bbs.baidu.com/_。那么可以通过使用" target="_blank" rel="noopener">http://bbs.baidu.com/_。那么可以通过使用</a> Alias 或者 mod_rewirte 建立 CNAME（保存一个域名和另外一个域名之间关系的 DNS 记录）来替代。</p>
<hr>

<ul>
<li><strong>删除重复的 JS 和 CSS</strong></li>
</ul>
<p>　　重复调用脚本，除了增加额外的HTTP请求外，多次运算也会浪费时间。在 IE 和 Firefox 中不管脚本是否可缓存，它们都存在重复运算<code>JavaScript</code>的问题。</p>
<hr>

<ul>
<li><strong>配置 ETags</strong></li>
</ul>
<p>　　它用来判断浏览器缓存里的元素是否和原来服务器上的一致。比 last-modified date 更具有弹性，例如某个文件在1秒内修改了10次，Etag 可以综合 Inode (文件的索引节点(inode)数)，MTime (修改时间)和 Size 来精准的进行判断，避开 UNIX 记录 MTime 只能精确到秒的问题。 服务器集群使用，可取后两个参数。使用 ETags 减少 Web 应用带宽和负载。</p>
<hr>

<ul>
<li><strong>可缓存的 AJAX</strong></li>
</ul>
<p>　　异步请求同样的造成用户等待，所以使用 ajax 请求时，要主动告诉浏览器如果该请求有缓存就去请求缓存内容。如下代码片段, cache:true 就是显式的要求如果当前请求有缓存的话，直接使用缓存：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$.ajax(&#123;</span><br><span class="line">    url : <span class="string">'url'</span>,</span><br><span class="line">    dataType : <span class="string">"json"</span>,</span><br><span class="line">    cache: <span class="literal">true</span>,</span><br><span class="line">    success : <span class="function"><span class="keyword">function</span>(<span class="params">son, status</span>)</span>&#123; </span><br><span class="line">      <span class="comment">// do something;</span></span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure>

<hr>

<ul>
<li><strong>使用 GET 来完成 AJAX 请求</strong></li>
</ul>
<p>　　当使用 XMLHttpRequest 时，浏览器中的 POST 方法是一个“两步走”的过程：首先发送文件头，然后才发送数据。因此使用 GET 获取数据时更加有意义。</p>
<hr>

<ul>
<li><strong>减少 DOM 元素数量</strong></li>
</ul>
<p>　　这是一门大学问，这里可以引申出一堆优化的细节。想要具体研究的可以看后面推荐书籍。总之大原则减少 DOM 数量，就会减少浏览器的解析负担。</p>
<hr>

<ul>
<li><strong>避免404</strong></li>
</ul>
<p>　　比如外链的 css 或者 js 文件出现问题返回 404 时，会破坏浏览器对文件的并行加载。并且浏览器会把试图在返回的 404 响应内容中找到可能有用的部分当作 JavaScript 代码来执行。</p>
<hr>

<ul>
<li><strong>减少 Cookie 的大小</strong></li>
</ul>
<p>　　Cookie 里面别塞那么多东西，因为每个请求都得带着他跑。</p>
<hr>

<ul>
<li><strong>使用无 cookie 的域</strong></li>
</ul>
<p>　　比如 CSS、js、图片等，客户端请求静态文件的时候，减少了 Cookie 的反复传输对主域名的影响。</p>
<hr>

<ul>
<li><strong>不要使用滤镜</strong></li>
</ul>
<p>　　IE 独有属性 AlphaImageLoader 用于修正7.0以下版本中显示PNG图片的半透明效果。这个滤镜的问题在于浏览器加载图片时它会终止内容的呈现并且冻结浏览器。在每一个元素（不仅仅是图片）它都会运算一次，增加了内存开支，因此它的问题是多方面的。完全避免使用 AlphaImageLoader 的最好方法就是使用PNG8格式来代替，这种格式能在IE中很好地工作。如果你确实需要使用 AlphaImageLoader，请使用下划线 _filter 又使之对 IE7 以上版本的用户无效。</p>
<hr>

<ul>
<li><strong>不要在 HTML 中缩放图片</strong></li>
</ul>
<p>　　比如你需要的图片尺寸是50* 50</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">width</span>=<span class="string">”50″</span> <span class="attr">height</span>=<span class="string">”50″</span> <span class="attr">src</span>=<span class="string">“hahah.jpg”</span> <span class="attr">alt</span>=<span class="string">”hahaha”</span> /&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　那就不用用一张500*500的大尺寸图片，影响加载。</p>
<hr>

<ul>
<li><strong>缩小 favicon.ico ，并缓存</strong></li>
</ul>
<p>　　很多时候开发者都会忽略这个文件，但是每当有用户收藏网站/网页时，浏览器会自动请求这个文件，就算这个图标文件没有在你的网页中明显说明，浏览器也会请求，如果不添加就会出现 404。</p>
<p class="tc">-------------- 分割线 --------------</p>

<p>　　在文章开始就已经提到过了，前端优化是一个很大的话题，远远不是这23条就可以说完了，下面一些是个人实践或参考各方大牛的实践总结出来的（一部分常用）优化技巧。</p>
<h2 id="HTML-性能优化"><a href="#HTML-性能优化" class="headerlink" title="HTML 性能优化"></a>HTML 性能优化</h2><hr>

<ol>
<li><p>HTML标签有始终。 减少浏览器的判断时间</p>
</li>
<li><p>减少<code>iframe</code>的使用，因为<code>iframe</code>会增加一条<code>http</code>请求，阻止页面加载，即使内容为空，加载也需要时间</p>
</li>
<li><p>id和class，在能看明白的基础上，简化命名，在含有关键字的连接词中连接符号用’-‘，不要用’_’（未测试）</p>
</li>
<li><p>保持统一大小写，统一大小写有利于浏览器缓存，虽然浏览器不区分大小写，但是 w3c 标准为小写</p>
</li>
<li><p>清除空格，虽然空格有助于我们查看代码，但是每个空格相当于一个字符，空格越多，页面体积越大，像 google、baidu 等搜索引擎的首页去掉了所有可以去掉的空格、回车等字符，这样可以加快 web 页面的传输。</p>
</li>
<li><p>减少不必要的嵌套，尽量扁平化，因为当浏览器编译器遇到一个标签时就开始寻找它的结束标签，直到它匹配上才能显示它的内容，所以当嵌套很多时打开页面就会特别慢。</p>
</li>
<li><p>减少注释，因为过多注释不光占用空间，如果里面有大量关键词会影响搜索引擎的搜索</p>
</li>
<li><p>代码要结构化、语义化</p>
</li>
<li><p>除去无用的标签和空标签</p>
</li>
<li><p>尽量少使用废弃的标签，如b、i等，尽管高版本浏览器是向后兼容的</p>
</li>
</ol>
<h2 id="CSS-性能优化"><a href="#CSS-性能优化" class="headerlink" title="CSS 性能优化"></a>CSS 性能优化</h2><hr>

<ol>
<li><p>慎重使用高性能属性：浮动、定位；</p>
</li>
<li><p>尽量减少页面重排、重绘   重排按照css的书写顺序：</p>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">位置：positon、top、left、z-index、float、dispay   </span><br><span class="line"></span><br><span class="line">大小：width、height、margin、padding   </span><br><span class="line"></span><br><span class="line">文字系列： font、line-height、color、letter-spacing   </span><br><span class="line"></span><br><span class="line">背景边框：background、 border   </span><br><span class="line"></span><br><span class="line">其它：anmation、transition   </span><br><span class="line"></span><br><span class="line">重绘:border、outline、background、box-shadow,能使用background-color,就尽量不要使用background;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li><p>去除空规则：｛｝；</p>
</li>
<li><p>属性值为0时，不加单位；</p>
</li>
<li><p>属性值为浮动小数0.**，可以省略小数点之前的0；</p>
</li>
<li><p>标准化各种浏览器前缀：带浏览器前缀的在前。标准属性在后；</p>
</li>
<li><p>不使用@import前缀，它会影响css的加载速度；</p>
</li>
<li><p>充分利用css继承属性，减少代码量；</p>
</li>
<li><p>抽象提取公共样式，减少代码量；</p>
</li>
<li><p>选择器优化嵌套，尽量避免层级过深；</p>
</li>
<li><p>css雪碧图，同一页面相近部分的小图标，方便使用，减少页面的请求次数，但是同时图片本身会变大，使用时，优劣考虑清楚，再使用;</p>
</li>
</ol>
<h2 id="图片优化"><a href="#图片优化" class="headerlink" title="图片优化"></a>图片优化</h2><hr>

<ul>
<li><strong>使用 base64 编码代替图片</strong>   </li>
</ul>
<p>　　适用于图片小于2KB，页面引用图片不多的情况。将图片转换为base64编码字符串inline到CSS或页面中，减少http的请求次数。</p>
<ul>
<li><strong>合并图片sprite（雪碧图）</strong>   </li>
</ul>
<p>　　任何用到页面图片的场景。将多个页面用到的北京图片合并成一个大的图片在页面中引用，可以有效地减少请求个数。</p>
<ul>
<li><strong>使用canvas代替图片</strong>  </li>
</ul>
<p>　　需要高性能的图片或动画，使用HTML5的canvas元素绘制图片，页面渲染性能较高。</p>
<ul>
<li><strong>响应式图片</strong>   </li>
</ul>
<p>　　不同终端对同一图片的需求不一样，根据终端加载不同的图片来节省不必要的流量。通过picture元素，picturefill或平台判断来为不同终端平台输出不同的图片。减少没必要的图片加载，灵活控制。</p>
<ul>
<li><strong>图片压缩</strong>   </li>
</ul>
<p>　　在不得不加载图片的前提下，进一步提高优化效果，通过有损或无损压缩所见图片的大小。减少图片加载流量，效果明显。</p>
<ul>
<li><strong>更好的图片格式</strong>  </li>
</ul>
<p>　　webp、bpg、sharpP等新图片格式具有更好的压缩比</p>
<h2 id="JavaScript-性能优化"><a href="#JavaScript-性能优化" class="headerlink" title="JavaScript 性能优化"></a>JavaScript 性能优化</h2><hr>

<blockquote>
<p><strong>常见的 DOM 操作的优化方法</strong></p>
</blockquote>
<ul>
<li>最小化dom访问次数，尽可能在js端执行；</li>
<li>如果需要多次访问某个dom节点，请使用局部变量存储对它的引用；</li>
<li>小心处理html集合，因为它实时连系着底层的文档，把集合的长度缓存到一个变量中，并在迭代中使用它，如果需要经常操作集合，建议把它拷贝到一个数组中；</li>
<li>如果可能的话，使用速度更快的API，比如querySelectorAll和firstElementChild；</li>
<li>要留意重绘和重排；批量修改样式时，“离线”操作dom树；使用缓存，并减少访问布局的次数；</li>
<li>动画中使用绝对定位，使用拖放代理</li>
<li>使用事件委托来减少事件处理器的数量</li>
</ul>
<blockquote>
<p><strong>对数据访问的优化</strong></p>
</blockquote>
<ul>
<li>函数中读写局部变量总是最快的，而全局变量的读取则是最慢的；</li>
<li>尽可能地少用with 语句，因为它会增加with 语句以外的数据的访问代价；</li>
<li>闭包尽管强大，但不可滥用，否则会影响到执行速度以及内存；</li>
<li>嵌套的对象成员会明显影响性能，尽量少用；</li>
<li>避免多次访问对象成员或函数中的全局变量，尽量将它们赋值给局部变量以缓存。</li>
</ul>
<blockquote>
<p><strong>实践小技巧</strong></p>
</blockquote>
<ul>
<li>字符串判断使用正则表达式</li>
<li>数字转换成字符：(“” +) &gt; String() &gt; .toString() &gt; new String()</li>
<li>数组拼接字符串用join(),不要用+=。</li>
<li>浮点数转换成整型：使用Math.floor()或者Math.round()。而不是使用parseInt(),该方法用于将字符串转换成数字。</li>
<li>动画中使用绝对定位，使用拖放代理</li>
<li>使用事件委托来减少事件处理器的数量</li>
<li>switch 替代 if else</li>
<li>var a,b,c; 替代 var a;var b; var c;</li>
<li>xxx[i++]; 替代  xxx[i];i++;</li>
<li>i++; 替代  i=i+1;</li>
<li>i+=5; 替代 i=i+5;</li>
</ul>
<div class="text-center"><h3>移动 H5 性能优化指南</h3><img src="/styles/images/web/optimize_3.jpg" alt="H5 优化">
</div>

<hr>]]></content>
      <categories>
        <category>面试总结</category>
      </categories>
      <tags>
        <tag>Web</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 常用算法总结</title>
    <url>/2017/02/15/article-js-arithmetic/</url>
    <content><![CDATA[<p>　　算法通俗的讲：就是解决问题的方法和步骤。在计算机发明之前便已经存在。只不过在计算机发明后，其应用变得更为广泛。通过简单的算法，利用电脑的计算速度，可以让问题变得简单。在前端运算中，算法也是非常重要的。记不清是谁说过“现在的前端已经不是选择器和超链接就能应付的了“，说得一点都没错。在移动端和大数据飞速发展的今天，性能已经是所有工程师所必须重视的部分。作为用户交互的先锋队，前端工程师的技术含量也愈发增加，随着用户体验的不断发展，页面交互的自由度及性能使我们必须深入设计的部分。</p>
<p>　　作为前端工程师，我们的工作已经不仅仅是完成UI的静态设计稿，还需要设计页面的呈现顺序（惰性加载），功能模块化，设计交互（页面滑动，3D效果等），设计网页游戏等，这时候就需要我们对算法和设计模式的研究。<a id="more"></a></p>
<h2 id="1-99乘法表"><a href="#1-99乘法表" class="headerlink" title="1. 99乘法表"></a>1. 99乘法表</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">1</span>; i &lt;= <span class="number">9</span>; i++) &#123;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">var</span> n = <span class="number">1</span>; n &lt;= i; n++) &#123;</span><br><span class="line">		<span class="built_in">document</span>.write(n+<span class="string">'*'</span>+i+<span class="string">'='</span>+i*n+<span class="string">'\t'</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">document</span>.write(<span class="string">'&lt;br/&gt;'</span>)</span><br><span class="line">&#125;<span class="comment">//9*9乘法表</span></span><br></pre></td></tr></table></figure>

<h2 id="2-斐波那契数列"><a href="#2-斐波那契数列" class="headerlink" title="2. 斐波那契数列"></a>2. 斐波那契数列</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fib</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (n&gt;<span class="number">2</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> fib(n - <span class="number">2</span>) + fib(n - <span class="number">1</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">alert(fib(<span class="number">6</span>));<span class="comment">//递归</span></span><br><span class="line"><span class="comment">//生成斐波那契数列</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getFibonacci</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> fibarr = [];</span><br><span class="line">    <span class="keyword">var</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (i &lt; n) &#123;</span><br><span class="line">        <span class="keyword">if</span> (i &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">            fibarr.push(i);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            fibarr.push(fibarr[i - <span class="number">1</span>] + fibarr[i - <span class="number">2</span>])</span><br><span class="line">        &#125;</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> fibarr;</span><br><span class="line">&#125;</span><br><span class="line">alert(getFibonacci(<span class="number">6</span>))</span><br></pre></td></tr></table></figure>

<h2 id="3-回文字符串判断"><a href="#3-回文字符串判断" class="headerlink" title="3. 回文字符串判断"></a>3. 回文字符串判断</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">palindrome</span>(<span class="params">str</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 删除字符串中不必要的字符</span></span><br><span class="line">    <span class="keyword">var</span> re = <span class="regexp">/[W_]/g</span>;</span><br><span class="line">    <span class="comment">// 将字符串变成小写字符</span></span><br><span class="line">    <span class="keyword">var</span> lowRegStr = str.toLowerCase().replace(re, <span class="string">''</span>);</span><br><span class="line">    <span class="comment">// 如果字符串lowRegStr的length长度为0时，字符串即是palindrome</span></span><br><span class="line">    <span class="keyword">if</span> (lowRegStr.length === <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果字符串的第一个和最后一个字符不相同，那么字符串就不是palindrome</span></span><br><span class="line">    <span class="keyword">if</span> (lowRegStr[<span class="number">0</span>] !== lowRegStr[lowRegStr.length - <span class="number">1</span>]) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> palindrome(lowRegStr.slice(<span class="number">1</span>, lowRegStr.length - <span class="number">1</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">alert(palindrome(<span class="string">'asddfa'</span>))</span><br></pre></td></tr></table></figure>

<h2 id="4-获取任意长度的随机字符串"><a href="#4-获取任意长度的随机字符串" class="headerlink" title="4. 获取任意长度的随机字符串"></a>4. 获取任意长度的随机字符串</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> getRandomString = <span class="function"><span class="keyword">function</span>(<span class="params">n</span>)</span>&#123;</span><br><span class="line">    <span class="comment">// 定义随机字符串的字符库</span></span><br><span class="line">    <span class="keyword">var</span> str = <span class="string">'qwertyuiopasdfghjklzxcvbnm1234567890'</span>;</span><br><span class="line">    <span class="comment">// 定义一个临时变量tmp存储生成的随机字符串</span></span><br><span class="line">    <span class="keyword">var</span> tmp = <span class="string">''</span>;</span><br><span class="line">    <span class="comment">//获取str的长度</span></span><br><span class="line">    <span class="keyword">var</span> len = str.length;</span><br><span class="line">    <span class="comment">// 生成一个长度为n的随机字符串</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">        tmp += str.charAt(<span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * len));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> tmp;</span><br><span class="line">&#125;;</span><br><span class="line">alert(getRandomString(<span class="number">10</span>))</span><br></pre></td></tr></table></figure>

<h2 id="5-阶乘算法"><a href="#5-阶乘算法" class="headerlink" title="5. 阶乘算法"></a>5. 阶乘算法</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">factorialize</span>(<span class="params">num</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> result = num;</span><br><span class="line">    <span class="keyword">if</span> (num &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (num === <span class="number">0</span> || num === <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">while</span> (num &gt; <span class="number">1</span>) &#123;</span><br><span class="line">            num--;</span><br><span class="line">            result *= num;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line">alert(factorialize(<span class="number">5</span>));</span><br></pre></td></tr></table></figure>

<h2 id="6-数组排序"><a href="#6-数组排序" class="headerlink" title="6. 数组排序"></a>6. 数组排序</h2><h3 id="6-1-快速排序"><a href="#6-1-快速排序" class="headerlink" title="6.1 快速排序"></a>6.1 快速排序</h3><ul>
<li>在数据集之中，选择一个元素作为”基准”（pivot）。</li>
<li>所有小于”基准”的元素，都移到”基准”的左边；所有大于”基准”的元素，都移到”基准”的右边。</li>
<li>对”基准”左边和右边的两个子集，不断重复第一步和第二步，直到所有子集只剩下一个元素为止。</li>
</ul>
<blockquote>
<p>快排的时间复杂度是O(nlogn)，属于不稳定的排序!</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">quickSort</span>(<span class="params">arr</span>)</span>&#123;</span><br><span class="line">    <span class="comment">//如果数组&lt;=1,则直接返回</span></span><br><span class="line">    <span class="keyword">if</span>(arr.length&lt;=<span class="number">1</span>)&#123;<span class="keyword">return</span> arr;&#125;</span><br><span class="line">    <span class="keyword">var</span> pivotIndex=<span class="built_in">Math</span>.floor(arr.length/<span class="number">2</span>);</span><br><span class="line">    <span class="comment">//找基准，并把基准从原数组删除</span></span><br><span class="line">    <span class="keyword">var</span> pivot=arr.splice(pivotIndex,<span class="number">1</span>)[<span class="number">0</span>];</span><br><span class="line">    <span class="comment">//定义左右数组</span></span><br><span class="line">    <span class="keyword">var</span> left=[];</span><br><span class="line">    <span class="keyword">var</span> right=[];</span><br><span class="line"></span><br><span class="line">    <span class="comment">//比基准小的放在left，比基准大的放在right</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>;i&lt;arr.length;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(arr[i]&lt;=pivot)&#123;</span><br><span class="line">            left.push(arr[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            right.push(arr[i]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//递归</span></span><br><span class="line">    <span class="keyword">return</span> quickSort(left).concat([pivot],quickSort(right));</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> arr=[<span class="number">3</span>,<span class="number">1</span>,<span class="number">5</span>,<span class="number">2</span>,<span class="number">6</span>];</span><br><span class="line">alert(quickSort(arr))</span><br></pre></td></tr></table></figure>

<h3 id="6-2-冒泡排序"><a href="#6-2-冒泡排序" class="headerlink" title="6.2. 冒泡排序"></a>6.2. 冒泡排序</h3><p>　　冒泡排序分两种，一种小泡泡往上吐，一种大泡泡往上吐。也就是从小到大和从大到小。</p>
<blockquote>
<p>具体步骤:</p>
</blockquote>
<p>　　冒泡排序就是从最开始的位置或结尾的位置反方向对比，如果比它大/小,就交换然后继续走，第一遍走完,最后一个位置是最大值或者最小值。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bubbleSort</span>(<span class="params">arr</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">1</span>;i&lt;arr.length;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">var</span> j=<span class="number">0</span>;j&lt;arr.length-i;j++)&#123;</span><br><span class="line">            <span class="keyword">var</span> temp;</span><br><span class="line">            <span class="keyword">if</span>(arr[j]&gt;arr[j+<span class="number">1</span>])&#123;</span><br><span class="line">                temp=arr[j];</span><br><span class="line">                arr[j]=arr[j+<span class="number">1</span>];</span><br><span class="line">                arr[j+<span class="number">1</span>]=temp;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> arr=[<span class="number">2</span>,<span class="number">3</span>,<span class="number">1</span>,<span class="number">4</span>,<span class="number">11</span>,<span class="number">58</span>,<span class="number">45</span>];</span><br><span class="line">alert(bubbleSort(arr));</span><br><span class="line"></span><br><span class="line"><span class="comment">//冒泡排序逻辑</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> array=[<span class="number">1</span>,<span class="number">20</span>,<span class="number">4</span>,<span class="number">18</span>,<span class="number">7</span>,<span class="number">3</span>];</span><br><span class="line"><span class="keyword">var</span> temp=<span class="number">0</span>;</span><br><span class="line"><span class="built_in">document</span>.writeln(<span class="string">"排序前:"</span>+array+<span class="string">"&lt;/br&gt;"</span>);</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>;i&lt;array.length<span class="number">-1</span>;i++)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> j=i+<span class="number">1</span>;j&lt;array.length;j++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(array[i]&gt;array[j])</span><br><span class="line">        &#123;</span><br><span class="line">            temp=array[i];</span><br><span class="line">            array[i]=array[j];</span><br><span class="line">            array[j]=temp;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> m=i+<span class="number">1</span>;</span><br><span class="line">    <span class="built_in">document</span>.write(<span class="string">"第"</span>+m+<span class="string">"次排序:"</span>+array+<span class="string">"&lt;/br&gt;"</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">document</span>.writeln(<span class="string">"排序后:"</span>+array+<span class="string">"&lt;/br&gt;"</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">document</span>.write(arr);</span><br></pre></td></tr></table></figure>

<h3 id="6-3-插入排序"><a href="#6-3-插入排序" class="headerlink" title="6.3 插入排序"></a>6.3 插入排序</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//插入排序</span></span><br><span class="line"><span class="built_in">Array</span>.prototype.insertSort=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">   <span class="keyword">var</span> len = <span class="keyword">this</span>.length,</span><br><span class="line">        i, j, tmp;</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">1</span>; i&lt;len; i++)&#123;</span><br><span class="line">        tmp = <span class="keyword">this</span>[i];</span><br><span class="line">        j = i - <span class="number">1</span>;</span><br><span class="line">	<span class="comment">//初始时数组索引值为1的与为0的比较，如果索引值为1的小于则将其值换为索引值为0的</span></span><br><span class="line">        <span class="keyword">while</span>(j&gt;=<span class="number">0</span> &amp;&amp; tmp &lt; <span class="keyword">this</span>[j])&#123;</span><br><span class="line">            <span class="keyword">this</span>[j+<span class="number">1</span>] = <span class="keyword">this</span>[j];</span><br><span class="line">            j--;</span><br><span class="line">        &#125;</span><br><span class="line">	<span class="comment">//随着j--循环的次，则找到tmp的前面有序区应该的位置。</span></span><br><span class="line">        <span class="keyword">this</span>[j+<span class="number">1</span>] = tmp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>关于更多的排序知识，可移步 <a href="https://zhuanlan.zhihu.com/p/25379917" target="_blank" rel="noopener">这里</a>。</p>
</blockquote>
<h2 id="7-求一个数组的最大差值"><a href="#7-求一个数组的最大差值" class="headerlink" title="7. 求一个数组的最大差值"></a>7. 求一个数组的最大差值</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> getMaxProfit = <span class="function"><span class="keyword">function</span>(<span class="params">arr</span>)</span>&#123;</span><br><span class="line">    <span class="comment">// 定义两个变量，分别存贮最大值和最小值</span></span><br><span class="line">    <span class="keyword">var</span> maxNum = arr[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">var</span> minNum = arr[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; arr.length; i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(arr[i] &gt; maxNum)&#123;</span><br><span class="line">            maxNum = arr[i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(arr[i] &lt; minNum)&#123;</span><br><span class="line">            minNum = arr[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> maxNum - minNum;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">var</span> arr=[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>];</span><br><span class="line">alert(getMaxProfit(arr))</span><br></pre></td></tr></table></figure>

<h2 id="8-数组的偶数位删除"><a href="#8-数组的偶数位删除" class="headerlink" title="8. 数组的偶数位删除"></a>8. 数组的偶数位删除</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr=[];</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">1</span>;i&lt;=<span class="number">100</span>;i++)&#123;</span><br><span class="line">    arr.push(i);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">do</span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> a=arr.length;a&gt;<span class="number">0</span>;a--)&#123;</span><br><span class="line">        <span class="keyword">if</span>(a%<span class="number">2</span>==<span class="number">1</span>)&#123;</span><br><span class="line">            arr.splice(a,<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="keyword">while</span> (arr.length&gt;<span class="number">2</span>);</span><br><span class="line">alert(arr);</span><br></pre></td></tr></table></figure>

<h2 id="9-数组去重"><a href="#9-数组去重" class="headerlink" title="9. 数组去重"></a>9. 数组去重</h2><h3 id="9-1-利用-key-去重"><a href="#9-1-利用-key-去重" class="headerlink" title="9.1 利用 key 去重"></a>9.1 利用 key 去重</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">unique</span>(<span class="params">arr</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> hashTable = &#123;&#125;;</span><br><span class="line">    <span class="keyword">var</span> data = [];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">0</span>, l = arr.length; i &lt; l; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span>(!hashTable[arr[i]]) &#123;</span><br><span class="line">            hashTable[arr[i]] = <span class="literal">true</span>;</span><br><span class="line">            data.push(arr[i]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> data;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> arr=[<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">6</span>,<span class="number">1</span>,<span class="number">6</span>];</span><br><span class="line">alert(unique(arr))</span><br></pre></td></tr></table></figure>

<h3 id="9-2-逻辑去重"><a href="#9-2-逻辑去重" class="headerlink" title="9.2 逻辑去重"></a>9.2 逻辑去重</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr=[<span class="number">21</span>,<span class="number">32</span>,<span class="number">45</span>,<span class="number">32</span>,<span class="number">21</span>,<span class="number">56</span>,<span class="number">45</span>];</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> temp=[];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>;i&lt;arr.length;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(temp.indexOf(arr[i])==<span class="number">-1</span>)&#123;</span><br><span class="line">            temp.push(arr[i]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> temp;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> a=fn(arr);<span class="comment">//接收返回值</span></span><br><span class="line"><span class="built_in">document</span>.write(a);</span><br></pre></td></tr></table></figure>

<h3 id="9-3-传统-key-方法"><a href="#9-3-传统-key-方法" class="headerlink" title="9.3 传统 key 方法"></a>9.3 传统 key 方法</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr=[<span class="number">2</span>,<span class="number">3</span>,<span class="number">1</span>,<span class="number">4</span>,<span class="number">2</span>,<span class="number">1</span>,<span class="number">4</span>,<span class="number">12</span>,<span class="number">12</span>,<span class="number">3</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">5</span>];</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">toObject</span>(<span class="params">arr</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> obj=&#123;&#125;;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>;i&lt;arr.length;i++)&#123;</span><br><span class="line">        obj[arr[i]]=<span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">toArr</span>(<span class="params">obj</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> arr=[];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> attr <span class="keyword">in</span> obj)&#123;</span><br><span class="line">        arr.push(attr);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">newArr</span>(<span class="params">Arr</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> toArr(toObject(Arr))</span><br><span class="line">&#125;</span><br><span class="line">alert(newArr(arr));</span><br></pre></td></tr></table></figure>

<h2 id="10-水仙花数及四叶玫瑰数"><a href="#10-水仙花数及四叶玫瑰数" class="headerlink" title="10. 水仙花数及四叶玫瑰数"></a>10. 水仙花数及四叶玫瑰数</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">100</span>; i &lt;= <span class="number">999</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">var</span> g, s, b;</span><br><span class="line">    g = i % <span class="number">10</span>;</span><br><span class="line">    s = <span class="built_in">parseInt</span>(i / <span class="number">10</span>) % <span class="number">10</span>;</span><br><span class="line">    b = <span class="built_in">parseInt</span>(i / <span class="number">100</span>);</span><br><span class="line">    <span class="keyword">var</span> sum1 = g * g * g + s * s * s + b * b * b;</span><br><span class="line">    <span class="keyword">if</span>(i == sum1) &#123;</span><br><span class="line">        <span class="built_in">document</span>.write(i + <span class="string">'&lt;br/&gt;'</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">&#125; <span class="comment">//水仙花数</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">1000</span>; i &lt;= <span class="number">9999</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">var</span> g, s, b, q;</span><br><span class="line">    g = i % <span class="number">10</span>;</span><br><span class="line">    s = <span class="built_in">parseInt</span>(i / <span class="number">10</span>) % <span class="number">10</span>;</span><br><span class="line">    b = <span class="built_in">parseInt</span>(i / <span class="number">100</span>) % <span class="number">10</span>;</span><br><span class="line">    q = <span class="built_in">parseInt</span>(i / <span class="number">1000</span>);</span><br><span class="line">    <span class="keyword">var</span> sum2 = g * g * g * g + s * s * s * s + b * b * b * b + q * q * q * q;</span><br><span class="line">    <span class="keyword">if</span>(i == sum2) &#123;</span><br><span class="line">        <span class="built_in">document</span>.write(i + <span class="string">'&lt;br/&gt;'</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">&#125; <span class="comment">//四叶玫瑰数</span></span><br></pre></td></tr></table></figure>

<h2 id="11-交换两个值为数字的两个变量的值"><a href="#11-交换两个值为数字的两个变量的值" class="headerlink" title="11. 交换两个值为数字的两个变量的值"></a>11. 交换两个值为数字的两个变量的值</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> swap = <span class="function"><span class="keyword">function</span>(<span class="params">a,b</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(a === b)&#123;</span><br><span class="line">        <span class="keyword">return</span> [a,b];</span><br><span class="line">    &#125;</span><br><span class="line">    b = b - a; <span class="comment">// 此处的 b - a中的b和a的值是最初的值</span></span><br><span class="line">    a = a + b; <span class="comment">// a = a + b -a; 实现了将b的值赋给a</span></span><br><span class="line">    b = a - b; <span class="comment">// b = a - (b - a) = 2a - b 相当于 2b = 2a;实现了将a的值赋给b</span></span><br><span class="line">    <span class="keyword">return</span> [a,b];</span><br><span class="line">&#125;;</span><br><span class="line">alert(swap(<span class="number">3</span>,<span class="number">6</span>))</span><br></pre></td></tr></table></figure>

<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>原生 JavaScript 实现 AJAX、JSONP</title>
    <url>/2017/02/14/article-js-ajax-jsonp/</url>
    <content><![CDATA[<p>　　<strong>本文转自 <a href="http://ghmagical.com/" target="_blank" rel="noopener">TG</a> 的 <a href="http://ghmagical.com/article/page/id/AASiankfBJWp" target="_blank" rel="noopener">原生 JavaScript 实现 AJAX、JSONP</a>。</strong></p>
<p>　　相信大多数前端开发者在需要与后端进行数据交互时，为了方便快捷，都会选择<code>JQuery</code>中封装的AJAX方法，但是有些时候，我们只需要<code>JQuery</code>的<code>AJAX</code>请求方法，而其他的功能用到的很少，这显然是没必要的。</p>
<p>　　其实，原生<code>JavaScript</code>实现<code>AJAX</code>并不难，这篇文章将会讲解如何实现简单的<code>AJAX</code>，还有跨域请求<code>JSONP</code>！<a id="more"></a></p>
<h2 id="1-AJAX"><a href="#1-AJAX" class="headerlink" title="1. AJAX"></a>1. AJAX</h2><p>一个完整的AJAX请求一般包括以下步骤：</p>
<ul>
<li>实例化XMLHttpRequest对象</li>
<li>连接服务器</li>
<li>发送请求</li>
<li>接收响应数据</li>
</ul>
<p>我将<code>AJAX</code>请求封装成<code>ajax()</code>方法，它接受一个配置对象<code>params</code>。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ajax</span>(<span class="params">params</span>) </span>&#123;</span><br><span class="line">    params = params || &#123;&#125;;</span><br><span class="line">    params.data = params.data || &#123;&#125;;</span><br><span class="line">    <span class="comment">// 判断是ajax请求还是jsonp请求</span></span><br><span class="line">    <span class="keyword">var</span> json = params.jsonp ? jsonp(params) : json(params);</span><br><span class="line">    <span class="comment">// ajax请求   </span></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">json</span>(<span class="params">params</span>) </span>&#123;</span><br><span class="line">            <span class="comment">//  请求方式，默认是GET</span></span><br><span class="line">            params.type = (params.type || <span class="string">'GET'</span>).toUpperCase();</span><br><span class="line">            <span class="comment">// 避免有特殊字符，必须格式化传输数据  </span></span><br><span class="line">            params.data = formatParams(params.data);</span><br><span class="line">            <span class="keyword">var</span> xhr = <span class="literal">null</span>;</span><br><span class="line">            <span class="comment">// 实例化XMLHttpRequest对象   </span></span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">window</span>.XMLHttpRequest) &#123;</span><br><span class="line">                xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// IE6及其以下版本   </span></span><br><span class="line">                xhr = <span class="keyword">new</span> ActiveXObjcet(<span class="string">'Microsoft.XMLHTTP'</span>);</span><br><span class="line">            &#125;;</span><br><span class="line">            <span class="comment">// 监听事件，只要 readyState 的值变化，就会调用 readystatechange 事件 </span></span><br><span class="line">            xhr.onreadystatechange = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                <span class="comment">//  readyState属性表示请求/响应过程的当前活动阶段，4为完成，已经接收到全部响应数据</span></span><br><span class="line">                <span class="keyword">if</span> (xhr.readyState == <span class="number">4</span>) &#123;</span><br><span class="line">                    <span class="keyword">var</span> status = xhr.status;</span><br><span class="line">                    <span class="comment">//  status：响应的HTTP状态码，以2开头的都是成功</span></span><br><span class="line">                    <span class="keyword">if</span> (status &gt;= <span class="number">200</span> &amp;&amp; status &lt; <span class="number">300</span>) &#123;</span><br><span class="line">                        <span class="keyword">var</span> response = <span class="string">''</span>;</span><br><span class="line">                        <span class="comment">// 判断接受数据的内容类型  </span></span><br><span class="line">                        <span class="keyword">var</span> type = xhr.getResponseHeader(<span class="string">'Content-type'</span>);</span><br><span class="line">                        <span class="keyword">if</span> (type.indexOf(<span class="string">'xml'</span>) !== <span class="number">-1</span> &amp;&amp; xhr.responseXML) &#123;</span><br><span class="line">                            response = xhr.responseXML; <span class="comment">//Document对象响应   </span></span><br><span class="line">                        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (type === <span class="string">'application/json'</span>) &#123;</span><br><span class="line">                            response = <span class="built_in">JSON</span>.parse(xhr.responseText); <span class="comment">//JSON响应   </span></span><br><span class="line">                        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                            response = xhr.responseText; <span class="comment">//字符串响应   </span></span><br><span class="line">                        &#125;;</span><br><span class="line">                        <span class="comment">// 成功回调函数 </span></span><br><span class="line">                        params.success &amp;&amp; params.success(response);</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        params.error &amp;&amp; params.error(status);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;;</span><br><span class="line">            &#125;;</span><br><span class="line">            <span class="comment">// 连接和传输数据   </span></span><br><span class="line">            <span class="keyword">if</span> (params.type == <span class="string">'GET'</span>) &#123;</span><br><span class="line">                <span class="comment">// 三个参数：请求方式、请求地址(get方式时，传输数据是加在地址后的)、是否异步请求(同步请求的情况极少)；</span></span><br><span class="line">                xhr.open(params.type, params.url + <span class="string">'?'</span> + params.data, <span class="literal">true</span>);</span><br><span class="line">                xhr.send(<span class="literal">null</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                xhr.open(params.type, params.url, <span class="literal">true</span>);</span><br><span class="line">                <span class="comment">//必须，设置提交时的内容类型   </span></span><br><span class="line">                xhr.setRequestHeader(<span class="string">'Content-Type'</span>, <span class="string">'application/x-www-form-urlencoded; charset=UTF-8'</span>);</span><br><span class="line">                <span class="comment">// 传输数据  </span></span><br><span class="line">                xhr.send(params.data);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//格式化参数   </span></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">formatParams</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">var</span> arr = [];</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">var</span> name <span class="keyword">in</span> data) &#123;</span><br><span class="line">                <span class="comment">//   encodeURIComponent() ：用于对 URI 中的某一部分进行编码</span></span><br><span class="line">                arr.push(<span class="built_in">encodeURIComponent</span>(name) + <span class="string">'='</span> + <span class="built_in">encodeURIComponent</span>(data[name]));</span><br><span class="line">            &#125;;</span><br><span class="line">            <span class="comment">// 添加一个随机数参数，防止缓存   </span></span><br><span class="line">            arr.push(<span class="string">'v='</span> + random());</span><br><span class="line">            <span class="keyword">return</span> arr.join(<span class="string">'&amp;'</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 获取随机数   </span></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">random</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * <span class="number">10000</span> + <span class="number">500</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　<em>在上面的代码中，已经添加具体的注释，如需了解更多<code>AJAX</code>，可查看 <a href="http://ghmagical.com/" target="_blank" rel="noopener">TG</a> 的书籍《<a href="http://www.kancloud.cn/dennis/tgjavascript" target="_blank" rel="noopener">JavaScript半知半解</a>》中的<code>AJAX</code>章节：<a href="http://www.kancloud.cn/dennis/tgjavascript/241825" target="_blank" rel="noopener">AJAX</a>。</em></p>
<p>使用实例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">ajax(&#123;   </span><br><span class="line">    url: <span class="string">'test.php'</span>,   <span class="comment">// 请求地址</span></span><br><span class="line">    type: <span class="string">'POST'</span>,   <span class="comment">// 请求类型，默认"GET"，还可以是"POST"</span></span><br><span class="line">    data: &#123;<span class="string">'b'</span>: <span class="string">'异步请求'</span>&#125;,   <span class="comment">// 传输数据</span></span><br><span class="line">    success: <span class="function"><span class="keyword">function</span>(<span class="params">res</span>)</span>&#123;   <span class="comment">// 请求成功的回调函数</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">JSON</span>.parse(res));   </span><br><span class="line">  &#125;,</span><br><span class="line">    error: <span class="function"><span class="keyword">function</span>(<span class="params">error</span>) </span>&#123;&#125;   <span class="comment">// 请求失败的回调函数</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="2-JSONP"><a href="#2-JSONP" class="headerlink" title="2. JSONP"></a>2. JSONP</h2><blockquote>
<p><strong>同源策略</strong></p>
</blockquote>
<p>　　<code>AJAX</code>之所以需要“跨域”，罪魁祸首就是浏览器的同源策略。即，一个页面的<code>AJAX</code>只能获取这个页面相同源或者相同域的数据。 如何叫“同源”或者“同域”呢？——_协议、域名、端口号都必须相同_。例如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://example.com 和 https://example.com 不同，因为协议不同；   </span><br><span class="line">http://localhost:8080 和 http://localhost:1000 不同，因为端口不同；   </span><br><span class="line">http://localhost:8080 和 https://example.com 不同，协议、域名、端口号都不同，根本不是一家的。</span><br></pre></td></tr></table></figure>

<p>　　当跨域请求时，一般都会看到这个错误：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">XMLHttpRequest cannot load http://ghmagical.com/article/?intro=jsonp%E8%AF%B7%E6%B1%82&amp;v=5520. No &apos;Access-Control-Allow-Origin&apos; header is present on the requested resource. Origin &apos;http://localhost&apos; is therefore not allowed access.</span><br></pre></td></tr></table></figure>

<p>　　那如何跨域请求呢？这时，<strong>JSONP</strong>就登场了！</p>
<p>　　<strong>JSONP(JSON with Padding)</strong> 是一种跨域请求方式。主要原理是利用了<code>script</code> 标签可以跨域请求的特性，由其 <code>src</code> 属性发送请求到服务器，服务器返回 <code>JavaScript</code> 代码，浏览器接受响应，然后就直接执行了，这和通过 <code>script</code> 标签引用外部文件的原理是一样的。</p>
<p>　　<strong>JSONP</strong>由两部分组成：回调函数和数据，回调函数一般是在浏览器控制，作为参数发往服务器端（当然，你也可以固定回调函数的名字，但客户端和服务器端的名称一定要一致）。当服务器响应时，服务器端就会把该函数和数据拼成字符串返回。<br>　　JSONP的请求过程：</p>
<ul>
<li>请求阶段：浏览器创建一个 script 标签，并给其src 赋值(类似 <a href="http://example.com/api/?callback=jsonpCallback）。" target="_blank" rel="noopener">http://example.com/api/?callback=jsonpCallback）。</a></li>
<li>发送请求：当给<code>script</code>的<code>src</code>赋值时，浏览器就会发起一个请求。</li>
<li>数据响应：服务端将要返回的数据作为参数和函数名称拼接在一起(格式类似”<code>jsonpCallback({name: &#39;abc&#39;})</code>”)返回。当浏览器接收到了响应数据，由于发起请求的是 <code>script</code>，所以相当于直接调用 <code>jsonpCallback</code> 方法，并且传入了一个参数。</li>
</ul>
<blockquote>
<p><strong>用原生JavaScript如何实现</strong></p>
</blockquote>
<p>　　依旧是<code>ajax()</code>方法里添加<code>JSONP</code>，后面会将两者整合在一起，<code>JSONP</code>的配置参数主要多了一个<code>jsonp</code>参数，它就是你的回调函数名。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ajax</span>(<span class="params">params</span>) </span>&#123;</span><br><span class="line">    params = params || &#123;&#125;;</span><br><span class="line">    params.data = params.data || &#123;&#125;;</span><br><span class="line">    <span class="keyword">var</span> json = params.jsonp ? jsonp(params) : json(params);</span><br><span class="line">    <span class="comment">// jsonp请求   </span></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">jsonp</span>(<span class="params">params</span>) </span>&#123;</span><br><span class="line">        <span class="comment">//创建script标签并加入到页面中   </span></span><br><span class="line">        <span class="keyword">var</span> callbackName = params.jsonp;</span><br><span class="line">        <span class="keyword">var</span> head = <span class="built_in">document</span>.getElementsByTagName(<span class="string">'head'</span>)[<span class="number">0</span>];</span><br><span class="line">        <span class="comment">// 设置传递给后台的回调参数名   </span></span><br><span class="line">        params.data[<span class="string">'callback'</span>] = callbackName;</span><br><span class="line">        <span class="keyword">var</span> data = formatParams(params.data);</span><br><span class="line">        <span class="keyword">var</span> script = <span class="built_in">document</span>.createElement(<span class="string">'script'</span>);</span><br><span class="line">        head.appendChild(script);</span><br><span class="line">        <span class="comment">//创建jsonp回调函数   </span></span><br><span class="line">        <span class="built_in">window</span>[callbackName] = <span class="function"><span class="keyword">function</span>(<span class="params">json</span>) </span>&#123;</span><br><span class="line">            head.removeChild(script);</span><br><span class="line">            clearTimeout(script.timer);</span><br><span class="line">            <span class="built_in">window</span>[callbackName] = <span class="literal">null</span>;</span><br><span class="line">            params.success &amp;&amp; params.success(json);</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="comment">//发送请求   </span></span><br><span class="line">        script.src = params.url + <span class="string">'?'</span> + data;</span><br><span class="line">        <span class="comment">//为了得知此次请求是否成功，设置超时处理   </span></span><br><span class="line">        <span class="keyword">if</span> (params.time) &#123;</span><br><span class="line">            script.timer = setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                <span class="built_in">window</span>[callbackName] = <span class="literal">null</span>;</span><br><span class="line">                head.removeChild(script);</span><br><span class="line">                params.error &amp;&amp; params.error(&#123;</span><br><span class="line">                    message: <span class="string">'超时'</span></span><br><span class="line">                &#125;);</span><br><span class="line">            &#125;, time);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">//格式化参数   </span></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">formatParams</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> arr = [];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> name <span class="keyword">in</span> data) &#123;</span><br><span class="line">            arr.push(<span class="built_in">encodeURIComponent</span>(name) + <span class="string">'='</span> + <span class="built_in">encodeURIComponent</span>(data[name]));</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="comment">// 添加一个随机数，防止缓存   </span></span><br><span class="line">        arr.push(<span class="string">'v='</span> + random());</span><br><span class="line">        <span class="keyword">return</span> arr.join(<span class="string">'&amp;'</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 获取随机数   </span></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">random</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * <span class="number">10000</span> + <span class="number">500</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><code>注意：</code></strong>因为 script 标签的 src 属性只在第一次设置的时候起作用，导致 script 标签没法重用，所以每次完成操作之后要移除；</p>
<p>使用实例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">ajax(&#123;   </span><br><span class="line"></span><br><span class="line">  url: <span class="string">'test.php'</span>,    <span class="comment">// 请求地址</span></span><br><span class="line"></span><br><span class="line">  jsonp: <span class="string">'jsonpCallback'</span>,  <span class="comment">// 采用jsonp请求，且回调函数名为"jsonpCallbak"，可以设置为合法的字符串</span></span><br><span class="line"></span><br><span class="line">  data: &#123;<span class="string">'b'</span>: <span class="string">'异步请求'</span>&#125;,   <span class="comment">// 传输数据</span></span><br><span class="line"></span><br><span class="line">  success:<span class="function"><span class="keyword">function</span>(<span class="params">res</span>)</span>&#123;   <span class="comment">// 请求成功的回调函数</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">console</span>.log(res);   </span><br><span class="line"></span><br><span class="line">  &#125;,</span><br><span class="line"></span><br><span class="line">  error: <span class="function"><span class="keyword">function</span>(<span class="params">error</span>) </span>&#123;&#125;   <span class="comment">// 请求失败的回调函数</span></span><br><span class="line"></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p><strong><code>注意：</code></strong>别漏了用函数名与数据拼接返回。</p>
<p>当然，前面也说过，你可以给定固定回调函数名：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">jsonpCallback</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line"><span class="meta">&lt;?php</span> </span><br><span class="line">  <span class="keyword">echo</span> <span class="string">'jsonpCallback('</span>.$data.<span class="string">')'</span>;</span><br></pre></td></tr></table></figure>

<p>最后我已经将<code>AJAX</code>和<code>JSONP</code>请求合并在一起了，下载链接：<a href="/download/ajax.zip">ajax.zip</a>。</p>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>Ajax</tag>
        <tag>jsonp</tag>
      </tags>
  </entry>
  <entry>
    <title>webpack 入门指南</title>
    <url>/2017/02/12/article-webpack-know/</url>
    <content><![CDATA[<blockquote>
<p><strong>本文转自博客园的 <a href="http://www.cnblogs.com/chris-oil/p/5681225.html" target="_blank" rel="noopener">一小时包教会 —— webpack 入门指南</a>。</strong></p>
</blockquote>
<blockquote>
<p><strong>有些知识点已过时，请参考结合 <a href="/2017/12/19/article-webpack-3-10-0/">webpack 3 入门指南</a></strong></p>
</blockquote>
<p>　　webpack 是近期最火的一款模块加载器兼打包工具，它能把各种资源，例如 JS（含JSX）、coffee、样式（含less/sass）、图片等都作为模块来使用和处理。</p>
<p>　　你可以不打算将其用在你的项目上，但没有理由不去掌握它，因为以近期 Github 上各大主流的（React相关）项目来说，它们仓库上所展示的示例已经是基于 webpack 来开发的，比如 React-Boostrap 和 Redux。<a id="more"></a></p>
<p>　　我们可以直接使用 require(XXX) 的形式来引入各模块，即使它们可能需要经过编译（比如JSX和sass），但我们无须在上面花费太多心思，因为 webpack 有着各种健全的加载器（loader）在默默处理这些事情，这块我们后续会提到。<br>s<br>　　想对其进行更详细了解的可以点 <a href="http://webpack.github.io/" target="_blank" rel="noopener">webpack的官网</a> 及 <a href="http://webpack.github.io/docs/" target="_blank" rel="noopener">文档地址</a> 进去瞧一瞧。</p>
<blockquote>
<p>webpack 的优势主要可以归类为如下几个：</p>
</blockquote>
<ol>
<li><p>webpack 是以 commonJS 的形式来书写脚本滴，但对 AMD/CMD 的支持也很全面，方便旧项目进行代码迁移。</p>
</li>
<li><p>能被模块化的不仅仅是 JS 了。</p>
</li>
<li><p>开发便捷，能替代部分 grunt/gulp 的工作，比如打包、压缩混淆、图片转base64等。</p>
</li>
<li><p>扩展性强，插件机制完善，特别是支持 React 热插拔（见 react-hot-loader ）的功能让人眼前一亮。</p>
</li>
</ol>
<p>　　我们谈谈第一点。以 AMD/CMD 模式来说，鉴于模块是异步加载的，所以我们常规需要使用 define 函数来帮我们搞回调：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">define([<span class="string">'package/lib'</span>], <span class="function"><span class="keyword">function</span>(<span class="params">lib</span>)</span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        lib.log(<span class="string">'hello world!'</span>);</span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        foo: foo</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>　　另外为了可以兼容 commonJS 的写法，我们也可以将 define 这么写：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">define(<span class="function"><span class="keyword">function</span> (<span class="params">require, exports, module</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> someModule = <span class="built_in">require</span>(<span class="string">"someModule"</span>);</span><br><span class="line">    <span class="keyword">var</span> anotherModule = <span class="built_in">require</span>(<span class="string">"anotherModule"</span>);    </span><br><span class="line"> </span><br><span class="line">    someModule.doTehAwesome();</span><br><span class="line">    anotherModule.doMoarAwesome();</span><br><span class="line"> </span><br><span class="line">    exports.asplode = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">        someModule.doTehAwesome();</span><br><span class="line">        anotherModule.doMoarAwesome();</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>　　然而对 webpack 来说，我们可以直接在上面书写 commonJS 形式的语法，无须任何 define （毕竟最终模块都打包在一起，webpack 也会最终自动加上自己的加载器）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> someModule = <span class="built_in">require</span>(<span class="string">"someModule"</span>);</span><br><span class="line">    <span class="keyword">var</span> anotherModule = <span class="built_in">require</span>(<span class="string">"anotherModule"</span>);    </span><br><span class="line"> </span><br><span class="line">    someModule.doTehAwesome();</span><br><span class="line">    anotherModule.doMoarAwesome();</span><br><span class="line"> </span><br><span class="line">    exports.asplode = <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">        someModule.doTehAwesome();</span><br><span class="line">        anotherModule.doMoarAwesome();</span><br><span class="line"> &#125;;</span><br></pre></td></tr></table></figure>

<p>　　这样撸码自然更简单，跟回调神马的说 byebye～～不过即使你保留了之前 define 的写法也是可以滴，毕竟 webpack 的兼容性相当出色，方便你旧项目的模块直接迁移过来。</p>
<h2 id="1-安装和配置"><a href="#1-安装和配置" class="headerlink" title="1. 安装和配置"></a>1. 安装和配置</h2><h3 id="1-1-安装"><a href="#1-1-安装" class="headerlink" title="1.1 安装"></a>1.1 安装</h3><p>　　我们常规直接使用 npm 的形式来安装：</p>
<pre><code>$ npm install webpack -g</code></pre><p>　　当然如果常规项目还是把依赖写入 package.json 包去更人性化：</p>
<pre><code>$ npm init
$ npm install webpack --save-dev</code></pre><h3 id="1-2-配置"><a href="#1-2-配置" class="headerlink" title="1.2 配置"></a>1.2 配置</h3><p>　　每个项目下都必须配置有一个 webpack.config.js ，它的作用如同常规的 gulpfile.js/Gruntfile.js ，就是一个配置项，告诉 webpack 它需要做什么。</p>
<p>　　我们看看下方的示例：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> webpack = <span class="built_in">require</span>(<span class="string">'webpack'</span>);</span><br><span class="line"><span class="keyword">var</span> commonsPlugin = <span class="keyword">new</span> webpack.optimize.CommonsChunkPlugin(<span class="string">'common.js'</span>);</span><br><span class="line"> </span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">    <span class="comment">//插件项</span></span><br><span class="line">    plugins: [commonsPlugin],</span><br><span class="line">    <span class="comment">//页面入口文件配置</span></span><br><span class="line">    entry: &#123;</span><br><span class="line">        index : <span class="string">'./src/js/page/index.js'</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">//入口文件输出配置</span></span><br><span class="line">    output: &#123;</span><br><span class="line">        path: <span class="string">'dist/js/page'</span>,</span><br><span class="line">        filename: <span class="string">'[name].js'</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="built_in">module</span>: &#123;</span><br><span class="line">        <span class="comment">//加载器配置</span></span><br><span class="line">        loaders: [</span><br><span class="line">            &#123; <span class="attr">test</span>: <span class="regexp">/\.css$/</span>, <span class="attr">loader</span>: <span class="string">'style-loader!css-loader'</span> &#125;,</span><br><span class="line">            &#123; <span class="attr">test</span>: <span class="regexp">/\.js$/</span>, <span class="attr">loader</span>: <span class="string">'jsx-loader?harmony'</span> &#125;,</span><br><span class="line">            &#123; <span class="attr">test</span>: <span class="regexp">/\.scss$/</span>, <span class="attr">loader</span>: <span class="string">'style!css!sass?sourceMap'</span>&#125;,</span><br><span class="line">            &#123; <span class="attr">test</span>: <span class="regexp">/\.(png|jpg)$/</span>, <span class="attr">loader</span>: <span class="string">'url-loader?limit=8192'</span>&#125;</span><br><span class="line">        ]</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">//其它解决方案配置</span></span><br><span class="line">    resolve: &#123;</span><br><span class="line">        root: <span class="string">'E:/github/flux-example/src'</span>, <span class="comment">//绝对路径</span></span><br><span class="line">        extensions: [<span class="string">''</span>, <span class="string">'.js'</span>, <span class="string">'.json'</span>, <span class="string">'.scss'</span>],</span><br><span class="line">        alias: &#123;</span><br><span class="line">            AppStore : <span class="string">'js/stores/AppStores.js'</span>,</span><br><span class="line">            ActionType : <span class="string">'js/actions/ActionType.js'</span>,</span><br><span class="line">            AppAction : <span class="string">'js/actions/AppAction.js'</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>plugins 是插件项，这里我们使用了一个 CommonsChunkPlugin 的插件，它用于提取多个入口文件的公共脚本部分，然后生成一个 common.js 来方便多页面之间进行复用。</p>
</li>
<li><p>entry 是页面入口文件配置，output 是对应输出项配置（即入口文件最终要生成什么名字的文件、存放到哪里），其语法大致为：</p>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    entry: &#123;</span><br><span class="line">        page1: <span class="string">"./page1"</span>,</span><br><span class="line">        <span class="comment">//支持数组形式，将加载数组中的所有模块，但以最后一个模块作为输出</span></span><br><span class="line">        page2: [<span class="string">"./entry1"</span>, <span class="string">"./entry2"</span>]</span><br><span class="line">    &#125;,</span><br><span class="line">    output: &#123;</span><br><span class="line">        path: <span class="string">"dist/js/page"</span>,</span><br><span class="line">        filename: <span class="string">"[name].bundle.js"</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　该段代码最终会生成一个 page1.bundle.js 和 page2.bundle.js，并存放到 ./dist/js/page 文件夹下。</p>
<ul>
<li>module.loaders 是最关键的一块配置。它告知 webpack 每一种文件都需要使用什么加载器来处理：</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line">        <span class="comment">//加载器配置</span></span><br><span class="line">        loaders: [</span><br><span class="line">            <span class="comment">//.css 文件使用 style-loader 和 css-loader 来处理</span></span><br><span class="line">            &#123; <span class="attr">test</span>: <span class="regexp">/\.css$/</span>, <span class="attr">loader</span>: <span class="string">'style-loader!css-loader'</span> &#125;,</span><br><span class="line">            <span class="comment">//.js 文件使用 jsx-loader 来编译处理</span></span><br><span class="line">            &#123; <span class="attr">test</span>: <span class="regexp">/\.js$/</span>, <span class="attr">loader</span>: <span class="string">'jsx-loader?harmony'</span> &#125;,</span><br><span class="line">            <span class="comment">//.scss 文件使用 style-loader、css-loader 和 sass-loader 来编译处理</span></span><br><span class="line">            &#123; <span class="attr">test</span>: <span class="regexp">/\.scss$/</span>, <span class="attr">loader</span>: <span class="string">'style!css!sass?sourceMap'</span>&#125;,</span><br><span class="line">            <span class="comment">//图片文件使用 url-loader 来处理，小于8kb的直接转为base64</span></span><br><span class="line">            &#123; <span class="attr">test</span>: <span class="regexp">/\.(png|jpg)$/</span>, <span class="attr">loader</span>: <span class="string">'url-loader?limit=8192'</span>&#125;</span><br><span class="line">        ]</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>　　如上，”-loader”其实是可以省略不写的，多个loader之间用“!”连接起来。</p>
<p>　　注意所有的加载器都需要通过 npm 来加载，并建议查阅它们对应的 readme 来看看如何使用。</p>
<p>　　拿最后一个 <a href="https://github.com/webpack-contrib/url-loader" target="_blank" rel="noopener">url-loader</a> 来说，它会将样式中引用到的图片转为模块来处理，使用该加载器需要先进行安装：</p>
<blockquote>
<p>npm install url-loader -save-dev</p>
</blockquote>
<p>　　配置信息的参数“?limit=8192”表示将所有小于8kb的图片都转为base64形式（其实应该说超过8kb的才使用 url-loader 来映射到文件，否则转为data url形式）。</p>
<ul>
<li>最后是 resolve 配置，这块很好理解，直接写注释了：</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">resolve: &#123;</span><br><span class="line">        <span class="comment">//查找module的话从这里开始查找</span></span><br><span class="line">        root: <span class="string">'E:/github/flux-example/src'</span>, <span class="comment">//绝对路径</span></span><br><span class="line">        <span class="comment">//自动扩展文件后缀名，意味着我们require模块可以省略不写后缀名</span></span><br><span class="line">        extensions: [<span class="string">''</span>, <span class="string">'.js'</span>, <span class="string">'.json'</span>, <span class="string">'.scss'</span>],</span><br><span class="line">        <span class="comment">//模块别名定义，方便后续直接引用别名，无须多写长长的地址</span></span><br><span class="line">        alias: &#123;</span><br><span class="line">            AppStore : <span class="string">'js/stores/AppStores.js'</span>,<span class="comment">//后续直接 require('AppStore') 即可</span></span><br><span class="line">            ActionType : <span class="string">'js/actions/ActionType.js'</span>,</span><br><span class="line">            AppAction : <span class="string">'js/actions/AppAction.js'</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>　　<strong>关于 webpack.config.js 更详尽的配置可以参考<a href="http://webpack.github.io/docs/configuration.html" target="_blank" rel="noopener">这里</a>。</strong></p>
<h2 id="2-运行-webpack"><a href="#2-运行-webpack" class="headerlink" title="2. 运行 webpack"></a>2. 运行 webpack</h2><p>　　webpack 的执行也很简单，直接执行：</p>
<blockquote>
<p>webpack –display-error-details</p>
</blockquote>
<p>　　即可，后面的参数“–display-error-details”是推荐加上的，方便出错时能查阅更详尽的信息（比如 webpack 寻找模块的过程），从而更好定位到问题。</p>
<p>　　其他主要的参数有：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ webpack --config XXX.js   //使用另一份配置文件（比如webpack.config2.js）来打包</span><br><span class="line"> </span><br><span class="line">$ webpack --watch   //监听变动并自动打包</span><br><span class="line"> </span><br><span class="line">$ webpack -p    //压缩混淆脚本，这个非常非常重要！</span><br><span class="line"> </span><br><span class="line">$ webpack -d    //生成map映射文件，告知哪些模块被最终打包到哪里了</span><br></pre></td></tr></table></figure>

<p>　　其中的 -p 是很重要的参数，曾经一个未压缩的 700kb 的文件，压缩后直接降到 180kb（主要是样式这块一句就独占一行脚本，导致未压缩脚本变得很大）。</p>
<h2 id="3-模块引入"><a href="#3-模块引入" class="headerlink" title="3. 模块引入"></a>3. 模块引入</h2><p>　　上面唠嗑了那么多配置和执行方法，下面开始说说寻常页面和脚本怎么使用呗。</p>
<h3 id="3-1-HTML"><a href="#3-1-HTML" class="headerlink" title="3.1 HTML"></a>3.1 HTML</h3><p>　　直接在页面引入 webpack 最终生成的页面脚本即可，不用再写什么 data-main 或 seajs.use 了：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">title</span>&gt;</span>demo<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"dist/js/page/common.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"dist/js/page/index.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　可以看到我们连样式都不用引入，毕竟脚本执行时会动态生成<style>并标签打到head里。</p>
<h3 id="3-2-JS"><a href="#3-2-JS" class="headerlink" title="3.2 JS"></a>3.2 JS</h3><p>　　各脚本模块可以直接使用 commonJS 来书写，并可以直接引入未经编译的模块，比如 JSX、sass、coffee等（只要你在 webpack.config.js 里配置好了对应的加载器）。</p>
<p>　　我们再看看编译前的页面入口文件（index.js）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">require</span>(<span class="string">'../../css/reset.scss'</span>); <span class="comment">//加载初始化样式</span></span><br><span class="line"><span class="built_in">require</span>(<span class="string">'../../css/allComponent.scss'</span>); <span class="comment">//加载组件样式</span></span><br><span class="line"><span class="keyword">var</span> React = <span class="built_in">require</span>(<span class="string">'react'</span>);</span><br><span class="line"><span class="keyword">var</span> AppWrap = <span class="built_in">require</span>(<span class="string">'../component/AppWrap'</span>); <span class="comment">//加载组件</span></span><br><span class="line"><span class="keyword">var</span> createRedux = <span class="built_in">require</span>(<span class="string">'redux'</span>).createRedux;</span><br><span class="line"><span class="keyword">var</span> Provider = <span class="built_in">require</span>(<span class="string">'redux/react'</span>).Provider;</span><br><span class="line"><span class="keyword">var</span> stores = <span class="built_in">require</span>(<span class="string">'AppStore'</span>);</span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> redux = createRedux(stores);</span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> App = React.createClass(&#123;</span><br><span class="line">    render: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">            &lt;Provider redux=&#123;redux&#125;&gt;</span><br><span class="line">                &#123;<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">AppWrap</span> /&gt;</span>; &#125;&#125;</span></span><br><span class="line"><span class="xml">            <span class="tag">&lt;/<span class="name">Provider</span>&gt;</span></span></span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"> </span><br><span class="line">React.render(</span><br><span class="line">    &lt;App /&gt;, <span class="built_in">document</span>.body</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>　　一切就是这么简单么么哒~~ 后续各种有的没的，webpack 都会帮你进行处理。</p>
<h2 id="4-其他"><a href="#4-其他" class="headerlink" title="4. 其他"></a>4. 其他</h2><p>　　至此我们已经基本上手了 webpack 的使用，下面是补充一些有用的技巧。</p>
<h3 id="4-1-shimming"><a href="#4-1-shimming" class="headerlink" title="4.1 shimming"></a>4.1 shimming</h3><p>　　在 AMD/CMD 中，我们需要对不符合规范的模块（比如一些直接返回全局变量的插件）进行 shim 处理，这时候我们需要使用 <a href="https://github.com/webpack-contrib/exports-loader">exports-loader</a> 来帮忙：</p>
<pre><code>{ test: require.resolve(&quot;./src/js/tool/swipe.js&quot;),  loader: &quot;exports?swipe&quot;}</code></pre><p>　　之后在脚本中需要引用该模块的时候，这么简单地来使用就可以了：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">require</span>(<span class="string">'./tool/swipe.js'</span>);</span><br><span class="line">swipe();</span><br></pre></td></tr></table></figure>

<h3 id="4-2-自定义公共模块提取"><a href="#4-2-自定义公共模块提取" class="headerlink" title="4.2 自定义公共模块提取"></a>4.2 自定义公共模块提取</h3><p>　　在文章开始我们使用了 CommonsChunkPlugin 插件来提取多个页面之间的公共模块，并将该模块打包为 common.js 。</p>
<p>　　但有时候我们希望能更加个性化一些，我们可以这样配置：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> CommonsChunkPlugin = <span class="built_in">require</span>(<span class="string">"webpack/lib/optimize/CommonsChunkPlugin"</span>);</span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">    entry: &#123;</span><br><span class="line">        p1: <span class="string">"./page1"</span>,</span><br><span class="line">        p2: <span class="string">"./page2"</span>,</span><br><span class="line">        p3: <span class="string">"./page3"</span>,</span><br><span class="line">        ap1: <span class="string">"./admin/page1"</span>,</span><br><span class="line">        ap2: <span class="string">"./admin/page2"</span></span><br><span class="line">    &#125;,</span><br><span class="line">    output: &#123;</span><br><span class="line">        filename: <span class="string">"[name].js"</span></span><br><span class="line">    &#125;,</span><br><span class="line">    plugins: [</span><br><span class="line">        <span class="keyword">new</span> CommonsChunkPlugin(<span class="string">"admin-commons.js"</span>, [<span class="string">"ap1"</span>, <span class="string">"ap2"</span>]),</span><br><span class="line">        <span class="keyword">new</span> CommonsChunkPlugin(<span class="string">"commons.js"</span>, [<span class="string">"p1"</span>, <span class="string">"p2"</span>, <span class="string">"admin-commons.js"</span>])</span><br><span class="line">    ]</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// &lt;script&gt;s required:</span></span><br><span class="line"><span class="comment">// page1.html: commons.js, p1.js</span></span><br><span class="line"><span class="comment">// page2.html: commons.js, p2.js</span></span><br><span class="line"><span class="comment">// page3.html: p3.js</span></span><br><span class="line"><span class="comment">// admin-page1.html: commons.js, admin-commons.js, ap1.js</span></span><br><span class="line"><span class="comment">// admin-page2.html: commons.js, admin-commons.js, ap2.js</span></span><br></pre></td></tr></table></figure>

<h3 id="4-3-独立打包样式文件"><a href="#4-3-独立打包样式文件" class="headerlink" title="4.3 独立打包样式文件"></a>4.3 独立打包样式文件</h3><p>　　有时候可能希望项目的样式能不要被打包到脚本中，而是独立出来作为.css，然后在页面中以<link>标签引入。这时候我们需要 <a href="https://github.com/webpack/extract-text-webpack-plugin">extract-text-webpack-plugin</a> 来帮忙：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> webpack = <span class="built_in">require</span>(<span class="string">'webpack'</span>);</span><br><span class="line">    <span class="keyword">var</span> commonsPlugin = <span class="keyword">new</span> webpack.optimize.CommonsChunkPlugin(<span class="string">'common.js'</span>);</span><br><span class="line">    <span class="keyword">var</span> ExtractTextPlugin = <span class="built_in">require</span>(<span class="string">"extract-text-webpack-plugin"</span>);</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">module</span>.exports = &#123;</span><br><span class="line">        plugins: [commonsPlugin, <span class="keyword">new</span> ExtractTextPlugin(<span class="string">"[name].css"</span>)],</span><br><span class="line">        entry: &#123;&#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//...省略其它配置</span></span><br></pre></td></tr></table></figure>

<p>　　最终 webpack 执行后会乖乖地把样式文件提取出来：</p>
<p><img src="/styles/images/webpack/webpack_list.png" alt="webpack样式文件"></p>
<h3 id="4-4-使用CDN-远程文件"><a href="#4-4-使用CDN-远程文件" class="headerlink" title="4.4 使用CDN/远程文件"></a>4.4 使用CDN/远程文件</h3><p>　　有时候我们希望某些模块走CDN并以<script>的形式挂载到页面上来加载，但又希望能在 webpack 的模块中使用上。</p>
<p>　　这时候我们可以在配置文件里使用 externals 属性来帮忙：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    externals: &#123;</span><br><span class="line">        <span class="comment">// require("jquery") 是引用自外部模块的</span></span><br><span class="line">        <span class="comment">// 对应全局变量 jQuery</span></span><br><span class="line">        <span class="string">"jquery"</span>: <span class="string">"jQuery"</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　需要留意的是，得<strong>确保 CDN 文件必须在 webpack 打包文件引入之前先引入</strong>。</p>
<p>　　我们倒也可以使用 script.js 在脚本中来加载我们的模块：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> $script = <span class="built_in">require</span>(<span class="string">"scriptjs"</span>);</span><br><span class="line">$script(<span class="string">"//ajax.googleapis.com/ajax/libs/jquery/2.0.0/jquery.min.js"</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  $(<span class="string">'body'</span>).html(<span class="string">'It works!'</span>)</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="4-5-与-grunt-gulp-配合"><a href="#4-5-与-grunt-gulp-配合" class="headerlink" title="4.5 与 grunt/gulp 配合"></a>4.5 与 grunt/gulp 配合</h3><p>　　以 gulp 为示例，我们可以这样混搭：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">gulp.task(<span class="string">"webpack"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// run webpack</span></span><br><span class="line">    webpack(&#123;</span><br><span class="line">        <span class="comment">// configuration</span></span><br><span class="line">    &#125;, <span class="function"><span class="keyword">function</span>(<span class="params">err, stats</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(err) <span class="keyword">throw</span> <span class="keyword">new</span> gutil.PluginError(<span class="string">"webpack"</span>, err);</span><br><span class="line">        gutil.log(<span class="string">"[webpack]"</span>, stats.toString(&#123;</span><br><span class="line">            <span class="comment">// output options</span></span><br><span class="line">        &#125;));</span><br><span class="line">        callback();</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>　　当然我们只需要把配置写到 webpack({ ... }) 中去即可，无须再写 webpack.config.js 了。更多参照信息请参阅：<a href="http://webpack.github.io/docs/usage-with-grunt.html">grunt配置</a> / <a href="http://webpack.github.io/docs/usage-with-gulp.html">gulp配置</a> 。</p>
<h3 id="4-6-React-相关"><a href="#4-6-React-相关" class="headerlink" title="4.6 React 相关"></a>4.6 React 相关</h3><p>1 推荐使用 npm install react 的形式来安装并引用 React 模块，而不是直接使用编译后的 react.js，这样最终编译出来的 React 部分的脚本会减少 10-20 kb左右的大小。</p>
<p>2 <a href="https://github.com/gaearon/react-hot-loader">react-hot-loader</a> 是一款非常好用的 React 热插拔的加载插件，通过它可以实现修改-运行同步的效果，配合 <a href="http://webpack.github.io/docs/webpack-dev-server.html">webpack-dev-server</a> 使用更佳！</p>
<p>基于 webpack 的入门指引就到这里，希望本文能对你有所帮助，你也可以参考这篇文章来入门： <a href="https://github.com/petehunt/webpack-howto">webpack-howto</a>。</p>
<hr></style></p>]]></content>
      <categories>
        <category>前沿技术</category>
      </categories>
      <tags>
        <tag>ES6</tag>
        <tag>JavaScript</tag>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>详解五种跨域方式及其原理</title>
    <url>/2017/02/10/article-js-jsonp/</url>
    <content><![CDATA[<p>　　这里说的 js 跨域是指通过 js 在不同的域之间进行数据传输或通信，比如用 ajax 向一个不同的域请求数据，或者通过 js 获取页面中不同域的框架中(iframe)的数据。只要协议、域名、端口有任何一个不同，都被当作是不同的域。<a id="more"></a></p>
<p>　　 <strong>本文参考自 <a href="http://mp.weixin.qq.com/s/YykAwXyEqMDgc_5-b_cdsA" target="_blank" rel="noopener">JavaScript （微信公众号）</a>。</strong></p>
<blockquote>
<ul>
<li>同源定义：</li>
</ul>
</blockquote>
<p>　　如果两个页面拥有相同的协议（protocol），端口（如果指定），和主机，那么这两个页面就属于同一个源（origin）。以下是同源检测的示例：</p>
<div class="table-responsive"><table class="table table-bordered table-hover"><thead><tr align="center"><td>URL</td><td>结果</td><td>原因</td></tr></thead><tbody><tr><td>http://store.company.com/dir2/other.html</td><td>Success</td><td></td></tr><tr><td>http://store.company.com/dir/inner/another.html</td><td>Success</td><td></td></tr><tr><td>https://store.company.com/secure.html</td><td>Failure</td><td>协议不同</td></tr><tr><td>http://store.company.com:81/dir/etc.html</td><td>Failure</td><td>端口不同</td></tr><tr><td>http://news.company.com/dir/other.html</td><td>Failure</td><td>主机不同</td></tr></tbody></table></div>

<h2 id="1-jsonp"><a href="#1-jsonp" class="headerlink" title="1. jsonp"></a>1. jsonp</h2><p>　　script 标签是不受同源策略影响的，它可以引入来自任何地方的 js 文件。 </p>
<p>　　而 jsonp 的原理就是，在客户端和服务端定义一个函数，当客户端发起一个请求时，服务端返回一段<code>javascript</code>代码，其中调用了在客户端定义的函数，并将相应的数据作为参数传入该函数。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">jsonp_cb</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">ajax</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> url = <span class="string">"http://xx.com/test.php?jsonp_callback=jsonp_cb"</span>;</span><br><span class="line">    <span class="keyword">var</span> script = <span class="built_in">document</span>.createElement(<span class="string">'script'</span>);</span><br><span class="line">    <span class="comment">// 发送请求</span></span><br><span class="line">    script.src = url;</span><br><span class="line">    <span class="built_in">document</span>.head.appendChild(script);</span><br><span class="line">&#125;</span><br><span class="line">ajax()</span><br></pre></td></tr></table></figure>

<p>　　服务端获取到jsonp_callback传递的函数名jsonp_cb，返回一段对该函数调用的js代码：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">jsonp_cb(&#123;</span><br><span class="line">   <span class="string">"name"</span>: <span class="string">"story"</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="2-img-ping"><a href="#2-img-ping" class="headerlink" title="2. img ping"></a>2. img ping</h2><p>　　<code>img</code> 标签也是没有跨域限制的，但它只能用来发送 GET 请求，且无法获取服务端的响应文本，可以利用它实现一些简单的、单向的跨域通信，例如跟踪用户的点击：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> img = <span class="keyword">new</span> Image();</span><br><span class="line">img.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'done'</span>)</span><br><span class="line">    img.onload = <span class="literal">null</span>;</span><br><span class="line">    img = <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br><span class="line">img.src = <span class="string">"http://xx/xx.gif"</span></span><br></pre></td></tr></table></figure>

<h2 id="3-window-name"><a href="#3-window-name" class="headerlink" title="3. window.name"></a>3. window.name</h2><p>　　window 对象拥有 name 属性，它有一个特点：相同协议下，在一个页面中，不随URL的改变而改变。</p>
<blockquote>
<p>示例代码:</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.name = <span class="string">'string'</span> <span class="comment">// 字符串，一般允许的最大值为2M</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.name)</span><br><span class="line">location = <span class="string">'http://funteas.com/'</span></span><br></pre></td></tr></table></figure>

<p>　　此时，在控制台输入 window.name，结果依然是”string”</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.name <span class="comment">// "string"</span></span><br></pre></td></tr></table></figure>

<p>　　window.name 的值只能是字符串，任何其他类型的值都会“转化”为字符串，例如：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.name = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">window</span>.name)</span><br><span class="line"><span class="comment">// "function()&#123;&#125;"</span></span><br></pre></td></tr></table></figure>

<p>　　通过 window.name 实现跨域也很简单，iframe 拥有 contentWindow 属性，其指向该 iframe 的 window 对象的引用，如果在 iframe 的 src 指向的页面中设置 window.name 值，那么就可以通过 iframe.contentWindow.name 就可以拿到这个值了。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> url = <span class="string">"http://funteas.com/lab/windowName"</span>;</span><br><span class="line"><span class="keyword">var</span> iframe = <span class="built_in">document</span>.createElement(<span class="string">'iframe'</span>)</span><br><span class="line">iframe.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> data = iframe.contentWindow.name</span><br><span class="line">    <span class="built_in">console</span>.log(data)</span><br><span class="line">&#125;</span><br><span class="line">iframe.src = url</span><br><span class="line"><span class="built_in">document</span>.body.appendChild(iframe)</span><br></pre></td></tr></table></figure>

<p>　　然而，chrome 会提示你跨域了！而我们已经知道 window.name 不随 URL 的改变而改版，也就是说，onload 时，已经获取到了 name，只不过因为不同源，当前页面的脚本无法拿到 iframe.contentWindow.name，此时只需要把 iframe.src 改为同源即可。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> url = <span class="string">"http://funteas.com/lab/windowName"</span>;</span><br><span class="line"><span class="keyword">var</span> iframe = <span class="built_in">document</span>.createElement(<span class="string">'iframe'</span>)</span><br><span class="line">iframe.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    iframe.src = <span class="string">'favicon.ico'</span>;</span><br><span class="line">    <span class="keyword">var</span> data = iframe.contentWindow.name</span><br><span class="line">    <span class="built_in">console</span>.log(data)</span><br><span class="line">&#125;</span><br><span class="line">iframe.src = url</span><br><span class="line"><span class="built_in">document</span>.body.appendChild(iframe)</span><br></pre></td></tr></table></figure>

<p>　　刷新页面，你会发现 iframe 不断刷新，这是因为每次 onload，iframe 的 src 被修改，然后再次触发 onload，从而导致iframe 循环刷新，修改下即可：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> url = <span class="string">"http://funteas.com/lab/windowName"</span>;</span><br><span class="line"><span class="keyword">var</span> iframe = <span class="built_in">document</span>.createElement(<span class="string">'iframe'</span>)</span><br><span class="line"><span class="keyword">var</span> state = <span class="literal">true</span>;</span><br><span class="line">iframe.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(state === <span class="literal">true</span>)&#123;</span><br><span class="line">        iframe.src = <span class="string">'favicon.ico'</span>;</span><br><span class="line">        state = <span class="literal">false</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span> <span class="keyword">if</span>(state === <span class="literal">false</span>)&#123;</span><br><span class="line">        state = <span class="literal">null</span></span><br><span class="line">        <span class="keyword">var</span> data = iframe.contentWindow.name</span><br><span class="line">        <span class="built_in">console</span>.log(data)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">iframe.src = url</span><br><span class="line"><span class="built_in">document</span>.body.appendChild(iframe)</span><br></pre></td></tr></table></figure>

<p>　　上面请求的是一个静态页面，而服务端通常需要的是动态数据：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">echo <span class="string">'&lt;script&gt; window.name = "&#123;\"name\":\"story\"&#125;"&lt;/script&gt;'</span>;</span><br></pre></td></tr></table></figure>

<h2 id="4-postMessage"><a href="#4-postMessage" class="headerlink" title="4. postMessage"></a>4. postMessage</h2><p>　　postMessage 允许不同源之间的脚本进行通信，用法：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">otherWindow.postMessage(message, targetOrigin);</span><br></pre></td></tr></table></figure>

<ul>
<li>otherWindow 引用窗口 iframe.contentwindow 或 window.open返回的对象</li>
<li>message 为要传递的数据</li>
<li>targetOrigin 为目标源</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// http://127.0.0.1:80</span></span><br><span class="line"><span class="keyword">var</span> iframe = <span class="built_in">document</span>.createElement(<span class="string">'iframe'</span>)</span><br><span class="line">iframe.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> popup = iframe.contentWindow</span><br><span class="line">    popup.postMessage(<span class="string">"hello"</span>, <span class="string">"http://127.0.0.1:5000"</span>);</span><br><span class="line">&#125;</span><br><span class="line">iframe.src = <span class="string">'http://127.0.0.1:5000/lab/postMessage'</span></span><br><span class="line"><span class="built_in">document</span>.body.appendChild(iframe)</span><br><span class="line"><span class="comment">// 监听返回的postMessage</span></span><br><span class="line"><span class="built_in">window</span>.addEventListener(<span class="string">"message"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">event</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (event.origin !== <span class="string">"http://127.0.0.1:5000"</span>) <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">console</span>.log(event.data)</span><br><span class="line">&#125;, <span class="literal">false</span>)</span><br><span class="line"><span class="comment">// http://127.0.0.1:5000/lab/postMessage</span></span><br><span class="line"><span class="built_in">window</span>.addEventListener(<span class="string">"message"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">event</span>)</span>&#123;</span><br><span class="line">    <span class="comment">// 验证消息来源</span></span><br><span class="line">    <span class="keyword">if</span> (event.origin !== <span class="string">"http://127.0.0.1"</span>) <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">console</span>.log(event.source); <span class="comment">// 消息源 popup</span></span><br><span class="line">    <span class="built_in">console</span>.log(event.origin); <span class="comment">// 消息源URI https://secure.example.net </span></span><br><span class="line">    <span class="built_in">console</span>.log(event.data); <span class="comment">// 来自消息源的数据 hello</span></span><br><span class="line">    <span class="comment">// 返回数据</span></span><br><span class="line">    <span class="keyword">var</span> message = <span class="string">'world'</span>;</span><br><span class="line">    event.source.postMessage(message, event.origin);</span><br><span class="line">&#125;, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<h2 id="5-CORS"><a href="#5-CORS" class="headerlink" title="5. CORS"></a>5. CORS</h2><p>　　CORS（跨域资源共享）是一种跨域访问的机制，可以让 AJAX 实现跨域访问。它允许一个域上的脚本向另一个域提交跨域 AJAX 请求。实现此功能非常简单，只需由服务器发送一个响应标头即可。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">Access-Control-Allow-Origin: * <span class="comment">// 允许来自任何域的请求</span></span><br><span class="line">Access-Control-Allow-Origin: http:<span class="comment">//funteas.com/ // 仅允许来自http://funteas.com/的请求</span></span><br></pre></td></tr></table></figure>

<p>　　当客户端的 ajax 请求的 url 为其他域时，对于支持 CORS 的浏览器，请求头会自动添加 Origin，值为当前 host。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line"><span class="keyword">var</span> url = <span class="string">'http://bar.other/resources/public-data/'</span>;</span><br><span class="line">xhr.open(<span class="string">'GET'</span>, url, <span class="literal">true</span>);</span><br><span class="line">xhr.send();</span><br></pre></td></tr></table></figure>

<p>　　CORS 默认不发送 cookie，如果要发送 cookie，需要设置 withCredentials：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">xhr.withCredentials = <span class="literal">true</span>;</span><br></pre></td></tr></table></figure>

<p>　　同时，服务端也要设置：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">Access-Control-Allow-Credentials: <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<p>　　<strong>详细可查看MDN <a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Access_control_CORS" target="_blank" rel="noopener">关于CORS的介绍</a>。</strong></p>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Ajax 基础详解</title>
    <url>/2017/02/09/article-js-ajax/</url>
    <content><![CDATA[<p>　　Ajax 全称为“Asynchronous JavaScript And XML”（异步 JavaScript 和 XML） 是指一种创建交互式网页应用的开发技术、改善用户体验，实现无刷新效果。<a id="more"></a></p>
<h2 id="Ajax-基础"><a href="#Ajax-基础" class="headerlink" title="Ajax 基础"></a>Ajax 基础</h2><h3 id="1-什么是-Ajax"><a href="#1-什么是-Ajax" class="headerlink" title="1. 什么是 Ajax"></a>1. 什么是 Ajax</h3><p>　　Ajax 不是一种新的编程语言，而是一种用于创建更好更快以及交互性更强的 Web 应用程序的技术。它是一套综合了多项技术的浏览器端网页开发技术。这些技术包括<code>Javascript</code>、<code>XHTML</code> 和 <code>CSS</code>、<code>DOM</code>、<code>XML</code> 和 <code>XMLHttpRequest</code>。</p>
<h3 id="2-为什么使用-Ajax？"><a href="#2-为什么使用-Ajax？" class="headerlink" title="2. 为什么使用 Ajax？"></a>2. 为什么使用 Ajax？</h3><blockquote>
<p>优点:</p>
</blockquote>
<p>　　使用 Ajax 的最大优点，就是能在不更新整个页面的前提下维护数据。这使得 Web 应用程序更为迅捷地响应用户动作，并避免了在网络上发送那些没有改变的 <code>html</code> 代码信息。</p>
<ol>
<li>减轻服务器负担，按需要获得数据。</li>
<li>无刷新更新页面，减少用户的实际和心理的等待时间。</li>
<li>更好的用户体验。</li>
<li>减轻宽带的负担。</li>
<li>主流浏览器支持。</li>
</ol>
<blockquote>
<p>缺点:</p>
</blockquote>
<ol>
<li>Ajax 的程序必须测试针对各个浏览器的兼容性。</li>
<li>Ajax 更新页面内容的时候并没有刷新整个页面，因此，网页的后退功能是失效的；需提醒用户。</li>
<li>对搜索引擎支持不好。</li>
</ol>
<h3 id="3-Ajax-原理"><a href="#3-Ajax-原理" class="headerlink" title="3. Ajax 原理"></a>3. Ajax 原理</h3><ul>
<li><p>运用 <code>HTML</code> 和 <code>css</code> 来实现页面,表达信息</p>
</li>
<li><p>通过浏览器的 <code>XmlHttpRequest</code> ( Ajax 引擎)对象</p>
</li>
<li><p>来向服务器(发送异步请求)并(接收服务器)的响应数据，</p>
</li>
<li><p>然后用 JavaScript 来操作 DOM，实现动态局部刷新。</p>
</li>
</ul>
<img src="/styles/images/ajax/ajax.gif">

<h3 id="4-Ajax-实现过程"><a href="#4-Ajax-实现过程" class="headerlink" title="4. Ajax 实现过程"></a>4. Ajax 实现过程</h3><p>通常是：</p>
<blockquote>
<p>第一步: <code>Javascript</code> 监听浏览器网页事件(点击,提交,更改等)</p>
</blockquote>
<blockquote>
<p>第二步:由 <code>Javascript</code> 创建 Ajax 引擎对象</p>
</blockquote>
<blockquote>
<p>第三步:通过 Ajax 引擎对象发出请求</p>
</blockquote>
<blockquote>
<p>第四步: Ajax 引擎等待并且接受服务器的响应内容</p>
</blockquote>
<blockquote>
<p>第五步: <code>Javascript</code> 再从 <em>Ajax引擎对象</em> 中获取响应内容,并且通过 DOM 改变网页界面显示效果</p>
</blockquote>
<h4 id="4-1-创建Ajax引擎对象"><a href="#4-1-创建Ajax引擎对象" class="headerlink" title="4.1 创建Ajax引擎对象"></a>4.1 创建Ajax引擎对象</h4><p>　　Ajax 引擎对象，就是 <code>XMLHtttpRequest</code> 对象,所有现代浏览器均支持 <code>XMLHttpRequest</code> 对象。</p>
<blockquote>
<p>兼容性处理：</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xhr = <span class="literal">null</span>;</span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">window</span>.XMLHttpRequest) &#123;</span><br><span class="line">    <span class="comment">//所有现代浏览器(IE7+.Firefox.Chrome.Safari以及opera)</span></span><br><span class="line">    xhr = <span class="keyword">new</span> XMLHttpRequest;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="comment">//老版本的Internet Explorer使用ActiveX对象</span></span><br><span class="line">    xhr = <span class="keyword">new</span> ActiveXObject(<span class="string">"Microsoft.XMLHttp"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="4-2-配置请求对象的信息"><a href="#4-2-配置请求对象的信息" class="headerlink" title="4.2  配置请求对象的信息"></a>4.2  配置请求对象的信息</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">xhr.open(<span class="string">'GET/POST'</span>,<span class="string">'url地址'</span>,[<span class="string">'是否异步'</span>]);</span><br><span class="line"></span><br><span class="line"><span class="comment">//注意：如果使用post请求,就一定要设置请求头</span></span><br><span class="line"></span><br><span class="line">xhr.setRequestHeader(<span class="string">"Content-Type"</span>,<span class="string">"application/x-www-form-urlencoded;charset=gb2312"</span>);</span><br></pre></td></tr></table></figure>

<h4 id="4-3-发送请求"><a href="#4-3-发送请求" class="headerlink" title="4.3 发送请求"></a>4.3 发送请求</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">xhr.send([post请求参数字符串]);</span><br></pre></td></tr></table></figure>

<p>　　发送请求分为两种情况:</p>
<ol>
<li>监控用户的事件(onclick,onchange等),通过ajax发送请求</li>
<li>监控浏览器的事件(onload),通过ajax发送请求</li>
</ol>
<h4 id="4-4-监听-Ajax-引擎对象的改变-是否正确做出了响应"><a href="#4-4-监听-Ajax-引擎对象的改变-是否正确做出了响应" class="headerlink" title="4.4 监听 Ajax 引擎对象的改变(是否正确做出了响应)"></a>4.4 监听 Ajax 引擎对象的改变(是否正确做出了响应)</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">xmlHttpRequest.onreadystatechange = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (xmlHttpRequest.readyState== <span class="number">4</span> &amp;&amp; xmlHttpRequest==<span class="number">200</span>) &#123;</span><br><span class="line">            <span class="comment">//xmlHttpRequest.responseText</span></span><br><span class="line">            <span class="comment">//xmlHttpRequest.responseText</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<ul>
<li><strong>详解 <code>onreadystatechange</code></strong></li>
</ul>
</blockquote>
<p>　　<code>onreadystatechange</code>属性是一个方法，监控到响应内容的返回，根据响应内容使用<code>javascript</code>改变当前页面的部分<code>html</code>代码,从而达到不刷新改变局部<code>html</code>代码。当 Ajax 引擎的状态发生改变时都会执行<code>onreadystatechange</code>属性对应的方法。</p>
<blockquote>
<p>其他方法：</p>
</blockquote>
<pre><code>abort:取消当前请求

getAllResponseHeaders:获取响应的所有http头

getResponseHeader:从响应信息中获取指定的http头

open(方式get/post，url地址，同步异步):
创建一个新的http请求，打开请求，并指定此请求的方法、URL以及验证信息(用户名/密码)

send():发送请求到http服务器并接收回应

setRequestHeader:单独指定请求的某个http头，header()设置http头协议信息</code></pre><blockquote>
<p>其他属性：</p>
</blockquote>
<pre><code>onreadystatechange:指定当readyState属性改变时的事件处理句柄。

readyState:返回当前请求的状态，ajax行进过程中不同状态

responseBody:将回应信息正文以unsigned byte数组形式返回.

responseStream:以Ado Stream对象的形式返回响应信息。

responseText:将响应信息作为字符串返回.只读

responseXML:将响应信息格式化为Xml Document对象并返回，只读

status:返回当前请求的http状态码. 200 ok 304 缓存；404 not found; 403 没有权限；501 服务器级别错误

statusText:返回当前请求的响应行状态文本， ok not found forbidden</code></pre><blockquote>
<ul>
<li><strong>详解状态属性 <code>readyState(0 1 2 3 4)</code></strong></li>
</ul>
</blockquote>
<ol>
<li>请求未初始化</li>
<li>服务器连接已建立</li>
<li>请求已接收</li>
<li>请求处理中</li>
<li>请求已完成,并且响应已就绪</li>
</ol>
<blockquote>
<p>当Ajax引擎的状态属性为<code>readyState</code>为 <strong>4</strong> 时,说明服务器的响应已经发送给 Ajax 请求了；</p>
</blockquote>
<blockquote>
<p>但是响应的状态吗为<strong>200</strong>时：说明响应的内容是正确的,这时才会根据相应内容对当前页面的<code>html</code>处理。</p>
</blockquote>
<h3 id="5-总结示例"><a href="#5-总结示例" class="headerlink" title="5. 总结示例"></a>5. 总结示例</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest;</span><br><span class="line">xhr.open(<span class="string">'GET/POST'</span>,<span class="string">'url地址'</span>,[<span class="string">'是否异步'</span>]);</span><br><span class="line">xhr.send([post请求参数字符串]);</span><br><span class="line">xhr.onreadystatechange = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (xmlHttpRequest.readyState== <span class="number">4</span> &amp;&amp; xmlHttpRequest==<span class="number">200</span>) &#123;</span><br><span class="line">            <span class="comment">//做一些事情</span></span><br><span class="line">            <span class="comment">//xhr.responseText</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Ajax-相关知识点"><a href="#Ajax-相关知识点" class="headerlink" title="Ajax 相关知识点"></a>Ajax 相关知识点</h2><h3 id="1-同步请求和异步请求"><a href="#1-同步请求和异步请求" class="headerlink" title="1. 同步请求和异步请求"></a>1. 同步请求和异步请求</h3><p>　　先解释一下同步和异步的概念：</p>
<ul>
<li><p>同步是指：发送方发出数据后，等接收方发回响应以后才发下一个数据包的通讯方式。</p>
</li>
<li><p>异步是指：发送方发出数据后，不等接收方发回响应，接着发送下个数据包的通讯方式。</p>
</li>
</ul>
<hr>

<h4 id="1-1-同步请求"><a href="#1-1-同步请求" class="headerlink" title="1.1 同步请求"></a>1.1 同步请求</h4><p>　　客户端请求(等待) -&gt; 服务端处理 -&gt; 响应 -&gt; 页面载入（缺少对象:<code>XMLhttpRequest</code>）这时候如果有错误，只能再次发送请求，再次等待。</p>
<h4 id="1-2-异步请求"><a href="#1-2-异步请求" class="headerlink" title="1.2 异步请求"></a>1.2 异步请求</h4><p>　　比如当你填写邮箱地址的时候，页面当时就把邮箱地址发送到了服务器（也就是页面发送了一个请求），服务器做<strong>处理和响应</strong>，得到你的邮箱地址填写重复了，把响应结果发给页面，在这个过程中你仍然可以填写其他内容，这时候服务器会提示你有重复的邮箱地址，在页面上的表现只是邮箱地址旁别加了一行字或者把整个文本框标红，并没有重新刷你的页面，所有的填写错误会实时的显示出来，你也会实时的更正。这个过程中，你会有整个页面的刷新，也不会有整个页面的提交和等待，最后提交，一切都会搞定的</p>
<blockquote>
<p><strong>页面上的操作和服务器端的操作互相之间不会造成阻塞</strong>！</p>
</blockquote>
<h3 id="2-HTTP是什么"><a href="#2-HTTP是什么" class="headerlink" title="2. HTTP是什么"></a>2. HTTP是什么</h3><p>　　http 是计算机通过网络进行通信的规则。</p>
<blockquote>
<p><strong>一个完整的HTTP请求过程的七个步骤</strong>：</p>
</blockquote>
<ol>
<li>建立 TCP 连接</li>
<li>web 浏览器向 web 服务器发送请求命令</li>
<li>web 浏览器发送请求头信息</li>
<li>web 服务器应答</li>
<li>web 服务器发送应答头信息</li>
<li>web 服务器向浏览器发送数据</li>
<li>web 服务器关闭 TCP 连接</li>
</ol>
<h4 id="2-1-作用"><a href="#2-1-作用" class="headerlink" title="2.1 作用"></a>2.1 作用</h4><p>　　客户端向服务端发送请求信息和服务。</p>
<blockquote>
<p><strong>客户端</strong>：自己的计算机的浏览器(web浏览器)</p>
</blockquote>
<blockquote>
<p><strong>服务端</strong>：服务器,一台高性能的计算机，作为存储，处理网络上的数据，信息(web服务器) ，服务器指一个管理资源并为用户提供服务的计算机软件，通常分为文件服务器、数据库服务器和应用程序服务器。运行以上软件的计算机或计算机系统也被称为服务器。</p>
</blockquote>
<h4 id="2-2-特点"><a href="#2-2-特点" class="headerlink" title="2.2 特点"></a>2.2 特点</h4><p>　　是一种无状态的协议(不建立持久的连接,服务端不保留连接的信息)。</p>
<h4 id="2-4-请求方法"><a href="#2-4-请求方法" class="headerlink" title="2.4 请求方法"></a>2.4 请求方法</h4><ul>
<li><p><strong><code>GET</code>：</strong></p>
<ul>
<li>一般用于信息获取,用get请求–&gt;查询或获取的操作</li>
<li>使用URL传递参数</li>
<li>对所发送信息的数量有限制,一般在2000个字</li>
</ul>
</li>
</ul>
<ul>
<li><p><strong><code>POST</code>：</strong></p>
<ul>
<li>一般用于修改服务器上的数据.比如:新建,修改,删除,发送表单数据</li>
<li>对所发送信息的数量无限制</li>
</ul>
</li>
</ul>
<h4 id="2-5-HTTP-状态码"><a href="#2-5-HTTP-状态码" class="headerlink" title="2.5 HTTP 状态码"></a>2.5 HTTP 状态码</h4><ul>
<li><strong>100</strong>：信息类,表示web浏览器请求,正在进一步的处理中</li>
<li><strong>200</strong>：成功,表示用户请求被正确接收正在进一步的处理中 200 OK</li>
<li><strong>300</strong>：表示请求没有成功,客户端必须采取进一步的动作</li>
<li><strong>400</strong>：客户端错误,表示客户端提交的请求有错误 例如:404 NOT Found,意味着请求中所引用的文档不存在</li>
<li><strong>500</strong>：服务器错误 表示服务器不能完成对请求的处理,如500</li>
</ul>
<h4 id="2-6-HTTP-响应"><a href="#2-6-HTTP-响应" class="headerlink" title="2.6 HTTP 响应"></a>2.6 HTTP 响应</h4><p>　　HTTP 响应一般由三部分组成：</p>
<ol>
<li>一个数字和文字组成的状态码,用来显示请求是成功还是失败</li>
<li>响应头:响应头也和请求头一样包含许多有用的信息,例如：服务器类型,日期时间,内容类型和长度等</li>
<li>响应体,也就是响应正文</li>
</ol>
<h4 id="2-7-HTTP-请求"><a href="#2-7-HTTP-请求" class="headerlink" title="2.7 HTTP 请求"></a>2.7 HTTP 请求</h4><p>　　HTTP 请求的四部分组成：</p>
<ol>
<li>HTTP 请求的方法或动作,比如 GET 还是 POST</li>
<li>正在请求的 URL,总得知道请求的地址是什么</li>
<li>请求头,包含一些客户端环境信息,身份验证信息等</li>
<li>请求体,也就是请求正文,请求正文中可以包含客户提交的查询字符串信息,表单信息等等</li>
</ol>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>Ajax</tag>
      </tags>
  </entry>
  <entry>
    <title>你可能学了假的 JavaScript！</title>
    <url>/2017/02/08/article-js-kills1/</url>
    <content><![CDATA[<p>　　Adobe 一个高级工程师 Dmitry Baranovskiy 在<a href="http://dmitry.baranovskiy.com/" target="_blank" rel="noopener"> 他的博客 </a>上写了这么一篇博文<a href="http://dmitry.baranovskiy.com/post/91403200" target="_blank" rel="noopener"> So,you think you know JavaScript? </a>，并在博文中贴出了五个小示例（先不要看我贴的测试结果），大家可以感受一下，看看你是不是学了假的 JavaScript！<a id="more"></a></p>
<h2 id="1-示例展示"><a href="#1-示例展示" class="headerlink" title="1. 示例展示"></a>1. 示例展示</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!(<span class="string">"a"</span> <span class="keyword">in</span> <span class="built_in">window</span>)) &#123;</span><br><span class="line">    <span class="keyword">var</span> a = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line">alert(a);</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span>,</span><br><span class="line">    b = <span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">        x &amp;&amp; a(--x);</span><br><span class="line">    &#125;;</span><br><span class="line">alert(a);</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x * <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> a;</span><br><span class="line">alert(a);</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">b</span>(<span class="params">x, y, a</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">arguments</span>[<span class="number">2</span>] = <span class="number">10</span>;</span><br><span class="line">    alert(a);</span><br><span class="line">&#125;</span><br><span class="line">b(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    alert(<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br><span class="line">a.call(<span class="literal">null</span>);</span><br></pre></td></tr></table></figure>

<p>　　感受完了请先将你的答案写出来，然后再去实际测试一下 … … 如果错了一两个说明你 JavaScript 基础不牢，如果错了三四个了，那就只能说明你学了假的 JavaScript！</p>
<h2 id="2-示例测试"><a href="#2-示例测试" class="headerlink" title="2. 示例测试"></a>2. 示例测试</h2><ol>
<li><p>undefined </p>
</li>
<li><p>1 </p>
</li>
<li><p>function a (x) {<br>return x * 2;<br>}</p>
</li>
<li><p>1、2、3、undefined、10 </p>
</li>
<li><p>object Windows</p>
</li>
</ol>
<h2 id="3-示例分析"><a href="#3-示例分析" class="headerlink" title="3. 示例分析"></a>3. 示例分析</h2><h3 id="3-1-示例-1"><a href="#3-1-示例-1" class="headerlink" title="3.1 示例 1"></a>3.1 示例 1</h3><blockquote>
<p>示例1原型：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">分析：首先会解析所有函数，其次是var声明的变量，但是不会赋值。因为javascript没有块的概念。像for(var i in array)这里的i依然是全局变量。 </span><br><span class="line">执行顺序： </span><br><span class="line">1.var a;//声明一个变量，但是不会赋值 </span><br><span class="line">2.if语句，全局变量相当于window的属性，所以”a” in window为真，取反为假。故不会执行大括号里面的语句。 </span><br><span class="line">3.alert(a);//undefined</span><br></pre></td></tr></table></figure>

<blockquote>
<p>示例1变形：</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ((<span class="string">"a"</span> <span class="keyword">in</span> <span class="built_in">window</span>)) &#123;</span><br><span class="line">        <span class="keyword">var</span> a = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    alert (a);</span><br></pre></td></tr></table></figure>

<p>　　Javascript变量的scope是根据方法块来划分的（也就是说以function的一对大括号｛ ｝来划分）。切记，是function块，而for、while、if块并不是作用域的划分标准!所以这里的a依然是全局变量。</p>
<h3 id="3-2-示例-2"><a href="#3-2-示例-2" class="headerlink" title="3.2 示例 2"></a>3.2 示例 2</h3><blockquote>
<p>示例2原型：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">分析： JavaScript中定义变量时，也可以同时定义多个，例如var a,b,c;在定义变量时使用函数表达式就类似于局部变量，不会被全局作用域中访问到。 </span><br><span class="line">执行顺序： </span><br><span class="line">1.声明两个变量var a,b;并给他们赋值a=1,b=function a()&#123;…&#125;;这里的function a是局部变量，外部无法访问到。因此全局a还是1. </span><br><span class="line">2.alert(a);//1</span><br></pre></td></tr></table></figure>

<blockquote>
<p>示例2变形：</p>
</blockquote>
<p>　　这样写的话大家比较一下可能更能理解一点。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span>,</span><br><span class="line"> b = <span class="function"><span class="keyword">function</span> <span class="title">a</span> (<span class="params">x</span>) </span>&#123;</span><br><span class="line">    x &amp;&amp; a (--x);</span><br><span class="line">    alert(a);<span class="comment">//打印看一下a是什么值，和外面打印的a进行比较</span></span><br><span class="line">    <span class="keyword">return</span> x;<span class="comment">//返回一个值，这个值其实就是给b赋的值</span></span><br><span class="line">&#125;;</span><br><span class="line">alert (a);<span class="comment">//这里只能取到全局变量a</span></span><br><span class="line">alert (a(<span class="number">1</span>)); <span class="comment">//报错: a is not a function</span></span><br><span class="line">alert(b);</span><br><span class="line">alert(b(<span class="number">1</span>));</span><br></pre></td></tr></table></figure>

<h3 id="3-3-示例-3"><a href="#3-3-示例-3" class="headerlink" title="3.3 示例 3"></a>3.3 示例 3</h3><blockquote>
<p>示例3原型：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">分析： js永远是先解析声明式函数，再解析变量。 </span><br><span class="line">执行顺序： </span><br><span class="line">1.解析函数a; </span><br><span class="line">2.声明变量var a;因为a此时没有被赋值，所以它还是指向原来的值。即function a; </span><br><span class="line">3.alert(a);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>示例3变形：</p>
</blockquote>
<p>　　如果要证明js解析的顺序，很简单，把代码位置换一下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span> (<span class="params">x</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x * <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line">alert (a);</span><br></pre></td></tr></table></figure>

<h3 id="3-4-示例-4"><a href="#3-4-示例-4" class="headerlink" title="3.4 示例 4"></a>3.4 示例 4</h3><blockquote>
<p>示例4原型：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">分析：在函数内部可以引用一个对象，它是arguments类似数组，但不是数组。它代表了函数实际接收参数的集合。可以通过下标对相应参数进行访问。如果修改此对象某些属性，如arguments[index]，则被传进来的第index(如果有的话，下标从0开始)变量的值也会被修改。 </span><br><span class="line">执行顺序： </span><br><span class="line">1.声明一个函数b; </span><br><span class="line">2.执行函数b(1,2,3);因为这里arguments[2]与变量a引用的是一个值，所以当arguments[2]改变时，a也随之改变。</span><br></pre></td></tr></table></figure>

<h3 id="3-5-示例-5"><a href="#3-5-示例-5" class="headerlink" title="3.5 示例 5"></a>3.5 示例 5</h3><blockquote>
<p>示例5原型：</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">call方法: </span><br><span class="line">语法：call([thisObj[,arg1[, arg2[, [,.argN]]]]]) </span><br><span class="line">定义：调用一个对象的一个方法，以另一个对象替换当前对象。 </span><br><span class="line">说明： call 方法可以用来代替另一个对象调用一个方法。call 方法可将一个函数的对象上下文从初始的上下文改变为由 thisObj 指定的新对象。 </span><br><span class="line">如果没有提供 thisObj 参数，那么 Global 对象被用作 thisObj。</span><br><span class="line"></span><br><span class="line">分析： </span><br><span class="line">call方法接受多个参数，其作用是借用别人的方法当作自己的方法。这样能保证执行的时候this能够指向自己。call方法的第二个参数到最后一个参数是传给借用过来函数的。第一个参数是借用的对象，如果这个对象为空，那么将会作为全局window对象调用。即函数中的this指向window。</span><br></pre></td></tr></table></figure>

<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>mate 标签大集会</title>
    <url>/2017/02/05/article-html-mate/</url>
    <content><![CDATA[<p>　　meta 是 html 语言 head 区的一个辅助性标签。也许你认为这些代码可有可无。其实如果你能够用好meta标签，会给你带来意想不到的效果，meta标签的作用有：搜索引擎优化（SEO），定义页面使用语言，自动刷新并指向新的页面，实现网页转换时的动态效果，控制页面缓冲，网页定级评价，控制网页显示的窗口等！<a id="more"></a></p>
<h2 id="1-meta-标签的组成"><a href="#1-meta-标签的组成" class="headerlink" title="1. meta 标签的组成"></a>1. meta 标签的组成</h2><p>　　meta 标签的组成：meta 标签共有两个属性，它们分别是 http-equiv 属性和 name 属性，不同的属性又有不同的参数值，这些不同的参数值就实现了不同的网页功能。</p>
<h3 id="1-1-name-属性"><a href="#1-1-name-属性" class="headerlink" title="1.1 name 属性"></a>1.1 name 属性</h3><p>　　name 属性主要用于描述网页，与之对应的属性值为 content，content 中的内容主要是便于搜索引擎机器人查找信息和分类信息用的。</p>
<p>　　meta 标签的 name 属性语法格式是：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"参数"</span> <span class="attr">content</span>=<span class="string">"具体的参数值"</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　其中name属性常见有以下几种参数：　</p>
<ul>
<li><p><strong>Keywords(关键字)</strong>：告诉搜索引擎你网页的关键字是什么；</p>
</li>
<li><p><strong>description(网站内容描述)</strong>：告诉搜索引擎你的网站主要内容；</p>
</li>
<li><p><strong>robots(机器人向导)</strong>：告诉搜索机器人哪些页面需要索引，哪些页面不需要索引；</p>
</li>
<li><p><strong>author(作者)</strong>：标注网页的作者。</p>
</li>
</ul>
<h3 id="1-2-http-equiv-属性"><a href="#1-2-http-equiv-属性" class="headerlink" title="1.2 http-equiv 属性"></a>1.2 http-equiv 属性</h3><p>　　http-equiv 顾名思义，相当于 http 的文件头作用，它可以向浏览器传回一些有用的信息，以帮助正确和精确地显示网页内容，与之对应的属性值为 content，content 中的内容其实就是各个参数的变量值。</p>
<p>　　meta 标签的 http-equiv 属性语法格式是：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"参数"</span> <span class="attr">content</span>=<span class="string">"参数变量值"</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　其中 http-equiv 属性常见有以下几种参数：</p>
<ul>
<li><p><strong>Expires(期限)</strong>：可以用于设定网页的到期时间。一旦网页过期，必须到服务器上重新传输。</p>
</li>
<li><p><strong>Pragma(cache模式)</strong>：禁止浏览器从本地计算机的缓存中访问页面内容；</p>
</li>
<li><p><strong>X-UA-Compatible</strong>：是 IE8 的专用标记,用来指定 IE8 浏览器去模拟某个特定版本的IE浏览器的渲染方式；</p>
</li>
<li><p><strong>Refresh(刷新)</strong>：自动刷新并指向新页面；</p>
</li>
<li><p><strong>Set-Cookie(cookie设定)</strong>：如果网页过期，那么存盘的cookie将被删除；</p>
</li>
<li><p><strong>Window-target(显示窗口的设定)</strong>：强制页面在当前窗口以独立页面显示；</p>
</li>
<li><p><strong>content-Type(显示字符集的设定)</strong>：设定页面使用的字符集；</p>
</li>
<li><p><strong>content-Language（显示语言的设定）</strong>：设定页面的语言；</p>
</li>
<li><p><strong>Cache-Control</strong>：指定请求和响应遵循的缓存机制。</p>
</li>
</ul>
<h2 id="2-常用-meta-标签总结"><a href="#2-常用-meta-标签总结" class="headerlink" title="2. 常用 meta 标签总结"></a>2. 常用 meta 标签总结</h2><h3 id="2-1-name-属性"><a href="#2-1-name-属性" class="headerlink" title="2.1 name 属性"></a>2.1 name 属性</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 设置作者姓名及联系方式 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"author"</span> <span class="attr">contect</span>=<span class="string">"name, xxx@163.com"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 页面描述 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"description"</span> <span class="attr">content</span>=<span class="string">"meta元素共有三个可选属性(不超过150字符)"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 页面关键词 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"keywords"</span> <span class="attr">content</span>=<span class="string">"关键字1,关键字2,..."</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 页面生成器 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"generator"</span> <span class="attr">content</span>=<span class="string">"hexo"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 页面修改信息 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"revised"</span> <span class="attr">content</span>=<span class="string">"story,2015/07/22"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 版权信息 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"copyright"</span> <span class="attr">content</span>=<span class="string">"All Rights Reserved"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 页面爬虫设置 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"robots"</span> <span class="attr">content</span>=<span class="string">"index,follow"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- robots的content取值 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- all：文件将被检索，且页面上的链接可以被查询 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- none：文件将不被检索，且页面上的链接不可以被查询 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- index：文件将被检索 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- follow：页面上的链接可以被查询 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- noindex：文件将不被检索，但页面上的链接可以被查询 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- nofollow：文件将被检索，但页面上的链接不可以被查询 --&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-2-http-equiv-属性"><a href="#2-2-http-equiv-属性" class="headerlink" title="2.2 http-equiv 属性"></a>2.2 http-equiv 属性</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 字符编码 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"content-type"</span> <span class="attr">content</span>=<span class="string">"text/html;charset=UTF-8"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"utf-8"</span>&gt;</span><span class="comment">&lt;!--简写版--&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 页面到期时间，必须使用GMT的时间格式 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"expire"</span> <span class="attr">content</span>=<span class="string">"Wed,22Jul201511:11:11GMT"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 页面重刷新，0秒后刷新并跳转 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"refresh"</span> <span class="attr">content</span>=<span class="string">"0;URL=''"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- cookie设置 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"set-cookie"</span> <span class="attr">content</span>=<span class="string">"cookie value=xxx;expires=Wed,22-Jul-201511:11:11GMT；path=/"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 脚本类型 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"Content-Script-Type"</span> <span class="attr">content</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 禁止从本地缓存中读取页面 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"Pragma"</span> <span class="attr">content</span>=<span class="string">"no-cache"</span>&gt;</span> </span><br><span class="line"><span class="comment">&lt;!--清除缓存（再访问这个网站要重新下载！）--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"cache-control"</span> <span class="attr">content</span>=<span class="string">"no-cache, must-revalidate"</span>&gt;</span> </span><br><span class="line"><span class="comment">&lt;!--优先使用 IE 最新版本和 Chrome 内核--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"X-UA-Compatible"</span> <span class="attr">content</span>=<span class="string">"IE=Edge,chrome=1"</span> &gt;</span></span><br><span class="line"><span class="comment">&lt;!--强制页面在当前窗口以独立页面显示，用来防止别人在框架里调用自己的页面--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"Window-target"</span> <span class="attr">content</span>=<span class="string">"_top"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--不让百度转码--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">"Cache-Control"</span> <span class="attr">content</span>=<span class="string">"no-siteapp"</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-3-移动端"><a href="#2-3-移动端" class="headerlink" title="2.3 移动端"></a>2.3 移动端</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width, initial-scale=1.0,maximum-scale=1.0, user-scalable=no"</span>/&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- viewport的content取值 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- width：宽度（数值 / device-width）（200~10000，默认为980px） --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- height：高度（数值 / device-height）（223~10000） --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- initial-scale：初始缩放比例 （0~10） --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- minimum-scale：允许用户缩放到的最小比例 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- maximum-scale：允许用户缩放到的最大比例 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- user-scalable：是否允许用户缩放 (no/yes)  --&gt;</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">&lt;!-- uc强制竖屏 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"screen-orientation"</span> <span class="attr">content</span>=<span class="string">"portrait"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- QQ强制竖屏 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"x5-orientation"</span> <span class="attr">content</span>=<span class="string">"portrait"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- UC强制全屏 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"full-screen"</span> <span class="attr">content</span>=<span class="string">"yes"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- QQ强制全屏 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"x5-fullscreen"</span> <span class="attr">content</span>=<span class="string">"true"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- UC应用模式 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"browsermode"</span> <span class="attr">content</span>=<span class="string">"application"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- QQ应用模式 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"x5-page-mode"</span> <span class="attr">content</span>=<span class="string">"app"</span>&gt;</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">&lt;!-- IOS启用 WebApp 全屏模式 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"apple-mobile-web-app-capable"</span> <span class="attr">content</span>=<span class="string">"yes"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- IOS全屏模式下隐藏状态栏/设置状态栏颜色 content的值为default | black | black-translucent  --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"apple-mobile-web-app-status-bar-style"</span> <span class="attr">content</span>=<span class="string">"black-translucent"</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- IOS添加到主屏后的标题 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"apple-mobile-web-app-title"</span> <span class="attr">content</span>=<span class="string">"标题"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- IOS添加智能 App 广告条 Smart App Banner --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"apple-itunes-app"</span> <span class="attr">content</span>=<span class="string">"app-id=myAppStoreID, affiliate-data=myAffiliateData, app-argument=myURL"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--微软的老式浏览器--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"MobileOptimized"</span> <span class="attr">content</span>=<span class="string">"320"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--针对手持设备优化，主要是针对一些老的不识别 viewport 的浏览器，比如黑莓--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"HandheldFriendly"</span> <span class="attr">content</span>=<span class="string">"true"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 去除iphone 识别数字为号码 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"format-detection"</span> <span class="attr">content</span>=<span class="string">"telephone=no"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 不识别邮箱 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"format-detection"</span> <span class="attr">content</span>=<span class="string">"email=no"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 禁止跳转至地图 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"format-detection"</span> <span class="attr">content</span>=<span class="string">"adress=no"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 可以连写--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"format-detection"</span> <span class="attr">content</span>=<span class="string">"telephone=no,email=no,adress=no"</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--启用360浏览器的极速模式(webkit)--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"renderer"</span> <span class="attr">content</span>=<span class="string">"webkit"</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　以上为平时常用 mate 标签，有遗漏的地方在下面留言就OK，会不断更新… …</p>
<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS：font 总结</title>
    <url>/2017/02/04/article-css-font/</url>
    <content><![CDATA[<p>　　本文记录总结一些平时经常用的字体知识，方便查阅。（<code>font-family</code>、<code>font-size</code>、<code>font-weight</code>等。）</p>
<h2 id="font-family"><a href="#font-family" class="headerlink" title="font-family"></a>font-family</h2><p>　　中文字体多数使用宋体、雅黑，英文用Helvetica，如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123; <span class="attribute">font-family</span>: Microsoft Yahei,SimSun,Helvetica; &#125;</span><br></pre></td></tr></table></figure>

<p>　　那么常用的中文字体的英文名称：</p>
<ul>
<li>宋体      SimSun</li>
<li>黑体      SimHei</li>
<li>微信雅黑   Microsoft Yahei</li>
<li>微软正黑体 Microsoft JhengHei</li>
<li>新宋体    NSimSun</li>
<li>新细明体  MingLiU </li>
<li>细明体    MingLiU</li>
<li>标楷体    DFKai-SB</li>
<li>仿宋     FangSong</li>
<li>楷体     KaiTi</li>
<li>仿宋_GB2312  FangSong_GB2312</li>
<li>楷体_GB2312  KaiTi_GB2312  </li>
</ul>
<h2 id="font-size"><a href="#font-size" class="headerlink" title="font-size"></a>font-size</h2><p>　　移动端字体单位 font-size 选择 px 还是 rem：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*如需适配多种移动设备，建议使用rem。以下为参考值：*/</span></span><br><span class="line">html &#123; font-size: 62.5%; &#125;   //10*16 = 62.5%</span><br><span class="line"><span class="comment">/*设置12px字体   这里注意在rem前要加上对应的px值，解决不支持rem的浏览器的兼容问题，做到优雅降级*/</span></span><br><span class="line"><span class="selector-tag">body</span> &#123; <span class="attribute">font-size</span>:<span class="number">12px</span>; <span class="attribute">font-size</span>:<span class="number">1.2rem</span>; &#125;</span><br></pre></td></tr></table></figure>

<p>　　针对谷歌默认最小字体12px的正确解决方案：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.small-font</span>&#123;</span><br><span class="line">    <span class="attribute">font-size</span>:<span class="number">12px</span>;</span><br><span class="line">    <span class="attribute">-webkit-transform-origin-x</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">-webkit-transform</span>: <span class="built_in">scale</span>(0.90);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*兼容IE7，IE8*/</span></span><br><span class="line"><span class="selector-class">.smallsize-font</span> &#123;</span><br><span class="line">    <span class="attribute">font-size</span>:<span class="number">10.8px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>有趣的 fullPage.js</title>
    <url>/2017/02/03/article-js-fullpage/</url>
    <content><![CDATA[<p>　　如今我们经常能见到全屏网站，尤其是国外网站。这些网站用几幅很大的图片或色块做背景，再添加一些简单的内容，显得格外的高端大气上档次。比如 iPhone 5C 的介绍页面（查看），QQ浏览器的官网站。如果你也希望你的网站能设计成全屏的，显得更上档次，你可以试试 fullPage.js。<a id="more"></a></p>
<p class="tc"><a class="btn btn-success btn-lg" title="演示" href="http://doubleray.win/FullPages/" target="_blank">演 示</a> <a class="btn btn-success btn-lg" title="下载" href="https://github.com/DoubleRayWang/FullPages" target="_blank">下 载</a></p>

<hr><br>

<h2 id="1-简介"><a href="#1-简介" class="headerlink" title="1. 简介"></a>1. 简介</h2><p>　　fullPage.js 是一个基于 jQuery 的插件，它能够很方便、很轻松的制作出全屏网站，主要功能有：</p>
<ul>
<li>支持鼠标滚动</li>
<li>支持前进后退和键盘控制</li>
<li>多个回调函数</li>
<li>支持手机、平板触摸事件</li>
<li>支持 CSS3 动画</li>
<li>支持窗口缩放</li>
<li>窗口缩放时自动调整</li>
<li>可设置滚动宽度、背景颜色、滚动速度、循环选项、回调、文本对齐方式等等</li>
</ul>
<div class="tc"><img src="/styles/images/js/fullpage.png" alt="fullpage.js"></div>

<h2 id="2-兼容性"><a href="#2-兼容性" class="headerlink" title="2. 兼容性"></a>2. 兼容性</h2><h3 id="jQuery-兼容"><a href="#jQuery-兼容" class="headerlink" title="jQuery 兼容"></a>jQuery 兼容</h3><p>　　兼容 jQuery 1.7+。</p>
<h3 id="浏览器兼容"><a href="#浏览器兼容" class="headerlink" title="浏览器兼容"></a>浏览器兼容</h3><div class="table-responsive"><table class="table table-bordered table-hover"><thead><tr><th><img title="IE" src="/styles/images/browser/ie.png" alt="IE"></th><th><img title="Chrome" src="/styles/images/browser/chrome.png" alt="Chrome"></th><th><img title="Firefox" src="/styles/images/browser/firefox.png" alt="Firefox"></th><th><img title="Opera" src="/styles/images/browser/opera.png" alt="Opera"></th><th><img title="Safari" src="/styles/images/browser/safari.png" alt="Safari"></th></tr></thead><tbody style="text-align:center"><tr><td>IE8+ ✔</td><td>Chrome ✔</td><td>Firefox ✔</td><td>Opera ✔</td><td>Safari ✔</td></tr></tbody></table></div>

<h2 id="3-使用方法"><a href="#3-使用方法" class="headerlink" title="3. 使用方法"></a>3. 使用方法</h2><h3 id="1、引入文件"><a href="#1、引入文件" class="headerlink" title="1、引入文件"></a>1、引入文件</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">href</span>=<span class="string">"css/jquery.fullPage.css"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"js/jquery.min.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- jquery.easings.min.js 用于 easing 参数，也可以使用完整的 jQuery UI 代替，如果不需要设置 easing 参数，可去掉改文件 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"js/jquery.easings.min.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 如果 scrollOverflow 设置为 true，则需要引入 jquery.slimscroll.min.js，一般情况下不需要 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"js/jquery.slimscroll.min.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"js/jquery.fullPage.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2、HTML"><a href="#2、HTML" class="headerlink" title="2、HTML"></a>2、HTML</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"dowebok"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"section"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">h3</span>&gt;</span>第一屏<span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"section"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">h3</span>&gt;</span>第二屏<span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"section"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">h3</span>&gt;</span>第三屏<span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"section"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">h3</span>&gt;</span>第四屏<span class="tag">&lt;/<span class="name">h3</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　每个 section 代表一屏，默认显示“第一屏”，如果要指定加载页面时显示的“屏幕”，可以在对应的 section 加上 class=”active”，如：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"section active"</span>&gt;</span>第三屏<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　同时，可以在 section 内加入 slide，如：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"dowebok"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"section"</span>&gt;</span>第一屏<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"section"</span>&gt;</span>第二屏<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"section"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"slide"</span>&gt;</span>第三屏的第一屏<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"slide"</span>&gt;</span>第三屏的第二屏<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"slide"</span>&gt;</span>第三屏的第三屏<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"slide"</span>&gt;</span>第三屏的第四屏<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"section"</span>&gt;</span>第四屏<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3、JavaScript"><a href="#3、JavaScript" class="headerlink" title="3、JavaScript"></a>3、JavaScript</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    $(<span class="string">'#dowebok'</span>).fullpage();</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="4-配置"><a href="#4-配置" class="headerlink" title="4. 配置"></a>4. 配置</h2><h3 id="1、选项"><a href="#1、选项" class="headerlink" title="1、选项"></a>1、选项</h3><div class="table-responsive"><table class="table table-bordered table-hover"><thead><tr><th>选项</th><th style="width:80px">类型</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td>verticalCentered</td><td>字符串</td><td>true</td><td>内容是否垂直居中</td></tr><tr><td>resize</td><td>布尔值</td><td>false</td><td>字体是否随着窗口缩放而缩放</td></tr><tr><td>slidesColor</td><td>函数</td><td>无</td><td>设置背景颜色</td></tr><tr><td>anchors</td><td>数组</td><td>无</td><td>定义锚链接</td></tr><tr><td>scrollingSpeed</td><td>整数</td><td>700</td><td>滚动速度，单位为毫秒</td></tr><tr><td>easing</td><td>字符串</td><td>easeInQuart</td><td>滚动动画方式</td></tr><tr><td>menu</td><td>布尔值</td><td>false</td><td>绑定菜单，设定的相关属性与 anchors 的值对应后，菜单可以控制滚动</td></tr><tr><td>navigation</td><td>布尔值</td><td>false</td><td>是否显示项目导航</td></tr><tr><td>navigationPosition</td><td>字符串</td><td>right</td><td>项目导航的位置，可选 left 或 right</td></tr><tr><td>navigationColor</td><td>字符串</td><td>#000</td><td>项目导航的颜色</td></tr><tr><td>navigationTooltips</td><td>数组</td><td>空</td><td>项目导航的 tip</td></tr><tr><td>slidesNavigation</td><td>布尔值</td><td>false</td><td>是否显示左右滑块的项目导航</td></tr><tr><td>slidesNavPosition</td><td>字符串</td><td>bottom</td><td>左右滑块的项目导航的位置，可选 top 或 bottom</td></tr><tr><td>controlArrowColor</td><td>字符串</td><td>#fff</td><td>左右滑块的箭头的背景颜色</td></tr><tr><td>loopBottom</td><td>布尔值</td><td>false</td><td>滚动到最底部后是否滚回顶部</td></tr><tr><td>loopTop</td><td>布尔值</td><td>false</td><td>滚动到最顶部后是否滚底部</td></tr><tr><td>loopHorizontal</td><td>布尔值</td><td>true</td><td>左右滑块是否循环滑动</td></tr><tr><td>autoScrolling</td><td>布尔值</td><td>true</td><td>是否使用插件的滚动方式，如果选择 false，则会出现浏览器自带的滚动条</td></tr><tr><td>scrollOverflow</td><td>布尔值</td><td>false</td><td>内容超过满屏后是否显示滚动条</td></tr><tr><td>css3</td><td>布尔值</td><td>false</td><td>是否使用 CSS3 transforms 滚动</td></tr><tr><td>paddingTop</td><td>字符串</td><td>0</td><td>与顶部的距离</td></tr><tr><td>paddingBottom</td><td>字符串</td><td>0</td><td>与底部距离</td></tr><tr><td>fixedElements</td><td>字符串</td><td>无</td><td></td></tr><tr><td>normalScrollElements</td><td></td><td>无</td><td></td></tr><tr><td>keyboardScrolling</td><td>布尔值</td><td>true</td><td>是否使用键盘方向键导航</td></tr><tr><td>touchSensitivity</td><td>整数</td><td>5</td><td></td></tr><tr><td>continuousVertical</td><td>布尔值</td><td>false</td><td>是否循环滚动，与 loopTop 及 loopBottom 不兼容</td></tr><tr><td>animateAnchor</td><td>布尔值</td><td>true</td><td></td></tr><tr><td>normalScrollElementTouchThreshold</td><td>整数</td><td>5</td><td></td></tr></tbody></table></div>

<h3 id="2、方法"><a href="#2、方法" class="headerlink" title="2、方法"></a>2、方法</h3><div class="table-responsive"><table class="table table-bordered table-hover"><thead><tr><th>名称</th><th>说明</th></tr></thead><tbody><tr><td>moveSectionUp()</td><td>向上滚动</td></tr><tr><td>moveSectionDown()</td><td>向下滚动</td></tr><tr><td>moveTo(section, slide)</td><td>滚动到</td></tr><tr><td>moveSlideRight()</td><td>slide 向右滚动</td></tr><tr><td>moveSlideLeft()</td><td>slide 向左滚动</td></tr><tr><td>setAutoScrolling()</td><td>设置页面滚动方式，设置为 true 时自动滚动</td></tr><tr><td>setAllowScrolling()</td><td>添加或删除鼠标滚轮/触控板控制</td></tr><tr><td>setKeyboardScrolling()</td><td>添加或删除键盘方向键控制</td></tr><tr><td>setScrollingSpeed()</td><td>定义以毫秒为单位的滚动速度</td></tr></tbody></table></div>


<h3 id="3、回调函数"><a href="#3、回调函数" class="headerlink" title="3、回调函数"></a>3、回调函数</h3><div class="table-responsive"><table class="table table-bordered table-hover"><thead><tr><th>名称</th><th>说明</th></tr></thead><tbody><tr><td>afterLoad</td><td>滚动到某一屏后的回调函数，接收 anchorLink 和 index 两个参数，anchorLink 是锚链接的名称，index 是序号，从1开始计算</td></tr><tr><td>onLeave</td><td><p>滚动前的回调函数，接收 index、nextIndex 和 direction 3个参数：index 是离开的“页面”的序号，从1开始计算；</p><p>nextIndex 是滚动到的“页面”的序号，从1开始计算；</p><p>direction 判断往上滚动还是往下滚动，值是 up 或 down。</p></td></tr><tr><td>afterRender</td><td>页面结构生成后的回调函数，或者说页面初始化完成后的回调函数</td></tr><tr><td>afterSlideLoad</td><td>滚动到某一水平滑块后的回调函数，与 afterLoad 类似，接收 anchorLink、index、slideIndex、direction 4个参数</td></tr><tr><td>onSlideLeave</td><td>某一水平滑块滚动前的回调函数，与 onLeave 类似，接收 anchorLink、index、slideIndex、direction 4个参数</td></tr></tbody></table></div>

<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>常用 JavaScript 效果</title>
    <url>/2017/02/03/article-js-effect/</url>
    <content><![CDATA[<blockquote class="blockquote-center"><p>一直只顾着码效果，都是想到哪儿就写到哪儿，今天对平时用过的效果做一下总结。</p>
</blockquote><a id="more"></a>

<h2 id="1-图片遮罩条"><a href="#1-图片遮罩条" class="headerlink" title="1. 图片遮罩条"></a>1. 图片遮罩条</h2><p>　　仿百度图片随鼠标划入划出方向入出（基于 JQuery）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">shade</span>(<span class="params">parent,item</span>) </span>&#123;</span><br><span class="line">    <span class="comment">//parent为遮罩条的父级元素选择器</span></span><br><span class="line">   <span class="comment">//item为遮罩条的选择名（字符串形式  如：".span"）</span></span><br><span class="line">    parent.on(<span class="string">'mouseenter mouseleave'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">                <span class="keyword">var</span> w = $(<span class="keyword">this</span>).width(),</span><br><span class="line">                    h = $(<span class="keyword">this</span>).height(),</span><br><span class="line">                    x = (e.pageX - <span class="keyword">this</span>.offsetLeft - (w / <span class="number">2</span>)) * (w &gt; h ? (h / w) : <span class="number">1</span>),</span><br><span class="line">                    y = (e.pageY - <span class="keyword">this</span>.offsetTop - (h / <span class="number">2</span>)) * (h &gt; w ? (w / h) : <span class="number">1</span>);</span><br><span class="line">                <span class="keyword">var</span> dirNum = <span class="built_in">Math</span>.round((((<span class="built_in">Math</span>.atan2(y, x) * (<span class="number">180</span> / <span class="built_in">Math</span>.PI)) + <span class="number">180</span>) / <span class="number">90</span>) + <span class="number">3</span>) % <span class="number">4</span>; <span class="comment">//根据鼠标的x，y随机出现0,1,2,3（来判断方向）</span></span><br><span class="line">                <span class="keyword">var</span> eventType = e.type;</span><br><span class="line">                <span class="keyword">var</span> aPos = [&#123;</span><br><span class="line">                    left: <span class="number">0</span>,</span><br><span class="line">                    bottom: h</span><br><span class="line">                &#125;, &#123;</span><br><span class="line">                    left: w,</span><br><span class="line">                    bottom: <span class="number">0</span></span><br><span class="line">                &#125;, &#123;</span><br><span class="line">                    left: <span class="number">0</span>,</span><br><span class="line">                    bottom: -h</span><br><span class="line">                &#125;, &#123;</span><br><span class="line">                    left: -w,</span><br><span class="line">                    bottom: <span class="number">0</span></span><br><span class="line">                &#125;]; <span class="comment">//不同方向的动作放在一个数组里，用随机出来的的下标来取值</span></span><br><span class="line">                <span class="keyword">if</span> (eventType == <span class="string">'mouseenter'</span>) &#123;</span><br><span class="line">                    $(<span class="keyword">this</span>).children(item).css(aPos[dirNum]).stop(<span class="literal">true</span>, <span class="literal">true</span>).animate(&#123;</span><br><span class="line">                        left: <span class="number">0</span>,</span><br><span class="line">                        bottom: <span class="number">0</span></span><br><span class="line">                    &#125;, <span class="number">400</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    $(<span class="keyword">this</span>).children(item).stop(<span class="literal">true</span>, <span class="literal">true</span>).animate(aPos[dirNum], <span class="number">400</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-图片左右动作轮播"><a href="#2-图片左右动作轮播" class="headerlink" title="2. 图片左右动作轮播"></a>2. 图片左右动作轮播</h2><p>　　图片左右动作轮播（基于 JQuery）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">carouselA</span>(<span class="params">parent,next,pre,btn</span>) </span>&#123;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * parent为轮播图片的父级，一般为ul；</span></span><br><span class="line"><span class="comment"> * next为下一张图片的按钮</span></span><br><span class="line"><span class="comment"> * pre为上一张图片的按钮</span></span><br><span class="line"><span class="comment"> * btn为底部小圆点按钮（$('.btn li')）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">    <span class="keyword">var</span> timer = <span class="literal">null</span>,</span><br><span class="line">                pIn = <span class="number">0</span>;</span><br><span class="line">            clone = parent.children().first().clone(); <span class="comment">//克隆了第一个li</span></span><br><span class="line">            parent.append(clone); <span class="comment">//放在了ul的最后</span></span><br><span class="line">            <span class="keyword">var</span> picW = parent.children().width(),</span><br><span class="line">                size = parent.children().size();</span><br><span class="line">            parent.width(picW * (size + <span class="number">1</span>));</span><br><span class="line">            btn.first().css(<span class="string">'background'</span>, <span class="string">'#a10000'</span>);</span><br><span class="line">            ht(parent.children());</span><br><span class="line">            ht(pre);</span><br><span class="line">            ht(next);</span><br><span class="line">            ht(btn);</span><br><span class="line">            timer = setInterval(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                pIn++;</span><br><span class="line">                move();</span><br><span class="line">            &#125;, <span class="number">2000</span>); <span class="comment">//轮播动起来</span></span><br><span class="line">            <span class="function"><span class="keyword">function</span> <span class="title">ht</span>(<span class="params">area</span>) </span>&#123;</span><br><span class="line">                    area.hover(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                        clearInterval(timer);</span><br><span class="line">                    &#125;, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                        timer = setInterval(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                            pIn++;</span><br><span class="line">                            move();</span><br><span class="line">                        &#125;, <span class="number">2000</span>);</span><br><span class="line">                    &#125;);</span><br><span class="line">                &#125; <span class="comment">//悬停动画</span></span><br><span class="line">            btn.hover(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                <span class="keyword">var</span> index = $(<span class="keyword">this</span>).index();</span><br><span class="line">                pIn = index; <span class="comment">//将当前index给全局的索引，否则鼠标离开后会恢复到先前的索引</span></span><br><span class="line">                parent.stop().animate(&#123;</span><br><span class="line">                    left: -index * picW</span><br><span class="line">                &#125;, <span class="number">800</span>);</span><br><span class="line">                $(<span class="keyword">this</span>).css(<span class="string">'background'</span>, <span class="string">'#a10000'</span>).siblings().css(<span class="string">'background'</span>, <span class="string">'#ddd'</span>);</span><br><span class="line">            &#125;); <span class="comment">//按钮动作</span></span><br><span class="line">            pre.click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                pIn++;</span><br><span class="line">                move();</span><br><span class="line">            &#125;); <span class="comment">//向左</span></span><br><span class="line">            next.click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                pIn--;</span><br><span class="line">                move();</span><br><span class="line">            &#125;); <span class="comment">//向右</span></span><br><span class="line">            <span class="function"><span class="keyword">function</span> <span class="title">move</span>(<span class="params"></span>) </span>&#123; <span class="comment">//因为将第一li克隆后放在了最后，所以现在的索引是0-7</span></span><br><span class="line">                <span class="keyword">if</span> (pIn == size) &#123;</span><br><span class="line">                    parent.css(<span class="string">'left'</span>, <span class="number">0</span>);</span><br><span class="line">                    pIn = <span class="number">1</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (pIn == <span class="number">-1</span>) &#123;</span><br><span class="line">                    parent.css(<span class="string">'left'</span>, -(size - <span class="number">1</span>) * picW);</span><br><span class="line">                    pIn = size - <span class="number">2</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                parent.stop().animate(&#123;</span><br><span class="line">                    left: -pIn * picW</span><br><span class="line">                &#125;, <span class="number">800</span>);</span><br><span class="line">                <span class="keyword">if</span> (pIn == size - <span class="number">1</span>) &#123;</span><br><span class="line">                    btn.eq(<span class="number">0</span>).css(<span class="string">'background'</span>, <span class="string">'#a10000'</span>).siblings().css(<span class="string">'background'</span>, <span class="string">'#ddd'</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    btn.eq(pIn).css(<span class="string">'background'</span>, <span class="string">'#a10000'</span>).siblings().css(<span class="string">'background'</span>, <span class="string">'#ddd'</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<p>　　图片左右轮播（原生 js）：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">title</span>&gt;</span>javascript<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="css">	  *&#123;<span class="attribute">margin</span>:<span class="number">0</span>;<span class="attribute">padding</span>:<span class="number">0</span>;<span class="attribute">border</span>:<span class="number">0</span>;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-tag">a</span>&#123;<span class="attribute">text-decoration</span>:none;<span class="attribute">color</span>:<span class="number">#fff</span>;<span class="attribute">font-size</span>:<span class="number">40px</span>;<span class="attribute">line-height</span>:<span class="number">200px</span>;<span class="attribute">display</span>:none;<span class="attribute">text-align</span>:center;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-id">#container</span>&#123;<span class="attribute">width</span>:<span class="number">300px</span>;<span class="attribute">height</span>:<span class="number">200px</span>;<span class="attribute">margin</span>:<span class="number">50px</span> auto;<span class="attribute">position</span>:relative;<span class="attribute">overflow</span>:hidden;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-id">#list</span>&#123;<span class="attribute">width</span>:<span class="number">2100px</span>;<span class="attribute">height</span>:<span class="number">200px</span>;<span class="attribute">position</span>:absolute;<span class="attribute">top</span>:<span class="number">0</span>;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-id">#list</span> <span class="selector-tag">span</span>&#123;<span class="attribute">width</span>:<span class="number">300px</span>;<span class="attribute">height</span>:<span class="number">200px</span>;<span class="attribute">display</span>:inline-block;<span class="attribute">text-align</span>:center;<span class="attribute">font-size</span>:<span class="number">22px</span>;<span class="attribute">float</span>:left;<span class="attribute">color</span>:<span class="number">#fff</span>;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-class">.one</span>&#123;<span class="attribute">background</span>:red;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-class">.two</span>&#123;<span class="attribute">background</span>:orange;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-class">.three</span>&#123;<span class="attribute">background</span>:blue;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-class">.four</span>&#123;<span class="attribute">background</span>:green;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-class">.five</span>&#123;<span class="attribute">background</span>:black;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-id">#buttons</span>&#123;<span class="attribute">width</span>:<span class="number">200px</span>;<span class="attribute">height</span>:<span class="number">30px</span>;<span class="attribute">position</span>:absolute;<span class="attribute">bottom</span>:<span class="number">0px</span>;<span class="attribute">left</span>:<span class="number">100px</span>;<span class="attribute">z-index</span>:<span class="number">9</span>;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-id">#buttons</span> <span class="selector-tag">span</span>&#123;<span class="attribute">display</span>:inline-block;<span class="attribute">cursor</span>:pointer;<span class="attribute">width</span>:<span class="number">12px</span>;<span class="attribute">height</span>:<span class="number">12px</span>;<span class="attribute">border-radius</span>:<span class="number">6px</span>;<span class="attribute">background</span>: <span class="number">#2a2a2a</span>&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-id">#prev</span>&#123;<span class="attribute">width</span>:<span class="number">40px</span>;<span class="attribute">height</span>:<span class="number">200px</span>;<span class="attribute">position</span>:absolute;<span class="attribute">left</span>:<span class="number">0px</span>;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-id">#next</span>&#123;<span class="attribute">width</span>:<span class="number">40px</span>;<span class="attribute">height</span>:<span class="number">200px</span>;<span class="attribute">position</span>:absolute;<span class="attribute">right</span>:<span class="number">0px</span>;&#125;</span></span><br><span class="line"><span class="css">	  <span class="selector-id">#container</span> <span class="selector-class">.on</span>&#123;<span class="attribute">background</span>:<span class="number">#fff</span>;&#125;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"container"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"list"</span> <span class="attr">style</span>=<span class="string">"left:-300px"</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"five"</span>&gt;</span>我是黑色第五张<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"one"</span>&gt;</span>我是红色第一张<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"two"</span>&gt;</span>我是黄色第二张<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"three"</span>&gt;</span>我是蓝色第三张<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"four"</span>&gt;</span>我是绿色第四张<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"five"</span>&gt;</span>我是黑色第五张<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"one"</span>&gt;</span>我是红色第一张<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"buttons"</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"on"</span> <span class="attr">index</span>=<span class="string">"1"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">index</span>=<span class="string">"2"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">index</span>=<span class="string">"3"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">index</span>=<span class="string">"4"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">index</span>=<span class="string">"5"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">a</span> <span class="attr">id</span>=<span class="string">"prev"</span> <span class="attr">href</span>=<span class="string">"javascript:;"</span>&gt;</span>&amp;lt;<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">a</span> <span class="attr">id</span>=<span class="string">"next"</span> <span class="attr">href</span>=<span class="string">"javascript:;"</span>&gt;</span>&amp;gt;<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">        <span class="keyword">var</span> container = <span class="built_in">document</span>.getElementById(<span class="string">'container'</span>),</span></span><br><span class="line"><span class="javascript">        	list = <span class="built_in">document</span>.getElementById(<span class="string">'list'</span>),</span></span><br><span class="line"><span class="javascript">        	buttons = <span class="built_in">document</span>.getElementById(<span class="string">'buttons'</span>).getElementsByTagName(<span class="string">'span'</span>),</span></span><br><span class="line"><span class="javascript">        	prev = <span class="built_in">document</span>.getElementById(<span class="string">'prev'</span>),</span></span><br><span class="line"><span class="javascript">        	next = <span class="built_in">document</span>.getElementById(<span class="string">'next'</span>),</span></span><br><span class="line">        	index = 1,</span><br><span class="line"><span class="vbscript">        	<span class="built_in">len</span> = <span class="number">5</span>,</span></span><br><span class="line">        	interval = 3000,</span><br><span class="line"><span class="actionscript">        	animated = <span class="literal">false</span>,</span></span><br><span class="line"><span class="vbscript">        	<span class="built_in">timer</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="actionscript">    	<span class="function"><span class="keyword">function</span> <span class="title">animate</span><span class="params">(offset)</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">    		<span class="keyword">if</span>(offset == <span class="number">0</span>) <span class="keyword">return</span>;</span></span><br><span class="line"><span class="actionscript">    		animated = <span class="literal">true</span>;</span></span><br><span class="line"><span class="actionscript">    		<span class="keyword">var</span> time = <span class="number">150</span>,</span></span><br><span class="line">    			inter = 5,</span><br><span class="line"><span class="vbscript">    			speed = offset/(<span class="built_in">time</span>/inter),</span></span><br><span class="line"><span class="vbscript">    			<span class="built_in">left</span> = parseInt(list.style.<span class="built_in">left</span>) + offset;</span></span><br><span class="line"><span class="actionscript">			<span class="keyword">var</span> go = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="vbscript">				<span class="keyword">if</span>((speed&gt;<span class="number">0</span> &amp;&amp; parseInt(list.style.<span class="built_in">left</span>)&lt;<span class="built_in">left</span>) || (speed&lt;<span class="number">0</span> &amp;&amp; parseInt(list.style.<span class="built_in">left</span>)&gt;<span class="built_in">left</span>))&#123;</span></span><br><span class="line"><span class="javascript">					list.style.left = <span class="built_in">parseInt</span>(list.style.left) + speed + <span class="string">'px'</span>;</span></span><br><span class="line">					setTimeout(go,inter);</span><br><span class="line"><span class="actionscript">				&#125;<span class="keyword">else</span>&#123;</span></span><br><span class="line"><span class="vbscript">					list.style.<span class="built_in">left</span> = <span class="built_in">left</span> + <span class="comment">'px';</span></span></span><br><span class="line"><span class="vbscript">					<span class="keyword">if</span>(<span class="built_in">left</span> &gt; <span class="number">-100</span>)&#123;</span></span><br><span class="line"><span class="vbscript">						list.style.<span class="built_in">left</span> = <span class="number">-300</span>*<span class="built_in">len</span> + <span class="comment">'px';</span></span></span><br><span class="line">					&#125;</span><br><span class="line"><span class="vbscript">					<span class="keyword">if</span>(<span class="built_in">left</span> &lt; (<span class="number">-300</span>*<span class="built_in">len</span>))&#123;</span></span><br><span class="line"><span class="actionscript">						list.style.left = <span class="string">'-300px'</span></span></span><br><span class="line">					&#125;</span><br><span class="line"><span class="actionscript">					animated = <span class="literal">false</span>;</span></span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">			go();</span><br><span class="line">    	&#125;</span><br><span class="line"></span><br><span class="line"><span class="actionscript">    	<span class="function"><span class="keyword">function</span> <span class="title">showButton</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">    		<span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span> ; i&lt;buttons.length ; i++)&#123;</span></span><br><span class="line"><span class="actionscript">    			<span class="keyword">if</span>(buttons[i].className == <span class="string">'on'</span>)&#123;</span></span><br><span class="line"><span class="actionscript">    				buttons[i].className = <span class="string">''</span>;</span></span><br><span class="line"><span class="actionscript">    				<span class="keyword">break</span>;</span></span><br><span class="line">    			&#125;</span><br><span class="line">    		&#125;</span><br><span class="line"><span class="actionscript">    		buttons[index - <span class="number">1</span>].className = <span class="string">'on'</span>;</span></span><br><span class="line">    	&#125;</span><br><span class="line"></span><br><span class="line"><span class="actionscript">    	<span class="function"><span class="keyword">function</span> <span class="title">play</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">    		timer = setTimeout(<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="vbscript">    			<span class="keyword">next</span>.onclick();</span></span><br><span class="line">    			play();</span><br><span class="line">    		&#125;,interval);</span><br><span class="line">    	&#125;</span><br><span class="line"></span><br><span class="line"><span class="actionscript">    	<span class="function"><span class="keyword">function</span> <span class="title">stop</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="vbscript">    		clearTimeout(<span class="built_in">timer</span>);</span></span><br><span class="line">    	&#125;</span><br><span class="line"></span><br><span class="line"><span class="actionscript">    	next.onclick = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">    		<span class="keyword">if</span>(animated) &#123;</span></span><br><span class="line"><span class="actionscript">    			<span class="keyword">return</span>;</span></span><br><span class="line">    		&#125;</span><br><span class="line"><span class="actionscript">    		<span class="keyword">if</span>(index == <span class="number">5</span>)&#123;</span></span><br><span class="line">    			index = 1;</span><br><span class="line"><span class="actionscript">    		&#125;<span class="keyword">else</span>&#123;</span></span><br><span class="line">    			index++;</span><br><span class="line">    		&#125;</span><br><span class="line">    		animate(-300);</span><br><span class="line">    		showButton();</span><br><span class="line">    	&#125;</span><br><span class="line"></span><br><span class="line"><span class="actionscript">    	prev.onclick = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">    		<span class="keyword">if</span>(animated) &#123;</span></span><br><span class="line"><span class="actionscript">    			<span class="keyword">return</span>;</span></span><br><span class="line">    		&#125;</span><br><span class="line"><span class="actionscript">    		<span class="keyword">if</span>(index == <span class="number">1</span>)&#123;</span></span><br><span class="line">    			index = 5;</span><br><span class="line"><span class="actionscript">    		&#125;<span class="keyword">else</span>&#123;</span></span><br><span class="line">    			index--;</span><br><span class="line">    		&#125;</span><br><span class="line">    		animate(300);</span><br><span class="line">    		showButton();</span><br><span class="line">    	&#125;</span><br><span class="line"></span><br><span class="line"><span class="actionscript">    	<span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; buttons.length; i++) &#123;</span></span><br><span class="line"><span class="actionscript">                buttons[i].onclick = <span class="function"><span class="keyword">function</span> <span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">                    <span class="keyword">if</span> (animated) &#123;</span></span><br><span class="line"><span class="actionscript">                        <span class="keyword">return</span>;</span></span><br><span class="line">                    &#125;</span><br><span class="line"><span class="actionscript">                    <span class="keyword">if</span>(<span class="keyword">this</span>.className == <span class="string">'on'</span>) &#123;</span></span><br><span class="line"><span class="actionscript">                        <span class="keyword">return</span>;</span></span><br><span class="line">                    &#125;</span><br><span class="line"><span class="javascript">                    <span class="keyword">var</span> myIndex = <span class="built_in">parseInt</span>(<span class="keyword">this</span>.getAttribute(<span class="string">'index'</span>));</span></span><br><span class="line"><span class="actionscript">                    <span class="keyword">var</span> offset = <span class="number">-300</span> * (myIndex - index);</span></span><br><span class="line"></span><br><span class="line">                    animate(offset);</span><br><span class="line">                    index = myIndex;</span><br><span class="line">                    showButton();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line"><span class="actionscript">    	container.onmouseover = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">    		prev.style.display = next.style.display = <span class="string">'block'</span>;</span></span><br><span class="line"><span class="vbscript">    		<span class="keyword">stop</span>();</span></span><br><span class="line">    	&#125;</span><br><span class="line"><span class="actionscript">    	container.onmouseout = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">    		prev.style.display = next.style.display = <span class="string">'none'</span>;</span></span><br><span class="line">    		play();</span><br><span class="line">		&#125;</span><br><span class="line">    	play();</span><br><span class="line">	<span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="3-返回顶部"><a href="#3-返回顶部" class="headerlink" title="3. 返回顶部"></a>3. 返回顶部</h2><p>　　返回顶部按钮(基于 JQuery)：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">goTop</span>(<span class="params">btn</span>) </span>&#123;</span><br><span class="line">    $(<span class="built_in">window</span>).scroll(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                <span class="keyword">if</span> ($(<span class="built_in">window</span>).scrollTop() &gt; <span class="number">400</span>) &#123;</span><br><span class="line">                    btn.fadeIn(<span class="number">800</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    btn.fadeOut(<span class="number">1000</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;); <span class="comment">//按钮消失和出现</span></span><br><span class="line">            btn.click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">                $(<span class="string">'body,html'</span>).animate(&#123;</span><br><span class="line">                    scrollTop: <span class="number">0</span></span><br><span class="line">                &#125;, <span class="number">500</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<h2 id="4-倒计时"><a href="#4-倒计时" class="headerlink" title="4. 倒计时"></a>4. 倒计时</h2><p>　　获取验证码倒计时(基于 JQuery)：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">countDown</span>(<span class="params">btn</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> one = <span class="literal">true</span>,</span><br><span class="line">        onceGetTime = <span class="number">60</span>;</span><br><span class="line">        <span class="comment">//onceGetTime为倒计时总时间</span></span><br><span class="line">    btn.click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (one) &#123;</span><br><span class="line">            getcode();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">sendCode</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (onceGetTime &gt; <span class="number">1</span>) &#123;</span><br><span class="line">                onceGetTime--;</span><br><span class="line">                gsc.html(onceGetTime + <span class="string">"s"</span> + <span class="string">"后重新获取"</span>);</span><br><span class="line">                timer = setTimeout(<span class="built_in">arguments</span>.callee, <span class="number">1000</span>);</span><br><span class="line">                one = <span class="literal">false</span>;</span><br><span class="line">                gsc.css(&#123;</span><br><span class="line">                    cursor: <span class="string">'default'</span>,</span><br><span class="line">                    background: <span class="string">'#f5f5f5'</span></span><br><span class="line">                &#125;)</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                gsc.html(<span class="string">"再次获取验证码"</span>);</span><br><span class="line">                one = <span class="literal">true</span>;</span><br><span class="line">                gsc.css(&#123;</span><br><span class="line">                    cursor: <span class="string">'pointer'</span>,</span><br><span class="line">                    background: <span class="string">'#ccc'</span></span><br><span class="line">                &#125;)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">getcode</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">            onceGetTime = <span class="number">60</span>;</span><br><span class="line">            sendCode();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="5-复选框的全选和反选"><a href="#5-复选框的全选和反选" class="headerlink" title="5. 复选框的全选和反选"></a>5. 复选框的全选和反选</h2><p>　　实现复选框的全选和反选（基于 JQuery）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 全选</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">allPick</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  $(<span class="string">"[type=checkbox]:checkbox"</span>).each(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.checked = <span class="literal">true</span>;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 全不选</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">unAllPick</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  $(<span class="string">"[type=checkbox]:checkbox"</span>).each(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.checked = <span class="literal">false</span>;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 反转</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">inverserPick</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  $(<span class="string">"[type=checkbox]:checkbox"</span>).each(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.checked = !<span class="keyword">this</span>.checked;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="6-导航滑块（跟随鼠标）"><a href="#6-导航滑块（跟随鼠标）" class="headerlink" title="6. 导航滑块（跟随鼠标）"></a>6. 导航滑块（跟随鼠标）</h2><p>　　跟随鼠标的导航滑块（基于 JQuery）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * slip为滑块</span></span><br><span class="line"><span class="comment"> * nav为单个导航</span></span><br><span class="line"><span class="comment"> * padding为单个导航的左右padding距离</span></span><br><span class="line"><span class="comment"> * index为滑块初始位置(通过为初始位置加 .active 类名)</span></span><br><span class="line"><span class="comment"> * eg：setSlip($('#navSlip'), $('.navList li'), 10, $('.navBig .active'));</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">setSlip</span>(<span class="params">slip, nav, padding, index</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> a = index.parent().index(),</span><br><span class="line">            w = nav.eq(a).width(),</span><br><span class="line">            b = <span class="number">2</span> * a + <span class="number">1</span>;</span><br><span class="line">        slip.width(nav.children().width());</span><br><span class="line">        slip.css(<span class="string">'left'</span>, <span class="built_in">parseInt</span>(nav.eq(a).position().left) + padding * b + <span class="string">'px'</span>);</span><br><span class="line">        nav.hover(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (slip.css(<span class="string">'display'</span>) == <span class="string">'none'</span>) &#123;</span><br><span class="line">                slip.show();</span><br><span class="line">                nav.children().removeClass(<span class="string">'active'</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (navigator.userAgent.indexOf(<span class="string">"MSIE"</span>) != <span class="number">-1</span>) &#123;</span><br><span class="line">                <span class="comment">//IE浏览器下体验animate</span></span><br><span class="line">                slip.stop().animate(&#123;</span><br><span class="line">                    left: <span class="built_in">parseInt</span>($(<span class="keyword">this</span>).position().left) + padding + <span class="string">'px'</span></span><br><span class="line">                &#125;, <span class="number">300</span>)</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">//非IE下体验css3</span></span><br><span class="line">                slip.stop().css(<span class="string">'left'</span>, <span class="built_in">parseInt</span>($(<span class="keyword">this</span>).position().left) + padding + <span class="string">'px'</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (navigator.userAgent.indexOf(<span class="string">"MSIE"</span>) != <span class="number">-1</span>) &#123;</span><br><span class="line">                slip.animate(&#123;</span><br><span class="line">                    left: a * w + b * padding + <span class="string">'px'</span></span><br><span class="line">                &#125;, <span class="number">300</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                slip.css(<span class="string">'left'</span>, a * w + b * padding + <span class="string">'px'</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;)</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="7-输入框字数提示"><a href="#7-输入框字数提示" class="headerlink" title="7. 输入框字数提示"></a>7. 输入框字数提示</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"meArea"</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">label</span> <span class="attr">for</span>=<span class="string">"mArea"</span>&gt;</span>&amp;nbsp;&amp;nbsp; 内容：<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">textarea</span> <span class="attr">id</span>=<span class="string">"mArea"</span> <span class="attr">placeholder</span>=<span class="string">"留言需审核后才会显示"</span> <span class="attr">required</span> <span class="attr">maxlength</span>=<span class="string">"200"</span> <span class="attr">name</span>=<span class="string">"myText"</span>&gt;</span><span class="tag">&lt;/<span class="name">textarea</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"wordN"</span>&gt;</span>已输入<span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">"readyN"</span>&gt;</span>0<span class="tag">&lt;/<span class="name">span</span>&gt;</span>个字符，还可输入<span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">"leftN"</span>&gt;</span>200<span class="tag">&lt;/<span class="name">span</span>&gt;</span>个字符。<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">$(<span class="string">'#mArea'</span>).on(<span class="string">'keyup blur'</span>,<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="javascript">    <span class="keyword">var</span> area=$(<span class="keyword">this</span>);</span></span><br><span class="line"><span class="javascript">    <span class="keyword">var</span> max=<span class="built_in">parseInt</span>(area.attr(<span class="string">'maxlength'</span>),<span class="number">10</span>);<span class="comment">//转化为10进制</span></span></span><br><span class="line"><span class="actionscript">    <span class="keyword">if</span> (max&gt;<span class="number">0</span>)&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">if</span> (area.val().length &gt; max) &#123; <span class="comment">//textarea的文本长度大于maxlength</span></span></span><br><span class="line"><span class="actionscript">            area.val(area.val().substr(<span class="number">0</span>, max)); <span class="comment">//截断textarea的文本重新赋值</span></span></span><br><span class="line">        &#125;</span><br><span class="line"><span class="actionscript">        <span class="keyword">var</span> already=area.val().length,left=max-already;</span></span><br><span class="line"><span class="javascript">        $(<span class="string">'#readyN'</span>).text(already);</span></span><br><span class="line"><span class="javascript">        $(<span class="string">'#leftN'</span>).text(left);</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="8-随机验证码"><a href="#8-随机验证码" class="headerlink" title="8. 随机验证码"></a>8. 随机验证码</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">label</span> <span class="attr">for</span>=<span class="string">"mCheck"</span>&gt;</span>&amp;nbsp;验证码：<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">id</span>=<span class="string">"mCheck"</span> <span class="attr">placeholder</span>=<span class="string">"不区分大小写"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">"seccode"</span>&gt;</span><span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"javascript:;"</span> <span class="attr">id</span>=<span class="string">"changeA"</span>&gt;</span>换一换<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="actionscript"><span class="function"><span class="keyword">function</span> <span class="title">rand</span><span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> i=<span class="number">0</span>, t=<span class="string">''</span>;</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">do</span> &#123;</span></span><br><span class="line"><span class="javascript">        <span class="keyword">var</span> tempNum=<span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random()*<span class="number">123</span>);</span></span><br><span class="line"><span class="handlebars"><span class="xml">        if ((48<span class="tag">&lt;<span class="name">=tempNum&amp;&amp;tempNum</span>&lt;=<span class="string">57)||(65</span>&lt;=<span class="string">tempNum&amp;&amp;tempNum</span>&lt;=<span class="string">90)||(97</span>&lt;=<span class="string">tempNum&amp;&amp;tempNum</span>&lt;=<span class="string">122))&#123;</span></span></span></span></span><br><span class="line"><span class="javascript">            t+= <span class="built_in">String</span>.fromCharCode(tempNum);</span></span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line"><span class="actionscript">    &#125;<span class="keyword">while</span> (i&lt;<span class="number">4</span>);</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">return</span> t;</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="javascript">$(<span class="string">'#seccode'</span>).text(rand());</span></span><br><span class="line"><span class="javascript">$(<span class="string">'#changeA'</span>).click(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="javascript">   $(<span class="string">'#seccode'</span>).text(rand());</span></span><br><span class="line">&#125;);</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="9-超出部分以省略号显示"><a href="#9-超出部分以省略号显示" class="headerlink" title="9. 超出部分以省略号显示"></a>9. 超出部分以省略号显示</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"newsOver"</span>&gt;</span>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusamus, animi beatae dicta dolorem excepturi facilis, illum maiores quia, repellat sit vero voluptate voluptates? Aspernatur et iusto, omnis qui quo velit.<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="actionscript"><span class="function"><span class="keyword">function</span> <span class="title">cut</span><span class="params">(text)</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">    text.each(<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">var</span> maxwidth=<span class="number">55</span>;<span class="comment">//限制的字符数</span></span></span><br><span class="line"><span class="javascript">        <span class="keyword">if</span>($(<span class="keyword">this</span>).text().length&gt;maxwidth)&#123;</span></span><br><span class="line"><span class="javascript">            $(<span class="keyword">this</span>).text($(<span class="keyword">this</span>).text().substring(<span class="number">0</span>,maxwidth));</span></span><br><span class="line"><span class="javascript">            $(<span class="keyword">this</span>).html($(<span class="keyword">this</span>).html()+<span class="string">'... ...'</span>);</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"><span class="javascript">cut($(<span class="string">'.newsOver'</span>));</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　<strong>以上代码都是经本人亲自测试过的，不保证性能最佳，留着做个参考。</strong></p>
<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>common.js 总结</title>
    <url>/2017/02/02/article-js-common/</url>
    <content><![CDATA[<span itemprop="image" itemscope itemtype="http://schema.org/ImageObject"><img itemprop="url image" src="/styles/banner/javascript.jpg" class="full-image" alt="通用脚本" title="JavaScript"><meta itemprop="width" content="auto"><meta itemprop="height" content="auto"></span>

<p>　　本文旨在总结一些在 JavaScript 中常用的<strong>算法</strong>及<strong>通用脚本</strong>。<a id="more"></a></p>
<h2 id="1-rem-布局处理根字体"><a href="#1-rem-布局处理根字体" class="headerlink" title="1. rem 布局处理根字体"></a>1. rem 布局处理根字体</h2><h3 id="1-1-旧版（已过时）"><a href="#1-1-旧版（已过时）" class="headerlink" title="1.1 旧版（已过时）"></a>1.1 旧版（已过时）</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * document.documentElement为html</span></span><br><span class="line"><span class="comment"> * 640为设计尺寸（按需改）</span></span><br><span class="line"><span class="comment"> * 100为初始字体大小方便在css中进行计算（好计算）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">win, doc</span>) </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">change</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">document</span>.documentElement.style.fontSize = <span class="number">100</span> / <span class="number">640</span> * <span class="built_in">document</span>.documentElement.clientWidth + <span class="string">'px'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    doc.addEventListener(<span class="string">'DOMContentLoaded'</span>, change, <span class="literal">false</span>);</span><br><span class="line">    win.addEventListener(<span class="string">'resize'</span>, change, <span class="literal">false</span>);</span><br><span class="line">&#125;)(<span class="built_in">window</span>, <span class="built_in">document</span>);</span><br></pre></td></tr></table></figure>

<h3 id="1-2-新版（推荐）"><a href="#1-2-新版（推荐）" class="headerlink" title="1.2 新版（推荐）"></a>1.2 新版（推荐）</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//这是阿里团队的高清方案布局代码，所谓高清方案就是根据设备屏幕的DPR（设备像素比，又称DPPX，比如dpr=2时，表示1个CSS像素由4个物理像素点组成） 动态设置 html 的font-size, 同时根据设备DPR调整页面的缩放值，进而达到高清效果。</span></span><br><span class="line">! <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">t</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (i[a]) <span class="keyword">return</span> i[a].exports;</span><br><span class="line">        <span class="keyword">var</span> n = i[a] = &#123;</span><br><span class="line">            exports: &#123;&#125;,</span><br><span class="line">            id: a,</span><br><span class="line">            loaded: !<span class="number">1</span></span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">return</span> e[a].call(n.exports, n, n.exports, t), n.loaded = !<span class="number">0</span>, n.exports</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> i = &#123;&#125;;</span><br><span class="line">    <span class="keyword">return</span> t.m = e, t.c = i, t.p = <span class="string">""</span>, t(<span class="number">0</span>)</span><br><span class="line">&#125;([<span class="function"><span class="keyword">function</span>(<span class="params">e, t</span>) </span>&#123;</span><br><span class="line"><span class="meta">    "use strict"</span>;</span><br><span class="line">    <span class="built_in">Object</span>.defineProperty(t, <span class="string">"__esModule"</span>, &#123;</span><br><span class="line">        value: !<span class="number">0</span></span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">var</span> i = <span class="built_in">window</span>;</span><br><span class="line">    t[<span class="string">"default"</span>] = i.flex = <span class="function"><span class="keyword">function</span>(<span class="params">e, t</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> a = e || <span class="number">100</span>,</span><br><span class="line">            n = t || <span class="number">1</span>,</span><br><span class="line">            r = i.document,</span><br><span class="line">            o = navigator.userAgent,</span><br><span class="line">            d = o.match(<span class="regexp">/Android[\S\s]+AppleWebkit\/(\d&#123;3&#125;)/i</span>),</span><br><span class="line">            l = o.match(<span class="regexp">/U3\/((\d+|\.)&#123;5,&#125;)/i</span>),</span><br><span class="line">            c = l &amp;&amp; <span class="built_in">parseInt</span>(l[<span class="number">1</span>].split(<span class="string">"."</span>).join(<span class="string">""</span>), <span class="number">10</span>) &gt;= <span class="number">80</span>,</span><br><span class="line">            p = navigator.appVersion.match(<span class="regexp">/(iphone|ipad|ipod)/gi</span>),</span><br><span class="line">            s = i.devicePixelRatio || <span class="number">1</span>;</span><br><span class="line">        p || d &amp;&amp; d[<span class="number">1</span>] &gt; <span class="number">534</span> || c || (s = <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">var</span> u = <span class="number">1</span> / s,</span><br><span class="line">            m = r.querySelector(<span class="string">'meta[name="viewport"]'</span>);</span><br><span class="line">        m || (m = r.createElement(<span class="string">"meta"</span>), m.setAttribute(<span class="string">"name"</span>, <span class="string">"viewport"</span>), r.head.appendChild(m)), m.setAttribute(<span class="string">"content"</span>, <span class="string">"width=device-width,user-scalable=no,initial-scale="</span> + u + <span class="string">",maximum-scale="</span> + u + <span class="string">",minimum-scale="</span> + u), r.documentElement.style.fontSize = a / <span class="number">2</span> * s * n + <span class="string">"px"</span></span><br><span class="line">    &#125;, e.exports = t[<span class="string">"default"</span>]</span><br><span class="line">&#125;]);</span><br><span class="line">flex(<span class="number">100</span>, <span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<h2 id="2-处理-CSS3-在-JS-中的兼容性（自动加前缀）"><a href="#2-处理-CSS3-在-JS-中的兼容性（自动加前缀）" class="headerlink" title="2. 处理 CSS3 在 JS 中的兼容性（自动加前缀）"></a>2. 处理 CSS3 在 JS 中的兼容性（自动加前缀）</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * obj事件对象</span></span><br><span class="line"><span class="comment"> * name属性名</span></span><br><span class="line"><span class="comment"> * val属性值；</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">setStyle</span>(<span class="params">obj, name, val</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> w = name.charAt(<span class="number">0</span>).toUpperCase() + name.substring(<span class="number">1</span>);</span><br><span class="line">        obj.style[<span class="string">'webkit'</span> + w] = val;</span><br><span class="line">        obj.style[<span class="string">'moz'</span> + w] = val;</span><br><span class="line">        obj.style[<span class="string">'ms'</span> + w] = val;</span><br><span class="line">        obj.style[<span class="string">'o'</span> + w] = val;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-获得从n到m的随机数"><a href="#3-获得从n到m的随机数" class="headerlink" title="3. 获得从n到m的随机数"></a>3. 获得从n到m的随机数</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">rnd</span>(<span class="params">n, m</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">parseInt</span>(<span class="built_in">Math</span>.random() * (n - m) + n)</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="4-角度转弧度（三角函数时用）"><a href="#4-角度转弧度（三角函数时用）" class="headerlink" title="4. 角度转弧度（三角函数时用）"></a>4. 角度转弧度（三角函数时用）</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a2d</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> n * <span class="number">180</span> / <span class="built_in">Math</span>.PI;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="5-勾股定理求斜边长"><a href="#5-勾股定理求斜边长" class="headerlink" title="5. 勾股定理求斜边长"></a>5. 勾股定理求斜边长</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">sideX</span>(<span class="params">x, y</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">Math</span>.sqrt(<span class="built_in">Math</span>.pow(x, <span class="number">2</span>) + <span class="built_in">Math</span>.pow(y, <span class="number">2</span>));</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="6-交换变量值为数字的两个变量的值"><a href="#6-交换变量值为数字的两个变量的值" class="headerlink" title="6. 交换变量值为数字的两个变量的值"></a>6. 交换变量值为数字的两个变量的值</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">swap</span>(<span class="params">a, b</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (a === b) &#123;</span><br><span class="line">            <span class="keyword">return</span> [a, b];</span><br><span class="line">        &#125;</span><br><span class="line">        b = b - a; <span class="comment">// 此处的 b - a中的b和a的值是最初的值</span></span><br><span class="line">        a = a + b; <span class="comment">// a = a + b -a; 实现了将b的值赋给a</span></span><br><span class="line">        b = a - b; <span class="comment">// b = a - (b - a) = 2a - b 相当于 2b = 2a;实现了将a的值赋给b</span></span><br><span class="line">        <span class="keyword">return</span> [a, b];</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="7-数组的冒泡排序方法"><a href="#7-数组的冒泡排序方法" class="headerlink" title="7. 数组的冒泡排序方法"></a>7. 数组的冒泡排序方法</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bubbleSort</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">1</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">var</span> j = <span class="number">0</span>; j &lt; arr.length - i; j++) &#123;</span><br><span class="line">                <span class="keyword">var</span> temp;</span><br><span class="line">                <span class="keyword">if</span> (arr[j] &gt; arr[j + <span class="number">1</span>]) &#123;</span><br><span class="line">                    temp = arr[j];</span><br><span class="line">                    arr[j] = arr[j + <span class="number">1</span>];</span><br><span class="line">                    arr[j + <span class="number">1</span>] = temp;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> arr;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="8-数组的快速排序方法"><a href="#8-数组的快速排序方法" class="headerlink" title="8. 数组的快速排序方法"></a>8. 数组的快速排序方法</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">quickSort</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">        <span class="comment">//如果数组&lt;=1,则直接返回</span></span><br><span class="line">        <span class="keyword">if</span> (arr.length &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> arr;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">var</span> pivotIndex = <span class="built_in">Math</span>.floor(arr.length / <span class="number">2</span>);</span><br><span class="line">        <span class="comment">//找基准，并把基准从原数组删除</span></span><br><span class="line">        <span class="keyword">var</span> pivot = arr.splice(pivotIndex, <span class="number">1</span>)[<span class="number">0</span>];</span><br><span class="line">        <span class="comment">//定义左右数组</span></span><br><span class="line">        <span class="keyword">var</span> left = [];</span><br><span class="line">        <span class="keyword">var</span> right = [];</span><br><span class="line"></span><br><span class="line">        <span class="comment">//比基准小的放在left，比基准大的放在right</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (arr[i] &lt;= pivot) &#123;</span><br><span class="line">                left.push(arr[i]);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                right.push(arr[i]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//递归</span></span><br><span class="line">        <span class="keyword">return</span> quickSort(left).concat([pivot], quickSort(right));</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="9-求数组中的最大差值"><a href="#9-求数组中的最大差值" class="headerlink" title="9. 求数组中的最大差值"></a>9. 求数组中的最大差值</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getMaxProfit</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">        <span class="comment">// 定义两个变量，分别存贮最大值和最小值</span></span><br><span class="line">        <span class="keyword">var</span> maxNum = arr[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">var</span> minNum = arr[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (arr[i] &gt; maxNum) &#123;</span><br><span class="line">                maxNum = arr[i];</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (arr[i] &lt; minNum) &#123;</span><br><span class="line">                minNum = arr[i];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> maxNum - minNum;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="10-数组去重的方法"><a href="#10-数组去重的方法" class="headerlink" title="10. 数组去重的方法"></a>10. 数组去重的方法</h2><h3 id="10-1-对象的key的方法"><a href="#10-1-对象的key的方法" class="headerlink" title="10.1 对象的key的方法"></a>10.1 对象的key的方法</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">unique</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> hashTable = &#123;&#125;;</span><br><span class="line">        <span class="keyword">var</span> data = [];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>, l = arr.length; i &lt; l; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!hashTable[arr[i]]) &#123;</span><br><span class="line">                hashTable[arr[i]] = <span class="literal">true</span>;</span><br><span class="line">                data.push(arr[i]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> data;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="10-2-逻辑去重的方法"><a href="#10-2-逻辑去重的方法" class="headerlink" title="10.2 逻辑去重的方法"></a>10.2 逻辑去重的方法</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> temp = [];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (temp.indexOf(arr[i]) == <span class="number">-1</span>) &#123;</span><br><span class="line">                temp.push(arr[i]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> temp;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="11-生成斐波那契数列"><a href="#11-生成斐波那契数列" class="headerlink" title="11. 生成斐波那契数列"></a>11. 生成斐波那契数列</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getFibonacci</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> fibArr = [];</span><br><span class="line">        <span class="keyword">var</span> i = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (i &lt; n) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">                fibArr.push(i);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                fibArr.push(fibarr[i - <span class="number">1</span>] + fibArr[i - <span class="number">2</span>])</span><br><span class="line">            &#125;</span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> fibArr;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="12-获取任意长度的字符串"><a href="#12-获取任意长度的字符串" class="headerlink" title="12. 获取任意长度的字符串"></a>12. 获取任意长度的字符串</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getRandomString</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">        <span class="comment">// 定义随机字符串的字符库</span></span><br><span class="line">        <span class="keyword">var</span> str = <span class="string">'qwertyuiopasdfghjklzxcvbnm1234567890'</span>;</span><br><span class="line">        <span class="comment">// 定义一个临时变量tmp存储生成的随机字符串</span></span><br><span class="line">        <span class="keyword">var</span> tmp = <span class="string">''</span>;</span><br><span class="line">        <span class="comment">//获取str的长度</span></span><br><span class="line">        <span class="keyword">var</span> len = str.length;</span><br><span class="line">        <span class="comment">// 生成一个长度为n的随机字符串</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            tmp += str.charAt(<span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * len));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> tmp;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="13-阶乘的算法"><a href="#13-阶乘的算法" class="headerlink" title="13. 阶乘的算法"></a>13. 阶乘的算法</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">factorialize</span>(<span class="params">num</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> result = num;</span><br><span class="line">    <span class="keyword">if</span> (num &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (num === <span class="number">0</span> || num === <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">while</span> (num &gt; <span class="number">1</span>) &#123;</span><br><span class="line">            num--;</span><br><span class="line">            result *= num;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="14-判断不同设备加载不同代码"><a href="#14-判断不同设备加载不同代码" class="headerlink" title="14. 判断不同设备加载不同代码"></a>14. 判断不同设备加载不同代码</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> checkMobile = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="keyword">var</span> ua = navigator.userAgent.toLowerCase();</span><br><span class="line">            <span class="keyword">var</span> brower = &#123;&#125;;</span><br><span class="line">            <span class="keyword">if</span> (ua.match(<span class="regexp">/iphone|nokia|sony|ericsson|mot|samsung|sgh|lg|philips|panasonic|alcatel|lenovo|cldc|midp|wap|mobile/i</span>)) &#123;</span><br><span class="line">                brower.mobile = <span class="literal">true</span>;<span class="comment">//判断是否是手机访问</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (ua.match(<span class="regexp">/MicroMessenger/i</span>)) &#123;</span><br><span class="line">                brower.weinxin = <span class="literal">true</span>;	<span class="comment">//判断是不是微信</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (ua.match(<span class="regexp">/iphone|ipad/i</span>)) &#123;</span><br><span class="line">                brower.ios = <span class="literal">true</span>;  <span class="comment">//判断是不是ios系统</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (ua.match(<span class="regexp">/android/i</span>)) &#123;</span><br><span class="line">                brower.android = <span class="literal">true</span>;  <span class="comment">//判断是不是安卓系统</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (ua.match(<span class="regexp">/Windows Phone/i</span>)) &#123;</span><br><span class="line">                brower.wp = <span class="literal">true</span>;  <span class="comment">//判断是不是windows系统</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> brower;</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">if</span>(!!checkMobile().mobile)&#123;</span><br><span class="line">            <span class="built_in">window</span>.location.href = <span class="string">"mobile/index_m.html"</span>;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>理解 Web 缓存</title>
    <url>/2017/01/29/article-web-cache/</url>
    <content><![CDATA[<p>　　提到 <code>Web</code> 缓存大家都会觉得很简单，不就是检查资源是否有缓存，如果有就加以利用。如果追究下去，多数人还能扯出 <code>cache-control, last-modified, etag</code>之类的名词，但如果真的考究一下这些字段之间有什么区别，又是怎么工作的，其实很多人只有一个极为模糊的认识，所以写一篇文章来梳理一下 <code>Web</code> 缓存的工作方式。<a id="more"></a></p>
<blockquote>
<p><strong>缓存不仅仅是浏览器的事情</strong></p>
</blockquote>
<p>　　很多人对 Web 缓存的认识就是浏览器判断是否使用缓存，但缓存不是浏览器本身能够完成的事情，因为在没有服务器端的其他信息的情况下浏览器是无法判断一个资源是否过期的。代理缓存，CDN 缓存等不在本文的讨论范围内。</p>
<h2 id="Headers"><a href="#Headers" class="headerlink" title="Headers"></a>Headers</h2><p>　　因为针对缓存的控制需要浏览器和服务器端协同完成，所以他们需要一个传递的信息的方式，事实上目前的 Web 缓存主要通过 <code>Headers</code> 来传递信息。</p>
<ul>
<li><strong>以知乎的一张图片资源为例</strong></li>
</ul>
<p>　　随便以知乎的一张图片资源为例，我们可以打开 <code>Chrome</code> 的开发者工具中的 <code>network</code>，随便找一个 200(from cache) 的资源，可以看到请求报文和响应报文的 <code>Headers</code>。</p>
<img src="/styles/images/web/cache_1.png" alt="Headers">

<h3 id="cache-control-和-max-age"><a href="#cache-control-和-max-age" class="headerlink" title="cache-control 和 max-age"></a>cache-control 和 max-age</h3><p>　　<code>cache-control</code> 大概是最广为人知的控制缓存的 <code>Headers</code> 了，这也是最简单的缓存控制策略，即浏览器通过最大生存时间来判断资源的缓存是否有效。</p>
<p>　　如图所见，来自服务器端的 <code>response headers</code> 的 <code>Headers</code> 中有 <code>cache-control:public, max-age=31536000</code>，这就是告诉浏览器这个资源的生存时间，在这个时间以内，浏览器不需要向服务器端再做任何确认，直接使用即可。下面我们也可以看到 <code>request headers</code> 一栏是空的。因为浏览器根本没有发出请求，这里显示的 <code>response headers</code> 是之前的请求中缓存的。</p>
<p>　　<code>cache-control</code> 除了 <code>max-age</code> 外还有一些其他的参数，本文暂不阐述。</p>
<h3 id="expires"><a href="#expires" class="headerlink" title="expires"></a>expires</h3><p>　　我们注意到在缓存的 response headers 里还有一个 Expires:Mon, 29 Jan 2018 07:38:47 GMT 字段。这个字段的意义实际上和 cache-control: max-age 的效果是相似的，在指定的时间之前浏览器都可以认为缓存是有效的。但当两个字段同时存在时，expires 会被 cache-control 覆盖。</p>
<p>　　那么为什么知乎要同时设置两个字段呢？由于 expires 是 HTTP/1.0 定义的而 cache-control 是 HTTP/1.1 定义的，我猜测可能是为了保持尽可能大的兼容性（待考证）。</p>
<h3 id="304-Not-Modified"><a href="#304-Not-Modified" class="headerlink" title="304 Not Modified"></a>304 Not Modified</h3><p>　　上面的缓存策略只能很简单的让浏览器来确定缓存是否有效，而浏览器能够依赖的只有上次请求时服务器端留给它的资源存活时间。我们不能把存活时间设成永远，因为可能什么时候我们会更新资源，但隔一段时间重新请求一次并没有改变的资源同样浪费带宽。所以我们必须要有让服务器告诉浏览器缓存仍然有效的方法，那便是 304 Not Modified。</p>
<p>　　在服务器端判断缓存仍然有效时将会返回状态码 304 的响应。</p>
<p>　　那么服务器如何判断浏览器持有的缓存是否有效呢？这就需要浏览器将一些信息传递给服务器。</p>
<h3 id="If-None-Match-ETag"><a href="#If-None-Match-ETag" class="headerlink" title="If-None-Match/ETag"></a>If-None-Match/ETag</h3><p>　　知乎采用的是 ETag 来判断缓存是否有效，服务器端会在 response headers 中返回 ETag（文件的 hash）：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">etag:&quot;Ft-ifpb80DCLRrATs5py8dg84mtf&quot;</span><br></pre></td></tr></table></figure>

<p>　　而当资源改变时 ETag 也会发生改变。浏览器在发起请求时在 If-None-Match字段携带缓存的 ETag：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">If-None-Match:&quot;2afd9676ae9046ed99dedd4635bb6e4a-gzip&quot;</span><br></pre></td></tr></table></figure>

<p>　　服务器接到请求后如果一致（即资源没有修改），则返回 304 Not Modified，否则返回新的资源（200）。</p>
<h3 id="If-Modified-Since-Last-Modified"><a href="#If-Modified-Since-Last-Modified" class="headerlink" title="If-Modified-Since/Last-Modified"></a>If-Modified-Since/Last-Modified</h3><p>　　除了文件特征码之外也可以通过上次修改时间，服务器端返回资源时通过 Last-Modified 携带资源修改时间，浏览器通过 If-Modified-Since 携带缓存中的资源的修改时间。</p>
<h3 id="ETag-和-Last-Modified-的区别"><a href="#ETag-和-Last-Modified-的区别" class="headerlink" title="ETag 和 Last-Modified 的区别"></a>ETag 和 Last-Modified 的区别</h3><p>　　Last-Modified 有个缺点就是它是精确到秒的，如果一秒中资源多次服务器不会感知到缓存失效，但这不是一个常见的需求。</p>
<p>　　而一般不推荐使用 ETag，原因有几点：</p>
<ul>
<li>Last-Modified 的缺点基本可以忽略不计</li>
<li>ETag 本身需要消耗 CPU，而它的优先级比 Last-Modified 高，当它存在时服务器无论 Last-Modified 是否存在都会使用它判断</li>
<li>ETag 在分布式系统中生成的值可能不一样，会导致缓存失效</li>
</ul>
<h3 id="一图胜千言"><a href="#一图胜千言" class="headerlink" title="一图胜千言"></a>一图胜千言</h3><img src="/styles/images/web/cache_2.png" alt="Cache">

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>　　大公司的静态资源优化方案，基本上要实现这么几个东西：</p>
<ol>
<li><p>配置超长时间的本地缓存 —— 节省带宽，提高性能</p>
</li>
<li><p>版本控制 —— 精确的缓存控制</p>
</li>
<li><p>静态资源 <strong>CDN部署</strong> —— 优化网络请求</p>
</li>
<li><p>更新资源发布路径实现 <strong>非覆盖式发布</strong> —— 平滑升级</p>
</li>
</ol>
<br>

<p>　　<em>本文参考自 <a href="https://codefalling.com/2017/01/understand-web-cache" target="_blank" rel="noopener">M-x codefalling 的 理解 Web 缓存</a> 和 <a href="http://simplyy.space/article/57e8f1777a73cae6287343d0" target="_blank" rel="noopener">SimplyY 的 浏览器缓存介绍精简版</a></em>。</p>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>Web</tag>
      </tags>
  </entry>
  <entry>
    <title>call() 和 apply() 详解</title>
    <url>/2017/01/25/article-js-callapply/</url>
    <content><![CDATA[<p>　　call() 和 apply() 都是为了改变某个函数运行时的 context 即上下文而存在的，换句话说，就是为了改变函数体内部 this 的指向。因为 JavaScript 的函数存在「定义时上下文」和「运行时上下文」以及「上下文是可以改变的」这样的概念。</p>
<p>　　二者的作用完全一样，只是接受参数的方式不太一样!<a id="more"></a></p>
<h2 id="1-定义"><a href="#1-定义" class="headerlink" title="1. 定义"></a>1. 定义</h2><p>apply() : 方法能劫持另外一个对象的方法，继承另外一个对象的属性。</p>
<ul>
<li>Function.apply(obj,args)方法能接收两个参数</li>
<li>obj：这个对象将代替Function类里this对象</li>
<li>args：这个是数组，它将作为参数传给Function（args–&gt;arguments）</li>
</ul>
<p>call() : 和 apply() 的意思一样,只不过是参数列表不一样。</p>
<ul>
<li>Function.call(obj,[param1[,param2[,…[,paramN]]]])</li>
<li>obj：这个对象将代替Function类里this对象</li>
<li>params：这个是一个参数列表</li>
</ul>
<h2 id="2-应用"><a href="#2-应用" class="headerlink" title="2. 应用"></a>2. 应用</h2><p>　　<strong>首先要非常明确几个概念</strong>：</p>
<ul>
<li><p>JS 是面向对象语言；</p>
</li>
<li><p>JS 的函数本质是对象，有对象就会有方法；</p>
</li>
<li><p>call() 和 apply() 方法是JS函数对象的特有的方法，也就是自己调用自己。</p>
</li>
</ul>
<p>　　以下是一段很简单的代码： </p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">writeZhihu</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">   <span class="built_in">console</span>.log(<span class="keyword">this</span>)</span><br><span class="line">&#125;</span><br><span class="line">writeZhihu(); <span class="comment">// [Object global] 在浏览器就是window; 在nodeJS中就是指node;</span></span><br></pre></td></tr></table></figure>

<p>　　大家都知道在JS函数中都会有一个固定的实参this，这个this是指什么？</p>
<blockquote>
<p>因为我们在全局环境中声明的这个函数，这个函数目前不属于任何对象，只属于全局对象；在浏览器环境中，该函数他爹就是 window;在 nodeJS 环境中就是 node;</p>
</blockquote>
<p>　　因为我们要面向对象编程么？编程就要声明对象，有对象就有方法，或者说对象就要调用函数。好了，以下我们就要给这个函数找个新爹；</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">writeZhihu</span>(<span class="params"></span>)</span>&#123;  <span class="comment">//声明一个写知乎的能力函数；</span></span><br><span class="line">   <span class="built_in">console</span>.log(<span class="keyword">this</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> author =&#123;<span class="attr">name</span>:<span class="string">"Zhangwei"</span>&#125; <span class="comment">//我们声明一个对象，这个人的名字叫Zhangwei;</span></span><br><span class="line">writeZhihu.call(author);  <span class="comment">//所以当writeZhihu()这个函数自己调用自己的时候，同时传入一个对象的时候，我就牛逼了，我就有写作的超能力了。</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//这时候console.log(this)的this打印出来就是&#123;name:"Zhangwei"&#125;；</span></span><br><span class="line"><span class="comment">//writeZhihu()这个函数表示有了归属感，么么哒。</span></span><br></pre></td></tr></table></figure>

<p>　　那么这个方法的意义是什么？</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">writeZhihu</span>(<span class="params"></span>)</span>&#123;  <span class="comment">//写知乎的能力不可能就一个人拥有吧，很多人都可以啊？</span></span><br><span class="line">   <span class="built_in">console</span>.log(<span class="keyword">this</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> author1 =&#123;<span class="attr">name</span>:<span class="string">"Zhangwei"</span>&#125;</span><br><span class="line"><span class="keyword">var</span> author2 =&#123;<span class="attr">name</span>:<span class="string">"Mayun"</span>&#125;</span><br><span class="line"><span class="keyword">var</span> author3 =&#123;<span class="attr">name</span>:<span class="string">"Mahuateng"</span>&#125;<span class="comment">//我们创建很多个对象；</span></span><br><span class="line"></span><br><span class="line">writeZhihu.call(author1);  <span class="comment">//&#123;name:"Zhangwei"&#125;</span></span><br><span class="line">writeZhihu.call(author2);   <span class="comment">//&#123;name:"Mayun"&#125;</span></span><br><span class="line">writeZhihu.call(author3);   <span class="comment">//&#123;name:"Mahuateng"&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//这样这些人就有写知乎的能力了，writeZhihu()函数表示好多土豪包养我呀！！！</span></span><br></pre></td></tr></table></figure>

<p>　　那么这些人，每个人写知乎的内容都应该不一样吧，所以这就涉及到传参数的问题；</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">writeZhihu</span>(<span class="params">header,content</span>)</span>&#123;  <span class="comment">//写知乎的能力不可能就一个人拥有吧，很多人都可以啊？</span></span><br><span class="line">   <span class="built_in">console</span>.log(<span class="keyword">this</span>.name+<span class="string">"的文章"</span>+header+<span class="string">"，内容："</span>+content)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> author1 =&#123;<span class="attr">name</span>:<span class="string">"Zhangwei"</span>&#125;</span><br><span class="line"><span class="keyword">var</span> author2 =&#123;<span class="attr">name</span>:<span class="string">"Mayun"</span>&#125;</span><br><span class="line"><span class="keyword">var</span> author3 =&#123;<span class="attr">name</span>:<span class="string">"Mahuateng"</span>&#125;<span class="comment">//我们创建很多个对象；</span></span><br><span class="line"></span><br><span class="line">writeZhihu.call(author1,<span class="string">"《JS权威指南》"</span>,<span class="string">"js不拉不拉..."</span>);  </span><br><span class="line">writeZhihu.call(author2,<span class="string">"《老司机带带我》"</span>,<span class="string">"江南皮革厂倒闭了..."</span>);  </span><br><span class="line">writeZhihu.call(author3,<span class="string">"《不转不是中国人》"</span>,<span class="string">"14亿中国人都看哭了..."</span>);   </span><br><span class="line"></span><br><span class="line"><span class="comment">/*那么打印出来的内容就是：</span></span><br><span class="line"><span class="comment">1.zhangwei的文章《JS权威指南》，内容：js不拉不拉...</span></span><br><span class="line"><span class="comment">2.Mayun的文章《老司机带带我》，内容：江南皮革厂倒闭了...</span></span><br><span class="line"><span class="comment">3.Mahuateng的文章《不转不是中国人》，内容：14亿中国人都看哭了...*/</span></span><br></pre></td></tr></table></figure>

<p>　　那么好了，问题来了，尼玛，你标题不是说call()方法和apply()方法吗？为什么一直都在说call()方法，apply()方法喂狗了吗？</p>
<h3 id="call-和apply-的方法的区别"><a href="#call-和apply-的方法的区别" class="headerlink" title="call()和apply()的方法的区别"></a>call()和apply()的方法的区别</h3><p>　　<strong>call()和apply()的方法的区别在于参数的传递；</strong></p>
<p>　　call()方法有个缺陷就是传实参的时候走位比较耿直，比如我要传6个实参(header, content,license,platform,date,language;)；</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">writeZhihu.call(author1,<span class="string">"《五年高考三年模拟》"</span>,<span class="string">"我想你最爱"</span>,<span class="string">"教育部"</span>,<span class="string">"知乎"</span>,<span class="string">"2016"</span>,<span class="string">"Chinese"</span>);</span><br></pre></td></tr></table></figure>

<p>　　是不是觉得这样传参数很傻逼。最要命的是，如果有的对象传递参数的数量的3个有的是4有的是5个，你是不是就彻底整懵逼了。</p>
<p>　　apply() 方法就是解决这个问题的，apply() 方法实参的传递是数组传递；</p>
<p>　　这个时候就可以这样咯：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> args = [<span class="string">"《五年高考三年模拟》"</span>,<span class="string">"我想你最爱"</span>,<span class="string">"教育部"</span>,<span class="string">"知乎"</span>,<span class="string">"2016"</span>,<span class="string">"Chinese"</span>];</span><br><span class="line"><span class="keyword">var</span> args2 = [<span class="string">"《黄冈密卷》"</span>,<span class="string">"童年阴影"</span>,<span class="string">"教育部"</span>,<span class="string">"2016"</span>,<span class="string">"Chinese"</span>];</span><br><span class="line">writeZhihu.apply(author,args);</span><br><span class="line">writeZhihu.apply(author,args2)</span><br></pre></td></tr></table></figure>

<h2 id="3-总结"><a href="#3-总结" class="headerlink" title="3. 总结"></a>3. 总结</h2><p>　　这个一定要多敲代码，理解，目前我的理解是aplly,call都是调用别人的方法并且讲方法里面的this改变，两者传参数不同，正因为传参数不同，所以可以用aplly用在一些目标函数只需要接受n个参数列表，而不接受数组的函数里面，文章中部分内容参考自<a href="http://www.cnblogs.com/xiaohongwu/archive/2011/06/15/2081237.html" target="_blank" rel="noopener">这里</a>、<a href="http://ghmagical.com/article/page/id/UPLfoGI9vJ91" target="_blank" rel="noopener">JS中的call、apply、bind方法</a>。</p>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 严格模式（strict）</title>
    <url>/2017/01/24/article-js-strict/</url>
    <content><![CDATA[<p>　　JavaScript 严格模式（strict mode）即在严格的条件下运行。</p>
<p>　　本文转自 <a href="http://www.ruanyifeng.com/blog/" target="_blank" rel="noopener">阮一峰的网络日志</a> 的 <a href="http://www.ruanyifeng.com/blog/2013/01/javascript_strict_mode.html" target="_blank" rel="noopener">Javascript 严格模式详解</a> ，参考文档<a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Strict_mode#Changes_in_strict_mode" target="_blank" rel="noopener">Strict mode</a>。<a id="more"></a></p>
<h2 id="1-概述"><a href="#1-概述" class="headerlink" title="1. 概述"></a>1. 概述</h2><p>　　除了正常运行模式，<code>ECMAscript 5</code>添加了第二种运行模式：<strong>“严格模式”</strong>（<code>strict mode</code>）。顾名思义，这种模式使得<code>Javascript</code>在更严格的条件下运行。</p>
<p>　　设立”严格模式”的目的，主要有以下几个：</p>
<ul>
<li>消除<code>Javascript</code>语法的一些不合理、不严谨之处，减少一些怪异行为;</li>
<li>消除代码运行的一些不安全之处，保证代码运行的安全；</li>
<li>提高编译器效率，增加运行速度；</li>
<li>为未来新版本的<code>Javascript</code>做好铺垫。</li>
</ul>
<p>　　“严格模式”体现了<code>Javascript</code>更合理、更安全、更严谨的发展方向，包括 IE 10 在内的主流浏览器，都已经支持它，许多大项目已经开始全面拥抱它。</p>
<p>　　另一方面，同样的代码，在”严格模式”中，可能会有不一样的运行结果；一些在”正常模式”下可以运行的语句，在”严格模式”下将不能运行。掌握这些内容，有助于更细致深入地理解<code>Javascript</code>，让你变成一个更好的程序员。</p>
<h2 id="2-进入标志"><a href="#2-进入标志" class="headerlink" title="2. 进入标志"></a>2. 进入标志</h2><p>　　进入”严格模式”的标志，是下面这行语句：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　"use strict"</span>;</span><br></pre></td></tr></table></figure>

<p>　　老版本的浏览器会把它当作一行普通字符串，加以忽略。</p>
<h2 id="3-如何调用"><a href="#3-如何调用" class="headerlink" title="3. 如何调用"></a>3. 如何调用</h2><p>　　“严格模式”有两种调用方法，适用于不同的场合。</p>
<h3 id="3-1-针对整个脚本文件"><a href="#3-1-针对整个脚本文件" class="headerlink" title="3.1 针对整个脚本文件"></a>3.1 针对整个脚本文件</h3><p>　　将<code>&quot;use strict&quot;</code>放在脚本文件的第一行(之前有任何代码（包括一个空的分号）都不会启用严格模式)，则整个脚本都将以”严格模式”运行。如果这行语句不在第一行，则无效，整个脚本以”正常模式”运行。如果不同模式的代码文件合并成一个文件，这一点需要特别注意。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">　<span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript"><span class="meta">　　　　"use strict"</span>;</span></span><br><span class="line"><span class="javascript">　　　　<span class="built_in">console</span>.log(<span class="string">"这是严格模式。"</span>);</span></span><br><span class="line">　　<span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">　　<span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">　　　　<span class="built_in">console</span>.log(<span class="string">"这是正常模式。"</span>);kly, it<span class="string">'s almost 2 years ago now. I can admit it now - I run it on my school'</span>s network that has about <span class="number">50</span> computers.</span></span><br><span class="line">　　<span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　上面的代码表示，一个网页中依次有两段<code>Javascript</code>代码。前一个<code>script</code>标签是严格模式，后一个不是。</p>
<h3 id="3-2-针对单个函数"><a href="#3-2-针对单个函数" class="headerlink" title="3.2 针对单个函数"></a>3.2 针对单个函数</h3><p>　　将<code>&quot;use strict&quot;</code>放在函数体的第一行，则整个函数以”严格模式”运行。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">strict</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line"><span class="meta">　　　　"use strict"</span>;</span><br><span class="line">　　　　<span class="keyword">return</span> <span class="string">"这是严格模式。"</span>;</span><br><span class="line">　　&#125;</span><br><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">notStrict</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">　　　　<span class="keyword">return</span> <span class="string">"这是正常模式。"</span>;</span><br><span class="line">　　&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-3-脚本文件的变通写法"><a href="#3-3-脚本文件的变通写法" class="headerlink" title="3.3 脚本文件的变通写法"></a>3.3 脚本文件的变通写法</h3><p>　　因为第一种调用方法不利于文件合并，所以更好的做法是，借用第二种方法，将整个脚本文件放在一个立即执行的匿名函数之中。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">　　(<span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="meta">　　　　"use strict"</span>;</span><br><span class="line">　　　　<span class="comment">// some code here</span></span><br><span class="line"></span><br><span class="line">　　 &#125;)();</span><br></pre></td></tr></table></figure>

<h2 id="4-语法和行为改变"><a href="#4-语法和行为改变" class="headerlink" title="4. 语法和行为改变"></a>4. 语法和行为改变</h2><p>　　严格模式对<code>Javascript</code>的语法和行为，都做了一些改变。</p>
<h3 id="4-1-全局变量显式声明"><a href="#4-1-全局变量显式声明" class="headerlink" title="4.1 全局变量显式声明"></a>4.1 全局变量显式声明</h3><p>　　在正常模式中，如果一个变量没有声明就赋值，默认是全局变量。严格模式禁止这种用法，全局变量必须显式声明。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">"use strict"</span>;</span><br><span class="line">　　v = <span class="number">1</span>; <span class="comment">// 报错，v未声明</span></span><br><span class="line">　　<span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++) &#123; <span class="comment">// 报错，i未声明</span></span><br><span class="line">　　&#125;</span><br></pre></td></tr></table></figure>

<p>　　因此，严格模式下，变量都必须先用<code>var</code>命令声明，然后再使用。</p>
<h3 id="4-2-静态绑定"><a href="#4-2-静态绑定" class="headerlink" title="4.2 静态绑定"></a>4.2 静态绑定</h3><p>　　<code>Javascript</code>语言的一个特点，就是允许”动态绑定”，即某些属性和方法到底属于哪一个对象，不是在编译时确定的，而是在运行时（<code>runtime</code>）确定的。</p>
<p>　　严格模式对动态绑定做了一些限制。某些情况下，只允许静态绑定。也就是说，属性和方法到底归属哪个对象，在编译阶段就确定。这样做有利于编译效率的提高，也使得代码更容易阅读，更少出现意外。</p>
<p>　　具体来说，涉及以下几个方面。</p>
<h4 id="4-2-1-禁止使用with语句"><a href="#4-2-1-禁止使用with语句" class="headerlink" title="4.2.1 禁止使用with语句"></a>4.2.1 禁止使用with语句</h4><p>　　因为<code>with</code>语句无法在编译时就确定，属性到底归属哪个对象。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">var</span> v = <span class="number">1</span>;</span><br><span class="line">　　<span class="keyword">with</span> (o)&#123; <span class="comment">// 语法错误 </span></span><br><span class="line">　　　　v = <span class="number">2</span>;</span><br><span class="line">　　&#125;</span><br></pre></td></tr></table></figure>

<h4 id="4-2-2-创设eval作用域"><a href="#4-2-2-创设eval作用域" class="headerlink" title="4.2.2 创设eval作用域"></a>4.2.2 创设eval作用域</h4><p>　　正常模式下，<code>Javascript</code>语言有两种变量作用域（<code>scope</code>）：全局作用域和函数作用域。</p>
<p>　　严格模式创设了第三种作用域：<code>eval</code>作用域。</p>
<p>　　正常模式下，<code>eval</code>语句的作用域，取决于它处于全局作用域，还是处于函数作用域。严格模式下，<code>eval</code>语句本身就是一个作用域，不再能够生成全局变量了，它所生成的变量只能用于<code>eval</code>内部。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">var</span> x = <span class="number">2</span>;</span><br><span class="line">　　<span class="built_in">console</span>.info(<span class="built_in">eval</span>(<span class="string">"var x = 5; x"</span>)); <span class="comment">// 5</span></span><br><span class="line">　　<span class="built_in">console</span>.info(x); <span class="comment">// 2</span></span><br></pre></td></tr></table></figure>

<h3 id="4-3-增强的安全措施"><a href="#4-3-增强的安全措施" class="headerlink" title="4.3 增强的安全措施"></a>4.3 增强的安全措施</h3><h4 id="4-3-1-禁止this关键字指向全局对象"><a href="#4-3-1-禁止this关键字指向全局对象" class="headerlink" title="4.3.1 禁止this关键字指向全局对象"></a>4.3.1 禁止this关键字指向全局对象</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">　　　　<span class="keyword">return</span> !<span class="keyword">this</span>;</span><br><span class="line">　　&#125; </span><br><span class="line">　　<span class="comment">// 返回false，因为"this"指向全局对象，"!this"就是false</span></span><br><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>)</span>&#123; </span><br><span class="line"><span class="meta">　　　　"use strict"</span>;</span><br><span class="line">　　　　<span class="keyword">return</span> !<span class="keyword">this</span>;</span><br><span class="line">　　&#125; </span><br><span class="line">　　<span class="comment">// 返回true，因为严格模式下，this的值为undefined，所以"!this"为true。</span></span><br></pre></td></tr></table></figure>

<p>　　因此，使用构造函数时，如果忘了加<code>new</code>，<code>this</code>不再指向全局对象，而是报错。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line"><span class="meta">　　　　"use strict"</span>;</span><br><span class="line">　　　　<span class="keyword">this</span>.a = <span class="number">1</span>;</span><br><span class="line">　　&#125;;</span><br><span class="line">　　f();<span class="comment">// 报错，this未定义</span></span><br></pre></td></tr></table></figure>

<h4 id="4-3-2-禁止在函数内部遍历调用栈"><a href="#4-3-2-禁止在函数内部遍历调用栈" class="headerlink" title="4.3.2 禁止在函数内部遍历调用栈"></a>4.3.2 禁止在函数内部遍历调用栈</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line"><span class="meta">　　　　"use strict"</span>;</span><br><span class="line">　　　　f1.caller; <span class="comment">// 报错</span></span><br><span class="line">　　　　f1.arguments; <span class="comment">// 报错</span></span><br><span class="line">　　&#125;</span><br><span class="line">　　f1();</span><br></pre></td></tr></table></figure>

<h3 id="4-4-禁止删除变量"><a href="#4-4-禁止删除变量" class="headerlink" title="4.4 禁止删除变量"></a>4.4 禁止删除变量</h3><p>　　严格模式下无法删除变量。只有<code>configurable</code>设置为<code>true</code>的对象属性，才能被删除。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">var</span> x;</span><br><span class="line">　　<span class="keyword">delete</span> x; <span class="comment">// 语法错误</span></span><br><span class="line">　　<span class="keyword">var</span> o = <span class="built_in">Object</span>.create(<span class="literal">null</span>, &#123;<span class="string">'x'</span>: &#123;</span><br><span class="line">　　　　　　value: <span class="number">1</span>,</span><br><span class="line">　　　　　　configurable: <span class="literal">true</span></span><br><span class="line">　　&#125;&#125;);</span><br><span class="line">　　<span class="keyword">delete</span> o.x; <span class="comment">// 删除成功</span></span><br></pre></td></tr></table></figure>

<h3 id="4-5-显式报错"><a href="#4-5-显式报错" class="headerlink" title="4.5 显式报错"></a>4.5 显式报错</h3><p>　　正常模式下，对一个对象的只读属性进行赋值，不会报错，只会默默地失败。严格模式下，将报错。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line"></span><br><span class="line">　　<span class="keyword">var</span> o = &#123;&#125;;</span><br><span class="line">　　<span class="built_in">Object</span>.defineProperty(o, <span class="string">"v"</span>, &#123; <span class="attr">value</span>: <span class="number">1</span>, <span class="attr">writable</span>: <span class="literal">false</span> &#125;);</span><br><span class="line">　　o.v = <span class="number">2</span>; <span class="comment">// 报错</span></span><br></pre></td></tr></table></figure>

<p>　　严格模式下，对一个使用<code>getter</code>方法读取的属性进行赋值，会报错。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">var</span> o = &#123;</span><br><span class="line">　　　　<span class="keyword">get</span> v() &#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span><br><span class="line">　　&#125;;</span><br><span class="line">　　o.v = <span class="number">2</span>; <span class="comment">// 报错</span></span><br></pre></td></tr></table></figure>

<p>　　严格模式下，对禁止扩展的对象添加新属性，会报错。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">var</span> o = &#123;&#125;;</span><br><span class="line">　　<span class="built_in">Object</span>.preventExtensions(o);</span><br><span class="line">　　o.v = <span class="number">1</span>; <span class="comment">// 报错</span></span><br></pre></td></tr></table></figure>

<p>　　严格模式下，删除一个不可删除的属性，会报错。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">delete</span> <span class="built_in">Object</span>.prototype; <span class="comment">// 报错</span></span><br></pre></td></tr></table></figure>

<h3 id="4-6-重名错误"><a href="#4-6-重名错误" class="headerlink" title="4.6 重名错误"></a>4.6 重名错误</h3><p>　　严格模式新增了一些语法错误。</p>
<h4 id="4-6-1-对象不能有重名的属性"><a href="#4-6-1-对象不能有重名的属性" class="headerlink" title="4.6.1 对象不能有重名的属性"></a>4.6.1 对象不能有重名的属性</h4><p>　　正常模式下，如果对象有多个重名属性，最后赋值的那个属性会覆盖前面的值。严格模式下，这属于语法错误。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">var</span> o = &#123;</span><br><span class="line">　　　　p: <span class="number">1</span>,</span><br><span class="line">　　　　p: <span class="number">2</span></span><br><span class="line">　　&#125;; <span class="comment">// 语法错误</span></span><br></pre></td></tr></table></figure>

<h4 id="4-6-2-函数不能有重名的参数"><a href="#4-6-2-函数不能有重名的参数" class="headerlink" title="4.6.2 函数不能有重名的参数"></a>4.6.2 函数不能有重名的参数</h4><p>　　正常模式下，如果函数有多个重名的参数，可以用<code>arguments[i]</code>读取。严格模式下，这属于语法错误。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">a, a, b</span>) </span>&#123; <span class="comment">// 语法错误</span></span><br><span class="line">　　　　<span class="keyword">return</span> ;</span><br><span class="line">　　&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-7-禁止八进制表示法"><a href="#4-7-禁止八进制表示法" class="headerlink" title="4.7 禁止八进制表示法"></a>4.7 禁止八进制表示法</h3><p>　　正常模式下，整数的第一位如果是 0，表示这是八进制数，比如 0100 等于十进制的 64。严格模式禁止这种表示法，整数第一位为 0，将报错。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">var</span> n = <span class="number">0100</span>; <span class="comment">// 语法错误</span></span><br></pre></td></tr></table></figure>

<h3 id="4-8-arguments对象的限制"><a href="#4-8-arguments对象的限制" class="headerlink" title="4.8 arguments对象的限制"></a>4.8 arguments对象的限制</h3><h4 id="4-8-1-不允许对arguments赋值"><a href="#4-8-1-不允许对arguments赋值" class="headerlink" title="4.8.1 不允许对arguments赋值"></a>4.8.1 不允许对arguments赋值</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">"use strict"</span>;</span><br><span class="line">　　<span class="built_in">arguments</span>++; <span class="comment">// 语法错误</span></span><br><span class="line">　　<span class="keyword">var</span> obj = &#123; <span class="keyword">set</span> p(arguments) &#123; &#125; &#125;; <span class="comment">// 语法错误</span></span><br><span class="line">　　<span class="keyword">try</span> &#123; &#125; <span class="keyword">catch</span> (<span class="built_in">arguments</span>) &#123; &#125; <span class="comment">// 语法错误</span></span><br><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">arguments</span>(<span class="params"></span>) </span>&#123; &#125; <span class="comment">// 语法错误</span></span><br><span class="line">　　<span class="keyword">var</span> f = <span class="keyword">new</span> <span class="built_in">Function</span>(<span class="string">"arguments"</span>, <span class="string">"'use strict'; return 17;"</span>); <span class="comment">// 语法错误</span></span><br></pre></td></tr></table></figure>

<h4 id="4-8-2-arguments不再追踪参数的变化"><a href="#4-8-2-arguments不再追踪参数的变化" class="headerlink" title="4.8.2 arguments不再追踪参数的变化"></a>4.8.2 arguments不再追踪参数的变化</h4><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line">　　　　a = <span class="number">2</span>;</span><br><span class="line">　　　　<span class="keyword">return</span> [a, <span class="built_in">arguments</span>[<span class="number">0</span>]];</span><br><span class="line">　　&#125;</span><br><span class="line">　　f(<span class="number">1</span>); <span class="comment">// 正常模式为[2,2]</span></span><br><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line"><span class="meta">　　　　"use strict"</span>;</span><br><span class="line">　　　　a = <span class="number">2</span>;</span><br><span class="line">　　　　<span class="keyword">return</span> [a, <span class="built_in">arguments</span>[<span class="number">0</span>]];</span><br><span class="line">　　&#125;</span><br><span class="line">　　f(<span class="number">1</span>); <span class="comment">// 严格模式为[2,1]</span></span><br></pre></td></tr></table></figure>

<h4 id="4-8-3-禁止使用arguments-callee"><a href="#4-8-3-禁止使用arguments-callee" class="headerlink" title="4.8.3 禁止使用arguments.callee"></a>4.8.3 禁止使用arguments.callee</h4><p>　　这意味着，你无法在匿名函数内部调用自身了。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">var</span> f = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="keyword">return</span> <span class="built_in">arguments</span>.callee; &#125;;</span><br><span class="line">　　f(); <span class="comment">// 报错</span></span><br></pre></td></tr></table></figure>

<h3 id="4-9-函数必须声明在顶层"><a href="#4-9-函数必须声明在顶层" class="headerlink" title="4.9 函数必须声明在顶层"></a>4.9 函数必须声明在顶层</h3><p>　　将来<code>Javascript</code>的新版本会引入”块级作用域”。为了与新版本接轨，严格模式只允许在全局作用域或函数作用域的顶层声明函数。也就是说，不允许在非函数的代码块内声明函数。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">　　"use strict"</span>;</span><br><span class="line">　　<span class="keyword">if</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">　　　　<span class="function"><span class="keyword">function</span> <span class="title">f</span>(<span class="params"></span>) </span>&#123; &#125; <span class="comment">// 语法错误</span></span><br><span class="line">　　&#125;</span><br><span class="line">　　<span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">　　　　<span class="function"><span class="keyword">function</span> <span class="title">f2</span>(<span class="params"></span>) </span>&#123; &#125; <span class="comment">// 语法错误</span></span><br><span class="line">　　&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-10-保留字"><a href="#4-10-保留字" class="headerlink" title="4.10 保留字"></a>4.10 保留字</h3><p>　　为了向将来<code>Javascript</code>的新版本过渡，严格模式新增了一些保留字：<code>implements, interface, let, package, private, protected, public, static, yield</code>。</p>
<p>　　使用这些词作为变量名将会报错。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">package</span>(<span class="params">protected</span>) </span>&#123; <span class="comment">// 语法错误</span></span><br><span class="line"><span class="meta">　　　　"use strict"</span>;</span><br><span class="line">　　　　<span class="keyword">var</span> implements; <span class="comment">// 语法错误</span></span><br><span class="line">　　&#125;</span><br></pre></td></tr></table></figure>

<p>　　此外，<code>ECMAscript</code>第五版本身还规定了另一些保留字（<code>class, enum, export, extends, import, super, const等</code>）。还可参考 <a href="http://es6.ruanyifeng.com/#docs/reference" target="_blank" rel="noopener">ECMAScript 6</a>，了解新版本特性，结合 “严格模式” 写出更加优雅的代码。</p>
<p>　　</p>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 中 Null 和 Undefined</title>
    <url>/2017/01/20/article-js-nullundefined/</url>
    <content><![CDATA[<p>　　本文转自 <a href="http://yanhaijing.com/" target="_blank" rel="noopener">颜海镜</a>的<a href="http://yanhaijing.com/javascript/2014/01/05/exploring-the-abyss-of-null-and-undefined-in-javascript/" target="_blank" rel="noopener">探索JavaScript中Null和Undefined的深渊</a>。</p>
<p>　　当讨论<code>JavaScript</code>中的原始数据类型时，大多数人都知道的基本知识，从<code>String</code>，<code>Number</code>到<code>Boolean</code>。这些原始类型相当简单，行为符合常识。但是，本文将更多聚焦独特的原始数据类型<code>Null</code>和<code>Undefined</code>，是什么让他们如此相似，却又似是而非。<a id="more"></a></p>
<h2 id="1-理解Null和Undefined"><a href="#1-理解Null和Undefined" class="headerlink" title="1. 理解Null和Undefined"></a>1. 理解<code>Null</code>和<code>Undefined</code></h2><p>　　在<code>JavaScript</code>中，<code>null</code>是字面量同时也是语言中的关键字，用来表示无法识别的对象值。换句话说，这用来表示 <strong>无值（no value）</strong>。虽然相似，<code>undefined</code>实际上代表了<strong>不存在的值（non-existence of a value）</strong>。都是完全不可变的，没有属性和方法，也不能给其属性赋值。事实上，试图访问或定义一个属性将会引发一个类型错误（TypeError）。正如他们的名字暗示的那样，他们是完全无效的值。</p>
<p>　　没有值代表的布尔值是<code>false</code>，这意味着他们在条件上下文中会被被计算为<code>false</code>，如<code>if</code>语句。使用相等操作符( <code>==</code> )比较这两个值和其他<code>false</code>值,他们并不等于除了自己:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="literal">null</span> == <span class="number">0</span>; <span class="comment">// false</span></span><br><span class="line"><span class="literal">undefined</span> == <span class="string">""</span>; <span class="comment">// false</span></span><br><span class="line"><span class="literal">null</span> == <span class="literal">false</span>; <span class="comment">// false</span></span><br><span class="line"><span class="literal">undefined</span> == <span class="literal">false</span>; <span class="comment">// false</span></span><br><span class="line"><span class="literal">null</span> == <span class="literal">undefined</span>; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>　　尽管如此，和其他相似之处，但<code>null</code>和<code>undefined</code>并不是等价的。每个作为其独特的类型的唯一成员,<code>undefined</code>是<code>Undefined</code>类型和<code>null</code>是<code>Null</code>类型。使用全等操作符（<code>===</code>）比较这两个值，这要求类型和值都相等，下面证明这一点：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="literal">undefined</span> === <span class="literal">null</span>; <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<p>　　<strong>这是一个重要的区别，服务于不同的目的和理由。区分这两个值，你可以认为<code>undefined</code>代表一个意想不到的没有值而<code>null</code>作为预期没有值的代表。</strong></p>
<h2 id="2-产生Undefined"><a href="#2-产生Undefined" class="headerlink" title="2. 产生Undefined"></a>2. 产生<code>Undefined</code></h2><p>　　有许多的方法产生一个<code>undefined</code>值的代码。它通常遇到当试图访问一个不存在的值时。在这种情况下，在<code>JavaScript</code>这种动态的弱类型语言中，只会默认返回一个<code>undefined</code>值，而不是上升为一个错误。 任何声明变量时没有提供一个初始值，都会有一个为<code>undefined</code>的默认值:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> foo; <span class="comment">// 默认值为 undefined</span></span><br></pre></td></tr></table></figure>

<p>　　当试图访问一个不存在的对象属性或数组项时，返回一个<code>undefined</code>值:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> array = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="keyword">var</span> foo = array.foo; <span class="comment">// foo 属性不存在, 返回 undefined</span></span><br><span class="line"><span class="keyword">var</span> item = array[<span class="number">5</span>]; <span class="comment">// 数组中没有索引为5的项，返回 undefined</span></span><br></pre></td></tr></table></figure>

<p>　　如果省略了函数的返回语句,返回<code>undefined</code>:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> value = (<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;)(); <span class="comment">// 返回 undefined</span></span><br></pre></td></tr></table></figure>

<p>　　函数调用时未提供的值结果将为<code>undefined</code>参数值：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">undefined</span>)</span>&#123;</span><br><span class="line">    <span class="comment">// 参数是 undefined</span></span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>　　<code>void</code>操作符也可以返回一个<code>undefined</code>值。像<code>Underscore</code>的库使用它作为一个防御式的类型检查，因为它是不可变的，可以在任何上下文依赖返回<code>undefined</code>:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isUndefined</span>(<span class="params">obj</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> obj === <span class="keyword">void</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　最后，<code>undefined</code>是一个预定义的全局变量(不像<code>null</code>关键字)初始化为<code>undefined</code>值:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="string">'undefined'</span> <span class="keyword">in</span> <span class="built_in">window</span>; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>　　<code>ECMAScript 5</code>中，这个变量是只读的，以前并非如此。</p>
<h2 id="3-Null的用例"><a href="#3-Null的用例" class="headerlink" title="3. Null的用例"></a>3. <code>Null</code>的用例</h2><p>　　<code>null</code>的用例是使他与众不同的主要方面，因为不像<code>undefined</code>，<code>null</code>被认为是更有用。这正是为什么<code>typeof</code>操作符作用于<code>null</code>值时返回“<code>object</code>”。最初的理由是，现在仍然是，通常用作一个空引用一个空对象的预期,就像一个占位符。<code>typeof</code>的这种行为已经被确认为一个错误，虽然提出了修正，出于后兼容的目的，这一点已经保持不变。 这就是为什么<code>JavaScript</code>环境从来没有设置一个值为<code>null</code>；它必须以编程方式完成。正如文档MDN所说：</p>
<blockquote>
<p>在api中，<code>null</code>是经常检索对象的地方可以预期，但没有相关的对象。</p>
</blockquote>
<p>　　这适用于<code>DOM</code>，它是独立于语言的，不属于<code>ECMAScript</code>规范的范围。因为它是一个外部<code>API</code>，试图获取一个不存在的元素返回一个<code>null</code>值，而不是<code>undefined</code>。 一般来说,如果你需要给一个变量或属性指定一个不变值，将它传递给一个函数，或者从一个函数返回<code>null</code>，<code>null</code>几乎总是最好的选择。简而言之，<code>JavaScript</code>使用<code>undefined</code>并且程序员应该使用<code>null</code>。 <code>null</code>的另一个可行的用例，也被认为是良好的实践是一个显式指定变量为无效(<code>object= null</code>)当一个引用不再是必需的。通过分配<code>null</code>值，有效地清除引用，并假设对象没有引用其他代码，指定垃圾收集，确保回收内存。</p>
<h2 id="4-深入挖掘"><a href="#4-深入挖掘" class="headerlink" title="4. 深入挖掘"></a>4. 深入挖掘</h2><p>　　使<code>null</code>和<code>undefined</code>像黑洞的不只是他们的行为，而是在他们在<code>JavaScript</code>环境的内部的处理方式。他们似乎通常并不具有同样的关联特征与其他原生或内置对象。 在 ES5 中，<code>Object.prototype.toString</code>方法，已经成为实际的类型检查标准，这在<code>null</code>和<code>undefined</code>中被证明是一致的：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.prototype.toString.call(<span class="literal">null</span>); <span class="comment">// [object Null]</span></span><br><span class="line"><span class="built_in">Object</span>.prototype.toString.call(<span class="literal">undefined</span>); <span class="comment">// [object Undefined]</span></span><br></pre></td></tr></table></figure>

<p>　　然而，<code>Object.prototype.toString</code>方法实际上并不是检索null的内部<code>[[Class]]</code>属性或<code>undefined</code>的公开构造函数。根据文档，以下步骤发生在被调用过程中：</p>
<ol>
<li>如果值是<code>undefined</code>，返回“<code>[object Undefined]</code>”。</li>
<li>如果这个值为<code>null</code>，则返回“<code>[object Null]</code>”。</li>
<li>让 O 作为调用<code>ToObject</code>同时传递<code>this</code>值作为参数的结果值。</li>
<li>让<code>class</code>是 O 的内部属性<code>[[Class]]</code>的值。</li>
<li>返回的结果连接三个字符串“<code>[object ”，class，和“]</code>”的结果的字符串值。</li>
</ol>
<p>　　该方法执行一个简单的字符串返回值，如果它检测到<code>null</code>或<code>undefined</code>和其他对象统一的功能。在整个规范中这是很常见的，因为当遇到<code>null</code>和<code>undefined</code>值时大多数方法包含一个简单的捕捉并返回。事实上，没有迹象表明他们包含与任何原生对象相关联的内部属性。就好像他们不是对象。我很想知道如果一个<code>JavaScript</code>的原生环境内部实际存在的显式方案会怎样？也许有人更熟悉一个可以参与的实现。</p>
<h2 id="5-结论"><a href="#5-结论" class="headerlink" title="5. 结论"></a>5. 结论</h2><p>　　无论这些原生对象多么不寻常，理解<code>null</code>和<code>undefined</code>之间的差异，和他们在<code>JavaScript</code>的语言基础中截然不同的角色。它可能不能使你的应用程序有所突破，但是一般来说，它仅被证明在开发和调试中长期有益。</p>
<hr>]]></content>
      <categories>
        <category>面试总结</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>初探 CoffeeScript</title>
    <url>/2017/01/18/article-cs-know/</url>
    <content><![CDATA[<p>　　<code>CoffeeScript</code>是一门简洁的，构架于<code>JavaScript</code>之上的预处理器语言，可以静态编译成<code>JavaScript</code>，语法主要受<code>ruby</code>和<code>python</code>影响，目前已经为众多<code>rails</code>和<code>node</code>项目采用。<a id="more"></a></p>
<h2 id="1-前言"><a href="#1-前言" class="headerlink" title="1. 前言"></a>1. 前言</h2><blockquote>
<p>　为什么要用<code>CoffeeScript</code>?</p>
</blockquote>
<ul>
<li>更少，更紧凑，和更清晰的代码</li>
<li>通过规避和改变对<code>JavaScript</code>中不良部分的使用，只留下精华，让代码减少出错率，更容易维护</li>
<li>在很多常用模式的实现上采用了<code>JavaScript</code>中的最佳实践</li>
<li><code>CoffeeScript</code>生成的<code>JavaScript</code>代码都可以完全通过<code>JSLint</code>的检测</li>
</ul>
<blockquote>
<p>　什么情况下不推荐使用<code>CoffeeScript</code>?</p>
</blockquote>
<ul>
<li><code>CoffeeScript</code>不是<code>JavaScript</code>的超集，也<strong>不是完全替代品</strong>，不应该在不会<code>JavaScript</code>的情况下使用<code>CoffeeScript</code>工作。</li>
</ul>
<p>　　<code>CoffeeScript</code>是一种需要预编译的语言，不能在运行时(<code>Runtime</code>)解释，这造成了她普遍被人质疑的一点，就是如果代码中出现运行时错误时难以调试，不过从实际使用上来看，因为<code>CoffeeScript</code>的编译结果大部分情况下自然而合理，至少我从来没有发现从生成的<code>JavaScript</code>代码回溯到对应的<code>CoffeeScript</code>代码有什么困难之处，我们稍后会看到这种对应关系的细节。</p>
<p>　　这种静态编译还有一个额外的好处，就是<code>CoffeeScript</code>和现有的环境(浏览器, Node , Rhino 等)与库完全兼容。</p>
<blockquote>
<p>　<code>CoffeeScript</code>安装</p>
</blockquote>
<p>　　最简单的安装和测试<code>CoffeeScript</code>的方法，是使用<code>node.js</code>的<code>npm</code>安装，然后使用命令行脚本实时编译:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install -g coffee-script</span><br><span class="line"># watch and compile</span><br><span class="line">coffee -w --output lib --compile src</span><br></pre></td></tr></table></figure>

<p>　　这里假设你的<code>coffee</code>代码在<code>src</code>目录下，这个 demo 会自动检测文件的改变，并编译成<code>js</code>文件放到<code>lib</code>目录下。</p>
<h2 id="2-语法"><a href="#2-语法" class="headerlink" title="2. 语法"></a>2. 语法</h2><p>　　与<code>SASS/LESS</code>和<code>CSS</code>的关系不同，<strong><code>CoffeeScript</code>不是<code>JavaScript</code>的超集，不能在<code>CoffeeScript</code>程序中写<code>JavaScript</code>代码</strong>，比如<code>function</code>等关键字。</p>
<h3 id="2-1-格式"><a href="#2-1-格式" class="headerlink" title="2.1 格式"></a>2.1 格式</h3><p>　　在<code>js</code>中，如果认为当前语句和随后语句是一个整体的话，就不会自己加<code>;</code>，比如以下<code>javascript</code>代码:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">//javascript code</span></span><br><span class="line"><span class="keyword">var</span> y = x+f</span><br><span class="line">(a+b).toString()</span><br><span class="line"></span><br><span class="line"><span class="comment">//parsed to:</span></span><br><span class="line"><span class="keyword">var</span> y = x+f(a+b).toString();</span><br></pre></td></tr></table></figure>

<p>　　很多<code>js</code>中的问题由此引起(实际上现在把<code>;</code>放在哪里，在<code>js</code>社区内也是个争论的话题)；</p>
<p>　　而<code>CoffeeScript</code>在编译时为每条语句加上<code>;</code>，因此在代码中不需要写<code>;</code>；</p>
<p>　　<code>CoffeeScript</code>中的注释采用<code>#</code>:</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line"><span class="comment"># single line comment</span></span><br><span class="line"><span class="comment">### </span></span><br><span class="line"><span class="comment">  multi line comment</span></span><br><span class="line"><span class="comment">###</span></span><br></pre></td></tr></table></figure>

<p>　　<code>CoffeeScript</code>中对空白敏感，这种做法来自<code>python</code>，任何需要(<code>{}</code>)的场合下，可以用缩进代替。</p>
<h3 id="2-2-作用域"><a href="#2-2-作用域" class="headerlink" title="2.2 作用域"></a>2.2 作用域</h3><p>　　在<code>js</code>中最糟糕的设计就是全局变量，当你忘记用<code>var</code>声明变量的时候，这个变量会成为全局对象上的一个属性;<code>CoffeeScript</code>避免了这点：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">foo = <span class="string">"bar"</span></span><br></pre></td></tr></table></figure>

<p>　　会编译成：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> foo;</span><br><span class="line">  foo = <span class="string">"bar"</span>;</span><br><span class="line">&#125;).call(<span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>

<p>　　任何的代码都会使用<strong><code>Immediate Function</code></strong>包装，这样<code>foo</code>成为了本地变量，并且，可以通过<code>call</code>指定的<code>this</code>引用全局对象，为了方便起见，之后的编译后代码描述不会再加上这个包装。</p>
<p>　　实际上在<code>CoffeeScript</code>中，你也不需要再用<code>var</code>声明变量，编译后会自动加上<code>var</code>，并且将声明<code>hoisting</code>，即放到作用域的顶部，看一个来自官方文档的例子：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">outer = <span class="number">1</span></span><br><span class="line"><span class="function"><span class="title">change</span> = -&gt;</span></span><br><span class="line">  inner = <span class="number">-1</span></span><br><span class="line">  outer = <span class="number">10</span></span><br><span class="line">inner = change()</span><br></pre></td></tr></table></figure>

<p>　　<code>-&gt;</code>是函数定义的简写方式，之后我们会探讨；编译后的<code>js</code>如下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> change, inner, outer;</span><br><span class="line"></span><br><span class="line">outer = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">change = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> inner;</span><br><span class="line">  inner = <span class="number">-1</span>;</span><br><span class="line">  <span class="keyword">return</span> outer = <span class="number">10</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">inner = change();</span><br></pre></td></tr></table></figure>

<p>　　这是类似<code>ruby</code>中的自然的作用域实现方式，<code>inner</code>在<code>change()</code>内定义成了局部变量，因为在代码中之前没有定义过。</p>
<h3 id="2-3-赋值"><a href="#2-3-赋值" class="headerlink" title="2.3 赋值"></a>2.3 赋值</h3><p>　　首先是字符串可以用类<code>ruby</code>的语法内嵌：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">target = <span class="string">"world"</span></span><br><span class="line">alert <span class="string">"hello, <span class="subst">#&#123;target&#125;</span>"</span></span><br></pre></td></tr></table></figure>

<p>　　其次是字面量，可以用类似<code>YAML</code>的方法定义对象字面量：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">object1 = one: <span class="number">1</span>, two: <span class="number">2</span></span><br><span class="line">object2 =</span><br><span class="line">  one: <span class="number">1</span></span><br><span class="line">  two: <span class="number">2</span></span><br><span class="line">  class: "numbers"</span><br></pre></td></tr></table></figure>

<p>　　注意保留字<code>class</code>，现在可以直接作为对象的<code>key</code>了。</p>
<p>　　数组也可以分行：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">arr = [</span><br><span class="line">  <span class="number">1</span></span><br><span class="line">  <span class="number">2</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>　　也可以解构赋值(Destructuring)：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">obj = &#123;a:<span class="string">"foo"</span>, b:<span class="string">"bar"</span>&#125;</span><br><span class="line">&#123;a, b&#125; = obj</span><br><span class="line">arr = [<span class="number">1</span>, <span class="number">2</span>]</span><br><span class="line">[a, b] = arr</span><br></pre></td></tr></table></figure>

<h3 id="2-4-数组"><a href="#2-4-数组" class="headerlink" title="2.4 数组"></a>2.4 数组</h3><p>　　数组的操作引入了来自<code>ruby</code>的<code>Range</code>概念，并且可以将字符串完全作为数组操作：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">numbers = [<span class="number">0.</span><span class="number">.9</span>]</span><br><span class="line">numbers[<span class="number">3.</span><span class="number">.5</span>] = [<span class="number">-3</span>, <span class="number">-4</span>, <span class="number">-5</span>]</span><br><span class="line">my = <span class="string">"my string"</span>[<span class="number">0.</span><span class="number">.1</span>]</span><br></pre></td></tr></table></figure>

<p>　　判断一个值是否在数组内，在<code>js</code>中可以用<code>Array.prototype.indexOf</code>，不过 IE8 及以下不支持，<code>CoffeeScript</code>提供了跨浏览器的<code>in</code>操作符解决：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">arr = [<span class="string">"foo"</span>, <span class="string">"bar"</span>]</span><br><span class="line"><span class="string">"foo"</span> <span class="keyword">in</span> arr</span><br></pre></td></tr></table></figure>

<p>　　具体的实现上，是一个对<code>indexOf</code>的<code>Shim</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"> <span class="keyword">var</span> arr,</span><br><span class="line">   __indexOf = [].indexOf || <span class="function"><span class="keyword">function</span>(<span class="params">item</span>) </span>&#123; </span><br><span class="line">     <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>, l = <span class="keyword">this</span>.length; i &lt; l; i++) &#123; </span><br><span class="line">       <span class="keyword">if</span> (i <span class="keyword">in</span> <span class="keyword">this</span> &amp;&amp; <span class="keyword">this</span>[i] === item) </span><br><span class="line">         <span class="keyword">return</span> i; </span><br><span class="line">     &#125; </span><br><span class="line">     <span class="keyword">return</span> <span class="number">-1</span>; </span><br><span class="line">   &#125;;</span><br><span class="line"></span><br><span class="line">arr = [<span class="string">"foo"</span>, <span class="string">"bar"</span>];</span><br><span class="line"></span><br><span class="line">__indexOf.call(arr, <span class="string">"foo"</span>) &gt;= <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>　　<code>for..in</code>语法可以用在数组上了，背后是用<code>js</code>的<code>for</code>循环实现的，这比数组的迭代器方法要效率高一些：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> name, i <span class="keyword">in</span> [<span class="string">"Roger"</span>, <span class="string">"Roderick"</span>]</span><br><span class="line">  alert <span class="string">"<span class="subst">#&#123;i&#125;</span> - Release <span class="subst">#&#123;name&#125;</span>"</span></span><br></pre></td></tr></table></figure>

<p>　　　也具有过滤器<code>when</code>：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">prisoners = [<span class="string">"Roger"</span>, <span class="string">"Roderick"</span>, <span class="string">"Brian"</span>]</span><br><span class="line">release prisoner <span class="keyword">for</span> prisoner <span class="keyword">in</span> prisoners <span class="keyword">when</span> prisoner[<span class="number">0</span>] <span class="keyword">is</span> <span class="string">"R"</span></span><br></pre></td></tr></table></figure>

<p>　　看起来很像普通英语了，也可以用<code>()</code>收集遍历的结果：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">result = (item <span class="keyword">for</span> item <span class="keyword">in</span> array <span class="keyword">when</span> item.name <span class="keyword">is</span> <span class="string">"test"</span>)</span><br></pre></td></tr></table></figure>

<p>　　遍历对象的属性可以用<code>of</code>,这是用<code>js</code>自己的<code>for..in</code>实现的：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">names = sam: seaborn, donna: moss</span><br><span class="line">alert(<span class="string">"<span class="subst">#&#123;first&#125;</span> <span class="subst">#&#123;last&#125;</span>"</span>) <span class="keyword">for</span> first, last <span class="keyword">of</span> names</span><br></pre></td></tr></table></figure>

<h3 id="2-5-流程控制"><a href="#2-5-流程控制" class="headerlink" title="2.5 流程控制"></a>2.5 流程控制</h3><p>　　<code>CoffeeScript</code>使用来自<code>ruby</code>的省略语法，让控制流变得很紧凑，也引进了<code>unless,not,then</code>等语法糖式的关键字：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">result = <span class="keyword">if</span> <span class="keyword">not</span> <span class="literal">true</span> <span class="keyword">then</span> <span class="string">"false"</span></span><br><span class="line">result = <span class="keyword">unless</span> <span class="literal">true</span> <span class="keyword">then</span> <span class="string">"false"</span></span><br></pre></td></tr></table></figure>

<p>　　<code>CoffeeScript</code>中非常好的一点，就是直接取消了<code>js</code>中的<code>==</code>判断，改成全部用<code>===</code>进行严格比较，<code>js</code>中的<code>==</code>会做大量诡异的类型转换，很多情况下是 bug 的来源；</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="string">"1"</span> == <span class="number">1</span> </span><br><span class="line">  alert(<span class="string">"equal"</span>)</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">  alert(<span class="string">"not equal"</span>)</span><br></pre></td></tr></table></figure>

<p>　　在使用<code>if</code>来进行空值的判断时，<code>js</code>有时会让人困扰，因为<code>&quot;&quot;</code>和<code>0</code>都会被转换成<code>false</code>，Coffee 提供了<code>?</code>操作符解决这个问题，她只有在变量为<code>null</code>或<code>undefined</code>时才为<code>false</code>。</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line"><span class="string">""</span>? <span class="comment">#true</span></span><br><span class="line"><span class="literal">null</span>? <span class="comment">#false</span></span><br></pre></td></tr></table></figure>

<p>　　也可以用常见的类似<code>ruby</code>中<code>||=</code>的方法，判断赋值，此外还可以用<code>and,or,is</code>关键字代替<code>&amp;&amp;,||,==</code>。</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">hash <span class="keyword">or</span>= &#123;&#125;</span><br><span class="line">hash ?= &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>　　经常有当某个属性存在的时候，才会调用属性上的方法的情况，这时候也可以用<code>?</code>；</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">knight.hasSword()?.poke()</span><br></pre></td></tr></table></figure>

<p>　　只有当<code>hasSword()</code>返回对象不为空时，才会调用<code>poke</code>方法，以下是编译的<code>js</code>代码：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> _ref;</span><br><span class="line"><span class="keyword">if</span> ((_ref = knight.hasSword()) != <span class="literal">null</span>) &#123;</span><br><span class="line">  _ref.poke();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　另一种情况是当<code>poke</code>方法存在时才调用：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">knight.hasSword().poke?()</span><br></pre></td></tr></table></figure>

<p>　　对应的js代码：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> _base;</span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">typeof</span> (_base = knight.hasSword()).poke === <span class="string">"function"</span>) &#123;</span><br><span class="line">  _base.poke();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　<code>switch case</code>语句也有了一些语法糖，并且会默认加上<code>break</code>：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">switch</span> day</span><br><span class="line">  <span class="keyword">when</span> <span class="string">"Sun"</span> <span class="keyword">then</span> go relax</span><br><span class="line">  <span class="keyword">when</span> <span class="string">"Sat"</span> <span class="keyword">then</span> go dancing</span><br><span class="line">  <span class="keyword">else</span> go work</span><br></pre></td></tr></table></figure>

<h3 id="2-6-函数"><a href="#2-6-函数" class="headerlink" title="2.6 函数"></a>2.6 函数</h3><p>　　<code>CoffeeScript</code>对<code>JavaScript</code>的函数做了很大的简化，举个例子，看一个求和函数：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">sum</span> = <span class="params">(nums...)</span> -&gt;</span></span><br><span class="line">  nums.reduce (x, y) -&gt; x+y</span><br><span class="line"></span><br><span class="line">sum <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span></span><br></pre></td></tr></table></figure>

<p>　　对应<code>JavaScript</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> sum,</span><br><span class="line">    __slice = [].slice;</span><br><span class="line"></span><br><span class="line">sum = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> nums;</span><br><span class="line">  nums = <span class="number">1</span> &lt;= <span class="built_in">arguments</span>.length ? __slice.call(<span class="built_in">arguments</span>, <span class="number">0</span>) : [];</span><br><span class="line">  <span class="keyword">return</span> nums.reduce(<span class="function"><span class="keyword">function</span>(<span class="params">x, y</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x + y;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">sum(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li>可以使用和<code>ruby 1.9</code>类似的<code>lambda</code>函数写法<code>-&gt;</code>来代替<code>function</code></li>
<li>参数列表放在<code>-&gt;</code>的前边，且可省略</li>
<li>取消了函数声明，只能将函数作为值定义</li>
<li>在<code>CoffeeScript</code>中，任何语句都是表达式(除了<code>break</code>和<code>continue</code>)，都有返回值，因此像<code>ruby</code>一样，不需要显式<code>return</code></li>
<li><code>js</code>的函数参数有一个很讨厌的地方，就是参数对象<code>arguments</code>不是一个真正的数组，要使用数组方法，必须转换成数组<code>[].slice.call(arguments, 0)</code>这样，而在<code>CoffeeScript</code>中收束<code>(加...)</code>的参数是一个真正的数组</li>
</ul>
<p>　　<code>CoffeeScript</code>的函数可以有默认参数，如：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">times</span> = <span class="params">(a = <span class="number">1</span>, b = <span class="number">2</span>)</span> -&gt;</span> a * b</span><br></pre></td></tr></table></figure>

<p>　　<code>CoffeeScript</code>的函数调用可以不用<code>()</code>语法包围参数，像<code>ruby</code>一样跟在函数名后面就可以，不过这也有时候会带来问题，特别是没有参数的调用：</p>
<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">alert</span><br></pre></td></tr></table></figure>

<p>　　对应的<code>js</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">alert;</span><br></pre></td></tr></table></figure>

<p>　　而不是<code>alert()</code>，这和<code>ruby</code>不同，需要注意!</p>
<p>　　缩进的格式有时需要小心，比如用多个函数做参数的时候，需要这样写：</p>
<figure class="highlight coffee"><table><tr><td class="code"><pre><span class="line">$(<span class="string">".toggle"</span>).toggle -&gt;</span><br><span class="line">  <span class="string">"on"</span></span><br><span class="line">, <span class="function">-&gt;</span></span><br><span class="line">  <span class="string">"off"</span></span><br></pre></td></tr></table></figure>

<p>　　对应<code>js</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">$(<span class="string">".toggle"</span>).toggle(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">"on"</span>;</span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">"off"</span>;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="3-模式"><a href="#3-模式" class="headerlink" title="3. 模式"></a>3. 模式</h2><p>　　使用<code>CoffeeScript</code>的一个重要理由，就是她用自己的语法实现了很多很常用的<code>js</code>编程模式，而且，通常是在社区内广泛被承认的最佳实践，如果不熟悉<code>JavaScript</code>的这些模式，可能会在调试代码上遇到一些麻烦，不过，基本上来说还是比较简单易懂的，下面我们会花一些时间研究一下<code>CoffeeScript</code>是用什么样的方法来封装这些通用编程模式的。</p>
<h3 id="3-1-闭包"><a href="#3-1-闭包" class="headerlink" title="3.1 闭包"></a>3.1 闭包</h3><p>　　在<code>js</code>中，普遍会使用闭包实现各种事件的<code>handler</code>或封装模块，以下是<code>CoffeeScript</code>对这一普遍模式的实现。</p>
<figure class="highlight coffee"><table><tr><td class="code"><pre><span class="line">closure = <span class="keyword">do</span> -&gt;</span><br><span class="line">  _private = <span class="string">"foo"</span></span><br><span class="line">  -&gt; _private</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(closure()) <span class="comment">#=&gt; "foo"</span></span><br></pre></td></tr></table></figure>

<p>　　<code>do</code>关键词可以产生一个<strong>Immediate Function</strong>,下面是对应js代码：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> closure;</span><br><span class="line"></span><br><span class="line">closure = (<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> _private;</span><br><span class="line">  _private = <span class="string">"foo"</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> _private;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>　　闭包中经常需要绑定<code>this</code>的值给闭包的私有变量，<code>CoffeeScript</code>使用特殊的<code>=&gt;</code>语法省去了这个麻烦。</p>
<figure class="highlight coffee"><table><tr><td class="code"><pre><span class="line">@clickHandler = <span class="function">-&gt;</span> alert <span class="string">"clicked"</span></span><br><span class="line">element.addEventListener <span class="string">"click"</span>, <span class="function"><span class="params">(e)</span> =&gt;</span> @clickHandler(e)</span><br></pre></td></tr></table></figure>

<p>　　使用<code>=&gt;</code>生成函数，可以看到生成代码中会加上对<code>this</code>的绑定：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> _this = <span class="keyword">this</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">this</span>.clickHandler = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> alert(<span class="string">"clicked"</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">element.addEventListener(<span class="string">"click"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> _this.clickHandler(e);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>　　这里<code>CoffeeScript</code>对于<code>this</code>有简单的别名<code>@</code>。</p>
<h3 id="3-2-扩展"><a href="#3-2-扩展" class="headerlink" title="3.2 扩展"></a>3.2 扩展</h3><p>　　在<code>js</code>中，所有的对象都是开放的，有时候会扩展原有对象的行为(比如对数组的ECMA5 shim)，这也称为<code>Monkey patching</code>：</p>
<figure class="highlight coffee"><table><tr><td class="code"><pre><span class="line">String::dasherize = <span class="function">-&gt;</span> @replace <span class="regexp">/_/g</span>, <span class="string">"-"</span></span><br></pre></td></tr></table></figure>

<p>　　<code>::</code>代表原型的引用，<code>js</code>代码如下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">String</span>.prototype.dasherize = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>.replace(<span class="regexp">/_/g</span>, <span class="string">"-"</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="3-3-类"><a href="#3-3-类" class="headerlink" title="3.3 类"></a>3.3 类</h3><p>　　在<code>js</code>中是否要模拟传统编程语言的类，是个一直以来都有争议的话题，不同的项目，不同的团队，在类的使用上会有不同的看法，不过，一旦决定要使用类，那么至少需要一套良好的实现，<code>CoffeeScript</code>在语言内部实现了类的模拟，我们来看一看一个完整的例子：</p>
<figure class="highlight coffee"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Gadget</span></span></span><br><span class="line">  @CITY = <span class="string">"beijing"</span></span><br><span class="line"></span><br><span class="line">  @create: <span class="function"><span class="params">(name, price)</span> -&gt;</span></span><br><span class="line">    <span class="keyword">new</span> Gadget(name, price)</span><br><span class="line"></span><br><span class="line">  _price = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">  constructor: <span class="function"><span class="params">(@name, price)</span> -&gt;</span></span><br><span class="line">    _price = price</span><br><span class="line"></span><br><span class="line">  sell: <span class="function">=&gt;</span></span><br><span class="line">    <span class="string">"Buy <span class="subst">#&#123;@name&#125;</span> with <span class="subst">#&#123;_price&#125;</span> in <span class="subst">#&#123;Gadget.CITY&#125;</span>"</span></span><br><span class="line"></span><br><span class="line">iphone = <span class="keyword">new</span> Gadget(<span class="string">"iphone"</span>, <span class="number">4999</span>)</span><br><span class="line"><span class="built_in">console</span>.log iphone.name <span class="comment">#=&gt; "iphone"</span></span><br><span class="line"><span class="built_in">console</span>.log iphone.sell() <span class="comment">#=&gt; "Buy iphone with 4999 in beijing"</span></span><br><span class="line"></span><br><span class="line">ipad = Gadget.create(<span class="string">"ipad"</span>, <span class="number">3999</span>)</span><br><span class="line"><span class="built_in">console</span>.log ipad.sell() <span class="comment">#=&gt; "Buy ipad with 3999 in beijing"</span></span><br></pre></td></tr></table></figure>

<p>　　这个<code>Gadget</code>类具有通常语言中类的功能：</p>
<ul>
<li><code>constructor</code>是构造函数，必须用这个名称，类似<code>ruby</code>中的<code>initialize</code></li>
</ul>
<ul>
<li><code>name</code>是实例变量,可以通过<code>iphone.name</code>获取</li>
<li>构造函数中如果给实例变量赋值，直接将<code>@name</code>写在参数中即可，等价于在函数体中的<code>@name = name</code></li>
<li><code>_price</code>是私有变量,需要赋初始值</li>
<li><code>sell</code>是实例方法</li>
<li><code>create</code>是类方法，注意这里使用了<code>@create</code>，这和<code>ruby</code>有些像，在定义时的<code>this</code>指的是这个类本身</li>
<li><code>CITY</code>是类变量</li>
</ul>
<p>　　要注意的是，对于实例方法，要用<code>=&gt;</code>来绑定<code>this</code>，这样可以作为闭包传递，比如：</p>
<figure class="highlight coffee"><table><tr><td class="code"><pre><span class="line">iphone = <span class="keyword">new</span> Gadget(<span class="string">"iphone"</span>, <span class="number">4999</span>)</span><br><span class="line">$(<span class="string">"#sell"</span>).click(iphone.sell())</span><br></pre></td></tr></table></figure>

<p>　　如果不用<code>=&gt;</code>，闭包被调用时就会丢失实例对象的值(<code>iphone</code>)。</p>
<p>　　对于熟悉基于类的面向对象编程的人，<code>CoffeeScript</code>的类是一目了然的，下面来看看对应的<code>js</code>代码：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Gadget,</span><br><span class="line">  __bind = <span class="function"><span class="keyword">function</span>(<span class="params">fn, me</span>)</span>&#123; <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="keyword">return</span> fn.apply(me, <span class="built_in">arguments</span>); &#125;; &#125;;</span><br><span class="line"></span><br><span class="line">Gadget = (<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> _price;</span><br><span class="line"></span><br><span class="line">  Gadget.name = <span class="string">'Gadget'</span>;</span><br><span class="line"></span><br><span class="line">  Gadget.CITY = <span class="string">"beijing"</span>;</span><br><span class="line"></span><br><span class="line">  Gadget.create = <span class="function"><span class="keyword">function</span>(<span class="params">name, price</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> Gadget(name, price);</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  _price = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">Gadget</span>(<span class="params">name, price</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.sell = __bind(<span class="keyword">this</span>.sell, <span class="keyword">this</span>);</span><br><span class="line">    <span class="keyword">this</span>.name = name;</span><br><span class="line">    _price = price;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  Gadget.prototype.sell = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Buy "</span> + <span class="keyword">this</span>.name + <span class="string">" with "</span> + _price + <span class="string">" in "</span> + Gadget.CITY;</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> Gadget;</span><br><span class="line"></span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>　　以上的代码有很多值得注意的地方：</p>
<ul>
<li>整体上来说，<code>CoffeeScript</code>的类模拟使用的是一个构造函数闭包，这是最常用的模拟类的模式，好处是可以完整地封装内部变量，且可以使用<code>new</code>来生成实例对象</li>
<li><code>_price</code>就是被封装在闭包内部的私有变量</li>
<li><code>sell</code>这样的实例方法是原型方法，并且在初始化时使用自定义的<code>bind</code>函数绑定实例(用<code>=&gt;</code>定义的情况)</li>
<li><code>reate</code>和<code>CITY</code>这样的类成员使用构造函数的属性实现，重复一下，在<code>CoffeeScript</code>类定义中的<code>this</code>指的是整个闭包<code>Gadget</code></li>
<li><code>Gadget.name</code>是额外定义的类名属性</li>
</ul>
<h3 id="3-4-类的继承"><a href="#3-4-类的继承" class="headerlink" title="3.4 类的继承"></a>3.4 类的继承</h3><p>　　<code>CoffeeScript</code>中为方便地实现类的继承也定义了自己的语法，我们把上面的类简化，来看一下如何继承：</p>
<figure class="highlight coffee"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Gadget</span></span></span><br><span class="line">  constructor: <span class="function"><span class="params">(@name)</span> -&gt;</span></span><br><span class="line">  sell: <span class="function">=&gt;</span></span><br><span class="line">    <span class="string">"Buy <span class="subst">#&#123;@name&#125;</span>"</span> </span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">IPhone</span> <span class="keyword">extends</span> <span class="title">Gadget</span></span></span><br><span class="line">  constructor: <span class="function">-&gt;</span> <span class="keyword">super</span>(<span class="string">"iphone"</span>)</span><br><span class="line">  nosell: <span class="function">=&gt;</span></span><br><span class="line">    <span class="string">"Don't <span class="subst">#&#123;@sell()&#125;</span>"</span></span><br><span class="line"></span><br><span class="line">iphone = <span class="keyword">new</span> IPhone</span><br><span class="line">iphone.nosell() <span class="comment">#=&gt; Don't Buy iphone</span></span><br></pre></td></tr></table></figure>

<ul>
<li>使用<code>extends</code>关键字可以继承父类中的所有实例属性,比如<code>sell</code></li>
<li><code>super</code>方法可以调用父类的同名方法</li>
<li>如果不覆盖<code>constructor</code>，则她被子类默认调用</li>
</ul>
<p>　　来看一下对应的<code>js</code>代码，这有一些复杂，我们把和上边类定义中重复的地方去掉，只留下继承的实现部分；</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> Gadget, IPhone,</span><br><span class="line">  __extends = <span class="function"><span class="keyword">function</span>(<span class="params">child, parent</span>) </span>&#123; </span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> key <span class="keyword">in</span> parent) &#123; </span><br><span class="line">      <span class="keyword">if</span> (&#123;&#125;.hasOwnProperty.call(parent, key)) </span><br><span class="line">        child[key] = parent[key]; </span><br><span class="line">    &#125; </span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">ctor</span>(<span class="params"></span>) </span>&#123; <span class="keyword">this</span>.constructor = child; &#125; </span><br><span class="line"></span><br><span class="line">    ctor.prototype = parent.prototype; </span><br><span class="line">    child.prototype = <span class="keyword">new</span> ctor; </span><br><span class="line">    child.__super__ = parent.prototype; </span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> child; </span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">IPhone = (<span class="function"><span class="keyword">function</span>(<span class="params">_super</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  __extends(IPhone, _super);</span><br><span class="line"></span><br><span class="line">  IPhone.name = <span class="string">'IPhone'</span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">IPhone</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.nosell = __bind(<span class="keyword">this</span>.nosell, <span class="keyword">this</span>);</span><br><span class="line">    IPhone.__super__.constructor.call(<span class="keyword">this</span>, <span class="string">"iphone"</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  IPhone.prototype.nosell = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Don't "</span> + (<span class="keyword">this</span>.sell());</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> IPhone;</span><br><span class="line"></span><br><span class="line">&#125;)(Gadget);</span><br></pre></td></tr></table></figure>

<p>　　这里重点有三个：</p>
<ul>
<li><p><code>__extends</code>函数使用了代理构造函数ctor来实现继承，这是非常普遍的<code>js</code>中对象继承的实践模式，进一步解释一下：</p>
<ul>
<li><p>使用代理构造函数的目的是为了避免子类被更改时父类受到影响</p>
</li>
<li><p>使用<code>ctor.prototype = parent.prototype</code>的意义是只继承定义在<code>prototype</code>上的公用属性</p>
</li>
</ul>
</li>
<li><p>父类的类成员被直接引用拷贝到子类，而不是原型继承</p>
</li>
<li><p><code>super</code>的实现方法是<code>parent.prototype.constructor.call(this)</code></p>
</li>
</ul>
<h3 id="3-5-混入-Mixin"><a href="#3-5-混入-Mixin" class="headerlink" title="3.5 混入(Mixin)"></a>3.5 混入(Mixin)</h3><p>　　在<code>ruby</code>语言中的<code>Mixin</code>，能够让你的类获得多个模块的方法，可以说是对多重继承一种很好的实现，虽然在<code>CoffeeScript</code>中并没有像<code>ruby</code>的<code>include</code>一样的内置功能，但很容易实现他：</p>
<figure class="highlight coffee"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Module</span></span></span><br><span class="line">  @extend: <span class="function"><span class="params">(obj)</span> -&gt;</span></span><br><span class="line">    <span class="keyword">for</span> key, value <span class="keyword">of</span> obj </span><br><span class="line">      @[key] = value</span><br><span class="line"></span><br><span class="line">  @include: <span class="function"><span class="params">(obj)</span> -&gt;</span></span><br><span class="line">    <span class="keyword">for</span> key, value <span class="keyword">of</span> obj </span><br><span class="line">      @::[key] = value</span><br><span class="line"></span><br><span class="line">classProperties =</span><br><span class="line">  find: <span class="function"><span class="params">(id)</span> -&gt;</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"find <span class="subst">#&#123;id&#125;</span>"</span>)</span><br><span class="line"></span><br><span class="line">instanceProperties =</span><br><span class="line">  save: <span class="function">-&gt;</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"save"</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span> <span class="keyword">extends</span> <span class="title">Module</span></span></span><br><span class="line">  @extend classProperties</span><br><span class="line">  @include instanceProperties</span><br><span class="line"></span><br><span class="line">user = User.find(<span class="number">1</span>)</span><br><span class="line">user = <span class="keyword">new</span> User</span><br><span class="line">user.save()</span><br></pre></td></tr></table></figure>

<ul>
<li>继承了<code>Module</code>的类才可以<code>Mixin</code>，当然，这里也可以用组合或者直接为<code>js</code>的构造函数做<code>Monkey patching</code></li>
<li><code>classProperties</code>是类成员模块，使用<code>@extend</code>来Mixin`，实现是简单的拷贝对象的属性</li>
<li><code>instanceProperties</code>是实例成员模块，使用<code>@include</code>来<code>Mixin</code>，实现是拷贝对象原型的属性</li>
<li>需要指出的是，这里的拷贝是引用拷贝，有可能外部会更改被<code>Mixin</code>的模块内部值，更好的方法是深层值拷贝(<code>clone</code>)，包括<code>JQuery</code>在内的很多类库都实现了这类扩展方法</li>
</ul>
<h2 id="4-结语"><a href="#4-结语" class="headerlink" title="4. 结语"></a>4. 结语</h2><p>　　<code>CoffeeScript</code>提供了一门比<code>JavaScript</code>更强大，优雅，表现力丰富的语言，但她毕竟架构于<code>JavaScript</code>之上，而且是静态地编译成<code>JavaScript</code>代码，也就是说，她不能完全避免对<code>JavaScript</code>中一些不良部分的滥用，比如<code>eval,typeof,instanceof</code>等，所以，在任何情况下，建议始终开启<code>Strict Mode</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="meta">"use strict"</span></span><br></pre></td></tr></table></figure>

<p>　　严格模式是一个<code>ECMA5</code>标准提出的<code>js</code>子集，禁用了很多js设计中不好的方面，在未来会逐渐成为<code>js</code>的语言标准，详细介绍在<a href="/2017/01/24/js-strict/">这里</a>。</p>
<hr>]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>CoffeeScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 闭包（Closure）</title>
    <url>/2017/01/16/article-js-closure/</url>
    <content><![CDATA[<p>　　<code>JavaScript</code> 里的闭包（Closure），对于跟 <code>js</code> 接触不够深的前端新手来说基本是闻之变色，那么闭包真的有那么难以理解和使用吗？前辈们有很多有声有色的解释和阐述，比如<a href="http://www.ruanyifeng.com/blog/" target="_blank" rel="noopener">阮一峰老师</a>的 <a href="http://www.ruanyifeng.com/blog/2009/08/learning_javascript_closures.html" target="_blank" rel="noopener">学习Javascript闭包（Closure）</a>、<a href="https://www.gracecode.com/" target="_blank" rel="noopener">明城</a>的 <a href="https://www.gracecode.com/posts/2385.html" target="_blank" rel="noopener">闭包的秘密</a> 、<a href="http://blog.csdn.net/vuturn/" target="_blank" rel="noopener">vuturn的专栏</a> 的 <a href="http://blog.csdn.net/vuturn/article/details/44560717" target="_blank" rel="noopener">JavaScript闭包详解</a> 等等好多，今天正好看到《JavaScript 高级程序设计》里的闭包一块，我就集百家之长再说一说闭包。<a id="more"></a></p>
<h2 id="1-什么是闭包"><a href="#1-什么是闭包" class="headerlink" title="1. 什么是闭包"></a>1. 什么是闭包</h2><p>　　<strong>闭包</strong> 是指有权访问另一个函数作用域中的变量的函数。</p>
<p>　　或者这么理解，<strong>函数</strong> 和 <strong>函数内部能访问到的变量</strong>（也叫环境）的总和，就是一个闭包。</p>
<p>　　闭包的常见形式一般就是一个函数内部创建另一个函数，如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//原生js经典案例</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fx</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> local = <span class="number">88</span>;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    local++</span><br><span class="line">    <span class="keyword">return</span> local;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> bar;</span><br><span class="line">&#125; <span class="comment">//local 变量和 bar 函数就组成了一个闭包（Closure）</span></span><br><span class="line"><span class="keyword">var</span> func = fx();</span><br><span class="line">func()</span><br><span class="line"></span><br><span class="line"><span class="comment">//jQuery经典案例</span></span><br><span class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">   <span class="keyword">var</span> selections = []; </span><br><span class="line">   $(<span class="string">".niners"</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">// 此闭包可以访问选择变量​</span></span><br><span class="line">   selections.push (<span class="keyword">this</span>.prop(<span class="string">"name"</span>)); <span class="comment">// 更新外部函数作用域中的选择变量</span></span><br><span class="line">   &#125;)</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="1-1-为什么要函数套函数呢？"><a href="#1-1-为什么要函数套函数呢？" class="headerlink" title="1.1 为什么要函数套函数呢？"></a>1.1 为什么要函数套函数呢？</h3><p>　　其实函数套函数只是为了造出一个<strong>局部变量</strong>，闭包的原文是 <code>Closure</code>，其实跟 <strong>包</strong> 没有任何关系，并不是说非得用什么包起什么来才行。</p>
<h3 id="1-2-闭包与作用域链"><a href="#1-2-闭包与作用域链" class="headerlink" title="1.2 闭包与作用域链"></a>1.2 闭包与作用域链</h3><p>　　闭包中内部函数访问了外部函数的变量 <em>local</em>。即使这个内部函数被返回了，而且是在其他地方被调用了，但是他仍然可以访问外部函数的变量 <em>local</em>，之所以能够访问变量 <em>local</em>，是因为内部函数的作用域链中包含了 <code>fx()</code> 的作用域。</p>
<p>　　<em>小提示</em>：理解 <a href="/2017/01/13/js-variate/">js 的作用域链</a> 及 <a href="/2017/01/13/js-variate/">js 的变量范围</a> 对真正的彻底的理解闭包至关重要。</p>
<h2 id="2-闭包的作用"><a href="#2-闭包的作用" class="headerlink" title="2. 闭包的作用"></a>2. 闭包的作用</h2><p>　　闭包可以用在许多地方。它的最大用处有两个，一个是可以<strong>读取函数内部的变量</strong>，另一个就是让这些<strong>变量的值始终保持在内存中</strong>(<a href="http://www.ruanyifeng.com/blog/2009/08/learning_javascript_closures.html" target="_blank" rel="noopener">参考这里</a>)。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">　　<span class="function"><span class="keyword">function</span> <span class="title">f1</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">　　　　<span class="keyword">var</span> n=<span class="number">999</span>;</span><br><span class="line">　　　　nAdd=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;n+=<span class="number">1</span>&#125;</span><br><span class="line">　　　　<span class="function"><span class="keyword">function</span> <span class="title">f2</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">　　　　　　alert(n);</span><br><span class="line">　　　　&#125;</span><br><span class="line">　　　　<span class="keyword">return</span> f2;</span><br><span class="line">　　&#125;</span><br><span class="line">　　<span class="keyword">var</span> result=f1();</span><br><span class="line">　　result(); <span class="comment">// 999</span></span><br><span class="line">　　nAdd();</span><br><span class="line">　　result(); <span class="comment">// 1000</span></span><br></pre></td></tr></table></figure>

<p>　　<code>函数 result</code> 执行了两次，第一次的值是 999，第二次的值是 1000。这证明了，函数 f1 中的局部变量 n 一直保存在内存中，并没有在 f1 调用后被自动清除。</p>
<p>　　这段代码中另一个值得注意的地方，就是 “<code>nAdd=function(){n+=1}</code>“ 这一行，首先在 <code>nAdd</code> 前面没有使用 <code>var</code> 关键字，因此 <code>nAdd</code> 是一个全局变量，而不是局部变量。其次，<code>nAdd</code> 的值是一个匿名函数（anonymous function），而这个匿名函数本身也是一个闭包，可以在函数外部对函数内部的局部变量进行操作。</p>
<p>　　通过上面的阐述，其实可以延伸出闭包在实际中的应用：</p>
<h3 id="2-1-匿名自执行函数"><a href="#2-1-匿名自执行函数" class="headerlink" title="2.1 匿名自执行函数"></a>2.1 匿名自执行函数</h3><p>　　我们知道所有的变量，如果不加上var关键字，则默认的会添加到全局对象的属性上去，这样的临时变量加入全局对象有很多坏处，比如：别的函数可能误用这些变量；造成全局对象过于庞大，影响访问速度(因为变量的取值是需要从原型链上遍历的)。</p>
<p>　　除了每次使用变量都是用var关键字外，我们在实际情况下经常遇到这样一种情况，即有的函数只需要执行一次，其内部变量无需维护，比如UI的初始化，那么我们可以使用闭包：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> datamodel = &#123;    </span><br><span class="line">    table : [],    </span><br><span class="line">    tree : &#123;&#125;    </span><br><span class="line">&#125;;      </span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">dm</span>)</span>&#123;    </span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; dm.table.rows; i++)&#123;    </span><br><span class="line">       <span class="keyword">var</span> row = dm.table.rows[i];    </span><br><span class="line">       <span class="keyword">for</span>(<span class="keyword">var</span> j = <span class="number">0</span>; j &lt; row.cells; i++)&#123;    </span><br><span class="line">           drawCell(i, j);    </span><br><span class="line">       &#125;    </span><br><span class="line">    &#125;    </span><br><span class="line">&#125;)(datamodel);</span><br></pre></td></tr></table></figure>

<p>　　我们创建了一个匿名的函数，并立即执行它，由于外部无法引用它内部的变量，因此在执行完后很快就会被释放，关键是这种机制不会污染全局对象。但是这种立即执行函数 （IIFE） 也要合理的使用，具体可以参看如何 <a href="/2017/01/14/js-IIFE/">合理使用IIFE</a>。</p>
<h3 id="2-2-闭包缓存"><a href="#2-2-闭包缓存" class="headerlink" title="2.2 闭包缓存"></a>2.2 闭包缓存</h3><p>　　设想我们有一个处理过程很耗时的函数对象，每次调用都会花费很长时间，那么我们就需要将计算出来的值存储起来，当调用这个函数的时候，首先在缓存中查找，如果找不到，则进行计算，然后更新缓存并返回值，如果找到了，直接返回查找到的值即可。闭包正是可以做到这一点，因为它不会释放外部的引用，从而函数内部的值可以得以保留。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> CachedSearchBox = (<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;    </span><br><span class="line">    <span class="keyword">var</span> cache = &#123;&#125;,    </span><br><span class="line">       count = [];    </span><br><span class="line">    <span class="keyword">return</span> &#123;    </span><br><span class="line">       attachSearchBox : <span class="function"><span class="keyword">function</span>(<span class="params">dsid</span>)</span>&#123;    </span><br><span class="line">           <span class="keyword">if</span>(dsid <span class="keyword">in</span> cache)&#123;<span class="comment">//如果结果在缓存中    </span></span><br><span class="line">              <span class="keyword">return</span> cache[dsid];<span class="comment">//直接返回缓存中的对象    </span></span><br><span class="line">           &#125;    </span><br><span class="line">           <span class="keyword">var</span> fsb = <span class="keyword">new</span> uikit.webctrl.SearchBox(dsid);<span class="comment">//新建    </span></span><br><span class="line">           cache[dsid] = fsb;<span class="comment">//更新缓存    </span></span><br><span class="line">           <span class="keyword">if</span>(count.length &gt; <span class="number">100</span>)&#123;<span class="comment">//保正缓存的大小&lt;=100    </span></span><br><span class="line">              <span class="keyword">delete</span> cache[count.shift()];    </span><br><span class="line">           &#125;    </span><br><span class="line">           <span class="keyword">return</span> fsb;          </span><br><span class="line">       &#125;,    </span><br><span class="line">       clearSearchBox : <span class="function"><span class="keyword">function</span>(<span class="params">dsid</span>)</span>&#123;    </span><br><span class="line">           <span class="keyword">if</span>(dsid <span class="keyword">in</span> cache)&#123;    </span><br><span class="line">              cache[dsid].clearSelection();      </span><br><span class="line">           &#125;    </span><br><span class="line">       &#125;    </span><br><span class="line">    &#125;;    </span><br><span class="line">&#125;)();    </span><br><span class="line">CachedSearchBox.attachSearchBox(<span class="string">"input1"</span>);</span><br></pre></td></tr></table></figure>

<p>　　这样，当我们第二次调用 <code>CachedSearchBox.attachSerachBox(“input1”)</code> 的时候，我们就可以从缓存中取道该对象，而不用再去创建一个新的<code>searchbox</code> 对象。</p>
<h3 id="2-3-实现封装"><a href="#2-3-实现封装" class="headerlink" title="2.3 实现封装"></a>2.3 实现封装</h3><p>　　先看一个关于封装的经典例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">celebrityID</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> celebrityID = <span class="number">999</span>;</span><br><span class="line">    <span class="comment">//用内部函数返回一个对象 </span></span><br><span class="line">    <span class="comment">// 所有的内部函数可以访问外部函数的变量 </span></span><br><span class="line">    <span class="keyword">return</span> &#123; <span class="attr">getID</span>: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; </span><br><span class="line">                <span class="comment">//这个内部函数将返回更新celebrityID变量 </span></span><br><span class="line">                <span class="keyword">return</span> celebrityID; </span><br><span class="line">                <span class="comment">// 它将返回celebrityID的当前值，即使changeTheID函数更改 </span></span><br><span class="line">            &#125;,</span><br><span class="line">            setID: <span class="function"><span class="keyword">function</span> (<span class="params">theNewID</span>) </span>&#123; </span><br><span class="line">                <span class="comment">//这个内部函数会随时改变外部函数的变量 </span></span><br><span class="line">                celebrityID = theNewID;</span><br><span class="line">            &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">var</span> mjID = celebrityID (); </span><br><span class="line"><span class="comment">// 此时，该celebrityID外部函数返回。</span></span><br><span class="line">mjID.getID(); <span class="comment">// 999​ </span></span><br><span class="line">mjID.setID(<span class="number">567</span>); <span class="comment">// 改变外部函数变量 </span></span><br><span class="line">mjID.getID(); <span class="comment">// 567</span></span><br></pre></td></tr></table></figure>

<p>　　这个例子又说明了 <strong>闭包引用外部函数的变量存储 ，不存储实际值</strong>，而是储存的整个变量对象；这个例子同时也实现了面向对象中的对象。</p>
<h2 id="3-闭包的副作用"><a href="#3-闭包的副作用" class="headerlink" title="3. 闭包的副作用"></a>3. 闭包的副作用</h2><h3 id="3-1-内存消耗"><a href="#3-1-内存消耗" class="headerlink" title="3.1 内存消耗"></a>3.1 内存消耗</h3><p>　　通常来说，函数的活动对象会随着执行期上下文一起销毁，但是，由于闭包引用另外一个函数的活动对象，因此这个活动对象无法被销毁，也就是要维护额外的作用域，这意味着，闭包比一般的函数需要更多的内存消耗。尤其在 IE 浏览器中需要关注。由于 IE 使用非原生 <code>javascript</code> 对象实现 DOM 对象，因此闭包会导致内存泄露问题，例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">A</span>(<span class="params"></span>)</span>&#123;  </span><br><span class="line">      <span class="keyword">var</span> a=<span class="built_in">document</span>.createElement(<span class="string">"div"</span>),<span class="comment">//  </span></span><br><span class="line">            msg=<span class="string">"Hello"</span>;  </span><br><span class="line">       a.onclick=<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;  </span><br><span class="line">          alert(msg);  </span><br><span class="line">          &#125;  </span><br><span class="line">   &#125;  </span><br><span class="line"> A();</span><br></pre></td></tr></table></figure>

<p>　　以上的闭包会在IE下导致内存泄露，假设<code>A()</code>执行时创建的作用域对象 ScopeA，ScopeA 引用了 DOM 对象 a,DOM 对象 a 引用了 <code>function(aleert(msg))</code>，函数 <code>function(alert(msg))</code> 引用了 ScopeA，这是一个循环引用，在 IE 会导致内存泄露。</p>
<h3 id="3-2-性能问题"><a href="#3-2-性能问题" class="headerlink" title="3.2 性能问题"></a>3.2 性能问题</h3><p>　　使用闭包时，会涉及到跨作用域访问，每次访问都会导致性能损失。因此在脚本中，最好小心使用闭包，它同时会涉及到内存和速度问题。</p>
<p>　　不过我们可以通过把跨作用域变量存储在局部变量中，然后直接访问局部变量，来减轻对执行速度的影响。</p>
<hr>]]></content>
      <categories>
        <category>面试总结</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Flex 布局新旧混合写法详解</title>
    <url>/2017/01/15/article-css-flex/</url>
    <content><![CDATA[<p>　　<code>flex</code> 是个非常好用的属性，如果说有什么可以完全代替 <code>float</code> 和 <code>position</code> ，那么肯定是非它莫属了（虽然现在还有很多不支持 <code>flex</code> 的浏览器）。然而在移动开发中，本来绝大多数浏览器（包括安卓2.3以上的自带浏览器）都支持的属性，偏偏有个例外，就是国产某某 X5 内核神器（不知哪个版本的webkit，仅支持 <code>display:box</code>），自主研发这东西也不好多说什么了，下面入正题。<a id="more"></a></p>
<p>　　首先还是从两个版本的语法开始讲吧，这里还是假设 <code>flex</code> 容器为 .box ，子元素为 .item 。</p>
<p>　　先来一张属性图来梳理一下他们之间的一些关系：</p>
<img src="/styles/images/css/articlex12.png">

<p>　　这张图可以在接下来的属性分析中用到。</p>
<img src="/styles/images/css/articlex13.png">

<h2 id="旧语法篇"><a href="#旧语法篇" class="headerlink" title="旧语法篇"></a>旧语法篇</h2><h3 id="1-定义容器的display属性"><a href="#1-定义容器的display属性" class="headerlink" title="1. 定义容器的display属性"></a>1. 定义容器的display属性</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">display</span>: -moz-box; <span class="comment">/*Firefox*/</span></span><br><span class="line">    <span class="attribute">display</span>: -webkit-box; <span class="comment">/*Safari,Opera,Chrome*/</span></span><br><span class="line">    <span class="attribute">display</span>: box;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-容器属性"><a href="#2-容器属性" class="headerlink" title="2. 容器属性"></a>2. 容器属性</h3><h4 id="2-1-box-pack-属性"><a href="#2-1-box-pack-属性" class="headerlink" title="2.1 box-pack 属性"></a>2.1 box-pack 属性</h4><p>　　box-pack定义子元素主轴对齐方式。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-moz-box-pack</span>: center; <span class="comment">/*Firefox*/</span></span><br><span class="line">    <span class="attribute">-webkit-box-pack</span>: center; <span class="comment">/*Safari,Opera,Chrome*/</span></span><br><span class="line">    <span class="attribute">box-pack</span>: center;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　box-pack属性总共有4个值：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">box-pack</span>: start | end | center | justify;</span><br><span class="line"><span class="comment">/*主轴对齐：左对齐（默认）| 右对齐 | 居中对齐 | 左右对齐*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-2-box-align-属性"><a href="#2-2-box-align-属性" class="headerlink" title="2.2 box-align 属性"></a>2.2 box-align 属性</h4><p>　　box-align定义子元素交叉轴对齐方式。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-moz-box-align</span>: center; <span class="comment">/*Firefox*/</span></span><br><span class="line">    <span class="attribute">-webkit-box-align</span>: center; <span class="comment">/*Safari,Opera,Chrome*/</span></span><br><span class="line">    <span class="attribute">box-align</span>: center;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　box-align属性总共有5个值：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">box-align</span>: start | end | center | baseline | stretch;</span><br><span class="line"><span class="comment">/*交叉轴对齐：顶部对齐（默认） | 底部对齐 | 居中对齐 | 文本基线对齐 | 上下对齐并铺满*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-3-box-direction-属性"><a href="#2-3-box-direction-属性" class="headerlink" title="2.3 box-direction 属性"></a>2.3 box-direction 属性</h4><p>　　box-direction定义子元素的显示方向。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-moz-box-direction</span>: reverse; <span class="comment">/*Firefox*/</span></span><br><span class="line">    <span class="attribute">-webkit-box-direction</span>: reverse; <span class="comment">/*Safari,Opera,Chrome*/</span></span><br><span class="line">    <span class="attribute">box-direction</span>: reverse;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　box-direction属性总共有3个值：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">box-direction</span>: normal | reverse | inherit;</span><br><span class="line"><span class="comment">/*显示方向：默认方向 | 反方向 | 继承子元素的 box-direction*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-4-box-orient-属性"><a href="#2-4-box-orient-属性" class="headerlink" title="2.4 box-orient 属性"></a>2.4 box-orient 属性</h4><p>　　box-orient定义子元素是否应水平或垂直排列。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-moz-box-orient</span>: horizontal; <span class="comment">/*Firefox*/</span></span><br><span class="line">    <span class="attribute">-webkit-box-orient</span>: horizontal; <span class="comment">/*Safari,Opera,Chrome*/</span></span><br><span class="line">    <span class="attribute">box-orient</span>: horizontal;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　box-orient属性总共有5个值：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">box-orient</span>: horizontal | vertical | inline-axis | block-axis | inherit;</span><br><span class="line">    <span class="comment">/*排列方向：水平 | 垂直 | 行内方式排列（默认） | 块方式排列 | 继承父级的box-orient*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-5-box-lines-属性"><a href="#2-5-box-lines-属性" class="headerlink" title="2.5 box-lines 属性"></a>2.5 box-lines 属性</h4><p>　　box-lines定义当子元素超出了容器是否允许子元素换行。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-moz-box-lines</span>: multiple; <span class="comment">/*Firefox*/</span></span><br><span class="line">    <span class="attribute">-webkit-box-lines</span>: multiple; <span class="comment">/*Safari,Opera,Chrome*/</span></span><br><span class="line">    <span class="attribute">box-lines</span>: multiple;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　box-lines属性总共有2个值：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">box-lines</span>: single | multiple;</span><br><span class="line">    <span class="comment">/*允许换行：不允许（默认） | 允许*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-子元素属性"><a href="#3-子元素属性" class="headerlink" title="3. 子元素属性"></a>3. 子元素属性</h3><h4 id="3-1-box-flex-属性"><a href="#3-1-box-flex-属性" class="headerlink" title="3.1 box-flex 属性"></a>3.1 box-flex 属性</h4><p>　　box-flex定义是否允许当前子元素伸缩。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">-moz-box-flex</span>: <span class="number">1.0</span>; <span class="comment">/*Firefox*/</span></span><br><span class="line">    <span class="attribute">-webkit-box-flex</span>: <span class="number">1.0</span>; <span class="comment">/*Safari,Opera,Chrome*/</span></span><br><span class="line">    <span class="attribute">box-flex</span>: <span class="number">1.0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　box-flex属性使用一个浮点值：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">box-flex</span>: &lt;value&gt;;</span><br><span class="line">    <span class="comment">/*伸缩：&lt;一个浮点数，默认为0.0，即表示不可伸缩，大于0的值可伸缩，柔性相对&gt;*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-2-box-ordinal-group-属性"><a href="#3-2-box-ordinal-group-属性" class="headerlink" title="3.2 box-ordinal-group 属性"></a>3.2 box-ordinal-group 属性</h4><p>　　box-ordinal-group定义子元素的显示次序，数值越小越排前。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">-moz-box-ordinal-group</span>: <span class="number">1</span>; <span class="comment">/*Firefox*/</span></span><br><span class="line">    <span class="attribute">-webkit-box-ordinal-group</span>: <span class="number">1</span>; <span class="comment">/*Safari,Opera,Chrome*/</span></span><br><span class="line">    <span class="attribute">box-ordinal-group</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　box-direction属性使用一个整数值：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">box-ordinal-group</span>: &lt;integer&gt;;</span><br><span class="line">    <span class="comment">/*显示次序：&lt;一个整数，默认为1，数值越小越排前&gt;*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="新版语法"><a href="#新版语法" class="headerlink" title="新版语法"></a>新版语法</h2><h3 id="1-定义容器的display属性-1"><a href="#1-定义容器的display属性-1" class="headerlink" title="1. 定义容器的display属性"></a>1. 定义容器的display属性</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">display</span>: -webkit-flex; <span class="comment">/*webkit*/</span></span><br><span class="line">    <span class="attribute">display</span>: flex;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*行内flex*/</span></span><br><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">display</span>: -webkit-inline-flex; <span class="comment">/*webkit*/</span></span><br><span class="line">    <span class="attribute">display</span>:inline-flex;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-容器样式"><a href="#2-容器样式" class="headerlink" title="2. 容器样式"></a>2. 容器样式</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">flex-direction</span>: row | row-reverse | column | column-reverse;</span><br><span class="line">    <span class="comment">/*主轴方向：左到右（默认） | 右到左 | 上到下 | 下到上*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">flex-wrap</span>: nowrap | wrap | wrap-reverse;</span><br><span class="line">    <span class="comment">/*换行：不换行（默认） | 换行 | 换行并第一行在下方*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">flex-flow</span>: &lt;flex-direction&gt; || &lt;flex-wrap&gt;;</span><br><span class="line">    <span class="comment">/*主轴方向和换行简写*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">justify-content</span>: flex-start | flex-end | center | space-between | space-around;</span><br><span class="line">    <span class="comment">/*主轴对齐方式：左对齐（默认） | 右对齐 | 居中对齐 | 两端对齐 | 平均分布*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">align-items</span>: flex-start | flex-end | center | baseline | stretch;</span><br><span class="line">    <span class="comment">/*交叉轴对齐方式：顶部对齐（默认） | 底部对齐 | 居中对齐 | 上下对齐并铺满 | 文本基线对齐*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">align-content</span>: flex-start | flex-end | center | space-between | space-around | stretch;</span><br><span class="line">    <span class="comment">/*多主轴对齐：顶部对齐（默认） | 底部对齐 | 居中对齐 | 上下对齐并铺满 | 上下平均分布*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-子元素属性-1"><a href="#3-子元素属性-1" class="headerlink" title="3. 子元素属性"></a>3. 子元素属性</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">order</span>: &lt;integer&gt;;</span><br><span class="line">    <span class="comment">/*排序：数值越小，越排前，默认为0*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">flex-grow</span>: &lt;number&gt;; <span class="comment">/* default 0 */</span></span><br><span class="line">    <span class="comment">/*放大：默认0（即如果有剩余空间也不放大，值为1则放大，2是1的双倍大小，以此类推）*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">flex-shrink</span>: &lt;number&gt;; <span class="comment">/* default 1 */</span></span><br><span class="line">    <span class="comment">/*缩小：默认1（如果空间不足则会缩小，值为0不缩小）*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">flex-basis</span>: &lt;length&gt; | auto; <span class="comment">/* default auto */</span></span><br><span class="line">    <span class="comment">/*固定大小：默认为0，可以设置px值，也可以设置百分比大小*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">flex</span>: none | [ &lt;<span class="string">'flex-grow'</span>&gt; &lt;<span class="string">'flex-shrink'</span>&gt;? || &lt;<span class="string">'flex-basis'</span>&gt; ]</span><br><span class="line">    <span class="comment">/*flex-grow, flex-shrink 和 flex-basis的简写，默认值为0 1 auto，*/</span></span><br><span class="line"> </span><br><span class="line">    align-self: auto | flex-start | flex-end | center | baseline | stretch;</span><br><span class="line">    <span class="comment">/*单独对齐方式：自动（默认） | 顶部对齐 | 底部对齐 | 居中对齐 | 上下对齐并铺满 | 文本基线对齐*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="兼容写法"><a href="#兼容写法" class="headerlink" title="兼容写法"></a>兼容写法</h2><h3 id="1-定义容器的-display-属性"><a href="#1-定义容器的-display-属性" class="headerlink" title="1. 定义容器的 display 属性"></a>1. 定义容器的 display 属性</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">display</span>: -webkit-box; <span class="comment">/* 老版本语法: Safari, iOS, Android browser, older WebKit browsers. */</span></span><br><span class="line">    <span class="attribute">display</span>: -moz-box; <span class="comment">/* 老版本语法: Firefox (buggy) */</span></span><br><span class="line">    <span class="attribute">display</span>: -ms-flexbox; <span class="comment">/* 混合版本语法: IE 10 */</span></span><br><span class="line">    <span class="attribute">display</span>: -webkit-flex; <span class="comment">/* 新版本语法: Chrome 21+ */</span></span><br><span class="line">    <span class="attribute">display</span>: flex; <span class="comment">/* 新版本语法: Opera 12.1, Firefox 22+ */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　由于旧版语法并没有列入W3C标准，所以这里不用写 display:box ，下面的语法也是一样的。</p>
<p>　　这里还要注意的是，如果子元素是行内元素，在很多情况下都要使用 display:block 或 display:inline-block 把行内子元素变成块元素（例如使用 box-flex 属性），这也是旧版语法和新版语法的区别之一。</p>
<h3 id="2-子元素主轴对齐方式"><a href="#2-子元素主轴对齐方式" class="headerlink" title="2. 子元素主轴对齐方式"></a>2. 子元素主轴对齐方式</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-webkit-box-pack</span>: center;</span><br><span class="line">    <span class="attribute">-moz-justify-content</span>: center;</span><br><span class="line">    <span class="attribute">-webkit-justify-content</span>: center;</span><br><span class="line">    <span class="attribute">justify-content</span>: center;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/styles/images/css/articlex0.png" alt="子元素主轴对齐方式">

<p>　　这里旧版语法有4个参数，而新版语法有5个参数，兼容写法新版语法的 space-around 是不可用的：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">box-pack</span>: start | end | center | justify;</span><br><span class="line">    <span class="comment">/*主轴对齐：左对齐（默认） | 右对齐 | 居中对齐 | 左右对齐*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">justify-content</span>: flex-start | flex-end | center | space-between | space-around;</span><br><span class="line">    <span class="comment">/*主轴对齐方式：左对齐（默认） | 右对齐 | 居中对齐 | 两端对齐 | 平均分布*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-子元素交叉轴对齐方式"><a href="#3-子元素交叉轴对齐方式" class="headerlink" title="3. 子元素交叉轴对齐方式"></a>3. 子元素交叉轴对齐方式</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-webkit-box-align</span>: center;</span><br><span class="line">    <span class="attribute">-moz-align-items</span>: center;</span><br><span class="line">    <span class="attribute">-webkit-align-items</span>: center;</span><br><span class="line">    <span class="attribute">align-items</span>: center;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/styles/images/css/articlex1.png" alt="子元素交叉轴对齐方式">

<p>　　这里的参数除了写法不同，其实是功能是一样的：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">box-align</span>: start | end | center | baseline | stretch;</span><br><span class="line">    <span class="comment">/*交叉轴对齐：顶部对齐（默认） | 底部对齐 | 居中对齐 | 文本基线对齐 | 上下对齐并铺满*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">align-items</span>: flex-start | flex-end | center | baseline | stretch;</span><br><span class="line">    <span class="comment">/*交叉轴对齐方式：顶部对齐（默认） | 底部对齐 | 居中对齐 | 上下对齐并铺满 | 文本基线对齐*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-子元素的显示方向"><a href="#4-子元素的显示方向" class="headerlink" title="4. 子元素的显示方向"></a>4. 子元素的显示方向</h3><p>　　子元素的显示方向可通过 box-direction + box-orient + flex-direction 实现，下面请看实例：</p>
<h4 id="4-1-左到右"><a href="#4-1-左到右" class="headerlink" title="4.1 左到右"></a>4.1 左到右</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-webkit-box-direction</span>: normal;</span><br><span class="line">    <span class="attribute">-webkit-box-orient</span>: horizontal;</span><br><span class="line">    <span class="attribute">-moz-flex-direction</span>: row;</span><br><span class="line">    <span class="attribute">-webkit-flex-direction</span>: row;</span><br><span class="line">    <span class="attribute">flex-direction</span>: row;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/styles/images/css/articlex2.png" alt="子元素左到右对齐方式">

<h4 id="4-2-右到左"><a href="#4-2-右到左" class="headerlink" title="4.2 右到左"></a>4.2 右到左</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-webkit-box-pack</span>: end;</span><br><span class="line">    <span class="attribute">-webkit-box-direction</span>: reverse;</span><br><span class="line">    <span class="attribute">-webkit-box-orient</span>: horizontal;</span><br><span class="line">    <span class="attribute">-moz-flex-direction</span>: row-reverse;</span><br><span class="line">    <span class="attribute">-webkit-flex-direction</span>: row-reverse;</span><br><span class="line">    <span class="attribute">flex-direction</span>: row-reverse;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/styles/images/css/articlex3.png" alt="子元素右到左对齐方式">

<p>　　这里补充说明一点： box 写法的 box-direction 只是改变了子元素的排序，并没有改变对齐方式，需要新增一个 box-pack 来改变对齐方式。</p>
<h4 id="4-3-上到下"><a href="#4-3-上到下" class="headerlink" title="4.3 上到下"></a>4.3 上到下</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-webkit-box-direction</span>: normal;</span><br><span class="line">    <span class="attribute">-webkit-box-orient</span>: vertical;</span><br><span class="line">    <span class="attribute">-moz-flex-direction</span>: column;</span><br><span class="line">    <span class="attribute">-webkit-flex-direction</span>: column;</span><br><span class="line">    <span class="attribute">flex-direction</span>: column;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/styles/images/css/articlex4.png" alt="子元素上到下对齐方式">

<h4 id="4-4-下到上"><a href="#4-4-下到上" class="headerlink" title="4.4 下到上"></a>4.4 下到上</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.box</span>&#123;</span><br><span class="line">    <span class="attribute">-webkit-box-pack</span>: end;</span><br><span class="line">    <span class="attribute">-webkit-box-direction</span>: reverse;</span><br><span class="line">    <span class="attribute">-webkit-box-orient</span>: vertical;</span><br><span class="line">    <span class="attribute">-moz-flex-direction</span>: column-reverse;</span><br><span class="line">    <span class="attribute">-webkit-flex-direction</span>: column-reverse;</span><br><span class="line">    <span class="attribute">flex-direction</span>: column-reverse;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/styles/images/css/articlex5.png" alt="子元素上到下对齐方式">

<h3 id="5-是否允许子元素伸缩"><a href="#5-是否允许子元素伸缩" class="headerlink" title="5. 是否允许子元素伸缩"></a>5. 是否允许子元素伸缩</h3><img src="/styles/images/css/articlex6.png" alt="是否允许子元素伸缩">

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">-webkit-box-flex</span>: <span class="number">1.0</span>;</span><br><span class="line">    <span class="attribute">-moz-flex-grow</span>: <span class="number">1</span>;</span><br><span class="line">    <span class="attribute">-webkit-flex-grow</span>: <span class="number">1</span>;</span><br><span class="line">    <span class="attribute">flex-grow</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">-webkit-box-flex</span>: <span class="number">1.0</span>;</span><br><span class="line">    <span class="attribute">-moz-flex-shrink</span>: <span class="number">1</span>;</span><br><span class="line">    <span class="attribute">-webkit-flex-shrink</span>: <span class="number">1</span>;</span><br><span class="line">    <span class="attribute">flex-shrink</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/styles/images/css/articlex7.png" alt="是否允许子元素伸缩">

<p>　　上面是允许放大，box语法中 box-flex 如果不是0就表示该子元素允许伸缩，而flex是分开的，上面 flex-grow 是允许放大（默认不允许），下面的 flex-shrink 是允许缩小（默认允许）。box-flex 默认值为0，也就是说，在默认的情况下，在两个浏览器中的表现是不一样的：</p>
<img src="/styles/images/css/articlex8.png" alt="是否允许子元素伸缩">

<p>　　这里还有一点，就是新旧语法的算法是不一样的，假设 box-flex 的值不等于0，旧语法中，如果有多余的空间，box-flex 的值越大，说明空白部分的占比越多，反之亦然：</p>
<img src="/styles/images/css/articlex9.png" alt="是否允许子元素伸缩">

<p>　　而新版的语法中，放大的比例是直接按 flex-grow 的值来分配的，flex-grow 的缩放会覆盖 flex-shrink:0，看例子：</p>
<img src="/styles/images/css/articlex10.png" alt="是否允许子元素伸缩">

<p>　　参数：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">box-flex</span>: &lt;value&gt;;</span><br><span class="line">    <span class="comment">/*伸缩：&lt;一个浮点数，默认为0.0，即表示不可伸缩，大于0的值可伸缩，柔性相对&gt;*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">flex-grow</span>: &lt;number&gt;; <span class="comment">/* default 0 */</span></span><br><span class="line">    <span class="comment">/*放大：默认0（即如果有剩余空间也不放大，值为1则放大，2是1的双倍大小，以此类推）*/</span></span><br><span class="line"> </span><br><span class="line">    <span class="attribute">flex-shrink</span>: &lt;number&gt;; <span class="comment">/* default 1 */</span></span><br><span class="line">    <span class="comment">/*缩小：默认1（如果空间不足则会缩小，值为0不缩小）*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-子元素的显示次序"><a href="#6-子元素的显示次序" class="headerlink" title="6. 子元素的显示次序"></a>6. 子元素的显示次序</h3><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">-webkit-box-ordinal-group</span>: <span class="number">1</span>;</span><br><span class="line">    <span class="attribute">-moz-order</span>: <span class="number">1</span>;</span><br><span class="line">    <span class="attribute">-webkit-order</span>: <span class="number">1</span>;</span><br><span class="line">    <span class="attribute">order</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<img src="/styles/images/css/articlex11.png" alt="是否允许子元素伸缩">

<hr>
]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>CSS</tag>
        <tag>Flex</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 原型及原型链</title>
    <url>/2017/01/14/article-js-prototypes/</url>
    <content><![CDATA[<p>　　之前对 JavaScript 中的原型链和原型对象有所了解，每当别人问我什么是原型链和原型对象时，我总是用很官方（其实自己不懂）的解释去描述。有一句话说的好：如果你不能把一个很复杂的东西用最简单的话语描述出来，那就说明你没有真正的理解。</p>
<p>　　本文转自 <a href="https://segmentfault.com/a/1190000000662547#articleHeader4" target="_blank" rel="noopener">trigkit4专栏—— 原型和原型链详解</a>。<a id="more"></a></p>
<hr>

<h2 id="1-私有变量和函数"><a href="#1-私有变量和函数" class="headerlink" title="1. 私有变量和函数"></a>1. 私有变量和函数</h2><p>　　在函数内部定义的变量和函数，如果不对外提供接口，外部是无法访问到的，也就是该函数的私有的变量和函数。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Box</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">var</span> color = <span class="string">"blue"</span>;<span class="comment">//私有变量</span></span></span><br><span class="line"><span class="javascript">        <span class="keyword">var</span> fn = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) //私有函数</span></span></span><br><span class="line">        &#123;</span><br><span class="line">            </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　这样在函数对象<code>Box</code>外部无法访问变量<code>color</code>和<code>fn</code>，他们就变成私有的了：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = <span class="keyword">new</span> Box();</span><br><span class="line">    alert(obj.color);<span class="comment">//弹出 undefined</span></span><br><span class="line">    alert(obj.fn);<span class="comment">//同上</span></span><br></pre></td></tr></table></figure>

<h2 id="2-静态变量和函数"><a href="#2-静态变量和函数" class="headerlink" title="2. 静态变量和函数"></a>2. 静态变量和函数</h2><p>　　当定义一个函数后通过点号 “.” 为其添加的属性和函数，通过对象本身仍然可以访问得到，但是其实例却访问不到，这样的变量和函数分别被称为<strong>静态变量</strong>和<strong>静态函数</strong>。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Obj</span><span class="params">()</span></span>&#123;&#125;;</span></span><br><span class="line"></span><br><span class="line"><span class="actionscript">    Obj.num = <span class="number">72</span>;<span class="comment">//静态变量</span></span></span><br><span class="line"><span class="javascript">    Obj.fn = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)  //静态函数</span></span></span><br><span class="line">    &#123;</span><br><span class="line">        </span><br><span class="line">    &#125;  </span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    alert(Obj.num);<span class="comment">//72</span></span></span><br><span class="line"><span class="actionscript">    alert(<span class="keyword">typeof</span> Obj.fn)<span class="comment">//function</span></span></span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> t = <span class="keyword">new</span> Obj();</span></span><br><span class="line"><span class="actionscript">    alert(t.name);<span class="comment">//undefined</span></span></span><br><span class="line"><span class="actionscript">    alert(<span class="keyword">typeof</span> t.fn);<span class="comment">//undefined</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="3-实例变量和函数"><a href="#3-实例变量和函数" class="headerlink" title="3. 实例变量和函数"></a>3. 实例变量和函数</h2><p>　　在面向对象编程中除了一些库函数我们还是希望在对象定义的时候同时定义一些属性和方法，实例化后可以访问，<code>js</code>也能做到这样:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">          <span class="function"><span class="keyword">function</span> <span class="title">Box</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">                <span class="keyword">this</span>.a=[]; <span class="comment">//实例变量</span></span></span><br><span class="line"><span class="actionscript">                <span class="keyword">this</span>.fn=<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123; <span class="comment">//实例方法  </span></span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(<span class="keyword">typeof</span> Box.a); <span class="comment">//undefined</span></span></span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(<span class="keyword">typeof</span> Box.fn); <span class="comment">//undefined</span></span></span><br><span class="line"><span class="actionscript">            <span class="keyword">var</span> box=<span class="keyword">new</span> Box();</span></span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(<span class="keyword">typeof</span> box.a); <span class="comment">//object</span></span></span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(<span class="keyword">typeof</span> box.fn); <span class="comment">//function</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　为实例变量和方法添加新的方法和属性:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript"><span class="function"><span class="keyword">function</span> <span class="title">Box</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">                <span class="keyword">this</span>.a=[]; <span class="comment">//实例变量</span></span></span><br><span class="line"><span class="actionscript">                <span class="keyword">this</span>.fn=<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123; <span class="comment">//实例方法</span></span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"><span class="actionscript">            <span class="keyword">var</span> box1=<span class="keyword">new</span> Box();</span></span><br><span class="line">            box1.a.push(1);</span><br><span class="line">            box1.fn=&#123;&#125;;</span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(box1.a); <span class="comment">//[1]</span></span></span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(<span class="keyword">typeof</span> box1.fn); <span class="comment">//object</span></span></span><br><span class="line"><span class="actionscript">            <span class="keyword">var</span> box2=<span class="keyword">new</span> Box();</span></span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(box2.a); <span class="comment">//[]</span></span></span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(<span class="keyword">typeof</span> box2.fn); <span class="comment">//function</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　在<code>box1</code>中修改了<code>a</code>和<code>fn</code>，而在<code>box2</code>中没有改变，由于数组和函数都是对象，是引用类型，这就说明<code>box1</code>中的属性和方法与<code>box2</code>中的属性与方法虽然同名但却不是一个引用，而是对<code>Box</code>对象定义的属性和方法的一个复制。</p>
<p>　　这个对属性来说没有什么问题，但是对于方法来说问题就很大了，因为方法都是在做完全一样的功能，但是却又两份复制，如果一个函数对象有上千和实例方法，那么它的每个实例都要保持一份上千个方法的复制，这显然是不科学的，这可肿么办呢，<code>prototype</code>应运而生。</p>
<h2 id="4-基本概念"><a href="#4-基本概念" class="headerlink" title="4. 基本概念"></a>4. 基本概念</h2><p>　　我们创建的每个函数都有一个<code>prototype</code>属性，这个属性是一个指针，指向一个对象，这个对象的用途是包含可以由特定类型的所有实例共享的属性和方法。那么，<code>prototype</code>就是通过调用 <strong>构造函数</strong> 而创建的那个对象实例的原型对象。</p>
<p>　　使用原型的好处是可以让对象实例共享它所包含的属性和方法。也就是说，不必在构造函数中添加定义对象信息，而是可以直接将这些信息添加到原型中。使用构造函数的主要问题就是每个方法都要在每个实例中创建一遍。</p>
<p>　　在<code>JavaScript</code>中,一共有两种类型的值,原始值和对象值。每个对象都有一个内部属性 <code>prototype</code> ,我们通常称之为 <strong>原型</strong> 。原型的值可以是一个对象,也可以是<code>null</code>。如果它的值是一个对象，则这个对象也一定有自己的原型。这样就形成了一条线性的链，我们称之为 <strong>原型链</strong> 。</p>
<h3 id="4-1-含义"><a href="#4-1-含义" class="headerlink" title="4.1 含义"></a>4.1 含义</h3><p>　　函数可以用来作为构造函数来使用。另外只有函数才有<code>prototype</code>属性并且可以访问到，但是对象实例不具有该属性，只有一个内部的不可访问的<code>__proto__</code>属性。<code>__proto__</code>是对象中一个指向相关原型的神秘链接。按照标准，<code>__proto__</code>是不对外公开的，也就是说是个私有属性，但是 <em>Firefox</em> 的引擎将他暴露了出来成为了一个共有的属性，我们可以对外访问和设置。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> Browser = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;&#125;;</span></span><br><span class="line"><span class="actionscript">    Browser.prototype.run = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        alert(<span class="string">"I'm Gecko,a kernel of firefox￼"</span>);</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> Bro = <span class="keyword">new</span> Browser();</span></span><br><span class="line">    Bro.run();</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　当我们调用<code>Bro.run()</code>方法时，由于<code>Bro</code>中没有这个方法，所以，他就会去他的<code>__proto__</code>中去找，也就是Browser.prototype<code>，所以最终执行了该</code>run()`方法（在这里，函数首字母大写的都代表构造函数，以用来区分普通函数）。</p>
<p>　　当调用构造函数创建一个实例的时候，实例内部将包含一个内部指针（<code>__proto__</code>）指向构造函数的<code>prototype</code>，这个连接存在于实例和构造函数的<code>prototype</code>之间，而不是实例与构造函数之间。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript"><span class="function"><span class="keyword">function</span> <span class="title">Person</span><span class="params">(name)</span></span>&#123;                             <span class="comment">//构造函数</span></span></span><br><span class="line"><span class="actionscript">                <span class="keyword">this</span>.name=name;</span></span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line"><span class="javascript">            Person.prototype.printName=<span class="function"><span class="keyword">function</span>(<span class="params"></span>) //原型对象</span></span></span><br><span class="line">            &#123;</span><br><span class="line"><span class="actionscript">                alert(<span class="keyword">this</span>.name);</span></span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line"><span class="actionscript">            <span class="keyword">var</span> person1=<span class="keyword">new</span> Person(<span class="string">'Byron'</span>);<span class="comment">//实例化对象</span></span></span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(person1.__proto__);<span class="comment">//Person</span></span></span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(person1.constructor);<span class="comment">//自己试试看会是什么吧</span></span></span><br><span class="line"><span class="javascript">            <span class="built_in">console</span>.log(Person.prototype);<span class="comment">//指向原型对象Person</span></span></span><br><span class="line"><span class="actionscript">            <span class="keyword">var</span> person2=<span class="keyword">new</span> Person(<span class="string">'Frank'</span>);</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　<code>Person</code>的实例<code>person1</code>中包含了<code>name</code>属性，同时自动生成一个<code>__proto__</code>属性，该属性指向<code>Person</code>的<code>prototype</code>，可以访问到<code>prototype</code>内定义的<code>printName</code>方法，大概就是这个样子的:</p>
<p><img src="/styles/images/js/prototype1.png" alt="ptototype"></p>
<p>　　每个<code>JavaScript</code>函数都有<code>prototype</code>属性，这个属性引用了一个对象，这个对象就是原型对象。原型对象初始化的时候是空的，我们可以在里面自定义任何属性和方法，这些方法和属性都将被该构造函数所创建的对象继承。</p>
<p>　　那么，现在问题来了。构造函数、实例和原型对象三者之间有什么关系呢？</p>
<h2 id="5-构造函数、实例和原型对象的区别"><a href="#5-构造函数、实例和原型对象的区别" class="headerlink" title="5. 构造函数、实例和原型对象的区别"></a>5. 构造函数、实例和原型对象的区别</h2><p>　　实例就是通过构造函数创建的。实例一创造出来就具有<code>constructor</code>属性（指向构造函数）和<code>__proto__</code>属性（指向原型对象），构造函数中有一个<code>prototype</code>属性，这个属性是一个指针，指向它的原型对象。原型对象内部也有一个指针（<code>constructor</code>属性）指向构造函数：<code>Person.prototype.constructor = Person</code>；实例可以访问原型对象上定义的属性和方法。在这里<code>person1</code>和<code>person2</code>就是实例，<code>prototype</code>是他们的原型对象。</p>
<p>　　再举个栗子：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="javascript">    <span class="function"><span class="keyword">function</span> <span class="title">Animal</span>(<span class="params">name</span>)   //积累构造函数</span></span></span><br><span class="line">    &#123;</span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.name = name;<span class="comment">//设置对象属性</span></span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="javascript">    Animal.prototype.behavior = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) //给基类构造函数的<span class="title">prototype</span>添加<span class="title">behavior</span>方法</span></span></span><br><span class="line">    &#123;  </span><br><span class="line"><span class="actionscript">        alert(<span class="string">"this is a "</span>+<span class="keyword">this</span>.name);</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> Dog = <span class="keyword">new</span> Animal(<span class="string">"dog"</span>);<span class="comment">//创建Dog对象</span></span></span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> Cat = <span class="keyword">new</span> Animal(<span class="string">"cat"</span>);<span class="comment">//创建Cat对象</span></span></span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    Dog.behavior();<span class="comment">//通过Dog对象直接调用behavior方法</span></span></span><br><span class="line"><span class="actionscript">    Cat.behavior();<span class="comment">//output "this is a cat"</span></span></span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    alert(Dog.behavior==Cat.behavior);<span class="comment">//output true;</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　可以从程序运行结果看出，<strong>构造函数</strong>的<code>prototype</code>上定义的方法确实可以通过对象直接调用到，而且代码是共享的。（可以试一下将<code>Animal.prototype.behavior</code> 中的<code>prototype</code>属性去掉，看看还能不能运行。）在这里，<code>prototype</code>属性指向<code>Animal</code>对象。</p>
<h2 id="6-数组对象实例"><a href="#6-数组对象实例" class="headerlink" title="6. 数组对象实例"></a>6. 数组对象实例</h2><p>　　再看个数组对象的实例。当我们创建出<code>array1</code>这个对象的时候，<code>array1</code>实际在<code>Javascript</code>引擎中的对象模型如下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> array1 = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>];</span><br></pre></td></tr></table></figure>

<p><img src="/styles/images/js/prototype2.png" alt="ptototype"></p>
<p>　　array1对象具有一个length属性值为3，但是我们可以通过如下的方法来为array1增加元素：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">array1.push(<span class="number">4</span>);</span><br></pre></td></tr></table></figure>

<p>　　<code>push</code>这个方法来自于<code>array1</code>的<code>__proto__</code>成员指向对象的一个方法(<code>Array.prototye.push()</code>)。正是因为所有的数组对象（通过<code>[]</code>来创建的）都包含有一个指向同一个具有<code>push</code>,<code>reverse</code>等方法对象(<code>Array.prototype</code>)的<code>__proto__</code>成员，才使得这些数组对象可以使用<code>push</code>,<code>reverse</code>等方法。</p>
<h2 id="7-函数对象实例"><a href="#7-函数对象实例" class="headerlink" title="7. 函数对象实例"></a>7. 函数对象实例</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Base</span>(<span class="params"></span>) </span>&#123;  </span><br><span class="line">    <span class="keyword">this</span>.id = <span class="string">"base"</span> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/styles/images/js/prototype3.png" alt="ptototype"></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = <span class="keyword">new</span> Base();</span><br></pre></td></tr></table></figure>

<p>　　这样代码的结果是什么，我们在<code>Javascript</code>引擎中看到的对象模型是：</p>
<p><img src="/styles/images/js/prototype4.png" alt="ptototype"></p>
<p>　　<code>new</code>操作符具体干了什么呢?其实很简单，就干了三件事情:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj  = &#123;&#125;;  </span><br><span class="line">obj.__proto__ = Base.prototype;  </span><br><span class="line">Base.call(obj);</span><br></pre></td></tr></table></figure>

<h2 id="8-原型链"><a href="#8-原型链" class="headerlink" title="8. 原型链"></a>8. 原型链</h2><p>　　<strong>原型链</strong>：当从一个对象那里调取属性或方法时，如果该对象自身不存在这样的属性或方法，就会去自己关联的<code>prototype</code>对象那里寻找，如果<code>prototype</code>没有，就会去<code>prototype</code>关联的前辈<code>prototype</code>那里寻找，如果再没有则继续查找<code>Prototype.Prototype</code>引用的对象，依次类推，直到<code>Prototype</code>.….<code>Prototype</code>为<code>undefined</code>（<code>Object</code>的<code>Prototype</code>就是<code>undefined</code>）从而形成了所谓的“<strong>原型链</strong>”。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Shape</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.name = <span class="string">"shape"</span>;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.toString = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">            <span class="keyword">return</span> <span class="keyword">this</span>.name;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">TwoShape</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.name = <span class="string">"2 shape"</span>;</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Triangle</span><span class="params">(side,height)</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.name = <span class="string">"Triangle"</span>;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.side = side;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.height = height;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.getArea = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">            <span class="keyword">return</span> <span class="keyword">this</span>.side*<span class="keyword">this</span>.height/<span class="number">2</span>;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    TwoShape.prototype = <span class="keyword">new</span> Shape();</span></span><br><span class="line"><span class="actionscript">    Triangle.prototype = <span class="keyword">new</span> TwoShape();</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　这里，用构造器<code>Shape()</code>新建了一个实体，然后用它去覆盖该对象的原型。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Shape</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.name = <span class="string">"shape"</span>;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.toString = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">            <span class="keyword">return</span> <span class="keyword">this</span>.name;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">TwoShape</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.name = <span class="string">"2 shape"</span>;</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Triangle</span><span class="params">(side,height)</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.name = <span class="string">"Triangle"</span>;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.side = side;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.height = height;</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.getArea = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">            <span class="keyword">return</span> <span class="keyword">this</span>.side*<span class="keyword">this</span>.height/<span class="number">2</span>;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    TwoShape.prototype = <span class="keyword">new</span> Shape();</span></span><br><span class="line"><span class="actionscript">    Triangle.prototype = <span class="keyword">new</span> TwoShape();</span></span><br><span class="line">    </span><br><span class="line">    TwoShape.prototype.constructor = TwoShape;</span><br><span class="line">    Triangle.prototype.constructor = Triangle;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> my = <span class="keyword">new</span> Triangle(<span class="number">5</span>,<span class="number">10</span>);</span></span><br><span class="line">    my.getArea();</span><br><span class="line"><span class="actionscript">    my.toString();<span class="comment">//Triangle</span></span></span><br><span class="line"><span class="actionscript">    my.constructor;<span class="comment">//Triangle(side,height)</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="9-原型继承"><a href="#9-原型继承" class="headerlink" title="9. 原型继承"></a>9. 原型继承</h2><p>　　<strong>原型继承</strong>：在原型链的末端，就是<code>Object</code>构造函数<code>prototype</code>属性指向的那个原型对象。这个原型对象是所有对象的祖先，这个老祖宗实现了诸如<code>toString</code>等所有对象天生就该具有的方法。其他内置构造函数，如<code>Function</code>，<code>Boolean</code>，<code>String</code>，<code>Date</code>和<code>RegExp</code>等的<code>prototype</code>都是从这个老祖宗传承下来的，但他们各自又定义了自身的属性和方法，从而他们的子孙就表现出各自宗族的那些特征。</p>
<p>　　<code>ECMAScript</code>中，实现继承的方法就是依靠原型链实现的。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Father</span><span class="params">()</span></span>&#123;             <span class="comment">//被继承的函数叫做超类型（父类，基类）</span></span></span><br><span class="line"><span class="actionscript">    <span class="keyword">this</span>.name = <span class="string">"Jack"</span>;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="actionscript"><span class="function"><span class="keyword">function</span> <span class="title">Son</span><span class="params">()</span></span>&#123;          <span class="comment">//继承的函数叫做子类型（子类，派生类）</span></span></span><br><span class="line"><span class="actionscript">    <span class="keyword">this</span>.age = <span class="number">10</span>;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="actionscript"><span class="comment">//通过原型链继承，赋值给子类型的原型属性</span></span></span><br><span class="line"><span class="actionscript"><span class="comment">//new Father()会将father构造里的信息和原型里的信息都交给Son</span></span></span><br><span class="line"></span><br><span class="line"><span class="actionscript">Son.prototype = <span class="keyword">new</span> Father();<span class="comment">//Son继承了Father,通过原型，形成链条</span></span></span><br><span class="line"></span><br><span class="line"><span class="actionscript"><span class="keyword">var</span> son = <span class="keyword">new</span> Son();</span></span><br><span class="line"><span class="actionscript">alert(son.name);<span class="comment">//弹出 Jack</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　<strong>原型链的问题</strong>：原型链虽然很强大，可以用它来实现继承，但它也存在一些问题。其中最主要的问题来自包含引用类型的值原型。包含引用类型的原型属性会被所有实例共享；而这也正是为什么要在构造函数中，而不是在原型对象中定义属性的原因。在通过原型来实现继承时，原型实际上回变成另一个类型的实例。于是，原先的实例属性也就变成了原型的属性。</p>
<p>　　在创建子类型的实例时，不能向超类型的构造函数中传递参数。实际上，应该说是没有办法在不影响所有对象实例的情况下，给超类型的构造函数传递参数。再加上刚刚讨论的由于原型中包含引用类型值所带来的问题，实践中很少会单独使用原型链。</p>
<p>　　再举个栗子：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Person</span><span class="params">(name)</span></span></span></span><br><span class="line">    &#123;</span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.name = name;<span class="comment">//设置对象属性</span></span></span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    Person.prototype.company = <span class="string">"Microsoft"</span>;<span class="comment">//设置原型的属性</span></span></span><br><span class="line"><span class="javascript">    Person.prototype.SayHello = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) //原型的方法</span></span></span><br><span class="line">    &#123;  </span><br><span class="line"><span class="actionscript">        alert(<span class="string">"Hello,I'm "</span>+ <span class="keyword">this</span>.name+ <span class="string">" of "</span> + <span class="keyword">this</span>.company);</span></span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> BillGates = <span class="keyword">new</span> Person(<span class="string">"BillGates"</span>);<span class="comment">//创建person对象</span></span></span><br><span class="line"><span class="actionscript">    BillGates.SayHello();<span class="comment">//继承了原型的内容，输出"Hello,I'm BillGates of Microsoft"</span></span></span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> Jobs = <span class="keyword">new</span> Person(<span class="string">"Jobs"</span>);</span></span><br><span class="line"><span class="actionscript">    Jobs.company = <span class="string">"Apple"</span>;<span class="comment">//设置自己的company属性，掩盖了原型的company属性</span></span></span><br><span class="line"><span class="actionscript">    Jobs.SayHello = <span class="function"><span class="keyword">function</span><span class="params">()</span></span></span></span><br><span class="line">    &#123;</span><br><span class="line"><span class="actionscript">        alert(<span class="string">"Hi,"</span>+<span class="keyword">this</span>.name + <span class="string">" like "</span> + <span class="keyword">this</span>.company);</span></span><br><span class="line">    &#125;;</span><br><span class="line"><span class="actionscript">    Jobs.SayHello();<span class="comment">//自己覆盖的属性和方法，输出"Hi,Jobs like Apple"</span></span></span><br><span class="line"><span class="actionscript">    BillGates.SayHello();<span class="comment">//Jobs的覆盖没有影响原型，BillGates还是照样输出</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="10-ptoto属性"><a href="#10-ptoto属性" class="headerlink" title="10. ptoto属性"></a>10. <strong>ptoto</strong>属性</h2><p>　　<code>__ptoto__</code>属性（IE 浏览器不支持）是实例指向原型对象的一个指针，它的作用就是指向构造函数的原型属性<code>constructor</code>，通过这两个属性，就可以访问原型里的属性和方法了。</p>
<p>　　<code>Javascript</code>中的对象实例本质上是由一系列的属性组成的，在这些属性中，有一个内部的不可见的特殊属性——<code>__proto__</code>，该属性的值指向该对象实例的原型，一个对象实例只拥有一个唯一的原型。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Box</span><span class="params">()</span></span>&#123;        <span class="comment">//大写，代表构造函数</span></span></span><br><span class="line"><span class="actionscript">        Box.prototype.name = <span class="string">"trigkit4"</span>;<span class="comment">//原型属性</span></span></span><br><span class="line"><span class="actionscript">        Box.prototype.age = <span class="string">"21"</span>;</span></span><br><span class="line"><span class="javascript">        Box.prototype.run = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)//原型方法</span></span></span><br><span class="line">        &#123;  </span><br><span class="line"><span class="actionscript">            <span class="keyword">return</span> <span class="keyword">this</span>.name + <span class="keyword">this</span>.age + <span class="string">'studying'</span>;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> box1 = <span class="keyword">new</span> Box();</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> box2 = <span class="keyword">new</span> Box();</span></span><br><span class="line"><span class="actionscript">    alert(box1.constructor);<span class="comment">//构造属性，可以获取构造函数本身，</span></span></span><br><span class="line"><span class="actionscript">                            <span class="comment">//作用是被原型指针定位，然后得到构造函数本身</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="10-1-proto属性和prototype属性的区别"><a href="#10-1-proto属性和prototype属性的区别" class="headerlink" title="10.1 proto属性和prototype属性的区别"></a>10.1 <strong>proto</strong>属性和prototype属性的区别</h3><p>　　<code>prototype</code>是<code>function</code>对象中专有的属性。 </p>
<p>　　<code>__proto__</code>是普通对象的隐式属性，在<code>new</code>的时候，会指向<code>prototype</code>所指的对象；<code>__ptoto__</code>实际上是某个实体对象的属性，而<code>prototype</code>则是属于构造函数的属性。<code>__ptoto__</code>只能在学习或调试的环境下使用。</p>
<h2 id="11-原型模式的执行流程"><a href="#11-原型模式的执行流程" class="headerlink" title="11. 原型模式的执行流程"></a>11. 原型模式的执行流程</h2><ol>
<li><p>先查找构造函数实例里的属性或方法，如果有，就立即返回。</p>
</li>
<li><p>如果构造函数的实例没有，就去它的原型对象里找，如果有，就立即返回。</p>
</li>
</ol>
<h3 id="11-1-原型对象的"><a href="#11-1-原型对象的" class="headerlink" title="11.1 原型对象的"></a>11.1 原型对象的</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Box</span><span class="params">()</span></span>&#123;        <span class="comment">//大写，代表构造函数</span></span></span><br><span class="line"><span class="actionscript">        Box.prototype.name = <span class="string">"trigkit4"</span>;<span class="comment">//原型属性</span></span></span><br><span class="line"><span class="actionscript">        Box.prototype.age = <span class="string">"21"</span>;</span></span><br><span class="line"><span class="javascript">        Box.prototype.run = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)//原型方法</span></span></span><br><span class="line">        &#123;  </span><br><span class="line"><span class="actionscript">            <span class="keyword">return</span> <span class="keyword">this</span>.name + <span class="keyword">this</span>.age + <span class="string">'studying'</span>;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> box1 = <span class="keyword">new</span> Box();</span></span><br><span class="line"><span class="actionscript">    alert(box1.name);<span class="comment">//trigkit4,原型里的值</span></span></span><br><span class="line"><span class="actionscript">    box1.name = <span class="string">"Lee"</span>;</span></span><br><span class="line"><span class="actionscript">    alert(box1.name);<span class="comment">//Lee,就进原则</span></span></span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> box2 = <span class="keyword">new</span> Box();</span></span><br><span class="line"><span class="actionscript">    alert(box2.name);<span class="comment">//trigkit4,原型的值，没有被box1修改</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="11-2-构造函数的"><a href="#11-2-构造函数的" class="headerlink" title="11.2 构造函数的"></a>11.2 构造函数的</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Box</span><span class="params">()</span></span>&#123;                   </span></span><br><span class="line"><span class="actionscript">        <span class="keyword">this</span>.name = <span class="string">"Bill"</span>;</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    Box.prototype.name = <span class="string">"trigkit4"</span>;<span class="comment">//原型属性</span></span></span><br><span class="line"><span class="actionscript">    Box.prototype.age = <span class="string">"21"</span>;</span></span><br><span class="line"><span class="javascript">    Box.prototype.run = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)//原型方法</span></span></span><br><span class="line">    &#123;  </span><br><span class="line"><span class="actionscript">            <span class="keyword">return</span> <span class="keyword">this</span>.name + <span class="keyword">this</span>.age + <span class="string">'studying'</span>;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> box1 = <span class="keyword">new</span> Box();</span></span><br><span class="line"><span class="actionscript">    alert(box1.name);<span class="comment">//Bill,原型里的值</span></span></span><br><span class="line"><span class="actionscript">    box1.name = <span class="string">"Lee"</span>;</span></span><br><span class="line"><span class="actionscript">    alert(box1.name);<span class="comment">//Lee,就进原则</span></span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　综上，整理一下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">Person</span><span class="params">()</span></span>&#123;&#125;;</span></span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    Person.prototype.name = <span class="string">"trigkit4"</span>;</span></span><br><span class="line"><span class="actionscript">    Person.prototype.say = <span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line"><span class="actionscript">        alert(<span class="string">"￼Hi"</span>);</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> p1 = <span class="keyword">new</span> Person();<span class="comment">//prototype是p1和p2的原型对象</span></span></span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> p2 = <span class="keyword">new</span> Person();<span class="comment">//p2为实例化对象，其内部有一个__proto__属性，指向Person的prototype</span></span></span><br><span class="line">    </span><br><span class="line"><span class="javascript">    <span class="built_in">console</span>.log(p1.prototype);<span class="comment">//undefined,这个属性是一个对象，访问不到</span></span></span><br><span class="line"><span class="javascript">    <span class="built_in">console</span>.log(Person.prototype);<span class="comment">//Person</span></span></span><br><span class="line"><span class="javascript">    <span class="built_in">console</span>.log(Person.prototype.constructor);<span class="comment">//原型对象内部也有一个指针（constructor属性）指向构造函数</span></span></span><br><span class="line"><span class="javascript">    <span class="built_in">console</span>.log(p1.__proto__);<span class="comment">//这个属性是一个指针指向prototype原型对象</span></span></span><br><span class="line"><span class="actionscript">    p1.say();<span class="comment">//实例可以访问到在原型对象上定义的属性和方法</span></span></span><br><span class="line">    </span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">构造函数.prototype = 原型对象</span><br><span class="line">原型对象.constructor = 构造函数(模板)</span><br><span class="line">原型对象.isPrototypeof(实例对象)  <span class="comment">// 判断实例对象的原型 是不是当前对象</span></span><br></pre></td></tr></table></figure>

<h2 id="12-工厂模式"><a href="#12-工厂模式" class="headerlink" title="12. 工厂模式"></a>12. 工厂模式</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"> <span class="function"><span class="keyword">function</span> <span class="title">createObject</span>(<span class="params">name,age</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> obj = <span class="keyword">new</span> <span class="built_in">Object</span>();</span><br><span class="line">    obj.name = name;</span><br><span class="line">    obj.age = age;</span><br><span class="line">    <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　工厂模式解决了实例化对象大量重复的问题，但还有一个问题，那就是根本无法搞清楚他们到底是哪个对象的实例。使用构造函数的方法，既解决了重复实例化的问题，又解决了对象识别的问题。</p>
<p>　　使用构造函数的方法和工厂模式的不同之处在于：</p>
<hr>

<ol>
<li>构造函数方法没有显示的创建对象(<code>new Object()</code>);</li>
<li>直接将属性和方法赋值给<code>this</code>对象;</li>
<li>没有<code>return</code> 语句。</li>
</ol>
<hr>

<p>　　当使用了构造函数，并且<code>new</code> 构造函数，那么就在后台执行了<code>new Object()</code>；</p>
<p>　　函数体内的<code>this</code>代表了<code>new Object()</code>出来的对象。</p>
<hr>

<ol>
<li>判断属性是在构造函数的实例里，还是在原型里，可以使用<code>hasOwnProperty()</code>函数</li>
<li>字面量创建的方式使用<code>constructor</code>属性不会指向实例，而会指向<code>Object</code>，构造函数创建的方式则相反；为什么指向<code>Object</code>？因为<code>Box.prototype = {}</code>;这种写法其实就是创建了一个新对象。而每创建一个函数，就会同时创建它的<code>prototype</code>，这个对象也会自动获取<code>constructor</code>属性</li>
<li>如果是实例方法，不同的实例化，他们的方法地址是不一样的，是唯一的</li>
<li>如果是原型方法，那么他们的地址的共享的</li>
</ol>
<hr>]]></content>
      <categories>
        <category>面试总结</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>IIFE 详解</title>
    <url>/2017/01/14/article-js-IIFE/</url>
    <content><![CDATA[<p>　　在 JavaScript 中，每一个函数在被调用的时候都会创建一个执行上下文，在该函数内部定义的变量和函数只能在该函数内部被使用，而正是因为这个上下文，使得我们在调用函数的时候能创建一些私有变量。</p>
<p>　　<strong>本文参考 <a href="https://zhuanlan.zhihu.com/p/23629542" target="_blank" rel="noopener">合理使用 IIFE 优化 JS 引擎的性能</a>、<a href="http://web.jobbole.com/82520/" target="_blank" rel="noopener">详解javascript立即执行函数表达式（IIFE）</a>。</strong><a id="more"></a></p>
<h2 id="1-立即执行函数（IIFE）"><a href="#1-立即执行函数（IIFE）" class="headerlink" title="1. 立即执行函数（IIFE）"></a>1. 立即执行函数（IIFE）</h2><p>　　说起<strong>立即执行函数</strong>（IIFE，Immediately-invoked function expression）大家应该都不陌生，在 <code>JavaScript</code> 中可以声明一个函数然后立即执行它：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;<span class="comment">/* 函数体 */</span>&#125;)()</span><br><span class="line"></span><br><span class="line">!<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;<span class="comment">/* 函数体 */</span>&#125;()</span><br></pre></td></tr></table></figure>

<p>　　<code>IIFE</code> 通常用于实现私有变量、实现独立模块等等地方，比如喜闻乐见的 <code>jQuery</code> 最顶层的结构就是这样的：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">global, factory</span>) </span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;)(<span class="keyword">typeof</span> <span class="built_in">window</span> !== <span class="string">"undefined"</span> ? <span class="built_in">window</span> : <span class="keyword">this</span>, <span class="function"><span class="keyword">function</span>(<span class="params">window, noGlobal</span>) </span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="1-1-写法"><a href="#1-1-写法" class="headerlink" title="1.1 写法"></a>1.1 写法</h3><p>　　<strong>立即执行函数</strong> 的写法可以说是五花八门：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 最常用的两种写法</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;()); <span class="comment">// 老道推荐写法</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;)(); <span class="comment">// 当然这种也可以</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// 括号和JS的一些操作符（如 = &amp;&amp; || ,等）可以在函数表达式和函数声明上消除歧义</span></span><br><span class="line"><span class="comment">// 如下代码中，解析器已经知道一个是表达式了，于是也会把另一个默认为表达式</span></span><br><span class="line"><span class="comment">// 但是两者交换则会报错</span></span><br><span class="line"><span class="keyword">var</span> i = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="keyword">return</span> <span class="number">10</span>; &#125;();</span><br><span class="line"><span class="literal">true</span> &amp;&amp; <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;();</span><br><span class="line"><span class="number">0</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;();</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 如果你不怕代码晦涩难读，也可以选择一元运算符</span></span><br><span class="line">!<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;();</span><br><span class="line">~<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;();</span><br><span class="line">-<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;();</span><br><span class="line">+<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;();</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 你也可以这样</span></span><br><span class="line"><span class="keyword">new</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;</span><br><span class="line"><span class="keyword">new</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="comment">/* code */</span> &#125;() <span class="comment">// 带参数</span></span><br></pre></td></tr></table></figure>

<p>　　<strong>无论何时，给立即执行函数加上括号是个好习惯</strong>。</p>
<h3 id="1-2-与闭包的暧昧关系"><a href="#1-2-与闭包的暧昧关系" class="headerlink" title="1.2 与闭包的暧昧关系"></a>1.2 与闭包的暧昧关系</h3><p>　　立即执行函数能配合闭包保存状态。</p>
<p>　　像普通的函数传参一样，立即执行函数也能传参数。如果在函数内部再定义一个函数，而里面的那个函数能引用外部的变量和参数（闭包），利用这一点，我们能使用立即执行函数锁住变量保存状态。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 并不会像你想象那样的执行，因为i的值没有被锁住</span></span><br><span class="line"><span class="comment">// 当我们点击链接的时候，其实for循环已经执行完了</span></span><br><span class="line"><span class="comment">// 于是在点击的时候i的值其实已经是elems.length了</span></span><br><span class="line"><span class="keyword">var</span> elems = <span class="built_in">document</span>.getElementsByTagName( <span class="string">'a'</span> );</span><br><span class="line"> </span><br><span class="line"><span class="keyword">for</span> ( <span class="keyword">var</span> i = <span class="number">0</span>; i &lt; elems.length; i++ ) &#123;</span><br><span class="line"> </span><br><span class="line">  elems[ i ].addEventListener( <span class="string">'click'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>)</span>&#123;</span><br><span class="line">    e.preventDefault();</span><br><span class="line">    alert( <span class="string">'I am link #'</span> + i );</span><br><span class="line">  &#125;, <span class="string">'false'</span> );</span><br><span class="line"> </span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 这次我们得到了想要的结果</span></span><br><span class="line"><span class="comment">// 因为在立即执行函数内部，i的值传给了lockedIndex，并且被锁在内存中</span></span><br><span class="line"><span class="comment">// 尽管for循环结束后i的值已经改变，但是立即执行函数内部lockedIndex的值并不会改变</span></span><br><span class="line"><span class="keyword">var</span> elems = <span class="built_in">document</span>.getElementsByTagName( <span class="string">'a'</span> );</span><br><span class="line"> </span><br><span class="line"><span class="keyword">for</span> ( <span class="keyword">var</span> i = <span class="number">0</span>; i &lt; elems.length; i++ ) &#123;</span><br><span class="line"> </span><br><span class="line">  (<span class="function"><span class="keyword">function</span>(<span class="params"> lockedInIndex </span>)</span>&#123;</span><br><span class="line"> </span><br><span class="line">    elems[ i ].addEventListener( <span class="string">'click'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>)</span>&#123;</span><br><span class="line">      e.preventDefault();</span><br><span class="line">      alert( <span class="string">'I am link #'</span> + lockedInIndex );</span><br><span class="line">    &#125;, <span class="string">'false'</span> );</span><br><span class="line"> </span><br><span class="line">  &#125;)( i );</span><br><span class="line"> </span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 你也可以这样，但是毫无疑问上面的代码更具有可读性</span></span><br><span class="line"><span class="keyword">var</span> elems = <span class="built_in">document</span>.getElementsByTagName( <span class="string">'a'</span> );</span><br><span class="line"> </span><br><span class="line"><span class="keyword">for</span> ( <span class="keyword">var</span> i = <span class="number">0</span>; i &lt; elems.length; i++ ) &#123;</span><br><span class="line"> </span><br><span class="line">  elems[ i ].addEventListener( <span class="string">'click'</span>, (<span class="function"><span class="keyword">function</span>(<span class="params"> lockedInIndex </span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">e</span>)</span>&#123;</span><br><span class="line">      e.preventDefault();</span><br><span class="line">      alert( <span class="string">'I am link #'</span> + lockedInIndex );</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;)( i ), <span class="string">'false'</span> );</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　其实上面代码的<code>lockedIndex</code>也可以换成<code>i</code>，因为两个<code>i</code>是在不同的作用域里，所以不会互相干扰，但是写成不同的名字更好解释。以上便是<strong>立即执行函数+闭包的作用</strong>。</p>
<h3 id="1-3-IIFE-与-Module-模式"><a href="#1-3-IIFE-与-Module-模式" class="headerlink" title="1.3 IIFE 与 Module 模式"></a>1.3 IIFE 与 Module 模式</h3><p>　　立即执行函数在模块化中也大有用处。用立即执行函数处理模块化可以减少全局变量造成的空间污染，构造更多的私有变量。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 创建一个立即执行的匿名函数</span></span><br><span class="line"><span class="comment">// 该函数返回一个对象，包含你要暴露的属性</span></span><br><span class="line"><span class="comment">// 如下代码如果不使用立即执行函数，就会多一个属性i</span></span><br><span class="line"><span class="comment">// 如果有了属性i，我们就能调用counter.i改变i的值</span></span><br><span class="line"><span class="comment">// 对我们来说这种不确定的因素越少越好</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">var</span> counter = (<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> i = <span class="number">0</span>;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="keyword">get</span>: function()&#123;</span><br><span class="line">      <span class="keyword">return</span> i;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="keyword">set</span>: function( val )&#123;</span><br><span class="line">      i = val;</span><br><span class="line">    &#125;,</span><br><span class="line">    increment: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> ++i;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;());</span><br><span class="line"> </span><br><span class="line"><span class="comment">// counter其实是一个对象</span></span><br><span class="line"> </span><br><span class="line">counter.get(); <span class="comment">// 0</span></span><br><span class="line">counter.set( <span class="number">3</span> );</span><br><span class="line">counter.increment(); <span class="comment">// 4</span></span><br><span class="line">counter.increment(); <span class="comment">// 5</span></span><br><span class="line"> </span><br><span class="line">counter.i; <span class="comment">// undefined i并不是counter的属性</span></span><br><span class="line">i; <span class="comment">// ReferenceError: i is not defined (函数内部的是局部变量)</span></span><br></pre></td></tr></table></figure>

<h3 id="1-4-自执行匿名函数和立即执行的函数表达式区别"><a href="#1-4-自执行匿名函数和立即执行的函数表达式区别" class="headerlink" title="1.4 自执行匿名函数和立即执行的函数表达式区别"></a>1.4 自执行匿名函数和立即执行的函数表达式区别</h3><p>　　在这篇帖子里，我们一直叫自执行函数，确切的说是自执行匿名函数（Self-executing anonymous function），但英文原文作者一直倡议使用立即调用的函数表达式（Immediately-Invoked Function Expression）这一名称，作者又举了一堆例子来解释，好吧，我们来看看：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这是一个自执行的函数，函数内部执行自身，递归</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123; foo(); &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这是一个自执行的匿名函数，因为没有标示名称</span></span><br><span class="line"><span class="comment">// 必须使用arguments.callee属性来执行自己</span></span><br><span class="line"><span class="keyword">var</span> foo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; <span class="built_in">arguments</span>.callee(); &#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这可能也是一个自执行的匿名函数，仅仅是foo标示名称引用它自身</span></span><br><span class="line"><span class="comment">// 如果你将foo改变成其它的，你将得到一个used-to-self-execute匿名函数</span></span><br><span class="line"><span class="keyword">var</span> foo = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; foo(); &#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 有些人叫这个是自执行的匿名函数（即便它不是），因为它没有调用自身，它只是立即执行而已。</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; <span class="comment">/* code */</span> &#125; ());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 为函数表达式添加一个标示名称，可以方便Debug</span></span><br><span class="line"><span class="comment">// 但一定命名了，这个函数就不再是匿名的了</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123; <span class="comment">/* code */</span> &#125; ());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 立即调用的函数表达式（IIFE）也可以自执行，不过可能不常用罢了</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; <span class="built_in">arguments</span>.callee(); &#125; ());</span><br><span class="line">(<span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123; foo(); &#125; ());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 另外，下面的代码在黑莓5里执行会出错，因为在一个命名的函数表达式里，他的名称是undefined</span></span><br><span class="line"><span class="comment">// 呵呵，奇怪</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123; foo(); &#125; ());</span><br></pre></td></tr></table></figure>

<h2 id="2-针对-JS-引擎的一处性能优化"><a href="#2-针对-JS-引擎的一处性能优化" class="headerlink" title="2. 针对 JS 引擎的一处性能优化"></a>2. 针对 JS 引擎的一处性能优化</h2><p>　　先从一个小问题说起吧，想实现一个立即执行的函数，上面提到有很多种写法，比如下面这两种：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 方法一，传入一个匿名函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span>(<span class="params">f</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> f();</span><br><span class="line">&#125;</span><br><span class="line">run(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 方法二，使用IIFE</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;)()</span><br></pre></td></tr></table></figure>

<p>　　是的，这两种写法完全是等价的，无论怎么看都不会有什么区别，但是在一些 <code>JavaScript</code> 引擎中，它们其实性能相差甚远。</p>
<p>　　使用 <code>Node</code> 分别对两种情况运行了十万次，方法一的运行时间平均在 360 毫秒左右，方法二平均是 50 毫秒左右，性能相差 7 倍还多。</p>
<p>　　为什么使用 <code>IIFE</code> 之后性能会提升那么多呢？这就要从 <code>JS</code> 引擎（比如V8、SpiderMonkey）对于函数的优化上说起了。</p>
<p>　　现在的 <code>JS</code> 引擎都是十分聪明的，它们在真正执行代码之前会对代码中函数声明做一遍 pre-parse（预解析），为啥要做 pre-parse 呢？因为实际情况中大多数的函数都不是立即被使用的（甚至完全没被调用过），不需要对它们做一次完整的解析，只需要做一次性能开销更小的 pre-parse（比如检查一下语法错误），等函数真正被调用时，再进行完整的 full-parse。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 下面的函数会先进行pre-parse</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2秒之后函数被执行，又会进行一遍full-parse</span></span><br><span class="line">setTimeout(foo, <span class="number">2000</span>);</span><br></pre></td></tr></table></figure>

<p>　　但是有个问题！对于立即执行函数这种奇葩来说，它不适用于上面的规则，应该直接进行 full-parse。现在的大多数引擎也完全考虑到了这一点：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 只会进行一次full-parse</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">   <span class="comment">//......</span></span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>　　但是还有个问题！！现在的大多数引擎检测 <code>IIFE</code> 的时候都不完全，大部分都是通过判别函数声明前有没有类似『 ( 』或者『 ! 』这样的字符来实现的，比如下面这种情况就被忽略掉了：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这里要进行pre-parse和full-parse，而前者是多余的</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span>(<span class="params">f</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> f();</span><br><span class="line">&#125;</span><br><span class="line">run(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>　　所以我们可以通过一个小 trick 来优化这里的性能（加了一对括号），这样引擎就会把这里识别为立即执行函数，然后只做一次 full-parse：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 只进行一次full-parse</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">run</span>(<span class="params">f</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> f();</span><br><span class="line">&#125;</span><br><span class="line">run((<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;));</span><br></pre></td></tr></table></figure>

<p>　　所以针对这个问题，有一个专门的小工具来解决：</p>
<p>　　<a href="https://github.com/nolanlawson/optimize-js" target="_blank" rel="noopener">nolanlawson/optimize-js: Optimize a JavaScript file for faster initial load by wrapping eagerly-invoked functions</a></p>
<p>　　还有一个相关的讨论：</p>
<p>　　<a href="https://github.com/mishoo/UglifyJS2/issues/886" target="_blank" rel="noopener">Turn off negate_iife by default as it hurts V8 performance. · Issue #886 · mishoo/UglifyJS2</a></p>
<p>　　这个看似不起眼的 trick 实际对于性能有很显著的提升：</p>
<p><img src="/styles/images/js/trick.png" alt="trick"></p>
<p>　　这个问题本质上来讲是 <code>JS</code> 引擎对于立即执行函数的识别有遗漏导致的，比如在 Safari 10 中这个问题基本不会发生，而 Chrome 的 V8 中就经常出现。不过感觉随着引擎版本的迭代，这个问题应该会得到修复。</p>
<hr>]]></content>
      <categories>
        <category>学习笔记</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 变量作用域</title>
    <url>/2017/01/13/article-js-variate/</url>
    <content><![CDATA[<p>　　在 <code>JavaScript</code> 当中一个变量的作用域（scope）是程序中定义这个变量的区域。变量分为两类：全局（global）的和局部的。其中全局变量的作用域是全局性的，即在 <code>JavaScript</code> 代码中，它处处都有定义。而在函数之内声明的变量，就只在函数体内部有定义。它们是局部变量，作用域是局部性的。 <strong>函数的参数也是局部变量</strong>，它们只在函数体内部有定义，<strong>局部变量省略了 var 也就默认成为了全局变量</strong>。</p>
<p>　　在函数体内部，局部变量的优先级高于同名的全局变量，如果在函数体内声明一个局部变量或者函数的参数中带有的变量和全局变量重名，那么全局变量就会被局部变量所遮盖，<strong>函数体内部可以读取到函数外的变量，而函数外不能读取到函数内的变量</strong>。<a id="more"></a></p>
<p>先看一个实例：</p>
<ul>
<li>code 1</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> i=<span class="number">10</span>;   </span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;   </span><br><span class="line">    alert(i);   </span><br><span class="line">&#125;;   </span><br><span class="line">a();</span><br></pre></td></tr></table></figure>

<ul>
<li>code 2</li>
</ul>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> i=<span class="number">10</span>;   </span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;   </span><br><span class="line">    alert(i);   </span><br><span class="line">    <span class="keyword">var</span> i = <span class="number">2</span>;   </span><br><span class="line">&#125;;   </span><br><span class="line">a();</span><br></pre></td></tr></table></figure>

<p>　　根据“多年”的编程经验你可能觉得这两个代码输出是一样的，但是事实却是第一个代码正常输出了变量的值—10，而第二个代码输出的却是 undefined。由此可以引出关于变量应该关注的几个问题：</p>
<h3 id="1-函数作用域"><a href="#1-函数作用域" class="headerlink" title="1. 函数作用域"></a>1. 函数作用域</h3><p>　　为什么说<code>JavaScript</code>没有块级作用域呢，有以下代码为证：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name=<span class="string">"global"</span>;  </span><br><span class="line"><span class="keyword">if</span>(<span class="literal">true</span>)&#123;  </span><br><span class="line">    <span class="keyword">var</span> name=<span class="string">"local"</span>;  </span><br><span class="line">    <span class="built_in">console</span>.log(name)  </span><br><span class="line">&#125;  </span><br><span class="line"><span class="built_in">console</span>.log(name);</span><br></pre></td></tr></table></figure>

<p>　　都输出是 “local”，如果有块级作用域，明显 <code>if</code> 语句将创建局部变量 name，并不会修改全局 name，可是没有这样，所以Js没有块级作用域。</p>
<h3 id="2-声明提升"><a href="#2-声明提升" class="headerlink" title="2. 声明提升"></a>2. 声明提升</h3><p>　　<code>JavaScript</code> 的变量声明具有<code>hoisting</code>机制，<code>JavaScript</code>引擎在执行的时候，会把所有变量的声明都提升到当前作用域的最前面，包括变量和函数的声明。</p>
<p>　　先看一段代码:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> a = <span class="string">"1"</span>;</span><br><span class="line">  <span class="keyword">var</span> f = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;;</span><br><span class="line">  <span class="keyword">var</span> b = <span class="string">"2"</span>;</span><br><span class="line">  <span class="keyword">var</span> c = <span class="string">"3"</span>;</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>　　变量 a,f,b,c 的声明会被提升到函数作用域的最前面，类似如下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> a,f,b,c;</span><br><span class="line">  a = <span class="string">"1"</span>;</span><br><span class="line">  f = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;;</span><br><span class="line">  b = <span class="string">"2"</span>;</span><br><span class="line">  c = <span class="string">"3"</span>;</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>　　请注意函数表达式并没有被提升，这也是函数表达式与函数声明的区别。进一步看二者的区别：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="comment">//var f1,function f2()&#123;&#125;; //hoisting,被隐式提升的声明</span></span><br><span class="line"> </span><br><span class="line">  f1(); <span class="comment">//ReferenceError: f1 is not defined</span></span><br><span class="line">  f2();</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">var</span> f1 = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">f2</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>　　上面代码中函数声明<code>f2</code>被提升，所以在前面调用<code>f2</code>是没问题的。虽然变量<code>f1</code>也被提升，但<code>f1</code>提升后的值为<code>undefined</code>,其真正的初始值是在执行到函数表达式处被赋予的。所以只有声明是被提升的。</p>
<ul>
<li><strong>名字解析顺序</strong></li>
</ul>
<p>　　<code>JavaScript</code> 中一个名字(name)以四种方式进入作用域(scope)，其优先级顺序如下：</p>
<ol>
<li>语言内置：所有的作用域中都有 this 和 arguments 关键字</li>
<li>形式参数：函数的参数在函数作用域中都是有效的</li>
<li>函数声明：形如function foo() {}</li>
<li>变量声明：形如var bar;</li>
</ol>
<p>　　名字声明的优先级如上所示，也就是说如果一个变量的名字与函数的名字相同，那么函数的名字会覆盖变量的名字，无论其在代码中的顺序如何。但名字的初始化却是按其在代码中书写的顺序进行的，不受以上优先级的影响。看代码：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">var</span> foo;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">typeof</span> foo); <span class="comment">//function</span></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>)</span>&#123;&#125;</span><br><span class="line">    foo = <span class="string">"foo"</span>;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">typeof</span> foo); <span class="comment">//string</span></span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>　　如果形式参数中有多个同名变量，那么最后一个同名参数会覆盖其他同名参数，即使最后一个同名参数并没有定义。</p>
<p>　　以上的名字解析优先级存在例外，比如可以覆盖语言内置的名字arguments。</p>
<h3 id="3-作用域链"><a href="#3-作用域链" class="headerlink" title="3. 作用域链"></a>3. 作用域链</h3><p>　　先来看一段代码：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> name=<span class="string">"lwy"</span>;  </span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">t</span>(<span class="params"></span>)</span>&#123;  </span><br><span class="line">    <span class="keyword">var</span> name=<span class="string">"tlwy"</span>;  </span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">s</span>(<span class="params"></span>)</span>&#123;  </span><br><span class="line">        <span class="keyword">var</span> name=<span class="string">"slwy"</span>;  </span><br><span class="line">        <span class="built_in">console</span>.log(name);  </span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">ss</span>(<span class="params"></span>)</span>&#123;  </span><br><span class="line">        <span class="built_in">console</span>.log(name);  </span><br><span class="line">    &#125;  </span><br><span class="line">    s();  </span><br><span class="line">    ss();  </span><br><span class="line">&#125;  </span><br><span class="line">t();</span><br></pre></td></tr></table></figure>

<p>　　当执行 <code>s()</code> 时，将创建函数s的执行环境(调用对象),并将该对象置于链表开头，然后将函数 t 的调用对象链接在之后，最后是全局对象。然后从链表开头寻找变量 name,很明显 name 是 “slwy”。但执行 <code>ss()</code>时，作用域链是： <code>ss() -&gt; t() -&gt; window</code>，所以 name 是 “tlwy”，下面看一个很容易犯错的例子：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span>  </span><br><span class="line"><span class="actionscript"><span class="function"><span class="keyword">function</span> <span class="title">buttonInit</span><span class="params">()</span></span>&#123;  </span></span><br><span class="line"><span class="actionscript">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">1</span>;i&lt;<span class="number">4</span>;i++)&#123;  </span></span><br><span class="line"><span class="javascript">        <span class="keyword">var</span> b=<span class="built_in">document</span>.getElementById(<span class="string">"button"</span>+i);  </span></span><br><span class="line"><span class="actionscript">        b.addEventListener(<span class="string">"click"</span>,<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123; alert(<span class="string">"Button"</span>+i);&#125;,<span class="literal">false</span>);  </span></span><br><span class="line">    &#125;  </span><br><span class="line">&#125;  </span><br><span class="line"><span class="javascript"><span class="built_in">window</span>.onload=buttonInit;  </span></span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">"button1"</span>&gt;</span>Button1<span class="tag">&lt;/<span class="name">button</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">"button2"</span>&gt;</span>Button2<span class="tag">&lt;/<span class="name">button</span>&gt;</span>  </span><br><span class="line">    <span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">"button3"</span>&gt;</span>Button3<span class="tag">&lt;/<span class="name">button</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span>  </span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　当文档加载完毕，给几个按钮注册点击事件，当我们点击按钮时，会弹出什么提示框呢？</p>
<p>　　很容易犯错，对是的，三个按钮都是弹出：”Button4”，你答对了吗？当注册事件结束后， i 的值为4，当点击按钮时，事件函数即<code>function(){ alert(&quot;Button&quot;+i);}</code>这个匿名函数中没有i，根据作用域链，所以到<code>buttonInit</code>函数中找，此时i的值为4，所以弹出 ”button4“。</p>
<ul>
<li><strong>with 语句</strong></li>
</ul>
<p>　　说到作用域链，不得不说 with<code>语句。</code>with` 语句主要用来临时扩展作用域链，将语句中的对象添加到作用域的头部。看下面代码：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">person=&#123;<span class="attr">name</span>:<span class="string">"yhb"</span>,<span class="attr">age</span>:<span class="number">22</span>,<span class="attr">height</span>:<span class="number">175</span>,<span class="attr">wife</span>:&#123;<span class="attr">name</span>:<span class="string">"lwy"</span>,<span class="attr">age</span>:<span class="number">21</span>&#125;&#125;;  </span><br><span class="line"><span class="keyword">with</span>(person.wife)&#123;  </span><br><span class="line">    <span class="built_in">console</span>.log(name);  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>　　with 语句将 person.wife 添加到当前作用域链的头部，所以输出的就是：“lwy”。with 语句结束后，作用域链恢复正常。</p>
<hr>]]></content>
      <categories>
        <category>面试总结</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>gulp 实用指南</title>
    <url>/2017/01/11/article-gulp-configure/</url>
    <content><![CDATA[<p>　　<strong>本文旨在记录 gulp 配置及使用的过程，方便日后查阅。（本文参考 <a href="http://www.ydcss.com/" target="_blank" rel="noopener">一点|gulp详细入门教程</a> ）</strong></p>
<p>　　gulp 是前端开发过程中对代码进行构建的工具，是自动化项目的构建利器；她不仅能对网站资源进行优化，而且在开发过程中很多重复的任务能够使用正确的工具自动完成；使用她，我们不仅可以很愉快的编写代码，而且大大提高我们的工作效率。<a id="more"></a></p>
<p>　　gulp 是基于 Nodejs 的自动任务运行器， 她能自动化地完成javascript/coffee/sass/less/html/image/css等文件的的测试、检查、合并、压缩、格式化、浏览器自动刷新、部署文件生成，并监听文件在改动后重复指定的这些步骤。在实现上，它借鉴了Unix操作系统的管道（pipe）思想，前一级的输出，直接变成后一级的输入，使得在操作上非常简单。通过本文，我们将学习如何使用Gulp来改变开发流程，从而使开发更加快速高效。gulp 和 grunt 非常类似，但相比于 grunt 的频繁 IO 操作，gulp 的流操作，能更快地更便捷地完成构建工作。</p>
<p>　　gulp 参考地址：<a href="http://gulpjs.com" target="_blank" rel="noopener">gulp官方网址</a>、<a href="http://gulpjs.com/plugins" target="_blank" rel="noopener">gulp插件地址</a>、<a href="https://github.com/gulpjs/gulp/blob/master/docs/API.md" target="_blank" rel="noopener">gulp 官方API</a>、<a href="http://www.ydcss.com/archives/424" target="_blank" rel="noopener">gulp 中文API</a>、<a href="http://www.gulpjs.com.cn/" target="_blank" rel="noopener">gulp.js 中文网</a>… …</p>
<br>

<h2 id="gulp-配置"><a href="#gulp-配置" class="headerlink" title="gulp 配置"></a>gulp 配置</h2><p>　　首先当然是安装 <a href="https://nodejs.org/en/" target="_blank" rel="noopener">nodejs</a>，通过 <a href="https://nodejs.org/en/" target="_blank" rel="noopener">nodejs</a> 的 npm 全局安装和项目安装 gulp，其次在项目里安装所需要的 gulp 插件，然后新建 gulp 的配置文件 gulpfile.js 并写好配置信息（定义 gulp 任务），最后通过命令提示符运行 gulp 任务即可。</p>
<p>　　<strong>安装nodejs -&gt; 全局安装gulp -&gt; 项目安装gulp以及gulp插件 -&gt; 配置gulpfile.js -&gt; 运行任务</strong></p>
<h3 id="1-node-js-安装"><a href="#1-node-js-安装" class="headerlink" title="1. node.js 安装"></a>1. node.js 安装</h3><p>　　打开<a href="https://nodejs.org/en/" target="_blank" rel="noopener">nodejs官网</a>，点击硕大的绿色 Download 按钮，它会根据系统信息选择对应版本（.msi文件）。然后像安装QQ一样安装它就可以了（安装路径随意）。</p>
<p>　　通过命令行　<em>node -v</em>　和　<em>npm -v</em>　分别检查 node 和 npm 是否全局安装成功（如下图出现版本号即为安装成功，npm是在安装nodejs时一同安装的）。</p>
<img src="/styles/images/gulp/node.png" alt="nodejs安装成功">

<h3 id="2-npm-使用"><a href="#2-npm-使用" class="headerlink" title="2. npm 使用"></a>2. npm 使用</h3><p>　　npm（node package manager）nodejs 的包管理器，用于 node 插件管理（包括安装、卸载、管理依赖等），使用npm安装插件：命令提示符执行 <strong>npm install &lt;name&gt; -g --save-dev</strong> ,-g 为全局安装、--save-dev 为保存到本地，信息会配置到 package.json 内。</p>
<p>　　因为 npm 的服务器在国外，受网络影响大，可能出现异常，所以我们选择<a href="http://npm.taobao.org/" target="_blank" rel="noopener"> 淘宝的 npmjs.org 镜像 </a>，命令提示符执行 <strong>npm install cnpm -g –registry=<a href="https://registry.npm.taobao.org" target="_blank" rel="noopener">https://registry.npm.taobao.org</a></strong>；　<strong>注意</strong>：安装完后最好查看其版本号 <em>cnpm -v</em> 或关闭命令提示符重新打开，安装完直接使用有可能会出现错误。</p>
<p>　　<strong>注</strong>：cnpm 跟 npm 用法完全一致，只是在执行命令时将 npm 改为 cnpm （以下操作将以 cnpm 代替 npm ）。</p>
<h3 id="3-gulp-安装"><a href="#3-gulp-安装" class="headerlink" title="3. gulp 安装"></a>3. gulp 安装</h3><p>　　在安装 gulp 之前需先在项目根目录下新建 package.json 文件或通过命令提示符执行 <strong>cnpm init</strong>（文件内不允许写注释，<a href="https://www.zhihu.com/question/23004511" target="_blank" rel="noopener">为什么？</a>）。</p>
<br>

<h4 id="3-1-全局安装-gulp"><a href="#3-1-全局安装-gulp" class="headerlink" title="3.1 全局安装 gulp"></a>3.1 全局安装 gulp</h4><p>　　命令提示符执行 <strong>cnpm install gulp -g</strong> 全局安装 gulp，<em>gulp -v</em> 出现版本号即为正确安装（全局安装 gulp 目的是为了通过她执行 gulp 任务）。</p>
<br>

<h4 id="3-2-本地安装-gulp"><a href="#3-2-本地安装-gulp" class="headerlink" title="3.2 本地安装 gulp"></a>3.2 本地安装 gulp</h4><p>　　在项目根目录下执行命令提示符 <strong>cnpm install gulp --save-dev</strong> 本地安装 gulp（本地安装 gulp 目的是为了调用 gulp 插件的功能）。</p>
<h3 id="4-gulp-插件安装"><a href="#4-gulp-插件安装" class="headerlink" title="4. gulp 插件安装"></a>4. gulp 插件安装</h3><p>　　gulp的插件数量虽然没有grunt那么多，但也可以说是应有尽有了，下面列举一些常用的插件。</p>
<br>

<ul>
<li><p>浏览器自动刷新页面：命令提示符执行 <strong>cnpm install gulp-livereload --save-dev</strong>；</p>
</li>
<li><p>自动处理浏览器前缀：命令提示符执行 <strong>cnpm install gulp-autoprefixer --save-dev</strong>；</p>
</li>
<li><p>合并javascript文件：命令提示符执行 <strong>cnpm install gulp-concat --save-dev</strong>；</p>
</li>
<li><p>压缩css文件：命令提示符执行 <strong>cnpm install gulp-clean-css --save-dev</strong>；</p>
</li>
<li><p>压缩图片文件：命令提示符执行 <strong>cnpm install gulp-imagemin --save-dev</strong>；</p>
</li>
<li><p>压缩html：命令提示符执行 <strong>cnpm install gulp-htmlmin --save-dev</strong>；</p>
</li>
<li><p>less(sass)文件编译成css：命令提示符执行 <strong>cnpm install gulp-less(sass) --save-dev</strong>；</p>
</li>
</ul>
<br>

<p>　　插件安装完成后打开　package.json　查看一下各个插件的版本号是否已被记录在 devDependencies 下，如被记录（如下）则说明插件安装成功。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;devDependencies&quot;: &#123;</span><br><span class="line">    &quot;gulp&quot;: &quot;^3.9.1&quot;,</span><br><span class="line">    &quot;gulp-autoprefixer&quot;: &quot;^3.1.1&quot;,</span><br><span class="line">    &quot;gulp-clean-css&quot;: &quot;^2.3.2&quot;,</span><br><span class="line">    &quot;gulp-concat&quot;: &quot;^2.6.1&quot;,</span><br><span class="line">    &quot;gulp-htmlmin&quot;: &quot;^3.0.0&quot;,</span><br><span class="line">    &quot;gulp-imagemin&quot;: &quot;^3.1.1&quot;,</span><br><span class="line">    &quot;gulp-less&quot;: &quot;^3.3.0&quot;,</span><br><span class="line">    &quot;gulp-livereload&quot;: &quot;^3.8.1&quot;,</span><br><span class="line">    &quot;gulp-uglify&quot;: &quot;^2.0.0&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h2 id="gulp-运行"><a href="#gulp-运行" class="headerlink" title="gulp 运行"></a>gulp 运行</h2><p>　　在 gulp 插件安装完成之后，还需要在项目根目录下新建一个 <strong>gulpfile.js</strong> 的配置文件（很重要）,参考配置内容如下：</p>
<br>


<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"> <span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>),</span><br><span class="line"> <span class="comment">//导入工具包 require('node_modules里对应模块')</span></span><br><span class="line">     less = <span class="built_in">require</span>(<span class="string">'gulp-less'</span>),<span class="comment">//编译less</span></span><br><span class="line">     clean = <span class="built_in">require</span>(<span class="string">'gulp-clean-css'</span>),<span class="comment">//压缩css</span></span><br><span class="line">     imagemin = <span class="built_in">require</span>(<span class="string">'gulp-imagemin'</span>),<span class="comment">//图片压缩</span></span><br><span class="line">     uglify = <span class="built_in">require</span>(<span class="string">'gulp-uglify'</span>),<span class="comment">//压缩js</span></span><br><span class="line">     htmlmin = <span class="built_in">require</span>(<span class="string">'gulp-htmlmin'</span>), <span class="comment">//html压缩</span></span><br><span class="line">     livereload = <span class="built_in">require</span>(<span class="string">'gulp-livereload'</span>),  <span class="comment">//浏览器自动刷新页面</span></span><br><span class="line">     autoprefixer = <span class="built_in">require</span>(<span class="string">'gulp-autoprefixer'</span>),  <span class="comment">//根据设置浏览器版本自动处理浏览器前缀</span></span><br><span class="line">     concat = <span class="built_in">require</span>(<span class="string">'gulp-concat'</span>);  <span class="comment">//合并js文件</span></span><br><span class="line"> <span class="comment">//css压缩</span></span><br><span class="line"> gulp.task(<span class="string">'testCssmin'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">     gulp.src(<span class="string">'src/index.css'</span>)</span><br><span class="line">         .pipe(clean(&#123;</span><br><span class="line">             advanced: <span class="literal">true</span>,  <span class="comment">//类型：Boolean 默认：true [是否开启高级优化（合并选择器等）]</span></span><br><span class="line">             compatibility: <span class="string">'ie7'</span>,  <span class="comment">//保留ie7及以下兼容写法 类型：String 默认：''or'*' [启用兼容模式； 'ie7'：IE7兼容模式，'ie8'：IE8兼容模式，'*'：IE9+兼容模式]</span></span><br><span class="line">             keepBreaks: <span class="literal">true</span>,  <span class="comment">//类型：Boolean 默认：false [是否保留换行]</span></span><br><span class="line">             keepSpecialComments: <span class="string">'*'</span>  <span class="comment">//保留所有特殊前缀 当你用autoprefixer生成的浏览器前缀，如果不加这个参数，有可能将会删除你的部分前缀</span></span><br><span class="line">         &#125;))</span><br><span class="line">     .   pipe(gulp.dest(<span class="string">'src'</span>)); </span><br><span class="line"> &#125;);</span><br><span class="line"> <span class="comment">//less编译</span></span><br><span class="line"> gulp.task(<span class="string">'less'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">     gulp.src(<span class="string">'Machinery/public/less/index.less'</span>)</span><br><span class="line">         .pipe(less())</span><br><span class="line">         .pipe(gulp.dest(<span class="string">'Machinery/build/css'</span>))</span><br><span class="line"> &#125;);</span><br><span class="line"> <span class="comment">//js压缩</span></span><br><span class="line"> gulp.task(<span class="string">'jsmin'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">     gulp.src(<span class="string">'Machinery/public/js/*.js'</span>)</span><br><span class="line">         .pipe(uglify())</span><br><span class="line">         .pipe(gulp.dest(<span class="string">'Machinery/build/js'</span>));</span><br><span class="line"> &#125;);</span><br><span class="line"> <span class="comment">//js合并</span></span><br><span class="line"> gulp.task(<span class="string">'testConcat'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">     gulp.src(<span class="string">'src/*.js'</span>)</span><br><span class="line">         .pipe(concat(<span class="string">'zepto.concat.js'</span>))<span class="comment">//合并后的文件名</span></span><br><span class="line">         .pipe(gulp.dest(<span class="string">'src'</span>));</span><br><span class="line"> &#125;);</span><br><span class="line"> <span class="comment">//图片压缩</span></span><br><span class="line"> gulp.task(<span class="string">'testImagemin'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">     gulp.src(<span class="string">'last/construction/public/images/*.&#123;png,jpg,gif,ico&#125;'</span>)</span><br><span class="line">         .pipe(imagemin())</span><br><span class="line">         .pipe(gulp.dest(<span class="string">'last/construction/build/images'</span>));</span><br><span class="line"> &#125;);</span><br><span class="line"> <span class="comment">//html压缩</span></span><br><span class="line"> gulp.task(<span class="string">'testHtmlmin'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">     <span class="keyword">var</span> options = &#123;</span><br><span class="line">         removeComments: <span class="literal">true</span>,<span class="comment">//清除HTML注释</span></span><br><span class="line">         collapseWhitespace: <span class="literal">true</span>,<span class="comment">//压缩HTML</span></span><br><span class="line">         collapseBooleanAttributes: <span class="literal">true</span>,<span class="comment">//省略布尔属性的值 &lt;input checked="true"/&gt; ==&gt; &lt;input /&gt;</span></span><br><span class="line">         removeEmptyAttributes: <span class="literal">true</span>,<span class="comment">//删除所有空格作属性值 &lt;input id="" /&gt; ==&gt; &lt;input /&gt;</span></span><br><span class="line">         removeScriptTypeAttributes: <span class="literal">true</span>,<span class="comment">//删除&lt;script&gt;的type="text/javascript"</span></span><br><span class="line">         removeStyleLinkTypeAttributes: <span class="literal">true</span>,<span class="comment">//删除&lt;style&gt;和&lt;link&gt;的type="text/css"</span></span><br><span class="line">         minifyJS: <span class="literal">true</span>,<span class="comment">//压缩页面JS</span></span><br><span class="line">         minifyCSS: <span class="literal">true</span><span class="comment">//压缩页面CSS</span></span><br><span class="line">     &#125;;</span><br><span class="line">     gulp.src(<span class="string">'Machinery/*.html'</span>)</span><br><span class="line">         .pipe(htmlmin(options))</span><br><span class="line">         .pipe(gulp.dest(<span class="string">'Machinery/.min.html'</span>));</span><br><span class="line"> &#125;);</span><br><span class="line"> <span class="comment">//自动刷新页面</span></span><br><span class="line"> gulp.task(<span class="string">'less'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">     gulp.src(<span class="string">'src/less/*.less'</span>)</span><br><span class="line">         .pipe(less())</span><br><span class="line">         .pipe(gulp.dest(<span class="string">'src/css'</span>))</span><br><span class="line">         .pipe(livereload());</span><br><span class="line"> &#125;);</span><br><span class="line"> <span class="comment">//自动处理浏览器前缀</span></span><br><span class="line"> gulp.task(<span class="string">'testAutoFx'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">     gulp.src(<span class="string">'src/css/index.css'</span>)</span><br><span class="line">         .pipe(autoprefixer(&#123;</span><br><span class="line">             browsers: [<span class="string">'last 2 versions'</span>, <span class="string">'Android &gt;= 4.0'</span>,<span class="string">'last 2 Explorer versions'</span>],</span><br><span class="line">             cascade: <span class="literal">false</span>, <span class="comment">//是否美化属性值 默认：true 像这样：</span></span><br><span class="line">             <span class="comment">//-webkit-transform: rotate(45deg);</span></span><br><span class="line">             <span class="comment">//        transform: rotate(45deg);</span></span><br><span class="line">             remove:<span class="literal">true</span> <span class="comment">//是否去掉不必要的前缀 默认：true</span></span><br><span class="line">         &#125;))</span><br><span class="line">         .pipe(gulp.dest(<span class="string">'dist/css'</span>));</span><br><span class="line"> &#125;);</span><br><span class="line"> gulp.task(<span class="string">'default'</span>,[<span class="string">'testLess'</span>, <span class="string">'elseTask'</span>]); <span class="comment">//定义默认任务 elseTask为其他任务，该示例没有定义elseTask任务</span></span><br><span class="line"> <span class="comment">//当执行gulp default或gulp将会调用default任务里的所有任务[‘testLess’,’elseTask’]。</span></span><br><span class="line">  </span><br><span class="line"><span class="comment">//gulp.task(name[, deps], fn) 定义任务  name：任务名称 deps：依赖任务名称 fn：回调函数</span></span><br><span class="line"> <span class="comment">//gulp.src(globs[, options]) 执行任务处理的文件  globs：处理的文件路径(字符串或者字符串数组) </span></span><br><span class="line"> <span class="comment">//gulp.dest(path[, options]) 处理完后文件生成路径</span></span><br></pre></td></tr></table></figure>

<p>　　以上为我现在本地的配置内容，最基础的使用足够，还有更深度的配置可参照 <a href="http://www.ydcss.com/archives/category/%E6%9E%84%E5%BB%BA%E5%B7%A5%E5%85%B7" target="_blank" rel="noopener">一点|gulp详细入门教程</a>，gulp 相关的 API 介绍看 <a href="http://www.ydcss.com/archives/424" target="_blank" rel="noopener">这里</a> 。</p>
<h3 id="1-命令提示符运行-gulp-任务"><a href="#1-命令提示符运行-gulp-任务" class="headerlink" title="1. 命令提示符运行 gulp 任务"></a>1. 命令提示符运行 gulp 任务</h3><p>　　<strong>运行 gulp 时执行命令提示符 <em>gulp 任务名称</em>，例如：编译less：命令提示符执行 <em>gulp testLess</em>。</strong></p>
<img src="/styles/images/gulp/gulp-cmd.png" alt="命令提示符运行 gulp 任务">

<h3 id="2-webstorm-运行gulp任务"><a href="#2-webstorm-运行gulp任务" class="headerlink" title="2. webstorm 运行gulp任务"></a>2. webstorm 运行gulp任务</h3><p>　　使用 webstorm 可视化运行 gulp 任务，首先将项目导入 webstorm ，右键 gulpfile.js  选择 ”Show Gulp Tasks” 打开 Gulp 窗口，若出现 ”No task found” ，选择右键 ”Reload tasks” ，双击运行即可。</p>
<p><img src="/styles/images/gulp/gulp-w1.png" alt="webstorm运行gulp任务"><img src="/styles/images/gulp/gulp-w2.png" alt="webstorm运行gulp任务"></p>
<br>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><p>安装 nodejs；</p>
</li>
<li><p>新建 package.json 文件；</p>
</li>
<li><p>全局和本地安装 gulp；</p>
</li>
<li><p>安装 gulp 插件；</p>
</li>
<li><p>新建 gulpfile.js 文件；</p>
</li>
<li><p>通过命令提示符或 webstorm 运行 gulp 任务。</p>
</li>
</ul>
<hr>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>gulp</tag>
        <tag>node</tag>
      </tags>
  </entry>
  <entry>
    <title>awesome-gulp 中文版</title>
    <url>/2017/01/11/article-awesome-gulp/</url>
    <content><![CDATA[<ul>
<li>一份<a href="https://github.com/gulpjs/gulp" target="_blank" rel="noopener">gulp</a>的资源，插件和使用实例清单， 致力于打造更好的前端工程构建流程。</li>
</ul>
<p><strong>本文转自 <a href="https://github.com/Pines-Cheng/" target="_blank" rel="noopener">Pines-Cheng</a>  github 的 <a href="https://github.com/Pines-Cheng/awesome-gulp-cn" target="_blank" rel="noopener">awesome-gulp-cn</a> 项目。</strong></p>
<p>被老外的<a href="https://github.com/sindresorhus/awesome" target="_blank" rel="noopener">awesome</a> 清单刺激到，觉得有必要翻译一份，为国产的程序员们做点事情，本清单将保持实时更新同步。<a id="more"></a>PS:进都进来了，就顺便看看其他的吧:</p>
<ul>
<li><a href="https://github.com/Pines-Cheng/awesome-nodejs-cn" target="_blank" rel="noopener">awesome-nodejs-cn</a></li>
<li><a href="https://github.com/Pines-Cheng/awesome-react-cn" target="_blank" rel="noopener">awesome-react-cn</a></li>
<li><a href="https://github.com/Pines-Cheng/awesome-npm-cn" target="_blank" rel="noopener">awesome-npm-cn</a></li>
<li><a href="https://github.com/Pines-Cheng/awesome-react-native-cn" target="_blank" rel="noopener">awesome-react-native-cn</a></li>
</ul>
<blockquote>
<p>项目的Github地址：<a href="https://github.com/Pines-Cheng/awesome-gulp-cn" target="_blank" rel="noopener">awesome-react-cn</a>,欢迎start。</p>
<p>翻译得仓促，如有问题，请提<a href="https://github.com/Pines-Cheng/awesome-gulp-cn/issues" target="_blank" rel="noopener">issues</a>。</p>
<p>如果想贡献，请<a href="https://github.com/Pines-Cheng/awesome-gulp-cn/pulls" target="_blank" rel="noopener">Pull Requests</a>。</p>
</blockquote>
<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul>
<li><a href="#资源">资源</a><ul>
<li><a href="#通用资源">通用资源</a></li>
<li><a href="#官方文档">官方文档</a></li>
<li><a href="#组织">组织</a></li>
<li><a href="#入门教程">入门教程</a><ul>
<li><a href="#gulp入门">Gulp 入门</a></li>
<li><a href="#gulp-4-入门">Gulp 4 入门</a></li>
<li><a href="#gulp-with-browserify">Gulp with Browserify</a></li>
<li><a href="#gulp-with-browserify">Gulp with Angular</a></li>
<li><a href="#gulp-with-angular-and-browserify">Gulp with Angular and Browserify</a></li>
<li><a href="#gulp-with-angular-and-webpack">Gulp with Angular and Webpack</a></li>
<li><a href="#gulp-with-react-and-browserify">Gulp with React and Browserify</a></li>
<li><a href="#gulp-with-ember">Gulp with Ember</a></li>
</ul>
</li>
<li><a href="#其他资源">其他资源</a></li>
</ul>
</li>
<li><a href="#插件">插件</a><ul>
<li><a href="#编辑">编辑</a></li>
<li><a href="#编译">编译</a></li>
<li><a href="#合并">合并</a></li>
<li><a href="#压缩">压缩</a></li>
<li><a href="#优化">优化</a></li>
<li><a href="#资源注入">资源注入</a></li>
<li><a href="#模板">模板</a></li>
<li><a href="#代码校验">代码校验</a></li>
<li><a href="#实时加载">实时加载</a></li>
<li><a href="#缓存">缓存</a></li>
<li><a href="#流控制">流控制</a></li>
<li><a href="#日志">日志</a></li>
<li><a href="#测试">测试</a></li>
<li><a href="#其他插件">其他插件</a></li>
</ul>
</li>
<li><a href="#脚手架">脚手架</a><ul>
<li><a href="#参考工程">参考工程</a></li>
<li><a href="#yeoman生成器">Yeoman生成器</a></li>
</ul>
</li>
<li><a href="#其他">其他</a></li>
</ul>
<h2 id="资源"><a href="#资源" class="headerlink" title="资源"></a>资源</h2><h3 id="1-通用资源"><a href="#1-通用资源" class="headerlink" title="1.通用资源"></a>1.通用资源</h3><ul>
<li><a href="http://gulpjs.com/" target="_blank" rel="noopener">官网</a></li>
<li><a href="https://github.com/gulpjs/gulp" target="_blank" rel="noopener">Github库</a></li>
<li><a href="http://gulpjs.com/plugins/" target="_blank" rel="noopener">插件注册</a></li>
<li><a href="https://www.npmjs.com/package/gulp" target="_blank" rel="noopener">NPM模块</a></li>
<li><a href="https://github.com/gulpjs/plugins/blob/master/src/blackList.json" target="_blank" rel="noopener">插件黑名单</a></li>
</ul>
<h3 id="2-官方文档"><a href="#2-官方文档" class="headerlink" title="2.官方文档"></a>2.官方文档</h3><ul>
<li><a href="https://github.com/gulpjs/gulp/blob/master/docs/getting-started.md" target="_blank" rel="noopener">快速开始</a></li>
<li><a href="https://github.com/gulpjs/gulp/blob/master/docs/API.md" target="_blank" rel="noopener">API文档</a></li>
<li><a href="https://github.com/gulpjs/gulp/tree/master/docs#articles" target="_blank" rel="noopener">CLI 文档</a></li>
<li><a href="https://github.com/gulpjs/gulp/blob/master/docs/writing-a-plugin/README.md" target="_blank" rel="noopener">开始写一个插件</a></li>
<li><a href="https://github.com/gulpjs/gulp/tree/master/docs/recipes" target="_blank" rel="noopener">使用诀窍</a></li>
</ul>
<h3 id="3-组织"><a href="#3-组织" class="headerlink" title="3.组织"></a>3.组织</h3><ul>
<li><a href="http://stackoverflow.com/questions/tagged/gulp" target="_blank" rel="noopener">StackOverflow</a></li>
<li><a href="https://twitter.com/gulpjs" target="_blank" rel="noopener">Twitter</a></li>
</ul>
<h3 id="4-入门教程"><a href="#4-入门教程" class="headerlink" title="4.入门教程"></a>4.入门教程</h3><h4 id="1）Gulp入门"><a href="#1）Gulp入门" class="headerlink" title="1）Gulp入门"></a>1）Gulp入门</h4><ul>
<li><a href="https://www.smashingmagazine.com/2014/06/building-with-gulp/" target="_blank" rel="noopener">使用Gulp构建前端工程</a></li>
<li><a href="https://scotch.io/tutorials/automate-your-tasks-easily-with-gulp-js" target="_blank" rel="noopener">通过Gulp.js轻松自动化构建你的前端工程</a></li>
<li><a href="https://medium.com/@contrahacks/gulp-3828e8126466" target="_blank" rel="noopener">Gulp，让前端工程可视化</a></li>
<li><a href="http://stefanimhoff.de/tag/gulp/" target="_blank" rel="noopener">Gulp.js是什么？</a></li>
<li><a href="http://blog.johnnyreilly.com/2015/02/using-gulp-in-asp-net-instead-of-web-optimization.html" target="_blank" rel="noopener">使用Gulp在你的HTML中直接插入Scripts和Styles标签</a></li>
<li><a href="http://denbuzze.com/post/5-lessons-learned-using-gulpjs/" target="_blank" rel="noopener">5节课学会使用Gulp.js</a></li>
<li><a href="http://lab.brightnorth.co.uk/2014/08/13/automating-linkage-how-i-learned-to-stop-worrying-and-love-the-build/" target="_blank" rel="noopener">我是怎样摆脱前端工程的困扰的？</a></li>
<li><a href="https://www.codementor.io/development-process/tutorial/how-to-set-up-gulp-beginner-guide#/" target="_blank" rel="noopener">第一次开始Gulp Task</a></li>
<li><a href="http://blog.overzealous.com/post/74121048393/why-you-shouldnt-create-a-gulp-plugin-or-how-to" target="_blank" rel="noopener">为什么你不自己写一个Gulp插件？</a></li>
<li><a href="http://blog.rangle.io/angular-gulp-bestpractices/" target="_blank" rel="noopener">6个最好的从根本改善你的开发经验的Gulp实战练习</a></li>
<li><a href="https://css-tricks.com/gulp-for-beginners/" target="_blank" rel="noopener">Gulp初学者教程</a></li>
</ul>
<h4 id="2）Gulp-4-入门"><a href="#2）Gulp-4-入门" class="headerlink" title="2）Gulp 4 入门"></a>2）Gulp 4 入门</h4><ul>
<li><a href="https://blog.wearewizards.io/migrating-to-gulp-4-by-example" target="_blank" rel="noopener">迁移到Gulp 4的例子</a></li>
<li><a href="http://fettblog.eu/gulp-4-parallel-and-series/" target="_blank" rel="noopener">Gulp 4: 新的task执行系统 - gulp.parallel 和 gulp.series</a></li>
</ul>
<h4 id="3）Gulp-with-Browserify"><a href="#3）Gulp-with-Browserify" class="headerlink" title="3）Gulp with Browserify"></a>3）Gulp with Browserify</h4><ul>
<li><a href="https://medium.com/@sogko/gulp-browserify-the-gulp-y-way-bb359b3f9623" target="_blank" rel="noopener">Gulp + Browserify, the Gulp-y Way</a></li>
<li><a href="https://viget.com/extend/gulp-browserify-starter-faq" target="_blank" rel="noopener">Gulp + Browserify</a></li>
<li><a href="https://github.com/gulpjs/gulp/blob/master/docs/recipes/fast-browserify-builds-with-watchify.md" target="_blank" rel="noopener">快速构建Browserify和Watchify</a></li>
</ul>
<h4 id="4）Gulp-with-Angular"><a href="#4）Gulp-with-Angular" class="headerlink" title="4）Gulp with Angular"></a>4）Gulp with Angular</h4><ul>
<li><a href="http://blog.jhades.org/what-every-angular-project-likely-needs-and-a-gulp-build-to-provide-it/" target="_blank" rel="noopener">Angular工程需要什么 - Gulp能提供什么</a></li>
</ul>
<h4 id="5）Gulp-with-Angular-and-Browserify"><a href="#5）Gulp-with-Angular-and-Browserify" class="headerlink" title="5）Gulp with Angular and Browserify"></a>5）Gulp with Angular and Browserify</h4><ul>
<li><a href="http://omarfouad.com/blog/2015/03/21/advanced-angularjs-structure-with-gulp-node-and-browserify/" target="_blank" rel="noopener">使用 Gulp, Node and Browserify构建先进的 AngularJS工程结构</a></li>
</ul>
<h4 id="6）Gulp-with-React-and-Browserify"><a href="#6）Gulp-with-React-and-Browserify" class="headerlink" title="6）Gulp with React and Browserify"></a>6）Gulp with React and Browserify</h4><ul>
<li><a href="https://hacks.mozilla.org/2014/08/browserify-and-gulp-with-react/" target="_blank" rel="noopener">Browserify、Gulp 和 React</a></li>
<li><a href="http://pomax.github.io/1420592591221/taking-react-to-the-next-level-mixins-gulp-and-browserify" target="_blank" rel="noopener">Taking React to the Next Level: Mixins, Gulp, and Browserify</a></li>
</ul>
<h4 id="7）Gulp-with-Ember"><a href="#7）Gulp-with-Ember" class="headerlink" title="7）Gulp with Ember"></a>7）Gulp with Ember</h4><ul>
<li><a href="http://www.sitepoint.com/improving-ember-js-workflow-using-gulp-js/" target="_blank" rel="noopener">使用Gulp.js改进你的Ember.js工作流</a></li>
</ul>
<h3 id="5-其他资源"><a href="#5-其他资源" class="headerlink" title="5.其他资源"></a>5.其他资源</h3><ul>
<li><a href="https://github.com/osscafe/gulp-cheatsheet" target="_blank" rel="noopener">Gulp 备忘录</a></li>
<li><a href="https://github.com/johnpapa/gulp-patterns" target="_blank" rel="noopener">Gulp清单</a></li>
</ul>
<h2 id="插件"><a href="#插件" class="headerlink" title="插件"></a>插件</h2><h3 id="1-编译"><a href="#1-编译" class="headerlink" title="1.编译"></a>1.编译</h3><ul>
<li><p><a href="https://github.com/dlmanning/gulp-sass" target="_blank" rel="noopener">gulp-sass</a> - 通过 <a href="https://github.com/sass/libsass" target="_blank" rel="noopener">libsass</a>将Sass编译成 CSS </p>
</li>
<li><p><a href="https://github.com/sindresorhus/gulp-ruby-sass" target="_blank" rel="noopener">gulp-ruby-sass</a> - 通过 Ruby Sass将Sass编译成CSS </p>
</li>
<li><p><a href="https://github.com/appleboy/gulp-compass" target="_blank" rel="noopener">gulp-compass</a> - 通过 Ruby Sass和CompassSass编译成CSS </p>
</li>
<li><p><a href="https://github.com/plus3network/gulp-less" target="_blank" rel="noopener">gulp-less</a> - <a href="https://github.com/less/less.js" target="_blank" rel="noopener">Less</a>编译成 CSS.</p>
</li>
<li><p><a href="https://github.com/stevelacy/gulp-stylus" target="_blank" rel="noopener">gulp-stylus</a> - <a href="https://github.com/stylus/stylus" target="_blank" rel="noopener">Stylus</a> 编译成 CSS.</p>
</li>
<li><p><a href="https://github.com/postcss/gulp-postcss" target="_blank" rel="noopener">gulp-postcss</a> - Pipe CSS 通过 <a href="https://github.com/postcss/postcss" target="_blank" rel="noopener">PostCSS</a> processors with a single parse.</p>
</li>
<li><p><a href="https://github.com/contra/gulp-coffee" target="_blank" rel="noopener">gulp-coffee</a> - <a href="https://github.com/jashkenas/coffeescript" target="_blank" rel="noopener">Coffeescript</a> 编译成 JavaScript.</p>
</li>
<li><p><a href="https://github.com/ivogabe/gulp-typescript" target="_blank" rel="noopener">gulp-typescript</a> - <a href="https://github.com/Microsoft/TypeScript" target="_blank" rel="noopener">TypeScript</a>编译成JavaScript.</p>
</li>
<li><p><a href="https://github.com/sindresorhus/gulp-react" target="_blank" rel="noopener">gulp-react</a> - Facebook <a href="https://github.com/facebook/react" target="_blank" rel="noopener">React</a> JSX 模板编译成JavaScript.</p>
</li>
<li><p><a href="https://github.com/shama/webpack-stream" target="_blank" rel="noopener">webpack-stream</a> - 将<a href="https://github.com/webpack/webpack" target="_blank" rel="noopener">webpack</a>集成在Gulp中使用。</p>
</li>
<li><p><a href="https://github.com/babel/gulp-babel" target="_blank" rel="noopener">gulp-babel</a> - ES6编译成ES5 通过 <a href="https://github.com/babel/babel" target="_blank" rel="noopener">babel</a>.</p>
</li>
<li><p><a href="https://github.com/sindresorhus/gulp-traceur" target="_blank" rel="noopener">gulp-traceur</a> - ES6编译成ES5 通过 <a href="https://github.com/google/traceur-compiler" target="_blank" rel="noopener">Traceur</a>.</p>
</li>
<li><p><a href="https://github.com/sindresorhus/gulp-regenerator" target="_blank" rel="noopener">gulp-regenerator</a> - ES6编译成ES5 通过 <a href="https://github.com/facebook/regenerator" target="_blank" rel="noopener">Regenerator</a>.</p>
</li>
<li><p><a href="https://github.com/sindresorhus/gulp-es6-transpiler" target="_blank" rel="noopener">gulp-es6-transpiler</a> - [过时的] ES6编译成ES5 通过 <a href="https://github.com/termi/es6-transpiler" target="_blank" rel="noopener">es6-transpiler</a>.</p>
</li>
<li><p><a href="https://github.com/sindresorhus/gulp-myth" target="_blank" rel="noopener">gulp-myth</a> - <a href="https://github.com/segmentio/myth" target="_blank" rel="noopener">Myth</a> - a polyfill for future versions of the CSS spec.</p>
</li>
<li><p><a href="https://github.com/MoOx/gulp-cssnext" target="_blank" rel="noopener">gulp-cssnext</a> - [过时的] 使用下一代的 CSS 规范通过 <a href="https://github.com/MoOx/postcss-cssnext" target="_blank" rel="noopener">cssnext</a>.</p>
</li>
</ul>
<h3 id="2-合并"><a href="#2-合并" class="headerlink" title="2.合并"></a>2.合并</h3><ul>
<li><a href="https://github.com/contra/gulp-concat" target="_blank" rel="noopener">gulp-concat</a> - 合并文件.</li>
</ul>
<h3 id="3-压缩"><a href="#3-压缩" class="headerlink" title="3.压缩"></a>3.压缩</h3><ul>
<li><a href="https://github.com/scniro/gulp-clean-css" target="_blank" rel="noopener">gulp-clean-css</a> - 压缩 CSS 通过 <a href="https://github.com/jakubpawlowicz/clean-css" target="_blank" rel="noopener">clean-css</a>.</li>
<li><a href="https://github.com/ben-eb/gulp-csso" target="_blank" rel="noopener">gulp-csso</a> - 压缩 CSS 通过 <a href="https://github.com/css/csso" target="_blank" rel="noopener">CSSO</a>.</li>
<li><a href="https://github.com/terinjokes/gulp-uglify" target="_blank" rel="noopener">gulp-uglify</a> - 压缩 JavaScript 通过 <a href="https://github.com/mishoo/UglifyJS2" target="_blank" rel="noopener">UglifyJS2</a>.</li>
<li><a href="https://github.com/jonschlinkert/gulp-htmlmin" target="_blank" rel="noopener">gulp-htmlmin</a> - 压缩 HTML 通过 <a href="https://github.com/kangax/html-minifier" target="_blank" rel="noopener">html-minifier</a>.</li>
<li><a href="https://github.com/murphydanger/gulp-minify-html" target="_blank" rel="noopener">gulp-minify-html</a> - 压缩 HTML 通过<br><a href="https://github.com/Swaagie/minimize" target="_blank" rel="noopener">Minimize</a>.</li>
<li><a href="https://github.com/sindresorhus/gulp-imagemin" target="_blank" rel="noopener">gulp-imagemin</a> - 压缩 PNG, JPEG, GIF and SVG 图片 通过 <a href="https://github.com/imagemin/imagemin" target="_blank" rel="noopener">imagemin</a>.</li>
<li><a href="https://github.com/ben-eb/gulp-svgmin" target="_blank" rel="noopener">gulp-svgmin</a> - 通过Gulp压缩 SVG 文件</li>
</ul>
<h3 id="4-优化"><a href="#4-优化" class="headerlink" title="4.优化"></a>4.优化</h3><ul>
<li><a href="https://github.com/ben-eb/gulp-uncss" target="_blank" rel="noopener">gulp-uncss</a> - 移除未使用的CSS选择器通过 <a href="https://github.com/giakki/uncss" target="_blank" rel="noopener">UnCSS</a>.</li>
<li><a href="https://github.com/zckrs/gulp-css-base64" target="_blank" rel="noopener">gulp-css-base64</a> - 将CSS文件中所有的资源(有url()声明的)变成base64-encoded 数据的URI字符串</li>
<li><a href="https://github.com/akoenig/gulp-svg2png" target="_blank" rel="noopener">gulp-svg2png</a> - 将SVGs转换成PNGs</li>
<li><a href="https://github.com/mahnunchik/gulp-responsive" target="_blank" rel="noopener">gulp-responsive</a> - 生成不同尺寸的图片</li>
<li><a href="https://github.com/w0rm/gulp-svgstore" target="_blank" rel="noopener">gulp-svgstore</a> -将svg files 合并成一个通过<symbol> 元素</symbol></li>
<li><a href="https://github.com/nfroidure/gulp-iconfont" target="_blank" rel="noopener">gulp-iconfont</a> - 通过SVG icons创建 icon fonts</li>
</ul>
<h3 id="5-资源注入"><a href="#5-资源注入" class="headerlink" title="5.资源注入"></a>5.资源注入</h3><ul>
<li><a href="https://github.com/jonkemp/gulp-useref" target="_blank" rel="noopener">gulp-useref</a> - 解析HTML文件中特殊标签里面的script或style标签，合并成一个script或css文件，并替换。</li>
<li><a href="https://github.com/klei/gulp-inject" target="_blank" rel="noopener">gulp-inject</a> - 将指定的css或js文件以标签的形式插入到HTML中的指定标志内。</li>
<li><a href="https://github.com/taptapship/wiredep" target="_blank" rel="noopener">wiredep</a> - 将Bower依赖自动注入HTML文件中。</li>
</ul>
<h3 id="6-模板"><a href="#6-模板" class="headerlink" title="6.模板"></a>6.模板</h3><ul>
<li><a href="https://github.com/miickel/gulp-angular-templatecache" target="_blank" rel="noopener">gulp-angular-templatecache</a> - 在$templateCache中联系并注册AngularJS模板</li>
<li><a href="https://github.com/phated/gulp-jade" target="_blank" rel="noopener">gulp-jade</a> - <a href="https://github.com/pugjs/jade" target="_blank" rel="noopener">Jade</a> 转换成 HTML.</li>
<li><a href="https://github.com/lazd/gulp-handlebars" target="_blank" rel="noopener">gulp-handlebars</a> - <a href="https://github.com/wycats/handlebars.js" target="_blank" rel="noopener">Handlebars</a>模板转换成 JavaScript.</li>
<li><a href="https://github.com/shannonmoeller/gulp-hb" target="_blank" rel="noopener">gulp-hb</a> - <a href="https://github.com/wycats/handlebars.js" target="_blank" rel="noopener">Handlebars</a> 模板转换成 HTML.</li>
<li><a href="https://github.com/sindresorhus/gulp-nunjucks" target="_blank" rel="noopener">gulp-nunjucks</a> - <a href="https://github.com/mozilla/nunjucks" target="_blank" rel="noopener">Nunjucks</a>模板转换成JavaScript.</li>
<li><a href="https://github.com/sindresorhus/gulp-dust" target="_blank" rel="noopener">gulp-dustjs</a> - <a href="https://github.com/linkedin/dustjs" target="_blank" rel="noopener">Dust</a>模板转换成JavaScript.</li>
<li><a href="https://github.com/e-jigsaw/gulp-riot" target="_blank" rel="noopener">gulp-riot</a> - <a href="https://github.com/riot/riot" target="_blank" rel="noopener">Riot</a>模板转换成JavaScript.</li>
<li><a href="https://github.com/sindresorhus/gulp-markdown" target="_blank" rel="noopener">gulp-markdown</a> - Markdown → HTML.</li>
<li><a href="https://github.com/sindresorhus/gulp-template" target="_blank" rel="noopener">gulp-template</a> - <a href="https://github.com/lodash/lodash" target="_blank" rel="noopener">Lodash </a>模板转换成JavaScript.</li>
<li><a href="https://github.com/colynb/gulp-swig" target="_blank" rel="noopener">gulp-swig</a> - <a href="https://github.com/paularmstrong/swig" target="_blank" rel="noopener">Swig</a>模板转换成HTML.</li>
<li><a href="https://github.com/denysdovhan/gulp-remark" target="_blank" rel="noopener">gulp-remark</a> - Gulp plugin for [remark]的Gulp插件(<a href="https://github.com/wooorm/remark" target="_blank" rel="noopener">https://github.com/wooorm/remark</a>) 通过插件处理markdown</li>
</ul>
<h3 id="7-代码校验"><a href="#7-代码校验" class="headerlink" title="7.代码校验"></a>7.代码校验</h3><ul>
<li><a href="https://www.npmjs.com/package/gulp-csslint" target="_blank" rel="noopener">gulp-csslint</a> - 通过<a href="https://github.com/CSSLint/csslint" target="_blank" rel="noopener">CSSLint</a>自动校验CSS.</li>
<li><a href="https://github.com/bezoerb/gulp-htmlhint" target="_blank" rel="noopener">gulp-htmlhint</a> - 通过<a href="https://github.com/yaniswang/HTMLHint" target="_blank" rel="noopener">HTMLHint</a>校验HTML.</li>
<li><a href="https://github.com/spalger/gulp-jshint" target="_blank" rel="noopener">gulp-jshint</a> - 通过<a href="https://github.com/jshint/jshint" target="_blank" rel="noopener">JSHint</a>发现错误和潜在的问题.</li>
<li><a href="https://github.com/jscs-dev/gulp-jscs" target="_blank" rel="noopener">gulp-jscs</a> - 通过<a href="https://github.com/jscs-dev/node-jscs" target="_blank" rel="noopener">jscs</a>检查JavaScript代码风格.</li>
<li><a href="https://github.com/janraasch/gulp-coffeelint" target="_blank" rel="noopener">gulp-coffeelint</a> - 一种用来保证<a href="https://github.com/jashkenas/coffeescript" target="_blank" rel="noopener">CoffeeScript</a>代码风格统一的检查。</li>
<li><a href="https://github.com/panuhorsmalahti/gulp-tslint" target="_blank" rel="noopener">gulp-tslint</a> - gulp的<a href="https://github.com/Microsoft/TypeScript" target="_blank" rel="noopener">TypeScript</a>代码校验插件.</li>
<li><a href="https://github.com/adametry/gulp-eslint" target="_blank" rel="noopener">gulp-eslint</a> - ECMAScript/JavaScript代码校验.</li>
<li><a href="https://github.com/callumacrae/gulp-w3cjs" target="_blank" rel="noopener">gulp-w3cjs</a> - 通过<a href="https://github.com/thomasdavis/w3cjs" target="_blank" rel="noopener">w3cjs</a>检验HTML.</li>
<li><a href="https://github.com/lesshint/gulp-lesshint" target="_blank" rel="noopener">gulp-lesshint</a> - 通过<a href="https://github.com/lesshint/lesshint" target="_blank" rel="noopener">lesshint</a>校验LESS.</li>
</ul>
<h3 id="8-实时加载"><a href="#8-实时加载" class="headerlink" title="8.实时加载"></a>8.实时加载</h3><ul>
<li><a href="https://github.com/BrowserSync/browser-sync" target="_blank" rel="noopener">browser-sync</a> - 保证多个浏览器或设备网页同步显示 (<a href="https://github.com/BrowserSync/gulp-browser-sync" target="_blank" rel="noopener">recipes</a>).</li>
<li><a href="https://github.com/vohof/gulp-livereload" target="_blank" rel="noopener">gulp-livereload</a> - Gulp的实时加载插件.</li>
</ul>
<h3 id="9-缓存"><a href="#9-缓存" class="headerlink" title="9.缓存"></a>9.缓存</h3><ul>
<li><a href="https://github.com/sindresorhus/gulp-changed" target="_blank" rel="noopener">gulp-changed</a> - 仅让发生改变的文件通过.</li>
<li><a href="https://github.com/contra/gulp-cached" target="_blank" rel="noopener">gulp-cached</a> - 一个简单的文件内存缓存.</li>
<li><a href="https://github.com/ahaurw01/gulp-remember" target="_blank" rel="noopener">gulp-remember</a> - 记忆并回收通过了的文件.</li>
<li><a href="https://github.com/tschaub/gulp-newer" target="_blank" rel="noopener">gulp-newer</a> - 只让新的源码通过.</li>
</ul>
<h3 id="10-流控制"><a href="#10-流控制" class="headerlink" title="10.流控制"></a>10.流控制</h3><ul>
<li><a href="https://github.com/grncdr/merge-stream" target="_blank" rel="noopener">merge-stream</a> - 合并多个流到一个插入的流.</li>
<li><a href="https://github.com/nfroidure/StreamQueue" target="_blank" rel="noopener">streamqueue</a> - 逐渐输入队列的流.</li>
<li><a href="https://github.com/OverZealous/run-sequence" target="_blank" rel="noopener">run-sequence</a> - 按要求运行一些依赖的Gulptask.</li>
<li><a href="https://github.com/robrich/gulp-if" target="_blank" rel="noopener">gulp-if</a> - 按照条件运行task.</li>
</ul>
<h3 id="11-日志"><a href="#11-日志" class="headerlink" title="11.日志"></a>11.日志</h3><ul>
<li><a href="https://github.com/mikaelbr/gulp-notify" target="_blank" rel="noopener">gulp-notify</a> - Gulp的通知插件.</li>
<li><a href="https://github.com/sindresorhus/gulp-size" target="_blank" rel="noopener">gulp-size</a> - 显示你的项目的大小.</li>
<li><a href="https://github.com/sindresorhus/gulp-debug" target="_blank" rel="noopener">gulp-debug</a> - 通过调试文件流来观察那些文件通过了你的Gulp管道.</li>
</ul>
<h3 id="12-测试"><a href="#12-测试" class="headerlink" title="12.测试"></a>12.测试</h3><ul>
<li><a href="https://github.com/sindresorhus/gulp-mocha" target="_blank" rel="noopener">gulp-mocha</a> - 运行<a href="https://github.com/mochajs/mocha" target="_blank" rel="noopener">Mocha</a>测试用例.</li>
<li><a href="https://github.com/sindresorhus/gulp-jasmine" target="_blank" rel="noopener">gulp-jasmine</a> - 在Node.js中运行<a href="https://github.com/jasmine/jasmine" target="_blank" rel="noopener">Jasmine 2</a> 测试用例.</li>
<li><a href="https://github.com/mllrsohn/gulp-protractor" target="_blank" rel="noopener">gulp-protractor</a> - 为<a href="https://github.com/angular/protractor" target="_blank" rel="noopener">Protractor</a>测试用例包裹Gulp.</li>
<li><a href="https://github.com/dylanb/gulp-coverage" target="_blank" rel="noopener">gulp-coverage</a> - 为Node.js覆盖相对于运行的测试运行独立的报告.</li>
<li><a href="https://github.com/karma-runner/gulp-karma" target="_blank" rel="noopener">gulp-karma</a> - 通过Gulp运行Karma测试用例.</li>
<li><a href="https://github.com/sindresorhus/gulp-ava" target="_blank" rel="noopener">gulp-ava</a>- 通过Gulp运行<a href="https://github.com/sindresorhus/ava" target="_blank" rel="noopener">AVA</a> 测试用例.</li>
</ul>
<h3 id="13-其他插件"><a href="#13-其他插件" class="headerlink" title="13.其他插件"></a>13.其他插件</h3><ul>
<li><a href="https://github.com/gulpjs/gulp-util" target="_blank" rel="noopener">gulp-util</a> - 包含一系列有用插件.</li>
<li><a href="https://github.com/floatdrop/gulp-plumber" target="_blank" rel="noopener">gulp-plumber</a> - 防止错误引起管道中断Prevent pipe breaking caused by errors.</li>
<li><a href="https://github.com/jackfranklin/gulp-load-plugins" target="_blank" rel="noopener">gulp-load-plugins</a> - 自动加载Gulp插件.</li>
<li><a href="https://github.com/ck86/main-bower-files" target="_blank" rel="noopener">main-bower-files</a> - 构建时自动获取bower库的文件.</li>
<li><a href="https://github.com/postcss/autoprefixer" target="_blank" rel="noopener">autoprefixer</a> - 解析CSS且根据规则添加浏览器兼容性前缀.</li>
<li><a href="https://github.com/floridoo/gulp-sourcemaps" target="_blank" rel="noopener">gulp-sourcemaps</a> - 提供source map支持.</li>
<li><a href="https://github.com/lazd/gulp-replace" target="_blank" rel="noopener">gulp-replace</a> - Gulp的一个字符串替换插件.</li>
<li><a href="https://github.com/hparra/gulp-rename" target="_blank" rel="noopener">gulp-rename</a> - 轻松重命名文件.</li>
<li><a href="https://github.com/sindresorhus/gulp-rev" target="_blank" rel="noopener">gulp-rev</a> - 在静态文件名的后面添加hash值，如: unicorn.css → unicorn-d41d8cd98f.css.</li>
<li><a href="https://github.com/sindresorhus/del" target="_blank" rel="noopener">del</a> - 使用globs删除文件/文件夹.</li>
<li><a href="https://github.com/robrich/gulp-exec" target="_blank" rel="noopener">gulp-exec</a> - 运行一个shell命令.</li>
<li><a href="https://github.com/sindresorhus/gulp-strip-debug" target="_blank" rel="noopener">gulp-strip-debug</a> - 除去javascript代码中的console,alert,debugger声明.</li>
<li><a href="https://github.com/unlight/gulp-cssimport" target="_blank" rel="noopener">gulp-cssimport</a> - 解析CSS文件，找到imports,将连接文件替换成imort声明.</li>
<li><a href="https://github.com/jonkemp/gulp-inline-css" target="_blank" rel="noopener">gulp-inline-css</a> - 将HTML中的css属性放到style标签中.</li>
<li><a href="https://github.com/shinnn/gulp-gh-pages" target="_blank" rel="noopener">gulp-gh-pages</a> - 将内容发布到GiHub有页面.</li>
<li><a href="https://github.com/Kagami/gulp-ng-annotate" target="_blank" rel="noopener">gulp-ng-annotate</a> - 通过<a href="https://github.com/olov/ng-annotate" target="_blank" rel="noopener">ng-annotate</a>添加Angular依赖注入.</li>
<li><a href="https://github.com/stevelacy/gulp-bump" target="_blank" rel="noopener">gulp-bump</a> - 通过Gulp Bump任何semvar JSON版本.</li>
<li><a href="https://github.com/coderhaoxin/gulp-file-include" target="_blank" rel="noopener">gulp-file-include</a> - 通过Gulp Include文件.</li>
<li><a href="https://github.com/sindresorhus/gulp-zip" target="_blank" rel="noopener">gulp-zip</a> - 以ZIP格式压缩文件.</li>
<li><a href="https://github.com/stevelacy/gulp-git" target="_blank" rel="noopener">gulp-git</a> - 通过Gulp运行git命令.</li>
<li><a href="https://github.com/sindresorhus/gulp-filter" target="_blank" rel="noopener">gulp-filter</a> - 使用globbing过滤文件.</li>
<li><a href="https://github.com/jas/gulp-preprocess" target="_blank" rel="noopener">gulp-preprocess</a> - 基于自定义内容或环境配置预处理文件.</li>
</ul>
<h2 id="脚手架"><a href="#脚手架" class="headerlink" title="脚手架"></a>脚手架</h2><h3 id="1-模板"><a href="#1-模板" class="headerlink" title="1.模板"></a>1.模板</h3><ul>
<li><a href="https://github.com/google/web-starter-kit" target="_blank" rel="noopener">web-starter-kit</a> - Google的Web Starter Kit.</li>
<li><a href="https://github.com/sindresorhus/gulp-plugin-boilerplate" target="_blank" rel="noopener">gulp-plugin-boilerplate</a> - 创建Gulp插件的开始模板.</li>
<li><a href="https://github.com/polymerelements/polymer-starter-kit" target="_blank" rel="noopener">polymer-starter-kit</a> -Polymer 1.0 应用的起点.</li>
<li><a href="https://github.com/este/este" target="_blank" rel="noopener">este</a> - 同构的web应用最全面的React/Flux开发栈和开始模板.</li>
<li><a href="https://github.com/mrmrs/mnml" target="_blank" rel="noopener">mnml</a> - 开发响应式HTML5/Sass项目的最小开始模板.</li>
<li><a href="https://github.com/cferdinandi/kraken" target="_blank" rel="noopener">kraken</a> 一个轻量级的、移动端优先的前端开发开始模板.</li>
<li><a href="https://github.com/jakemmarsh/angularjs-gulp-browserify-boilerplate" target="_blank" rel="noopener">angularjs-gulp-browserify-boilerplate</a> - 一个使用AngularJS, Sass, gulp, 和 Browserify技术的开始模板.</li>
<li><a href="https://github.com/poeticninja/hapi-ninja" target="_blank" rel="noopener">hapi-ninja</a> - 一个使用Node.js, Hapi, and Swig技术的开始模板.</li>
<li><a href="https://github.com/rappasoft/laravel-5-boilerplate" target="_blank" rel="noopener">laravel-5-boilerplate</a> - 一个Laravel 5 开始模板.</li>
<li><a href="https://github.com/wbkd/react-starterkit" target="_blank" rel="noopener">react-starterkit</a> - 包含react-router, Reflux, jest, webpack, gulp and Stylus的React开始模板.</li>
</ul>
<h3 id="2-Yeoman生成器"><a href="#2-Yeoman生成器" class="headerlink" title="2.Yeoman生成器"></a>2.Yeoman生成器</h3><ul>
<li><a href="https://github.com/yeoman/generator-gulp-webapp" target="_blank" rel="noopener">generator-gulp-webapp</a> - A 一个流行的web应用的gulp生成器.</li>
<li><a href="https://github.com/Swiip/generator-gulp-angular" target="_blank" rel="noopener">generator-gulp-angular</a> -  使用Gulp的AngularJS 的Yeoman生成器.</li>
<li><a href="https://github.com/randylien/generator-react-gulp-browserify" target="_blank" rel="noopener">generator-react-gulp-browserify</a> - 一个React库的Yeoman生成器，包含gulp, Browserify, Browsersync and Bootstrap.</li>
<li><a href="https://github.com/youngmountain/generator-node-gulp" target="_blank" rel="noopener">generator-node-gulp</a> - 一个Node.js模块生成器，包含gulp和 Mocha.</li>
<li><a href="https://github.com/niallobrien/generator-gulp-bootstrap" target="_blank" rel="noopener">generator-gulp-bootstrap</a> - 一个包含Bootstrap, gulp 和libsass的Yeoman生成器·.</li>
<li><a href="https://github.com/jgoux/generator-angulpify" target="_blank" rel="noopener">generator-angulpify</a> -  一个包含AngularJS, gulp和Browserify的Yeoman生成器.</li>
<li><a href="https://github.com/tmaximini/generator-ionic-gulp" target="_blank" rel="noopener">generator-ionic-gulp</a> - 一个Ionic工厂的Yeoman生成器.</li>
<li><a href="https://github.com/sindresorhus/generator-gulp-plugin-boilerplate" target="_blank" rel="noopener">generator-gulp-plugin-boilerplate</a> -一个输出 <a href="https://github.com/sindresorhus/gulp-plugin-boilerplate" target="_blank" rel="noopener">gulp plugin boilerplate</a>的脚手架.</li>
<li><a href="https://github.com/sondr3/generator-jekyllized" target="_blank" rel="noopener">generator-jekyllized</a> - 一个包含gulp, Sass, AutoPrefixer,资源优化，缓存等的Jekyll工作流.</li>
</ul>
<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><ul>
<li><a href="https://github.com/laravel/elixir" target="_blank" rel="noopener">elixir</a> - 一个为你的应用定义基本的gulp任务的干净、灵活的API.</li>
<li><a href="https://github.com/sindresorhus/gulp-app" target="_blank" rel="noopener">gulp-app</a> - 将Gulp作为一个应用(OS X).</li>
<li><a href="https://github.com/Lostmyname/lmn-gulp-tasks" target="_blank" rel="noopener">lmn-gulp-tasks</a> - gulp任务的单元测试示例.</li>
<li><a href="http://gulp-cookery.github.io/gulp-chef/" target="_blank" rel="noopener">gulp-chef</a> - 一个优雅的、简单的重复使用gulp task的方法.</li>
</ul>
<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>gulp</tag>
      </tags>
  </entry>
  <entry>
    <title>Liquid 指南</title>
    <url>/2017/01/10/article-liquid-skills/</url>
    <content><![CDATA[<p>　　最近折腾 github pages 上博客的时候, 遇到一些 jekyll 问题，准确的来说应该是 Liquid 用法的问题。</p>
<p>　　于是一系列搜索之后终于找到了两篇根据 <a href="https://github.com/shopify/liquid/wiki/liquid-for-designers" target="_blank" rel="noopener">Liquid for Designers</a> 翻译的不错的文档可以做基本的参考，分别是 <a href="http://blog.csdn.net/dont27/article/details/38097581" target="_blank" rel="noopener">Coffee coder 的 liquid用法笔记</a> 和 <a href="https://havee.me/internet/2013-11/jekyll-liquid-designers.html" target="_blank" rel="noopener">Havee’s Space 的 Jekyll 扩展的 Liquid 设计 </a>。<a id="more"></a></p>
<p>　　一下是本人参考以上文档做的小指南，方便以后翻阅。</p>
<hr>
<p>　　在 Liquid 中有两种类型的标记：<code>Output</code>(用于输出文本) 和 <code>Tag</code>(用于执行命令或者处理) 。</p>
<ul>
<li><code>Output</code> 标记被包含在：</li>
</ul>
<figure class="highlight ruby"><table><tr><td class="code"><pre><span class="line">&#123;&#123; 两个配对的花括号中 &#125;&#125;</span><br><span class="line"><span class="string">``</span><span class="string">` </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">+ `</span>Tag<span class="string">` 标记被包含在:</span></span><br><span class="line"><span class="string"> </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>ruby</span><br><span class="line">&#123;% 成对的花括号和百分号中 %&#125;</span><br><span class="line"><span class="string">``</span><span class="string">` </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">## Output</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">### 1. 基本输出</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">　　下面是关于输出标记的简单实例：</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>liquid</span><br><span class="line">Hello &#123;&#123; name &#125;&#125;</span><br><span class="line">Hello &#123;&#123; user.name &#125;&#125;</span><br><span class="line">Hello &#123;&#123; <span class="string">'tobi'</span> &#125;&#125;</span><br><span class="line"><span class="string">``</span><span class="string">` </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">### 2. 高级 Output：过滤器 (Filters)</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">　　输入标记带有过滤器，方法很简单。第一个参数总是过滤器左边值的输出。当下个过滤器运行时，刚刚所得到的过滤器返回值就会成为新的左边值。直到最后没有过滤器时，模板就会接受最后的结果字符串。</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>liquid</span><br><span class="line">Hello &#123;&#123; <span class="string">'tobi'</span> <span class="params">| upcase &#125;&#125;</span></span><br><span class="line"><span class="params">Hello tobi has &#123;&#123; 'tobi' |</span> size &#125;&#125; letters!</span><br><span class="line">Hello &#123;&#123; <span class="string">'tobi'</span> <span class="params">| capitalize &#125;&#125;</span></span><br><span class="line"><span class="params">Hello &#123;&#123; '1984-02-01' |</span> <span class="symbol">date:</span> <span class="string">"%Y"</span> &#125;&#125;</span><br><span class="line"><span class="string">``</span><span class="string">` </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">　　输出结果是：</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>ruby</span><br><span class="line">Hello &#123;&#123; <span class="string">'tobi'</span> <span class="params">| upcase &#125;&#125;</span></span><br><span class="line"><span class="params">Hello tobi has &#123;&#123; 'tobi' |</span> size &#125;&#125; letters!</span><br><span class="line">Hello &#123;&#123; <span class="string">'tobi'</span> <span class="params">| capitalize &#125;&#125;</span></span><br><span class="line"><span class="params">Hello &#123;&#123; '1984-02-01' |</span> <span class="symbol">date:</span> <span class="string">"%Y"</span> &#125;&#125;</span><br><span class="line"><span class="string">``</span><span class="string">` </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">### 3. 标准过滤器</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>liquid</span><br><span class="line">date - 格式化日期</span><br><span class="line">capitalize - 将输入语句的首字母大写</span><br><span class="line">downcase - 将输入字符串转为小写</span><br><span class="line">upcase - 将输入字符串转为大写</span><br><span class="line">first - 得到传递数组的第一个元素</span><br><span class="line">last - 得到传递数组的最后一个元素</span><br><span class="line">join - 将数组中的元素连成一串，中间通过某些字符分隔</span><br><span class="line">sort - 对数组元素进行排序</span><br><span class="line">map - 从一个给定属性中映射/收集一个数组</span><br><span class="line">size - 返回一个数组或字符串的大小</span><br><span class="line">escape - 对一串字符串进行编码</span><br><span class="line">escape_once - 返回一个转义的html版本，而不影响现有的转义文本</span><br><span class="line">strip_html - 去除一串字符串中的所有html标签</span><br><span class="line">strip_newlines - 从字符串中去除所有换行符(\n)</span><br><span class="line">newline_to_br - 将所有的换行符(\n)换成 html 的换行标记</span><br><span class="line">replace - 匹配每一处指定字符串，如 &#123;&#123; <span class="string">'foofoo'</span> <span class="params">| replace:'foo','bar' &#125;&#125; #=&gt; 'barbar'</span></span><br><span class="line"><span class="params">replace_first - 匹配第一处指定的字符串，如 &#123;&#123; 'barbar' |</span> <span class="symbol">replace_first:</span><span class="string">'bar'</span>,<span class="string">'foo'</span> &#125;&#125; <span class="comment">#=&gt; 'foobar'</span></span><br><span class="line">remove - 删除每一处匹配字符串，如 &#123;&#123; <span class="string">'foobarfoobar'</span> <span class="params">| remove:'foo' &#125;&#125; #=&gt; 'barbar'</span></span><br><span class="line"><span class="params">remove_first - 删除第一处匹配的字符串，如 &#123;&#123; 'barbar' |</span> <span class="symbol">remove_first:</span><span class="string">'bar'</span> &#125;&#125; <span class="comment">#=&gt; 'bar'</span></span><br><span class="line">truncate - 将一串字符串截断为x个字符</span><br><span class="line">truncatewords - 将一串字符串截断为x个单词</span><br><span class="line">prepend - 在一串字符串前面加上指定字符串，如 &#123;&#123; <span class="string">'bar'</span> <span class="params">| prepend:'foo' &#125;&#125; #=&gt; 'foobar'</span></span><br><span class="line"><span class="params">append - 在一串字符串后面加上指定字符串，如 &#123;&#123; 'foo' |</span> <span class="symbol">append:</span><span class="string">'bar'</span> &#125;&#125; <span class="comment">#=&gt; 'foobar'</span></span><br><span class="line">minus - 减，如 &#123;&#123; <span class="number">4</span> <span class="params">| minus:2 &#125;&#125; #=&gt; 2</span></span><br><span class="line"><span class="params">plus - 加，如 &#123;&#123; '1' |</span> <span class="symbol">plus:</span><span class="string">'1'</span> &#125;&#125; <span class="comment">#=&gt; '11', &#123;&#123; 1 | plus:1 &#125;&#125; #=&gt; 2</span></span><br><span class="line">times - 乘，如 &#123;&#123; <span class="number">5</span> <span class="params">| times:4 &#125;&#125; #=&gt; 20</span></span><br><span class="line"><span class="params">divided_by - 除，如 &#123;&#123; 10 |</span> <span class="symbol">divided_by:</span><span class="number">2</span> &#125;&#125; <span class="comment">#=&gt; 5</span></span><br><span class="line">split - 将一串字符串根据匹配模式分割成数组，如 &#123;&#123; <span class="string">"a~b"</span> <span class="params">| split:~ &#125;&#125; #=&gt; \['a','b'\]</span></span><br><span class="line"><span class="params">modulo - 余数，如 &#123;&#123; 3 |</span> <span class="symbol">modulo:</span><span class="number">2</span> &#125;&#125; <span class="comment">#=&gt; 1</span></span><br></pre></td></tr></table></figure>

<h2 id="Tags"><a href="#Tags" class="headerlink" title="Tags"></a>Tags</h2><p>　　<code>Tags</code> 用于你的模板逻辑。新的标签很容易开发，因此我希望在发布这些代码后，大家可以为标准标签库增加更多的内容。</p>
<p>　　下列是当前已经支持的标签：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">assign - 将一些值赋给一个变量</span><br><span class="line">capture - 块标记，把一些文本捕捉到一个变量中</span><br><span class="line">case - 块标记，标准的 case 语句</span><br><span class="line">Comments - 块标记，将一块文本作为注释</span><br><span class="line">cycle - Cycle 通常用于循环轮换值，如颜色或 DOM 类。</span><br><span class="line">for - 用于循环 For loop</span><br><span class="line">if - 标准的 if/else 块</span><br><span class="line">include - 包含其他的模板；对于区块化非常有效</span><br><span class="line">raw - 暂时性的禁用的标签的解析</span><br><span class="line">unless - if 语句的简版</span><br></pre></td></tr></table></figure>

<h3 id="1-Comments"><a href="#1-Comments" class="headerlink" title="1. Comments"></a>1. Comments</h3><p>　　注释是最简单的标签，它只是把内容包含起来。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">We made 1 million dollars &#123;% comment %&#125; in losses &#123;% endcomment %&#125; this year</span><br><span class="line">``` </span><br><span class="line"></span><br><span class="line">### 2. Raw</span><br><span class="line"></span><br><span class="line">　　Raw 暂时性的禁用标签的解析。这在需要展示一些可能产生冲突的内容（如本页面，要展示 liquid 语句，就需要包含在 raw 标签间，否则会被解析）时非常有用。</span><br><span class="line"> </span><br><span class="line"></span><br><span class="line"> &#123;% raw %&#125;</span><br><span class="line">```liquid</span><br><span class="line">｛% raw %｝</span><br><span class="line">    We made 1 million dollars &#123;% comment %&#125; in losses &#123;% endcomment %&#125; this year</span><br><span class="line">｛% endraw %｝</span><br></pre></td></tr></table></figure>


<h3 id="3-If-Else"><a href="#3-If-Else" class="headerlink" title="3. If/Else"></a>3. If/Else</h3><p>　　<code>if/else</code> 在其他编程语言里应该已经被熟知了。<code>Liquid</code> 使得你可以通过 if``` 或 <code>unless</code> ( <code>elsif</code> 和 <code>else</code> 为可选 ) 编写简单的表达式:</p>
<pre><code class="liquid">{% if user %}
    Hello {{ user.name }}
{% endif %}

{% if user.name == 'tobi' %}
    Hello tobi
{% elsif user.name == 'bob' %}
    Hello bob
{% endif %}

{% if user.name == 'tobi' or user.name == 'bob' %}
    Hello tobi or bob
{% endif %}

{% if user.name == 'bob' and user.age > 45 %}
    Hello old bob
{% endif %}

{% if user.name != 'tobi' %}
    Hello non-tobi
{% endif %}

# 同上
{% unless user.name == 'tobi' %}
    Hello non-tobi
{% endunless %}

# 检测是否用户有一张信用卡
{% if user.creditcard != null %}
   poor sob
{% endif %}

# 同上
{% if user.creditcard %}
   poor sob
{% endif %}

# Check for an empty array
{% if user.payments == empty %}
   you never paid !
{% endif %}

{% if user.age > 18 %}
   Login here
{% else %}
   Sorry, you are too young
{% endif %}

# array = 1,2,3
{% if array contains 2 %}
   array includes 2
{% endif %}

# string = &apos;hello world&apos;
{% if string contains 'hello' %}
   string includes 'hello'
{% endif %}</code></pre>
<h3 id="4-Case"><a href="#4-Case" class="headerlink" title="4. Case"></a>4. Case</h3><p>　　如果你需要更多的条件判断，你可以使用 <code>case</code> 语句:</p>
<pre><code class="liquid">{% case condition %}
    {% when 1 %}
        hit 1
    {% when 2 or 3 %}
        hit 2 or 3
    {% else %}
        ... else ...
{% endcase %}</code></pre>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="liquid">{% case template %}
    {% when 'label' %}
        // {{ label.title }}
    {% when 'product' %}
        // {{ product.vendor | link_to_vendor }} / {{ product.title }}
    {% else %}
        // {{page_title}}
{% endcase %}</code></pre>
<h3 id="5-Cycle"><a href="#5-Cycle" class="headerlink" title="5. Cycle"></a>5. Cycle</h3><p>　　我们常常需要在不同的颜色或类似的任务间轮流切换。<code>Liquid</code> 对于这样的操作有内置支持，通过使用 cicle``` 标签。</p>
<pre><code class="liquid">{% cycle 'one', 'two', 'three' %}
{% cycle 'one', 'two', 'three' %}
{% cycle 'one', 'two', 'three' %}
{% cycle 'one', 'two', 'three' %}

will result in

one
two
three
one</code></pre>
<p>　　如果一组 <code>cycle</code> 没有命名，那默认情况下有用相同参数的会被认为是一个组。如果你希望完全控制 <code>cycle</code> 组，你可以指定一个组名，这个组名甚至可以是一个变量。</p>
<pre><code class="liquid">{% cycle 'group 1': 'one', 'two', 'three' %}
{% cycle 'group 1': 'one', 'two', 'three' %}
{% cycle 'group 2': 'one', 'two', 'three' %}
{% cycle 'group 2': 'one', 'two', 'three' %}

will result in

one
two
one
two</code></pre>
<h3 id="6-for-循环"><a href="#6-for-循环" class="headerlink" title="6. for 循环"></a>6. for 循环</h3><p>　　<code>Liquid</code> 允许循环一个集合 :</p>
<pre><code class="liquid">{% for item in array %}
    {{ item }}
{% endfor %}</code></pre>
<p>　　在每次循环期间，下列的帮助变量都可用于额外的展示需要:</p>
<pre><code class="coffeescript">forloop.length      <span class="comment"># =&gt; length of the entire for loop</span>
forloop.index       <span class="comment"># =&gt; index of the current iteration</span>
forloop.index0      <span class="comment"># =&gt; index of the current iteration (zero based)</span>
forloop.rindex      <span class="comment"># =&gt; how many items are still left?</span>
forloop.rindex0     <span class="comment"># =&gt; how many items are still left? (zero based)</span>
forloop.first       <span class="comment"># =&gt; is this the first iteration?</span>
forloop.last        <span class="comment"># =&gt; is this the last iteration?</span></code></pre>
<p>　　你可以使用一些属性来影响接受循环中的哪项。</p>
<p>　　<code>limit:int</code> 使你可以限制接受的循环项个数；offset:int 可以可以让你从循环集合的第 n 项开始。</p>
<pre><code class="liquid"># array = \[1,2,3,4,5,6\]
{% for item in array limit:2 offset:2 %}
    {{ item }}
{% endfor %}
# results in 3,4</code></pre>
<p>　　反转循环:</p>
<pre><code class="liquid">{% for item in collection reversed %}
    {{item}}
{% endfor %}</code></pre>
<p>　　除了对一个已经存在的集合进行循环，你还可以定义一段范围区域内的数字进行循环。这段区域既可以通过文字也可以通过变量数定义得到:</p>
<pre><code class="liquid"># if item.quantity is 4...
{% for i in (1..item.quantity) %}
    {{ i }}
{% endfor %}
# results in 1,2,3,4</code></pre>
<h3 id="7-Variable-Assignment-变量赋值"><a href="#7-Variable-Assignment-变量赋值" class="headerlink" title="7. Variable Assignment(变量赋值)"></a>7. Variable Assignment(变量赋值)</h3><p>　　你可以把数据存储在你自己定义的变量中，以便在输出或者其他标签中使用。创建一个变量的最简单方式是使用 assign 标签，其语法也是简单明了的：</p>
<pre><code class="liquid">{% assign name = 'freestyle' %}

{% for t in collections.tags %}
    {% if t == name %}
        <p>Freestyle!</p>
    {% endif %}
{% endfor %}</code></pre>
<p>　　另一种常见用法是把 <code>true/false</code> 值赋给变量:</p>
<pre><code class="liquid">{% assign freestyle = false %}

{% for t in collections.tags %}
    {% if t == 'freestyle' %}
        {% assign freestyle = true %}
    {% endif %}
{% endfor %}

{% if freestyle %}
    <p>Freestyle!</p>
{% endif %}</code></pre>
<p>　　如果你希望把一系列字符串连接为一个字符串，并将其存储到变量中，你可以使用 <code>capture</code> 标签。这个标签是一个块级标签，它会 <code>captures</code> 任何在其中渲染的元素，并把捕获的值赋给给定的变量，而不是把这些值渲染在页面中。</p>
<pre><code class="html"><span class="comment">{% capture attribute_name %}{{ item.title | handleize }}-{{ i }}-color{% endcapture %}</span>

<span class="tag">&lt;<span class="name">label</span> <span class="attr">for</span>=<span class="string">"{{ attribute_name }}"</span>&gt;</span>Color:<span class="tag">&lt;/<span class="name">label</span>&gt;</span>
<span class="tag">&lt;<span class="name">select</span> <span class="attr">name</span>=<span class="string">"attributes[{{ attribute_name }}]"</span> <span class="attr">id</span>=<span class="string">"{{ attribute_name }}"</span>&gt;</span>
  <span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">"red"</span>&gt;</span>Red<span class="tag">&lt;/<span class="name">option</span>&gt;</span>
  <span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">"green"</span>&gt;</span>Green<span class="tag">&lt;/<span class="name">option</span>&gt;</span>
  <span class="tag">&lt;<span class="name">option</span> <span class="attr">value</span>=<span class="string">"blue"</span>&gt;</span>Blue<span class="tag">&lt;/<span class="name">option</span>&gt;</span>
<span class="tag">&lt;/<span class="name">select</span>&gt;</span></code></pre>
<hr>]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>Jekyll</tag>
      </tags>
  </entry>
  <entry>
    <title>Jekyll 插件（持续更新···）</title>
    <url>/2017/01/10/article-jekyll-plug-ins/</url>
    <content><![CDATA[<p>　　博客的基础已经搭建完成，该优化的也优化了，现在本该可以安安静静的写东西了，但谁让我是一个折腾起来停不下来的小菜鸟，看着静悄悄的博客，总感觉缺少的太多的，开始从网上撸各种 jekyll 的插件让自己的页面变得丰富起来。</p>
<p>　　<em>评论功能</em> 和 <em>目录</em> 已经在模板中躺着了，再次感谢 <a href="https://github.com/luoyan35714" target="_blank" rel="noopener">@Freud, Kang</a> 的 <a href="https://github.com/luoyan35714/LessOrMore" target="_blank" rel="noopener">LessOrMore</a>！本文旨在记录一下本博客中使用的 jekyll 插件 ··· ···<a id="more"></a></p>
<hr>
<h2 id="1-站内搜索"><a href="#1-站内搜索" class="headerlink" title="1. 站内搜索"></a>1. 站内搜索</h2><p>　　首先第一个想到的就是得有一个站内搜索的功能，终于被我发现了一个 JavaScript 库，可用于向 Jekyll 博客添加搜索功能。</p>
<p>　　这个 js 库的使用也是非常简单，步骤如下</p>
<ul>
<li><p>首先在自己的项目中引入 <strong><a href="https://github.com/DoubleRayWang/Simple-Jekyll-Search/tree/master/dest" target="_blank" rel="noopener">jekyll-search.js</a></strong> </p>
</li>
<li><p>在项目根目录下新建 search.json，该文件的数据参考 <a href="https://github.com/DoubleRayWang/Simple-Jekyll-Search/blob/master/example/search.json" target="_blank" rel="noopener">这里</a>，如果设置全文搜索可参考 <a href="https://github.com/DoubleRayWang/Simple-Jekyll-Search/blob/master/search.json" target="_blank" rel="noopener">这里</a>；</p>
</li>
<li><p>在相关的 html 中加入如下代码，并自行编写 css；</p>
</li>
</ul>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- Html Elements for Search --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"search-container"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">id</span>=<span class="string">"search-input"</span> <span class="attr">placeholder</span>=<span class="string">"search..."</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">"results-container"</span>&gt;</span><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li>在项目的 js 文件中加入如下配置：</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">SimpleJekyllSearch(&#123;</span><br><span class="line">    searchInput: <span class="built_in">document</span>.getElementById(<span class="string">'search-input'</span>),</span><br><span class="line">    resultsContainer: <span class="built_in">document</span>.getElementById(<span class="string">'results-container'</span>),</span><br><span class="line">    json: <span class="string">'/search.json'</span>,</span><br><span class="line">    searchResultTemplate: <span class="string">'&lt;li&gt;&lt;a href="&#123;url&#125;"&gt;&#123;title&#125;&lt;/a&gt;&lt;/li&gt;'</span>,</span><br><span class="line">    noResultsText: <span class="string">''</span>,</span><br><span class="line">    limit: <span class="number">6</span>,</span><br><span class="line">    fuzzy: <span class="literal">true</span>,</span><br><span class="line">    exclude: [<span class="string">'Welcome'</span>]</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>　　一个简单的搜索框就算完成了，当然要做到完美还差的多，如有任何问题可参看 <a href="https://github.com/DoubleRayWang/Simple-Jekyll-Search/tree/master" target="_blank" rel="noopener">Simple-Jekyll-Search的具体说明</a>。</p>
<h2 id="2-代码高亮"><a href="#2-代码高亮" class="headerlink" title="2. 代码高亮"></a>2. 代码高亮</h2><p>　　代码高亮可以说是程序员必备，无论是搭建模块，或者是产品的说明网站都是需要展示代码的，而代码的样式直接的影响了阅读效果，一个好的代码高亮插件将会有好的体验。</p>
<p>　　网上有很多的代码高亮插件的说明 <a href="http://blog.csdn.net/qiujuer/article/details/50419279" target="_blank" rel="noopener">QiuJuer Tinker</a> 列举了代码高亮的几种选择，而我选择了 Jekyll 可以默认编译的 Rouge 。</p>
<p>　　首先修改　Jekyll　项目的　<strong>_config.yml</strong> 文件，修改其中的 highlighter 为：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">markdown:</span> <span class="string">kramdown</span></span><br><span class="line"><span class="attr">highlighter:</span> <span class="string">rouge</span></span><br></pre></td></tr></table></figure>

<p>　　代码高亮简单的理解其实就是根据 language 编译出特定 class ，然后再通过这些类获得特定的样式，So 那么问题来了，从哪儿得到这个 css 呢？感谢 <strong><a href="http://pygments.org/demo/3666780/" target="_blank" rel="noopener">Pygments 官网的 demo</a></strong> 已经帮我们设计好了一些样式，从 demo 中获取 css 文件的方法一般前端人员都肯定是轻车熟路，右键查看网页源代码， head 中有一个 link 是 pygments.css， 单击这个路径出现这个文件的源码，就可以 Ctrl + A - C - V 一气呵成了。</p>
<p>　　在自己的项目下引用这个 <strong>pygments.css</strong> 文件，然后重启 Jekyll 服务。</p>
<p>　　接下来就是怎么在 Markdown 里面书写了，基本格式就是这样：</p>
<pre><code>{% highlight ruby %}
    def show
    @widget = Widget(params[:id])
    respond_to do |format|
        format.html # show.html.erb
        format.json { render json: @widget }
    end
    end
    #本段代码的作用其实就是ruby编译markdown上的代码生成html
    {% endhighlight %}

-------或者--------

<figure class="highlight ruby"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">show</span></span></span><br><span class="line">    @widget = Widget(params[<span class="symbol">:id</span>])</span><br><span class="line">    respond_to <span class="keyword">do</span> <span class="params">|format|</span></span><br><span class="line">        format.html <span class="comment"># show.html.erb</span></span><br><span class="line">        format.json &#123; render <span class="symbol">json:</span> @widget &#125;</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    <span class="comment">#本段代码的作用其实就是ruby编译markdown上的代码生成html</span></span><br></pre></td></tr></table></figure></code></pre><p>　　编译后的结果就是这个样子滴：</p>
<pre><code>{% highlight ruby %}
    def show
    @widget = Widget(params[:id])
    respond_to do |format|
        format.html # show.html.erb
        format.json { render json: @widget }
    end
    end
    #本段代码的作用其实就是ruby编译markdown上的代码生成html
    {% endhighlight %}</code></pre><p>　　highlight 后的 “ruby”（` 后的 “ruby” ）就是告诉编译器这段代码是什么 language，rouge 大概支持60多种语言，具体<a href="https://github.com/DoubleRayWang/rouge/blob/master/rouge%20support.md" target="_blank" rel="noopener">查阅rouge-support</a>。</p>
<h2 id="3-paginate-分页"><a href="#3-paginate-分页" class="headerlink" title="3. paginate 分页"></a>3. paginate 分页</h2><p>　　首先推荐参考 <a href="http://bamzc.top/2016/11/02/jekyll-paginate#1920" target="_blank" rel="noopener">白召策的博客</a> 比较详细的写了基本的分页功能以及自定义的分页插件。</p>
<p>　　在 Jekyll 3 中，需要在 gems 中安装 jekyll-paginate 插件，并添加到 _config.yml 中，然后在 _config.yml 里边开启分页功能，格式如下：</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">paginate:</span><span class="number">5</span></span><br><span class="line"><span class="attr">paginate_path:</span> <span class="string">"page:num"</span></span><br><span class="line"><span class="attr">gems:</span> <span class="string">[jekyll-paginate]</span></span><br></pre></td></tr></table></figure>

<p>　　博客在 username..github.io 目录下的话，在其根目录的 index.html 下（Jekyll 的分页功能不支持 Jekyll site 中的 Markdown 或 Textile 文件。分页功能从名为 index.html 的 HTML 文件中被调用时，才能工作）添加如下代码：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--遍历分页后的文章--&gt;</span></span><br><span class="line">&#123;% for post in paginator.posts %&#125;</span><br><span class="line">  <span class="tag">&lt;<span class="name">h1</span>&gt;</span><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"&#123;&#123; post.url &#125;&#125;"</span>&gt;</span>&#123;&#123; post.title &#125;&#125;<span class="tag">&lt;/<span class="name">a</span>&gt;</span><span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"author"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"date"</span>&gt;</span>&#123;&#123; post.date &#125;&#125;<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span></span><br><span class="line">    &#123;&#123; post.content &#125;&#125;</span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">&#123;% endfor %&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 分页链接 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"pagination"</span>&gt;</span></span><br><span class="line">  &#123;% if paginator.previous_page %&#125;</span><br><span class="line">    <span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"/page&#123;&#123; paginator.previous_page &#125;&#125;"</span> <span class="attr">class</span>=<span class="string">"previous"</span>&gt;</span>Previous<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">  &#123;% else %&#125;</span><br><span class="line">    <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"previous"</span>&gt;</span>Previous<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  &#123;% endif %&#125;</span><br><span class="line">  <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"page_number "</span>&gt;</span>Page: &#123;&#123; paginator.page &#125;&#125; of &#123;&#123; paginator.total_pages &#125;&#125;<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  &#123;% if paginator.next_page %&#125;</span><br><span class="line">    <span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">"/page&#123;&#123; paginator.next_page &#125;&#125;"</span> <span class="attr">class</span>=<span class="string">"next"</span>&gt;</span>Next<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">  &#123;% else %&#125;</span><br><span class="line">    <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"next "</span>&gt;</span>Next<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">  &#123;% endif %&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>　　个人觉得逻辑更好的分页在 <a href="http://www.bicky.me/blog/archive/my-pagination-on-homepage-base-on-jekyll-pagination-plugin/" target="_blank" rel="noopener">bicky.me</a>，本博客就是参考的这个。</p>
<hr>
]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>Jekyll</tag>
      </tags>
  </entry>
  <entry>
    <title>Markdown 实用技巧</title>
    <url>/2017/01/09/article-markdown-skills/</url>
    <content><![CDATA[<p>　　最开始接触了解 Markdown 是在 github 中写 README.md ，Markdown 的目标是实现「易读易写」，其语法格式最大灵感来源其实是纯文本电子邮件的格式，因此 Markdown 的语法全由一些符号所组成，这些符号经过精挑细选，其作用一目了然。</p>
<p>　　<a href="http://www.appinn.com/markdown/index.html#em" target="_blank" rel="noopener">Markdown 语法说明 (简体中文版)</a> 有对 Markdown 的比较完整的介绍，本文旨在总结一些实用的小技巧。<a id="more"></a></p>
<hr>
<h2 id="1-首行缩进"><a href="#1-首行缩进" class="headerlink" title="1. 首行缩进"></a>1. 首行缩进</h2><p>　　① 在段首加入 <code>&amp;ensp;</code> 来输入一个空格；加入 <code>&amp;emsp;</code> 来输入两个空格 （注：首行缩进为四个空格）；</p>
<p>　　② 将输入法切换到 <code>全角</code> 状态（Shift + Space），按两下空格即可，输完后记得切换回正常输入的 <code>半角</code> 状态（尽量不用）。</p>
<h2 id="2-插入代码"><a href="#2-插入代码" class="headerlink" title="2. 插入代码"></a>2. 插入代码</h2><p>　　① 在每行代码前加入4个空格或者添加一个制表符（Tab键），其缩进是相对于当前格式状态下的，在列表项状态下，需要输入两次TAB键(制表符)才能以代码格式插入。</p>
<p>　　② 在代码两侧添加三个反引号 ‘ <figure class="highlight plain"><figcaption><span>，最好在代码的前后行添加，而不是直接加在代码两边。</span></figcaption><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">## 3. 限制图片大小并居中</span><br><span class="line"></span><br><span class="line">　　MarkDown 中是直接按原图大小显示图片的，让图片大小一致且居中显示可以通过内嵌 html 代码来实现。</span><br><span class="line"></span><br><span class="line">```html</span><br><span class="line">&lt;div align=&quot;center&quot;&gt;</span><br><span class="line">    &lt;img src=&quot;图片地址&quot; width=&quot;宽度&quot; height=&quot;高度&quot; alt=&quot;图片名称&quot;/&gt;</span><br><span class="line">&lt;/div&gt;</span><br></pre></td></tr></table></figure></p>
<p><strong>注意</strong>：</p>
<ul>
<li>HTML 的块元素――比如 <code>&lt;div&gt;、&lt;table&gt;、&lt;pre&gt;、&lt;p&gt;</code> 等标签，必须在前后加上空行与其它内容区隔开，并且开始标签与结尾标签不能用制表符或空格来缩进；</li>
<li>HTML 的行内元素——如 <code>&lt;span&gt;、&lt;cite&gt;、&lt;del&gt;</code> 可以在 Markdown 的段落、列表或是标题里随意使用，<a href="http://blog.csdn.net/html5_/article/details/21639475" target="_blank" rel="noopener">特殊符号</a> 的输入同 html。</li>
</ul>
<h2 id="4-流程图画法"><a href="#4-流程图画法" class="headerlink" title="4. 流程图画法"></a>4. 流程图画法</h2><p>　　在编辑器中插入如下代码，在这之前需引入 <a href="https://cdnjs.com/libraries/flowchart" target="_blank" rel="noopener">flowchart.js</a> ,即可生成流程图，实例可参考 <a href="http://blog.csdn.net/ww1473345713/article/details/47620577" target="_blank" rel="noopener">Z.K.的博客</a>。</p>
<figure class="highlight smalltalk"><table><tr><td class="code"><pre><span class="line">st=&gt;start: <span class="type">Start</span>|past:&gt;http://www.baidu.com</span><br><span class="line">e=&gt;end:  <span class="type">Ende</span>|future:&gt;http://www.baidu.com</span><br><span class="line">op1=&gt;operation:  <span class="type">My</span> <span class="type">Operation</span></span><br><span class="line">op2=&gt;operation:  <span class="type">Stuff</span>|current</span><br><span class="line">sub1=&gt;subroutine:  <span class="type">My</span> <span class="type">Subroutine</span>|invalid</span><br><span class="line">cond=&gt;condition:  <span class="type">Yes</span> or <span class="type">No</span>|approved:&gt;http://www.google.com</span><br><span class="line">c2=&gt;condition:  <span class="type">Good</span> idea|rejected</span><br><span class="line">io=&gt;inputoutput:  catch something...|future</span><br><span class="line">st-&gt;op1(right)-&gt;cond</span><br><span class="line">cond(yes, right)-&gt;c2</span><br><span class="line">cond(no)-&gt;sub1(left)-&gt;op1</span><br><span class="line">c2(yes)-&gt;io-&gt;e</span><br><span class="line">c2(no)-&gt;op2-&gt;e</span><br></pre></td></tr></table></figure>

<img src="/styles/images/jekyll/flow.png" alt="Markdown 流程图">

<hr>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>Markdown</tag>
      </tags>
  </entry>
  <entry>
    <title>常用 git 命令行总结</title>
    <url>/2017/01/09/article-git-skills/</url>
    <content><![CDATA[<blockquote class="blockquote-center"><p>这些基础命令是首次使用 git 配置连接 github 时常用的命令（本人博客搭建过程中主要使用的）。</p>
</blockquote><a id="more"></a>

<pre><code>$ ssh-keygen -t rsa -C youremail@example.com
# 生成SSH Key与github连接用；

$ ssh -T git@github.com
# 通过连接一次到GitHub测试连接成功否；

$ git clone http[s]://example.com/path/to/repo.git/，
$ git clone ssh://example.com/path/to/repo.git/，
# 从github上fork其他人的repo；

$ git init
# 初始化一个版本库（.git的隐藏文件夹）；

$ git status
# 查看状态；

$ git push -u origin master
# 第一次将本地项目push到服务器,再次push时将-u去掉；

$ git commit -m &apos;first commit’
# 提交更改及描述到仓库；

$ git add (u&amp;A&amp;i)[&lt;path&gt;]
# 把我们要提交的文件的信息添加到索引库中;

$ git remote add origin git@github.com:daixu/WebApp.git
# 添加远程仓库;

$ git remote rm origin
# 删除远程仓库；

$ git remote -v
# 列出已经存在的远程分支；</code></pre><br>

<hr>
<p>　　<strong>以下内容参考学习 <a href="http://www.ruanyifeng.com/blog/2015/12/git-cheat-sheet.html" target="_blank" rel="noopener">阮一峰的网络日志</a>，转载请注明出处。</strong></p>
<p>　　一般来说，日常使用只要记住下图6个命令，就可以了。但是熟练使用，恐怕要记住60～100个命令。</p>
<img src="/styles/images/git/git-s.png" alt="git 常用命令">

<p>　　下面是我整理的常用 Git 命令清单。几个专用名词的译名如下。</p>
<pre><code>Workspace：工作区
Index / Stage：暂存区
Repository：仓库区（或本地仓库）
Remote：远程仓库</code></pre><h2 id="git-新建代码库"><a href="#git-新建代码库" class="headerlink" title="git 新建代码库"></a>git 新建代码库</h2><pre><code># 在当前目录新建一个Git代码库
$ git init

# 新建一个目录，将其初始化为Git代码库
$ git init [project-name]

# 下载一个项目和它的整个代码历史
$ git clone [url]</code></pre><h2 id="git-配置"><a href="#git-配置" class="headerlink" title="git 配置"></a>git 配置</h2><p>　　Git的设置文件为 <strong>.gitconfig</strong>，它可以在用户主目录下（全局配置），也可以在项目目录下（项目配置）。</p>
<pre><code># 显示当前的Git配置
$ git config --list

# 编辑Git配置文件
$ git config -e [--global]

# 设置提交代码时的用户信息
$ git config [--global] user.name &quot;[name]&quot;
$ git config [--global] user.email &quot;[email address]&quot;</code></pre><h2 id="git-增加-删除文件"><a href="#git-增加-删除文件" class="headerlink" title="git 增加/删除文件"></a>git 增加/删除文件</h2><pre><code># 添加指定文件到暂存区
$ git add [file1] [file2] ...

# 添加指定目录到暂存区，包括子目录
$ git add [dir]

# 添加当前目录的所有文件到暂存区
$ git add .

# 添加每个变化前，都会要求确认
# 对于同一个文件的多处变化，可以实现分次提交
$ git add -p

# 删除工作区文件，并且将这次删除放入暂存区
$ git rm [file1] [file2] ...

# 停止追踪指定文件，但该文件会保留在工作区
$ git rm --cached [file]

# 改名文件，并且将这个改名放入暂存区
$ git mv [file-original] [file-renamed]</code></pre><h2 id="git-代码提交"><a href="#git-代码提交" class="headerlink" title="git 代码提交"></a>git 代码提交</h2><pre><code># 提交暂存区到仓库区
$ git commit -m [message]

# 提交暂存区的指定文件到仓库区
$ git commit [file1] [file2] ... -m [message]

# 提交工作区自上次commit之后的变化，直接到仓库区
$ git commit -a

# 提交时显示所有diff信息
$ git commit -v

# 使用一次新的commit，替代上一次提交
# 如果代码没有任何新变化，则用来改写上一次commit的提交信息
$ git commit --amend -m [message]

# 重做上一次commit，并包括指定文件的新变化
$ git commit --amend [file1] [file2] ...</code></pre><h2 id="git-分支"><a href="#git-分支" class="headerlink" title="git 分支"></a>git 分支</h2><pre><code># 列出所有本地分支
$ git branch

# 列出所有远程分支
$ git branch -r

# 列出所有本地分支和远程分支
$ git branch -a

# 新建一个分支，但依然停留在当前分支
$ git branch [branch-name]

# 新建一个分支，并切换到该分支
$ git checkout -b [branch]

# 新建一个分支，指向指定commit
$ git branch [branch] [commit]

# 新建一个分支，与指定的远程分支建立追踪关系
$ git branch --track [branch] [remote-branch]

# 切换到指定分支，并更新工作区
$ git checkout [branch-name]

# 切换到上一个分支
$ git checkout -

# 建立追踪关系，在现有分支与指定的远程分支之间
$ git branch --set-upstream [branch] [remote-branch]

# 合并指定分支到当前分支
$ git merge [branch]

# 选择一个commit，合并进当前分支
$ git cherry-pick [commit]

# 删除分支
$ git branch -d [branch-name]

# 删除远程分支
$ git push origin --delete [branch-name]
$ git branch -dr [remote/branch]</code></pre><h2 id="git-标签"><a href="#git-标签" class="headerlink" title="git 标签"></a>git 标签</h2><pre><code># 列出所有tag
$ git tag

# 新建一个tag在当前commit
$ git tag [tag]

# 新建一个tag在指定commit
$ git tag [tag] [commit]

# 删除本地tag
$ git tag -d [tag]

# 删除远程tag
$ git push origin :refs/tags/[tagName]

# 查看tag信息
$ git show [tag]

# 提交指定tag
$ git push [remote] [tag]

# 提交所有tag
$ git push [remote] --tags

# 新建一个分支，指向某个tag
$ git checkout -b [branch] [tag]</code></pre><h2 id="git-查看信息"><a href="#git-查看信息" class="headerlink" title="git 查看信息"></a>git 查看信息</h2><pre><code># 显示有变更的文件
$ git status

# 显示当前分支的版本历史
$ git log

# 显示commit历史，以及每次commit发生变更的文件
$ git log --stat

# 搜索提交历史，根据关键词
$ git log -S [keyword]

# 显示某个commit之后的所有变动，每个commit占据一行
$ git log [tag] HEAD --pretty=format:%s

# 显示某个commit之后的所有变动，其&quot;提交说明&quot;必须符合搜索条件
$ git log [tag] HEAD --grep feature

# 显示某个文件的版本历史，包括文件改名
$ git log --follow [file]
$ git whatchanged [file]

# 显示指定文件相关的每一次diff
$ git log -p [file]

# 显示过去5次提交
$ git log -5 --pretty --oneline

# 显示所有提交过的用户，按提交次数排序
$ git shortlog -sn

# 显示指定文件是什么人在什么时间修改过
$ git blame [file]

# 显示暂存区和工作区的差异
$ git diff

# 显示暂存区和上一个commit的差异
$ git diff --cached [file]

# 显示工作区与当前分支最新commit之间的差异
$ git diff HEAD

# 显示两次提交之间的差异
$ git diff [first-branch]...[second-branch]

# 显示今天你写了多少行代码
$ git diff --shortstat &quot;@{0 day ago}&quot;

# 显示某次提交的元数据和内容变化
$ git show [commit]

# 显示某次提交发生变化的文件
$ git show --name-only [commit]

# 显示某次提交时，某个文件的内容
$ git show [commit]:[filename]

# 显示当前分支的最近几次提交
$ git reflog</code></pre><h2 id="git-远程同步"><a href="#git-远程同步" class="headerlink" title="git 远程同步"></a>git 远程同步</h2><pre><code># 下载远程仓库的所有变动
$ git fetch [remote]

# 显示所有远程仓库
$ git remote -v

# 显示某个远程仓库的信息
$ git remote show [remote]

# 增加一个新的远程仓库，并命名
$ git remote add [shortname] [url]

# 取回远程仓库的变化，并与本地分支合并
$ git pull [remote] [branch]

# 上传本地指定分支到远程仓库
$ git push [remote] [branch]

# 强行推送当前分支到远程仓库，即使有冲突
$ git push [remote] --force

# 推送所有分支到远程仓库
$ git push [remote] --all</code></pre><h2 id="git-撤销"><a href="#git-撤销" class="headerlink" title="git 撤销"></a>git 撤销</h2><pre><code># 恢复暂存区的指定文件到工作区
$ git checkout [file]

# 恢复某个commit的指定文件到暂存区和工作区
$ git checkout [commit] [file]

# 恢复暂存区的所有文件到工作区
$ git checkout .

# 重置暂存区的指定文件，与上一次commit保持一致，但工作区不变
$ git reset [file]

# 重置暂存区与工作区，与上一次commit保持一致
$ git reset --hard

# 重置当前分支的指针为指定commit，同时重置暂存区，但工作区不变
$ git reset [commit]

# 重置当前分支的HEAD为指定commit，同时重置暂存区和工作区，与指定commit一致
$ git reset --hard [commit]

# 重置当前HEAD为指定commit，但保持暂存区和工作区不变
$ git reset --keep [commit]

# 新建一个commit，用来撤销指定commit
# 后者的所有变化都将被前者抵消，并且应用到当前分支
$ git revert [commit]

# 暂时将未提交的变化移除，稍后再移入
$ git stash
$ git stash pop</code></pre><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><pre><code># 生成一个可供发布的压缩包
$ git archive</code></pre><hr>
]]></content>
      <categories>
        <category>干货分享</category>
      </categories>
      <tags>
        <tag>Git</tag>
      </tags>
  </entry>
  <entry>
    <title>Jekyll 博客搭建历程</title>
    <url>/2017/01/08/article-build-blog/</url>
    <content><![CDATA[<p>　　最近赋闲在家，想着自己也搭建一个自己的技术博客，记录分享一些技术点，说干就干，开始着手搭建感觉有点无从下手，是先写个页面？还是先找个例子学习一下？千般辗转，发现 github pages 的学习成本最低，相比其他搭建方式而言，不需要太多的服务器基础，天然基于 git 版本控制，便于对代码进行迁移，而且前一阵一直捣鼓 ruby 的一些东西，所以最终选择了 <strong>github + git + Jekyll</strong> 的搭建方案（ windows7 64bit 环境），那么开始折腾。</p>
<blockquote>
<p>现用 hexo+next 搭建，极大的简化了搭建流程！</p>
</blockquote>
<a id="more"></a>

<p>　　网上已经有很多关于这方面的教程，只是有一些教程已经过时了，比如大家可以参考 <a href="http://www.54tianzhisheng.cn/Blog/html/blog_github.html" target="_blank" rel="noopener">猿blog</a>、<a href="http://www.pchou.info/ssgithubPage/2013-01-03-build-github-blog-page-01.html" target="_blank" rel="noopener">Github建站系列教程</a>、<a href="http://www.jianshu.com/p/3f355c7872d5" target="_blank" rel="noopener">简书</a> 、<a href="http://pwnny.cn/original/2016/06/26/MakeBlog.html" target="_blank" rel="noopener">Pwnny’s Blog</a>、<a href="https://bigballon.github.io/posts/jekyll-github.html" target="_blank" rel="noopener">BIGBALLON的博客 </a>等的教程，这些融合在一起完全可以搭建一个简单的 github 博客，本文主要记录一下自己造的坑。</p>
<p>　　Github Pages 本身就可以作为一个 git 项目，其他人可以 fork 或者参与到你的博客建设中，这本身可以独立也可以互相协作，换而言之，这也是你的 contributions。</p>
<h2 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h2><br>

<ol>
<li>注册一个 Github 账号</li>
<li>安装并简单学习 git 版本控制</li>
<li>学习 Markdown 语法</li>
<li>安装 ruby、gem、jekyll 等</li>
<li>学习一下 jekyll 的基本配置和简单的工作机制流程</li>
<li>购买一个个人域名（ B 格提升专用）</li>
</ol>
<hr>
<h2 id="1-Github"><a href="#1-Github" class="headerlink" title="1.Github"></a>1.Github</h2><p>　　对于Github的注册及使用可以参考<a href="http://m.blog.csdn.net/article/details?id=51336332" title="Github的注册及使用" target="_blank" rel="noopener"> 这里</a>，我就不细表了，主要作一下补充及更新。</p>
<ul>
<li><p>在 setting 页面下的 github pages 里没有了之前的 Launch automatic page generator (启动自动页面生成器) 消失了，当然 github pages 完全可以直接放在 master 下;</p>
</li>
<li><p>同git建立连接时需要的SSH在如下图的位置（当然刚建库的时候也会看到，但当时真的没有注意）</p>
</li>
</ul>
<p>![SSH位置](/styles/images/build blog/SSH.png)</p>
<h2 id="2-git-版本控制"><a href="#2-git-版本控制" class="headerlink" title="2.git 版本控制"></a>2.git 版本控制</h2><ul>
<li><p>git 在 windows 下的安装路劲是任意的，这个不用纠结；</p>
</li>
<li><p>git的基本配置与命令行使用<a href="https://git-scm.com/book/zh/v2" target="_blank" rel="noopener"> Git - Book </a>的讲解的非常清楚，也可以参考我自己总结的<a href="/2017/01/09/git-skills/">常用 git 命令行</a>的总结；</p>
</li>
<li><p>当命令行使用熟练后推荐通过 <strong>webstorm</strong> 进行 git 的操作，使用过程一目了然而且还非常的方便。推荐教程 <a href="http://www.jianshu.com/p/4e2cb06cf3e4" target="_blank" rel="noopener"> 简书</a>、<a href="http://www.cnblogs.com/zhaoxiu/p/5772466.html" target="_blank" rel="noopener">戴眼镜的小女孩</a>。</p>
</li>
</ul>
<h2 id="3-Markdown-语法"><a href="#3-Markdown-语法" class="headerlink" title="3.Markdown 语法"></a>3.Markdown 语法</h2><p>　　Markdown的语法非常之简单，<a href="http://www.appinn.com/markdown/index.html" target="_blank" rel="noopener">Markdown 语法说明 </a>进行了详细的讲解；<a href="/2017/01/09/markdown-skills/">这里 </a> 是本人总结的写 Markdown 文本的一些小技巧。</p>
<h2 id="4-域名配置"><a href="#4-域名配置" class="headerlink" title="4.域名配置"></a>4.域名配置</h2><p>　　这个装X方案起初我内心是拒绝的，但后来转念一想个人博客还是有个人的域名比较好，这个过程还是遇到点的坑，网上关于 CNAME 这个东西给的解释很明确，但是通过我亲自尝试，最终的配置结果如下图</p>
<br>

<h4 id="DNS配置"><a href="#DNS配置" class="headerlink" title="DNS配置"></a>DNS配置</h4><img src="/styles/images/build blog/DNS.png" alt="DNS配置">

<br>

<h4 id="CNAME文件"><a href="#CNAME文件" class="headerlink" title="CNAME文件"></a>CNAME文件</h4><p>![CNAME文件](/styles/images/build blog/CNAME.png)</p>
<p>　　首先说明这个方案是成功的，这里没有设置 IP 的解析是因为亲测 IP 确实不太稳定，如果各位看官有更好的方案，欢迎留言交流讨论！</p>
<p>　　按照我的这个配置不成功的可以看看 <a href="http://blog.csdn.net/qq_29232943/article/details/52786603" target="_blank" rel="noopener">流风雨情的博客</a>。</p>
<h3 id="5-Jekyll-配置"><a href="#5-Jekyll-配置" class="headerlink" title="5.Jekyll 配置"></a>5.Jekyll 配置</h3><p>　　关于 jekyll 的安装与配置在 <a href="http://www.pchou.info/ssgithubPage/2013-01-03-build-github-blog-page-01.html" target="_blank" rel="noopener">Github建站系列教程</a> 讲解的很详细，参照着做就行了，另外推荐 <a href="http://jekyll.com.cn/" target="_blank" rel="noopener">jekyll 介绍与使用</a> 和 <a href="http://www.tuicool.com/articles/qm2Qfi" target="_blank" rel="noopener">Jekyll 语法小节</a> ，可加深对 Jekyll 的理解，并在构建博客的过程中更加得心应手。</p>
<p>　　jekyll也有不太丰富的插件提供，满足基本需求还是够的，不过本人推荐使用一些纯 js 的类插件来达到功能需求（因为github对.rb插件不怎么友好）。</p>
<p>　　关于 RubyGems 被墙的问题，淘宝的 gems 镜像已经不再维护了，可以用 <a href="https://gems.ruby-china.org/" target="_blank" rel="noopener">RubyGems 镜像- Ruby China</a> ，基于国内 CDN + 国外服务器的方式，能确保几乎无延迟的同步。</p>
<br>

<h3 id="6-过程总结"><a href="#6-过程总结" class="headerlink" title="6.过程总结"></a>6.过程总结</h3><p>最后对我自己的搭建过程做一下总结，基本的流程如下：</p>
<ol>
<li><p>先找了一个模板，fork 到自己的 github；</p>
</li>
<li><p>将项目名称改为 github 的用户名（ <em>usename.github.io</em> ），这样就可以直接通过 <em><a href="https://usename.github.io/" target="_blank" rel="noopener">https://usename.github.io/</a></em> 来访问 master 的页面（即博客内容）；</p>
</li>
<li><p>安装 git，在项目（ blog ）目录右键 Git Bash Here，进行 git 的基本设置（设置用户名，邮箱，生成 SSH Key 与 github 连接，设置远程仓库连接，init 本地仓库等）；</p>
</li>
<li><p>通过 <em>git pull origin master</em> 将 github 的内容pull到本地目录；</p>
</li>
<li><p>先通过 <em>git add -A</em> 将目录中的文件 tracked，然后进行 change；</p>
<p>1）如果没有自己的域名，目录中不能出现 CNAME 文件；</p>
<p>2）改 _config.yml 时 markdown: kramdown ， 如果项目名称为（usename.github.io）则 baseurl = “ “ ；</p>
</li>
<li><p>Change 完了之后通过 <em>git status</em> 查看当前的状态，检查一下；</p>
</li>
<li><p>通过 <em>git commit -m ‘first commit’</em>  将更改的东西添加到暂存区（再次提交去掉-m即可）；</p>
</li>
<li><p>最后在通过 <em>git push origin master</em> 将更改过的文件信息 push 到 github 上；</p>
</li>
</ol>
<p>　　在这之前可以在本地通过 jekyll 先查看检查一下，配置好 jekyll 之后在 git 的当前目录下输入 <em>jekyll build</em> ，然后 <em>jekyll serve</em> ，那么现在就可以通过 localhost：4000 来访问到更改后的本地页面了。</p>
<p>　　<strong>OK 到了这里，就可以踏踏实实的来专注自己的博文了。</strong></p>
<h3 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h3><p>　　本文大部分的技术点都在相关的推荐文档里，因为我可能和正在看本文的您一样也是第一次搭建 github 的博客，所以大部分知识都是从前辈总结的经验和官方的文档中得来的，只是自己通过实践来发现更适合自己的方法方式。</p>
<hr>]]></content>
      <categories>
        <category>实践之路</category>
      </categories>
      <tags>
        <tag>Jekyll</tag>
        <tag>Github</tag>
        <tag>Markdown</tag>
      </tags>
  </entry>
</search>
